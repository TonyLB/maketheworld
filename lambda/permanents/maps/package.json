{
  "name": "maps",
  "version": "1.0.0",
  "description": "Lambda proxy for getting maps from and putting to the permanent tables (AppSync resolver function)",
  "main": "src/app.js",
  "author": "Tony Lower-Basch",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.434.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
