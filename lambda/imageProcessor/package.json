{
  "name": "imageProcessor",
  "version": "1.0.0",
  "description": "Lambda for validating and processing uploaded images",
  "main": "app.ts",
  "scripts": {
    "test": "jest",
    "build:dev": "esbuild app.ts --bundle --platform=node --target=es2020,node18 --external:@aws-sdk --minify --outfile=build/lambda.js --sourcemap",
    "build": "esbuild app.ts --bundle --platform=node --target=es2020,node18 --external:@aws-sdk --minify --outfile=build/lambda.js"
  },
  "author": "Tony Lower-Basch",
  "license": "MIT",
  "dependencies": {
    "aws-xray-sdk": "^3.5.1",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-sns": "^3.0.0",
    "@aws-sdk/s3-request-presigner": "^3.0.0",
    "jimp": "^0.22.10"
  },
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "@types/node": "^18.0.0",
    "jest": "^29.6.4",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  }
}
