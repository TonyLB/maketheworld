# Examples cache (retired)

**Status:** **`ExamplesData`** and **[`examples.ts`](./examples.ts)** were removed (2026-05-19).

Feature and Knowledge display prose is served only through **`renderCache`** and **`ComponentRender`** (see **[`componentRender.AGENT.md`](./componentRender.AGENT.md)**). Steady-state Assets push is **`ExampleInvalidated`** on **`mtw.assets.componentExamples`** (no **`example`** body); payloads use **`situationId`** or **`componentIds`**, not **`EXAMPLE#`**. See [`renderCache/AGENT.md`](../dataSource/renderCache/AGENT.md).
