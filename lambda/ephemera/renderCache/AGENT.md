# Ephemera render cache (redirect)

**Canonical documentation** for the render cache (Dynamo schema, `internalCache.RenderCache`, persistence primitives, DataSource pass-through, and orchestration interaction) lives in **[`../dataSource/renderCache/AGENT.md`](../dataSource/renderCache/AGENT.md)** (`mtw.ephemera.renderCache`).

This file remains so existing links to `lambda/ephemera/renderCache/AGENT.md` resolve during deprecation of this directory.
