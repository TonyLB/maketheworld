import type { RenderTree } from '@tonylb/mtw-base/ts/renderTree'

/** Stub plan outcome body; replace with real generation later. */
export async function generatePlanOutcome(): Promise<RenderTree> {
    return ['Outcome: Stubbed']
}
