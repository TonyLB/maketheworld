import type { RenderTreeNode } from '@tonylb/mtw-base/ts/renderTree'

/** Single paragraph break for Coyote WorldMessage / harness formatting. */
export const COYOTE_RENDER_LINE_BREAK: RenderTreeNode = { data: { tag: 'br' }, children: [] }
