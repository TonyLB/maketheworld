/**
 * Top-level strictness toggle across Coyote Game LLM pipelines/parsers.
 *
 * Set false for demo-friendly tolerance where non-authoritative validation should
 * avoid interrupting the player flow.
 */
export const COYOTE_STRICT_FAIL_FAST_ENABLED = false
