/**
 * Read-only gateway for `Meta::Import::...` rows (shared with other lambdas via `@tonylb/mtw-gateways`).
 * Authoritative writer: this folder's DataSource (`mtw.assets.components.verticals`).
 */
export * from '@tonylb/mtw-gateways/ts/assets/components/verticals'
