{
  "name": "mtw-packages",
  "workspaces": [
    "packages/*",
    "charcoal-client"
  ],
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "tsc --build --verbose",
    "watch": "tsc --build --watch"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
