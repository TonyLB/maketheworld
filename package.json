{
  "name": "mtw-packages",
  "workspaces": [
    "mtw-utilities",
    "lambda/assets"
  ],
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "tsc --build --verbose",
    "watch": "tsc --build --watch"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
