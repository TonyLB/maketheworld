{
  "name": "mtw-packages",
  "workspaces": [
    "packages/*",
    "charcoal-client",
    "lambda/ephemera"
  ],
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "tsc --build --verbose",
    "watch": "tsc --build --watch"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
