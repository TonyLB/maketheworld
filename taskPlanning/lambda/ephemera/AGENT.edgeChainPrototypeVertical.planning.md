# Edge-chain prototype vertical (PV-1)

**Status:** In progress. PV1-2 and PV1-3 are both done at the mechanism/unit-test level; both slices' own top-level checkboxes stay open until PV1-4 observes them live. **Live-tested 2026-09-01: `tie string to cup` still fails** — the live ingress route isn't wired to PV1-3's crossing mechanism, stubbed as **PV1-3b** (see Progress, Open decisions, Recommended order). **PV1-3b-4 shipped 2026-09-01**: `satisfied` is deleted end to end and the ingress seed collapsed to one instruction. **PV1-3b-5 shipped 2026-09-02**: the live route's pre-fetch is now a depth-capped (5) async ancestry walk, so `findShardBoundary` can reach a common ancestor past an intermediate host, not just an already-shared one — `tie string to cup` still doesn't land live, though, since this route only carries a single leg and drops a port-address endpoint. **PV1-3b-7 shipped 2026-09-02**: relational steps now carry a mandatory `hostId`. **PV1-3b-1 shipped 2026-09-02**: `ParseCommandEstablishRelationResult` now carries the full Expansion-derived step chain and the live ingress route no longer drops crossing candidates — `tie string to cup` still doesn't land live, though, since the consumer/commit path (PV1-3b-2/3) don't yet understand a multi-step result and a crossing attempt now throws a commit-time `Error` instead. **Next build step: PV1-3b-3** (`executeEstablishEdgeChain`), then PV1-3b-2 (widen the published payload). PV1-4's readout is blocked on PV1-3b, not just owed a live run. **PV1-3b-12 shipped 2026-09-02** on the interleaved DissolveRelation track: the discovery primitive (`findRelationalChain.ts`) walking existing relational edges/ports to find a prior `tie`'s chain — standalone, not yet wired into `expandSameHost.ts` (PV1-3b-14) or consumed for removal steps (PV1-3b-13).

**This is an implementation plan.** It follows [`taskPlanning/AGENT.md`](../../AGENT.md) and **not** [`AGENT.designVariant.md`](../../AGENT.designVariant.md): code here is licensed by shipping, not by a tier; resolved decision rows are removed, not graduated; Progress is a running log, not a phase-grain view.

Area commands: [`taskPlanning/charcoal-client/AGENT.development.md`](../../charcoal-client/AGENT.development.md) for the client (Vitest, `npm run test:single`).

## Why this initiative exists

Three prototypes are shipped and inert: port machinery with no producer, `edgeId` (EA-8) minted by nothing, and `chainId` (P8 iteration 1) written by nothing. Each needs an observation that is only reachable once the other two are live. P8's rollback trigger (*a write path must decide same chain or not*) cannot fire, since no write path can express chain membership at all.

This vertical is the smallest configuration that makes all three observable at once. It is not a design initiative and settles no design question; it produces one readout (below).

## Mechanism this vertical builds on

[AB-54](dataSource/positions/AGENT.abstractionLayers.planning.md#settled-register) is settled: hosting kinds (`On`, `In`, `PartOf`) put the subordinate node in the superior's own shard (a cup `On` a tray is a member of the tray's `ludicGraph`, not the room's); peer kinds (`Under`, `Against`, `Custom`) leave both endpoints in one graph and host nothing.

Consequences that shape the build order:

1. Putting the cup on the tray requires the tray to be a host with its own persisted `ludicGraph` — [CD2h](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order). `In`/`PartOf` are deferred; one hosting kind is enough to make a boundary.
2. [`roomObjectCatalogForCharacter.ts`](../../../lambda/ephemera/dataSource/actions/roomObjectCatalogForCharacter.ts) enumerates the room's own `objectIds` only, so a hosted cup becomes unnameable unless referent search is extended to walk into hosted objects' graphs.
3. A `Custom` relation across that boundary ("tie the rope to the cup", rope in the room, cup in the tray's shard) cannot be one edge — it needs a port and produces two legs (room-side, tray-side). That is the first multi-leg chain the system will hold.

**Build order is 1→2 inverted:** [CD2h](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order) is gated on [CC3](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order) because a hosted cup vanishes from flat-catalog referent search with no guard to fire. Referent search (PV1-1) goes before hosting (PV1-2), built against hand-built nested fixtures — the graph already round-trips hosting shape (CD1 found `In`/`PartOf` persistence-tested but inert).

## What already exists to build on

- **Hosting, partly.** `On` is unauthorable today: [Channel D](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order) exists to make `On` root-anchored per AB-54; CD0/CD1/CD2 have landed, and CD2 retired `On` at the ingress lane ([`normalizeRelationSpan.ts:10`](../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/normalizeRelationSpan.ts) defers `on`/`onto`/`on top of`). The shard-hosting mechanism (CD2h) is unbuilt and gated on CC3, which PV1-1 discharges.
- **The move operation**, extended by PV1-2. [`executeObjectMove.ts`](../../../lambda/ephemera/dataSource/positions/manipulation/membership/executeObjectMove.ts) is the single call site for every object membership move, takes hosts rather than a verb (take-hold `room -> character`, drop `character -> room`, give `character -> character`), and re-derives the carry closure and boundary sweep fresh at commit time.
- **The step sequence a compiler can emit.** [`applyObjectRelationalChange.ts`](../../../lambda/ephemera/dataSource/positions/manipulation/relational/applyObjectRelationalChange.ts) commits `[dissolveRelation*, transferMembership, establishRelation]` through `commitStepSequence` as a repair path (see PV1-2 for why that is distinct from the hosting model).
- **Port shape, with no producer.** Types, terminal union, and matcher shipped when the ports plan closed 2026-08-23; nothing calls `withPorts` outside tests. `EphemeraPresencePort` gets its producer at PV1-2, `EphemeraCrossingPort` at PV1-3.
- **`chainId`, carried and compared, written by nothing.** The field round-trips through storage and `edgesMatch` consults it; the mutation vocabulary in [`manipulation/types.ts`](../../../lambda/ephemera/dataSource/positions/manipulation/types.ts) cannot express it.
- **The commit path already mixes item kinds.** `transactWrite([multiKeyItem, ...adjacencyItems])` — sibling records in one transaction is an established shape.

## The readout

Specified before the code (PV1-0), since PV-1's value is that it can produce an observation.

**The run**, taken by hand in a running client with the Dynamo console open, at PV1-4:

1. Order a table, a cup, and a string from Acme.
2. `put cup on table` (PV1-2).
3. `tie string to cup` (PV1-3) — the first chain the system will have held. This step is also PV1-1's test: if referent search cannot find the cup ("you don't see a cup"), PV1-1 broke, not PV1-3.
4. Read the four records below.

**Records to read.** No `EDGE#` item — PV-1 mints no Edge record, so both ids ride as optional fields on the legs themselves.

| Record | Where | What should be in it |
| --- | --- | --- |
| Room | `Meta::Room` on VORTEX, `ludicGraph` | table and string as nodes; the room-side leg, `string -> port` |
| Table | `Meta::Object` on the table, `ludicGraph` | the cup as a member node; its containment edge `cup -> root`, kind `On`; the crossing-port record (`fromHostId: ROOM#VORTEX`, stored interior-side); the table-side leg, `port -> cup` |
| Cup | `Meta::Object` on the cup, `ludicGraph` | its own root node (concepts clause 3), and one presence port — `{ fromHostId: OBJECT#<table>, kind: 'Present' }` |
| Adjacency | `EphemeraId: OBJECT#<cup>` | `DataCategory: POSITION#OBJECT#<table>` |

**Clauses to check against the run:**

- **Chains work** if both legs carry the same `chainId` and the same `edgeId`, and the string and the cup are both still nameable. Compare the two legs' fields directly — a leg count alone can read as success while the `chainId`s differ.
- **Chain identity is the wrong model** if PV1-3 cannot set the second leg's `chainId` without a fact no caller possesses, or if the ids have to be reconciled at read time rather than carried. Record this the moment it happens during construction, not only if it shows up in the dump.
- **The presence port earns its place** if the cup's presence port answers something the adjacency row does not — read the two rows side by side. If they say the same thing in two places, that is evidence for [AB-55](dataSource/positions/AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) (*does the presence port subsume adjacency?*) and grounds for taking the port back out.

**What this run cannot observe** — name these at PV1-4 rather than letting the silence read as coverage:

- The find path: PV-1 mints first chains only. A second route over an existing relationship needs an Edge record to find, and there is none.
- A relation that predates its boundary: optional second pass, tie the string first, then put the cup on the table (PV1-2's boundary sweep will most likely untie it — run it anyway and record which way it went).
- **Chain/edge identity, at all** (scope cut during PV1-3, confirmed with the user, not an oversight): PV1-3's legs carry no `chainId`/`edgeId` — building the general N-leg vocabulary for identity was judged not necessary for a first iteration, which can be inline/unidentified. The "Chains work"/"Chain identity is the wrong model" clauses above are therefore **not observable this vertical** — a later slice that actually mints `chainId`/`edgeId` on crossing legs would need to pick this up, against a settled readout run rather than reopening this one.

**Also not built, deliberately, this slice:** PV1-3's crossing-port producer handles at most one extra hop per side (chains where both `subjectPath`/`targetPath` length ≤ 2, and not both extra-hop at once) — a middle leg between two port addresses, needed once one side is nested two-deep, would hit `applyStepSequenceCore`'s BD-33 derive-don't-carry host resolution, which cannot resolve a leg with no primitive endpoint. Reports `notYetImplemented` rather than emitting a step that would throw at apply time. The live ingress route (`compileRelationalFromSkeleton.ts`) is not wired to this mechanism at all yet — its own one-hop pre-fetch and its seed's redundant sibling relational step (which would double-emit an invalid direct edge alongside the crossing legs) both need addressing first; see that file's own comment at the seed-construction site.

## Progress

| Date | Note |
| --- | --- |
| 2026-08-30 | Seeded from conversation. Nothing built. |
| 2026-08-31 | Design settled ahead of code: hosting is a rehost carrying a containment argument, not a relation with a side effect; presence ports are single (PR-10 collapses the edge set, not the port record) and crossing-only for CD6; referent search (PV1-1) moves ahead of hosting (PV1-2) per the CC3 gate; room-only membership entry points are not required by any live rule and join the same instruction/compiler/kernel pipeline as play-level, with the scale-specific mutators deleted rather than generalized (PV1-1b). PV1-0 done: table/cup/string manual run, four records specified — see [The readout](#the-readout). |
| 2026-08-31 | PV1-1 done: `roomObjectCatalogForCharacter.ts` walks into hosted objects' own `ludicGraph`s (recursive `getLudicGraph`, BFS, depth cap 5, visited set for cycles). Held-inventory catalog left flat, deliberately — out of scope for this vertical. Full suite and typecheck green. |
| 2026-08-31 | PV1-1b done: `executeMembershipTransfer` (new function in `executeObjectMove.ts`) absorbed the three scale-specific mutators' membership half; those three plus `manipulation/adapters/` deleted outright; five call sites migrated; docs updated. Full suite (343/344, 2699 tests) and typecheck green. |
| 2026-08-31 | PV1-2 done, mechanism and client wiring both. `containment` threaded through the move-op pipeline; `compilePositionKernelOp` emits the containment edge after transfer; presence-port minting got its own kernel step (`MutationKernelSetPresencePortStep`), since the port lives on a third graph outside the transfer's footprint. Client side: `classifySkeletonFamily`/`parseCommand.ts` route `On` to a dedicated `compileObjectRehostFromSkeleton.ts` (Identity only, skipping Grounding/Expansion/Validation, which solve peer-relation problems that don't apply to a rehost), carried across the publish/subscribe boundary by a new `Object Rehost` event trio; `positions/index.ts` re-resolves the object's current host fresh at execute time rather than trusting the parse-time value. Two pre-existing bugs found and fixed: `applyStepSequenceCore`'s host resolution used first-match-per-endpoint semantics, mis-resolving a host that's locked via two footprint graphs at once (replaced with intersection-based `findSharedHost`/`hostsOf`); and `orchestrateObjectMove` derived `roomId` by scanning `[fromHostId, toHostId]`, silently no-opping on every containment move (fixed by taking `roomId` explicitly). **Confirmed live in the running client** (`put cup on table`, `get cup`, `drop`) — both the containment edge and the presence port appear and disappear correctly. Full suite (344/345, 2743 tests) and typecheck green. |
| 2026-09-01 | PV1-3 done: general shard-boundary walk and crossing-leg producer. `findShardBoundary.ts` walks `getMembershipContainers` recursively from both endpoints (a host can itself be multiply-contained, so ancestry is a DAG, not a chain) and takes the Pareto-minimal frontier of common ancestors under (subjectDepth, targetDepth) domination — zero (`notFound`), one (`crossed`), or more than one (`ambiguous`, not-yet-implemented). `buildCrossingLegs.ts` turns a `crossed` result into legs plus one `EphemeraCrossingPort`, scoped to ≤1 extra hop per side (a genuine `applyStepSequenceCore` host-resolution limit, reported as `notYetImplemented` beyond that rather than emitting a step that would throw at apply time). Legs needed no new kernel step kind — `establishRelation`/`dissolveRelation` were already leg-shaped once their endpoints widened to `EphemeraLudicTerminalId` (LP7); only the port record itself needed one, wired into `expandSameHost`'s new `'crossed'` verdict via a side-channel (`extraKernelSteps`) since kernel-only steps can't ride the executor's typed worklist. No `chainId`/`edgeId` minting this slice (confirmed with the user) — legs stay inline/unidentified; PV1-0's readout gained a "cannot observe" line for it. The verb-lexicon fix ("tie") turned out to be server-side (`matchRelationalTemplate.ts`/`relationalTemplates.ts`), not client-side as originally scoped. Two gaps named, not fixed: `applyObjectRelationalChange.ts`'s port-qualified-edge skip (never routed through by a no-transfer relation like `tie`), and the live ingress route (`compileRelationalFromSkeleton.ts`, one-hop-only pre-fetch, seed still double-emits alongside a real crossing). Full suite (346/347, 2770 tests) and typecheck green. |
| 2026-09-01 | Live-tested `tie string to cup`: fails ("I couldn't understand that command"). Root cause: `compileRelationalFromSkeleton.ts` drops the candidate before `expandSameHost` can reach `'crossed'`, and even if it did, the downstream commit path (`ParseCommandEstablishRelationResult` → publish/subscribe → `executeObjectEstablishRelation` → `applyObjectRelationalChange`) is shaped for exactly one relation on exactly one host. Stubbed as **PV1-3b**, with six open design questions — not designed, not built yet; the user asked to refine it together before building. |
| 2026-09-01 | PV1-3b-7 decided, prompted by the user questioning whether a relation's host can always be derived from its endpoints alone: two counterexamples are real (endpoints multi-hosted in ≥2 shared graphs; port-to-port on one object, where interior-vs-exterior scope isn't recoverable from the two port addresses). Expansion already computes a resolved host (`expandSameHost`'s verdict, `buildCrossingLegs`'s per-leg placement) and discards it — the kernel step types don't carry it, so `applyStepSequenceCore` re-derives it via `findSharedHost`, which is exactly what breaks on the counterexamples. **Decided:** carry a mandatory `hostId` on relational steps, computed once at Expansion and surfaced; narrow `findSharedHost` from resolver to assertion (still throws — BD-33 preserved, only the computation moves). The general multi-boundary "which of several valid routes" question (the `ambiguous` verdict) stays explicitly out of scope, deferred as a later, likely LLM-reasoned problem. |
| 2026-09-01 | PV1-3b-1/2/3 decided. **PV1-3b-1:** widen `ParseCommandEstablishRelationResult` itself (not a sibling type) to carry the Expansion-derived step list — `compileRelationalFromSkeleton.ts` already holds it in scope. **PV1-3b-2:** widen `Object Establish Relation`'s existing payload/envelope, not a new event pair. **PV1-3b-3:** leg-time validation is sufficient on its own — PV1-3b-7's per-step assert at commit time already re-checks every leg, so no separate whole-crossing verification layer is needed; the new commit function is a pass-through. Settled details: merge order is crossing-port steps first, then legs (legs are independent of each other but each needs its port already placed); it's a new sibling function, `executeEstablishEdgeChain` (matching `executeMembershipTransfer`'s precedent), not a branch inside `executeObjectEstablishRelation`; that function's existing silent-swallow bug is left unfixed, since it may itself be a retirement candidate once crossing is lifted out. Separately, confirmed (not scheduled): `Under`/`Against` are meant to get crossing too — the repair-by-relocation path is an artifact of crossing being `Custom`-gated, not a real requirement of those kinds, so it's expected to retire once they get the same crossing path. |
| 2026-09-01 | **PV1-3b-8 shipped.** `findShardBoundary`'s ancestry walk now seeds each endpoint as its own zero-hop ancestor (plus a `visited` guard against containment cycles clobbering that seed), so `tie the cup to the table it sits on` resolves to a plain edge in the table's own graph instead of a pointless crossing port through the room. `buildCrossingLegs`'s zero-length-path guard relaxed to match. Verified the lock-set invariant this depends on structurally, not just against fixtures: an endpoint returned as `commonAncestor` is by construction the other endpoint's container, so it's always in the footprint lock set. New durable doc: the lock-set invariant, in `manipulation/AGENT.implementation.md`'s Footprint derivation. Full suite green (346 suites, 2766 tests). |
| 2026-09-01 | **PV1-3b-9 shipped:** crossing generalized from `Custom` to all peer kinds (`Under`/`Against`/`Custom`); hosting kinds get a named CD2h `error` instead of entering the gate (AB-54 forbids treating them as a boundary at all). `repaired` — now provably empty of live domain — retired **all the way to the wire, at the user's direction**: the verdict, its executor/compiler cases, and `transferFromHostId` across seven files deleted outright, verified first that `ParseCommandEstablishRelationResult` has exactly one producer and one consumer. Two durable docs corrected (`manipulation/AGENT.implementation.md`'s "Repair transfers" paragraph, `objectManipulation/AGENT.md`). Suite 346/2762, net −6/+4 tests. |
| 2026-09-01 | **PV1-3b-10 shipped:** `negate` dropped from the `sameHost` path (`SameHostAssertion`, both seed sites, `expandSameHost`'s input) — categorical, not just inert: `negate` is BD-14's flag for *assertions*, and PV1-3b-4 turns `sameHost` into a placement-resolver, which has no meaningful inverse. `containedBy` keeps its own `negate` untouched. No behaviour changed (nothing ever set the flag). `GroundedBinaryAssertion` still unions both predicates, so this only demotes the field to `containedBy`-only for now — full simplification waits on PV1-3b-4 removing `sameHost` from that union. One durable doc corrected: `actions/AGENT.concepts.md`'s Change-vs-Assertion paragraph, now per-predicate. Suite 346/2760 (net −2, the deleted negate-only cases). |
| 2026-09-01 | **PV1-3b-4 decided end to end**, and **PV1-3b-6 shipped, reordered ahead of it.** `satisfied` is deleted rather than widened — it's provably a second implementation of `buildCrossingLegs`'s degenerate single-leg case, not a shortcut past it (confirmed once PV1-3b-8 closed the one shape it used to get right that the general path didn't). Adjacency-vs-graph is a deliberate split: adjacency only proposes a route, every leg is re-validated graph-side at commit under one `transactWrite`, so misalignment is fail-closed (a failed validation, never a bad write) — left to PV1-4 that the failure surfaces as a thrown `Error`, not a graceful decline. Deleting `satisfied` spills two non-peer cases it used to absorb: hosting kinds (fixture-only, resolves to PV1-3b-9's CD2h error) and label-less `Custom` — the latter turned out live and bigger than expected, since the seed never set `relationLabel` at all, so `Custom` could never reach the crossing gate. That got pulled forward as **PV1-3b-6, shipped the same day**: the seed now threads the label (via the shared `relationKindAndLabelFrom` helper), and a label-less `Custom` errors explicitly at the top of `expandSameHost`, above every state lookup — input validation, not a routing decision, so it survives PV1-3b-4's deletion of everything below it. One durable doc corrected in passing (`synthesize/AGENT.implementation.md`'s module map). PV1-3b-5 is now the only open design question left in PV1-3b. Suite 346/2771, typecheck clean. |
| 2026-09-01 | **PV1-3b-4 shipped.** `satisfied` deleted end to end: `expandSameHost` lost the branch, its `getCurrentHost`/`getGraph` params, and the `satisfied` union member; `executor.ts`'s `commandExpand` dropped the now-impossible arm; `compileRelationalFromSkeleton.ts`'s seed collapsed to the single `sameHost` instruction (the sibling `relationalStepNoHost` and its construction are gone). `GroundedBinaryAssertion` narrowed to `containedBy`-only (`negate` required again); `sameHost` got its own sibling type, `GroundedSameHostAssertion`, in `GroundedAssertion`'s union rather than vanishing — it is very much still live, just no longer fused with `containedBy`. Removed `sameHost` from the *ungrounded* side too, confirmed dead scaffold: `SameHostAssertion`/`Assertion`'s member deleted (`ungroundedPrimitive.ts`), `compileRelationalUngroundedPlan`'s prefixing removed (`compileUngroundedPlan.ts`, zero live callers besides its own test), `groundAssertion.ts`'s fused case un-fused to `containedBy`-only. **Two regressions found and fixed during verification, neither anticipated by the row's original text:** (1) the live ingress route never surfaced its own one-hop `getMembershipContainers` pre-fetch to `findShardBoundary` at all (`createExpansionEnvironment` was called with no third argument) — deleting `satisfied` without fixing this would have made every already-shares-a-host candidate resolve `notFound` and silently Abstain; fixed by keeping the full container array (not just the single-winner case) alongside the existing `hostByObjectId` map and wiring it through — zero extra I/O, and distinct from PV1-3b-5's still-open deeper-walk gap, since this is only the one hop the route already fetches. (2) `buildCrossingLegs` was hardcoded to emit `establishRelation` regardless of the candidate's actual `kind` — harmless while a sibling `relationalStepNoHost` (carrying the real kind) always retired alongside it, but silently wrong once that sibling was gone: a same-host `dissolveRelation` candidate (e.g. `take rope off crate`) started emitting an `establishRelation` step instead. Fixed by threading `operationKind` through `GroundedSameHostAssertion` → `expandSameHost` → `buildCrossingLegs`, which now also guards dissolve against a real (ported) crossing as `notYetImplemented` (removing a minted port on dissolve is itself unbuilt, consistent with the row's existing establish-only crossing scope). Test rewrites across the whole `objectManipulation` tree (not just already-touched files, confirmed by grep): `expandSameHost.test.ts`, `executor.test.ts`, `groundAssertion.test.ts`, `compileUngroundedPlan.test.ts`, `ungroundedPrimitive.test.ts`, `index.test.ts`, plus `buildCrossingLegs.test.ts` for the new `operationKind` parameter (two new tests added there for the dissolve cases). `AGENT.md`'s "12b. Expansion" paragraph corrected (was already stale pre-dating this slice, still describing the retired `satisfied`/`repaired` pair). Full suite green (346/347 suites — the one skip is pre-existing; 2758/2767 tests, 9 pre-existing skips) and full typecheck (ephemera, mtw-interfaces, mtw-gateways) clean. |
| 2026-09-02 | **PV1-3b-5 shipped.** The live route's pre-fetch (`compileRelationalFromSkeleton.ts`) is now an eager, depth-capped (5) async walk of each distinct candidate's full containment ancestry, not a one-hop fetch. New `walkAncestryContainers` (`synthesize/findShardBoundary.ts`), async-ified against the real `getMembershipContainers` gateway, mirrors `walkAncestry`'s own frontier/visited-set shape but returns a flat `containerId -> its own direct containers` map; one walk runs per distinct object id (`Promise.all` — safe with no extra memoization, confirmed against `PositionsCacheHandler`'s existing per-invocation `DeferredCache`), merged into one map that backs both `getCurrentHostForExpansion` (single-hop, unchanged semantics) and `getMembershipContainersForExpansion` (now full-depth). Effect: `findShardBoundary` can now reach a common ancestor past an intermediate host on this route, where it previously dead-ended at `notFound`. **Confirmed this alone does not make `tie string to cup` live**: a genuine crossing's near-side leg has a port-address endpoint, and the route still only takes the *first* `establishRelation`/`dissolveRelation` step off the outcome and only accepts a plain `EphemeraObjectId` there — that narrowing is PV1-3b-1/2/3/7's job. Same-host cases are unaffected (still resolve live as before). One durable doc corrected: `objectManipulation/AGENT.md`'s "12b. Expansion" paragraph, "Live reach on this route today" sentence. Full suite green (346/347 suites, one pre-existing skip; 2763/2772 tests) and full typecheck (ephemera, mtw-interfaces, mtw-gateways) clean. |
| 2026-09-02 | **PV1-3b-7 shipped.** `hostId: EphemeraMembershipHostId` is now mandatory on `ExecutorEstablishRelationStep`/`ExecutorDissolveRelationStep` (`executorTypes.ts`), reused verbatim by `MutationKernelStep`. `buildCrossingLegs.ts` supplies it directly (each hop leg: `nearHost`, the same host its own freshly-minted port is added to; the final chain step: `commonAncestor`) — the intended, decided source. `applyStepSequenceCore.ts`'s `findSharedHost` narrowed to `confirmCarriedHost`: an endpoint absent from the whole locked footprint stays the `staleRelationalCandidate` legality outcome (unchanged, genuinely reachable via concurrent modification); an endpoint present somewhere but not on the *carried* host is now the BD-33 throw (previously an unresolvable intersection; now means Expansion computed the wrong host). Every other construction site needed a value too, none of them Expansion-computed: `compilePositionKernelOp.ts`'s `On`-containment establish (`hostId: op.to`, already guaranteed non-null) and its `dissolvedEdges` mapping (`hostId: op.froms[0]`, single-origin by construction — `dissolvedEdges` has exactly one producer, `executeObjectMove.ts`'s carry-closure path); `executeObjectMove.ts`'s `executeMembershipTransfer` boundary-sweep dissolve steps (the loop's own `hostId` was already in scope); `executor.ts`'s `groundInstruction` direct-`Change` establish/dissolve path (`hostId: candidate.hostRoomId`, confirmed dead on every live route since PV1-3b-4 but still real code) and its `isolatedFromRelations` boundary-edge dissolve branch (`hostId`, the same value the branch already resolved `startId`'s current host to — found necessary during implementation, not anticipated by the row's original text); `applyObjectRelationalChange.ts`'s single-step commit (`hostId: args.hostId`, already carried on `RelationalIngressArgs`). **Explicitly left alone, named so a later slice doesn't rediscover them:** `computeStepSequenceFootprint.ts` still re-derives each endpoint's host via an injected `getCurrentHost` callback rather than reading `step.hostId` (conservative over-locking, not incorrect, and not named by this row); `factsForStep.ts` still re-derives via its own `findHostOf` for the narration fact (same category, same reasoning). Full suite green (346/347 suites, one pre-existing skip; 2763/2772 tests) and full typecheck (ephemera, mtw-interfaces, mtw-gateways) clean. |
| 2026-09-02 | **PV1-3b-1 shipped.** `ParseCommandEstablishRelationResult` (`actions/baseClasses.ts`) widened to carry `steps: readonly MutationKernelStep[]` — **revised from the row's own literal "steps + extraKernelSteps" wording during planning, discussed with the user:** a single field, not two (`ExecutorEstablishRelationStep`/`ExecutorDissolveRelationStep` are already `MutationKernelStep` members; the split only existed inside `runExecutor`'s own worklist-vs-side-channel plumbing), and no flat `hostId` (once `steps` can span multiple hosts, a single "the" host has no principled value — each leg already carries its own, PV1-3b-7; the earlier draft's "source it from the last step" was an arbitrary tie-break of the same shape as two bugs already shipped in this project, `findSharedHost`'s first-match resolution and `orchestrateObjectMove`'s roomId-by-scanning). `compileRelationalFromSkeleton.ts`'s per-candidate loop now carries every relational/port step of the outcome (not just the first establish/dissolve step) and drops the port-address guard that discarded every crossing candidate; portless candidates keep the existing BD-23 `filterLegalRelationalCandidates` check unchanged, a genuine crossing skips it (per PV1-3b-3's already-decided "leg-time validation is sufficient" call) and relies on `applyRelationalPatch`'s own `bothObjectsOnGraph` throw at commit time as the structural safety net. **Named gap, not fixed:** `detectRelationalCycle` is not re-run for a crossing `Under` candidate anywhere, so a cyclic one is not rejected pre-commit — not a blocker for `tie` (`Custom`). Order of the merged steps is not arbitrary: `buildCrossingLegs.ts` mints each hop's port immediately before the leg that references it, so prepending `extraKernelSteps` (ports) ahead of `steps` (legs/final) reconstructs the true order — correct only within today's <=1-hop-per-side scope, flagged as a PV1-6 follow-up. The sole existing consumer (`actions/index.ts`) updated to read the final step's `hostId` (filtering out `transferMembership`, the one `MutationKernelStep` kind without one) as a mechanical stand-in until PV1-3b-2/3 land; **accepted interim regression**, confirmed with the user: a live crossing attempt now flows through and throws a commit-time `Error` (fail-closed) instead of being silently declined, acceptable pre-rollout with no external consumers. One existing test (`compileRelationalFromSkeleton.test.ts`'s `tie string to cup` fixture) flipped from `Abstain` to a real `EstablishRelation` with a 3-step chain (port, table leg, room leg) matching PV1-0's readout exactly; a second (`actions/index.test.ts`'s sameHost-defer fixture) had a masked bug surfaced by the guard's removal — its mock's blanket `getMembershipContainers` default accidentally also answered "what contains the acting character" as the room, making two previously-genuinely-disjoint shards resolve a real crossing through the character's own room membership; fixed by making the fixture's shards genuinely disjoint again. **`LD-13` split**, discussed with the user and recorded in `AGENT.abstractionLayers.planning.md` beside CD4: it bundled a still-sound *kind*-narrowing clause (which relation kinds ingress may express, BD-2's four-kind question) with a now-false *step-shape*-narrowing clause (ingress endpoints/host stay Object/single-host-typed because "downstream can always fill in the rest") — the second is falsified by the same PV1-3b-7 finding that forced a carried per-leg `hostId`, generalized one field further. `baseClasses.ts`'s own type doc comment corrected accordingly. Full suite green (346/347 suites, one pre-existing skip; 2763/2772 tests) and full typecheck (ephemera, mtw-interfaces, mtw-gateways) clean. |
| 2026-09-02 | **DissolveRelation track opened**, from conversation after PV1-3b-1 shipped. **First framing was wrong and corrected in the same conversation:** claimed PV1-3b-1 "shipped for both establish and dissolve at once" because the dissolve tests passed unchanged — true only of the widened *type*/merge logic, not of the *mechanism* that decides what to put in it. `expandSameHost.ts` calls `findShardBoundary` (a containment-ancestry walk) unconditionally regardless of `operationKind`; this is silently correct for dissolve only in the portless/same-host case (a portless edge can only live in the one host both endpoints already share, so "where ancestries meet" and "where the existing edge is" coincide by construction) and has never been exercised for a genuine crossing dissolve, since `buildCrossingLegs` refuses that case (`notYetImplemented`) before the coincidence could break. Corrected framing, from the user: establish's question ("where do I mint a fresh chain?") and dissolve's question ("what existing chain do I remove?") need genuinely different discovery mechanisms — a containment walk vs. a walk over existing relational edges/ports — not a parallel application of the same one. **Also surfaced, separately:** this isn't distant future work — PV1-4's own readout text already describes an optional second pass ("tie the string first, then put the cup on the table") that needs the first tie undone (or fresh objects) to mean anything, so it's closer to blocking than "someday, once PV1-6 lands." Six new rows opened (PV1-3b-11 through 16, see Open decisions and Recommended order), confirmed with the user as interleaved with the EstablishRelation rows rather than sequenced after them — PV1-3b-11 (discovery design) and PV1-3b-12 (build it) don't depend on PV1-3b-3/2 and can proceed alongside them. Nothing built yet; this row records the framing and the six new rows only. |
| 2026-09-02 | **PV1-3b-11 mostly decided** (six of seven original clauses; a new one opened). Walk starts at `subjectId`; not-found declines rather than throwing; built to arbitrary depth immediately rather than capped like today's `buildCrossingLegs` (no catch-up work owed later); live-route pre-fetch confirmed as needing its own strategy (deferred to PV1-3b-12 as a build detail, not a design question); `getGraph` confirmed sufficient, no new `ExpansionEnvironment` callback. **The `applyObjectRelationalChange.ts` "port-qualified-edge skip" citation from PV1-3's Progress table (2026-09-01) was challenged by the user as possibly invented, traced through git history, and found genuinely stale rather than fabricated**: real code existed (commit `c05fc53bd`, 2026-08-22, a `dissolveSteps` filter inside the old boundary-sweep repair branch) but was deleted the same day PV1-3 named it as a gap, by PV1-3b-9's own commit (`f1aae349e`, 2026-09-01) retiring that branch wholesale — the citation is dropped as moot; a distinct, still-live instance of the same pattern in `executor.ts`'s `isolatedFromRelations` case (PV1-2's automatic boundary sweep, not this row's player-invoked discovery) is named separately, in scope for later once PV1-3b-13 lands, not this row. **Disambiguation reopened, sharper than originally framed**, after looking up the leg/chain/edge vocabulary (`AGENT.abstractionLayers.proposals.planning.md`) per the user's instruction: a leg is one `from`/`to` pair inside one graph, a chain is a route across boundary crossings composed of legs, and an Edge can carry N chains simultaneously — so two objects can be linked by more than one *chain* on the same *edge*, not just by coincidentally-identical separate relations, and a forward walk on matching kind/label can find more than one. With no `chainId`/`edgeId` minted this vertical, the walk has no identity to group candidates by — structurally the same unresolved shape as `findShardBoundary`'s own `ambiguous` verdict on the establish side. Left open: dissolve-all, decline, or a stated pick rule — and whether answering it requires minting chain identity now. Nothing built; this row records the resolved clauses and the corpus corrections only. |
| 2026-09-02 | **PV1-3b-11 fully decided.** Disambiguation resolved: decline, mirroring `findShardBoundary`'s `ambiguous` verdict, when discovery finds more than one qualifying chain on the same edge. Deliberately not treated as forcing `chainId`/`edgeId` minting forward — stays deferred, matching PV1-3's own scope cut; revisit only if `ambiguous`/multi-chain turns out common enough in practice to be worth resolving rather than declining. All seven clauses of PV1-3b-11 are now decided; nothing built yet, PV1-3b-12 is next. |
| 2026-09-02 | **PV1-3b-12 shipped:** the dissolve-side discovery primitive, `synthesize/findRelationalChain.ts` — `findShardBoundary`'s mirror image, walking existing relational edges/ports outward from `subjectId` (via `getCurrentHost`, then `getGraph`/`.relationalEdges`/`.ports`) instead of containment ancestry, since a crossing's `portId` carries no trace back to an object's current position. A port-address far-endpoint's `owner` names the port's own storage host directly (crossing ports are stored interior-side, PV1-3), so the walk needs no separate `fromHostId` hop-off to cross a boundary. Three verdicts mirroring `findShardBoundary`'s own shape (`found`/`notFound`/`ambiguous`), all seven of PV1-3b-11's decisions realized as written — no depth cap, decline (not throw) on no match, decline (not pick) on more than one qualifying chain, `getGraph`/`getCurrentHost` sufficient with no new callback. Standalone and unwired, per this row's own scope: `expandSameHost.ts` still walks `findShardBoundary` unconditionally for `dissolveRelation` (PV1-3b-14's job to fix), and nothing yet consumes a found chain to emit removal steps (PV1-3b-13's job). Two durable docs updated (`objectManipulation/AGENT.md`'s "12b. Expansion" paragraph, `synthesize/AGENT.implementation.md`'s Files table); PV1-3b-11's Open-decisions row struck now that its decisions are consumed by shipped code and recorded there. Full suite green (347/348 suites, one pre-existing skip; 2770/2779 tests, 9 pre-existing skips) and full typecheck (ephemera, mtw-interfaces, mtw-gateways) clean. |

## Open decisions (implementation --- plan only)

Plan-only: decisions we are making in order to implement the next slice(s).
Do not copy into package `AGENT.concepts.md`. When a decision ships, record it
in `AGENT.contract.md` / `AGENT.implementation.md` and remove the row here.

| ID | Decision | Blocks slice | Status |
| --- | --- | --- | --- |
| PV1-3b-2 | **Decided:** widen `Object Establish Relation`'s existing published payload/envelope to carry the widened result, rather than adding a new event pair — matching PV1-3b-1's widen-not-sibling-type call. | PV1-3b | Decided (not yet built) |
| PV1-3b-3 | **Decided:** a new sibling function, `executeEstablishEdgeChain` (not a branch inside `executeObjectEstablishRelation`, matching `executeMembershipTransfer`'s relationship to `executeObjectMove`). It is a pass-through: merge `steps`/`extraKernelSteps` (crossing-port steps first, then legs — legs are independent of each other but each depends on its referenced port already existing), build `getCurrentHost` from each step's carried `hostId` (PV1-3b-7, shipped), and call `commitStepSequence` directly. No separate whole-crossing verification layer — PV1-3b-7's per-step assert at commit time already re-checks every leg against live state, so leg-time validation is sufficient on its own. `executeObjectEstablishRelation`'s silent-swallow bug stays out of scope, since `executeObjectEstablishRelation` may itself be a retirement candidate once crossing is lifted out. (This row's original text also named `applyObjectRelationalChange.ts`'s "port-qualified-edge skip" as out of scope — **traced 2026-09-02 and found moot**: that code was deleted the same day it was named, by PV1-3b-9's own commit retiring the boundary-sweep repair branch it lived in; see PV1-3b-11.) | PV1-3b | Decided (not yet built) |
| PV1-6 | **Decided:** generalize `buildCrossingLegs.ts` from its current ≤1-hop-per-side, not-both-sides special case into one loop per side over the full `subjectPath`/`targetPath`, minting a chain of ports rather than at most one. Found while reviewing PV1-3b-7: the original scope cut's stated reason (`applyStepSequenceCore` couldn't resolve a host for a step with no primitive endpoint) is stale now that `hostId` is carried, not derived — `confirmCarriedHost` skips the membership check entirely for a port-address endpoint, and `applyRelationalPatch`'s `bothObjectsOnGraph` only requires a port's `owner` to be a node of that step's own host graph, which holds by construction at every hop (`subjectPath[i]`'s own container is exactly `subjectPath[i+1]`, that being how `findShardBoundary` built the path). **Mechanism, generalizing today's single-hop code as the `path.length === 2` case of a loop:** for a path `[h0, h1, ..., h(N-1)=commonAncestor]`, iterate `i` from `0` to `N-2`: mint a port at `hi` with `fromHostId: h(i+1)`; add a leg at `hi` connecting the running terminal (the raw endpoint when `i === 0`, else the previous hop's port address) to this hop's fresh port address. Do this independently per side; a path of length ≤ 1 makes the loop a no-op, leaving the raw endpoint as the terminal (today's degenerate zero/one-hop cases, unaffected). Push each port step immediately before its own leg (existing PV1-3b-3 merge-order convention, generalized from "the one port" to "each hop's port"); the final `operationKind` step at `commonAncestor` connects the two sides' resulting terminals, after both chains. **Explicitly still out of scope:** dissolving an actual crossing (any hop minted) stays `notYetImplemented` — this row is establish-chain-only, matching PV1-3's own establish-only crossing scope, not a silent expansion of it; `findShardBoundary`'s `ambiguous` verdict (which route among several) is unrelated and stays deferred. Not required for PV1-3b/PV1-4's readout (`tie string to cup` only ever needs one hop) — an independent generalization, not a blocker on or blocked by either. | PV1-6 | Decided (not yet built) |

## Recommended order

Pending work is `- [ ]` and completed work is `- [X]`; mark nested lines `[X]` as each is done, so partial progress on a slice is visible. Updating these checkboxes is part of *done* for the slice, after tests pass.

- [X] **PV1-0. Write the readout, before any code.** Done 2026-08-31 — see [The readout](#the-readout).
- [X] **PV1-1. Referent search survives nesting.**
  - [X] Extend the catalog to walk into hosted objects' graphs by Dynamo read recursion. No `ludicCache` — a correct recursive read discharges CC3's prerequisite and a cache can substitute later behind a stable interface.
  - [X] Depth cap of five (testing bound, not a claim about real nesting depth) plus a visited set (terminates cyclic graphs).
  - [X] Build against hand-built nested fixtures — the graph already round-trips hosting shape (CD1: `In`/`PartOf` persistence-tested but inert).
  - [X] **Done when:** a cup nested in a tray fixture can be named and acted on from the room, with a test that fails against the pre-nesting enumeration.
- [X] **PV1-1b. Consolidate object-lifecycle membership onto the instruction / planner / compiler / kernel pipeline; retire the scale-specific mutators.** Done 2026-08-31.
  - [X] **Correction found during implementation:** widening `parsePlanStep.ts`'s `TransferMembershipStep` and `buildObjectMoveOp.ts` was **not done** — a usage survey showed both are read as scalars throughout the real player-command pipeline (Grounding, Expansion, relational repair), none of which touch the routes this slice migrates. `kernelStep.ts`'s own doc comment already states the design principle this would have violated: the Synthesize executor's step vocabulary stays object-only/unwidened by design, and `fromExecutorStep` widens *at the adapter boundary*. Left untouched.
  - [X] Added a new sibling function, `executeMembershipTransfer`, in [`executeObjectMove.ts`](../../../lambda/ephemera/dataSource/positions/manipulation/membership/executeObjectMove.ts) — single entity (object or character), diffed against its own fetched `priorContainers` (not a caller-supplied `fromHostId`/`toHostId` pair), no carry closure, never touches the Synthesize executor or `buildObjectMoveOp`. Object entities get an explicit per-host `boundaryEdgeOutcomes` sweep; character entities never do. Carries `errorCode`/`errorMessage` on failure and `{froms, to, changed}` on success (the result-shape gap the original checklist named on `ExecuteObjectMoveResult` was instead solved here, since none of the migrated call sites use `executeObjectMove` itself). `executeObjectMove` (the carry-closure, single-origin real-transfer function for take-hold/drop/give) is unchanged.
  - [X] Threaded `suppressRelationalFacts` and `compileMutationSteps` through `executeMembershipTransfer` (not `executeObjectMove`, which no migrated call site uses).
  - [X] Deleted `applyObjectRoomMembership`, `applyObjectClearMembership`, and the adapters `planMembershipTransfer`, `computeEndStateRoomDiff`, `planObjectClearFromAllHosts` (and the now-dead `ObjectMembershipApplyArgs`/`ObjectClearMembershipApplyArgs`/`MembershipTransferProjection`/`MembershipTransferPlan` types), plus their test files. `manipulation/adapters/` no longer exists.
  - [X] Moved five call sites onto `executeMembershipTransfer`: `spawnImprovisationObjectsBatch`, `repairObjectPlacementDrift`, `applyObjectsChange`, `clearCoyoteGameImprovisationObjects`, and `applyCharacterRoomMembership`'s membership half (kept as a thin wrapper: roster snapshots, `CharacterMeta` invalidation, `EphemeraUpdate` publish, and the `compileMutationSteps`/`narrationHandledInline` hooks navigate/connect/disconnect depend on). Room-stack machinery (`membershipRoomStack`, `mergeRoomStack`, `trimEvictionLadder`, `persistRoomStackNavigate`) confirmed untouched — it has zero references in `applyCharacterRoomMembership.ts` and lives one caller-hop away.
  - [X] Struck `AGENT.implementation.md:51`'s "do not extend room-only entry points" and the `D14` citation in `AGENT.contract.md:228`; also updated both files' file-role tables, the "Object room membership" and "Object membership-changed bundle (room-only)" contract sections, `AGENT.concepts.md`'s spawn/place/remove bullets, `objects/AGENT.md`'s spawn/clear prose, and `AGENT.CoyoteGame.implementation.md`'s one reference. Marked `manipulation/AGENT.implementation.md`'s Section B (the retired adapter writeup) `[SUPERSEDED, PV1-1b]` rather than rewriting it — full rewrite, the `bounded`-mode question, and `D8`/`D13`/`D14`/`D16` register-or-strike stay explicit PV1-5 work, per this row's own original deferral.
  - [X] **Done when:** every object membership change (spawn, place, remove, drift repair, clear, take-hold, drop, navigate, connect, disconnect) reaches the kernel through `executeMembershipTransfer` or `executeObjectMove`; the retired coordinators and adapters are deleted; full ephemera suite (343 suites / 2699 tests), `tsc --noEmit` (ephemera, mtw-interfaces, mtw-gateways) all green.
- [X] **PV1-2. `On` nests end to end, as a rehost carrying a containment argument** (not a relation with a side effect; establishing `On` moves nothing and there is no operation that "establishes `On`"). Done 2026-08-31.
  - [X] Added `containment?: 'On' | 'In' | 'PartOf'` to `ExecuteObjectMoveArgs`, `BuildObjectMoveOpArgs`, and `PositionKernelMoveOp` (hosting kinds only, AB-54). `OrchestrateObjectMoveArgs` gained it too.
  - [X] `compilePositionKernelOp` emits `establishRelation(primaryMovedId -> op.to, containment)` after `transferStep` (endpoint resolution needs the moved object already a node of the destination graph — before the transfer, `findHostOf` would resolve the old host). `MutationKernelTransferStep` left unwidened, per this row's own instruction — the containment edge commits through the ordinary `establishRelation` step, already a legal (if previously unemitted) kernel step kind.
  - [X] Presence port: **not** landed as a plain field mutation — no existing mechanism could write it transactionally, since it lives on a *third* graph (the moved object's own) outside `fromHostId`/`toHostId`'s footprint. Added a new kernel step kind, `MutationKernelSetPresencePortStep` (`kernelStep.ts`), wired through `computeStepSequenceFootprint`/`applyStepSequenceCore`/`factsForStep` (no fact — not a narration channel yet) exactly like `establishRelation`/`dissolveRelation` are. `compilePositionKernelOp` emits it whenever `op.moved.kind === 'closure'` (object closures only — the gate the checklist below asked for, achieved for free: character moves use `kind: 'entity'` and never reach this branch) and `op.to` is set: replaces any existing `kind: 'Present'` port with a fresh one naming the destination (presence is at-most-one, PR-10, so this is a *set* not an *add* — no read of the prior port needed). `EphemeraLudicGraph` gained `addPort`/`removePort` (mirroring the `addRelationalEdge`/`removeRelationalEdge`/`withEdges` pattern) to back it.
  - [X] Confirmed live in `buildObjectMoveOp`/`compilePositionKernelOp` (reaches `executeObjectMove` only, i.e. take/drop/give) — not `executeMembershipTransfer` (spawn/place/remove/drift-repair/clear), which is untouched by this row. Its own `boundaryEdgeOutcomes` call will still throw if it ever meets a hosting-kind boundary edge; none of its current callers can construct one, so this is an inert gap, flagged in its doc comment, not a live one.
  - [X] No explicit "mint a ludicGraph" step needed — already covered by existing behavior: `fromObjectMeta`/`fromPlainHostMeta` (`ludicGraph/index.ts`) already default a missing `ludicGraph` field to a fresh rooted graph (`{ rootId: hostId, nodes: [nodeFromId(hostId)], edges: [], ports: [] }`) on every read, both at `internalCache.Positions.getLudicGraph` and at `commitStepSequence`'s own `MultiKeyUpdate` fetch. Confirmed by reading `graphFromMeta`/`fromPlainHostMeta` directly rather than assumed.
  - [X] Old containment edge removed explicitly in `executeObjectMove.ts`, **before** the executor ever sees the graph — not via `classifyInteractionUnderTransfer`. Structural inspection (`edge.from === primaryObjectId && edge.to === fromGraph.rootId`, hosting kind) finds the moved object's own edge into the host it's leaving; stripped from the graph handed to `createExpansionEnvironment`, then prepended to `dissolvedEdges` so it still renders as an ordinary `dissolveRelation` step. This matters because the Synthesize executor's own operand-expansion calls `boundaryEdgeOutcomes` internally — leaving the edge in place would throw *inside* `runExecutor`, uncatchable from `executeObjectMove`. `classifyInteractionUnderTransfer`'s throw-comment narrowed to record this one shape now has a producer; the throw itself is untouched for every other case (LD-11 not retired, only bypassed for this one shape — CD3 below is partial, not full, discharge).
  - [X] `orchestrateObjectMove`'s **pre-existing bug**, found while wiring this: it derived both `characterId` and `roomId` by scanning `[fromHostId, toHostId]`, so any containment move (destination is an object, never a room) silently no-op'd. Fixed by taking `roomId` as an explicit required argument instead of deriving it; both existing take/drop call sites in `positions/index.ts` updated to pass it (`content.roomId`, already in scope). Verified against the existing take/drop tests before `containment` was threaded in, so the fix and the new feature are separable in the test history.
  - [X] **Client wiring done 2026-08-31.** `SkeletonFamilyMatch`'s `relationalDefer` variant (`classifySkeletonFamily.ts`) widened to forward `operationKind`/`subject`/`target` (previously dropped on the floor) alongside `kind`. `parseCommand.ts`'s `relationalDefer` branch now special-cases `kind === 'On'`: a new function, `compileObjectRehostFromSkeleton.ts`, resolves `subject`/`target` to object ids via the same `runIdentityStageOverSkeleton`/`resolvedSpansFromPools` pair `compileRelationalFromSkeleton.ts` already uses, then stops — deliberately **skipping** Grounding/Expansion/Validation, which solve peer-relation-specific problems (candidate combinations, same-host boundary legality) that don't apply to a rehost. `In`/`PartOf` are untouched (still the pre-existing hard error). A new result type, `ParseCommandObjectRehostResult` (`baseClasses.ts`) — deliberately separate from `ParseCommandEstablishRelationResult`, which narrowed `On` out on 2026-08-22 — carries `subjectId`/`targetId`/`hostId`/`containment`/`confidence` across the `mtw.ephemera.actions` → `mtw.ephemera.positions` publish/subscribe boundary (`streamEvent`/`messageBus` are not available at `parseCommand.ts`'s layer — confirmed by reading the two `DataSource` registrations directly — so this follows the same publish-then-resubscribe shape every other object-manipulation result already uses, not an in-process shortcut): a new `Object Rehost` published payload (`publishedEvents.ts`) and subscribed envelope (`subscribedEvents.ts`), and a new branch in `positions/index.ts`'s `receiveEvents`. That branch resolves `subjectId`'s **current** host fresh via `internalCache.Positions.getMembershipContainers` (erroring — a no-op, not a guess — on zero or multiple containers) rather than trusting a value published at parse time, since the object can move between parse and execution; it then calls `orchestrateObjectMove` with `containment: 'On'`. Scope cuts, deliberate: multi-candidate (ambiguous) subject/target resolution errors out rather than disambiguating; `take cup off tray` (dissolve, no containment kind) is untouched — what destination that should resolve to (the existing acquire lane, per AB-54) is a design answer, not yet code. Tests added at every layer: `compileObjectRehostFromSkeleton.test.ts` (new), `classifySkeletonFamily.test.ts`/`parseCommand.test.ts`/`publishedEvents.test.ts`/`actions/index.test.ts`/`positions/subscribedEvents.test.ts` (extended), and `positions/receivePaths.integration.test.ts` (extended — the established real-messageBus-round-trip convention this repo uses in place of a cross-Lambda harness). Full ephemera suite (344/345 suites, 2730 tests) and full typecheck (ephemera, mtw-interfaces, mtw-gateways) green.
  - [ ] Note: [CD6](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order) does not apply here — it is a crossing-port kind-agreement check, and a presence port has no exterior edge to disagree with. CD6 stays with PV1-3; reword its trigger at PV1-5.
  - [X] **Done when:** a cup put on a tray is a member of the tray's `ludicGraph` (not the room's), its containment edge is root-anchored inside the tray's graph, its presence port names the tray, and both are gone when it leaves — asserted in test (`executeObjectMove.test.ts`'s `PV1-2: On rehost` suite, and the client-wiring row above carrying a parsed `put cup on tray` command through to `orchestrateObjectMove`) **and observed live in the running client 2026-08-31**: `put cup on table` showed the cup as a table-graph member with its containment edge and a presence port naming the table (table absent from the room's contained nodes); `get cup` then `drop` showed both gone — the port reverted to naming the room and the table lost its contained node. Two bugs found and fixed along the way, neither anticipated by this row: `applyStepSequenceCore`'s host resolution for `establishRelation`/`dissolveRelation` used first-match-per-endpoint semantics, which mis-resolved the shared host whenever a hosting-kind target sat in two locked footprint graphs at once (fixed with an intersection-based `findSharedHost`); and `Object Take Hold`'s handler trusted `content.roomId` as the object's current host, which broke for a nested object (fixed by resolving it fresh via `getMembershipContainers`, same pattern as `Object Rehost`). This slice's own live-client observation is discharged; PV1-4's fuller readout (Acme order flow, PV1-3's tie-string chain, reading the four Dynamo records directly) is still owed separately.
- [X] **PV1-3. A `Custom` relation across the boundary, and the crossing-port producer it needs.** Unlike PV1-2 (a rehost carrying a containment argument), nothing here moves — `tie rope to cup` leaves both the rope and the cup exactly where they are; the boundary is crossed by a port pair, not by a transfer. That is what makes this row's server gap different in kind from PV1-2's, not just in degree. Done 2026-09-01.
  - [X] **Server, not client — lexicon fix confirmed and made.** The row's original "Client:" label was wrong: `classifyVerb`/`ESTABLISH_VERBS`/`DISSOLVE_VERBS` live in `lambda/ephemera/dataSource/actions/enrich/objectManipulation/plan/matchRelationalTemplate.ts` (mirrored in `dataSource/actions/deterministicTemplate/relationalTemplates.ts`) — both server-side, not `charcoal-client`. "tie" was in neither set, and the fallback for an unrecognized verb is `noMatch`, not `establish` (confirmed by a test before changing anything, per this row's own original instruction). Added `'tie'` to `ESTABLISH_VERBS` in both files — no synonyms, matching `put`/`place`/`lean`'s own one-entry-each convention. `normalizeRelationSpan.ts`'s "to" → `Custom` by exclusion (no new phrase entry needed) was confirmed correct as originally written.
  - [X] **Server: the actual gap, confirmed.** `applyStepSequenceCore.ts`'s `findSharedHost`/`hostsOf` throw on a genuine host mismatch (BD-33); `expandSameHost.ts` (BD-16) had no outcome for *legitimately never share a host, mint a crossing pair instead*. That third outcome is now `ExpandSameHostResult`'s `'crossed'` verdict.
  - [X] **Shard-boundary predicate, general case** — `synthesize/findShardBoundary.ts`. Walks `getMembershipContainers` recursively from each endpoint (uniform at every node/depth — `EphemeraPositionAdjacencyContainedId`, i.e. Character/Object/Feature, is exactly the set the primitive accepts; Room/Area terminate a branch without a further call) until every branch terminates. **Correction found during plan review, before code:** "closest common ancestor" is not a combined-depth minimum — it is the Pareto-minimal frontier of common nodes under (subjectDepth, targetDepth) domination (a common node dominated by another common node that is no-worse on both counts and strictly better on at least one is excluded, since routing through it would mean walking up past a perfectly good crossing point and back down through it again). This can still have more than one member when the two ancestry DAGs branch incomparably — verdicts are `notFound` (zero), `crossed` (one, carries `commonAncestor`/`subjectPath`/`targetPath`), or `ambiguous` (more than one, not-yet-implemented). Unit-tested directly: the same-shard degenerate case, the room/table one-hop case, a multiply-contained intermediate host (cup on table *and* in a place setting, both leading to the same room), a `notFound` case, and a constructed fixture with two genuinely incomparable Pareto-minimal ancestors (not just two starting points converging on one node) — `findShardBoundary.test.ts`.
  - [X] **Crossing-port producer, general case** — `synthesize/buildCrossingLegs.ts`, consuming `findShardBoundary`'s `'crossed'` result. **Correction found during plan review:** legs don't need a new kernel step kind — `establishRelation`/`dissolveRelation` were already leg-shaped once `ExecutorEstablishRelationStep`/`ExecutorDissolveRelationStep` widened from `EphemeraLudicTerminalPrimitive` to `EphemeraLudicTerminalId` (a leg's far-side endpoint is a port address), matching what `HostRelationalEdge`/`EphemeraLudicRelationalEdgeBase` already supported (LP7). One fresh `EphemeraCrossingPort` is minted per crossing, stored on the interior side (nearer the endpoint the relation is reaching toward) — exactly the readout's own asymmetry, generalized. **Scope cut, deliberate (confirmed with the user):** supports at most one extra hop per side, and never both sides at once — that combination needs a *middle* leg between two port addresses with no primitive endpoint, which `applyStepSequenceCore`'s BD-33 derive-don't-carry host resolution cannot resolve without carrying an explicit host on the step (a bigger, not-taken change). Reports `notYetImplemented` for that case rather than emitting a step that would throw at apply time. Establish-only — dissolve/untie is unbuilt. Unit-tested directly: the readout's own case (rope in room, cup on table — one port minted, two legs), the symmetric case (subject nested instead of target), the degenerate single-leg case, a non-`Custom` kind (no `relationLabel`), and both `notYetImplemented` shapes — `buildCrossingLegs.test.ts`.
  - [X] **`findSharedHost`/`hostsOf` widened for port-address endpoints** (found necessary during implementation, not originally in this row): a port address's host is its own `owner` field (graph-local addressing, not membership search) — used as a candidate in the existing intersection, with a fallback to the primitive endpoint's own (unambiguous) candidate when the intersection comes up empty, since the exterior side of a crossing has no port record of its own to intersect against. `computeStepSequenceFootprint.ts`/`factsForStep.ts` widened correspondingly (footprint resolves whichever endpoint(s) are primitives; facts return `[]` for any port-qualified leg — no established fact/narration shape yet, same precedent as `setPresencePort`).
  - [X] **Kernel step (new), narrowed to the port record only.** `MutationKernelAddCrossingPortStep`/`MutationKernelRemoveCrossingPortStep` (`kernelStep.ts`) — add/remove by `portId` (crossing ports are not at-most-one, unlike presence). Wired into `isKernelMutationStep`, `computeStepSequenceFootprint.ts`, `factsForStep.ts` (returns `[]`, same as `setPresencePort`), and `applyStepSequenceCore.ts`'s dispatch (calls the existing `addPort`/`removePort`).
  - [X] **Wired into Expansion.** `expandSameHost.ts`'s `ExpandSameHostResult` gained the `'crossed'` verdict (its own doc comment had already flagged the union open to a third outcome); `GroundedBinaryAssertion` gained an optional `relationLabel` (needed to build a `Custom` crossing port/leg, previously only `relationKind` was carried). `executor.ts`'s `commandExpand` `'sameHost'` case: leg steps (`establishRelation`, already executor-shaped) retire through the ordinary worklist; the port-record step cannot (`addCrossingPort` is not an `ExecutorParsePlanStep`) and rides a new side-channel, `CommandExpandOutcome`/`ExecutorOutcome`'s optional `extraKernelSteps` — present and non-empty only for a crossing, so every other caller/test is unaffected. Unit-tested directly at both layers: `expandSameHost.test.ts` (a violated Custom relation crossing instead of deferring; falling back to defer when no `relationLabel` is supplied) and `executor.test.ts` (`runExecutor` end to end: crossing legs land in `steps`, the port record in `extraKernelSteps`).
  - [X] **No `chainId`/`edgeId` minting this slice** (scope cut, confirmed with the user during plan review, overriding this row's original text) — legs are inline/unidentified. PV1-0's readout gained a line under "what this run cannot observe" naming this consequence.
  - [X] **`applyObjectRelationalChange.ts`'s port-qualified-edge skip, checked, not fixed.** It only runs during a membership-transfer's boundary sweep; `tie rope to cup` triggers no transfer (nothing moves), so this slice never routes through it. Stays a named, deliberate gap for whichever slice first needs to *dissolve* a crossing relation.
  - [X] **Live ingress route (`compileRelationalFromSkeleton.ts`) intentionally not wired** — two gaps, named in a comment at its own seed-construction site: its `getMembershipContainers` pre-fetch is one-hop only (the walk needs to keep going past intermediate hosts), and even with deeper pre-fetching its seed's sibling `relationalStepNoHost` item would retire unmodified alongside the crossing legs, double-emitting an invalid direct edge — the satisfied/repaired outcomes rely on that sibling; a crossing replaces it entirely and needs the seed built accordingly. `expandSameHost`/`commandExpand`/`buildCrossingLegs` are unit-tested directly instead (listed above).
  - [X] **Done when:** unit tests cover the shard-boundary predicate (including its multiply-contained and zero/many-common-ancestor branches), the crossing-port producer and kernel step, the `expandSameHost`/`commandExpand` wiring end to end, and the verb-lexicon fix — all landed, full ephemera suite green (346/347 suites, 2770 tests — one suite skipped, pre-existing) and full typecheck (ephemera, mtw-interfaces, mtw-gateways) green. The live-client `tie rope to cup` run itself is PV1-4's job, not this row's.
- [ ] **PV1-3b. Wire the live ingress route to PV1-3's crossing mechanism.** Stubbed 2026-09-01 from live-testing `tie string to cup` (see Progress). Two interleaved tracks: **EstablishRelation** (design questions decided; see Open decisions for the current shape of each remaining build row) and **DissolveRelation**, opened 2026-09-02 — a genuinely different mechanism, not a mechanical parallel; design decided (PV1-3b-11), build not started. **PV1-3b-4 shipped** (collapse the ingress seed, delete `satisfied`); **PV1-3b-5 shipped** (deepen the live route's pre-fetch to full depth-capped ancestry); **PV1-3b-7 shipped** (carry a mandatory `hostId` on relational steps); **PV1-3b-1 shipped** (widen `ParseCommandEstablishRelationResult`, stop dropping crossing candidates at ingress — this one did land shared across both operation kinds, unlike the rows above it). Remaining build: the new multi-host commit path (PV1-3b-2/3) and, interleaved, the dissolve discovery mechanism and its own commit path (PV1-3b-11 through 16).
  - [X] **PV1-3b-8: `findShardBoundary` counts each endpoint as a candidate common ancestor.** Shipped — endpoints seed at depth 0 (`walkAncestry`, with a `visited` guard so a containment cycle can't overwrite that seed), so a same-shard relation resolves to a plain edge instead of a pointless crossing port. `buildCrossingLegs`'s zero-length-path guard relaxed to match (`> 2` and `2 && 2` are the only remaining guards).
  - [X] **PV1-3b-9: generalize crossing to `Under`/`Against`; retire `repaired`.** Shipped — `expandSameHost`'s gate tests peer-ness (`Under`/`Against`/`Custom`), not `Custom` alone; hosting kinds get a named CD2h `error` instead (AB-54 forbids treating them as a boundary). `Under`/`Against` defer like `Custom` when no crossing can be built, with a distinct reason string. `repaired` is deleted end to end — verdict, executor case, and `transferFromHostId` across all seven carrying files — not just made unreachable.
  - [X] **PV1-3b-10: drop `negate` from the `sameHost` path.** Shipped — dead weight once `sameHost` stops being an assertion (PV1-3b-4); `containedBy` keeps its own `negate` untouched. `GroundedBinaryAssertion` still unions both predicates for now (full split waits on PV1-3b-4).
  - [X] **PV1-3b-6: thread `relationLabel` into the seeded assertion.** Shipped, reordered ahead of PV1-3b-4 — the live seed never set it, so `Custom` could never reach the crossing gate, and deleting `satisfied` first would have surfaced that as a misleading defer instead of a fix. A label-less `Custom` now errors explicitly at the top of `expandSameHost`, above every state lookup (input validation, not routing — survives PV1-3b-4's deletion of everything below it).
  - [X] **PV1-3b-4: collapse the seed to one instruction; delete `satisfied`.** Shipped 2026-09-01 — see Progress. `compileRelationalFromSkeleton.ts` seeds `[sameHostAssertion]` alone; `expandSameHost`'s `satisfied` branch and its `getCurrentHost`/`getGraph` callbacks are gone (`getMembershipContainers` kept, now also wired one hop deep on the live route to avoid a same-host regression — found during implementation, not in this row's original text). `executor.ts`'s `commandExpand` dropped the `satisfied` arm entirely. Existing `satisfied` fixtures rewritten across the whole `objectManipulation` tree (not just files already touched); full suite and typecheck green.
  - [X] **In scope, same slice: split `GroundedBinaryAssertion` (`executorTypes.ts`) so `sameHost` leaves the union.** Shipped 2026-09-01 — `GroundedBinaryAssertion` collapsed to `containedBy` alone (`negate` unconditionally required again); `sameHost` got its own sibling type, `GroundedSameHostAssertion` (plus `operationKind`, found necessary during implementation — see Progress), in `GroundedAssertion`'s union rather than disappearing, since it is still very much live. `groundAssertion.ts`'s fused case un-fused to `containedBy`-only, confirmed dead-code-only on the `sameHost` side by the call-graph reach already recorded here; `compileUngroundedPlan.ts`'s `sameHost` construction and `ungroundedPrimitive.ts`'s `SameHostAssertion`/`Assertion` member deleted outright (the union no longer admits it) rather than kept in sync. `tsc --noEmit` stayed clean throughout.
  - [X] **PV1-3b-5: deepen the live route's pre-fetch.** Shipped 2026-09-02 — see Progress. `compileRelationalFromSkeleton.ts` now pre-fetches an eager, depth-capped (5) async walk of each distinct candidate's full ancestry (`walkAncestryContainers`, `synthesize/findShardBoundary.ts`, async-ified against `walkAncestry`'s own traversal shape) instead of one hop, so `findShardBoundary` can reach a common ancestor past an intermediate host. Confirmed this alone does not make a genuine crossing land live on this route (single-relStep/port-address guard) — that stays PV1-3b-1/2/3/7's job.
  - [X] **PV1-3b-7: carry a mandatory `hostId` on relational steps.** Shipped 2026-09-02 — see Progress.
  - [X] **PV1-3b-1: widen `ParseCommandEstablishRelationResult`** to carry the Expansion-derived step list, host-attributed per PV1-3b-7. Shipped 2026-09-02 — see Progress. Revised the row's own literal field shape after discussion: one `steps: readonly MutationKernelStep[]` field, not a `steps`/`extraKernelSteps` split, and no flat `hostId`.

  **DissolveRelation track, opened 2026-09-02 from conversation — interleaved with the EstablishRelation rows below, not sequenced after them.** Not required for PV1-4's core readout run (`tie string to cup` never needs an untie), but blocking a *second* attempt or any cleanup pass — PV1-4's own readout text already describes an optional second pass ("tie the string first, then put the cup on the table") that needs the first tie undone, or fresh objects, to mean anything. See [Open decisions](#open-decisions-implementation--plan-only), PV1-3b-11, for why this is a design problem and not a mechanical extension of the establish rows: dissolve needs a fundamentally different discovery mechanism (walk *existing* relational edges/ports, not containment ancestry), not a parallel application of `findShardBoundary`.
  - [X] **PV1-3b-11: decide the dissolve-side discovery mechanism.** Design only, no code. **Decided 2026-09-02** — see Open decisions: walk direction (start at subject), not-found handling (decline, not throw), depth scope (arbitrary depth immediately, no ≤1-hop cut), live-route pre-fetch (needs its own strategy, deferred as a PV1-3b-12 build detail), the `applyObjectRelationalChange.ts` premise (traced and found moot — the code was deleted the same day PV1-3 named it, by PV1-3b-9's own commit), environment shape (`getGraph` suffices), and disambiguation (**decline, mirroring `findShardBoundary`'s own `ambiguous` verdict**, when discovery finds more than one qualifying chain on the same edge — leg/chain/edge vocabulary looked up per the user's instruction; deliberately not treated as forcing `chainId`/`edgeId` minting forward). Ready for PV1-3b-12.
  - [X] **PV1-3b-12: build the discovery primitive.** Shipped 2026-09-02 — new file `synthesize/findRelationalChain.ts`, sibling to `findShardBoundary.ts`. `findRelationalChain(input, env)` takes `{ subjectId, targetId, relationKind, relationLabel? }` (both `EphemeraObjectId`, matching `expandSameHost`'s own pairing) and `Pick<ExpansionEnvironment, 'getGraph' | 'getCurrentHost'>` — no new callback, both already exist on `ExpansionEnvironment` (decision 7). Starts at `getCurrentHost(subjectId)`, then depth-first walks `graph.relationalEdges` matching `relationKind`/`relationLabel` outward from the current terminal: a primitive far-endpoint equal to `targetId` completes a chain; a port-address far-endpoint resolves via `ephemeraLudicTerminalOwner` to the next host's graph (the port's own storage host — PV1-3's crossing ports are stored interior-side, and the far endpoint's `owner` names exactly that host, so no separate `fromHostId` hop-off is needed) and recurses, guarded by a `visitedPortIds` set against a malformed cycle; any other primitive is a dead end (chains cross host boundaries only through ports, by construction). No depth cap (decision 4). Three verdicts: `found` (one chain, `steps: RelationalChainStep[]` alternating `{type:'edge', hostId, edge}`/`{type:'port', hostId, port}`, ordered outward from subject), `notFound` (decline — no matching chain, or `getCurrentHost` itself resolves nothing), `ambiguous` (decline, carries only a `chainCount` — PV1-3b-11 decision 2, no pick-one). New `findRelationalChain.test.ts`: zero-hop portless edge, PV1-0's own one-hop room/table readout shape (found, ordered `[edge, port, edge]`), not-found on no matching edge, not-found on wrong kind/label, a non-matching edge alongside a matching one (confirms filtering), ambiguous on two structurally distinct qualifying edges, not-found on no current host. **Deliberately not touched, per this row's own scope:** `expandSameHost.ts` (still calls `findShardBoundary` unconditionally for both `operationKind`s — rewiring the `dissolveRelation` branch onto this function is PV1-3b-14) and `buildCrossingLegs.ts` (emitting the actual remove-leg/remove-port steps from a found chain is PV1-3b-13). Two durable docs updated: `objectManipulation/AGENT.md`'s "12b. Expansion" paragraph (names `findRelationalChain` and states it isn't wired in yet) and `synthesize/AGENT.implementation.md`'s Files table (new row, plus a note on `expandSameHost.ts`'s own row). Full ephemera suite green (347/348 suites, one pre-existing skip; 2770/2779 tests, 9 pre-existing skips) and full typecheck (ephemera, mtw-interfaces, mtw-gateways) clean.
  - [ ] **PV1-3b-13: extend `buildCrossingLegs.ts` (or a sibling) with remove-leg/remove-port step emission**, consuming PV1-3b-12's discovery result — the mirror of today's add-leg/add-port minting. Confirmed with the user (2026-09-02): the primitives need to support this.
  - [ ] **PV1-3b-3: build `executeEstablishEdgeChain`.** Port steps first, then legs (legs are mutually independent but each needs its port already placed); build `getCurrentHost` from each step's carried `hostId`; call `commitStepSequence` directly — no separate pre-commit check needed, PV1-3b-7's per-step assert already covers it. Leave `executeObjectEstablishRelation`'s silent-swallow bug alone (out of scope here). This row's own earlier text also named a port-qualified-edge dissolve skip in `applyObjectRelationalChange.ts` as out of scope — **traced 2026-09-02 and found moot**, that code was deleted the same day it was named; see PV1-3b-11.
  - [ ] **PV1-3b-14: wire `expandSameHost`'s `dissolveRelation` branch onto PV1-3b-12/13** instead of always calling `findShardBoundary` regardless of `operationKind` (today's behavior, silently correct only for the portless case — see PV1-3b-11). Portless dissolve keeps resolving the way it does today; a genuine crossing dissolve becomes buildable instead of permanently `notYetImplemented`.
  - [ ] **PV1-3b-2: widen `Object Establish Relation`'s published payload/envelope** to carry the widened result across the `mtw.ephemera.actions` → `mtw.ephemera.positions` boundary.
  - [ ] **PV1-3b-15: widen `Object Dissolve Relation`'s published payload/envelope** the same way, mirroring PV1-3b-2 — a genuinely separate published event with its own handler, not touched by PV1-3b-2. Natural to build right after PV1-3b-2, while the widening pattern is fresh.
  - [ ] **PV1-3b-16: dissolve-side commit path.** Confirm whether `executeEstablishEdgeChain` (PV1-3b-3) is already `operationKind`-agnostic (a pure pass-through over whatever `steps` it's given) and just misleadingly named, or whether it needs a small extension/sibling for dissolve's remove-shaped steps. Decide and build once PV1-3b-3/13 both exist to compare against.
  - [ ] **Done when (EstablishRelation track):** `executeEstablishEdgeChain` is built (port steps before legs, no bespoke verification, single `commitStepSequence` call) alongside PV1-3b-1/2/4/5/7; full ephemera suite and typecheck green.
  - [ ] **Done when (DissolveRelation track):** a genuine crossing can be dissolved end to end (discovery, removal-step emission, wiring, payload, commit) with the same live-route confirmation standard as the establish track; not required for PV1-4's core run, but required before PV1-4's optional second pass or any interactive re-testing that doesn't re-provision fresh Acme objects each time.
- [ ] **PV1-4. Take the readout.** Run PV1-0's observations against the built vertical and write down what happened, including the find path this vertical could not observe. **Blocked on PV1-3b** — `tie string to cup` does not work live yet.
- [ ] **PV1-5. Write back into the design corpus** — the `P8-i1-dependent` tag names the rows that owe an update (currently one: [EA-10](dataSource/positions/AGENT.edgeAbstractions.planning.md#ea-10)).
  - [ ] [P8](dataSource/positions/AGENT.abstractionLayers.proposals.planning.md#status-1): record what the readout showed, and whether the rollback trigger fired.
  - [ ] [EA-10](dataSource/positions/AGENT.edgeAbstractions.planning.md#ea-10): the five structure-as-identity sites now have live traffic — say what that changed.
  - [ ] [AB-55/AB-62](dataSource/positions/AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only): both port producers exist — close or re-scope. AB-55 takes the readout's presence-port clause directly.
  - [ ] [CD6](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order): reword its travel rule from "whichever step first writes a port record" to "first writes a *crossing* port record" (PV1-2 writes presence ports and does not discharge it).
  - [ ] [CD3](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order) / [CD2h](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order): record what PV1-2 did instead of the retired classification throw, and whether LD-11 is now live.
  - [ ] [AB-65](dataSource/positions/AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) (*is an edge a value or an entity?*): report evidence, do not answer it here.
  - [ ] Strike "do not extend room-only entry points" ([`AGENT.implementation.md:51`](../../../lambda/ephemera/dataSource/positions/AGENT.implementation.md)) and the `D14` bullet ([`AGENT.contract.md:228`](../../../lambda/ephemera/dataSource/positions/AGENT.contract.md)). Sweep references to the deleted coordinators/adapters in `AGENT.contract.md` (object room membership section, room-only bundle section, graph-forward repair, relational-patch prohibition), `AGENT.implementation.md` (file table, objects-lane rows), `AGENT.concepts.md` (spawn+place, place/remove), and `manipulation/AGENT.implementation.md`'s route table. Give `D8`/`D13`/`D14`/`D16` a register or strike the tags — they are cited in the contract and defined nowhere.
  - [ ] **Done when:** each row above is either updated or explicitly recorded as unaffected, and no new open rows are added from this plan.
- [ ] **PV1-6. Generalize `buildCrossingLegs` to a full port chain, not just ≤1 hop per side.** Not blocking, and not blocked by, PV1-3b/PV1-4/PV1-5 — see Open decisions for the full mechanism.
  - [ ] Replace the `subjectPath.length > 2` and `subjectPath.length === 2 && targetPath.length === 2` early-return guards with one loop per side over the full path, minting one port and one leg per hop (see Open decisions for the exact per-hop shape).
  - [ ] Confirm the existing single-hop tests (`buildCrossingLegs.test.ts`) still pass unmodified as the loop's `N === 2` case, then add a genuine multi-hop fixture (e.g. cup on tray on table, tied to something in the room — three shards) covering both a chain on one side alone and (once the both-sides-2 case above is generalized) a chain on both sides at once.
  - [ ] Leave dissolve-of-a-real-crossing (`notYetImplemented`) and `findShardBoundary`'s `ambiguous` verdict untouched — both explicitly out of scope for this row.
  - [ ] **Done when:** `buildCrossingLegs` builds a full port/leg chain for a path of any length on either or both sides, unit-tested at depth >= 2 hops on at least one side; full ephemera suite and typecheck green.

## Verification

- **Ephemera:** `npm --prefix lambda/ephemera run test -- --watchAll=false`. Run the full suite, not a filtered subset — `*.integration.test.ts` files sit outside `tsconfig`, so `tsc` does not cover them.
- **Typecheck:** `npx tsc --noEmit` in `lambda/ephemera`, `packages/mtw-interfaces`, and `packages/mtw-gateways`.
- **Client:** `npm --prefix charcoal-client run test:single` (Vitest; not Jest, `--testPathPattern` does not apply).
- **The vertical itself:** PV1-0's observations, taken by hand in a running client at PV1-4. No unit test substitutes for this.

## When this finishes

Per [`taskPlanning/AGENT.md`](../../AGENT.md): move anything durable into the code-adjacent `AGENT.md` files under [`dataSource/positions/`](../../../lambda/ephemera/dataSource/positions/), then delete this plan. PV1-5 is what makes that safe.
