# Edge-chain prototype vertical (PV-1)

**Status:** In progress. **PV1-2 and PV1-3 are both done at the mechanism/unit-test level.** PV1-2's own top-level checkbox stays open: the "observed in the running client" clause of its **Done when** is owed to PV1-4's readout run, not this slice — same is true of PV1-3, which builds `tie rope to cup`'s crossing legs and port record but has not yet been run live. **Live-tested 2026-09-01: `tie string to cup` fails** ("I couldn't understand that command") — root cause traced and confirmed (see Open decisions below); the fix is bigger than the gap note PV1-3 originally left, so it's stubbed as **PV1-3b**, not yet designed. **Next step: refine PV1-3b's Open decisions with the user before building it**; PV1-4's readout is blocked on PV1-3b, not just owed a live run.

**This is an implementation plan.** It follows [`taskPlanning/AGENT.md`](../../AGENT.md) and **not** [`AGENT.designVariant.md`](../../AGENT.designVariant.md): code here is licensed by shipping, not by a tier; resolved decision rows are removed, not graduated; Progress is a running log, not a phase-grain view.

Area commands: [`taskPlanning/charcoal-client/AGENT.development.md`](../../charcoal-client/AGENT.development.md) for the client (Vitest, `npm run test:single`).

## Why this initiative exists

Three prototypes are shipped and inert: port machinery with no producer, `edgeId` (EA-8) minted by nothing, and `chainId` (P8 iteration 1) written by nothing. Each needs an observation that is only reachable once the other two are live. P8's rollback trigger (*a write path must decide same chain or not*) cannot fire, since no write path can express chain membership at all.

This vertical is the smallest configuration that makes all three observable at once. It is not a design initiative and settles no design question; it produces one readout (below).

## Mechanism this vertical builds on

[AB-54](dataSource/positions/AGENT.abstractionLayers.planning.md#settled-register) is settled: hosting kinds (`On`, `In`, `PartOf`) put the subordinate node in the superior's own shard (a cup `On` a tray is a member of the tray's `ludicGraph`, not the room's); peer kinds (`Under`, `Against`, `Custom`) leave both endpoints in one graph and host nothing.

Consequences that shape the build order:

1. Putting the cup on the tray requires the tray to be a host with its own persisted `ludicGraph` — [CD2h](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order). `In`/`PartOf` are deferred; one hosting kind is enough to make a boundary.
2. [`roomObjectCatalogForCharacter.ts`](../../../lambda/ephemera/dataSource/actions/roomObjectCatalogForCharacter.ts) enumerates the room's own `objectIds` only, so a hosted cup becomes unnameable unless referent search is extended to walk into hosted objects' graphs.
3. A `Custom` relation across that boundary ("tie the rope to the cup", rope in the room, cup in the tray's shard) cannot be one edge — it needs a port and produces two legs (room-side, tray-side). That is the first multi-leg chain the system will hold.

**Build order is 1→2 inverted:** [CD2h](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order) is gated on [CC3](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order) because a hosted cup vanishes from flat-catalog referent search with no guard to fire. Referent search (PV1-1) goes before hosting (PV1-2), built against hand-built nested fixtures — the graph already round-trips hosting shape (CD1 found `In`/`PartOf` persistence-tested but inert).

## What already exists to build on

- **Hosting, partly.** `On` is unauthorable today: [Channel D](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order) exists to make `On` root-anchored per AB-54; CD0/CD1/CD2 have landed, and CD2 retired `On` at the ingress lane ([`normalizeRelationSpan.ts:10`](../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/normalizeRelationSpan.ts) defers `on`/`onto`/`on top of`). The shard-hosting mechanism (CD2h) is unbuilt and gated on CC3, which PV1-1 discharges.
- **The move operation**, extended by PV1-2. [`executeObjectMove.ts`](../../../lambda/ephemera/dataSource/positions/manipulation/membership/executeObjectMove.ts) is the single call site for every object membership move, takes hosts rather than a verb (take-hold `room -> character`, drop `character -> room`, give `character -> character`), and re-derives the carry closure and boundary sweep fresh at commit time.
- **The step sequence a compiler can emit.** [`applyObjectRelationalChange.ts`](../../../lambda/ephemera/dataSource/positions/manipulation/relational/applyObjectRelationalChange.ts) commits `[dissolveRelation*, transferMembership, establishRelation]` through `commitStepSequence` as a repair path (see PV1-2 for why that is distinct from the hosting model).
- **Port shape, with no producer.** Types, terminal union, and matcher shipped when the ports plan closed 2026-08-23; nothing calls `withPorts` outside tests. `EphemeraPresencePort` gets its producer at PV1-2, `EphemeraCrossingPort` at PV1-3.
- **`chainId`, carried and compared, written by nothing.** The field round-trips through storage and `edgesMatch` consults it; the mutation vocabulary in [`manipulation/types.ts`](../../../lambda/ephemera/dataSource/positions/manipulation/types.ts) cannot express it.
- **The commit path already mixes item kinds.** `transactWrite([multiKeyItem, ...adjacencyItems])` — sibling records in one transaction is an established shape.

## The readout

Specified before the code (PV1-0), since PV-1's value is that it can produce an observation.

**The run**, taken by hand in a running client with the Dynamo console open, at PV1-4:

1. Order a table, a cup, and a string from Acme.
2. `put cup on table` (PV1-2).
3. `tie string to cup` (PV1-3) — the first chain the system will have held. This step is also PV1-1's test: if referent search cannot find the cup ("you don't see a cup"), PV1-1 broke, not PV1-3.
4. Read the four records below.

**Records to read.** No `EDGE#` item — PV-1 mints no Edge record, so both ids ride as optional fields on the legs themselves.

| Record | Where | What should be in it |
| --- | --- | --- |
| Room | `Meta::Room` on VORTEX, `ludicGraph` | table and string as nodes; the room-side leg, `string -> port` |
| Table | `Meta::Object` on the table, `ludicGraph` | the cup as a member node; its containment edge `cup -> root`, kind `On`; the crossing-port record (`fromHostId: ROOM#VORTEX`, stored interior-side); the table-side leg, `port -> cup` |
| Cup | `Meta::Object` on the cup, `ludicGraph` | its own root node (concepts clause 3), and one presence port — `{ fromHostId: OBJECT#<table>, kind: 'Present' }` |
| Adjacency | `EphemeraId: OBJECT#<cup>` | `DataCategory: POSITION#OBJECT#<table>` |

**Clauses to check against the run:**

- **Chains work** if both legs carry the same `chainId` and the same `edgeId`, and the string and the cup are both still nameable. Compare the two legs' fields directly — a leg count alone can read as success while the `chainId`s differ.
- **Chain identity is the wrong model** if PV1-3 cannot set the second leg's `chainId` without a fact no caller possesses, or if the ids have to be reconciled at read time rather than carried. Record this the moment it happens during construction, not only if it shows up in the dump.
- **The presence port earns its place** if the cup's presence port answers something the adjacency row does not — read the two rows side by side. If they say the same thing in two places, that is evidence for [AB-55](dataSource/positions/AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) (*does the presence port subsume adjacency?*) and grounds for taking the port back out.

**What this run cannot observe** — name these at PV1-4 rather than letting the silence read as coverage:

- The find path: PV-1 mints first chains only. A second route over an existing relationship needs an Edge record to find, and there is none.
- A relation that predates its boundary: optional second pass, tie the string first, then put the cup on the table (PV1-2's boundary sweep will most likely untie it — run it anyway and record which way it went).
- **Chain/edge identity, at all** (scope cut during PV1-3, confirmed with the user, not an oversight): PV1-3's legs carry no `chainId`/`edgeId` — building the general N-leg vocabulary for identity was judged not necessary for a first iteration, which can be inline/unidentified. The "Chains work"/"Chain identity is the wrong model" clauses above are therefore **not observable this vertical** — a later slice that actually mints `chainId`/`edgeId` on crossing legs would need to pick this up, against a settled readout run rather than reopening this one.

**Also not built, deliberately, this slice:** PV1-3's crossing-port producer handles at most one extra hop per side (chains where both `subjectPath`/`targetPath` length ≤ 2, and not both extra-hop at once) — a middle leg between two port addresses, needed once one side is nested two-deep, would hit `applyStepSequenceCore`'s BD-33 derive-don't-carry host resolution, which cannot resolve a leg with no primitive endpoint. Reports `notYetImplemented` rather than emitting a step that would throw at apply time. The live ingress route (`compileRelationalFromSkeleton.ts`) is not wired to this mechanism at all yet — its own one-hop pre-fetch and its seed's redundant sibling relational step (which would double-emit an invalid direct edge alongside the crossing legs) both need addressing first; see that file's own comment at the seed-construction site.

## Progress

| Date | Note |
| --- | --- |
| 2026-08-30 | Seeded from conversation. Nothing built. |
| 2026-08-31 | Hosting reframed: a rehost carrying a containment argument, not a relation with a side effect. Nothing built. |
| 2026-08-31 | Presence ports added: one port, no `Present` edges. PR-10 collapses the cover's edge set, not the port record. CD6 is crossing-only. Nothing built. |
| 2026-08-31 | Open decisions resolved; referent search and hosting order swapped per the CC3 gate. Nothing built. |
| 2026-08-31 | PV1-0 done: table/cup/string manual run, four records specified. Found while checking it: the cup's own row and the adjacency row both carry clause 3; Acme delivery does not go through `executeObjectMove`, so the spawn path's missing ports are an uncovered path, not clause-3 evidence. Nothing built. |
| 2026-08-31 | PV1-1b added: room-only entry points are not required by any live rule (`AGENT.implementation.md`'s citation of `D14` is undefined in the repo, and the atomicity gap it traces to was closed by the `MultiKeyUpdate` migration). Presence-port mint location pinned at PV1-2. Nothing built. |
| 2026-08-31 | PV1-1b re-scoped: room-level membership joins the same pipeline as play-level, and the scale-specific mutators are deleted (not generalized in place). The op layer and kernel are already host-general and nullable-destination; all three narrowings sit in the ring between them. PV1-2's singular-`fromHostId` bullet corrected to match. Nothing built. |
| 2026-08-31 | PV1-1 done: `roomObjectCatalogForCharacter.ts` walks into hosted objects' own `ludicGraph`s (`collectNestedObjectIds`, BFS, depth cap 5, visited set for cycles), each object fetched via `internalCache.Positions.getLudicGraph`. Tested against hand-built fixtures (nested-cup regression test, depth-cap boundary, cyclic-fixture termination). Scope is the room catalog only — `heldInventoryCatalogForCharacter.ts` is left flat, deliberately, since neither this slice's done-when nor the readout's test step needs held-object nesting; CC3's fuller discharge (both catalogs) is future work. `roomObjectLabelsForCharacter.test.ts`'s shared deps fixture needed the same `getObjectLudicGraph` no-op default the new recursion introduced. Full ephemera suite and `tsc --noEmit` (ephemera, mtw-interfaces) green. |
| 2026-08-31 | PV1-1b done: `executeMembershipTransfer` (new function, `executeObjectMove.ts`) absorbed `applyObjectRoomMembership`/`applyObjectClearMembership`/`applyCharacterRoomMembership`'s membership half; those three plus the `manipulation/adapters/` planner deleted outright. The checklist's own two type-widening bullets turned out to be unneeded (would have pushed generality into the unrelated Grounding/Expansion pipeline) — found and corrected mid-implementation, recorded above. All five call sites migrated; docs updated (contract, implementation, concepts, objects/AGENT.md); Section B of `manipulation/AGENT.implementation.md` marked superseded rather than rewritten. Full suite (343/344 suites, 2699 tests) and full typecheck green. |
| 2026-08-31 | PV1-2 server-side mechanism done; client wiring deferred (see the row's own unchecked sub-items). `containment` threaded through `ExecuteObjectMoveArgs`/`BuildObjectMoveOpArgs`/`PositionKernelMoveOp`/`OrchestrateObjectMoveArgs`; `compilePositionKernelOp` emits `establishRelation` after the transfer (endpoint resolution needs the transfer to have already run). Presence-port minting needed a new kernel step kind (`MutationKernelSetPresencePortStep`) rather than a plain field write — the port lives on a third graph (the moved object's own) outside `fromHostId`/`toHostId`'s footprint, so it needed the same footprint/commit/facts wiring `establishRelation`/`dissolveRelation` already have; `EphemeraLudicGraph` gained `addPort`/`removePort` to back it. Found while building: no explicit "mint a ludicGraph" step was needed — `fromObjectMeta`'s existing missing-field default already produces a fresh rooted graph on read. Found and fixed a **pre-existing bug** in `orchestrateObjectMove`: it derived `roomId` by scanning `[fromHostId, toHostId]`, silently no-opping on any move whose destination isn't a room (every containment move) — fixed by taking `roomId` explicitly. Client-side type plumbing landed (`normalizeRelationSpan`/`matchRelationalTemplate`/`classifySkeletonFamily` now carry `On` vs `In`/`PartOf` through to `parseCommand.ts`'s dispatch point), but `parseCommand.ts` itself still hard-errors for `On` — routing it to `orchestrateObjectMove` needs a new object-span resolution pipeline, scoped out rather than built hastily; see the row's client-wiring bullet. New tests: `executeObjectMove.test.ts`'s `PV1-2: On rehost` suite (put-on-empty-tray, tray-to-tray rehost with no executor throw, take-off with no containment), `compilePositionKernelOp.test.ts`'s containment/presence-port cases, `ludicGraph/index.test.ts`'s port mutator cases, `orchestrateObjectMove.test.ts`'s roomId-fix and containment-threading cases, plus updated client-layer defer-kind assertions. Full suite (343/344 suites, 2714 tests) and full typecheck (ephemera, mtw-interfaces, mtw-gateways) green. |
| 2026-08-31 | PV1-2's client-wiring gap corrected (conversation, no code): "new object-span-to-id resolution pipeline" overstated it. Traced the actual pipeline (`groundReferent.ts`/`resolvedSpansFromPools.ts`/`identityStage.ts`/`resolveCatalogSpanToPool.ts`) and confirmed Identity already resolves nested ids with no residency check, fed by PV1-1's `collectNestedObjectIds` via the flat `roomObjectCatalog` — this is shared machinery, already live for ordinary peer-relational commands, not something PV1-2 still needs to build. The real remaining gap is a short-circuit: take Identity's two resolved ids for an `On`-kind defer and call `orchestrateObjectMove` directly, skipping Grounding/Expansion/Validation (`compileRelationalFromSkeleton.ts`), which solve peer-relation-specific problems (candidate combinations, same-host boundary-edge legality) that don't apply to a rehost. Row's own text corrected in place. Nothing built. |
| 2026-08-31 | PV1-2 done: live-client observation (`put cup on table`, `get cup`, `drop`) confirmed all four `Done when` clauses, including both gone when the cup leaves. Two bugs found and fixed in the process: `applyStepSequenceCore`'s establishRelation/dissolveRelation host resolution used first-match-per-endpoint semantics (`findHostOf`), which mis-resolved the shared host whenever a hosting-kind target sat in two locked footprint graphs at once — the table is simultaneously a room member and its own shard's root — replaced with an intersection-based `findSharedHost`/`hostsOf` pair, kind-agnostic, no relation-kind special-casing; and `Object Take Hold`'s handler in `positions/index.ts` trusted `content.roomId` as the object's current host, which broke once objects can nest (a cup left `On` a table is a node of the table's graph, not the room's) — fixed by resolving `fromHostId` fresh via `getMembershipContainers`, mirroring `Object Rehost`'s existing fix. New regression tests: `applyStepSequenceCore.test.ts`'s AB-54 intersection case, and two new `Object Take Hold` cases in `receivePaths.integration.test.ts`. Full suite (344/345 suites, 2743 tests) and full typecheck green. |
| 2026-08-31 | PV1-2's client wiring done: `classifySkeletonFamily.ts` forwards `operationKind`/`subject`/`target` on `relationalDefer`; `parseCommand.ts` routes `kind === 'On'` to a new `compileObjectRehostFromSkeleton.ts` (Identity only, no Grounding/Expansion/Validation); a new `ParseCommandObjectRehostResult`/`Object Rehost` published-payload/subscribed-envelope trio carries the result across the `mtw.ephemera.actions` → `mtw.ephemera.positions` publish/subscribe boundary (confirmed no in-process shortcut exists — the two are separate `DataSource` registrations with independently-scoped `streamEvent`s); `positions/index.ts`'s new branch resolves `fromHostId` fresh via `getMembershipContainers` at execution time rather than trusting a parse-time value, then calls `orchestrateObjectMove`. Found while building: `runIdentityStageOverSkeleton` (already used by `compileRelationalFromSkeleton.ts`) resolves both referents in one call since a 4-token skeleton has exactly two object spans — no separate two-referent resolver needed, contrary to what the prior correction implied might be required. Scope cuts, deliberate and recorded in the row itself: ambiguous (multi-candidate) resolution errors out; `take cup off tray` (dissolve, no containment) untouched. Tests added at every layer (parse, publish, subscribe/execute, and the established real-messageBus integration convention); full suite (344/345 suites, 2730 tests) and full typecheck (ephemera, mtw-interfaces, mtw-gateways) green. |
| 2026-09-01 | PV1-3's shard-boundary predicate and crossing-port producer rows (117–118) generalized in conversation, before code: instead of a fixed one-hop room/table case with a hardcoded two-leg producer, both endpoints' ancestry is walked via recursive `getMembershipContainers` (no single-parent assumption at any depth — a host can itself be multiply-contained, e.g. a cup on a table *and* in a place-setting, so the call is made uniformly at every node, not just at the two starting endpoints) up to Room, giving each endpoint a DAG rather than a chain; the closest-common-ancestor comparison between the two DAGs yields zero (not-found), one (proceed), or more-than-one (not-yet-implemented, reported) outcomes. The crossing-port producer generalizes from "always exactly two legs" to (a) egress legs from the subject up to the common ancestor, (b) one leg within the common ancestor's own graph, (c) legs from the common ancestor down into the target — the readout's own room/table case is the depth-1 instance, unchanged in shape. Deliberately correctness-first over efficiency: the recursive-fetch walk is expected to shrink once a `ludicCache` exists; built general now rather than single-parent now and generalized later. Nothing built. |
| 2026-09-01 | PV1-3 done. Three corrections made during plan review, before code, on top of the previous day's generalization: (1) "closest common ancestor" is the Pareto-minimal frontier of common nodes under (subjectDepth, targetDepth) domination, not a combined-depth minimum — genuinely incomparable ancestors can both survive, not just multi-start convergence on one node. (2) Legs don't need a new kernel step kind — `establishRelation`/`dissolveRelation` were already leg-shaped once their endpoint type widened from `EphemeraLudicTerminalPrimitive` to `EphemeraLudicTerminalId` (matching `HostRelationalEdge`'s already-widened `from`/`to`, LP7); the only genuinely new step is for the `EphemeraCrossingPort` record itself. (3) No `chainId`/`edgeId` minting this slice (confirmed with the user, overriding the previous day's rows 120-121/125-126) — legs stay inline/unidentified; PV1-0's readout gained a "cannot observe" line naming the consequence. Built: `findShardBoundary.ts` (the general ancestry-DAG walk, Pareto-minimal-frontier comparison), `buildCrossingLegs.ts` (the leg/port producer, scoped to ≤1 extra hop per side — a genuine `applyStepSequenceCore` BD-33 limit found while implementing, reported as `notYetImplemented` beyond that rather than emitting a step that would throw at apply time), `MutationKernelAddCrossingPortStep`/`MutationKernelRemoveCrossingPortStep` (the port-record kernel step), a `findSharedHost`/`hostsOf` widening for port-address endpoints (a port's `owner` field is a host *candidate*, not a veto — found necessary once legs could have a port-address endpoint), and `expandSameHost`'s new `'crossed'` verdict wired into `commandExpand`/`runExecutor` via a new `extraKernelSteps` side-channel (kernel-only steps can't ride the executor's `ExecutorParsePlanStep`-typed worklist, the same reason `MutationKernelSetPresencePortStep` is compiler-emitted rather than executor-emitted). The verb-lexicon fix (row 116) turned out to be server-side (`matchRelationalTemplate.ts`/`relationalTemplates.ts`), not client-side as originally labeled — "tie" added to `ESTABLISH_VERBS` in both. Two gaps named, not fixed, this slice: `applyObjectRelationalChange.ts`'s port-qualified-edge skip (checked — never routed through by a no-transfer relation like `tie`, so left alone) and the live ingress route (`compileRelationalFromSkeleton.ts`, still one-hop-only pre-fetch, and its seed's sibling relational step would double-emit alongside a real crossing — documented in a comment at its own seed-construction site, not fixed). Full ephemera suite (346/347 suites, 2770 tests) and full typecheck (ephemera, mtw-interfaces, mtw-gateways) green. |
| 2026-09-01 | Live-tested `tie string to cup`: fails with the generic "I couldn't understand that command." Traced to root cause (not a new bug — the named gap PV1-3 left, just bigger than its own note suggested): `compileRelationalFromSkeleton.ts` drops the candidate before `expandSameHost` can ever reach `'crossed'`, and even if it did, the entire downstream commit path (`ParseCommandEstablishRelationResult` → `Object Establish Relation` publish/subscribe → `executeObjectEstablishRelation` → `applyObjectRelationalChange`) is shaped for exactly one relation on exactly one host — there's nowhere for a multi-leg, multi-host step sequence to go. Stubbed as **PV1-3b** below, with six open design questions (PV1-3b-1 through PV1-3b-6) — not designed, not built; the user asked to refine it together before building, not have it built now. Nothing built.
| 2026-09-01 | Revisited BD-33 in conversation, prompted by the user questioning whether relations can always derive their host from endpoints alone. Confirmed two counterexamples are real, not hypothetical: two endpoints multi-hosted in the same ≥2 graphs (the same shape `findShardBoundary`'s already-flagged `'ambiguous'` verdict names, and that `findSharedHost`'s intersection fix silently assumes away), and port-to-port on one object where interior-vs-exterior scope isn't recoverable from the two port addresses at all. Traced the pipeline stage by stage (parse → identity → `compileRelationalFromSkeleton` seed → `expandSameHost`/`buildCrossingLegs` → step vocabulary → `applyStepSequenceCore`) and found Expansion is the only stage that already computes a resolved host (via `expandSameHost`'s verdict and `buildCrossingLegs`'s per-leg placement) before that value is discarded — the kernel step types don't carry it, so `applyStepSequenceCore` re-derives it from scratch via `findSharedHost`, which is exactly what breaks on the counterexamples. **Decided (PV1-3b-7):** carry a mandatory `hostId` on relational steps, computed once at Expansion and surfaced rather than dropped; narrow `applyStepSequenceCore`'s `findSharedHost` from resolver to assertion (still throws — BD-33's discipline is preserved, only the computation moves). The general multi-boundary "which containers benefit from surfacing this edge" question (the `'ambiguous'` verdict, multi-hop-both-sides) is explicitly deferred as a separate, likely LLM-reasoned problem, easier to take on once a single resolved host can be carried at all. PV1-3b's sub-tasks fleshed out with this conclusion; PV1-3b-1/2/3 remain open.
| 2026-09-01 | PV1-3b-1 and PV1-3b-3 narrowed in the same conversation, once PV1-3b-7 made clear `compileRelationalFromSkeleton.ts` already holds Expansion's full, host-attributed step list in scope before `ParseCommandEstablishRelationResult` is built. **PV1-3b-1 decided:** widen that result type itself (not a sibling type) to carry the step list — only its field shape is still open. **PV1-3b-3 refined, not fully decided:** execute-side needs a freshness check against the parse→execute gap (mirroring `Object Rehost`'s fresh host re-resolution, PV1-2), but as a structural re-check of the *specific* legs Expansion already chose (their carried `hostId`s and referenced ports still hold), not a re-run of `findShardBoundary`/`buildCrossingLegs`'s search — fail the crossing on mismatch rather than silently re-deriving a different route. PV1-3b-2 (the publish/subscribe event pair) untouched at the time. Nothing built.
| 2026-09-01 | PV1-3b-2 decided: widen `Object Establish Relation`'s existing published payload/envelope rather than add a new event pair — consistent with PV1-3b-1's widen-not-sibling-type call. Only PV1-3b-3 (the execute-side function's shape and its relationship to `executeObjectEstablishRelation`/`applyObjectRelationalChange`) remains open in PV1-3b. Nothing built.
| 2026-09-01 | Chased down whether `applyObjectRelationalChange`'s repair-by-relocation path (`expandSameHost`'s `'repaired'` verdict) is a real requirement of `Under`/`Against` or an artifact of crossing being gated to `Custom` only (`expandSameHost.ts:85`). **Confirmed: an artifact, not a requirement — `Under`/`Against` are definitely meant to get crossing too**, not scheduled now but recorded as wanted expressiveness. Example given: a fork `Under` a chandelier, then wrapped in a "Place Setting" abstraction (hosted inside it) — the fork no longer shares a host with the chandelier, but should still be able to express `Under` via a port-crossing rather than being forced to relocate out of the Place Setting to stay under the chandelier. Consequence for PV1-3b-3: `applyObjectRelationalChange`'s transfer-repair branch is not a permanent fixture — once `Under`/`Against` get the same crossing path `Custom` has, `'repaired'` may become unreachable and that branch could retire outright. Not acted on this slice; recorded so PV1-3b-3's design doesn't accidentally treat the repair branch as load-bearing long-term. No new task-plan row opened — genuinely future, unscheduled work, per the user's own framing. Nothing built.
| 2026-09-01 | PV1-3b-3 settled (leaning on containment structure): leg-time validation is genuinely sufficient — legality of a whole crossing reduces to legality of its individual legs, so no separate whole-crossing verification layer is needed. PV1-3b-7's per-step assert (`applyStepSequenceCore`, given a carried `hostId`) already re-checks each leg against freshly-fetched graphs at commit time; the new execute-side function is a pass-through (merge `steps`/`extraKernelSteps`, build `getCurrentHost` from carried hosts, call `commitStepSequence`). Four sub-items remain before PV1-3b-3 itself is fully scoped: the `steps`/`extraKernelSteps` merge order, where the function is called from (`executeObjectEstablishRelation` branch vs. new sibling entry point), whether `executeObjectEstablishRelation`'s existing silent-swallow of `applyObjectRelationalChange`'s result needs fixing (found in conversation), and reconfirming the port-qualified-edge skip stays out of scope. PV1-3b's Open decisions are now: PV1-3b-4/5/6 (unchanged, seed-cancellation/deep-prefetch/relationLabel), PV1-3b-3's four sub-items, and the deferred-not-scheduled `Under`/`Against` crossing generalization. Nothing built.
| 2026-09-01 | PV1-3b-3's remaining three sub-items decided. **Merge order:** `addCrossingPort` step(s) first, then legs — legs are mutually independent but each depends on its referenced port already existing; nothing else precedes them. **Function identity:** a new sibling function, `executeEstablishEdgeChain`, not a branch inside `executeObjectEstablishRelation` — matching `executeMembershipTransfer`'s relationship to `executeObjectMove` and `Object Rehost`'s own precedent (PV1-1b/PV1-2). **Silent-swallow bug:** left unfixed, deliberately — `executeObjectEstablishRelation` may itself be a retirement candidate once crossing is lifted out to its own sibling (open question, not settled), so fixing a bug in code that might not survive isn't worth doing yet. PV1-3b-3 is now fully decided; PV1-3b's only remaining open items are PV1-3b-4/5/6 and the deferred `Under`/`Against` generalization. Nothing built.

## Open decisions (implementation --- plan only)

Plan-only: decisions we are making in order to implement the next slice(s).
Do not copy into package `AGENT.concepts.md`. When a decision ships, record it
in `AGENT.contract.md` / `AGENT.implementation.md` and remove the row here.

| ID | Decision | Blocks slice | Status |
| --- | --- | --- | --- |
| PV1-3b-1 | Where does a multi-host crossing's result live: widen `ParseCommandEstablishRelationResult` (`baseClasses.ts`) to optionally carry a step list, or add a sibling result type — mirroring PV1-2's separate `ParseCommandObjectRehostResult` rather than widening `ParseCommandEstablishRelationResult` itself? Narrowed in conversation 2026-09-01, given PV1-3b-7: `compileRelationalFromSkeleton.ts` already holds the full, Expansion-derived, now host-attributed step list (`steps` + `extraKernelSteps`) in scope by the time it builds this result — there is no separate execute-side derivation to make room for. **Decided: widen `ParseCommandEstablishRelationResult` itself** (not a sibling type) to carry that step list; what remains open is only the field shape, not whether/when the data exists. | PV1-3b | Decided (not yet built) |
| PV1-3b-2 | New publish/subscribe event pair for the crossing commit (mirroring `Object Rehost`'s own trio), or widen `Object Establish Relation`'s existing payload/envelope? **Decided 2026-09-01:** widen the existing `Object Establish Relation` payload/envelope, matching PV1-3b-1's widen (not sibling-type) call on `ParseCommandEstablishRelationResult` itself. | PV1-3b | Decided (not yet built) |
| PV1-3b-3 | New execute-side function that commits a `MutationKernelStep[]` across every host it touches via `commitStepSequence` (footprint-locking all of them in one transaction) — how does it relate to `executeObjectEstablishRelation`/`applyObjectRelationalChange`, both single-host today? Does `applyObjectRelationalChange.ts`'s port-qualified-edge skip (PV1-3's row, checked-not-fixed) become live here? Refined in conversation 2026-09-01: the parse→execute gap needs *some* freshness check — the world can change between publish and subscribe, same reason `Object Rehost` re-resolves its host fresh at execute time rather than trusting a parse-time value (PV1-2) — but **not** by re-running `findShardBoundary`/`buildCrossingLegs`'s search. **Settled further, same day: leg-time validation is genuinely sufficient — no separate execute-side check needed.** PV1-3b-7's per-step assert (`applyStepSequenceCore`, given a carried `hostId`, confirms both endpoints belong to it against freshly-fetched graphs inside `commitStepSequence`'s `MultiKeyUpdate`) already re-verifies every leg against live state at commit time; the containment *structure* is what lets whole-crossing legality reduce to per-leg legality, with no separate whole-crossing check layered on top. This new function is therefore a pass-through: merge `steps`/`extraKernelSteps` into one `MutationKernelStep[]`, build `getCurrentHost` from each step's own carried `hostId`, call `commitStepSequence`, and let the kernel's own assert fail the transaction (and thus the whole crossing) if anything went stale. **Fully decided, same day:** merge order is crossing-port steps first, then legs — the legs are independent of each other but each depends on the port they reference already existing; nothing else needs to precede the legs. This is a new sibling function, `executeEstablishEdgeChain` (not a branch inside `executeObjectEstablishRelation`), matching `executeMembershipTransfer`'s relationship to `executeObjectMove` and `Object Rehost`'s own branch precedent. `executeObjectEstablishRelation`'s silent-swallow bug is **left unfixed for now** — deliberately, not an oversight: with crossing lifted out to its own sibling, `executeObjectEstablishRelation` itself may be a retirement candidate (open question, not yet decided — see PV1-3b's own `Under`/`Against` note above about `applyObjectRelationalChange`'s repair branch), so fixing a bug in code that might not survive isn't worth doing yet; revisit if/when `executeObjectEstablishRelation`'s own fate is settled. | PV1-3b | Decided (not yet built) |
| PV1-3b-4 | `compileRelationalFromSkeleton.ts`'s seed pairs `[sameHostAssertion, relationalStepNoHost]` per candidate; the sibling step retires unmodified for `satisfied`/`repaired`, which is correct there, but must NOT for `crossed` — a direct subject→target edge is never valid once the relation has to cross a boundary. The executor's worklist has no way to cancel a sibling instruction once seeded. Restructure so a crossing bypasses the executor's worklist for that candidate (calling `expandSameHost` directly before building the seed, as PV1-3's own comment at that seed-construction site anticipates), or find another mechanism? | PV1-3b | Open |
| PV1-3b-5 | The live route's `getMembershipContainers` pre-fetch (`compileRelationalFromSkeleton.ts`) is one-hop only; `findShardBoundary`'s walk needs to keep going past intermediate hosts. Pre-fetch each endpoint's full ancestry (async, before constructing the synchronous `ExpansionEnvironment`), or find a different sync/async boundary for this route? | PV1-3b | Open |
| PV1-3b-6 | `sameHostAssertion` doesn't carry `relationLabel` today even though `GroundedBinaryAssertion` supports it now (PV1-3) — needed for a `Custom` crossing's port `exteriorRelationLabel`/leg label. Trivial thread-through, listed because it lives at the same seed-construction site as PV1-3b-4. | PV1-3b | Open |
| PV1-3b-7 | Where does a leg's target `hostId` get resolved and carried, given `applyStepSequenceCore`'s `findSharedHost` intersection can't disambiguate when endpoints share more than one common host (two objects multi-hosted in the same ≥2 graphs) or admit more than one legitimate scope (port-to-port on a single object — interior to that object's own graph, or exterior in wherever it's currently hosted)? Raised in conversation 2026-09-01 (not from live testing). **Decided:** `hostId` becomes a mandatory field on `ExecutorEstablishRelationStep`/`ExecutorDissolveRelationStep` and the corresponding `MutationKernelStep`, matching the `hostId` field `MutationKernelAddCrossingPortStep`/`RemoveCrossingPortStep` already carry. It is computed once, at Expansion, not re-derived at apply time: `expandSameHost`'s `'satisfied'`/`'repaired'` verdicts already resolve a `hostId` internally and currently discard it when `commandExpand` builds the executor step — that value should be surfaced instead; `buildCrossingLegs` already has to pick a specific target graph per leg to place the crossing port and each leg correctly — that per-leg host should be surfaced the same way. `applyStepSequenceCore`'s `findSharedHost`/`hostsOf` narrows from resolver to assertion: given the carried `hostId`, confirm both endpoints actually belong to that graph, throw if not — this keeps BD-33's assert-and-throw discipline (checking a precondition at apply time) rather than reintroducing derive-from-endpoints, which is what made the two counterexamples above unresolvable in the first place. Explicitly **out of scope**: the general N-boundary question of which of several legitimate crossing routes an edge should be routed through / highlighted in (multi-hop-both-sides, and the `findShardBoundary` `'ambiguous'` verdict) — that's a later, likely LLM-reasoned selection problem, easier to take on once a single resolved host can be carried unambiguously at all. This decision only supplies the field and the assert-not-derive discipline; it does not attempt to resolve the still-open ambiguous cases. | PV1-3b | Decided (not yet built) |

## Recommended order

Pending work is `- [ ]` and completed work is `- [X]`; mark nested lines `[X]` as each is done, so partial progress on a slice is visible. Updating these checkboxes is part of *done* for the slice, after tests pass.

- [X] **PV1-0. Write the readout, before any code.** Done 2026-08-31 — see [The readout](#the-readout).
- [X] **PV1-1. Referent search survives nesting.**
  - [X] Extend the catalog to walk into hosted objects' graphs by Dynamo read recursion. No `ludicCache` — a correct recursive read discharges CC3's prerequisite and a cache can substitute later behind a stable interface.
  - [X] Depth cap of five (testing bound, not a claim about real nesting depth) plus a visited set (terminates cyclic graphs).
  - [X] Build against hand-built nested fixtures — the graph already round-trips hosting shape (CD1: `In`/`PartOf` persistence-tested but inert).
  - [X] **Done when:** a cup nested in a tray fixture can be named and acted on from the room, with a test that fails against the pre-nesting enumeration.
- [X] **PV1-1b. Consolidate object-lifecycle membership onto the instruction / planner / compiler / kernel pipeline; retire the scale-specific mutators.** Done 2026-08-31.
  - [X] **Correction found during implementation:** widening `parsePlanStep.ts`'s `TransferMembershipStep` and `buildObjectMoveOp.ts` was **not done** — a usage survey showed both are read as scalars throughout the real player-command pipeline (Grounding, Expansion, relational repair), none of which touch the routes this slice migrates. `kernelStep.ts`'s own doc comment already states the design principle this would have violated: the Synthesize executor's step vocabulary stays object-only/unwidened by design, and `fromExecutorStep` widens *at the adapter boundary*. Left untouched.
  - [X] Added a new sibling function, `executeMembershipTransfer`, in [`executeObjectMove.ts`](../../../lambda/ephemera/dataSource/positions/manipulation/membership/executeObjectMove.ts) — single entity (object or character), diffed against its own fetched `priorContainers` (not a caller-supplied `fromHostId`/`toHostId` pair), no carry closure, never touches the Synthesize executor or `buildObjectMoveOp`. Object entities get an explicit per-host `boundaryEdgeOutcomes` sweep; character entities never do. Carries `errorCode`/`errorMessage` on failure and `{froms, to, changed}` on success (the result-shape gap the original checklist named on `ExecuteObjectMoveResult` was instead solved here, since none of the migrated call sites use `executeObjectMove` itself). `executeObjectMove` (the carry-closure, single-origin real-transfer function for take-hold/drop/give) is unchanged.
  - [X] Threaded `suppressRelationalFacts` and `compileMutationSteps` through `executeMembershipTransfer` (not `executeObjectMove`, which no migrated call site uses).
  - [X] Deleted `applyObjectRoomMembership`, `applyObjectClearMembership`, and the adapters `planMembershipTransfer`, `computeEndStateRoomDiff`, `planObjectClearFromAllHosts` (and the now-dead `ObjectMembershipApplyArgs`/`ObjectClearMembershipApplyArgs`/`MembershipTransferProjection`/`MembershipTransferPlan` types), plus their test files. `manipulation/adapters/` no longer exists.
  - [X] Moved five call sites onto `executeMembershipTransfer`: `spawnImprovisationObjectsBatch`, `repairObjectPlacementDrift`, `applyObjectsChange`, `clearCoyoteGameImprovisationObjects`, and `applyCharacterRoomMembership`'s membership half (kept as a thin wrapper: roster snapshots, `CharacterMeta` invalidation, `EphemeraUpdate` publish, and the `compileMutationSteps`/`narrationHandledInline` hooks navigate/connect/disconnect depend on). Room-stack machinery (`membershipRoomStack`, `mergeRoomStack`, `trimEvictionLadder`, `persistRoomStackNavigate`) confirmed untouched — it has zero references in `applyCharacterRoomMembership.ts` and lives one caller-hop away.
  - [X] Struck `AGENT.implementation.md:51`'s "do not extend room-only entry points" and the `D14` citation in `AGENT.contract.md:228`; also updated both files' file-role tables, the "Object room membership" and "Object membership-changed bundle (room-only)" contract sections, `AGENT.concepts.md`'s spawn/place/remove bullets, `objects/AGENT.md`'s spawn/clear prose, and `AGENT.CoyoteGame.implementation.md`'s one reference. Marked `manipulation/AGENT.implementation.md`'s Section B (the retired adapter writeup) `[SUPERSEDED, PV1-1b]` rather than rewriting it — full rewrite, the `bounded`-mode question, and `D8`/`D13`/`D14`/`D16` register-or-strike stay explicit PV1-5 work, per this row's own original deferral.
  - [X] **Done when:** every object membership change (spawn, place, remove, drift repair, clear, take-hold, drop, navigate, connect, disconnect) reaches the kernel through `executeMembershipTransfer` or `executeObjectMove`; the retired coordinators and adapters are deleted; full ephemera suite (343 suites / 2699 tests), `tsc --noEmit` (ephemera, mtw-interfaces, mtw-gateways) all green.
- [X] **PV1-2. `On` nests end to end, as a rehost carrying a containment argument** (not a relation with a side effect; establishing `On` moves nothing and there is no operation that "establishes `On`"). Done 2026-08-31.
  - [X] Added `containment?: 'On' | 'In' | 'PartOf'` to `ExecuteObjectMoveArgs`, `BuildObjectMoveOpArgs`, and `PositionKernelMoveOp` (hosting kinds only, AB-54). `OrchestrateObjectMoveArgs` gained it too.
  - [X] `compilePositionKernelOp` emits `establishRelation(primaryMovedId -> op.to, containment)` after `transferStep` (endpoint resolution needs the moved object already a node of the destination graph — before the transfer, `findHostOf` would resolve the old host). `MutationKernelTransferStep` left unwidened, per this row's own instruction — the containment edge commits through the ordinary `establishRelation` step, already a legal (if previously unemitted) kernel step kind.
  - [X] Presence port: **not** landed as a plain field mutation — no existing mechanism could write it transactionally, since it lives on a *third* graph (the moved object's own) outside `fromHostId`/`toHostId`'s footprint. Added a new kernel step kind, `MutationKernelSetPresencePortStep` (`kernelStep.ts`), wired through `computeStepSequenceFootprint`/`applyStepSequenceCore`/`factsForStep` (no fact — not a narration channel yet) exactly like `establishRelation`/`dissolveRelation` are. `compilePositionKernelOp` emits it whenever `op.moved.kind === 'closure'` (object closures only — the gate the checklist below asked for, achieved for free: character moves use `kind: 'entity'` and never reach this branch) and `op.to` is set: replaces any existing `kind: 'Present'` port with a fresh one naming the destination (presence is at-most-one, PR-10, so this is a *set* not an *add* — no read of the prior port needed). `EphemeraLudicGraph` gained `addPort`/`removePort` (mirroring the `addRelationalEdge`/`removeRelationalEdge`/`withEdges` pattern) to back it.
  - [X] Confirmed live in `buildObjectMoveOp`/`compilePositionKernelOp` (reaches `executeObjectMove` only, i.e. take/drop/give) — not `executeMembershipTransfer` (spawn/place/remove/drift-repair/clear), which is untouched by this row. Its own `boundaryEdgeOutcomes` call will still throw if it ever meets a hosting-kind boundary edge; none of its current callers can construct one, so this is an inert gap, flagged in its doc comment, not a live one.
  - [X] No explicit "mint a ludicGraph" step needed — already covered by existing behavior: `fromObjectMeta`/`fromPlainHostMeta` (`ludicGraph/index.ts`) already default a missing `ludicGraph` field to a fresh rooted graph (`{ rootId: hostId, nodes: [nodeFromId(hostId)], edges: [], ports: [] }`) on every read, both at `internalCache.Positions.getLudicGraph` and at `commitStepSequence`'s own `MultiKeyUpdate` fetch. Confirmed by reading `graphFromMeta`/`fromPlainHostMeta` directly rather than assumed.
  - [X] Old containment edge removed explicitly in `executeObjectMove.ts`, **before** the executor ever sees the graph — not via `classifyInteractionUnderTransfer`. Structural inspection (`edge.from === primaryObjectId && edge.to === fromGraph.rootId`, hosting kind) finds the moved object's own edge into the host it's leaving; stripped from the graph handed to `createExpansionEnvironment`, then prepended to `dissolvedEdges` so it still renders as an ordinary `dissolveRelation` step. This matters because the Synthesize executor's own operand-expansion calls `boundaryEdgeOutcomes` internally — leaving the edge in place would throw *inside* `runExecutor`, uncatchable from `executeObjectMove`. `classifyInteractionUnderTransfer`'s throw-comment narrowed to record this one shape now has a producer; the throw itself is untouched for every other case (LD-11 not retired, only bypassed for this one shape — CD3 below is partial, not full, discharge).
  - [X] `orchestrateObjectMove`'s **pre-existing bug**, found while wiring this: it derived both `characterId` and `roomId` by scanning `[fromHostId, toHostId]`, so any containment move (destination is an object, never a room) silently no-op'd. Fixed by taking `roomId` as an explicit required argument instead of deriving it; both existing take/drop call sites in `positions/index.ts` updated to pass it (`content.roomId`, already in scope). Verified against the existing take/drop tests before `containment` was threaded in, so the fix and the new feature are separable in the test history.
  - [X] **Client wiring done 2026-08-31.** `SkeletonFamilyMatch`'s `relationalDefer` variant (`classifySkeletonFamily.ts`) widened to forward `operationKind`/`subject`/`target` (previously dropped on the floor) alongside `kind`. `parseCommand.ts`'s `relationalDefer` branch now special-cases `kind === 'On'`: a new function, `compileObjectRehostFromSkeleton.ts`, resolves `subject`/`target` to object ids via the same `runIdentityStageOverSkeleton`/`resolvedSpansFromPools` pair `compileRelationalFromSkeleton.ts` already uses, then stops — deliberately **skipping** Grounding/Expansion/Validation, which solve peer-relation-specific problems (candidate combinations, same-host boundary legality) that don't apply to a rehost. `In`/`PartOf` are untouched (still the pre-existing hard error). A new result type, `ParseCommandObjectRehostResult` (`baseClasses.ts`) — deliberately separate from `ParseCommandEstablishRelationResult`, which narrowed `On` out on 2026-08-22 — carries `subjectId`/`targetId`/`hostId`/`containment`/`confidence` across the `mtw.ephemera.actions` → `mtw.ephemera.positions` publish/subscribe boundary (`streamEvent`/`messageBus` are not available at `parseCommand.ts`'s layer — confirmed by reading the two `DataSource` registrations directly — so this follows the same publish-then-resubscribe shape every other object-manipulation result already uses, not an in-process shortcut): a new `Object Rehost` published payload (`publishedEvents.ts`) and subscribed envelope (`subscribedEvents.ts`), and a new branch in `positions/index.ts`'s `receiveEvents`. That branch resolves `subjectId`'s **current** host fresh via `internalCache.Positions.getMembershipContainers` (erroring — a no-op, not a guess — on zero or multiple containers) rather than trusting a value published at parse time, since the object can move between parse and execution; it then calls `orchestrateObjectMove` with `containment: 'On'`. Scope cuts, deliberate: multi-candidate (ambiguous) subject/target resolution errors out rather than disambiguating; `take cup off tray` (dissolve, no containment kind) is untouched — what destination that should resolve to (the existing acquire lane, per AB-54) is a design answer, not yet code. Tests added at every layer: `compileObjectRehostFromSkeleton.test.ts` (new), `classifySkeletonFamily.test.ts`/`parseCommand.test.ts`/`publishedEvents.test.ts`/`actions/index.test.ts`/`positions/subscribedEvents.test.ts` (extended), and `positions/receivePaths.integration.test.ts` (extended — the established real-messageBus-round-trip convention this repo uses in place of a cross-Lambda harness). Full ephemera suite (344/345 suites, 2730 tests) and full typecheck (ephemera, mtw-interfaces, mtw-gateways) green.
  - [ ] Note: [CD6](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order) does not apply here — it is a crossing-port kind-agreement check, and a presence port has no exterior edge to disagree with. CD6 stays with PV1-3; reword its trigger at PV1-5.
  - [X] **Done when:** a cup put on a tray is a member of the tray's `ludicGraph` (not the room's), its containment edge is root-anchored inside the tray's graph, its presence port names the tray, and both are gone when it leaves — asserted in test (`executeObjectMove.test.ts`'s `PV1-2: On rehost` suite, and the client-wiring row above carrying a parsed `put cup on tray` command through to `orchestrateObjectMove`) **and observed live in the running client 2026-08-31**: `put cup on table` showed the cup as a table-graph member with its containment edge and a presence port naming the table (table absent from the room's contained nodes); `get cup` then `drop` showed both gone — the port reverted to naming the room and the table lost its contained node. Two bugs found and fixed along the way, neither anticipated by this row: `applyStepSequenceCore`'s host resolution for `establishRelation`/`dissolveRelation` used first-match-per-endpoint semantics, which mis-resolved the shared host whenever a hosting-kind target sat in two locked footprint graphs at once (fixed with an intersection-based `findSharedHost`); and `Object Take Hold`'s handler trusted `content.roomId` as the object's current host, which broke for a nested object (fixed by resolving it fresh via `getMembershipContainers`, same pattern as `Object Rehost`). This slice's own live-client observation is discharged; PV1-4's fuller readout (Acme order flow, PV1-3's tie-string chain, reading the four Dynamo records directly) is still owed separately.
- [X] **PV1-3. A `Custom` relation across the boundary, and the crossing-port producer it needs.** Unlike PV1-2 (a rehost carrying a containment argument), nothing here moves — `tie rope to cup` leaves both the rope and the cup exactly where they are; the boundary is crossed by a port pair, not by a transfer. That is what makes this row's server gap different in kind from PV1-2's, not just in degree. Done 2026-09-01.
  - [X] **Server, not client — lexicon fix confirmed and made.** The row's original "Client:" label was wrong: `classifyVerb`/`ESTABLISH_VERBS`/`DISSOLVE_VERBS` live in `lambda/ephemera/dataSource/actions/enrich/objectManipulation/plan/matchRelationalTemplate.ts` (mirrored in `dataSource/actions/deterministicTemplate/relationalTemplates.ts`) — both server-side, not `charcoal-client`. "tie" was in neither set, and the fallback for an unrecognized verb is `noMatch`, not `establish` (confirmed by a test before changing anything, per this row's own original instruction). Added `'tie'` to `ESTABLISH_VERBS` in both files — no synonyms, matching `put`/`place`/`lean`'s own one-entry-each convention. `normalizeRelationSpan.ts`'s "to" → `Custom` by exclusion (no new phrase entry needed) was confirmed correct as originally written.
  - [X] **Server: the actual gap, confirmed.** `applyStepSequenceCore.ts`'s `findSharedHost`/`hostsOf` throw on a genuine host mismatch (BD-33); `expandSameHost.ts` (BD-16) had no outcome for *legitimately never share a host, mint a crossing pair instead*. That third outcome is now `ExpandSameHostResult`'s `'crossed'` verdict.
  - [X] **Shard-boundary predicate, general case** — `synthesize/findShardBoundary.ts`. Walks `getMembershipContainers` recursively from each endpoint (uniform at every node/depth — `EphemeraPositionAdjacencyContainedId`, i.e. Character/Object/Feature, is exactly the set the primitive accepts; Room/Area terminate a branch without a further call) until every branch terminates. **Correction found during plan review, before code:** "closest common ancestor" is not a combined-depth minimum — it is the Pareto-minimal frontier of common nodes under (subjectDepth, targetDepth) domination (a common node dominated by another common node that is no-worse on both counts and strictly better on at least one is excluded, since routing through it would mean walking up past a perfectly good crossing point and back down through it again). This can still have more than one member when the two ancestry DAGs branch incomparably — verdicts are `notFound` (zero), `crossed` (one, carries `commonAncestor`/`subjectPath`/`targetPath`), or `ambiguous` (more than one, not-yet-implemented). Unit-tested directly: the same-shard degenerate case, the room/table one-hop case, a multiply-contained intermediate host (cup on table *and* in a place setting, both leading to the same room), a `notFound` case, and a constructed fixture with two genuinely incomparable Pareto-minimal ancestors (not just two starting points converging on one node) — `findShardBoundary.test.ts`.
  - [X] **Crossing-port producer, general case** — `synthesize/buildCrossingLegs.ts`, consuming `findShardBoundary`'s `'crossed'` result. **Correction found during plan review:** legs don't need a new kernel step kind — `establishRelation`/`dissolveRelation` were already leg-shaped once `ExecutorEstablishRelationStep`/`ExecutorDissolveRelationStep` widened from `EphemeraLudicTerminalPrimitive` to `EphemeraLudicTerminalId` (a leg's far-side endpoint is a port address), matching what `HostRelationalEdge`/`EphemeraLudicRelationalEdgeBase` already supported (LP7). One fresh `EphemeraCrossingPort` is minted per crossing, stored on the interior side (nearer the endpoint the relation is reaching toward) — exactly the readout's own asymmetry, generalized. **Scope cut, deliberate (confirmed with the user):** supports at most one extra hop per side, and never both sides at once — that combination needs a *middle* leg between two port addresses with no primitive endpoint, which `applyStepSequenceCore`'s BD-33 derive-don't-carry host resolution cannot resolve without carrying an explicit host on the step (a bigger, not-taken change). Reports `notYetImplemented` for that case rather than emitting a step that would throw at apply time. Establish-only — dissolve/untie is unbuilt. Unit-tested directly: the readout's own case (rope in room, cup on table — one port minted, two legs), the symmetric case (subject nested instead of target), the degenerate single-leg case, a non-`Custom` kind (no `relationLabel`), and both `notYetImplemented` shapes — `buildCrossingLegs.test.ts`.
  - [X] **`findSharedHost`/`hostsOf` widened for port-address endpoints** (found necessary during implementation, not originally in this row): a port address's host is its own `owner` field (graph-local addressing, not membership search) — used as a candidate in the existing intersection, with a fallback to the primitive endpoint's own (unambiguous) candidate when the intersection comes up empty, since the exterior side of a crossing has no port record of its own to intersect against. `computeStepSequenceFootprint.ts`/`factsForStep.ts` widened correspondingly (footprint resolves whichever endpoint(s) are primitives; facts return `[]` for any port-qualified leg — no established fact/narration shape yet, same precedent as `setPresencePort`).
  - [X] **Kernel step (new), narrowed to the port record only.** `MutationKernelAddCrossingPortStep`/`MutationKernelRemoveCrossingPortStep` (`kernelStep.ts`) — add/remove by `portId` (crossing ports are not at-most-one, unlike presence). Wired into `isKernelMutationStep`, `computeStepSequenceFootprint.ts`, `factsForStep.ts` (returns `[]`, same as `setPresencePort`), and `applyStepSequenceCore.ts`'s dispatch (calls the existing `addPort`/`removePort`).
  - [X] **Wired into Expansion.** `expandSameHost.ts`'s `ExpandSameHostResult` gained the `'crossed'` verdict (its own doc comment had already flagged the union open to a third outcome); `GroundedBinaryAssertion` gained an optional `relationLabel` (needed to build a `Custom` crossing port/leg, previously only `relationKind` was carried). `executor.ts`'s `commandExpand` `'sameHost'` case: leg steps (`establishRelation`, already executor-shaped) retire through the ordinary worklist; the port-record step cannot (`addCrossingPort` is not an `ExecutorParsePlanStep`) and rides a new side-channel, `CommandExpandOutcome`/`ExecutorOutcome`'s optional `extraKernelSteps` — present and non-empty only for a crossing, so every other caller/test is unaffected. Unit-tested directly at both layers: `expandSameHost.test.ts` (a violated Custom relation crossing instead of deferring; falling back to defer when no `relationLabel` is supplied) and `executor.test.ts` (`runExecutor` end to end: crossing legs land in `steps`, the port record in `extraKernelSteps`).
  - [X] **No `chainId`/`edgeId` minting this slice** (scope cut, confirmed with the user during plan review, overriding this row's original text) — legs are inline/unidentified. PV1-0's readout gained a line under "what this run cannot observe" naming this consequence.
  - [X] **`applyObjectRelationalChange.ts`'s port-qualified-edge skip, checked, not fixed.** It only runs during a membership-transfer's boundary sweep; `tie rope to cup` triggers no transfer (nothing moves), so this slice never routes through it. Stays a named, deliberate gap for whichever slice first needs to *dissolve* a crossing relation.
  - [X] **Live ingress route (`compileRelationalFromSkeleton.ts`) intentionally not wired** — two gaps, named in a comment at its own seed-construction site: its `getMembershipContainers` pre-fetch is one-hop only (the walk needs to keep going past intermediate hosts), and even with deeper pre-fetching its seed's sibling `relationalStepNoHost` item would retire unmodified alongside the crossing legs, double-emitting an invalid direct edge — the satisfied/repaired outcomes rely on that sibling; a crossing replaces it entirely and needs the seed built accordingly. `expandSameHost`/`commandExpand`/`buildCrossingLegs` are unit-tested directly instead (listed above).
  - [X] **Done when:** unit tests cover the shard-boundary predicate (including its multiply-contained and zero/many-common-ancestor branches), the crossing-port producer and kernel step, the `expandSameHost`/`commandExpand` wiring end to end, and the verb-lexicon fix — all landed, full ephemera suite green (346/347 suites, 2770 tests — one suite skipped, pre-existing) and full typecheck (ephemera, mtw-interfaces, mtw-gateways) green. The live-client `tie rope to cup` run itself is PV1-4's job, not this row's.
- [ ] **PV1-3b. Wire the live ingress route to PV1-3's crossing mechanism.** Stubbed 2026-09-01, from live-testing `tie string to cup` (see the Progress row above and Open decisions PV1-3b-1 through PV1-3b-7). **Still not fully designed — PV1-3b-1/2/3 remain open — but PV1-3b-7's conclusion (below) is settled and belongs in the build.** Rough shape: `compileRelationalFromSkeleton.ts` needs to detect an `expandSameHost` `'crossed'` outcome without also retiring its seed's sibling relational step (PV1-3b-4), pre-fetch deep enough for `findShardBoundary` to find anything beyond one hop (PV1-3b-5), and thread `relationLabel` into the assertion it builds (PV1-3b-6) — then the result needs a new path to the kernel capable of committing a `MutationKernelStep[]` across every host it touches in one transaction (PV1-3b-1/2/3), since the existing parse-result/publish-subscribe/execute chain is single-relation/single-host shaped end to end.
  - [ ] **PV1-3b-7: carry an explicit, mandatory `hostId` on relational steps instead of deriving one at apply time.** Widen `ExecutorEstablishRelationStep`/`ExecutorDissolveRelationStep` and the corresponding `MutationKernelStep` with a `hostId: EphemeraMembershipHostId` field, matching what `MutationKernelAddCrossingPortStep`/`RemoveCrossingPortStep` already carry.
  - [ ] Populate that field at Expansion, not at kernel apply: surface `expandSameHost`'s already-resolved `hostId` for `'satisfied'`/`'repaired'` instead of discarding it in `commandExpand`; surface each `buildCrossingLegs` leg's own target host the same way (both `'crossed'` legs and the room/table-side legs the readout's own case produces).
  - [ ] Narrow `applyStepSequenceCore`'s `findSharedHost`/`hostsOf` from resolver to assertion: given the step's carried `hostId`, confirm both endpoints belong to that graph and throw if not, rather than deriving the host from the endpoints via intersection. Keep the throw (BD-33 discipline) — only the computation moves, not the check.
  - [ ] Confirm this doesn't regress PV1-3's existing unit tests (`expandSameHost.test.ts`, `executor.test.ts`, `applyStepSequenceCore.test.ts`'s AB-54 intersection case) — they'll need a carried `hostId` added to their fixtures rather than relying on derivation.
  - [ ] Out of scope for this row: resolving `findShardBoundary`'s `'ambiguous'` verdict or the multi-hop-both-sides case — PV1-3b-7 only carries a host once Expansion has resolved exactly one; it does not pick among several.
  - [ ] **PV1-3b-1: widen `ParseCommandEstablishRelationResult` to carry the Expansion-derived step list.** `compileRelationalFromSkeleton.ts` already holds `runExecutor`'s full output (`steps` + `extraKernelSteps`, each leg now host-attributed per PV1-3b-7) in scope when it builds this result — widen the existing type rather than adding a sibling type; only the field shape is still open.
  - [ ] **PV1-3b-3: a new pass-through commit function — no bespoke verification layer.** Leg-time validation is sufficient on its own (settled 2026-09-01): PV1-3b-7's per-step assert in `applyStepSequenceCore` already re-checks each leg's carried `hostId` against freshly-fetched graphs inside `commitStepSequence`, so the new function needs no pre-commit structural re-check of its own — it merges `steps`/`extraKernelSteps` into one `MutationKernelStep[]`, builds `getCurrentHost` from each step's own carried `hostId`, and calls `commitStepSequence` directly, letting the kernel's own assert fail the whole crossing on any staleness.
  - [ ] Merge order (decided): crossing-port `addCrossingPort` step(s) first, then the leg steps — legs are independent of each other but each depends on its referenced port already existing; nothing else precedes the legs.
  - [ ] Function identity (decided): a new sibling function, `executeEstablishEdgeChain` — not a branch inside `executeObjectEstablishRelation` — matching `executeMembershipTransfer`'s relationship to `executeObjectMove` and `Object Rehost`'s own precedent.
  - [ ] `executeObjectEstablishRelation`'s silent-swallow bug: left unfixed deliberately — it may itself be a retirement candidate once crossing is lifted out to `executeEstablishEdgeChain`; revisit only if/when that's settled.
  - [ ] Reconfirm `applyObjectRelationalChange.ts`'s port-qualified-edge skip stays out of scope: crossings are establish-only this slice (PV1-3's own note), so `executeEstablishEdgeChain` shouldn't need to handle a port-qualified dissolve leg yet.
  - [ ] **PV1-3b-2: widen `Object Establish Relation`'s existing published payload/envelope**, not a new event pair — carries the (now-widened, PV1-3b-1) result's step list across the `mtw.ephemera.actions` → `mtw.ephemera.positions` boundary the same way the existing single-relation shape does today.
  - [ ] **Done when:** `executeEstablishEdgeChain` is built (port steps before legs, no bespoke verification, single `commitStepSequence` call) alongside PV1-3b-1/2/7; full ephemera suite and typecheck green.
- [ ] **PV1-4. Take the readout.** Run PV1-0's observations against the built vertical and write down what happened, including the find path this vertical could not observe. **Blocked on PV1-3b** — `tie string to cup` does not work live yet.
- [ ] **PV1-5. Write back into the design corpus** — the `P8-i1-dependent` tag names the rows that owe an update (currently one: [EA-10](dataSource/positions/AGENT.edgeAbstractions.planning.md#ea-10)).
  - [ ] [P8](dataSource/positions/AGENT.abstractionLayers.proposals.planning.md#status-1): record what the readout showed, and whether the rollback trigger fired.
  - [ ] [EA-10](dataSource/positions/AGENT.edgeAbstractions.planning.md#ea-10): the five structure-as-identity sites now have live traffic — say what that changed.
  - [ ] [AB-55/AB-62](dataSource/positions/AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only): both port producers exist — close or re-scope. AB-55 takes the readout's presence-port clause directly.
  - [ ] [CD6](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order): reword its travel rule from "whichever step first writes a port record" to "first writes a *crossing* port record" (PV1-2 writes presence ports and does not discharge it).
  - [ ] [CD3](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order) / [CD2h](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order): record what PV1-2 did instead of the retired classification throw, and whether LD-11 is now live.
  - [ ] [AB-65](dataSource/positions/AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) (*is an edge a value or an entity?*): report evidence, do not answer it here.
  - [ ] Strike "do not extend room-only entry points" ([`AGENT.implementation.md:51`](../../../lambda/ephemera/dataSource/positions/AGENT.implementation.md)) and the `D14` bullet ([`AGENT.contract.md:228`](../../../lambda/ephemera/dataSource/positions/AGENT.contract.md)). Sweep references to the deleted coordinators/adapters in `AGENT.contract.md` (object room membership section, room-only bundle section, graph-forward repair, relational-patch prohibition), `AGENT.implementation.md` (file table, objects-lane rows), `AGENT.concepts.md` (spawn+place, place/remove), and `manipulation/AGENT.implementation.md`'s route table. Give `D8`/`D13`/`D14`/`D16` a register or strike the tags — they are cited in the contract and defined nowhere.
  - [ ] **Done when:** each row above is either updated or explicitly recorded as unaffected, and no new open rows are added from this plan.

## Verification

- **Ephemera:** `npm --prefix lambda/ephemera run test -- --watchAll=false`. Run the full suite, not a filtered subset — `*.integration.test.ts` files sit outside `tsconfig`, so `tsc` does not cover them.
- **Typecheck:** `npx tsc --noEmit` in `lambda/ephemera`, `packages/mtw-interfaces`, and `packages/mtw-gateways`.
- **Client:** `npm --prefix charcoal-client run test:single` (Vitest; not Jest, `--testPathPattern` does not apply).
- **The vertical itself:** PV1-0's observations, taken by hand in a running client at PV1-4. No unit test substitutes for this.

## When this finishes

Per [`taskPlanning/AGENT.md`](../../AGENT.md): move anything durable into the code-adjacent `AGENT.md` files under [`dataSource/positions/`](../../../lambda/ephemera/dataSource/positions/), then delete this plan. PV1-5 is what makes that safe.
