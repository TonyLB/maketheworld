# Object manipulation parse --- Phases B--D (frame, establishRelation, plan IR)

**Status:** Phase B complete (B6 durable docs shipped). **Fault-tolerant gateway Done (FT-4.3, 2026-07-10)** --- Gateway exit complete; planning file retired (git retains history). **Phase C unblocked** (ready for C1). Phase C design debt from that gateway is consolidated below (**Phase C design debt**). **Sibling plan split out (2026-07-10):** [`AGENT.planCompilerSandbox.planning.md`](AGENT.planCompilerSandbox.planning.md) now owns the in-memory dry-run sandbox design + build checklist --- a blocking prerequisite for C1 compound-plan legality.

Task-planning conventions: [`taskPlanning/AGENT.md`](../../../../AGENT.md).

Prerequisite (shipped): Phase A membership compiler --- [`actions/AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.implementation.md#object-manipulation-classify--enrich-steady-state-shipped---b25-split-intents) (**`verbClass`**, **`compileMembershipAtomic`**, merged-catalog identity); Phase B relational vertical shipped (frame extract, normalizer, compiler, egress, positions apply, perception). Module inventory: [`actions/enrich/AGENT.md`](../../../../../lambda/ephemera/dataSource/actions/enrich/AGENT.md).

## Purpose

Ship generic **`establishRelation`** --- one relational operator parameterized by relation kind (not per-preposition operators like **`placeOn`**). Player language ("on", "under", "leaning against", "tied around") becomes a **manipulation frame** (subject, target, relation), compiled deterministically into positions **host-local relational patches** and perception transcript.

Phases B--D evolve parse from coarse intent classification toward **split manipulation intents at classify (BD-11) + frame extraction + deterministic plan compilation + execution**, without requiring full LLM-authored multi-step plans on day one.

Retire this plan when Phase C--D steady-state docs land (Phase B durable docs shipped in B6); git retains history.

## Phase map

| Phase | Focus | Outcome |
| --- | --- | --- |
| **A** (shipped) | Membership atomics | **`verbClass`**, **`compileMembershipAtomic`** (compiler v0), legacy deletion, preposition guard |
| **B** | **`establishRelation`** vertical | Frame slots, relation normalization, stream/apply/presentation |
| **C** | Plan IR + composition | Deterministic compiler; optional multi-step (e.g. drop + relate) |
| **D** | Advanced planning (optional) | LLM plan generation only where compiler cannot derive steps |

## Target architecture (steady-state sketch)

```text
discriminateIntent (ObjectMembershipIntent + verbClass acquire | release OR ObjectRelateIntent)
  -> parseCommand routes by intent type (enrichRoute from classify)
  -> membership enrich OR frame extract LLM hop -> manipulation frame (subject / target / relationSpan + operationKind)
  -> plan compiler (deterministic: resolve spans, normalize relation, legality)
  -> plan executor -> stream event(s) -> positions -> perception
```

**Classify contract (Phase A interim + B2.5):** Replace the undifferentiated **`ObjectManipulationIntent`** umbrella with two classify outcomes (**BD-11**):

| Intent | Topology | Classify fields | Enrich path |
| --- | --- | --- | --- |
| **`ObjectMembershipIntent`** | Node / membership host --- which **`positionGraph`** hosts the object | **`objectSpans`**, **`verbClass`** (`acquire` \| `release`) | **`compileMembershipAtomic`** |
| **`ObjectRelateIntent`** | Edge / in-host relation between objects on a host graph | **`objectSpans`** only (no **`verbClass`**) | frame extract -> normalizer -> relational compiler |

Classify **must** own the membership vs relational distinction semantically (not enrich preposition regex once B2.5 ships). **`verbClass`** remains membership **language** direction only --- unchanged from Phase A, scoped to **`ObjectMembershipIntent`**. Frame extraction stays a dedicated enrich hop (**BD-4**) and owns relational **operator direction** via **`operationKind`** (**BD-12**).

**Classify vs enrich ownership (relational):**

| Field | Lane | Meaning |
| --- | --- | --- |
| Intent **`type`** (`ObjectRelateIntent`) | Classify | Membership vs in-host relational topology |
| **`operationKind`** (`establishRelation` \| `dissolveRelation`) | Frame extract LLM (**BD-12**) | Relational **operator** choice from player language |
| **`relationKind`** / **`relationLabel`** | Deterministic normalizer (B2) | Map LLM **`relationSpan`** to closed enum or **`Custom`** |
| Grounded ids, host, legality | Compiler | Catalog resolve, graph observation, BD-10 rules only |

**Plan IR (Phase C)** --- primitives are validated, not model-invented:

| Primitive | Role |
| --- | --- |
| ~~`resolveComponent`~~ | **Retired as a runtime primitive (FT-5, 2026-07-08).** Span grounding folds into the joint `(identity, plan)` hop (FT-2 (e)); the surviving deterministic per-span work is the **selector verdict** (`SpanCandidatePool` -> `SpanResolutionOutcome`) + an **existence/presence guard**, both facets of the compiler tail, not an executable step |
| `transferMembership` | **`takeHold`** / **`drop`** (existing membership kernel path). **Generalizing to multi-member (BD-13, 2026-07-10):** accepts an object **set**, not only a single object, and preserves internal relational edges across the host boundary --- kernel work, tracked here; simulation/classification side lives in the sandbox sibling plan. |
| `establishRelation` | Add edge on host `positionGraph` (slice 5+ kernel) |
| `dissolveRelation` | Remove edge on host `positionGraph` (BD-7; slice 5+ kernel) |
| `look` / others | Reuse existing affordance streams where composition needs them |

Phase B may compile to a **length-1 plan** internally without exposing a general executor API.

## Relation representation (preferred direction --- hybrid)

Product preference (from planning discussion): work deterministically with a **small closed enum** for common prepositions, reserve **`in`** for future **positionGraph nesting** (container hosts), and use **custom free-text** for long-tail relation phrases.

| Bucket | Examples | Persist / compile |
| --- | --- | --- |
| **Enum: surface relations** | `On`, `Under`, `Against` (BD-2 locked) | Canonical **`relationKind`** on edge |
| **Deferred: containment** | `in`, `inside` | Not **`establishRelation`** v1; routes to future nesting operator |
| **Custom** | `tied to`, `leaning against`, `wrapped around` | **`relationKind: Custom`** + **`relationLabel`** (player/normalized text) |

Enum members and custom persist shape: [`positions/AGENT.contract.md` --- Host-local relational patch](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md#host-local-relational-patch-phase-b-shipped-b4) (BD-2, BD-3).

## Scope

### Phase B --- in scope

- **Manipulation frame** schema: role-tagged spans (`subject`, `target`), **`relationSpan`**, and **`operationKind`** (`establishRelation` \| `dissolveRelation`, **BD-12**). **`ObjectRelateIntent`** at classify (**BD-11**) replaces preposition-regex as primary enrich entry signal once B2.5 ships; interim B1--B2 routing via [`relationalRoute.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/relationalRoute.ts).
- Frame extraction hop (**dedicated enrich prompt**, BD-4 --- not shared complexity LLM) + JSON validation; emits **`operationKind`** alongside spans (**BD-12**); replaces Phase A relational preposition guard for supported frames. B1 shipped span-only extract; B3 extends the hop before relational compiler work.
- **Relation normalizer**: map **`relationSpan`** -> **`relationKind`** (enum | `Custom`) + optional **`relationLabel`**.
- Terminal parse + egress: new top-level result variant (BD-1) --- e.g. **`ParseCommandEstablishRelationResult`** (`type: 'EstablishRelation'`) with subject/target ids, **`relationKind`**, optional **`relationLabel`**, and **`operationKind: 'establishRelation' | 'dissolveRelation'`** (BD-7). Membership atomics keep **`ObjectManipulation`** + **`takeHold` | `drop`** unchanged.
- Promote **`relationalPlacement`** from terminal-only Error to grounded success path when frame validates.
- Positions: **`applyHostRelationalPatch`** shipped --- [`manipulation/AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/positions/manipulation/AGENT.implementation.md#host-local-relational-patch-phase-b-shipped-b4).
- Perception: transcript template (withhold unstated geometry per [`diegeticLogic/AGENT.unknowns.concepts.md`](../../../../../lambda/ephemera/diegeticLogic/AGENT.unknowns.concepts.md)).
- Four-lane doc updates: [`diegeticLogic/AGENT.operators.concepts.md`](../../../../../lambda/ephemera/diegeticLogic/AGENT.operators.concepts.md), positions contract, actions implementation.

### Phase C --- in scope

- Explicit **Plan IR** types + deterministic **compiler** (frame + catalogs + membership -> ordered steps).
- **Composition rules (BD-8):** when single command implies membership change + relation (e.g. held object + "put X on Y"), compiler auto-inserts **`drop`** before **`establishRelation`** --- no require explicit drop language.
- **`MultipleCommands`** policy: reject vs allow bounded multi-step from one line.
- **Multi-step failure (BD-9):** composed plans (**drop** + **`establishRelation`**, etc.) apply **atomically** --- positions kernel bundles membership **`HostEffect[]`** + relational patches in **one** `transactWrite` (same family as graph+adjacency bundling in **`applyHostEffects`**). **Not** sequential per-step streams with first-step commit; **not** RoomStack-style fail-tolerant tail.
- **Compiler shape (from fault-tolerant gateway, 2026-07-08):** the C1/C2 compiler is a **candidate proposer** (deterministic fast-pass *or* LLM fallback) feeding a **shared deterministic validator** that runs a **pure dry-run legality evaluation over an in-memory sandbox** (no persist) and returns `verdict + decidable + resultPreview`. Selection is a deterministic **legality-gated + confidence** rubric (enum -> fast-approve; **`Custom`** / unmodeled -> LLM validator = BD-10 `defer`) that **auto-resolves** the winner or **Consults** ("commit" reserved for persist + publish). Span grounding is the deterministic **tail** of the joint `(identity, plan)` hop; **`resolveComponent` is retired as a standalone Plan IR primitive** --- the surviving per-span work is the selector verdict + an existence/presence guard. **New Phase C prerequisites:** (a) an **in-memory sandbox** capability for compound (BD-9) legality; (b) **deterministic interaction-under-transfer semantics per enum relation** (e.g. `On` dissolves on pickup; `Under` may block / compose) --- the modeled core that lets `get X off Y` fast-approve, whose absence for `Custom` routes legality to the LLM validator. Golden path (known verb + exact label + all-enum blast radius) stays **zero Bedrock** via staged fast-path composition. **Design, open decisions, and build checklist for the sandbox itself now live in a sibling plan:** [`AGENT.planCompilerSandbox.planning.md`](AGENT.planCompilerSandbox.planning.md).

### Phase D --- optional / deferrable

- **Defer vs hard Error (BD-10):** compiler **`defer`** only when non-trivial **existing in-host relational edges** on subject and/or target block a deterministic registry plan (interaction complexity --- analog to Phase A **`deferToComplexityLlm`**). **Phases B--C:** that bucket terminalizes as **hard Error** (complexity-style stub); Phase D plan LLM is the escalation path. See **BD-10** detail below.
- LLM emits step list when compiler returns **`defer`** (constrained to registry primitives; validate + atomic apply per BD-9).
- Reuse [`llm/pipeline/`](../../../../../lambda/ephemera/llm/pipeline/AGENT.md) if multi-hop frame+plan needs orchestration.

### Out of scope (unless plan updated)

- Full **`in`** / nested container host vertical (separate operator initiative).
- Client UI for relation editing.
- Retaining **`ObjectManipulationIntent`** as the classify umbrella (superseded by **`ObjectMembershipIntent`** + **`ObjectRelateIntent`** per **BD-11**).
- Arbitrary graph writes beyond registered primitives.

## Background (durable docs --- link, do not duplicate)

| Topic | Doc |
| --- | --- |
| Relational persist stub | [`positions/manipulation/AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/positions/manipulation/AGENT.implementation.md) |
| Positions concepts (edges, host-local patch) | [`positions/AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md) |
| Operator lane split | [`diegeticLogic/AGENT.implementation.md`](../../../../../lambda/ephemera/diegeticLogic/AGENT.implementation.md) |
| Unknowns / withhold | [`diegeticLogic/AGENT.unknowns.concepts.md`](../../../../../lambda/ephemera/diegeticLogic/AGENT.unknowns.concepts.md) |
| Current relational Error stub | [`enrich/objectManipulation/complexityClasses.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/complexityClasses.ts) |

## Getting started

1. Skim [`taskPlanning/AGENT.md`](../../../../AGENT.md).
2. Read Phase A steady-state in [`actions/AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.implementation.md#objectmanipulationintent-steady-state-shipped---membership-aware-classify--enrich--egress) (membership compiler shipped).
3. Read positions relational implementation: [`manipulation/AGENT.implementation.md` --- Host-local relational patch](../../../../../lambda/ephemera/dataSource/positions/manipulation/AGENT.implementation.md#host-local-relational-patch-phase-b-shipped-b4).
4. Trace relational enrich path: [`enrich/objectManipulation/index.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/index.ts) (interim **`relationalRoute`** -> frame extract; B2.5 routes **`ObjectRelateIntent`** at parse ingress), [`frameExtract/runFrameExtractStage.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/frameExtract/runFrameExtractStage.ts). Membership defer still uses complexity LLM in [`buildPrompt.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/buildPrompt.ts). **Note:** both the **frame extract** and **complexity LLM** hops are slated to **retire** as distinct LLM steps under Phase C (**C4**) --- see **Phase C design debt** (**Legacy hop retirement**).
5. Testing authority: [`lambda/ephemera/AGENT.testing.md`](../../../../../lambda/ephemera/AGENT.testing.md).
6. Baseline (includes positions manipulation tests when Phase B touches apply):

```bash
cd lambda/ephemera && npm run test -- --watchAll=false \
  dataSource/actions/enrich/objectManipulation/ \
  dataSource/actions/parseCommand.test.ts \
  dataSource/positions/manipulation/
```

## Open decisions (implementation --- plan only)

Plan-only: decisions we are making in order to implement Phases B--D. When a decision ships, record it in **`AGENT.contract.md`** / **`AGENT.implementation.md`** / operator concepts and remove the row here.

| ID | Decision | Blocks | Status |
| --- | --- | --- | --- |
| BD-1 | **Terminal parse shape** --- **new top-level result variant** (e.g. **`ParseCommandEstablishRelationResult`**, `type: 'EstablishRelation'`), **not** extending **`ParseCommandObjectManipulationResult.operationKind`**. Membership atomics stay on **`ObjectManipulation`** + **`takeHold` | `drop`**. | Phase B contract | Decided |
| BD-2 | **Hybrid relation enum** --- v1 members: **`On`**, **`Under`**, **`Against`**. **`In`** / **`inside`** excluded; route to future nesting operator with defer message (not **`establishRelation`**). | Normalizer + persist **`edge.kind`** | Decided |
| BD-3 | **Custom relation storage** --- persist **`relationKind: Custom`** + **`relationLabel`** on edge (not presentation-only). | Persist + transcript | Decided |
| BD-4 | **Frame extraction placement** --- **dedicated enrich hop** (new module under enrich); **not** shared complexity LLM. Relational formation is distinct from cross-graph membership movement. Primary relational **routing** moves to classify **`ObjectRelateIntent`** (**BD-11**); frame extract remains enrich-side and emits relational **`operationKind`** per **BD-12**. | Prompt layout + Bedrock budget | Decided (revised 2026-07-05) |
| BD-5 | **Target resolution** --- v1: **room object catalog only** for subject + target resolve. **Extend** to features/surfaces and nested **`positionGraph`** hierarchy targets when nested-host target matching ships (follow-on; not Phase B). Non-object targets ("floor", "wall") out of scope v1. | Frame extract + resolve | Decided |
| BD-6 | **Host selection** --- v1: always actor's **current room** **`positionGraph`** (`roomExitContext.fromRoomId`). | Positions ingress | Decided |
| BD-7 | **Inverse operator** --- include **`dissolveRelation`** in v1 (remove edge on host graph); pair with **`establishRelation`**. Frame extract classifies establish vs dissolve via **`operationKind`** (**BD-12**). | Phase B scope | Decided (revised 2026-07-05) |
| BD-8 | **Composition (Phase C)** --- compiler **auto-inserts** **`drop`** before **`establishRelation`** when subject is held (no require explicit "drop then put" language). | Compiler | Decided |
| BD-9 | **Multi-step failure** --- **atomic all-or-nothing** via positions **kernel bundling**: one compound apply / single `transactWrite` spanning membership **`HostEffect[]`** + relational **`HostRelationalPatch[]`** on affected hosts (mirror graph+adjacency in **`applyHostEffects`**). **Not** sequential stream-per-step with first-step commit; **not** RoomStack fail-tolerant tail. On apply failure: no partial graph change, player **Error**. | Executor + positions kernel (Phase C) | Decided |
| BD-10 | **LLM plan generation (Phase D)** --- **`defer`** when compiler cannot derive a deterministic registry plan because **non-trivial existing in-host relational topology** on subject and/or target interacts with the proposed patch (conflicting edges, replace/coexist/dissolve-first ambiguity). **Hard Error** (all phases): grounding/catalog failure, **`in`**/nesting (BD-2), **`MultipleCommands`**, nodes not on host graph, **`dissolveRelation`** with no matching edge, plan-LLM invoke/parse/validation failure. **B--C interim:** defer bucket -> terminal **Error** stub (no plan LLM yet). **Deterministic allow:** idempotent duplicate edge (exact edge already present). | Phase D entry; B3 legality; C1 compiler | Decided |
| BD-11 | **Split manipulation classify intents** --- replace undifferentiated **`ObjectManipulationIntent`** with **`ObjectMembershipIntent`** (object moves between membership hosts --- which **`positionGraph`** node hosts the object; **`objectSpans`** + **`verbClass`** `acquire` \| `release`; compiled via **`takeHold`** / **`drop`**) and **`ObjectRelateIntent`** (in-host edge between objects on a host graph --- **`objectSpans`** only, no **`verbClass`**; compiled via **`establishRelation`** / **`dissolveRelation`**). Classify owns semantic membership vs relational distinction via intent **`type`**, not a sub-field. **`parseCommand`** branches on intent type; enrich **`ObjectRelateIntent`** -> frame extract path, **`ObjectMembershipIntent`** -> **`compileMembershipAtomic`**. Retire **`relationalRoute`** preposition regex as primary gate after B2.5 parity tests (may keep as interim fallback one release). Remove **`ObjectManipulationIntent`** from classify prompt and parser when B2.5 ships. **Superseded in part by FT-7 (2026-07-08):** Phase C **reunifies** membership + relational into **one manipulation-family intent type** with an optional `{ subTopology?, verbClass?, confidence }` hint bundle --- the **top-level committed `type` split / `enrichRoute` fork is retired** (it was a trusted-output construct; a committed sub-topology route is the premature commitment fault tolerance removes). **Surviving from BD-11:** the manipulation family is classify-owned and **`verbClass`** stays membership language direction (`acquire` \| `release`). Only the first-class-type / committed-routing role is walked back. See **Phase C design debt** (**Classify reunify / FT-7**); lands with **C4**. | B2.5 classify; B3+ parse/enrich entry | Decided (revised 2026-07-04); routing role superseded by FT-7 (2026-07-08) |
| BD-12 | **Relational `operationKind` ownership** --- frame extract LLM (**BD-4**) emits required **`operationKind`**: `establishRelation` \| `dissolveRelation` (**BD-7**). Forbidden at classify (same as membership **`operationKind`**). Relational compiler **validates** and applies legality; **must not** infer operator choice from phrase buckets, prefix stripping, or command regex. Membership **`operationKind`** fast path remains graph-driven (**`complexityPreGates`**), not language-driven. | B3 frame extract + compiler | Decided (2026-07-05) |
| BD-13 | **Multi-member `transferMembership` + carry semantics** --- **decided (2026-07-10), informed by sandbox sibling plan.** "get tray" with a glass **`On`** the tray should deterministically compile to `dissolveRelation` (tray-table, clean per SB-5) + a **multi-member** `transferMembership` moving **`[tray, glass]`** as a set, Room -> Character, **preserving the internal `glass On tray` edge** (recreated on the destination host's `positionGraph`, since edges are host-scoped). This resolves the `On` / target-moves interaction-under-transfer cell from **`defer`** to a deterministic **`carry`** (absorb into the transfer set) --- see [`AGENT.planCompilerSandbox.planning.md`](AGENT.planCompilerSandbox.planning.md) (SB-5 revision) for the classification/simulation side. **Kernel work required here:** `applyObjectTakeHold` / `applyObjectDrop` (or a new multi-object variant) must accept an object set + its internal edge list and recreate those edges on the destination graph within the same atomic `HostEffect` bundle (BD-9) --- not a follow-up write. **Concrete blocker found during sandbox S2 (2026-07-10):** `applyRelationalPatch` ([`positions/positionGraph/index.ts:312-344`](../../../../../lambda/ephemera/dataSource/positions/positionGraph/index.ts)) hard-rejects any host that isn't a room (`isEphemeraRoomId` guard, line 316) --- this must be revisited before a carried edge can be recreated on a Character-hosted destination graph. **Kernel work status (2026-07-13): slices 1--2 of 3 done** (host-agnostic edge-carry persistence primitive; atomic bundling in `applyHostEffects`) --- see the C1 checklist bullet for detail. **Slice 3 (bus payload widening + real producer wiring), further broken down 2026-07-14** under the **Pipeline A -> Pipeline B migration** checklist item's own nested slices --- its kernel half (an object-*set*-aware apply primitive) shipped as that migration's Slice 1 (2026-07-14; see `applyObjectSetTakeHold.ts`/`applyObjectSetDrop.ts`); bus payload + execute* widening (that migration's Slice 3) and perception narration (Slice 4) remain. **Slice 3 done (2026-07-15) via the `MultiKeyUpdate` redesign, not the originally-sketched bespoke-builder path** --- `applyObjectSetTransfer.ts` reruns the transfer as a live `MultiKeyUpdate` reducer over both hosts rather than applying a precomputed diff; `applyObjectSetTakeHold.ts`/`applyObjectSetDrop.ts` are now thin wrappers over it. "get tray" (BD-13's own worked example) now resolves and carries the glass in production. | C1 Plan IR registry; positions kernel | Decided (2026-07-10) |
| BD-14 | **Ungrounded position planning primitive shape --- first draft (2026-07-13).** Deliberately not exhaustive-in-advance; expand as new use cases demand rather than pre-designing a general type language. Full detail: **Ungrounded position planning primitive shape** (design debt, below). Summary: (1) the primitive-step union splits into first-class **`Change`** and **`Assertion`** kinds (STRIPS precondition/effect split) rather than assertions nested under changes --- an `Assertion` step may exist with no paired `Change` (reserved for future non-position parsing, e.g. "push the red button"); (2) `Assertion` carries a `negate: boolean` flag rather than paired predicate names (`notContainedBy`) --- negation is **ternary-safe**: it flips a resolved satisfied/violated outcome but leaves a `defer` outcome as `defer`; (3) first predicate is `containedBy` (disjunctive: membership / `On` / `Under` deterministic, `embedded into` LLM-only --- mirrors the sandbox's existing `Custom`-relation defer); (4) `Referent` gets three base kinds --- `objectSpan` (object referred to by a parsed span), `actingCharacter` (the command's issuing character, no span involved), and `currentHost` (host of a nested `Referent`, recursively typed --- composes over `actingCharacter` to express "the room the actor is in" without a fourth dedicated kind). | C1 (ungrounded-primitive types) | Decided (first draft) |
| BD-15 | **Relational-patch host scope --- first draft (2026-07-13; graduated to Decided 2026-07-13).** BD-6's "always the actor's current room" was an unexamined v1 scope artifact, not a fundamental constraint. Motivating example: player holds both **`string`** and **`spinning top`**; "wrap string around top" should establish a **`Custom`** relation between two objects that share the character's **inventory** host, not a room --- today this is hard-rejected by `applyRelationalPatch`'s **`isEphemeraRoomId`** guard ([`positions/positionGraph/index.ts:316`](../../../../../lambda/ephemera/dataSource/positions/positionGraph/index.ts)) purely because neither object is room-hosted, even though the actual structural legality check already present in that method (**`bothObjectsOnGraph`**) would pass cleanly. **Proposed direction (not yet decided):** a new **`sameHost(subject, object)`** `Assertion` predicate --- distinct from `containedBy` (checks host-membership *equality* between two objects, not a structural relation to a container) --- whose Synthesize-stage evaluation both gates legality **and** determines the concrete host id for the grounded `EstablishRelationStep`/`DissolveRelationStep` (today hardcoded via BD-6 to "the room"). **Open questions, not yet resolved:** (1) **Scope of generalization** --- does *every* `establishRelation`/`dissolveRelation` (even the common room-hosted case) now require evaluating a `sameHost` assertion, or does room stay a fast-path default that skips this machinery, with `sameHost` invoked only for the held-item case? **Provisionally answered by BD-16 (2026-07-13): yes, every `establishRelation` needs the assertion, but satisfying it can itself be deterministically fast-pathed for enum relation kinds** --- see BD-16; this row's remaining sub-questions (2)--(4) are independent of that answer. (2) **Where the host referent lives --- provisionally resolved (2026-07-13): no host field needed on the `Change` at all.** `EstablishRelationChange`/`DissolveRelationChange` (shipped 2026-07-13, `plan/compileUngroundedPlan.ts`) currently carry **no host field**, and that turns out to be correct rather than a gap: `sameHost(subject, object)` reuses the `Assertion` type's existing generic `subject`/`object` fields (already present on `ContainedByAssertion` --- no new shape needed, per BD-14's "grow the union as concrete cases demand"), and those roles are *not* neutral --- they carry the same directional meaning as the paired `Change`'s `subject`/`target` (guaranteed to align, since the Plan-stage compiler builds both from the same frame in one pass). For enum relation kinds, the deterministic repair is "subject moves to object's current host" (see BD-16), so the shared host is simply *derived* as "wherever object/target is" once construction runs --- nothing needs to be carried as an explicit field. Still provisional, not exhaustively verified against a wide range of verbs (see BD-16 (1)). (3) **Blast radius --- reframed (2026-07-13): real, but not a compatibility risk.** Unlike everything else built this week (new, unwired or additive-only modules), this reaches into the currently-**shipped, in-request-path** `establishRelation`/`dissolveRelation` pathway ([`compileRelational.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/compileRelational.ts), [`resolveRelationalGrounding.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/resolveRelationalGrounding.ts), frame extraction's **`hostRoomId`** field, and the **`Object Establish Relation`**/**`Object Dissolve Relation`** bus-payload contract, all typed to **`roomId`** today) --- genuinely a different kind of change than anything else this week: it revises live behavior rather than adding dormant capability. **But this project is pre-rollout** (single dev instance, no external consumers of any kind, confirmed by the user) --- so the "narrower additive extension to avoid a breaking contract change" motivation **no longer applies**; there is no compatibility hazard to design around, only a same-codebase refactor. That reframes what's actually left to manage down to two ordinary engineering concerns, not risk-aversion: **regression correctness** (does the existing, exercised room-hosted path still behave identically after the change) and **reviewability** (keeping the diff a sensible, checkable size via slicing --- see the C1 slice breakdown in Recommended Order below). **Decided (2026-07-13):** given no compatibility constraint, **in-place widening** of `hostRoomId` (rename/generalize to a membership-host-typed field) --- not a narrower additive extension maintaining two parallel code paths (room-only legacy + held-item addition). The "narrower additive extension" option was mainly justified by compatibility avoidance, which is moot pre-rollout; in-place widening is simpler and there's no longer a reason to prefer the alternative. (4) **Relationship to BD-13's separate persistence primitive --- resolved (2026-07-13): stay deliberately separate, permanently, not just pending generalization.** BD-13 slice 1 ([`hostRelationalEdgeCarryTransactItems.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/relational/hostRelationalEdgeCarryTransactItems.ts), renamed from `...Recreation...` for clarity --- see the C1 checklist) deliberately built an **additive, separate** host-agnostic path instead of touching `applyRelationalPatch`/`HostRelationalPatch`. Initially reasoned as "that pathway is legitimately Room-only per BD-6" --- but the real, durable reason survives BD-6 being revised: **`applyRelationalPatch`/`HostRelationalPatch` create or destroy a fact** (a player deliberately asserting or retracting a relationship via `establishRelation`/`dissolveRelation`); **BD-13's mechanism relocates a fact that already exists**, intact, because its objects moved host together --- a **carry**, not an **add**. That's a difference in what operation is being performed, not merely which hosts are legal, so the two stay separate even once `applyRelationalPatch` itself generalizes beyond Room-only hosts (BD-15 (3))'s generalization applies to the create/destroy pathway only. | C1 relational `Change`/kernel; BD-6 revision | **Decided (first draft)** |
| BD-16 | **Synthesize's Expansion vs. Validation sub-roles, part 2: deterministic assertion-driven plan expansion --- first draft (2026-07-13; graduated to Decided 2026-07-13).** Motivating example: character holds **`tray`**; **`table`** is in the room. "put tray on table" is an `establishRelation` intent where subject and target don't share a host --- the deterministic fix is to insert a **`transferMembership`** step (tray, character -> room) before the `establishRelation` step, functionally identical to the player saying "drop tray" then "put tray on table," but derived from **one** command via assertion-driven expansion rather than explicit player sequencing. **Mirror-image of BD-13:** `interactionUnderTransfer` (BD-13) answers "a transfer is happening --- what happens to existing relations touching the moved object?" (enum kinds resolve deterministically to dissolve/carry; `Custom` defers to LLM). This is the **inverse** question --- "a relation is intended --- what transfer satisfies a violated `sameHost` precondition (BD-15)?" --- same enum-deterministic / `Custom`-deferred shape, opposite direction of inference; the classification-table shape from BD-13 is reusable, not a new mechanism. **Reconciles with, does not violate, the Grounding/Expansion/Validation split (now named and durable --- see `AGENT.concepts.md` "Synthesize's three sub-roles"):** the pure Validator (sandbox) still never expands a candidate --- this expansion belongs to Synthesize's **Expansion** sub-role, the same sub-role `computeCarryClosure` already belongs to (a KR-grounded function that completes an ungrounded primitive into a full candidate *before* Validation ever sees it). FT-5 confidence scoring still applies to the complete, post-expansion candidate, not a pre-expansion one, so this does not reopen the confidence-integrity concern that originally motivated keeping the sandbox from expanding on its own. **Connects to `multiPresent`:** the `Custom`/LLM-deferred case may resolve into a multi-host co-location outcome (e.g. "wrap string around top" could leave the string simultaneously held **and** room-hosted) --- exactly the state `AGENT.concepts.md` already scoped *out* of `Referent` and routed to the existing `multiPresent` complexity class, today a hard Error everywhere it's hit. If the LLM validator is ever allowed to resolve a `Custom`-relation defer into a multi-host state, `multiPresent` stops being purely an error outcome for at least this path --- a real dependency this creates, not solved here. **Open questions, not yet resolved:** (1) **Directionality --- provisionally resolved (2026-07-13): "subject moves to object's/target's current host," embedded directly on `sameHost` rather than re-derived from the sibling `Change`.** `sameHost`'s `subject`/`object` fields carry the same directional roles as the paired `Change`'s `subject`/`target` (see BD-15 (2)) --- so the repair rule is fully computable from the assertion step alone: "if `sameHost(subject, object)` is violated for an enum relation kind, insert `transferMembership(subject, from: currentHost(subject), to: currentHost(object))`," with no need for Synthesize's Expansion sub-role to inspect the sibling `Change` step at all (future-proofs the case where an `Assertion` has no paired `Change`, per BD-14). This does **not** conflate assertion with repair --- `sameHost` still only checks (satisfied / violated / defer); embedding `subject`/`object` is a naming/locality choice, not new behavior. Checked against two examples: "put tray **on** table" (subject=tray held, object=table in room --- subject moves to room, correct) and "attach charm **to** necklace" (subject=charm in room, object=necklace held --- subject moves to the character's inventory, i.e. "pick up the charm to attach it," also correct) --- both hold under a uniform "subject moves to object's host" rule, independent of which one happens to be held. "Wrap string around top" is where it stops working even in principle, which is a **second, independent** reason (beyond "mechanics unknown") that `Custom` must defer: a flexible/continuous object doesn't cleanly "relocate wholesale" to another host at all, so the entire "subject relocates" model the enum case relies on doesn't apply. Still a strong default hypothesis, not exhaustively verified across the space of verbs/relations --- flagged for revision if a counterexample surfaces, per BD-14's "expand as needed" discipline rather than guessed ahead further now. (2) **Where this expansion table lives in code --- resolved (2026-07-13): a new function alongside `interactionUnderTransfer.ts` in `synthesize/`, not folded into the same table, and not yet a dedicated subdirectory.** This generalized into durable vocabulary rather than staying a one-off placement call --- **Expansion** is now a named Synthesize sub-role (`AGENT.concepts.md`, "Synthesize's three sub-roles: Grounding, Expansion, and Validation"), alongside **Grounding** (referent -> id resolution; single-object slice 1 done 2026-07-13, see the Synthesize-stage compiler checklist bullet --- Expansion invocation and full interleaving with Grounding still not built) and **Validation** (the sandbox). Both known Expansion instances (`computeCarryClosure`, shipped; this `sameHost` repair table, not yet built) share one technique --- a classification table keyed by relation kind, enum-deterministic / `Custom`-deferred --- but that shape is explicitly documented as **the first pattern discovered, not Expansion's scope of authority**: a future precondition may need a resolution mechanism that doesn't fit this shape at all (consults more than two objects, ranks several valid repairs, has no relation-kind axis). Don't generalize the enum/`Custom` pattern into "how Expansion always works" from two examples. (3) **`multiPresent` follow-on --- resolved (2026-07-13): explicitly deferred future capability, out of scope for BD-16; audited for corner-painting risk, none found.** `multiPresent`'s trigger (`complexityPreGates.ts:26-27`, `containers.length > 1`) is a pre-existing KR-state check with zero coupling to anything built this week (`Referent`, `ParsePlanStep`, `sameHost`, the edge-carry primitive) --- nothing here constrains it. The kernel layer doesn't structurally forbid a multi-host outcome either: `applyHostEffects.ts`'s `validateHostEffects` validates each `HostEffect` independently against its own target host, so a batch adding one object to two hosts without a matching remove would already pass today's validation as-is. `sameHost`'s `Custom`-defer path stays genuinely open-ended (just "violated, defer," no commitment to a resolution shape), so a future LLM validator resolving into a multi-host state isn't precluded. **One concrete, real future touchpoint, not a corner:** `applyObjectTakeHold.ts`/`applyObjectDrop.ts` call `internalCache.Positions.setMembershipContainers({ containers: [singleHost] })`, which *replaces* rather than appends --- whoever eventually builds real multi-host resolution will need to touch that call (or add a parallel one). Not built, but not blocked. (4) **Relationship to BD-15** --- BD-16 provisionally answers BD-15's open question (1); BD-15's remaining sub-questions (host referent field placement, production blast radius, relationship to BD-13's separate persistence primitive) still need resolution independently. | C1 relational `Change`/Synthesize Expansion sub-role; BD-15 | **Decided (first draft)** |
| BD-18 | **Synthesize -> Identify backtrack/correction channel --- named future direction, not yet scoped (2026-07-14).** First written down 2026-07-13 as one paragraph under **Assertion failure modes** (Phase C design debt, "Ungrounded position planning primitive shape"), in the older Identify/Plan/Synthesize vocabulary, disconnected from the Grounding/Expansion/Validation names and the Pipeline A/B framing that came later --- promoted to its own row (2026-07-14) so it surfaces on future BD scans instead of staying buried in prose. **The gap:** none of Grounding's (`groundReferent.ts`/`groundChange.ts`, `ok: false`) or Expansion's (`expandTransferMembership.ts`, `verdict: 'error'`) terminal-failure outcomes can currently ask Identify to reconsider a candidate given what Synthesize found (e.g. "widen the search to this other host, given the relation Plan is asking for") --- every failure is hard-terminal or (for `defer`) escalates to an LLM validator, never loops back upstream. The richer alternative --- **Backtrack** the assertion/grounding failure into Identify as a correction/ranking signal (per the Backtrack fault-recovery pattern in [`llm/AGENT.concepts.md`](../../../../../lambda/ephemera/llm/AGENT.concepts.md)) --- is named but **deliberately not built**: Identify returns top-N candidates today independent of any downstream assertion, so a candidate that loses on pure lexical/semantic match could in principle win a re-ranked retry that weights what Synthesize learned. Does **not** violate Identify/Plan independence --- only Synthesize (already holding both stages' output) would ever mediate a backtrack. **Action for now, not a build commitment:** keep the terminal-failure result types (`GroundReferentResult`, `GroundChangeResult`, `ExpandTransferMembershipResult`, and whatever `Assertion` evaluation eventually returns) **open to a third outcome** rather than letting the Pipeline A -> B migration harden call sites around just today's two shapes (success / terminal-fail) --- flagged in code comments at each of those three result types. Revisit scoping this once the migration's own Grounding+Expansion call sites are being written, since that's exactly where a caller would first need to decide what to do with a hypothetical backtrack outcome. | Synthesize-stage compiler (Grounding/Expansion result shapes); Pipeline A -> Pipeline B migration | **Named, not scoped** |
| BD-17 | **Membership sandbox-wiring (Slice 4b) accepts a carry-relation rejection as correct, not a regression --- decided (2026-07-14); updated (2026-07-14) once Slice 2 of the Pipeline A -> B migration made the rejection real rather than a blanket rule.** Wiring the sandbox's full `applyTransferMembershipStep` into the live membership selector means an object with a `carry`-classified boundary relational edge (e.g. a glass sitting `On` a tray) surfaces as illegal for a plain "get tray" --- where pre-Slice-4b it silently succeeded, moving the tray alone and leaving the glass's `On` edge pointing at an object no longer co-located. **Confirmed with the user (2026-07-14):** this is the correct behavior, not a regression to avoid --- today's silent success is itself a latent correctness gap (BD-13's whole premise), and surfacing it as a rejected command is more honest than the current silent world-state drift. **Slice 4b's interim message was `incompleteTransferSet` (a blanket rule: any carry-classified edge -> reject, without computing what the real transfer set would be). Slice 2 of the Pipeline A -> B migration (done 2026-07-14) replaced this with a real computation:** `expandTransferMembership.ts` now runs during the dry run, correctly computes the carry-closed set + dissolve steps, and validates them via `evaluateSandboxPlan` --- the rejection persists, but now because the compiler (a KR-agnostic-to-the-kernel layer) genuinely cannot *apply* a multi-object move yet, not because the candidate looked incomplete. The message changed accordingly to **`multiObjectTransferNotYetSupported`**. **Resolved (2026-07-15):** the **Pipeline A -> Pipeline B migration**'s Slice 3 (bus payload + result-type + `execute*` widening, shipped via the `MultiKeyUpdate` redesign) landed --- "get tray" now correctly carries the glass along in production instead of either silently dropping it (pre-Slice-4b behavior) or rejecting the command (this row's interim `multiObjectTransferNotYetSupported` behavior, itself now removed from `sandboxMembershipDryRun`). **Recorded as its own BD row** (rather than folded into BD-13's own row) because it's a distinct, user-approved decision about interim production behavior during the Pipeline A -> B transition, not a design property of BD-13's carry-closure mechanism itself --- a future reader hitting a newly-rejected "get tray" command in the wild should find this row, not conclude it's an unreviewed bug. | Synthesize-stage compiler Slice 4b; BD-13; Pipeline A -> B migration | **Decided** |

### Deterministic enrich boundary (BD-12)

Graduated to [`llm/AGENT.contract.md`](../../../../../lambda/ephemera/llm/AGENT.contract.md) (**Deterministic enrich boundary**) and [`llm/AGENT.concepts.md`](../../../../../lambda/ephemera/llm/AGENT.concepts.md) (design seams, field ownership, fast-path closure). Output trust models (trusted-output vs fault-tolerant) are documented separately in the same concepts file.

### BD-10: defer vs hard Error (litmus)

| Outcome | When | Phases B--C | Phase D |
| --- | --- | --- | --- |
| **Success (deterministic plan)** | Clean host topology; compiler emits length-1 or BD-8 length-2 plan | Apply (atomic per BD-9) | Same |
| **`defer`** | Existing relational edges on subject/target require interaction reasoning (not covered by deterministic legality rules) | **Error** stub --- "rules don't cover this complexity" | Plan LLM hop -> validate registry steps -> atomic apply |
| **Hard Error** | Structural/policy (see BD-10 row) | Error | Error (no plan LLM) |

**Primary defer case (only expected defer driver for relational vertical):** pre-existing **surface/custom** edges on subject or target on the room host when establishing or changing a relation --- e.g. already **`On`** table, player says **`Under`** table; subject **`Against`** wall plus new target relation; may need **`dissolveRelation`** + **`establishRelation`** plan the compiler cannot derive without interaction assessment.

**Not defer:** frame extraction (Phase B dedicated hop); B2 paraphrase->enum LLM; membership exit-edge touch (Phase A **`deferToComplexityLlm`** path for atomics only). Split classify intents (**BD-11** / B2.5) is structural routing, not defer.

## Recommended order

Use `[ ]` for pending and `[X]` for complete. Mark nested lines as you finish each sub-step.

### Phase B --- establishRelation vertical

- [X] **B0. Decision lock**
  - [X] Close BD-1 -- BD-10 in **Open decisions** (2026-07-04); BD-11 added (2026-07-04).
  - [X] Capture enum list and edge persist shape in positions contract draft (BD-2, BD-3) --- [`positions/AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md#host-local-relational-patch-phase-b-shipped-b4).

- [X] **B1. Frame schema + extraction**
  - [X] Define **`ManipulationFrame`** types (subject/target/relation spans; role tags) in actions enrich layer.
  - [X] Add frame extraction prompt + interpreter (new module under [`enrich/objectManipulation/frameExtract/`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/frameExtract/)).
  - [X] Wire: relational route (preposition guard successor / multi-span) -> frame extract -> compiler stub; membership lines still use **`verbClass`** acquire/release path from Phase A.
  - [X] Tests: fixture commands ("put broom on table", "lean rope against anvil", "tie cord around crate").

- [X] **B2. Relation normalizer**
  - [X] Deterministic map: common prepositions -> enum; **`in`** -> explicit defer/nesting message; else **`Custom`** + label.
  - [X] Optional small LLM hop for paraphrase -> enum only if deterministic map insufficient (BD-2 locked enum; defer LLM unless map gaps found in B2 tests).
  - [X] Tests: enum paths, custom paths, excluded **`in`**.

- [X] **B2.5. Split manipulation classify intents (BD-11)**
  - [X] Add **`ObjectMembershipIntent`** and **`ObjectRelateIntent`** to classify JSON, parser, guards, and **`IntentClassificationResult`** union in [`baseClasses.ts`](../../../../../lambda/ephemera/dataSource/actions/baseClasses.ts); remove **`ObjectManipulationIntent`** when parity tests pass.
  - [X] Update [`buildIntentClassificationPrompt.ts`](../../../../../lambda/ephemera/dataSource/actions/discriminateIntent/buildIntentClassificationPrompt.ts): Section A2 **`ObjectMembershipIntent`** (membership host transfer; **`verbClass`** required); Section A2b **`ObjectRelateIntent`** (in-host edge change; semantic relational examples without fixed preposition list; no **`verbClass`**). Shared tie-breakers vs AcmeOrder / NavigationIntent for manipulation family.
  - [X] Update deterministic fast path ([`deterministicChecks.ts`](../../../../../lambda/ephemera/dataSource/actions/discriminateIntent/deterministicChecks.ts)): synthesize **`ObjectMembershipIntent`** for minimal take/drop/get (not **`ObjectRelateIntent`**).
  - [X] Wire [`parseCommand.ts`](../../../../../lambda/ephemera/dataSource/actions/parseCommand.ts): **`ObjectMembershipIntent`** -> **`enrichObjectManipulation`** membership path; **`ObjectRelateIntent`** -> relational frame-extract path. Demote [`relationalRoute.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/relationalRoute.ts) from primary gate after parity tests.
  - [X] Tests: **`ObjectMembershipIntent`** lines (`pick up broom`); **`ObjectRelateIntent`** with and without listed prepositions; reject **`verbClass`** on **`ObjectRelateIntent`**; parseCommand E2E with mocked classify intent type.
  - [X] Durable docs: classify contract in [`actions/AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.implementation.md); enrich sequence in [`enrich/AGENT.md`](../../../../../lambda/ephemera/dataSource/actions/enrich/AGENT.md).

- [X] **B3. Grounding + legality**
  - [X] **Frame extract extension (BD-12):** add required **`operationKind`** (`establishRelation` \| `dissolveRelation`) to frame extract prompt, interpreter, and **`ManipulationFrame`**; remove **`operationKind`** from frame-extract forbidden fields. Tests: establish fixtures (B1) plus dissolve ("take rope off crate", "remove cord from crate").
  - [X] Resolve subject + target against catalogs; host selection per BD-6.
  - [X] Relational compiler: pass through frame **`operationKind`**; run B2 **`normalizeRelationSpan`** on **`relationSpan`** only (no prefix stripping or dissolve phrase buckets).
  - [X] Legality (BD-10): both nodes on host graph; **idempotent** duplicate edge -> allow/no-op; **conflicting** or non-trivial existing relational topology on subject/target -> **Error** stub in B--C (compiler **`defer`** bucket until Phase D).
  - [X] Replace interim **`relationalRoute`** / **`relationalPlacement`** terminal Error stubs for supported frames with grounded terminal parse (requires B2.5 **`ObjectRelateIntent`** routing for production path).

- [X] **B4. Positions persist + ingress**
  - [X] Implement **`applyHostRelationalPatch`** + **`HostRelationalPatch`** types per stub.
  - [X] Stream contract + guard (**`Object Establish Relation`** / **`Object Dissolve Relation`**) in [`publishedEvents.ts`](../../../../../lambda/ephemera/dataSource/actions/publishedEvents.ts).
  - [X] Coordinator under [`manipulation/relational/`](../../../../../lambda/ephemera/dataSource/positions/manipulation/relational/) (create tree).
  - [X] Tests: apply, idempotency, reject invalid patch.

- [X] **B5. Actions egress + perception**
  - [X] [`index.ts`](../../../../../lambda/ephemera/dataSource/actions/index.ts) publish stream on grounded relational parse.
  - [X] Perception fan-in: intent + fact -> **`WorldMessage`** (withhold geometry; assert relation per unknowns).
  - [X] End-to-end tests: parse -> stream -> apply -> transcript.

- [X] **B6. Durable docs**
  - [X] [`diegeticLogic/AGENT.operators.concepts.md`](../../../../../lambda/ephemera/diegeticLogic/AGENT.operators.concepts.md) --- **`establishRelation`** + **`dissolveRelation`** sections; refresh membership classify refs; replace out-of-scope stub.
  - [X] [`positions/AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) --- promote planning contract to shipped; ingress subsections + relational-changed bundle.
  - [X] [`actions/AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.implementation.md) --- replace relational stub prose; add relational operator playbook.

### Phase C --- Plan IR and composition

**Prerequisite:** Fault-tolerant Gateway exit **complete (FT-4.3, 2026-07-10)** --- span resolution artifacts, FT-1--FT-5 decided/shipped, Abstain/Consult contract. The Plan IR **span-grounding tail** (selector verdict + existence/presence guard; `resolveComponent` retired as a standalone primitive) should consume provisional **`SpanCandidatePool`** handoffs, not trusted-output-only identity output. **C1 may begin.** Forward-looking design debt from that gateway is consolidated in **Phase C design debt** below (planning file deleted; git retains history).

- [ ] **C1. Plan IR types + registry**

  Reordered 2026-07-12 around the Identify / Plan / Synthesize decomposition --- see **Pipeline decomposition** note above and [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.concepts.md). The two new leading bullets are foundational prerequisites for everything below them.

  - [X] **Extract Synthesize module** (mechanical, low-risk; **done 2026-07-12**): moved `interactionUnderTransfer.ts` (contains `computeCarryClosure`) out of the sandbox's file family into `enrich/objectManipulation/synthesize/`, mirroring the existing `frameExtract/` / `embeddingMatch/` subdirectory convention; sandbox files (`sandboxState.ts` / `sandboxStep.ts` / `sandboxPlan.ts`) stay at `objectManipulation/` root and now import Synthesize-stage logic from its own module. No behavior change --- pure move (one importer, `sandboxStep.ts`, updated), all identifiers unchanged. See [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.concepts.md) ("Current conflation").
  - [X] **Define ungrounded position planning primitive types + Plan-stage compiler** (**done 2026-07-13**): `Referent` / `Change` / `Assertion` / `UngroundedPlanStep` types per BD-14, plus a pure, synchronous, zero-KR-access Plan-stage compiler in new [`enrich/objectManipulation/plan/`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/plan/) (`ungroundedPrimitive.ts`, `compileUngroundedPlan.ts`), mirroring the `frameExtract/` / `synthesize/` subdirectory convention. **Change-only in this slice** --- `compileMembershipUngroundedPlan` / `compileRelationalUngroundedPlan` map the shipped `MembershipManipulationFrame` / `ManipulationFrame` into `Change` steps only; no `Assertion` steps are emitted yet, because `MembershipManipulationFrame` carries no location-disambiguating span (e.g. "from table") to build one from, and the failure modes BD-14's `containedBy` examples resembled are already shipped Synthesize-side legality (`notCarryingObject` / `alreadyHoldingObject` / `dissolveNoMatchingEdge`). **New, unwired module** --- nothing existing imports from `plan/` yet, same pattern as the sandbox (S1--S5) before its readiness proof. Frame-extraction extension to unlock real `Assertion` emission is tracked as a new forward-looking item, not built here --- see **Ungrounded position planning primitive shape** in Phase C design debt below (updated) and [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.concepts.md) ("Ungrounded position planning primitives vs. grounded primitives").
  - [X] Define **`ParsePlanStep`** union and runtime primitive registry (transferMembership, establishRelation, dissolveRelation) --- the **grounded** shape the Synthesize-stage compiler emits (**done 2026-07-13**): [`enrich/objectManipulation/parsePlanStep.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/parsePlanStep.ts) --- `TransferMembershipStep` (BD-13-compliant, `objectIds: ReadonlySet<EphemeraObjectId>`, not a single id), `EstablishRelationStep`, `DissolveRelationStep`; `RUNTIME_PRIMITIVE_NAMES` closed tuple + `isRuntimePrimitiveName` guard (the registry Phase D's D2 bullet depends on: "steps must be valid primitives from registry only"). **`resolveComponent` is not a runtime primitive**; grounding is the selector verdict + existence/presence guard in the compiler tail, not an executable step --- confirmed as a passing regression test (`isRuntimePrimitiveName('resolveComponent')` is `false`). **Deliberately excludes `Assertion`** (not a runtime primitive --- gates/evaluates, produces no kernel effect) and **does not touch** `identityPlanCandidate.ts`'s stub types or `sandboxPlan.ts`'s `SandboxPlanStep` --- both explicitly point at "once C1 lands" / "Synthesize-stage compiler" as their reconciliation point, not this bullet. New, unwired file --- nothing existing imports from it yet.
  - [ ] **`transferMembership` accepts an object set (BD-13)**, not only a single object; compiler builds the set via the sandbox's internal/boundary edge partition (transitive closure over **`carry`**-classified edges) so e.g. "get tray" (glass **`On`** tray) compiles to `dissolveRelation` + a set-valued `transferMembership` rather than deferring. Kernel (`applyObjectTakeHold`/`applyObjectDrop`) must recreate internal edges on the destination host graph within the same atomic `HostEffect` bundle. **Slice 1 of 3 done (2026-07-13; renamed 2026-07-13 for clarity):** the named blocker (`applyRelationalPatch`'s room-only guard) is resolved via a new, additive, host-agnostic persistence primitive --- [`positions/manipulation/relational/hostRelationalEdgeCarryTransactItems.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/relational/hostRelationalEdgeCarryTransactItems.ts) (`HostRelationalEdgeCarry` type in `manipulation/types.ts`; originally named `HostRelationalEdgeRecreation`/`...RecreationTransactItems`, renamed once it was clear this is conceptually a **carry** --- an existing relationship persisting across a host move --- not a **create**, matching the `carry`-classified-edge vocabulary `interactionUnderTransfer.ts` already established), dispatching Room (`Meta::Room`/`fromRoomMeta`) vs. Character (`Meta::Character`/`fromCharacterMeta`) and reusing `EphemeraPositionGraph.addRelationalEdge` (host-agnostic, already used this way by the sandbox) rather than touching `applyRelationalPatch`/`HostRelationalPatch` at all --- that pathway stays intentionally Room-only per BD-6, **and stays deliberately separate even after BD-15/BD-16 generalize it beyond Room-only** (see BD-15 (4), resolved). **Slice 2 of 3 done (2026-07-13):** kernel bundling --- `ObjectTakeHoldApplyArgs`/`ObjectDropApplyArgs` (`positions/manipulation/membership/types.ts`) gained an optional `carriedEdges?: HostRelationalEdgeCarry[]` field (reusing the slice 1 type verbatim, including its own `hostId`), and `ApplyHostEffectsArgs` (`positions/manipulation/types.ts`) gained a matching optional `edgeCarries`. [`applyHostEffects.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/applyHostEffects.ts) is the single owner of the `transactWrite` call, so it concatenates `buildTransactItemsFromHostEffects(hostEffects)` and `buildHostRelationalEdgeCarryTransactItems(edgeCarries)` into one `transactItems` array before the one atomic `transactWrite` --- satisfying BD-9. `computePostApplyGraphsFromEffects` also applies each edge carry (`graph.addRelationalEdge`) so `postApplyGraphs` --- and therefore `applyObjectTakeHold`/`applyObjectDrop`'s cache-seeding --- reflects the carried edge. A new `validateEdgeCarries` check rejects an edge carry whose `hostId` isn't among the hosts already touched by `hostEffects` in the same call (the validation path that makes reusing `HostRelationalEdgeCarry` verbatim safe, rather than deriving/stripping `hostId`). Additive and back-compat at the call site: `executeObjectTakeHold`/`executeObjectDrop` are untouched, `carriedEdges`/`edgeCarries` default to `[]`. **Slice 3 of 3 --- Blocked (2026-07-13), not scheduled:** update the `Object Take Hold`/`Object Drop` bus payloads from singular `objectId` to a set, and wire whatever actually computes a carried set (sandbox `computeCarryClosure` output, or the future Synthesize-stage compiler) through `executeObjectTakeHold`/`executeObjectDrop` into this slice's new args. Traced where these bus events are produced today: **`actions/index.ts:476--514`** (`isParseCommandObjectManipulationResult` handling) --- the legacy single-object fast path, entirely unrelated to Plan-IR/Synthesize. **Deliberately deferred, not merely unscheduled**, on two preconditions: (a) the Synthesize-stage compiler (or the sandbox) must exist as a real producer of a carried set --- it doesn't today; (b) a resolved narration/copy design for how a carried-set take-hold/drop is described to the player via the perception leg ([`objectManipulationPresentationLegAdapters.ts`](../../../../../lambda/ephemera/dataSource/perception/objectManipulationPresentationLegAdapters.ts)) --- e.g. "you pick up the tray, and the glass on it comes along" is unresolved UX/copy design, not plumbing. Widening a shipped, in-request-path bus contract with no producer to validate the new shape against would be speculative; tracked as part of the **Pipeline A -> Pipeline B migration (cutover)** checklist item below (consolidated there 2026-07-14 --- this slice 3 deliverable and that migration's own final step turned out to be the same work), which is the trigger to revisit this.
  - [ ] **Synthesize-stage compiler**: ungrounded primitive + context (post-extraction Synthesize module + dry-run sandbox) -> grounded plan (usually length 1; sometimes 2+) or **`defer`** / **Error** per BD-10. (Was "deterministic compiler from `ManipulationFrame` + context -> plan" --- reworded 2026-07-12 to name the two stages explicitly.) **Confirmed (2026-07-13, three-agent research pass) to be a multi-slice epic, not one slice** --- recommended slice order below, so each can be picked up via `/next-slice` on its own line rather than re-deriving the whole scope each time.

**Framing note (2026-07-14), added after the user found slices 4b/5 hard to parse against this bullet's own name:** two parallel pipelines exist. **Pipeline A** (old, live, shipped, handles every real command today: `compileMembershipAtomic.ts`/`compileRelational.ts` -> propose-N -> FT-5 select -> legality -> guard -> bus event, working entirely on single-object `IdentityPlanCandidate`/`RelationalIdentityPlanCandidate`). **Pipeline B** (new, fully built by slices 1-3, fully tested, entirely unreachable from any real command: `compileUngroundedPlan.ts` -> `groundChange`/`groundReferent` -> `expandTransferMembership` -> `toSandboxPlanStep` -> `evaluateSandboxPlan`). **Slices 4a/4b/5 are not wiring Pipeline B in** --- they are convergence work *within Pipeline A*, making its two legality checks (membership, relational) route through the same `evaluateSandboxPlan` engine Pipeline B's Validation already uses, instead of each maintaining its own duplicate direct-call implementation. The payoff: Pipeline B's Validation leg is already carrying real production traffic (via Pipeline A) by the time anyone attempts the actual migration, so that migration's risk narrows to "does Grounding+Expansion produce correct grounded steps," not "does this whole new Validation logic hold up too." Proposal/Selection and Grounding/Expansion remain two entirely separate architectures between A and B --- 4a/4b/5 do not narrow that gap at all. **The actual "flip the switch" migration slice is not yet scoped** --- see the new checklist item below, added for exactly this reason (so it isn't confused with 4a/4b/5, and isn't forgotten as a real, larger, riskier piece of remaining work). **One more thing to keep in view before that migration is written:** Grounding's and Expansion's terminal-failure result types are currently hard-terminal-only (`ok: false` / `verdict: 'error'`) with no path back upstream to Identify --- BD-18 names a future Synthesize -> Identify backtrack channel that would want a third outcome on those same types; don't let the migration's new call sites overfit to just today's two.
    - [X] **Slice 1: Grounding.** **This is where Grounding lives** (`AGENT.concepts.md`, "Synthesize's three sub-roles") --- resolving each `Referent` (`objectSpan`/`actingCharacter`/`currentHost`) into a real id using Identify's output, the join step that turns Plan's `UngroundedPlanStep[]` into a `ParsePlanStep`-shaped candidate in the first place. **Done (2026-07-13):** [`synthesize/groundReferent.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/synthesize/groundReferent.ts) (resolves a single `Referent`, given already-resolved per-span ids --- not raw ranked pools --- plus an injected `getCurrentHost` callback for the live KR lookup `currentHost(X)` needs) and [`groundChange.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/synthesize/groundChange.ts) (grounds a whole `Change` into a `ParsePlanStep`: `establishRelation`/`dissolveRelation` derive their host as `currentHost(actingCharacter)` per BD-6's still-current default; `transferMembership` produces a **single-object, not-yet-carry-closed** `objectIds` set). New, additive, unwired --- no existing module imports from it yet, same pattern as Plan's compiler and `ParsePlanStep` before it.
    - [X] **Slice 2: invoke Expansion.** Call the already-shipped `computeCarryClosure`/`boundaryEdgeOutcomes` (`synthesize/interactionUnderTransfer.ts`, BD-13) on a grounded `transferMembership` candidate to grow its single-object `objectIds` set to full carry-closure --- shipped logic, zero real callers today. **Done (2026-07-13):** [`synthesize/expandTransferMembership.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/synthesize/expandTransferMembership.ts) --- takes a single-object `TransferMembershipStep` (slice 1's output) + an injected `getGraph` callback, grows `objectIds` to the carry-closed set, and emits a `DissolveRelationStep` for every boundary edge classifying `dissolve` (matches BD-13's own worked example: "get tray" -> `dissolveRelation`(tray-table) + `transferMembership([tray, glass])`). Any boundary `defer` outcome (`Under` subject-move or `Custom`) surfaces as `{ verdict: 'defer', decidable }`, per BD-10 --- not resolved here. **Known limitation, not fixed here:** a boundary `dissolve` edge on a Character-hosted source graph (possible today via an earlier BD-13 carry, independent of BD-6's room-only `establishRelation` default) can't be expressed as a `DissolveRelationStep` (`hostRoomId: EphemeraRoomId`-typed) --- surfaces as `{ verdict: 'error' }`; real fix is BD-15 slice 3's persistence-pathway widening. New, additive, unwired --- not called by `groundChange.ts` or anything else; **the "how do Grounding and Expansion interleave" orchestration question (`AGENT.concepts.md`) is still not answered**, deliberately deferred to a future integration slice, not resolved here despite this slice's original framing suggesting it would be.
    - [X] **Slice 3: reconcile the two grounded-candidate type families.** `IdentityPlanCandidate`/`RelationalIdentityPlanCandidate` (`identityPlanCandidate.ts`, stub-labeled, singular-object, what the sandbox and live selectors actually consume today) vs. `ParsePlanStep` (`parsePlanStep.ts`, the real BD-13-compliant registry, set-valued, shipped-but-unwired --- and now what `groundChange.ts` produces). Both stub files' comments point at "once C1 lands"/"Synthesize-stage compiler" as their reconciliation point --- this is that point. **Done (2026-07-13):** [`synthesize/toSandboxPlanStep.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/synthesize/toSandboxPlanStep.ts) --- `relationalStepToSandboxPlanStep`/`transferMembershipToSandboxPlanStep` convert `ParsePlanStep`s (including `expandTransferMembership.ts`'s `dissolveSteps`/`transferStep` output, slice 2) into `SandboxPlanStep`/`IdentityPlanCandidate` values. **Key finding that shaped the design:** reading `validatePlanDryRun.ts` in full showed its legality functions read only a narrow slice of `IdentityPlanCandidate` (`identity.objectId`/`identity.locus`/`plan.operationKind` for membership; `subject.objectId`/`target.objectId`/`plan.operationKind`/`plan.relation` for relational) --- never `confidence`/`jointRelevance`/`label`/`sourceTags`. Combined with `selectIdentityPlanTuple.ts`'s `selectPlanTuple` core already being generic over the candidate type, this meant **no live file needed to change at all**: the converter supplies real derived values (locus derived from `fromHostId`/`actingCharacterId`; `operationKind` derived from Room<->Character transfer direction) for what Validation reads, and documented placeholder values (unread) for the rest. Zero changes to `identityPlanCandidate.ts`, `validatePlanDryRun.ts`, `sandboxStep.ts`, `sandboxPlan.ts`, or any live selector file. **Verified end-to-end:** a test chains `groundChange` -> `expandTransferMembership` -> this converter -> `evaluateSandboxPlan` on BD-13's own "get tray" worked example and gets a `legal` verdict with the correct post-state --- the first test anywhere exercising Grounding + Expansion + Validation together, though nothing production-side calls this chain yet.
    - [X] **Slice 4a: wire the sandbox into the relational selector.** Feed `evaluateSandboxPlan` real KR state at the live relational call site (`selectRelationalFromPools.ts`), which today calls `validateRelationalPlanDryRun` directly, bypassing the sandbox entirely --- replacing the pattern the test-only `dryRunFromSandbox` adapter (`sandboxSelectorReadiness.test.ts`) proved out with real production glue. **Done (2026-07-13):** `selectRelationalFromPools.ts` gained a local `sandboxRelationalDryRun` function --- internal-only change, `SelectRelationalFromPoolsInput`'s signature is untouched, so `compileRelational.ts` needed zero changes. This was a **pure refactor, confirmed zero live behavior change**: research traced the call chain and found exactly one host graph is fetched today (`frame.hostRoomId`'s), and it's exactly the one `applyRelationalStep` needs (establishRelation/dissolveRelation are structurally single-room, BD-6) --- every existing legal/illegal test case in `selectRelationalFromPools.test.ts` passed unchanged, proving byte-identical `DryRunOutcome`s through the new path; full package test suite (311 suites, 2401 tests) also passed clean. `validateRelationalPlanDryRun` itself is untouched (still used internally by `sandboxStep.ts`'s `applyRelationalStep`). Deliberately discards the sandbox's mutated `outcome.state` --- this is a per-candidate legality probe during selection, not a commit.
    - [X] **Slice 4b: wire the sandbox into the membership selector.** Scoped out of slice 4a (confirmed with the user 2026-07-14) because it's genuinely messier, not just a bigger version of the same refactor: `compileMembershipAtomic.ts` calls `selectMembershipFromPool` with an **empty** `dryRunContext` today --- no graph at all reaches the dry-run during selection, meaning `validateMembershipPlanDryRun`'s exit-edge-defer branch is **dead code in production**; the real defer/complexity signal for membership comes entirely from the separate, later `complexityPreGates.ts` mechanism. **Done (2026-07-14, together with Slice 5):** `MembershipManipulationFrame` gained `hostRoomId` (already available at the call site via `ParseCommandInput.hostRoomId`, just not previously picked --- no new plumbing); `compileMembershipAtomic.ts` now pre-fetches the room's graph and the acting character's own inventory graph via `positionsReadDeps.getPositionGraph` **before** calling `selectMembershipFromPool`, and builds a `SandboxState` (`buildSandboxState`) from whichever resolved; `selectIdentityPlanTuple.ts` gained a `sandboxMembershipDryRun` function (promoting `sandboxSelectorReadiness.test.ts`'s `dryRunFromSandbox` pattern to production) that replaces the old `validateMembershipPlanDryRun(candidate, dryRunContext)` call, routing through `evaluateSandboxPlan`/`applyTransferMembershipStep` with real KR state. **Confirmed and accepted (2026-07-14, see BD-17): full sandbox routing means a `carry`-classified boundary edge (e.g. a glass `On` a tray) now surfaces as `illegal: incompleteTransferSet` during selection**, where it previously silently succeeded (moving the object alone, leaving the related object's edge stale) --- not a regression to avoid, but the correct behavior until the Pipeline A -> B migration wires in real carry-closure. Full package regression run (310 suites, 2396 tests) passed clean after this change, including extensive fixture updates across `selectIdentityPlanTuple.test.ts`, `selectMembershipFromPool.test.ts`, `compileMembershipAtomic.test.ts`, `index.test.ts`, and `parseCommand.test.ts` (all needed `hostRoomId`/`characterId` added to test fixtures and hostId-aware `getPositionGraph` mocks, since both host graphs are now fetched before selection regardless of outcome). Deleted `verbMembershipAgreement.ts`/its test as dead code (zero production callers, and its premise --- deriving `operationKind` from a pre-gate outcome --- no longer holds now that `ComplexityPreGateOutcome`'s `atomic` variant carries no `operationKind`).
    - [X] **Slice 5: retire `compileMembershipAtomic.ts`'s ad-hoc complexity-LLM fallback** for exit-edge/defer cases (comment there marks it pending "FT-3 sandbox retirement") --- done together with slice 4b, not as a separate pass, since the two were genuinely one piece of work. **Done (2026-07-14):** `complexityPreGates.ts`'s `evaluateComplexityPreGates` narrowed to exactly what the sandbox-mediated selector doesn't cover --- `containers.length === 0` (error) and `containers.length > 1` (`multiPresent`, still BD-16(3) deferred-future-capability territory, unaffected by this change) --- dropping the exit-edge branch and the locus-to-operationKind re-derivation branch entirely (both are the same check the selector itself now performs, with real graph access, earlier and more reliably; keeping both was the same `objectTouchesExitEdgeOnGraph` check running twice). The complexity LLM fallback itself is **not removed** --- it still exists and still gets invoked, just now only for cases that genuinely need an LLM (unmodeled locus, a sandbox `Custom`-boundary defer, or `multiPresent`), not for ordinary locus/exit-edge legality anymore.
  - [ ] **Pipeline A -> Pipeline B migration (cutover), including BD-13 slice 3.** Promoted out of the "Synthesize-stage compiler" bullet's own slice list (2026-07-14, at the user's prompting) --- it isn't part of *building* that compiler (functionally done as of Slice 3 above), it's the integration task that *consumes* it, alongside the already-built-but-unwired Plan-stage compiler and Identify's own live wiring (neither is called by any real command today either). **Consolidates what were briefly tracked separately as "Slice 6" (revisit BD-13 slice 3) and "Slice 7" (the migration itself)** --- they turned out to be the same work, not two. This is what actually lets a player carry a glass along with a tray in production. Bigger and riskier than anything above: it touches Identify/Plan's own live wiring, not just a legality-check swap. **Both Validation legs (relational, slice 4a; membership, slice 4b) are already fully converged on `evaluateSandboxPlan`, carrying real production traffic** --- this migration's remaining risk is genuinely just Grounding+Expansion correctness, not Validation. **One precondition BD-13 slice 3 flagged that this migration does not by itself resolve:** a narration/copy design for how a carried-set take-hold/drop is described to the player via the perception leg ([`objectManipulationPresentationLegAdapters.ts`](../../../../../lambda/ephemera/dataSource/perception/objectManipulationPresentationLegAdapters.ts)) --- e.g. "you pick up the tray, and the glass on it comes along" is unresolved UX/copy design, not plumbing, and stays open even once this migration ships. **Second precondition, added 2026-07-14 (BD-18):** don't let this migration's own call sites harden Grounding/Expansion's terminal-failure result types (`GroundReferentResult`, `GroundChangeResult`, `ExpandTransferMembershipResult`) around just today's two outcomes (success / terminal-fail) --- a future Synthesize -> Identify backtrack channel (BD-18, named but unbuilt) would want a third outcome on these same types, and this migration is exactly where new callers of them get written.

    **Broken into nested slices (2026-07-14), after three-agent research found "the migration" was not one integration task.** Notably, the research also found and corrected a wrong premise from the same research pass: it initially looked like realizing BD-13's own "get tray" worked example (dissolve `tray On table` + carry-move `[tray, glass]`) would need new **cross-primitive compound-apply** plumbing (bundling a `HostRelationalPatch` dissolve into the same atomic write as a membership move) --- building and testing that in Slice 1 surfaced a real bug (an edge dissolved twice, once by the patch and once automatically) that traced back to a wrong assumption. **Corrected finding: no such plumbing is needed.** `EphemeraPositionGraph.removeObject`'s own edge-stripping (`withoutEdgesReferencingObject`) already runs at commit time inside the existing membership-move `updateReducer`s ([`objectPlacementTransactItems.ts`](../../../../../lambda/ephemera/dataSource/positions/membership/objectPlacementTransactItems.ts), [`characterInventoryTransactItems.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/membership/characterInventoryTransactItems.ts)) --- so a `dissolve`-classified boundary edge (BD-13's `interactionUnderTransfer.ts` table) is *always* anchored on the object being removed from that very graph, and is therefore always already stripped as a side effect of that object's own hostEffect, atomically, with zero extra plumbing. `expandTransferMembership.ts`'s `DissolveRelationStep`s remain useful for Validation/legality purposes (confirming a boundary edge resolves to a clean outcome rather than `defer`) but need no separate kernel apply step. This narrowed Slice 1's real scope to just the object-*set*-aware apply layer.

    - [X] **Slice 1: compound atomic kernel-apply primitive (BD-13 slice 3, kernel half).** **Done (2026-07-14):** new [`applyObjectSetTakeHold.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/membership/applyObjectSetTakeHold.ts) / [`applyObjectSetDrop.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/membership/applyObjectSetDrop.ts) --- object-*set*-aware siblings of the existing singular `applyObjectTakeHold.ts`/`applyObjectDrop.ts`, accepting `objectIds: EphemeraObjectId[]` + optional `carriedEdges`. Each calls the existing pure `planObjectTakeHoldTransfer`/`planObjectDropTransfer` once per object (confirmed stateless, safe in a loop), merges the resulting `hostEffects` arrays, and makes **one** call to the already-built `applyHostEffects` (its `hostEffects: HostEffect[]` plural shape and `edgeCarries` bundling, BD-13 slices 1-2, needed **zero changes** --- confirmed after research and reverting an unneeded `relationalPatches` widening attempt, see the correction above). Builds one "Object Moved" fact + membership-container cache update per object in the set. Verified end-to-end against BD-13's own "get tray" worked example (`applyObjectSetTakeHold.test.ts`): moving `[tray, glass]` atomically dissolves `tray On table` automatically and recreates `glass On tray` on the destination via `carriedEdges`, in one `transactWrite`. **Purely additive --- zero production callers yet**, same pattern every prior Synthesize-stage-compiler slice used. `applyHostEffects.ts` itself is unchanged by this slice.
    - [X] **Slice 2: wire Expansion into the membership compiler path.** **Done (2026-07-14):** `sandboxMembershipDryRun` (`selectIdentityPlanTuple.ts`) now invokes `expandTransferMembership.ts` (Synthesize's Expansion sub-role) per candidate, replacing the naive singleton `transferSet` with the real carry-closed set + dissolve steps, before validating via `evaluateSandboxPlan` (the same shared legality authority every other candidate already goes through). **Grounding was confirmed unnecessary** --- Identify already resolves a span straight to a concrete `EphemeraObjectId`, so the `TransferMembershipStep` is built directly rather than via `groundChange`; `groundReferent.ts`/`groundChange.ts`/`compileUngroundedPlan.ts` stay untouched. **The sync/async mismatch resolved for free:** `compileMembershipAtomic.ts` already pre-fetches both graphs (Slice 4b), and `SandboxState` already is `Map<EphemeraMembershipHostId, EphemeraPositionGraph>`, so `state.get(hostId)` is already a valid synchronous `getGraph`. **Key architectural finding that reshaped this slice's terminal behavior:** `compileMembershipAtomic.ts` is a pure compiler --- it never touches the kernel; the actual apply only happens later via the bus/`execute*` path (Slice 3, not yet built). So even a fully-computed, Validation-legal multi-object transfer still can't be *applied* yet --- this slice declines it with a new, more accurate message, **`multiObjectTransferNotYetSupported`** (`resolveObjectSpan.ts`), replacing BD-17's `incompleteTransferSet` wording (confirmed with the user: the candidate is no longer "incomplete," Expansion fully computed it --- just not yet appliable). **A second finding, caught by testing:** a dissolve-only closure (no carry, e.g. an `Against`-classified boundary edge) needs **no** apply-layer changes at all --- `EphemeraPositionGraph.removeObject`'s existing edge-stripping (see Slice 1) already handles it for free, so only `transferStep.objectIds.size > 1` (an actual carry) triggers the decline, not the mere presence of dissolve steps. Ordinary commands with no boundary relational edges are provably unchanged (existing tests pass unmodified); `defer` cases (`Under`-subject-move, `Custom`) unchanged in shape, now reachable via real classification instead of never firing.
    - [X] **Slice 3: bus payload + result-type + execute* widening.** **Done (2026-07-15), shipped as the `MultiKeyUpdate` redesign** rather than the originally-sketched "widen types, call Slice 1's `applyObjectSetTakeHold`/`applyObjectSetDrop`" path --- the 2026-07-14 pointer's fork was resolved in favor of rerunning the transfer computation itself as a `MultiKeyUpdate` reducer, per [[project-membership-consolidation-needs-sandbox-plus-multikeyupdate]]. **New primitive:** [`applyObjectSetTransfer.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/membership/applyObjectSetTransfer.ts) --- one `MultiKeyUpdate` item (`Keys` = departure + arrival host) whose reducer re-fetches both host graphs fresh, re-validates the transfer (presence + `boundaryEdgeOutcomes`) at commit time, and derives internal edges to carry live from the fetched source graph (no precomputed `carriedEdges` param anymore); paired `positionAdjacency#<hostId>` rows ride as plain unconditioned sibling `Delete`/`Put` items in the same transact --- **`cascade` turned out not to be needed**, since the transfer set and both host ids are caller-known before the reducer runs. **Supersedes** Slice 1's `applyObjectSetTakeHold.ts`/`applyObjectSetDrop.ts` internals (now thin wrappers over the new primitive) **and** the legacy singular `applyObjectTakeHold.ts`/`applyObjectDrop.ts` (deleted --- `executeObjectTakeHold`/`executeObjectDrop` are the single call site for every transfer size now, 1 included). `ParseCommandObjectManipulationResult.objectId` -> `objectIds: EphemeraObjectId[]` (in-place widening); `sandboxMembershipDryRun` (`selectIdentityPlanTuple.ts`) now threads the Expansion-computed carry-closed set through via a new `DryRunOutcome.objectIds` field instead of declining with `multiObjectTransferNotYetSupported` (message deleted, BD-17 fully superseded --- "get tray" now resolves and carries the glass). Full package regression (310 suites, 2400 tests) green after fixture updates across the selection/bus/perception chain. Durable docs: [`positions/AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md), [`positions/manipulation/AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/positions/manipulation/AGENT.implementation.md).
    - [ ] **Slice 4: perception-leg narration for a carried-set take-hold/drop.** Still blocked on a copy/UX design (flagged above), not just plumbing.
    - [ ] **Follow-on, not yet scoped:** BD-15/16's relational held-item generalization is separate, larger work, not re-scoped by this breakdown.
  - [ ] **BD-15/BD-16: relational host generalization (`sameHost` assertion).** Both **Decided (first draft, 2026-07-13)** --- all sub-questions resolved (some as strong default hypotheses, flagged for revision if a counterexample surfaces, same confidence level as BD-14). Recommended slice order:
    - [ ] **Slice 1:** add the **`sameHost`** `Assertion` predicate to `plan/ungroundedPrimitive.ts`'s `Assertion` union (reuses the existing generic `subject`/`object` fields --- no new shape); the Plan-stage compiler (`compileRelationalUngroundedPlan`) emits a `sameHost` assertion step before every `establishRelation`/`dissolveRelation` `Change` step, unconditionally (BD-15 (1): every relation needs it, not only the held-item case).
    - [ ] **Slice 2:** the deterministic expansion/repair table --- Synthesize's second **Expansion**-sub-role instance (BD-16 (2), resolved; see `AGENT.concepts.md` "Synthesize's three sub-roles"), a new function alongside `interactionUnderTransfer.ts` in `synthesize/`, not folded into the same table. Violated `sameHost` + enum relation kind -> insert `transferMembership(subject, from: currentHost(subject), to: currentHost(object))` before the relation step; `Custom` kind -> defer to the LLM validator (BD-10). **Depends on Grounding existing first** --- single-object Grounding shipped 2026-07-13 (`synthesize/groundReferent.ts`/`groundChange.ts`, see the Synthesize-stage compiler checklist bullet), so the `currentHost(subject)`/`currentHost(object)` lookups this table needs are now buildable; still blocked, though, on the still-undecided Grounding/Expansion interleaving question (`AGENT.concepts.md`) being resolved, since this table's own output is itself an Expansion result that would need further grounding.
    - [ ] **Slice 3:** generalize the *real* persistence pathway --- widen `HostRelationalPatch.hostId` from `EphemeraRoomId` to `EphemeraMembershipHostId`, relax `applyRelationalPatch`'s room-only guard, extend `buildHostRelationalPatchTransactItems` to dispatch Room/Character (mirroring the dispatch BD-13 slice 1 already built for the separate edge-**carry** primitive, `hostRelationalEdgeCarryTransactItems.ts`, which stays separate --- see BD-15 (4), resolved). Pre-rollout status (BD-15 (3)) means this can be an in-place widening, not a parallel path.
    - [ ] **Slice 4:** update the production compiler path to reach the held-item case end-to-end --- `compileRelational.ts`, `resolveRelationalGrounding.ts`, frame extraction's **`hostRoomId`** field, and the **`Object Establish Relation`**/**`Object Dissolve Relation`** bus payload, generalized from `roomId` to a membership-host id in place (BD-15 (3): no compatibility constraint, so prefer in-place widening over a narrower additive variant).
    - [ ] **Follow-on, not yet scoped as a slice:** `multiPresent`'s all-Error treatment (BD-16 (3)) --- only needed once/if the `Custom`-defer LLM validator is actually allowed to resolve into a multi-host co-location outcome; can wait until that validator exists.
  - [ ] **LLM joint `(identity, plan)` proposer** (deferred from FT-3.2): open-language fallback when closed-world fast-path fails; emits N ranked tuples for FT-5 selection; owns semantic grey-band Abstain/Consult beyond numeric floors. See **Phase C design debt** (**Legacy hop retirement**) and the sibling sandbox plan (**Instruction compiler + validator**, [`AGENT.planCompilerSandbox.planning.md`](AGENT.planCompilerSandbox.planning.md)).
  - [ ] **Dry-run sandbox** (blocking prerequisite, mostly satisfied): the sandbox itself shipped (S1--S6, [`AGENT.planCompilerSandbox.planning.md`](AGENT.planCompilerSandbox.planning.md)). What remains is wiring/consumption by the Synthesize-stage compiler above, not building the sandbox.

- [ ] **C2. Executor + compound kernel apply**
  - [ ] Compiler emits ordered plan; executor routes **length-1** plans to existing single-step streams (Phase B path).
  - [ ] **Length-2+** composed plans (BD-8): **one** positions ingress / compound coordinator --- kernel **`transactWrite`** bundles all **`HostEffect[]`** + **`HostRelationalPatch[]`** (BD-9); **must not** emit sequential **`Object Drop`** then **`Object Establish Relation`** with independent partial commit.
  - [ ] On compound apply failure: return **Error** to player; no membership-changed or relational fact streams.
  - [ ] Perception/transcript: single composed outcome when compound apply succeeds (not separate drop + relate lines unless product decides otherwise at C2).

- [ ] **C3. Classifier / MultipleCommands policy**
  - [ ] Document when composite single-line commands compile vs **`MultipleCommands`** Error.
  - [ ] Tests: "pick up broom and go north" (likely reject); "toss pouch on floor" (compose if in scope).

- [ ] **C4. Refactor enrich entry** (shape set by FT-7 --- two-level classify / reunified family type)
  - [ ] Generalize **`compileMembershipAtomic`** (Phase A) into full plan compiler; the **reunified manipulation-family intent** (FT-7; membership + relational under one `type` with a provisional sub-topology hint) routes through the **single shared entry** into the FT-6 orchestrator --- **no `enrichRoute` fork** (the split-type route retired with BD-11's routing role).
  - [ ] Deprecate ad-hoc stage graph where compiler subsumes it; **retire complexity LLM + frame extract as distinct hops** (deferred from FT-3.2); remove **`relationalRoute`** preposition gate when B2.5 intent routing is steady-state. Sub-topology + **`verbClass`** arrive as **provisional hints** (evidence for the joint hop), not committed routes; **`confidence === 1.0`** marks a closed-world (deterministic template) hint that drives the zero-Bedrock fast-path. See **Phase C design debt** (**Classify reunify / FT-7**, **Legacy hop retirement**).

- [ ] **C5. Re-examine FT-1.3.1 legacy admissibility harness (follow-on housekeeping)**
  - [ ] Decide whether [`testing/legacyLexicalChannelGate`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/embeddingMatch/testing/legacyLexicalChannelGate.ts), the **`resolveLexicalChannelActive`** override on [`buildSpanCandidatePool`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/embeddingMatch/buildSpanCandidatePool.ts) / calibration runners, and the **legacy vs gate-off** baseline arm in [`compareAdmissibilityArms`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/embeddingMatch/testing/compareAdmissibilityArms.ts) still earn their keep.
  - [ ] **Timing rationale:** gate-off production (FT-1.3.1) will have been live far longer than the legacy gate existed (~18h); by Phase C steady-state, FT-5 selector + compound compiler should have exercised short-span pool paths in a production-shaped context.
  - [ ] **Likely outcome if deleting:** drop frozen legacy gate module; simplify harness to gate-off-only regression fixtures (spurious diverse-catalog length-1 below `T_JOINT_ABS`, shorthand cases allowed high); remove production injection hook unless another A/B baseline still needs it. Update [`embeddingMatch/AGENT.md`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/embeddingMatch/AGENT.md) + [`calibration/AGENT.md`](../../../../../lambda/ephemera/calibration/AGENT.md).
  - [ ] **Keep if:** identity-corpus ranking A/B still catches unintended lexical/combine drift, or calibration snapshots depend on the dual-arm comparison.

## Phase C design debt (consolidated from fault-tolerant gateway, 2026-07-10)

Plan-only forward-looking decisions absorbed when the fault-tolerant gateway plan was retired. Shipped enrich behavior lives in durable docs ([`objectManipulation/AGENT.md`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/AGENT.md), [`embeddingMatch/AGENT.md`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/embeddingMatch/AGENT.md), [`actions/AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.implementation.md)). Graduate rows into those docs when C1/C4 ship.

### Instruction compiler + validator (C1/C2)

**Pipeline decomposition (2026-07-12):** C1 is really **two** independent jobs joined by a third. Vocabulary graduated to [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.concepts.md) (**Target** --- not yet the shipped module shape): **Identify** (span -> UUID, needs KR) and **Plan** (verb/frame -> ungrounded position planning primitive family, does **not** need KR) are independent and can run in parallel; only **Synthesize** (ungrounded primitive + KR -> complete grounded instruction, legality-checked) is a genuine join of both. The existing sandbox (S1--S6 below) is the **Synthesize** stage's validation half; its `interactionUnderTransfer.ts` / `computeCarryClosure` are Synthesize-stage *closure* logic that currently sits alongside the sandbox's pure-validation code as a module-boundary artifact, not a design requirement --- see the concepts doc's "Current conflation" section. **Still open here, not yet in concepts.md:** the ungrounded-primitive type family itself (referent language for span-relative locations, e.g. "wherever X currently is") --- this is an implementation fork, not settled vocabulary, until it ships.

**Split out (2026-07-10) into a sibling plan:** [`AGENT.planCompilerSandbox.planning.md`](AGENT.planCompilerSandbox.planning.md) --- design debt, open decisions (SB-1--SB-4), and a build checklist (S1--S6) for the in-memory dry-run sandbox this compiler/validator depends on. That plan owns the sandbox itself (state shape, compound simulation, interaction-under-transfer rule table, `resultPreview` shape).

**What stays here (compiler/selector shell, not the sandbox):**

- **Propose-N, then deterministic legality-gated selection (not iterative backtrack).** The joint hop emits **N ranked `(identity, plan)` candidates** in one generation; a deterministic **selector** evaluates all N (via the sandbox) and picks-or-Consults. Selection is a **testable pure function** of `(candidates, current-state)`.
- **Legality gates, confidence ranks --- lexicographic.** Partition by legality (`clean-legal` > `defer` > `illegal`); rank **within legal survivors** by absolute calibrated confidence. Confidence never buys back illegality.
- **Selection may decline.** Below FT-5 floor or thin margin over runner-up -> **Consult/Abstain**, not argmax. Runner-up **legal** candidates are the Consult menu.
- **Keep proposer/validator split** even when both are deterministic on the golden path --- the validator (sandbox) is the **single shared legality authority** the LLM proposer must also pass.

### Ungrounded position planning primitive shape (C1 head, BD-14)

**First draft, 2026-07-13.** Scope discipline: this is deliberately not an attempt at a general, exhaustive type language up front --- expand `Referent` kinds and `Assertion` predicates as concrete use cases demand them, the same way the sandbox's own vocabulary (`AGENT.concepts.md`) grew from worked examples rather than upfront generality.

**Change vs. Assertion (first-class step kinds, not nested).** Every ungrounded primitive step is either a **`Change`** (an effect --- e.g. `transferMembership`) or an **`Assertion`** (a precondition-shaped check that gates continuation but produces no state transition), mirroring the classical AI-planning precondition/effect split. They are coequal variants in the same step union, **not** assertions nested under a change --- an `Assertion` step can exist with no paired `Change` at all, reserved for future non-position parsing (e.g. "push the red button on the control panel" has no `positionGraph` change but plausibly needs an assertion the button is reachable/on the panel). A plan is a sequence of these steps; **BD-9's existing abort-on-any-non-legal-step semantics apply uniformly** to both kinds --- an `Assertion` step just never produces a state transition on success, where a `Change` step does.

**Assertion evaluation is Synthesize's job, not Plan's.** Plan emits only the assertion's *shape* (predicate + referents) with zero KR access, same as `Change` primitives. Synthesize is where the predicate is actually evaluated against live graph state --- consistent with Synthesize being the stage that needs both Identify's and Plan's output simultaneously (`AGENT.concepts.md`).

**`negate: boolean`, not paired predicate names.** Rejected `notContainedBy` (and any other negated-predicate-name pattern) in favor of a `negate` flag on the assertion step, because the underlying predicate evaluation is often itself disjunctive (see `containedBy` below) --- a paired name would fork that disjunctive logic into two copies that must be kept in sync as it grows. A flag reuses one evaluation path. **Ternary-safe:** predicate evaluation can resolve `satisfied` / `violated` / **`defer`** (semantic residue needing an LLM judgment, e.g. `embedded into`); `negate` flips `satisfied` <-> `violated` but leaves `defer` as `defer` --- you cannot confidently assert the *negation* of a fact you can't yet confidently resolve. No general boolean-combinator tree (`and`/`or`) is needed at the assertion-step level: step **sequencing** already gives AND (per BD-9's abort-on-any-failure fold), and OR-shaped disjunction lives inside each predicate's own definition.

**First predicate: `containedBy(subject, object)`.** Disjunctive over membership, `On`, `Under` (deterministic, closed-world) and `embedded into` (semantic residue, LLM-only) --- structurally the same three-way shape (clean-satisfied / clean-violated / defer-to-LLM) the sandbox's `interactionUnderTransfer.ts` already uses for `Custom` edges (BD-10), so evaluation can plausibly reuse that pattern rather than invent a new one. Anticipated use: "get bag from table" -> `[Assertion(containedBy, bag, table), Change(transferMembership, bag, actor)]`; "get bag" alone (no "already carrying" check) -> `[Assertion(containedBy, bag, actor, negate: true), Change(...)]` to reject picking up something already held.

**Assertion failure modes --- shipped vs. deferred.** When an assertion fails against Identify's already-resolved candidate: **shipped-pattern reuse (v1, no new machinery)** is to route to the existing **Consult** outcome (e.g. "the bag isn't *on* the table, it's on the floor --- pick it up anyway?"). A richer, **explicitly deferred** enhancement is to **Backtrack** the assertion into Identify as a correction/ranking signal (per the Backtrack fault-recovery pattern in [`llm/AGENT.concepts.md`](../../../../../lambda/ephemera/llm/AGENT.concepts.md)) --- since Identify today returns its top-N candidates independent of any assertion, a candidate that loses on pure lexical/semantic match could win a re-ranked retry that weights the assertion. This does **not** violate Identify/Plan independence: Plan never talks to Identify directly, only Synthesize (which already holds both stages' output) is allowed to mediate a backtrack. **Promoted to its own row, BD-18 (2026-07-14),** once Grounding/Expansion actually shipped with hard-terminal result types and it became clear this paragraph --- written before that vocabulary existed --- needed to be discoverable from the Synthesize-stage compiler and Pipeline A -> B migration checklist items, not just this design-debt section.

**Still open, not yet decided:** whether assertion-step **ordering** within a plan carries meaning (precondition-only, evaluated before any `Change`, vs. a future post-condition check evaluated after) --- today's only worked example puts assertions first; no plan yet needs a post-condition check, so this is deferred until a concrete case forces the question, per this section's own scope discipline.

**Types shipped, `Assertion` emission still blocked (2026-07-13):** the Plan-stage compiler (`enrich/objectManipulation/plan/compileUngroundedPlan.ts`) is built and tested, but only emits `Change` steps --- confirmed during implementation that `MembershipManipulationFrame` (`membershipFrame.ts`) carries no field a `containedBy` assertion could be built from (only `rawObjectSpans` + `verbClass`, no location-disambiguating span like "table" in "get bag from table"). **Net-new forward-looking item:** extending classify + frame extraction to capture such a span (bigger, touches shipped production prompts/schemas, not just new isolated modules) is the actual prerequisite for real `Assertion` emission --- not yet scoped as its own C1 sub-bullet; add one when this is picked up.

**`Referent` --- three base kinds, first draft:**

| Kind | Shape | Meaning |
| --- | --- | --- |
| `objectSpan` | `{ referentType: 'objectSpan', span: string }` | The object referred to by this parsed **`objectSpan`** --- KR-ungrounded (not yet a `ComponentUUID`); this is exactly what distinguishes a `Referent` from an already-resolved id. |
| `actingCharacter` | `{ referentType: 'actingCharacter' }` | The character issuing the command --- contextual, not span-derived (most commands have no span naming the actor). |
| `currentHost` | `{ referentType: 'currentHost', referentTarget: Referent }` | The membership host of whatever the nested `Referent` resolves to. **Recursively typed over `Referent`** (not restricted to `objectSpan`) so it composes: `currentHost(actingCharacter)` expresses "the room the actor is currently in" --- exactly what `establishRelation`/`dissolveRelation`'s `hostRoomId` needs --- without a fourth dedicated kind. |

**Explicitly out of scope for `Referent` itself:** plurality (BD-13's carry-expanded object sets) and multi-host co-location. A `Change` primitive's object field stays a single `Referent`; growing it into a concrete set is Synthesize's job (carry closure), and multiple simultaneously-valid hosts for one object is the same territory as the existing **`multiPresent`** complexity class ([`actions/AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.implementation.md)) --- both route to Synthesize-stage defer-to-LLM, not new `Referent` shape.

### Span resolution I/O + selector (C1 tail)

**Two types (locked):** [`SpanCandidatePool`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/spanResolution.ts) (input evidence: `candidates[]` + per-candidate `locus`) and `SpanResolutionOutcome` (selector verdict: `resolved` | `consult` | `error`). No `status` on the input; emptiness is `candidates.length === 0`.

**`locus`:** discriminated union `room` | `heldByActor` | `heldByOtherCharacter` | `withinObject` --- deterministic graph/catalog evidence only; never licenses `operationKind` invention (BD-12). v1 production = room/held; `withinObject` is a future re-entrant supplement (not C1-blocking).

**Selector is two phases (one selection point):**

1. **Selection (cross-tuple):** legality partition + `T_JOINT_*` floor + margin over runner-up -> auto-resolve one tuple or Consult/Abstain.
2. **Existence/presence guard (per span on chosen tuple):** referential integrity only --- not a re-litigation of semantics.

**`resolveComponent` is not a Plan IR runtime primitive** --- grounding is this selector tail. Floor magnitude follows recoverability: illegal-if-wrong -> large discount (auto-resolve on modest confidence); legal-but-wrong -> hold floor + Consult.

**"Commit"** is reserved for persist + publish; the FT-5 act is **auto-resolve / select**.

### Legacy hop retirement (C1 proposer + C4)

**Complexity LLM --- retire as a distinct hop (C4).** Deterministic half -> dry-run sandbox legality; semantic residue (`Custom` / unmodeled) -> shared LLM validator (BD-10 `defer`). Membership `operationKind` becomes deterministic fallout once identity + host are pinned (`verbClass` is evidence only).

**Frame extract --- retire as always-on LLM hop (C4); work splits:**

- **Down (net-new deterministic fast-path):** closed-grammar relational frames (`put` / `place` / `lean` / `take off` + known prepositions) --- relation phrase is enum lookup, `operationKind` from verb template, roles from `V NP prep NP`. Does not exist today; build as relational analog of membership fast path.
- **Up (general joint hop, C1):** open relational language -> joint `(identity, plan)` proposer owns role decomposition + operator selection + identity jointly.
- Downstream **normalizer** + **legality** unchanged.

**No narrow-scope "tier-2" LLM** between template and general proposer. Untemplated-simple-language is a **classify** concern.

**Owner map (Abstain / Consult / defer):**

| Concern | Owner |
| --- | --- |
| Abstain / Consult | Proposer / joint hop (FT-5 gate for commit-vs-Consult) |
| Defer -> Error | Shared dry-run validator (BD-10) |
| Relational `operationKind` (BD-12) | Proposer (template / joint hop) --- **never** the deterministic compiler |

**FT-3.2 shipped on deterministic path only** (Consult/Abstain wire). Accepted interim until C1/C4: numeric-only grey-band; complexity LLM + frame extract still live production hops.

### Classify reunify / FT-7 (C4)

**Two-level classify:** trusted-output at the **family** level only (manipulation vs navigation vs speech vs Acme). Intra-manipulation sub-fields (`subTopology`, `verbClass`) become **provisional hints** consumed as evidence by the joint hop --- not committed routing.

**Reunified manipulation-family intent type** with optional `{ subTopology?, verbClass?, confidence }` --- supersedes BD-11's top-level `ObjectMembershipIntent` / `ObjectRelateIntent` **routing** role (family + `verbClass`-as-membership-direction semantics survive). **No `enrichRoute` fork** --- one shared entry into the FT-6 orchestrator (= concrete content of C4).

**Hint trust:** `confidence === 1.0` is a reserved closed-world sentinel (deterministic producers only; LLM clamped `< 1.0`); seam fast-path fires on strict equality, never a threshold. Hint confidence may weight joint-hop evidence; must **not** blend as a naive addend into the FT-5 absolute floor.

**Family-level wrong commit** stays trusted-output terminal (Abstain; no cross-family re-route this iteration).

### Phase D --- LLM plan generation (optional)

- [X] **D1. Defer criteria**
  - [X] Lock BD-10: **`defer`** = non-trivial existing in-host relational edges on subject/target; B--C -> Error stub.

- [ ] **D2. Plan LLM hop**
  - [ ] Constrained JSON: steps must be valid primitives from registry only.
  - [ ] Validate + execute or Error; no silent fallback to wrong operation.

- [ ] **D3. Pipeline integration**
  - [ ] Evaluate [`llm/pipeline/`](../../../../../lambda/ephemera/llm/pipeline/AGENT.md) vs inline enrich orchestration.

## Verification

**Phase B** (from **`lambda/ephemera/`**):

```bash
npm run test -- --watchAll=false \
  dataSource/actions/enrich/objectManipulation/ \
  dataSource/actions/parseCommand.test.ts \
  dataSource/actions/index.test.ts \
  dataSource/positions/manipulation/ \
  dataSource/perception/objectManipulationPresentationFanIn.test.ts

npm run build
```

**Phase C** adds compiler/executor unit tests under `dataSource/actions/enrich/` (exact path TBD at C1).

**Manual scenarios (post-B):**

- "put the broom on the table" -> relation edge on room host; transcript asserts relation, not exact placement geometry.
- "put the broom on the table" (already on table, same edge) -> idempotent allow/no-op (BD-10).
- "put the broom under the table" (already on table) -> **Error** stub in B--C; plan LLM candidate in Phase D (BD-10).
- "lean the ladder against the wall" -> **`Custom`** or enum **`Against`** (BD-2).
- "put the coin in the jar" -> nesting defer message, not **`establishRelation`** (**`In`** excluded per BD-2).
- Held object + surface relation -> composed plan: auto **`drop`** then **`establishRelation`** in **one atomic apply** (BD-8 + BD-9, Phase C); failure leaves object held.
- "take the rope off the crate" -> frame extract **`operationKind: dissolveRelation`** (BD-12); legality requires matching edge on room host (BD-7, BD-10).
- "get the tray" (glass **`On`** tray, tray **`On`** table) -> **`dissolveRelation`** (tray-table, clean) + multi-member **`transferMembership([tray, glass])`** Room -> Character, preserving `glass On tray` on the destination graph (BD-13); **not** a defer, and not a spilled glass.

## Progress

| Milestone | Status |
| --- | --- |
| Phases B--D task plan | Done |
| Phase A prerequisite | Not started (see companion plan) |
| BD-1--BD-11 open decisions locked | Done (2026-07-04) |
| BD-12 relational operationKind via frame extract | Done (2026-07-05 decision; 2026-07-05 implementation) |
| B0 decision lock + positions contract draft (BD-2, BD-3) | Done (2026-07-04) |
| B1 frame schema + extraction | Done (2026-07-04) |
| B2 relation normalizer (deterministic map; nesting defer; no LLM hop) | Done (2026-07-04) |
| B2.5 split classify intents (BD-11; ObjectMembershipIntent + ObjectRelateIntent) | Done (2026-07-04) |
| B3 grounding + legality | Done (2026-07-05) |
| B4 positions persist + ingress | Done (2026-07-05) |
| B5 actions egress + perception | Done (2026-07-05) |
| B6 durable docs (four-lane steady-state) | Done (2026-07-05) |
| Phase B establishRelation vertical | Done |
| Fault-tolerant gateway | **Done (FT-4.3, 2026-07-10)** --- Gateway exit complete; planning file retired; design debt consolidated in **Phase C design debt** |
| Phase C Plan IR | **In progress** --- C1 underway (see next two rows) |
| Phase C plan-compiler sandbox (sibling plan) | **Done (S1--S6, 2026-07-11)** --- see [`AGENT.planCompilerSandbox.planning.md`](AGENT.planCompilerSandbox.planning.md) |
| Phase C C1 sub-task: Extract Synthesize module | Done (2026-07-12) |
| Phase C C1 sub-task: ungrounded primitive types + Plan-stage compiler (BD-14) | Done (2026-07-13) --- Change-only; Assertion emission blocked on frame-extraction extension (see Phase C design debt) |
| Phase C C1 sub-task: `ParsePlanStep` + runtime primitive registry | Done (2026-07-13) --- Change-only; grounded Assertion + stub reconciliation (`identityPlanCandidate.ts`, `sandboxPlan.ts`) deferred to Synthesize-stage compiler bullet |
| Synthesize-stage compiler, slice 1 of 6: Grounding | Done (2026-07-13) --- Expansion invocation, `IdentityPlanCandidate`/`SandboxPlanStep` reconciliation, sandbox/selector wiring, `compileMembershipAtomic.ts` fallback retirement remain |
| Synthesize-stage compiler, slice 2 of 6: invoke Expansion | Done (2026-07-13) --- Grounding/Expansion interleaving still undecided; type reconciliation (slice 3), sandbox/selector wiring (slice 4) remain |
| Synthesize-stage compiler, slice 3 of 6: reconcile grounded-candidate types | Done (2026-07-13) --- zero live files touched; sandbox/selector wiring (slice 4) remains |
| Synthesize-stage compiler, slice 4a of 6: sandbox wiring, relational selector | Done (2026-07-14) --- pure refactor, zero live behavior change confirmed; membership selector wiring (slice 4b) remains, to be designed alongside slice 5 |
| Synthesize-stage compiler, slices 4b + 5 of 6 (together): sandbox wiring + fallback retirement, membership selector | Done (2026-07-14) --- accepted behavior change (BD-17): carry-related objects now error instead of silently resolving, until the Pipeline A -> B migration lands real carry-closure. Both Validation legs (relational + membership) now fully converged on `evaluateSandboxPlan`. The Pipeline A -> B migration (cutover, including BD-13 slice 3) remains, promoted to its own top-level checklist item (2026-07-14) rather than nested under Synthesize-stage compiler |
| BD-13 kernel work, slice 1 of 3: host-agnostic edge-carry persistence primitive | Done (2026-07-13) |
| BD-13 kernel work, slice 2 of 3: atomic edge-carry bundling in `applyHostEffects` | Done (2026-07-13) |
| BD-13 kernel work, slice 3 of 3: bus payload widening + real producer wiring | **Superseded (2026-07-14)** by the Pipeline A -> B migration's own nested slice breakdown --- see that checklist item and the two rows below |
| Pipeline A -> B migration, slice 1 of 4: compound atomic kernel-apply primitive | **Done (2026-07-14)** --- research initially assumed this needed new cross-primitive compound-apply plumbing (bundling a relational-patch dissolve into the same atomic write as a membership move); building and testing it surfaced a real bug that traced back to a wrong premise: `EphemeraPositionGraph.removeObject`'s existing edge-stripping already dissolves a boundary edge atomically as a side effect of the moving object's own hostEffect, so no new plumbing was needed there. Real scope narrowed to an object-*set*-aware apply primitive (`applyObjectSetTakeHold.ts`/`applyObjectSetDrop.ts`), verified end-to-end against BD-13's own "get tray" worked example. Zero production callers yet (by design) |
| Pipeline A -> B migration, slice 2 of 4: wire Expansion into the membership compiler path | **Done (2026-07-14)** --- `expandTransferMembership.ts` now runs per-candidate in `sandboxMembershipDryRun` (`selectIdentityPlanTuple.ts`), computing the real carry-closed transfer set + dissolve steps instead of a naive singleton, validated via `evaluateSandboxPlan`. Grounding confirmed unnecessary (Identify already resolves concrete ids); sync/async mismatch resolved for free (graphs already pre-fetched, `SandboxState.get` already synchronous). Terminal behavior for a genuine multi-object case is still an Error (`multiObjectTransferNotYetSupported`, replacing BD-17's `incompleteTransferSet`) since the compiler still can't apply a multi-object move --- that's Slice 3. A dissolve-only closure (no carry) needs no apply-layer changes at all, caught by testing (see Slice 1's `removeObject` finding) |
| Pipeline A -> B migration, slice 3 of 4: bus payload + result-type + execute* widening | **Done (2026-07-15)** --- shipped as the `MultiKeyUpdate`-reducer redesign (`applyObjectSetTransfer.ts`), not the originally-sketched call-Slice-1's-functions path; supersedes both Slice 1's `applyObjectSetTakeHold`/`applyObjectSetDrop` internals and the legacy singular `applyObjectTakeHold`/`applyObjectDrop` (deleted). "get tray" now really carries the glass in production |
| Pipeline A -> B migration, slice 4 of 4: perception narration for a carried-set take-hold/drop | Not started --- still blocked on copy/UX design, not plumbing |
| Phase C5 legacy admissibility harness review | Not started (follow-on after C4) |
| Phase D LLM plans | Not started |

## Coordination notes

- **Phase A** landed **`verbClass`** (`acquire` | `release`), merged-catalog identity, and the relational preposition guard before relational frame work. Classify emits **`ObjectMembershipIntent`** or **`ObjectRelateIntent`** (B2.5 shipped).
- **B2.5 (BD-11):** split **`ObjectMembershipIntent`** / **`ObjectRelateIntent`** replaces **`ObjectManipulationIntent`** at classify; **`parseCommand`** routes by intent **`type`** via **`enrichRoute`**. **`relationalRoute`** preposition regex removed as primary enrich gate (module retained for unit tests). **`verbClass`** applies only to **`ObjectMembershipIntent`** --- not a third value on a shared type. **FT-7 / C4** walks back the committed-routing role of this split (see **Phase C design debt**).
- Phase B shipped frame extract + **`establishRelation`** / **`dissolveRelation`** end-to-end; durable docs in B6 ([`diegeticLogic/AGENT.operators.concepts.md`](../../../../../lambda/ephemera/diegeticLogic/AGENT.operators.concepts.md), [`positions/AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md#host-local-relational-patch-phase-b-shipped-b4), [`actions/AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.implementation.md)).
- **`in`** / nesting is an explicit **deferral** --- player-facing copy when frame extract or relation normalizer detects containment language ([`AGENT.operators.concepts.md`](../../../../../lambda/ephemera/diegeticLogic/AGENT.operators.concepts.md) **Still out of scope**).
- **BD-12 (2026-07-05):** B1 frame extract shipped span-only; forbidding **`operationKind`** at classify *and* frame extract left no owner and invited phrase-bucket compiler hacks. **`operationKind`** (`establishRelation` \| `dissolveRelation`) is now owned by the frame extract LLM (no extra Bedrock hop). Compiler stays deterministic for grounding + legality only. Under C4, BD-12 ownership moves to the **proposer / joint hop** (see **Phase C design debt**). General seam rules graduated to [`llm/AGENT.contract.md`](../../../../../lambda/ephemera/llm/AGENT.contract.md) and [`llm/AGENT.concepts.md`](../../../../../lambda/ephemera/llm/AGENT.concepts.md).
- **Fault-tolerant gateway (retired 2026-07-10):** Gateway exit complete (FT-4.3). Production path emits `SpanCandidatePool` + FT-5 selector on membership and relational; Consult/Abstain first-class. Planning file deleted; Phase C design debt consolidated in this document. Durable trust posture: [`objectManipulation/AGENT.md`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/AGENT.md).
- **FT-1.3.1 admissibility gate retired (2026-07-09):** short-span admissibility gate retired; lexical always on for non-empty spans. FT-5 auto-resolve consumes `T_JOINT_*` on normalized joint relevance. Constants: [`thresholds.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/embeddingMatch/thresholds.ts). Legacy harness baseline retained; **C5** re-examines deletion after Phase C steady-state.
- **FT-1.4 leading-article normalization (2026-07-09):** classify-only QoL --- [`peelLeadingArticleWhenTail`](../../../../../lambda/ephemera/dataSource/actions/discriminateIntent/peelLeadingArticleWhenTail.ts).
