# Classify narrows, Plan generalizes: deterministic-first command/question routing (iteration 7)

**Status:** **Sub-iterations 1 and 2 shipped 2026-07-20.** Sub-iteration 1: routing skeleton (narrowed classify, `Command`/`WorldQuestion` dispatch, CPG-2 Command-branch/CPG-3 resolved; `DeterministicTemplate` wiring (CPG-1) deliberately deferred past that slice). Sub-iteration 2: real command-plan dispatch for five of the six non-object-manipulation families (`Help`/`Home`/`AwaitRoadRunner`/`Navigation`/`AcmeOrder`), closing their Sub-iteration 1 regression --- `DeterministicTemplate`'s `help`/`home`/`awaitRoadRunner` subset wired live, CPG-4 resolved, CPG-5 deliberately deferred. **`LookRoom` paraphrases deliberately excluded, by user direction on review:** open-ended forms are LLM-fallback territory, not a closed lexicon; bare `look`/`l` were never regressed. Sub-iteration 3 not started. Split out from the now-retired iteration-3 plan (`AGENT.parseTokenization.planning.md`, deleted 2026-07-20 after graduating to durable docs; git history has it), where this started as BD-26 ("merge classify and Parse into a single LLM hop," driven by observed production latency) --- that framing was proposed by Claude, rejected by the user as premature improvisation, and replaced through direct conversation with the design below, which is substantially larger in scope: it resolves BD-22 (Parse/Plan/Identify generalizing beyond object manipulation) rather than staying object-manipulation-scoped. The rejected-proposal record is condensed in this doc's own History below.

Task-planning conventions: [`taskPlanning/AGENT.md`](../../../../AGENT.md). Ladder position: [`AGENT.objectManipulationIterations.planning.md`](AGENT.objectManipulationIterations.planning.md), iteration 7 (added alongside this doc).

## Purpose

Today, `classify` (`discriminateIntent`) conflates two genuinely different jobs in one 13-section LLM decision tree: (1) is this line even a legitimate, single, in-world engagement --- not noise, not a prompt-injection attempt, not multiple bundled commands --- and if so, is it a **command** (an action) or a **world question** (a question about world state); and (2), given that, *which specific* command family or question type is it. Job (2) is redundant with what a properly generalized `Plan` stage should own (BD-22's still-open question, named 2026-07-18, never resolved) --- `matchRelationalTemplate.ts` already proves this pattern works for the relational family specifically. This initiative narrows `classify` to job (1) only, and generalizes `Plan` (and, following it, `Identify`) to own job (2) across every family, not just object manipulation.

**Originally motivated by observed production latency** on object-manipulation commands (two sequential Bedrock round-trips: `classify` then `Parse`, today). **Important correction, reached through conversation:** this design does **not** cut that round-trip count --- when the deterministic layer misses, `classify` and `parse` remain two separate calls, deliberately (see Design decisions). The latency win instead comes from (a) a generalized deterministic pre-check catching more traffic before any LLM runs at all, and (b) `classify`'s own prompt shrinking from 13 sections to 5 possible outcomes, reducing per-call generation cost even on the same model tier. Both `classify`'s and `Parse`'s Bedrock invoke wrappers are already on `NovaMicro` (confirmed by reading `invokeBedrockParseCommand.ts`/`invokeBedrockObjectManipulationParse.ts` --- `novaModel.ts` only defines `NovaMicro`/`Nova2Lite`, and both hops already default to the former), so there is no model-tier downgrade left as an additional lever.

## Getting started

1. Skim [`taskPlanning/AGENT.md`](../../../../AGENT.md) and [`AGENT.objectManipulationIterations.planning.md`](AGENT.objectManipulationIterations.planning.md) (this iteration's place in the ladder).
2. Read this doc's **Carried-in findings from BD-22** section (the cross-family generalization findings migrated from the retired iteration-3 plan) and its History entry on BD-26's rejected single-hop-merge proposal --- this doc is the resolution of both.
3. Read today's classify (`discriminateIntent/buildIntentClassificationPrompt.ts`, `discriminateIntent/intentClassification.ts`, `discriminateIntent/deterministicChecks.ts`) and the existing relational-family Plan matcher (`enrich/objectManipulation/plan/matchRelationalTemplate.ts`) --- the latter is the concrete precedent for what a generalized Plan dispatcher should look like for every other family.
4. Testing authority: [`lambda/ephemera/AGENT.testing.md`](../../../../../lambda/ephemera/AGENT.testing.md).
5. Baseline:

```bash
cd lambda/ephemera && npm run test -- --watchAll=false \
  dataSource/actions/discriminateIntent/ \
  dataSource/actions/parseCommand.test.ts \
  dataSource/actions/enrich/objectManipulation/
```

## Design decisions (confirmed through conversation, 2026-07-20)

### Architecture: five layers, replacing today's two-stage classify-then-Parse

1. **`DeterministicTemplate` matching layer (new).** Generalizes today's flat `deterministicChecks.ts`. Matches either a **raw command string** or a **token sequence** against known templates; on a hit, returns **both** an intent classification **and** a synthesized `ParseSkeleton` (even trivial --- e.g. a single `text` token for `"look"`), at zero Bedrock cost. Must carry the same context `ParseCommandInput` already does, not just bare command text --- some matches are context-dependent (Navigation's exit matching needs live `roomExits`; membership's `get <noun>` branch needs live `roomObjectLabels` to gate acquire vs. `AcmeOrder`, exactly as `deterministicChecks.ts` does today). **The pattern-driven half of this layer is built and graduated to durable docs** ([`actions/AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.concepts.md#deterministictemplate-shipped-2026-07-20) / [`actions/AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.implementation.md#deterministictemplate-module-built-not-yet-wired), 2026-07-20) --- bare-word and relational templates, `matchString`/`matchTokens` engines, the three-arm `DeterministicTemplateMatch` result. The **context-dependent** half named in this bullet (Navigation's `roomExits`, `get`'s `roomObjectLabels` gate) is **not built** --- that, plus wiring the built pattern-driven templates into the live path, remains this doc's plumbing work (CPG-1/CPG-3).
2. **`classify`, narrowed remit.** Runs only when layer 1 misses. Decides exactly **five** outcomes: `MultipleCommands`, `PromptInjectionAttempt`, `Unknown`, `Command`, `WorldQuestion`. Does **not** decide which specific command family or question type --- that moved to Plan (layer 4).
3. **`parse`, per-branch, not assumed shared.** Command and WorldQuestion each get their own segmentation treatment. **Explicitly not assumed to be the same job** --- commands are imperative/verb-noun-phrase shaped; world questions are interrogative and may need different span/text handling (rejected an earlier draft of this design that assumed one shared `parse` step for both, on direct user correction).
4. **`plan`, generalized and genuinely split by branch --- this is BD-22's resolution:**
   - **Command-plan:** deterministic dispatch off the skeleton across *every* command family (Navigation, Home, AwaitRoadRunner, LookRoom, Help, AcmeOrder, `ObjectMembershipIntent`, `ObjectRelateIntent`), not just object manipulation. `matchRelationalTemplate.ts` is the existing proof of pattern. Command-shaped input that matches no known template becomes `Unimplemented`.
   - **Question-plan:** deterministic (and eventually LLM-fallback --- sub-iteration 3) dispatch among known question types. Only `PredictHypothesis` is real today; everything else is `Unimplemented`.
   - **Structurally separate, not two registries under one dispatcher --- this was a real correction, not a stylistic choice.** The downstream consumer differs entirely: a matched command needs Synthesize's Grounding/Expansion/Validation to produce a world-mutating instruction; a matched question needs to produce an *answer*, which isn't the same kind of terminal action and has no existing machinery at all. An earlier draft of this design proposed trying both registries against one skeleton and letting whichever matched win (cheap, since deterministic matching has no LLM cost) --- rejected specifically for the case where *neither* matches and an LLM fallback is needed: **a single fallback prompt asking to resolve "which command primitive, or failing that, which question type, and if a question, how do I even answer it" would degrade both jobs**, per direct user correction. Deterministic-layer sharing is fine; LLM-fallback sharing is not.
5. **`Identify`, generalized (real new-build work, not refactoring).** Span/id resolution for objects/characters is already domain-general. Navigation's exit-label resolution (`exitResolution.ts`'s `resolveExitLabelToTargetId`) and Acme's product-catalog resolution are today separate, bespoke mechanisms living outside `Identify` entirely. Folding them into one `Identify` concept is part of what "generalizing the Identify/Plan/Synthesis cluster" (sub-iteration 2) means --- these don't already share an abstraction the way BD-22 initially hoped; building it is real work.

### Confirmed scope boundaries

- **The single-hop merge (classify + Parse combined into one LLM call) is explicitly rejected**, not deferred. Claude's original BD-26 proposal assumed it would be faster; the user was not convinced it actually would be, given output-token cost likely offsets any round-trip savings, and preferred not to build complexity on an untested assumption. Two separate, narrower calls it is.
- **Membership does not get a full native Plan matcher** (mirroring `matchRelationalTemplate.ts`) as part of this initiative --- stays on Step 2a's flat `rawObjectSpans` adapter (`enrich/objectManipulation/parse/objectSpansFromSkeleton.ts` -> `compileMembershipAtomic.ts`), per explicit scope confirmation.
- **But a smaller gap this creates must still be closed in Sub-iteration 1:** today, `classify` itself decides `ObjectMembershipIntent` vs. `ObjectRelateIntent` (the membership/relational split). Once `classify`'s remit shrinks to generic `Command`, nothing decides that split before dispatch --- relational already has `matchRelationalTemplate.ts` to derive it from the skeleton; membership does not. This needs a small, real piece of logic (not a full native matcher) --- most likely adapting `deterministicChecks.ts`'s existing acquire/release verb-classification regex to run over skeleton `text` runs instead of raw command text, mirroring how `matchRelationalTemplate.ts` already derives `operationKind` from verb text. Named explicitly here so it isn't discovered mid-build and mistaken for the native-matcher work that was deliberately scoped out.

## Recommended order

Use `[ ]` for pending and `[X]` for complete. Three sub-iterations, confirmed with the user 2026-07-20 as a deliberate staged rollout accepting temporary regressions --- appropriate here specifically because this codebase is single-dev-instance with no external consumers (see the `Pre-rollout status` note in durable memory), so a known regression window is cheap to accept and fix forward, rather than something to avoid via a slower, fully-atomic rollout.

- [X] **Sub-iteration 1: land the routing skeleton, accepting known regressions.**
  - [X] Refactor `classify` to the five-outcome remit (`MultipleCommands`/`PromptInjectionAttempt`/`Unknown`/`Command`/`WorldQuestion`) described above. Shipped 2026-07-20: `baseClasses.ts` (`ParseCommandCommandIntentResult`/`ParseCommandWorldQuestionIntentResult` + guards, narrowed `IntentClassificationResult`), `discriminateIntent/buildIntentClassificationPrompt.ts` (full 5-outcome rewrite, single-arg signature, no movement/object-label context), `discriminateIntent/intentClassification.ts` (interpreter narrowed to the 5 outcomes), `discriminateIntent/index.ts` (dead `NavigationIntent`/`HomeIntent` post-classify resolution block deleted), `discriminateIntent/baseClasses.ts` (retired family-specific guards deleted).
  - [X] Route `WorldQuestion` directly to today's `PredictHypothesis` handling (`parseCommand.ts`'s new `WorldQuestion` branch). **Accepted regression, live:** a genuinely different world question (e.g. "are the king's guards close enough to stop me from slapping him?") gets answered as if it were a Coyote-plan-prediction request, not flagged `Unimplemented`, until sub-iteration 3 lands.
  - [X] Route `Command` to object manipulation's existing dispatch (`parseCommand.ts`'s new `Command` branch: `runParseStage` unconditionally -> `classifySkeletonFamily` -> membership/relational enrich, or `Unimplemented` on no family match). **Accepted regression, live:** non-object-manipulation commands that classify used to handle directly via LLM paraphrase (e.g. "peruse the room," a `LookRoom`-shaped paraphrase; **also `AcmeOrder` paraphrases**, not just the doc's original `LookRoom` example --- Acme ordering via natural language is now unreachable through the classify path, though `enrichAcmeOrder` itself is untouched and still reachable via the affinities harness) start failing where they previously succeeded, until sub-iteration 2 lands. Exact/deterministic forms (bare `look`/`help`/`home`/`predict`, `take`/`get`/`drop <object>`, `go <exit>`) are unaffected --- they bypass classify entirely via `deterministicIntentChecks` and never regressed.
  - [X] Close the membership-vs-relational dispatch gap (see Confirmed scope boundaries above): shipped as `enrich/objectManipulation/plan/classifySkeletonFamily.ts` (CPG-3) --- tries `matchRelationalTemplate` first (reproducing classify's old relational-beats-membership tie-break and resolving the `take`/`remove` ambiguity structurally), then checks the skeleton's leading text token against a `take`/`get`->acquire, `drop`->release verb check mirroring `deterministicChecks.ts`'s regex.
  - [X] Decide and build (or confirm shared) `parse`'s segmentation approach for the `Command` branch specifically: resolved by reusing the existing object-manipulation `runParseStage` hop unconditionally (CPG-2, Command branch only --- object manipulation is the only real dispatch target this slice). `WorldQuestion`'s segmentation stays open, deferred to Sub-iteration 3.
  - [X] Scope decision (confirmed with user, not in the original checklist): wiring the already-built `DeterministicTemplate` layer (CPG-1) into the live path was deliberately deferred out of this slice --- its bare-word templates are fully redundant with `deterministicChecks.ts` (no behavior change from wiring), and its relational templates are a pure Bedrock-cost optimization with no regression riding on it (CPG-6). `deterministicTemplate/` was not touched.
- [X] **Sub-iteration 2: generalize the Identify/Plan/Synthesis command-handling cluster, close the Command regression.** Shipped 2026-07-20.
  - [X] Move the Identify/Plan/Synthesis cluster to a family-agnostic level, keeping object manipulation's current implementation as one concrete instance of the generalized shape (not rewritten). Realized as: Plan-stage dispatch now genuinely covers every family (`parseCommand.ts`'s `Command` branch tries object manipulation's `classifySkeletonFamily` alongside four new family matchers); Identify/Synthesize stayed object-manipulation-scoped, a deliberate scope call (see CPG-5 below and `actions/AGENT.concepts.md`'s "Three conceptual jobs" section) rather than forcing a premature cross-family `Identify` abstraction.
  - [X] Add real command-plan dispatch entries for the other command families (`Help`, `Home`, `AwaitRoadRunner`, `Navigation`, `AcmeOrder`) so `Command`-routed input for these regains the coverage lost in Sub-iteration 1. Shipped as: `deterministicTemplate/`'s `help`/`home` templates extended with closed paraphrase lexicons + a new `awaitRoadRunnerTemplate`, wired live via a new `matchNonObjectManipulationTemplate` (`Help`/`Home`/`AwaitRoadRunner`, no Bedrock); `plan/matchNavigationParaphrase.ts` (movement-verb paraphrases beyond bare `go`, no Bedrock); `plan/matchAcmeOrderFamily.ts` (post-Parse, calls `enrichAcmeOrder`). All wired into `parseCommand.ts`'s `Command` branch. **`LookRoom` paraphrases deliberately excluded (review correction, not an oversight):** the first draft hard-coded a `look` paraphrase lexicon (`peruse the room`/`survey the room`/`examine the room`); the user asked for it to be removed, since open-ended `look` paraphrases are free-form enough that a closed list isn't the right tool --- that's LLM-fallback territory (not yet built), not a deterministic template. Bare `look`/`l` were never regressed (always intercepted upstream by `deterministicChecks.ts`, before classify runs); `lookTemplate` stays bare-word-only, present in the full `deterministicTemplateRegistry` but excluded from `nonObjectManipulationTemplateRegistry` (it can never fire there).
  - [X] **Open design question, resolved:** CPG-4 --- `enrichAcmeOrder`'s existing LLM call fits as-is as this family's Synthesize step, invoked directly once `matchAcmeOrderFamily` extracts `rawOrders` from the skeleton. No new hook needed.
  - [X] **Scope decisions made during this slice, not anticipated by the original checklist** (mirrors Sub-iteration 1's own DeterministicTemplate-deferral pattern): (a) CPG-5 (unifying Navigation's/Acme's resolution into one cross-family `Identify` concept) stays deliberately deferred --- none of the six families need object manipulation's `EphemeraObjectId`-scoped catalog machinery, so building a shared abstraction now would be premature; (b) CPG-6 (relational `DeterministicTemplate` entries wired into the live path) stays deliberately deferred --- only the bare-word subset was wired, since consuming a relational match would require threading its synthesized skeleton into the same downstream enrich pipeline Parse's output goes into, a distinct change with its own test surface, not required to close this regression; (c) Navigation's paraphrase matcher was built as its own typed function rather than widening the shared `DeterministicTemplate.matchString(command: string)` interface to carry `ParseCommandInput` context, since only one concrete context-dependent case exists so far to generalize from.
- [ ] **Sub-iteration 3 (probably deferred): a real WorldQuestion-plan LLM fallback.** Differentiates `PredictHypothesis`-shaped requests from other question types, reporting `Unimplemented` on the rest instead of Sub-iteration 1's blanket `PredictHypothesis` routing. No concrete second question type exists yet to design this against --- named so the Sub-iteration 1 regression it closes is tracked, not started.

## Open decisions (implementation --- plan only)

| ID | Decision | Blocks | Status |
| --- | --- | --- | --- |
| CPG-1 | Exact `DeterministicTemplate` type shape --- must match either a raw string or a `ParseToken` sequence, and carry context beyond bare command text (room exits, room object labels) for the matchers that need it. **Type-shape shipped 2026-07-20**: `DeterministicTemplate`/`DeterministicTemplateMatch` (three-arm `noMatch`/`matched`/`defer`, intent payload reusing `ParseCommandResult`), 5 bare-word + 10 relational pattern-driven templates. **Bare-word subset wired live 2026-07-20 (Sub-iteration 2)** via a new `matchNonObjectManipulationTemplate`/`nonObjectManipulationTemplateRegistry` in `deterministicTemplate/index.ts`, called from `parseCommand.ts`'s `Command` branch pre-Parse; resolved as a "bypass" match (terminal `ParseCommandResult` returned directly). **Still open:** relational entries stay unwired (folded into CPG-6 below); the context-dependent implementation kind (Navigation's exit resolution) was satisfied via a separate, non-`DeterministicTemplate` matcher (`plan/matchNavigationParaphrase.ts`) rather than widening the shared interface --- a deliberate scope call, not a resolution of the original question, revisit if a second context-dependent case appears. | Sub-iteration 1 (type-shape); Sub-iteration 2 (bare-word wiring) | Type-shape and bare-word wiring shipped 2026-07-20; relational wiring (CPG-6) and interface widening for context-dependent matchers still open |
| CPG-2 | Whether `parse`'s segmentation logic is genuinely shared or genuinely separate between the `Command` and `WorldQuestion` branches --- explicitly flagged as uncertain by the user, not assumed either way. **Command branch resolved 2026-07-20:** shares the existing object-manipulation `runParseStage` hop (no new segmentation logic needed --- object manipulation is the only real `Command` dispatch target this slice). **Still open:** `WorldQuestion` branch has no real segmentation yet (it routes straight to `PredictHypothesis` without running Parse at all) --- deferred to Sub-iteration 3, when a second question type exists to design against. | Sub-iteration 1 (Command branch, resolved); Sub-iteration 3 (WorldQuestion branch, still open) | Command branch shipped 2026-07-20; WorldQuestion branch not designed |
| CPG-3 | Membership-vs-relational dispatch mechanism once `classify` stops deciding it. **Resolved and shipped 2026-07-20:** `enrich/objectManipulation/plan/classifySkeletonFamily.ts` --- tries `matchRelationalTemplate` against the skeleton first (relational wins ties, matching classify's old tie-break, and resolves the `take`/`remove` structural ambiguity), then falls back to a `take`/`get`->acquire, `drop`->release check on the skeleton's leading text token, adapted from `deterministicChecks.ts`'s acquire/release regex. Not a full native Plan matcher (stays out of scope, per Confirmed scope boundaries). | Sub-iteration 1 | **Shipped 2026-07-20** |
| CPG-4 | `AcmeOrder`'s successive-LLM-run question --- does its existing `enrichAcmeOrder` trope/catalog affinity call fit as-is under the generalized command-plan dispatch shape, or does the shape need a hook for "primitive matched, now run a further LLM stage before Synthesize"? **Resolved and shipped 2026-07-20:** fits as-is. `plan/matchAcmeOrderFamily.ts` extracts `rawOrders` from the Parse skeleton's `objectSpan` tokens (leading verb `order`/`buy`/`purchase`) and calls `enrichAcmeOrder` directly as the terminal step --- no new hook needed. | Sub-iteration 2 | **Shipped 2026-07-20** |
| CPG-5 | Full `Identify` generalization --- folding Navigation's `resolveExitLabelToTargetId` and Acme's product-catalog resolution into one `Identify` concept alongside object/character span resolution. **Deliberately deferred, not resolved, 2026-07-20:** none of the six Sub-iteration 2 families need object manipulation's `EphemeraObjectId`-scoped catalog machinery --- Navigation already has its own fit-for-purpose resolver, AcmeOrder's catalog matching lives entirely inside `enrichAcmeOrder`'s own LLM call, and the rest need no referent resolution at all. Building a shared abstraction now, with only two dissimilar concrete cases, would be premature per this codebase's "grow the technique set as concrete cases demand" discipline. Rationale recorded in `actions/AGENT.concepts.md`'s "Three conceptual jobs" section. **Revived 2026-07-24 by iteration 9** ([`AGENT.perceptionKernel.planning.md`](AGENT.perceptionKernel.planning.md)) --- object-directed look (`LookComponent`-shaped, "look `<object>`") is the concrete second case that makes the pattern worth building; that plan's Phase 2 owns the actual resolution now, not this row. | Sub-iteration 2 | Named 2026-07-20; deliberately deferred 2026-07-20; **revived by iteration 9, 2026-07-24** |
| CPG-6 | **Zero-Bedrock relational fast-path (the "Down" bullet) --- carried in from the retired iteration-3 plan on 2026-07-20** (its graduation-prep pass), where it was long-standing design debt with no home. It's a concrete relational instance of this doc's layer-1 `DeterministicTemplate` (CPG-1): closed-grammar relational frames (`put`/`place`/`lean`/`take off` + a closed set of prepositions) matched deterministically before any LLM runs, emitting both the `ObjectRelateIntent` classification and a synthesized skeleton --- relation phrase by enum lookup, `operationKind` from the verb template, roles from `V NP prep NP`. This is the relational analog of `deterministicChecks.ts`'s existing membership take/get/drop fast path (which layer 1 generalizes). **Not a prerequisite for anything and orthogonal to correctness** --- Parse's own LLM output is already correct-by-Backtrack (iteration 4), not by this optimization; this only saves Bedrock cost/latency on recognized phrasings. Note the post-Parse deterministic matcher `matchRelationalTemplate.ts` (iteration 3) already does the same `V NP prep NP` role extraction over an *already-produced* skeleton --- so building CPG-6 is largely lifting that matcher's logic to also run pre-Parse over the raw string, which is exactly what layer 1 does; likely a shared implementation, not a parallel one. **Templates shipped 2026-07-20** (see CPG-1). **Still not wired, deferred again in Sub-iteration 2 (2026-07-20):** consuming a relational `DeterministicTemplate` match would mean threading its synthesized skeleton into the same downstream enrich pipeline `runParseStage`'s output goes into (`classifySkeletonFamily` -> membership/relational enrich) --- a distinct, nontrivial change with its own test surface, not required to close Sub-iteration 1's regression, which Sub-iteration 2 only wired the bare-word subset to close. `parseCommand.ts`'s relational branch still runs classify + Parse unconditionally today; the Bedrock cost/latency saving CPG-6 promises isn't realized until this wiring lands. | Sub-iteration 1 (fits alongside CPG-1/CPG-3's deterministic layer), but genuinely optional --- pure optimization, no regression closes on it | Templates shipped 2026-07-20; wiring into the live path still not started (deferred again 2026-07-20) |

## Relationship to existing decision rows

- **Supersedes BD-26's original framing** (single-hop classify+Parse merge), which originated in the now-retired iteration-3 plan --- that framing was proposed by Claude and rejected by the user 2026-07-20; the record is condensed in this doc's History below (fuller blow-by-blow in git history).
- **Substantially resolves BD-22** --- the "should Plan/Identify generalize beyond object manipulation" question is now answered yes, with this doc owning the actual implementation; BD-22's original exploration findings are preserved in the **Carried-in findings** section above.
- **Interacts with BD-18 / iteration 4 (Backtrack channel, [`AGENT.backtrackChannel.planning.md`](AGENT.backtrackChannel.planning.md)).** Backtrack is fully unbuilt today (no live re-entry mechanism exists for any stage), so this redesign breaks nothing real --- but once Backtrack is designed, its addressable "Parse" target becomes whichever `parse` stage(s) this doc builds, not a single unified one (per CPG-2).
- **Interacts with iteration 2's BD-19** (plan-only/joint LLM fallback, [`AGENT.manipulationFrameAndRelational.planning.md`](AGENT.manipulationFrameAndRelational.planning.md)), currently paused pending a stable skeleton. This doc's generalized command-plan is a superset of what those fallbacks needed --- whether they fold into this initiative's own build or stay a separate later step is not yet decided.

## Carried-in findings from BD-22 (migrated here when iteration 3 retired, 2026-07-20)

BD-22 ("should Parse/Plan/Identify generalize beyond object manipulation?") was named and explored in the now-deleted iteration-3 plan and resolved by this doc. Two concrete findings from that exploration are preserved here because CPG-5 (full `Identify` generalization) will build on them directly:

- **Candidate command/question families for a widened Parse/Plan** (found by a direct read of `baseClasses.ts`'s intent-result types): `ObjectMembershipIntent`/`ObjectRelateIntent` (object manipulation, done); `AcmeOrderIntent` (its `rawOrders` is structurally identical to `objectSpans` --- a list of extracted noun-phrase strings, same shape, different domain); `LookComponent` ("look at X") already carries a referent, resolved via its own bespoke path today. `Navigation`/`Home`/`LookRoom`/`Help`/`PredictHypothesis`/the Coyote test intents all come back **zero-referent**, which is itself the classifying signal, not a reason to route them elsewhere.
- **Character-as-referent is not a structural rework (verified in `positions/positionGraph/index.ts`):** `EphemeraPositionGraph` nodes already carry a `tag` field with `'Character'` as a real value alongside `'Object'` (`{ tag: 'Character'; universalKey: EphemeraCharacterId }`), filtered/removed by the same machinery. Characters and Objects are siblings under one node mechanism --- so the referent-*resolution* mechanism ("which graph node does this span refer to") needn't differ for character vs. object referents; the graph doesn't force a split. **Two narrower gaps remain**, not a rework: (1) `RoomInPlayObjectCatalogEntry` (`roomObjectCatalogForCharacter.ts`) is `EphemeraObjectId`-typed only and never scans Character nodes --- a character-inclusive catalog would need building, the same shape of work `mergeObjectManipulationCatalogs` already does for room-vs-held; (2) `EphemeraObjectId`/`EphemeraCharacterId` are distinct branded types throughout, so a referent that could resolve to either needs a widened id union (or the vocabulary must stop presupposing "object" excludes characters).

## Progress

| Milestone | Status |
| --- | --- |
| Design named and confirmed through conversation (5-layer architecture, 3 sub-iterations, scope boundaries, BD-N carry-ins) | Done (2026-07-20) |
| CPG-1 type-shape + pattern-driven `DeterministicTemplate` templates (bare-word + relational) built and tested, standalone | Done (2026-07-20); bare-word subset wired live in Sub-iteration 2 (2026-07-20), relational entries still not wired (CPG-6) |
| Sub-iteration 1: routing skeleton (narrowed classify, `Command`/`WorldQuestion` dispatch, CPG-2 Command branch, CPG-3) | **Done (2026-07-20)** |
| Sub-iteration 2: generalize Identify/Plan/Synthesize cluster, close Command regression | **Done (2026-07-20)** --- CPG-4 resolved, CPG-5 deliberately deferred |
| Sub-iteration 3: real `WorldQuestion`-plan LLM fallback | Not started, probably deferred (no second question type exists yet) |

## Verification

```bash
cd lambda/ephemera && npm run test -- --watchAll=false \
  dataSource/actions/discriminateIntent/ \
  dataSource/actions/parseCommand.test.ts \
  dataSource/actions/enrich/objectManipulation/
cd lambda/ephemera && npx tsc --noEmit
```

## History

**2026-07-20: BD-26 proposed as a single-hop merge, rejected.** Claude's initial proposal (motivated by the same latency observation) was to fold Parse's token emission directly into classify's response as a sibling field, decided in one simultaneous LLM judgment, and wrote a complete implementation plan on that basis without confirming the design with the user first. Rejected: "you were just improvising pretty much all of the design work... I'd rather be involved in the decisions, especially in a codebase this complex." The secondary unilateral decision that compounded the problem (bundling Bedrock prompt-caching restructuring into the same slice) is recorded in the durable lesson `feedback_architectural_design_involvement` (auto-memory); the fuller blow-by-blow is in git history (retired iteration-3 plan).

**2026-07-20: revised through direct conversation to the design in this doc**, in stages: (1) segmentation-then-classification sequencing within one LLM call ("New Parse"), (2) corrected again into a `DeterministicTemplate`-first, LLM-fallback-only-on-miss shape once the user clarified the actual mechanism, (3) `classify`'s remit narrowed further by recognizing that "is this a command or a world question" is itself a judgment classify should make (surfaced by the `PredictHypothesis`-vs-"are the king's guards close enough" tension --- both are grammatically questions, but only one is currently supported, which classify alone can't resolve without either keeping some intent-specific knowledge or deferring the distinction downstream), (4) resolved by making `WorldQuestion` a full sibling category to `Command`, each with its own `parse`+`plan` pair rather than shared machinery, specifically because Plan's downstream consumer (Synthesize vs. answer-generation) and any eventual LLM-fallback tier both diverge too much to share, and (5) staged into the three sub-iterations in Recommended order, deliberately accepting temporary regressions given this codebase's single-dev-instance, no-external-consumer status.

**2026-07-20: CPG-1's `AGENT.deterministicTemplate.planning.md` sub-plan graduated and deleted.** Built through its own three-step `/next-slice` sequence: Step 1 (core machinery + 4 bare-word templates), Step 2 (10 relational templates --- 6 enum, 2 custom, 2 containment/defer --- plus a `makeDeferPatternTemplate` factory; this also partially lands CPG-6, see that row above), Step 3 (this graduation). Durable vocabulary and module map moved into [`actions/AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.concepts.md#deterministictemplate-shipped-2026-07-20) / [`actions/AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.implementation.md#deterministictemplate-module-built-not-yet-wired); CPG-1/CPG-6 rows and Architecture point 1 above updated to point at the built feature. What's still open and stays owned here: wiring `matchDeterministicTemplate` into the live classify/Parse path, the entry-vs-bypass model decision, and the context-dependent implementation kind (Navigation exit resolution, `get`'s room-object-label gating) --- none of which the sub-plan built, by deliberate scope boundary.

**2026-07-20: orphaned call-sites annotated.** Four types with zero producers now (`ParseCommandNavigationIntentResult`, `ParseCommandHomeIntentResult`, `ParseCommandAcmeOrderIntentResult`, `ParseCommandAwaitRoadrunnerResult` in `baseClasses.ts`), `enrichAcmeOrder` (`enrich/acmeOrder/index.ts`, no longer called from `parseCommand.ts`), the three now-unreachable `case` branches in `index.ts`'s `parseErrorMessageForPlayer`, and `navigationIntentErrorMessages` (`discriminateIntent/exitResolution.ts`, nothing constructs an `Error` with these messages anymore) each got a doc comment at the definition site explaining why they're currently unreachable and pointing back to Sub-iteration 2's Recommended order as where they get reconnected --- so a future reader hitting one of these in isolation doesn't mistake it for abandoned/dead code.

**2026-07-20: Sub-iteration 1 (routing skeleton) shipped, via `/next-slice`.** Narrowed `classify` to the five-outcome remit (`baseClasses.ts`, `discriminateIntent/buildIntentClassificationPrompt.ts`, `discriminateIntent/intentClassification.ts`, `discriminateIntent/index.ts`, `discriminateIntent/baseClasses.ts`); shipped CPG-3 as a new module (`enrich/objectManipulation/plan/classifySkeletonFamily.ts`); resolved CPG-2's Command-branch half by reusing `runParseStage`; wired `Command`/`WorldQuestion` into `parseCommand.ts`, deleting the now-dead `ObjectRelateIntent` and `AcmeOrderIntent` branches (classify's LLM can no longer produce either) and simplifying the `ObjectMembershipIntent` branch to its deterministic-fast-path-only shape. **Scope decision made during this slice, not anticipated by the original checklist:** confirmed with the user that wiring CPG-1's already-built `DeterministicTemplate` layer into the live path is a separate, later step, not required for this slice's correctness (its bare-word templates are redundant with `deterministicChecks.ts`; its relational templates are a pure optimization, CPG-6). Both `npx tsc --noEmit` and the object-manipulation/discriminateIntent/parseCommand test suites are green; substantial test-file rewrites were required in `discriminateIntent/intentClassification.test.ts`, `discriminateIntent/index.test.ts`, and `parseCommand.test.ts` to match the narrowed contract (deterministic-fast-path tests were unaffected).

**2026-07-20: Sub-iteration 2 (command-plan dispatch for the six non-object-manipulation families) shipped, via `/next-slice`.** Before drafting the implementation plan, three explore agents mapped the current Command dispatch pattern, the orphaned-family annotations, and the Identify-generalization surface; the user then confirmed three real design forks up front (all six families in one slice; closed deterministic lexicons, not an LLM fallback; `enrichAcmeOrder` fits as-is for CPG-4) plus AwaitRoadRunner's trigger lexicon. **Mid-plan-review correction:** the first draft proposed new bespoke regex matchers for the four zero-referent families; the user pointed out CPG-1's already-built `deterministicTemplate/` module (`makePatternTemplate`, pure-data `bareWordTemplates.ts` rows) was the right home for closed-lexicon paraphrase matching, not parallel code --- the plan was revised before implementation to reuse it, which also meant genuinely starting CPG-1's deferred "wiring" work rather than duplicating its intent.

Shipped: `deterministicTemplate/bareWordTemplates.ts`'s `help`/`home` templates extended with closed paraphrase lexicons, plus a new `awaitRoadRunnerTemplate` (bare `wait` had no prior producer); `deterministicTemplate/index.ts` gained `nonObjectManipulationTemplateRegistry`/`matchNonObjectManipulationTemplate` --- a sibling to the existing full registry that excludes the relational entries **by construction** (a separate array, not a runtime filter), keeping the CPG-6 scope boundary visible in code. Two new bespoke (non-`DeterministicTemplate`) matchers: `plan/matchNavigationParaphrase.ts` (movement-verb lexicon beyond bare `go`, calls the existing `resolveExitLabelToTargetId`) and `plan/matchAcmeOrderFamily.ts` (post-Parse, extracts `rawOrders` from the skeleton's `objectSpan` tokens, calls `enrichAcmeOrder` directly --- resolving CPG-4). `parseCommand.ts`'s `Command` branch now tries the template-registry match and the Navigation matcher pre-Parse (zero Bedrock), then `runParseStage`/`classifySkeletonFamily` as before, then the AcmeOrder matcher on a `none` result, before falling to `Unimplemented`. Deleted the three permanently-dead two-phase Intent types (`ParseCommandNavigationIntentResult`/`HomeIntentResult`/`AcmeOrderIntentResult`) since every new matcher produces a terminal result directly, not a two-phase Intent-then-resolve dance; kept and reconnected `ParseCommandAwaitRoadrunnerResult` (the real terminal type) and `navigationIntentErrorMessages` (`exitResolution.ts`) plus `index.ts`'s matching `case` branches, removing their "Orphaned" doc comments.

**Post-implementation review correction:** the initial implementation, following the plan as drafted, extended `lookTemplate` with a closed paraphrase lexicon (`peruse the room`/`survey the room`/`examine the room`), matching the treatment given to `help`/`home`. The user asked for this removed on review: open-ended `look` paraphrases are free-form enough that a closed list isn't the right tool --- that's LLM-fallback territory, not yet built, unlike `help`/`home`/`wait`'s genuinely closed, enumerable paraphrase sets. Reverted `lookTemplate` to bare-word-only (`look`/`l`); since bare `look`/`l` are always intercepted upstream by `deterministicChecks.ts` before classify runs, `lookTemplate` can never actually fire from `parseCommand.ts`'s `Command` branch at all --- so it was also removed from `nonObjectManipulationTemplateRegistry` (now 3 entries: `help`/`home`/`awaitRoadRunner`), staying only in the full `deterministicTemplateRegistry` for standalone testing/future use, the same status `predictTemplate` already had. Updated the affected tests (`bareWordTemplates.test.ts`, `deterministicTemplate/index.test.ts`, `parseCommand.test.ts`'s LookRoom-paraphrase case, now asserting the fall-through-to-Parse-then-Unimplemented behavior instead) and the durable docs written moments earlier in this same slice (`AGENT.concepts.md`, `AGENT.implementation.md`) that had claimed `LookRoom` paraphrase coverage.

A direct follow-up question (a good audit habit worth naming here) asked whether Sub-iteration 1's orphaned-annotated code was fully reconnected, or whether anything had turned out to be superseded/dead and should've been deleted rather than reconnected. Answer, confirmed by grep: no leftover "Orphaned" annotations remain anywhere in `dataSource/actions/`; the three deleted Intent types were correctly identified as dead (no reconnection was possible or needed under the new one-phase-terminal-result shape); everything else named in Sub-iteration 1's annotations was genuinely reconnected. Separately (not something Sub-iteration 1 flagged, but worth recording since it surfaced from the same audit): `deterministicTemplateRegistry`/`matchDeterministicTemplate` (the *full* registry, including the 10 relational templates and now `predictTemplate`/`lookTemplate`) still has zero production call sites --- this is CPG-6's deliberate deferral, not an accidental leftover.

**Scope calls made during this slice, not anticipated by the original checklist** (same pattern as Sub-iteration 1's DeterministicTemplate deferral): CPG-5 (cross-family `Identify` unification) deliberately deferred --- none of the six families need it; CPG-6 (relational `DeterministicTemplate` entries wired live) deliberately deferred again --- only the `help`/`home`/`awaitRoadRunner` subset was wired, since relational wiring needs its synthesized skeleton threaded into the same downstream enrich pipeline Parse's output goes into, a distinct change; Navigation's paraphrase matcher was built as its own typed function rather than widening the shared `DeterministicTemplate.matchString(command: string)` interface to carry `ParseCommandInput` context (CPG-1's named "context-dependent implementation kind"), since only one concrete case exists so far to generalize an interface change from; `LookRoom` paraphrases stayed unclosed by user direction (see correction above) --- `LookRoom` is the one family, of the original six, whose Sub-iteration 1 paraphrase regression is *not* fully closed by this slice, deliberately, pending a future LLM-fallback mechanism.

Durable docs updated: `actions/AGENT.concepts.md` (`DeterministicTemplate` status, "Three conceptual jobs" broadened with the Plan-generalizes-but-Identify-doesn't-yet framing and CPG-5 rationale), `actions/AGENT.implementation.md` (`DeterministicTemplate` module section retitled/updated, new Navigation/AcmeOrder matcher subsections, Command pipeline sequence diagram updated), `AGENT.objectManipulationIterations.planning.md` (iteration 7 status cell, BD-26 row). Verification: `npx tsc --noEmit` clean; `dataSource/actions/discriminateIntent/`, `parseCommand.test.ts`, `enrich/objectManipulation/`, `deterministicTemplate/`, `plan/`, `enrich/acmeOrder/` all green (91 suites / 774 tests); full `dataSource/actions/` + `dataSource/objects/` also green (120 suites / 1021 tests, 2 pre-existing skips unrelated to this slice). One test-writing snag along the way: two AcmeOrder tests initially used an empty `tropeAffinities: []` in their mocked Bedrock response, which `enrichAcmeOrder`'s own finalize logic flags as `tropeAffinitiesFailed: true` --- fixed by giving the mocks a non-empty affinity, matching the working precedent in `enrich/acmeOrder/index.test.ts`.
