# DeterministicTemplate: a first-class deterministic match feature (single-word first, referent-bearing next)

**Status:** **Designed through conversation 2026-07-20, all Open decisions (DT-1 -- DT-7) resolved, not yet started.** Focused feature-build carved out of iteration 7's CPG-1 ([`AGENT.classifyPlanGeneralization.planning.md`](AGENT.classifyPlanGeneralization.planning.md)) through direct conversation. Deliberately narrow: build `DeterministicTemplate` / `DeterministicTemplateMatch` as a standalone, tested feature in the codebase --- **no plumbing** into the live classify/Parse path. Graduation (Step 3) updates the CPG-1 owner to reference the built feature and hands the plumbing decisions back to it. Next step: Step 1 (core machinery + bare-word templates) --- ready to decompose into an implementation slice.

Task-planning conventions: [`taskPlanning/AGENT.md`](../../../../AGENT.md). Parent initiative: [`AGENT.classifyPlanGeneralization.planning.md`](AGENT.classifyPlanGeneralization.planning.md) (iteration 7, CPG-1). Ladder index: [`AGENT.objectManipulationIterations.planning.md`](AGENT.objectManipulationIterations.planning.md).

## Purpose

CPG-1 asks for the exact type shape of a `DeterministicTemplate`: the layer-1 rule that matches either a raw command string or a token sequence and, on a hit, emits **both** an intent classification **and** a synthesized `ParseSkeleton` at zero Bedrock cost. This plan builds that shape as a self-contained feature so the type contract is proven against real templates before anything downstream depends on it.

Two precedents in the tree produce **incompatible output shapes** that this feature reconciles:
- [`discriminateIntent/deterministicChecks.ts`](../../../../../lambda/ephemera/dataSource/actions/discriminateIntent/deterministicChecks.ts): raw string -> `ParseCommandResult` (family-level intent), **no skeleton**.
- [`enrich/objectManipulation/plan/matchRelationalTemplate.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/plan/matchRelationalTemplate.ts): already-built `ParseSkeleton` -> `Change` (a Plan primitive), **no classification**, run **post**-Parse.

`DeterministicTemplateMatch` is the type that carries classification **and** skeleton together; `DeterministicTemplate` is the contract that produces it from `ParseCommandInput`.

## Scope boundary (what this plan deliberately does NOT do)

- **No plumbing.** The live classify (`discriminateIntent`) and Parse paths are untouched. Migrating today's ad-hoc checks into templates is CPG-1/CPG-3/CPG-6's plumbing work, handed back at graduation.
- **No model-dependent templates.** Only templates whose emitted shape is **invariant** to the entry-vs-bypass pipeline decision are built here: **bare-word** (Step 1) and **relational** (Step 2). **Navigation** (exit resolution) and the **`get`/membership context-gating** check are *deliberately excluded* --- their emitted shape hinges on entry-vs-bypass, so building them here would silently force a decision this plan defers to CPG. (Design rationale: a relational hit always emits classification + synthesized skeleton because object-span resolution is Identify's job under *any* model; bare words have nothing to resolve.)

## Design decisions (confirmed through conversation, 2026-07-20)

1. **`DeterministicTemplate` is a shared structural interface, satisfied by a factory function --- not a `class`.** TypeScript's structural typing means polymorphism doesn't require inheritance: any value with the right method shape conforms, whether it's produced by a class instance or a plain object literal holding function-valued properties. The contract:

   ```ts
   interface DeterministicTemplate {
     matchString(command: string): DeterministicTemplateMatch
     matchTokens(skeleton: ParseSkeleton): DeterministicTemplateMatch
   }
   ```

   This plan builds exactly **one** implementation --- the pattern-driven kind, produced by a factory `makePatternTemplate(pattern: PatternElement[], templateIntent: <partial constant>): DeterministicTemplate` that closes over its data and delegates to two **generic** (not per-template) engine functions:
   - `PatternElement = { type: 'templateText'; options: string[] } | { type: 'objectSpan'; role: IntentFieldRole } | { type: 'capturedText'; role: IntentFieldRole }` --- three kinds, cleanly split by what each does: `templateText` is a **closed-vocabulary gate**, its matched value is never threaded into intent (every template using it already fixes the relevant intent field as a constant, regardless of which listed synonym matched); `objectSpan` is a **referent slot** (feeds both a skeleton token and an intent field); `capturedText` is a **free-text slot** (feeds a skeleton `text` token and an intent field, like `objectSpan`, but carries no referent/Identify semantics --- for open-ended values like a custom relation label, see design decision 2 and DT-5). `IntentFieldRole` is a closed union (DT-6), not a freeform string.
   - `matchString` (builds an anchored, case-insensitive regex from the element list --- `templateText` options become an alternation, `objectSpan`/`capturedText` become a non-greedy bounded capture) and `matchTokens` (positional walk against an existing `ParseSkeleton`: `templateText` checks the corresponding `TextToken.text` is in `options`, `objectSpan`/`capturedText` accept the corresponding token and capture its text/span). Both are shared infrastructure, proven once against the rows this plan authors, and reused unchanged by every pattern-driven template.
   - The pattern doubles as the **skeleton synthesis template** for `matchString` hits: walking matched elements and emitting one `ParseToken` per element (`objectSpan` -> `ObjectSpanToken`, `templateText`/`capturedText` -> `TextToken`, stamped via `stampStableRefKeys`) produces the skeleton for free, with no separate hand-written synthesis step per template.

   **Why the interface, not just a `{pattern, templateIntent}` value directly:** bare-word and relational templates are *not* two implementations (same generic engine, different-length pattern arrays --- see design decision 2's granularity point). The real second implementation is the **context-dependent** family scoped out of this plan (Navigation's `roomExits` lookup, `get`'s `roomObjectLabels` gate) --- those can't be expressed as `{pattern, templateIntent}` data at all, since they read fields with no relationship to pattern-matching. Confirmed in conversation: defining the interface now, with today's only implementation being a factory-produced value, leaves that future kind a seam to slot into (a hand-written object satisfying the same two-method shape) without reshaping the registry or `DeterministicTemplateMatch` --- while keeping the ~20 pattern-driven rows in this plan as plain `{pattern, templateIntent}` **data** (testable, comparable, enumerable) fed through `makePatternTemplate` once at registry-build time, not authored as class instances.
2. **No `deriveIntent` function --- templates are granular enough that intent is a constant, assembled by one shared, generic combinator.** Earlier drafts of this design considered one template per *family* (all relational verbs + all prepositions in one matcher with internal branching, mirroring `matchRelationalTemplate.ts`'s `classifyVerb`/`normalizeRelationSpan`) with a per-template `deriveIntent(captures)` function. **Rejected on direct user correction:** there is no reason a template must be that coarse. Splitting finer --- confirmed by a direct read of `normalizeRelationSpan.ts` (DT-5) --- turns the entire non-referent part of intent into a **constant** per template, in two tiers:
   - **Enum bucket (closed, genuinely enumerable):** `normalizeRelationSpan` groups synonym phrases into exactly 3 relation kinds --- `Against` (`against`/`lean against`/`leaning against`), `Under` (`under`/`beneath`/`underneath`), `On` (`on`/`onto`/`on top of`). One template per (verb-class x relationKind) --- 2 verb-classes x 3 kinds = 6 templates --- with all synonym phrases in that template's `templateText` `options` list and `templateIntent: { primitive, relationKind }` as a full constant.
   - **Custom bucket (open-ended, not enumerable):** anything not in the enum or containment sets falls through to `relationLabel` = the matched text itself. This can't be a finite set of data rows, so it needs the third `PatternElement` kind, `capturedText` (design decision 1) --- its matched value is threaded into `templateIntent.relationLabel` by the generic combinator. This is still **shared, generic machinery** (`capturedText`'s contract is "always feed the match into the named intent field," true for every template that uses it), not per-template derivation logic.

   What remains parametric across both tiers is exactly the `objectSpan` captures (subject/target refs), which are positional data, not derivation logic; one generic combinator (`{ ...templateIntent, ...roleTaggedCaptures }`, shared core machinery, not per-template code) assembles the final intent, whether the tagged captures come from `objectSpan` or `capturedText` elements. Net effect: **more, smaller templates, zero per-template functions** --- each new template (Step 1 or Step 2) is a pure data row.
3. **Reuse `ParseCommandResult` for the intent payload --- do not invent a new "intent info" type.** The ad-hoc `verbClass` / `operationKind` encoding already lives *in* the existing result union (`ObjectMembershipIntent.verbClass`, relational's derived `Change`). The `matched` arm carries `{ skeleton: ParseSkeleton, intent: ParseCommandResult }`; the union is discriminated by construction, so structurally different families (Navigation vs membership vs relational) coexist without a flattened bag.
4. **A match is not binary --- three arms.** `noMatch | matched | defer`. `defer` preserves `matchRelationalTemplate`'s existing `nestingDefer` case ("matched the shape, but it's a known-unsupported case with its own error path") and gives deterministic `Error`-style outcomes (cf. `deterministicChecks`'s malformed-coyote-slash `Error`) a home distinct from "didn't understand this at all." The full union is defined in Step 1 so the contract is stable, even though `defer` has no producer until Step 2.
5. **`matchString` has a hard structural limit, inherent to determinism itself, not a regex weakness.** Two adjacent `objectSpan` elements with no intervening `templateText` are unsegmentable from raw text (`"put bag box"` cannot be deterministically split into one span or two without semantic/world knowledge) --- true of any deterministic grammar, not specific to a regex implementation. A pattern with adjacent `objectSpan` elements is therefore **`matchString`-ineligible by construction** (still valid for `matchTokens`, since Parse's LLM has already segmented spans by the time tokens exist). This is not a gap to close later: commands that need that segmentation judgment are exactly the ones meant to fall through to Parse's LLM. State this as an invariant of the pattern language, not an open problem.
6. **Entry-vs-bypass is a plumbing decision, deferred.** Whether a live match is an *entry* to Plan/Identify (emit classification + skeleton, resolution stays downstream) or a *bypass* (fully resolved result) does **not** change the core machinery or the `matched` type --- `ParseCommandResult` already contains both coarse-classification and fully-resolved members, so the type forecloses neither. Confirmed in conversation: this plan builds the feature model-agnostically; CPG owns the model decision when it plumbs.

## Getting started

1. Skim [`taskPlanning/AGENT.md`](../../../../AGENT.md) and this plan's parent, [`AGENT.classifyPlanGeneralization.planning.md`](AGENT.classifyPlanGeneralization.planning.md) (CPG-1 is the row this feature resolves).
2. Read the two precedents named in **Purpose** and the token types ([`enrich/objectManipulation/parse/parseToken.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/parse/parseToken.ts)) and [`stampStableRefKeys.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/parse/stampStableRefKeys.ts) (reused for Step 2 skeleton synthesis).
3. Read `ParseCommandInput` / the `ParseCommandResult` union in [`baseClasses.ts`](../../../../../lambda/ephemera/dataSource/actions/baseClasses.ts) (the intent payload the `matched` arm reuses; the context fields templates may read).
4. Testing authority: [`lambda/ephemera/AGENT.testing.md`](../../../../../lambda/ephemera/AGENT.testing.md). Jest via `npm run test` from `lambda/ephemera`. If commands conflict, that doc wins.
5. Baseline (should pass before edits):

```bash
cd lambda/ephemera && npm run test -- --watchAll=false \
  dataSource/actions/discriminateIntent/ \
  dataSource/actions/enrich/objectManipulation/plan/
cd lambda/ephemera && npx tsc --noEmit
```

## Recommended order

Pending work uses `[ ]`, completed work uses `[X]`; mark each nested line `[X]` as it lands.

- [ ] **Step 1: Core machinery + bare-word templates (single-word pure matches).**
  - [ ] Create `dataSource/actions/deterministicTemplate/` (DT-1).
  - [ ] Define the `DeterministicTemplate` interface (`matchString(command): DeterministicTemplateMatch`, `matchTokens(skeleton): DeterministicTemplateMatch`) and `IntentFieldRole` as a closed union (DT-6; extend as new templates need new fields, starting with whatever Step 1/Step 2 actually require --- e.g. `'subject' | 'target' | 'relationLabel'`) in one module (DT-7).
  - [ ] Define `PatternElement = { type: 'templateText'; options: string[] } | { type: 'objectSpan'; role: IntentFieldRole } | { type: 'capturedText'; role: IntentFieldRole }` in the same interface module (DT-7).
  - [ ] Define `DeterministicTemplateMatch = { type: 'noMatch' } | { type: 'matched'; skeleton: ParseSkeleton; intent: ParseCommandResult } | { type: 'defer'; reason: DeterministicTemplateDeferReason }` (full three-arm union; `reason` is a closed union starting with `'nesting'` as its only member, DT-3).
  - [ ] Build the two generic engine functions the factory delegates to, in a separate pattern-driven module (DT-7): pattern -> anchored case-insensitive regex (extracts `objectSpan`/`capturedText` captures) for the string case; positional walk against an existing `ParseSkeleton` for the token case. Both are pure functions of `(pattern, input)`, shared infra proven once, not reimplemented per template.
  - [ ] Build `makePatternTemplate(pattern, templateIntent): DeterministicTemplate` --- the factory producing pattern-driven templates by closing over their data and delegating to the two engine functions above.
  - [ ] Build the one generic intent-assembly combinator: `{ ...templateIntent, ...roleTaggedCaptures }` (`objectSpan` and `capturedText` both contribute role-tagged captures; `templateText` contributes nothing) --- shared, not per-template, called from inside the factory's produced `matchString`/`matchTokens`.
  - [ ] Build skeleton synthesis from a matched pattern (walk elements, emit one `ParseToken` per element --- `objectSpan` -> `ObjectSpanToken`, `templateText`/`capturedText` -> `TextToken` --- stamp via `stampStableRefKeys`) --- shared, used by any `matchString` hit.
  - [ ] An ordered registry (`DeterministicTemplate[]`, agnostic to which factory/kind produced each entry) + a single dispatch entry point (`matchDeterministicTemplate(input)` -> first non-`noMatch` across all templates via `matchString`, else `noMatch`).
  - [ ] Bare-word templates (pure `{pattern, templateIntent}` data rows, zero `objectSpan`/`capturedText` elements, fed through `makePatternTemplate`): `look`/`l`, `help`, `home`, `predict` --- each a `pattern` of one `templateText` element and a `templateIntent` constant (`LookRoom`/`Help`/`Home`/`PredictHypothesis`), skeleton = a single `text` token holding the matched word (DT-2 default).
  - [ ] Tests: each bare word -> `matched` with correct intent + single-`text`-token skeleton; near-misses (`looks`, `helper`, trailing args) -> `noMatch`; registry ordering is deterministic; engine-function/combinator tests exercise the string and token matching directly (not only through bare-word templates), since Step 2 depends on them working for referent-bearing and free-text patterns too.
- [ ] **Step 2: Referent-bearing capability --- granular relational templates using the same executors.**
  - [ ] Author the enum-bucket templates: one per (verb-class x relationKind), 2 x 3 = 6 rows (`Against`/`Under`/`On`), `templateText` `options` holding all synonym phrases for that kind, `templateIntent: { primitive, relationKind }` a full constant. Reuse `matchRelationalTemplate`'s verb sets and `normalizeRelationSpan`'s phrase/kind groupings as the source data, not re-derived from scratch.
  - [ ] Author the custom-bucket templates (one per verb-class, using `capturedText` for the preposition slot; `templateIntent: { primitive, relationKind: 'Custom' }` with `relationLabel` filled by the generic combinator from the `capturedText` capture, per design decision 2).
  - [ ] Author the containment/defer-bucket templates (one per verb-class, preposition slot a `templateText` over `in`/`inside`/`into`) emitting `{ type: 'defer', reason: 'nesting' }`, not `matched`, not `noMatch`.
  - [ ] Verify the adjacency invariant (design decision 5) holds for every authored pattern --- no two `objectSpan`/`capturedText` elements without an intervening `templateText`.
  - [ ] Tests: enum bucket x verb-class -> correct skeleton + intent; custom bucket -> `relationLabel` correctly captures arbitrary preposition text; containment -> `defer` with `reason: 'nesting'`; malformed / non-relational -> `noMatch`. All via the **same generic executors and combinator** proven in Step 1 (no new matching logic, only new data rows). **Note:** this partially lands CPG-6 (relational logic now runs pre-Parse over a raw string) --- intentional, harmless (pure optimization, no regression rides on it).
- [ ] **Step 3: Graduation (retire this plan).**
  - [ ] Move durable vocabulary (`DeterministicTemplate`, `DeterministicTemplateMatch`, the three-arm result, skeleton-synthesis-on-hit) into [`actions/AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.concepts.md); module paths + behavior into [`actions/AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.implementation.md).
  - [ ] Update [`AGENT.classifyPlanGeneralization.planning.md`](AGENT.classifyPlanGeneralization.planning.md): annotate **CPG-1** as *type-shape built and living in code* (feature done; plumbing still open), point its layer-1 (Architecture point 1) description at the built feature, record that **CPG-6** partially landed, and confirm that the **entry-vs-bypass model decision** plus **Navigation / `get`-gating migration** remain CPG's open plumbing work.
  - [ ] Update the iteration-7 row / BD-N notes in [`AGENT.objectManipulationIterations.planning.md`](AGENT.objectManipulationIterations.planning.md) if a pointer is warranted.
  - [ ] Delete this planning doc (git retains history).

## Open decisions (implementation --- plan only)

Plan-only: decisions made in order to implement the next slice(s). When a decision ships, record it in `actions/AGENT.implementation.md` (or `AGENT.concepts.md` for vocabulary) and remove the row here.

| ID | Decision | Blocks | Status |
| --- | --- | --- | --- |
| DT-1 | Code placement --- a new cross-family `dataSource/actions/deterministicTemplate/` dir (it generalizes both `discriminateIntent/deterministicChecks.ts` and `enrich/.../matchRelationalTemplate.ts`, so it belongs under neither), vs. living inside `discriminateIntent/`. | Step 1 | **Decided 2026-07-20: new dir** |
| DT-2 | Skeleton shape for zero-referent (bare-word) matches --- a single `text` token holding the matched word (per CPG doc's "even trivial --- a single `text` token for `look`"; keeps downstream from special-casing empty), vs. an empty `[]` skeleton. | Step 1 | **Decided 2026-07-20: single-`text`-token default** |
| DT-3 | Whether `defer` carries a reason discriminator so distinct known-defer/error paths stay distinguishable, vs. a bare marker re-derived downstream. | Step 2 (first real producer) | **Decided 2026-07-20: enumerated `reason` field** --- `DeterministicTemplateDeferReason`, a closed union starting with `'nesting'` as its only member, extended as future defer producers are added |
| DT-4 | Whether `matched.intent` is the full `ParseCommandResult` union or a narrower classification-only subtype. | Step 1 | **Decided 2026-07-20: full reuse** (see design decision 3) |
| DT-5 | Whether the closed preposition set (`normalizeRelationSpan`'s enum) is exhaustively enumerable into one-template-per-preposition rows, vs. some bucket needing more to represent. | Step 2 | **Decided 2026-07-20, after reading `normalizeRelationSpan.ts`:** not fully enumerable. Enum bucket (`Against`/`Under`/`On`, synonym phrases grouped per kind) is closed --- 2 verb-classes x 3 kinds = 6 data rows. Custom bucket (arbitrary `relationLabel`) is open-ended --- **not** a second factory (would duplicate the shared engine for no new matching capability); resolved by adding a third `PatternElement` kind, `capturedText` (design decisions 1--2), still run through the same `makePatternTemplate`/engines |
| DT-6 | `objectSpan`/`capturedText` `role` tag vocabulary --- freeform `string` vs. a closed enum, so the generic combinator's field assignment can be typechecked rather than stringly-typed. | Step 1 (combinator) | **Decided 2026-07-20: closed enum for now** (`IntentFieldRole`), extended as new templates need new fields |
| DT-7 | Whether `makePatternTemplate` and the two engine functions live in the same module as the `DeterministicTemplate` interface, or split. | Step 1 | **Decided 2026-07-20: split** --- interface (`DeterministicTemplate`, `PatternElement`, `IntentFieldRole`, `DeterministicTemplateMatch`) in one module; pattern-driven factory + engines in another, so a future context-dependent implementation depends only on the interface module |

**Superseded by design decisions above (kept out of this table, not re-litigated):** the earlier "`match(input): DeterministicTemplateMatch` opaque function" and "per-template `deriveIntent(captures)` function" framings, and the intermediate "`{pattern, templateIntent}` as the template value itself" framing (now: that shape is the factory's *input*, not the `DeterministicTemplate` value --- the interface plus `makePatternTemplate` factory is the shape, design decision 1).

## Verification

```bash
# Feature tests (new dir per DT-1) + no regression in the precedents this generalizes:
cd lambda/ephemera && npm run test -- --watchAll=false \
  dataSource/actions/deterministicTemplate/ \
  dataSource/actions/discriminateIntent/ \
  dataSource/actions/enrich/objectManipulation/plan/
cd lambda/ephemera && npx tsc --noEmit
```

## History

**2026-07-20: created.** Split from CPG-1 through direct conversation: assessed the two-class (`DeterministicTemplate` / `DeterministicTemplateMatch`) shape as coherent, then agreed to build it as a focused, plumbing-free feature staged single-word-first -> referent-bearing, deferring the entry-vs-bypass pipeline model to CPG. Graduation folds the type into durable docs and hands plumbing back to CPG-1.

**2026-07-20 (same day, refinement pass): `DeterministicTemplate` recast as declarative pattern data, not a `match()` contract.** Revised through conversation in three steps: (1) `DeterministicTemplate` generalized to provide both `matchString` (regex-generated from a pattern) and `matchTokens` (positional token comparison) from one shared pattern-element list, rather than one opaque per-template function --- surfaced that the pattern doubles as the skeleton-synthesis template for free; (2) named and accepted, not solved, a hard structural limit: adjacent `objectSpan` elements with no intervening fixed text are unsegmentable from raw text by *any* deterministic grammar, not a regex-specific weakness --- such patterns are `matchString`-ineligible by construction, and that's the system correctly deferring to Parse's LLM, not a gap; (3) on direct user challenge ("why would `{put,place,lean}` and `{take,remove}` share a template?"), the per-template `deriveIntent(captures)` function was dropped entirely --- splitting templates one-per-(verb-class x preposition) turns intent into a `templateIntent` constant, leaving only role-tagged `objectSpan` captures and one generic, shared combinator (`{...templateIntent, ...roleTaggedCaptures}` plus a `custom`-relationLabel special case). Net effect: zero per-template logic, more/smaller templates, each a pure data row.

**2026-07-20 (same day, second refinement): `DeterministicTemplate` settled as a structural interface satisfied by a factory, not a `class`.** User pushed to reconsider a `class` (constructed from `{pattern, templateIntent}`, `matchString`/`matchTokens` as methods), reasoning that discussing this as a "contract" across two different template shapes (bare-word, referent-bearing) implied polymorphism was coming. Reassessed: bare-word and relational are **not** two implementations --- both run through the same generic engine over a `PatternElement[]` of different lengths, so no polymorphism is needed between them. The real future fork is pattern-driven (this plan) vs. context-dependent (Navigation/`get`-gating, already scoped out to CPG's plumbing) --- those genuinely can't share one engine. Resolved by defining `DeterministicTemplate` as a two-method structural interface (`matchString`/`matchTokens`), satisfied today by exactly one implementation --- a `makePatternTemplate(pattern, templateIntent)` factory, an object literal with function-valued properties, not a class instance --- leaving the interface as the seam a future context-dependent implementation can satisfy without inheritance, a base class, or reshaping the registry.

**2026-07-20 (same day, decisions resolved): all seven Open decisions (DT-1--DT-7) confirmed.** DT-1/DT-2/DT-4/DT-6/DT-7 confirmed per earlier recommendations; DT-3 confirmed as an enumerated `reason` field (not a bare marker). DT-5 required the flagged read of `normalizeRelationSpan.ts` before resolving: found the preposition set is **not** uniformly enumerable --- a closed `Against`/`Under`/`On` enum bucket (synonym phrases grouped per kind, 2 verb-classes x 3 kinds = 6 rows) alongside an open-ended `custom` bucket (arbitrary `relationLabel` text). Considered and rejected a second factory for the custom bucket (would duplicate the shared engine for no new capability); resolved instead by widening `PatternElement` with a third kind, `capturedText` (free-text slot, feeds its match into `templateIntent` via the same generic combinator) --- still one factory, one pair of engines, one combinator. Design decisions 1--2 and Step 1/2's task lists updated accordingly.
