# Perception kernel: heterogeneous instructions, object-directed look (iteration 9)

**Status:** In progress, 2026-07-25. Phase 1 done. Phase 2 (CPG-5) partly done: `Thing` union + Grounding/Identify candidate-typing widened; catalog population deferred to a follow-up slice. Phase 3 done (perception kernel built and tested); its Object-referent gap (PK-6) shipped as a shortName-only stub 2026-07-25 --- see PK-6 below. **Phase 4 done 2026-07-25**: object-directed look ("look `<object>`"/"examine `<object>`") now has a real text-parse path, and `executeStepSequence`/`perceiveStepSequence` have their first live production caller.

Task-planning conventions: [`taskPlanning/AGENT.md`](../../../../AGENT.md). Ladder position: [`AGENT.objectManipulationIterations.planning.md`](AGENT.objectManipulationIterations.planning.md), iteration 9.

## Purpose

Today's Execute-layer kernel (`positions/manipulation/kernel/`) is a pure mutation engine: its whole design --- atomic `transactWrite` bundling (BD-31), diff-derived fact streaming (`factsForStep.ts`) --- exists to solve problems only a graph *mutation* has. There is no path today for a command whose job is to **read** current state and deliver a description, rather than change anything.

This gap is concrete, not hypothetical: neither bare `look`/`l` paraphrases (e.g. "peruse the room") nor any object-directed look ("look rocket skates", "examine `<object>`") has a text-parse path today. `ParseCommandLookComponentResult` (`baseClasses.ts:170`) already exists, but its `componentId` is typed `EphemeraRoomId | EphemeraFeatureId | EphemeraKnowledgeId` --- **not** `EphemeraObjectId` --- and it's explicitly "Not produced by Bedrock parse," only by trusted UI clicks (`routeTrustedUiAction.ts:58-67`). Object-directed look is new scope, not a missing front end on an existing type.

This iteration:

1. Widens Synthesize's grounded-instruction vocabulary to include a **read/describe** kind alongside today's three mutation kinds, so `LookComponent`-shaped commands can reuse the same Grounding/Identify machinery object manipulation already has (this is what finally gives iteration 7's deferred **CPG-5** --- cross-family Identify/Grounding unification --- a second concrete case to build against; see [`AGENT.classifyPlanGeneralization.planning.md`](AGENT.classifyPlanGeneralization.planning.md), CPG-5 row).
2. Builds a new, deliberately lightweight **perception kernel** that consumes the `describe` instructions.
3. Wires strict sequential orchestration between the two kernels (positionGraph kernel commits first; perception kernel reads its committed output second) --- never list order, never parallel.
4. Adds a Plan-stage command-plan entry for object-directed look, following iteration 7's existing layer-4 pattern (`matchRelationalTemplate.ts` is the precedent).

**Explicitly separate from the `LookRoom`-paraphrase LLM-fallback gap** (iteration 7 sub-iteration 2's deliberately-unclosed regression --- "peruse the room" has no closed lexicon and needs an LLM fallback, not a template). That gap is about *classify/Plan not recognizing the command at all*; this iteration is about *what happens once Plan has produced a grounded read instruction*. Whether the paraphrase-fallback work folds into this iteration or stays separate is an open decision below, not assumed either way.

## Design decisions (confirmed through conversation, 2026-07-24)

- **No new "partition" stage.** Grounding stays one shared pass producing a single instruction list (already grounded, tagged by `kind` --- same shape as today's `KernelStep` union, just widened). Each kernel applies its own type-guard filter over that shared list to pull out the instructions it owns. Rejected explicitly: a dedicated dispatcher that sorts instructions into per-kernel buckets up front --- that's a second place that has to know about every kernel's step kinds and silently goes stale when a new kernel is added. This mirrors BD-30/BD-34's existing "shared environment, per-primitive dispatch" precedent rather than introducing a new pattern.
- **Kernels are invoked sequentially, not read off list/kind order and not run in parallel.** The positionGraph kernel's filter-and-commit runs first (unchanged behavior); only after it completes does the perception kernel's filter-and-render run, against the positionGraph kernel's resulting graph state. Reasoning, confirmed directly: descriptions exist only to be delivered to the player, and delivering one that a subsequent mutation in the same command immediately invalidates is worse than the extra sequencing. This ordering is a property of **invocation**, not of the instruction list's structure --- it must not be allowed to become an assumption baked into list order.
- **Perception kernel is not a second instance of the positionGraph kernel's machinery.** The worklist executor, `transactWrite` bundling, and carry-closure fixpoint all exist to solve mutation-specific problems (atomicity, multi-step consistency). A read-only kernel has none of those problems --- it's closer to "walk a short list of already-grounded referents, render each," and should be built that size, not scaled up to match the mutation kernel's internal shape out of a symmetry instinct.
- **Degenerate cases fall out for free** under this design: a pure manipulation command leaves the perception kernel's filtered list empty (no-op); a pure look command leaves the positionGraph kernel's filtered list empty (no-op, unchanged from today).
- **Always-last is safe to assume**, confirmed directly: descriptions are only ever generated in order to deliver them to the player, so there is no case where a later step in the same command needs to depend on an already-generated description.

## Getting started

1. Skim [`taskPlanning/AGENT.md`](../../../../AGENT.md) and [`AGENT.objectManipulationIterations.planning.md`](AGENT.objectManipulationIterations.planning.md) (ladder position, BD-N index --- especially BD-27/BD-30/BD-31/BD-34/BD-36, the executor/kernel decisions this iteration extends).
2. Read [`AGENT.classifyPlanGeneralization.planning.md`](AGENT.classifyPlanGeneralization.planning.md)'s CPG-5 pointer and [`actions/AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/actions/AGENT.concepts.md)'s "Three conceptual jobs" section --- the durable finding there (`EphemeraPositionGraph` nodes are already `'Object'`/`'Character'`-tagged siblings under one mechanism, so referent resolution doesn't need to structurally differ) is the premise Phase 2 below builds on; the concrete gaps that stand between that fact and working code are this plan's own debt (Phase 2), not restated in the durable doc.
3. Read the current kernel to understand what's being extended, not replaced: [`positions/manipulation/kernel/kernelStep.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/kernelStep.ts) (the mutation-only step vocabulary), [`positions/manipulation/kernel/factsForStep.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/factsForStep.ts) (diff-derived fact streaming --- note why this doesn't generalize to a read step), [`positions/manipulation/kernel/commitStepSequence.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/commitStepSequence.ts) (atomic commit, the thing the perception kernel must NOT need).
4. Read the existing `LookRoom` render-orchestration precedent (lane ordering: Perception Thread Registered -> flush(lane) -> default-lane Render Requested) --- referenced in the (now-retired) `AGENT.actionParse.plan.md`'s git history and in `dataSource/actions/AGENT.md`; confirm current shape before assuming it's still accurate.
5. Read `baseClasses.ts:169-176` (`ParseCommandLookComponentResult`) and `routeTrustedUiAction.ts:58-67` (its only current producer) to see exactly what's reused vs. new.
6. Read [`enrich/objectManipulation/synthesize/executorTypes.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/synthesize/executorTypes.ts) and [`plan/matchRelationalTemplate.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/plan/matchRelationalTemplate.ts) --- the instruction shape to widen, and the layer-4 Plan-dispatch precedent to follow for the new look-family matcher.
7. Testing authority: [`lambda/ephemera/AGENT.testing.md`](../../../../../lambda/ephemera/AGENT.testing.md).
8. Baseline:

```bash
cd lambda/ephemera && npm run test -- --watchAll=false \
  dataSource/positions/manipulation/kernel/ \
  dataSource/actions/enrich/objectManipulation/synthesize/ \
  dataSource/actions/parseCommand.test.ts
```

## Recommended order

Use `[ ]` for pending and `[X]` for complete. Mark nested lines as you finish each sub-step. Nothing below is built yet; all lines start `[ ]`.

- [X] **Phase 1. Widen the grounded-instruction vocabulary.**
  - [X] Decide where a new `describe` instruction kind lives: widen `KernelStep`/`ExecutorParsePlanStep` directly, or a sibling type the two kernels share. Must carry a resolved referent (id + kind: room/object/character/feature/knowledge) --- parameterize over referent kind the same way `KernelTransferMembershipStep` already generalized over entity kind (BD-36), rather than one step shape per look-variant.
    - **Decided (2026-07-24): widen directly.** Added `ExecutorDescribeStep` (`executorTypes.ts`) --- `{ kind: 'describe', referentId, referentKind: 'room'|'object'|'character'|'feature'|'knowledge' }`, singular referent (no carry-closure concept for a read) --- to `ExecutorParsePlanStep` and `isExecutorParsePlanStep`. Widened `KernelStep` (`kernelStep.ts`) to `KernelMutationStep | ExecutorDescribeStep`, reusing the describe step verbatim (no kernel-layer widening needed, same as relational steps). Split out `KernelMutationStep` (today's pre-widening shape) as the type the positionGraph kernel's commit machinery (`commitStepSequence.ts`, `applyStepSequenceCore.ts`, `computeStepSequenceFootprint.ts`, `factsForStep.ts`, `types.ts`, plus all four mutation-route call sites) keeps accepting --- a `describe` step must never reach that machinery, so those signatures stay narrow rather than widen to the shared list's type. `fromExecutorStep` is overloaded so mutation-only callers still get `KernelMutationStep` back without a cast. All touched suites green (195 tests, 18 suites).
  - [X] Decide whether `ParseCommandLookComponentResult` widens its `componentId` union to include `EphemeraObjectId` (and `EphemeraCharacterId`, if character-directed look is in scope), or whether object/character-directed look becomes its own terminal result type. Note it is explicitly documented today as "Not produced by Bedrock parse" --- widening it changes that invariant and needs its own confirmation, not an assumption.
    - **Decided (2026-07-24): widens.** `ParseCommandLookComponentResult.componentId` widens to `EphemeraRoomId | EphemeraFeatureId | EphemeraKnowledgeId | EphemeraObjectId` --- object-directed look reuses this same result type rather than getting its own terminal type. The "Not produced by Bedrock parse" invariant changes: Phase 4's new Plan-stage matcher becomes a second producer alongside `routeTrustedUiAction.ts`. Character-directed look is not decided here --- left to PK-3's scope call; if in scope, `EphemeraCharacterId` joins this same union rather than forcing a separate result type.
- [ ] **Phase 2. Resolve CPG-5 concretely for this one case.**
  - [X] **`Thing` union, confirmed through conversation 2026-07-24:** the referent-kind abstraction this phase needs is `Thing = Object | Character | Feature` (matching `ExecutorDescribeStep.referentKind`'s existing enumeration and `groundReferent.ts`'s existing `EphemeraObjectId | EphemeraCharacterId | EphemeraMembershipHostId` candidate union --- both already independently reinvented this shape). Widen catalog/Identify *typing* to `Thing` as a first step even with zero non-Object entries in the catalog data; adding real Character (or Feature) entries is then a separate, later question of "where does the data/embeddings come from," not blocking the type widening.
    - **Decided/built (2026-07-24):** added `EphemeraThingId = EphemeraObjectId | EphemeraCharacterId | EphemeraFeatureId` + `isEphemeraThingId` guard in a new file, [`actions/enrich/objectManipulation/thing.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/thing.ts). Widened `groundReferent.ts`'s `ResolvedSpan.candidateIds`, `GroundingContext.getCurrentHost`'s param, and `GroundReferentResult.candidates` (keeping `| EphemeraMembershipHostId` alongside) to use it --- confirmed behaviorally a no-op (Grounding never branches on id kind), so this purely names the abstraction CPG-5 asked for at the one place it was already load-bearing. Widened Identify's own candidate representation too: `spanResolution.ts`'s `ObjectSpanCandidate.id` (and `isObjectSpanCandidate`'s guard) now accept `EphemeraThingId`, with zero non-Object entries produced at runtime this slice (per plan). **Deviation from the original bullet's plan:** `SpanResolutionOutcome`/`SpanResolutionConsultAlternative` and `IdentityPlanIdentity.objectId` were investigated and deliberately left `EphemeraObjectId`-only, not widened --- both are membership/relational-**plan**-facing shapes (mutation machinery), a boundary the doc's own cross-reference note (below) already says stays Object-scoped. Added assert-and-throw narrowing at the two seams that convert a widened Identify candidate into that Object-only shape (`selectSingleSpanFromPool.ts`'s `assertObjectCandidate`, `identityPlanCandidate.ts`'s `identityFromSpanCandidate` guard) rather than propagating the widened type further, since nothing downstream of either seam can act on a Character/Feature id yet.
    - **Pre-existing `tsc` errors fixed in passing (2026-07-24):** `executeObjectDrop.ts`/`executeObjectTakeHold.ts` were already failing `tsc --noEmit` before this slice (introduced by Phase 1's `KernelStep` widening, not this slice) --- `outcome.steps.map(fromExecutorStep)` resolves to `KernelStep[]` rather than `KernelMutationStep[]` through `Array.prototype.map`, because overload resolution on a bare function reference picks the general signature, not the narrow one direct calls get. Fixed by adding an `isKernelMutationStep` type guard (`kernelStep.ts`) and filtering through it at both call sites, rather than casting --- the same "shared list, per-kernel filter" discipline Phase 3's perception-kernel filter will use, applied here too. `tsc --noEmit` is now fully clean (zero errors, not just no-new-errors).
    - Verified via `tsc --noEmit`: fully clean (zero errors repo-wide, confirmed against a pre-edit baseline capture). All touched suites green (36 suites, 348 tests: `positions/manipulation/kernel/`, `synthesize/`, `embeddingMatch/`, `parseCommand.test.ts`, plus `positions/manipulation/membership/`'s 3 suites/12 tests for the two fixed call sites).
    - **Knowledge deliberately excluded from `Thing`, confirmed through conversation 2026-07-24:** Object/Character/Feature are all physically-grounded `EphemeraPositionGraph` nodes --- resolving a reference to one is fundamentally a spatial-adjacency question, which is what Identify/Grounding exist to answer. Knowledge isn't that: its current wiring (`publishedEvents.ts:178-179`, trusted-UI `LookComponent` only) carries an optional `directResponse` straight to the session, with no graph position and no reachability check --- it reads as "a question about world-state," not "a thing you can look at." That maps onto `WorldQuestion`, an already-real intent-classification category (`discriminateIntent/baseClasses.ts:2`) distinct from `Command`, whose actual (LLM-backed) handling is explicitly "indefinitely deferred --- no second question type exists yet to design against; do not start speculatively" ([`AGENT.classifyPlanGeneralization.planning.md`](AGENT.classifyPlanGeneralization.planning.md):19). So Knowledge referents are not a gap in this iteration's `Thing`/catalog/Grounding scope --- they're out of scope on purpose, left for whenever `WorldQuestion`'s deferred backlog item is picked up.
  - [X] **Cross-reference/blocking dependency, named 2026-07-24:** `Thing` reaching read-only `describe` instructions (this plan's own scope) is safe --- Grounding already accepts Character candidates with no changes (point 3 of the research above), and a `describe` step never touches mutation machinery. But `Thing` reaching *relational* mutation steps (`EstablishRelationStep`/`DissolveRelationStep`, currently `EphemeraObjectId`-only per BD-36's explicitly-deferred widening) is a different, harder problem: entity-kind plausibility for a relation ("Maria on the table" vs. "Maria on the tray") is semantic, not deterministic, and `evaluateRelationalLegality.ts` has no entity-kind check today. See [`AGENT.manipulationFrameAndRelational.planning.md`](AGENT.manipulationFrameAndRelational.planning.md)'s BD-10 section (new note added 2026-07-24) --- that defer-to-LLM leg must exist before Character (or Feature) can reach relational steps as a usable command, not just a type-checkable one. This plan's own scope (read-only `describe`) is unaffected; flagging so the `Thing` widening here isn't mistaken for clearing that path too.
    - **Confirmed, not just asserted (2026-07-24):** implementing the type widening above verified this directly --- `groundReferent.ts` required zero code changes beyond the rename, and `EstablishRelationStep`/`DissolveRelationStep` were left untouched. The boundary held exactly as predicted; the two seams that needed active narrowing (`SpanResolutionOutcome`, `IdentityPlanIdentity`) turned out to be Plan-facing mutation shapes, not Grounding shapes, confirming the doc's own boundary line rather than contradicting it.
  - [X] **Type widening (this slice, 2026-07-24):** `spanResolution.ts`'s `ObjectSpanCandidate.id` now accepts `EphemeraThingId` (see the `Thing` union bullet above for detail). This closes concrete gap (2) below for Identify's own candidate representation --- the mutation-plan-facing shapes downstream of it (`SpanResolutionOutcome`, `IdentityPlanIdentity`) deliberately were not widened, narrowed instead via assert-and-throw at the seam.
  - [ ] **Catalog population (follow-up, NOT this slice):** `RoomInPlayObjectCatalogEntry` (`roomObjectCatalogForCharacter.ts`) is still `EphemeraObjectId`-typed only and `getRoomObjectCatalogForCharacter` still scans only `positionGraph.objectIds`, never `characterIds` --- confirmed `EphemeraPositionGraphNode`'s `tag` union is `'Character' | 'Object'` only (no `'Feature'` node kind exists in the graph today, so Feature population isn't buildable regardless). Building (or extending) a character-inclusive referent catalog for the look family --- reusing Grounding rather than a bespoke resolver, mirroring the shape `mergeObjectManipulationCatalogs` already has for room-vs-held --- is deferred to a later slice. Resolve only as far as that slice's concrete referent needs require --- do not build a fully general cross-family `Identify` abstraction speculatively. **Node-model guidance for the character-inclusive scan (durable, 2026-07-24):** the graph's `objectIds`/`characterIds` split is accessor-level over one already-heterogeneous `_nodes` array --- when this scan needs kind-indifferent presence, add an **additive** union getter (`nodeIds`/`memberIds`, *not* `thingIds`), don't collapse the typed surface. Full rationale + the Feature/Character asymmetry (characters need type widening, Features need a structural new tag and are deliberately never relation-subjects nor membership-moved) now lives in [`positions/positionGraph/AGENT.md`](../../../../../lambda/ephemera/dataSource/positions/positionGraph/AGENT.md) ("Node model" + "Known limitation (deferred)").
  - [X] Close concrete gap (2): `EphemeraObjectId`/`EphemeraCharacterId`/`EphemeraFeatureId` are distinct branded types throughout, so a referent that could resolve to any needs a widened id union --- `EphemeraThingId` (above) is that union, applied at Grounding and Identify's candidate typing. Concrete gap (1) (character-inclusive catalog scanning) is the "Catalog population" bullet above --- still open, not closed by this slice.
  - **`actions/AGENT.concepts.md` deliberately not touched this slice (2026-07-24):** nothing here graduates a Target mental model to Shipped or introduces new durable vocabulary --- "Grounding already handles Object/Character candidates structurally" is already stated there (line 31); naming that union `Thing`/`EphemeraThingId` is plan-scoped implementation detail per `taskPlanning/AGENT.md`'s litmus tests, not durable vocabulary, until the catalog-population follow-up actually ships and Identify itself becomes cross-family in practice (not just in type signature).
- [X] **Phase 3. Build the perception kernel.**
  - [X] New module under `positions/manipulation/kernel/` (kept alongside `commitStepSequence.ts`, not a sibling directory --- same `KernelStep` import, so kept where the other filter (`isKernelMutationStep`) already lives). [`kernel/perceiveStepSequence.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/perceiveStepSequence.ts) filters a shared `KernelStep[]` for `describe` steps (new `isDescribeStep` guard, `kernelStep.ts`, mirrors `isKernelMutationStep`) and renders each against already-committed graph state. No worklist/fixpoint, no `transactWrite` --- the module's own doc comment states this explicitly ("NOT a second `commitStepSequence`").
  - [X] Wire the sequential orchestration at the Execute dispatch boundary: [`kernel/executeStepSequence.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/executeStepSequence.ts) `await`s `commitStepSequence` (mutation kernel, filtered via `isKernelMutationStep`) to completion first, then calls `perceiveStepSequence` (filtered via `isDescribeStep`) against the same shared list second --- and only if the commit succeeded (`ok: true`); a failed commit skips perception entirely, since there is no committed state to describe when the mutation half aborted. Ordering is enforced by the `await`, verified by a call-order test (`executeStepSequence.test.ts`), not asserted only in a doc comment. **No live production caller yet** --- Phase 4 (Plan-stage dispatch for object-directed look) is what gives this a real command route; this slice's tests exercise both entry points against synthetic `KernelStep[]` fixtures only.
  - [X] **Resolved (2026-07-24): reuses the existing `Look Command Requested` pipeline verbatim, does not build a new delivery path (PK-4).** `perceiveStepSequence` publishes the same `Look Command Requested`-shaped event (`LookCommandRequestedPublishedPayload`, `actions/publishedEvents.ts`) that `routeTrustedUiAction.ts` / bare `look`/`l` parse already publish, via a new `ActionsPublishedPayload`-typed `streamEvent` dependency (kept separate from the mutation kernel's `PositionsPublishedPayload`-typed one --- see `executeStepSequence.ts`'s doc comment on why they aren't merged into one field). `renderOrchestration/handleLookCommandRequestedForRenderOrchestration.ts` originally needed **zero** changes for Room vs. Feature/Knowledge dispatch; a fourth branch (Object) was added 2026-07-25, see PK-6. `Character` `describe` steps still throw a clearly-named error rather than silently no-op --- out of scope, not asked for.
  - [X] **PK-6 stub shipped (2026-07-25):** Object `describe` steps now get real end-to-end delivery, shortName only. Re-verified through conversation that `StandardObjectData` genuinely has no `render`/description field or authoring surface (confirmed at every layer: WML parser content-model, standardize class, embedding pipeline) --- but the *pipeline* (PerceptionThreads registration, `Look Command Requested` correlation, Generating/Terminal delivery) doesn't need that authoring surface to exist; it only needs *some* resolvable text, and `shortName` already is one. Shipped: `EphemeraCacheComponentId`/`RenderComponentId` widened to include `EphemeraObjectId` (`renderCache/baseClasses.ts`, `messageBus/baseClasses.ts`, and their `mtw-gateways` twin, `packages/mtw-gateways/ts/ephemera/renderCache/types.ts` --- a duplicated type across the package boundary); `requestIntake.ts`'s cache-only host gate widened (renamed `isFeatureOrKnowledgeHost` → `isCacheOnlyHost`); new `renderCache/ensureObjectShortNameCacheRecord.ts` (resolves the object's current merged `shortName` via a helper extracted from `roomObjectCatalogForCharacter.ts` into `objects/objectShortName.ts`, writes exactly one cache row, drops into `orchestrateRenderRequest`'s existing `ensureAuthoredCatalog` injection seam --- no blueprint-versioning/single-flight/gateway-pull machinery reused, since none of that applies to live object state); new `renderOrchestration/prepareObjectRenderForCharacter.ts` (perspective resolved from the *acting character's current room*, not object asset linkage --- Object has no import-vertical participation the way Feature/Knowledge do); new `objectDescription` threadKind (`internalCache/perceptionThreads.ts`, mirrors `featureDescription` at every branch point) and `PerceptionThreadRegisterObjectDescriptionCommand` (`perception/localApiEvents.ts`); new `perception/objectRenderWmlFromCacheRecord.ts` and three new `handleObject*` fan-in functions in `perception/orchestrate.ts` (mirror the Feature/Knowledge trio, single-viewer, no directResponse/SESSION# targeting). **Real, structural finding along the way:** `<Object>`'s WML content model requires exactly one *non-empty* `ShortName` child, always (unlike Feature/Knowledge's optional `Render` facet) --- `objectRenderWmlFromCacheRecord.ts` falls back to the same word-joiner (U+2060) placeholder trick `orchestrate.ts` already uses for Feature/Knowledge's empty-title constraint, so a not-yet-resolved shortName still round-trips as valid WML rather than throwing on re-parse. `LookCommandRequestedPublishedPayload.componentId` (`actions/publishedEvents.ts`) and `PerceptionMessageMetaData`/`ComponentUUID` (`packages/mtw-interfaces/ts/messages.ts`, new `PerceptionObjectMetaData` variant) both widened too --- confirmed via `tsc --noEmit` across all three touched packages (`lambda/ephemera`, `mtw-gateways`, `mtw-interfaces`), all clean, full test suites green (2536/2536 lambda tests, 584/584 mtw-interfaces, 221/221 mtw-gateways). **Still explicitly deferred, unchanged from before:** real `<Render>`/`<Example>` WML authoring support for Object (a genuinely separate, larger cross-package effort --- the WML parser's Object content-model gate, `StandardObject`'s render-consumer, asset merge, and catalog/`AuthoredExampleSet` hydration are all untouched); Character `describe` steps (still throw). `mtw-wml` itself was **not** touched at all --- confirmed unnecessary once the stub stopped trying to mirror Feature/Knowledge's `.render`-based construction and used the field Object already has.
- [X] **Phase 4. Plan-stage dispatch for object-directed look.**
  - [X] Add a command-plan entry for object-directed look (e.g. `look <object>`/`examine <object>`), following iteration 7's layer-4 pattern (`matchRelationalTemplate.ts` is the concrete precedent) --- deterministic template or skeleton matcher, producing a grounded `describe` instruction via Phase 1/2's machinery.
    - **Shipped 2026-07-25.** New layer-4 matcher [`plan/matchLookTemplate.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/plan/matchLookTemplate.ts), mirroring `matchRelationalTemplate.ts`'s shape but simpler: matches a 2-token skeleton (`TEXT(verb) OBJECTSPAN`, verb in `{look, l, examine, x}`) and produces a bare ungrounded `Referent` (`objectSpanRef`), not a `Change` --- a describe referent is singular with no relation to another referent, so it skips `Change`/`Assertion`/the general Synthesize executor entirely. Wired into [`plan/classifySkeletonFamily.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/plan/classifySkeletonFamily.ts) as a new `'look'` family, and into `parseCommand.ts`'s dispatch chain alongside the `relational`/`membership` branches. New compile function [`compileDescribeFromSkeleton.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/compileDescribeFromSkeleton.ts): Plan match -> Identify (`runIdentityStageOverSkeleton`, reused verbatim) -> Grounding (`groundReferent`, the *singular* function Phase 2 already built, not `groundChange`) -> filter to `EphemeraObjectId` candidates only (Character/Feature aren't produced by today's catalog scan per Phase 2's deferred "Catalog population" bullet, but filtered defensively rather than assumed) -> returns `ParseCommandLookComponentResult`. Finished PK-2's implementation gap in passing (see PK-2 row below): `baseClasses.ts`'s `componentId` union and `isParseCommandLookComponentResult` guard were never actually widened to `EphemeraObjectId` despite being recorded as "decided" 2026-07-24 --- fixed as part of this slice.
    - **Delivery wiring, and a real architectural correction made mid-slice:** the original plan for this row assumed `dataSource/actions/index.ts` would publish a new cross-data-source event (`Object Look Requested`) consumed by a new `positions/index.ts` handler that calls `executeStepSequence`, mirroring `Object Take Hold`'s bus-hop shape. Investigation while implementing found this doesn't work: `EphemeraDataSource`'s `streamEvent` always stamps its own `dataSourceKey` (`abstract.ts`/`mtw-lambda-patterns/dataSource/index.ts:404`), and `renderOrchestration`'s subscription to `Look Command Requested` is keyed specifically to `dataSourceKey: 'mtw.ephemera.actions'` --- a `positions/index.ts` handler's `streamEvent` could only ever stamp `mtw.ephemera.positions`, so `perceiveStepSequence`'s publish would silently never reach `renderOrchestration`. Corrected design (shipped): `dataSource/actions/index.ts`'s `LookComponent` branch calls `executeStepSequence` **in-process**, directly, when `componentId` is an `EphemeraObjectId` --- no new bus event, no `positions/index.ts` change. The `commit` leg's deps are structural only (a describe-only step list has zero mutation steps, so `commitStepSequence`'s `steps.length === 0` fast path returns without ever touching `messageBus`/`streamEvent`/`getCurrentHost`); the `perceive` leg reuses `actions/index.ts`'s own already-`mtw.ephemera.actions`-bound `streamEvent` directly, which is exactly what `perceiveStepSequence` needs. Room/Feature/Knowledge `LookComponent` results are unchanged (still the direct `Look Command Requested` publish).
  - [X] End-to-end test: a "look `<object>`" command produces zero mutation instructions, one `describe` instruction, and a rendered description delivered to the acting character, with no interaction with the positionGraph kernel's commit path beyond reading its (unchanged) graph state.
    - **Shipped 2026-07-25**, `dataSource/actions/parseCommand.test.ts` ("returns LookComponent for object-directed look via the native skeleton pipeline") and `dataSource/actions/index.test.ts` ("routes an object component through the perception kernel, publishing Look Command Requested") --- the latter confirms `executeStepSequence`'s commit leg is a no-op (no mutation steps constructed) and `perceiveStepSequence` still fires, publishing `Look Command Requested` (hardcoded `confidence: 1`, per Phase 3's existing `perceiveStepSequence` behavior, not `parseResult.confidence`).

## Open decisions (implementation --- plan only)

| ID | Decision | Blocks | Status |
| --- | --- | --- | --- |
| PK-1 | Exact shape/location of the new `describe` instruction kind (widen `KernelStep` vs. sibling type) | Phase 1 | Resolved 2026-07-24: widened `KernelStep`/`ExecutorParsePlanStep` directly (see Phase 1 note) |
| PK-2 | Whether `ParseCommandLookComponentResult` widens, or object/character-directed look gets its own result type | Phase 1, Phase 4 | Decided 2026-07-24 (widen `componentId` to include `EphemeraObjectId`; character inclusion deferred to PK-3), but the decision was never actually implemented in `baseClasses.ts` until Phase 4 --- fixed 2026-07-25 (`componentId` union and `isParseCommandLookComponentResult` guard both now accept `EphemeraObjectId`). Fully resolved. |
| PK-3 | How far Phase 2's referent-catalog work goes --- object-only, or object+character | Phase 2 | Resolved 2026-07-24: this slice widens `Thing` typing through Grounding (`groundReferent.ts`) and Identify's candidate types (`spanResolution.ts`) only; catalog population (scanning `positionGraph.characterIds`, character short names, `RoomInPlayObjectCatalogEntry` widening) deferred to a follow-up slice, tracked as Phase 2's "Catalog population" sub-bullet |
| PK-4 | Perception kernel's output delivery path --- reuse `LookRoom`'s existing lane-ordering mechanism, or a new one | Phase 3 | Resolved 2026-07-24: reuses the existing `Look Command Requested` event/consumer chain verbatim for room/feature/knowledge (see Phase 3 note); no new delivery mechanism built |
| PK-5 | Whether the `LookRoom`-paraphrase LLM-fallback gap (iteration 7 sub-iteration 2's deliberately-unclosed regression) folds into this iteration's scope or stays a separate, later concern | None yet --- named so it isn't silently assumed either way | Open |
| PK-6 | Object render-content model does not exist (`StandardObjectData` has no `render`/description field; `RenderComponentId`/`EphemeraCacheComponentId` were Room/Feature/Knowledge-only) --- named 2026-07-24 during Phase 3 | Phase 4 (Object half unblocked) | **Partially resolved 2026-07-25: shortName-only stub shipped**, real end-to-end Object description delivery now works (see Phase 3's PK-6 note for the full file list). Explicitly **not** the same as building `<Render>`/`<Example>` authoring for Object --- that remains open, unscoped, and is what the rest of this row still tracks: real authored/generated Object description content is a separate, larger cross-package effort (`mtw-wml` parser content-model + `StandardObject` render-consumer + asset merge + catalog/`AuthoredExampleSet` hydration), likely its own task-planning doc. Character descriptions remain fully out of scope (`perceiveStepSequence` still throws for `referentKind: 'character'`) --- not part of this row, never was. |
| PK-7 | `EphemeraDataSource.streamEvent` always stamps its own `dataSourceKey` (confirmed 2026-07-25 while implementing Phase 4) --- a describe-step publish can only ever carry the `dataSourceKey` of whichever data source's `streamEvent` closure calls it. `renderOrchestration`'s `Look Command Requested` subscription is keyed to `mtw.ephemera.actions` specifically, so `executeStepSequence`/`perceiveStepSequence` can only be called from code running as (or dispatched from) the *actions* data source, never from `positions/index.ts`, no matter how the two are wired together | Phase 4 (resolved); durable for any future kernel-invoking work | Resolved 2026-07-25: `dataSource/actions/index.ts` calls `executeStepSequence` in-process rather than bus-hopping through `positions/index.ts` (see Phase 4's delivery-wiring note above). Recorded here as a durable constraint, not just a one-off fix --- any future work that wants to invoke `executeStepSequence`/`perceiveStepSequence` from `positions/`-side code will hit the same wall and needs the same in-process-from-actions shape. |
| PK-8 | Trusted-UI object-click support (`routeTrustedUiAction.ts`'s `look` branch still rejects `EphemeraObjectId` at its own type guard) --- named 2026-07-25, out of scope for this slice (Phase 4's stated scope was text-command dispatch only) | None yet | Open, deliberately deferred --- not silently dropped. `ParseCommandLookComponentResult`'s widened type would already support an Object-typed UI click; only `routeTrustedUiAction.ts`'s own guard needs updating whenever this is picked up. |
| PK-9 | Feature/Character dispatch via the new `matchLookTemplate`/`compileDescribeFromSkeleton` pair --- today only Object candidates are reachable (Phase 2's "Catalog population" follow-up hasn't shipped, so Identify's catalog scan never produces Feature/Character candidates), so `compileDescribeFromSkeleton` filters to `EphemeraObjectId` defensively | Phase 2's "Catalog population" follow-up | Open, deliberately deferred. Once catalog population ships, `compileDescribeFromSkeleton`'s `EphemeraObjectId`-only filter is the one place that needs revisiting to admit Feature (Character still blocked separately by `perceiveStepSequence`'s throw, per PK-6). |

## Verification

```bash
cd lambda/ephemera && npm run test -- --watchAll=false \
  dataSource/positions/manipulation/kernel/ \
  dataSource/actions/enrich/objectManipulation/synthesize/ \
  dataSource/actions/enrich/objectManipulation/plan/ \
  dataSource/actions/enrich/objectManipulation/compileDescribeFromSkeleton.test.ts \
  dataSource/actions/parseCommand.test.ts \
  dataSource/actions/index.test.ts
npm run build
```

## Progress

| Milestone | Status |
| --- | --- |
| Design confirmed through conversation (shared instruction list + per-kernel filter, sequential invocation, lightweight perception kernel) | Done (2026-07-24) |
| Phase 1 (instruction vocabulary widening) | Done (2026-07-24) |
| Phase 2 (CPG-5 resolved for this case) | In progress (2026-07-24) --- `Thing` union + Grounding/Identify candidate-typing widened; catalog population (Object+Character room scan) still open, deferred to a follow-up slice |
| Phase 3 (perception kernel + sequential orchestration) | Done (2026-07-24) --- `perceiveStepSequence.ts` + `executeStepSequence.ts` built and tested (`dataSource/positions/manipulation/kernel/`); reuses the existing `Look Command Requested` pipeline for room/feature/knowledge/**object** (object stub shipped 2026-07-25, see PK-6); character still deliberately deferred |
| Phase 4 (Plan-stage dispatch for object-directed look) | **Done (2026-07-25).** New `matchLookTemplate.ts`/`compileDescribeFromSkeleton.ts` Plan-stage pair, wired through `classifySkeletonFamily.ts`/`parseCommand.ts`; `dataSource/actions/index.ts` calls `executeStepSequence` in-process for `EphemeraObjectId` `LookComponent` results --- `executeStepSequence`/`perceiveStepSequence` now have a real production caller (PK-7's dataSourceKey-binding constraint, discovered mid-slice, is why the call is in-process rather than a new bus hop through `positions/index.ts`). PK-2's implementation gap fixed in passing. Real `<Render>`/`<Example>` authored content for Object remains open, unscoped, tracked in PK-6's remaining row; trusted-UI object clicks (PK-8) and Feature/Character dispatch via this matcher (PK-9) remain open, deliberately deferred. |
