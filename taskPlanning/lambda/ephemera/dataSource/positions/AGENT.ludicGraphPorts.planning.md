# `ludicGraph` ports: implementing the boundary mechanism

**Status: LP0 and LP1 landed. LP2 landed 2026-08-17 (structured terminal primitives + discriminator). LP3 landed 2026-08-17 (matchers fixed, PQ-10). LP4 landed 2026-08-17 (edge terminals widened to every legal host kind). LPM landed 2026-08-19 --- the clearing tool built, the reset executed and verified manually (including legacy `positionGraph` rows). LP4a landed 2026-08-19 --- `rootId` shipped, `CarryClosureFragment` collapsed into `EphemeraLudicGraph`. LP4h landed 2026-08-19 --- `applyTransferSet`'s transfer set widened to `Object | Character`, the caller-side split collapsed. LP4b landed 2026-08-19 --- `EphemeraLudicGraphNode` widened to the full terminal-kind set (Feature, Area), closing LP4's referential-integrity gap. LP4g landed 2026-08-19 --- the mutation kernel's relational step terminals widened to `EphemeraLudicTerminalPrimitive`, retiring the five throw-on-non-Object narrows LP4 left behind. Stage 2 waits on P7.** Next step is [LP4c](#recommended-order) --- adding the containment kinds --- still gated on [LD-11](#open-decisions-implementation--plan-only), which remains undecided **and was re-scoped 2026-08-19 to wait on an upstream question**: whether premise 9's *root to part* restriction on `In`/`PartOf` stands at all. Dropping it would let a containment edge travel as an **internal** transfer edge (which `applyTransferSet` already moves intact, with no classifier involvement) as well as a boundary one --- the case LD-11's original framing assumed impossible. That verdict is owed upstream and now has a row there --- **[AB-53](AGENT.abstractionLayers.planning.md#settled-register), opened 2026-08-19**, with the re-open trigger stated on [AB-4](AGENT.abstractionLayers.planning.md#settled-register)'s verdict (AB-48 is untouched). **LP4c was therefore split 2026-08-19: [LP4c-i](#recommended-order) (representation --- widen the union across the persistence lane, leave the ingress lane deliberately narrow) is startable now with no LD-11 dependency, and alone clears C14/Phase 5(b)'s gate; [LP4c-ii](#recommended-order) (behaviour under transfer) keeps the gate.** **LP4c-i landed 2026-08-19** --- `HostRelationalEdgeKind` admits `In`/`PartOf`; the four persistence-lane sites (`ephemeraMeta.ts`'s type and `HOST_RELATIONAL_EDGE_KINDS`, `ludicGraph/baseClasses.ts`, `positions/publishedEvents.ts`) widened, the four ingress-lane sites (`relationKind.ts`, `actions/baseClasses.ts` x2, `actions/publishedEvents.ts`) left narrow and commented per LD-13; a ninth site missed by grep (`parsePlanStep.ts`'s import-based reference) was found by `tsc` and narrowed to match, with two producer-side drop guards added. `classifyInteractionUnderTransfer` throws naming LD-11 for both new kinds rather than deciding them. **Direction changed 2026-08-19, upstream and from conversation, and LP4c-ii's scope changed with it.** [AB-53](AGENT.abstractionLayers.planning.md#settled-register) resolved --- containment **nesting** comes from **nesting graphs**, and ***root to part* is kept as an iteration-1 constructor discipline**, deliberately not re-locked --- and [AB-54](AGENT.abstractionLayers.planning.md#settled-register) decided that **hosting and containment are one mechanism**: `On`, `In` and `PartOf` are **hosting kinds** that put the subordinate node in the superior's own shard, while `Under`, `Against` and `Custom` are **peer kinds**. **[LD-11](#open-decisions-implementation--plan-only) is therefore largely dissolved rather than answered** --- the classifier never receives a containment kind, LP4c-i's placeholder `throw` becomes a **permanent invariant assertion** rather than a placeholder, and `carry` leaves `InteractionUnderTransferOutcome` entirely; what survives is only the `Against` reconciliation. **None of it is startable as code, and that is deliberate.** The change removes hosted objects from the flat catalogs the referent path reads, so **[LD-15](#open-decisions-implementation--plan-only) holds the `ludicGraph` constructor flat until `ludicCache` serves referent search ([CC3](AGENT.abstractionLayers.planning.md#recommended-order))**. **LP4c-ii must not be picked up as written.** **Next work, set 2026-08-20: [LP4i](#recommended-order), then [LP4d](#recommended-order)** --- the staleness sweep and self-heal first (taking clause 3's **missing root node** as its proving case), so LP4d becomes the first shape change that *exercises* that discipline instead of being hand-migrated. **The framing is deliberate: the implementation has outpaced its resiliency systems.** Both sit on the safe side of LD-15's gate; the cache side ([CC1a](AGENT.abstractionLayers.planning.md#recommended-order)/CC3) stays available in parallel. **AB-53 and AB-54 graduated 2026-08-20** to the [Settled register](AGENT.abstractionLayers.planning.md#settled-register), the `Against` reconciliation splitting out first as **[AB-56](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only)**; **[LD-7](#open-decisions-implementation--plan-only) closed as already-satisfied** and **[LD-16](#open-decisions-implementation--plan-only) closed**. **Open and unblocked:** [AB-55](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) and [LD-17](#open-decisions-implementation--plan-only), which want the same write-path enumeration and should be run once for both. **LP4i landed 2026-08-20** --- the structural-staleness sweep and self-heal, proven on clause 3's root-in-nodes gap (construction paths fixed, the payload guard now asserts it, a new diagnostics sweep and idempotent `ephemera`-side repair ship the detect-and-heal discipline as durable infrastructure, not a one-off). **LP4d landed 2026-08-20** --- `ports` (premise 12's egress list) shipped as a required, possibly-empty field on `EphemeraLudicGraphData`/`EphemeraLudicGraphFieldPayload`, threaded through `EphemeraLudicGraph`'s full construction/serialization surface, with `healLudicGraphStructure` extended to default a missing/malformed `ports` to `[]` --- exactly the first shape change LP4i's own framing predicted would *exercise* the discipline rather than being hand-migrated, in place of a second manual [LPM](#recommended-order)-style wipe. Its two named blockers dissolved as scoped rather than being decided (LD-7 already-satisfied; the non-empty-for-anything-hosted invariant deliberately left unenforced), and it opened nothing new --- AB-55/LD-17 (who writes a populated port, and whether the presence port subsumes adjacency) stay exactly as open as LP4d found them. **Next is [LP4f](#recommended-order)** --- the adjacency-row existential-invariant fix --- **then [LP5](#recommended-order)**. **[LP4f] was re-scoped 2026-08-20, from conversation, before any of it was built: its title was right and every one of its bullets was quantified over the wrong structure.** It stated the invariant over containment **edges**; the adjacency row is the reverse index of **node presence** in a host's `ludicGraph`, and edges of every kind are orthogonal to it (`AGENT.contract.md` has said *"must not write adjacency rows for relational edges"* in normative text throughout). **The `commitStepSequence.ts:154` defect it was built around is not real** --- adjacency items are filtered to `transferMembership` steps, so a dissolve never reaches that line --- and the invariant, correctly stated over nodes, **already holds**, verified by auditing every non-test `_nodes` mutator. **What survives is a documentation slice plus one forward-looking note:** write the *iff* down as a contract clause (it is implemented but nowhere stated as an iff), and record that when [AB-54](AGENT.abstractionLayers.planning.md#settled-register)'s hosting-as-shard lands, an edge dissolve will imply a membership change that **must** be routed as a `transferMembership` step --- not as adjacency learning to inspect edges. **The failure mode is new to this plan and worth naming: a step generated from a verdict's closing pointer ([LD-5](#open-decisions-implementation--plan-only)'s *"what the decision does require is LP4f"*) rather than from the structure the verdict was about**, which then invented a downstream code defect to justify itself. **[LP4j](#recommended-order) opened 2026-08-20, from the same conversation, and runs immediately after LP4f:** this design carries **two terms of art for one mechanism** --- *containment kind* (`In`/`PartOf`, fixed by [AB-48](AGENT.abstractionLayers.planning.md#settled-register) on 2026-08-09) and *hosting kind* (`On`/`In`/`PartOf`, [AB-54](AGENT.abstractionLayers.planning.md#settled-register) on 2026-08-19) --- **and AB-54's own verdict already merged them** (*"Hosting and containment are one mechanism"*), so the older term simply kept its narrower extension in the documents that state the merge. **The risk is codifying the drift**, and it is already partly codified: `ephemeraMeta.ts`'s canonical union annotates itself with the superseded split, and `interactionUnderTransfer.ts` throws a message that only parses if the two terms differ, on a justification that applies equally to the `On` case four lines above it. **LP4j retires *containment kind* as a named set, records the consequent obligation on [LD-12](#open-decisions-implementation--plan-only) (whose insulating `'On' | 'Under'` parameter is a *third* grouping, cutting across AB-54's partition), and then sweeps the type contracts --- carefully, because the LD-13 ingress narrows are *not* drift and merging the term carelessly would turn their doc comments into live bugs.** It ships **no behaviour change**. **LP4f landed 2026-08-20** --- exactly as the re-scope shrank it: a single falsifiable clause on [`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) stating the *iff* over node presence (immediately after the character-route *"`ludicGraph` wins"* line, cross-referencing rather than duplicating the existing *"must not write adjacency rows for relational edges"* rule) plus the AB-54 forward note, both carried over verbatim in substance. No code touched, no behaviour change. **LP4j landed 2026-08-20** --- retired *containment kind* as a named subset: `AGENT.concepts.md`'s hosting kind / peer kind row now states it is *the* partition with no second, narrower one; LD-12 got a future-obligation note (the `On`/`Under` cycle-detection parameter is a third grouping, to decide when LD-12 is scoped, not here); and the three confirmed sweep hits (`ephemeraMeta.ts`'s union comments, `interactionUnderTransfer.ts`'s throw message, and the four ingress-lane doc comments across `relationKind.ts`/`publishedEvents.ts`/`baseClasses.ts`) were reworded to match. Comment/doc only --- the one `Error` message changed wording, not trigger condition; no ingress-lane narrow widened. **LP5 landed 2026-08-20, from conversation, documentation only** --- premise 7 is answered **by rejecting the question's setup rather than by taking either branch**: a cable running through a box is `OBJECT#Cable` **multi-hosted** into `OBJECT#Box`'s `ludicGraph`, not two exterior edges terminating at box ports, so **nothing crosses the box's boundary and there is no interior link to be bare or mediated.** Neither of LC7's two bullets survives, which is stronger than the dissolution that case predicted for itself. The mediator question **relocates to the cable's own boundary** and lands on [C7](AGENT.abstractionLayers.corpus.planning.md#c7-ariadnes-thread)'s already-settled H1 clause 3 (*spanning things are modelled as parts*, on the bystander test). Verdict recorded upstream at [LC7](AGENT.abstractionLayers.ludicCache.corpus.planning.md#lc7-the-cable-that-passes-straight-through-the-box) per LP5's own instruction, with LC8 and gap 6 updated in lockstep; **LD-3 removed**; a rendering worry parked upstream with the split **connectivity free / presence open** (C7's three live options, AB-6); and **[LP7](#recommended-order)'s *conditional on LP5* clause retired as wrong** --- LP7 does not shrink, since all four of its bullets concern port-*qualified* terminals generally rather than the both-ends-are-ports case. **A second instance of [LD-5](#open-decisions-implementation--plan-only)'s failure mode was recorded on that row:** LP5, like LP4f, was generated from the *framing* of the document that raised it rather than from the structure the system uses. **No unblocked step remains --- LP6/LP7 are Stage 2, gated on P7, and the live thinking is [LC8](AGENT.abstractionLayers.ludicCache.corpus.planning.md#lc8-the-spring-in-the-box-and-in-the-contraption)'s upstream.**

**This is an implementation plan, deliberately, and the choice is load-bearing.** Ports were designed inside [`AGENT.abstractionLayers.planning.md`](AGENT.abstractionLayers.planning.md), which follows the **design-stage variant** ([`AGENT.designVariant.md`](../../../../AGENT.designVariant.md)). This plan follows the **base framework** ([`taskPlanning/AGENT.md`](../../../../AGENT.md)) instead, because all four of the variant's overrides are wrong for this work: resolved rows here are *finished*, not graduated; its open decisions **do block slices**; it ships into a published package rather than producing tier-licensed evidence; and Progress wants a running log of what landed. [`AGENT.md`'s step 0](../../../../AGENT.md) warns that deciding this late is expensive, so it is recorded here rather than assumed.

**What this plan does not own.** Every design verdict about ports stays in the abstraction-layers plan as a PQ row and graduates there. **This plan cites those rows as premises and never restates their content** --- the same discipline the `ludicCache` corpus file uses for C-series cases, adopted here to keep two files from drifting. If a premise below looks wrong, the fix is upstream, not here.

**And the trap this plan is most likely to fall into, named up front.** [CC1b](AGENT.abstractionLayers.planning.md#recommended-order) warns that *an implementation that runs smoothly under opaque ids has produced a convenience, not a verdict*. **Building [PQ-4](AGENT.abstractionLayers.proposals.planning.md#open-questions-ab-34-sub-questions-ids-stable-never-reused)'s provisional keying does not settle PQ-4.** Nothing in this plan may be cited upstream as evidence that a provisional choice was correct; smooth implementation is the expected outcome of *either* branch. The rollback line recorded on PQ-4 stays live for the duration.

## Getting Started

1. **Read the framework once:** [`taskPlanning/AGENT.md`](../../../../AGENT.md) for durability and content split.
2. **Command authority is [`lambda/ephemera/AGENT.testing.md`](../../../../../lambda/ephemera/AGENT.testing.md).** If any command here conflicts with it, follow that file.
3. **Runner and context.** `lambda/ephemera` is Jest-based and uses **`npm run test`**, not `npm test`. Run from the package root:
   ```bash
   cd lambda/ephemera && npm run test -- --watchAll=false dataSource/positions/
   ```
4. **Baseline before editing** --- this should pass unchanged:
   ```bash
   cd lambda/ephemera && npm run test -- --watchAll=false dataSource/positions/ludicGraph/
   cd packages/mtw-interfaces && npm run test -- --watchAll=false ts/ephemeraMeta.test.ts
   ```
5. **`npx tsc --noEmit` is not sufficient**, and this is not generic caution: `lambda/ephemera`'s `*.integration.test.ts` files sit outside `tsconfig`, so a rename or signature change can typecheck clean and still break the suite. Run the real suite.
6. **A green suite is not sufficient either, and this one cost a day to find.** Discovered 2026-08-20: [LP4a](#recommended-order) made `rootId` **required**, which broke two suites outside `tsc`'s program --- `dataSource/objects/collectObjectIdsFromRoomLudicGraphs.test.ts` and `dataSource/objects/persistImprovisationObject.test.ts`, whose `ludicGraph` fixtures omit it. **Both reported PASS on three consecutive full-suite runs across two days** (`344 passed`), because ts-jest caches type-check results per unchanged file and neither file had changed. They surfaced only when the cache happened to evict, and **failed identically on a clean `HEAD` checkout**, confirming they had been broken since LP4a landed and were never caused by the slice that revealed them. **Practical rule: after any change that makes a shared field required, run `npm run test -- --clearCache` once before believing a green suite** --- the fixture files that break are exactly the ones you did not touch, which is exactly what the cache keys on. Fixed 2026-08-20 (three `rootId` additions across the two files).
6. **Read the code this changes before changing it:** [`ludicGraph/AGENT.md`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/AGENT.md), then [`ludicGraph/index.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/index.ts) and [`ludicGraph/baseClasses.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/baseClasses.ts).

## Why this plan exists

**Ports are locked in design and absent from code, and no step anywhere owned building them.** Their shape is [H3 clause 1](AGENT.abstractionLayers.proposals.planning.md#h3-ports-as-the-boundary-mechanism-decomposition-as-a-modellers-choice), locked on 2026-08-06. But a survey of the abstraction-layers Recommended order on 2026-08-15 found **no step that implements them**: Channel C is CC0--CC10 and every step is about the cache, and Phases 0--7 are the design track. [PQ-10](AGENT.abstractionLayers.proposals.planning.md#open-questions-ab-34-sub-questions-ids-stable-never-reused) owns a bounded matcher fix and nothing owns the rest.

**That is a gap rather than a deferral, and the distinction matters:** a deferred item has a gate someone can re-open, an unassigned one is invisible until something trips over it. **CC1 is what tripped over it.**

**What is blocked.** [CC1](AGENT.abstractionLayers.planning.md#recommended-order) --- the `ludicCache` rebuild function --- cannot be written. Its edges terminate at port addresses ([CC1b](AGENT.abstractionLayers.planning.md#recommended-order)), and the reducer's innermost operation is a discrimination between node and port terminals. Neither exists.

**What is not blocked, and must go first.** **P7, the merge-reduce proposal, does not wait on this plan --- this plan waits on parts of P7.** Articulating the reducer is what *generates* requirements on ports, and two of them were generated on 2026-08-15 and are recorded nowhere else (see [Premises](#premises-and-where-each-is-recorded), items 6 and 7). **Building the port record before P7 lands would build it blind to its only consumer** --- the failure CC0 was created to prevent, one layer down. Hence the stage split below. **Rechecked and released 2026-08-23 (user): LP6 is not gated on P7, and the recheck is decidable from this sentence's own text rather than from judgement.** The gate names exactly **two** generated requirements --- premises 6 and 7 --- and claims no others. **Premise 6 is marked *largely satisfied by premise 12*, with only *"that both halves be looked up, not that either be built"* remaining --- and that remainder is a discipline on the reducer, not a shape of the record.** There is no code in LP6 that can get it wrong, so it is an obligation **on P7**, not a blocker on LP6. **Premise 7 is answered** by [LP5](#recommended-order)/LC7, and its *not yet general* remainder was [LP7](#recommended-order)'s retired conditional, not LP6's. **The *"blind to its only consumer"* premise is itself stale twice over.** The port record shipped in [LP4d](#recommended-order) **without P7** --- the empirical version of the point --- and the reducer is no longer its only consumer: [PR-10](AGENT.presence.planning.md#settled-register)'s implicitness predicate and [PR-9](AGENT.presence.planning.md#settled-register)'s totality checker both read arity as *count the `Present` ports*. **Checked against P7's own staged findings, not only against this plan's summary of them:** [all six](AGENT.abstractionLayers.ludicCache.corpus.planning.md#mechanism-findings-staged-for-p7) are about composition, entitlement, dropping and trigger --- **none mentions the port's relational `kind`**, and finding 1's *"discrimination between terminal kinds"* is node-versus-port (PQ-9's discriminator, shipped in [LP2](#recommended-order)), a different distinction that shares a word.

## Premises, and where each is recorded

**Cite, do not restate.** Each row names where the verdict lives and its standing. **A premise marked *provisional* may be built against; it may not be closed by building it.**

| # | Premise | Standing | Recorded at |
| --- | --- | --- | --- |
| 1 | Ports are the boundary mechanism; a whole owns its interior graph, crossing happens only through ports | **Locked** 2026-08-06 | [H3 clause 1](AGENT.abstractionLayers.proposals.planning.md#h3-ports-as-the-boundary-mechanism-decomposition-as-a-modellers-choice) |
| 2 | A plain object carries a presence port; there is no *plain object* category to switch from | **Forced yes by entailment** --- *"the base case locked clause 1 rests on"*. **Flagged 2026-08-15: re-run before relying on it.** The row was argued in a vocabulary with **one** containment relation; premise 9 splits it into two, so *plain* now has to say **empty of which** --- a box with a crystal ball but no lid has contents and no parts. The argument runs on *decomposition*, not on kind, so it probably survives; *probably* is not *verified*. **Partly re-run 2026-08-20, from conversation, and the flag narrows rather than clearing.** What the flag covers is **only the adjective *plain*** --- how one would enumerate the category this premise *denies exists*. **It says nothing about mechanism**, and in particular it does **not** reopen *whether a hosted thing has a presence port*, which this row answers **yes** and which is *"the base case locked clause 1 rests on."* **Recorded because the wider reading caused a false blocker:** [LP4d](#recommended-order) was called blocked on this flag, and it is not --- the flag is definitional housekeeping. **What genuinely remains unanswered is not in this row at all:** whether a hosted thing gets a *materialized* graph for the port to live in, which is **[LD-17](#open-decisions-implementation--plan-only)** and turns on [locked-frame clause 3](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md#wholes-parts-and-ports)'s deliberate silence about minting, not on this premise. **Still owed here:** the one-line statement of *empty of which* now that premise 9 splits containment in two | [PQ-13](AGENT.abstractionLayers.proposals.planning.md#open-questions-ab-34-sub-questions-ids-stable-never-reused) |
| 3 | Port ids are **compact opaque tokens**, not ordinals and not names | **Provisional**, with a named rollback line | [PQ-4](AGENT.abstractionLayers.proposals.planning.md#open-questions-ab-34-sub-questions-ids-stable-never-reused) |
| 4 | The separator is a second `#`, chosen so `isEphemeraTaggedId` **throws** rather than silently affirming | **Decided 2026-08-17, by [LP1](#recommended-order), scoped to the serde/Dynamo string form.** Argued and verified against shipped code 2026-08-13. **Rescoped 2026-08-15:** the terminal is now a **structured** type, so the separator governs only the **serde/Dynamo string form**, which is deferred behind a gate. The argument survives intact at that boundary | [PQ-9](AGENT.abstractionLayers.proposals.planning.md#open-questions-ab-34-sub-questions-ids-stable-never-reused) |
| 5 | Port-qualified endpoints are invisible to the shipped matchers; the fix is not architectural | **Open, bounded, and substantially deflated 2026-08-15.** A structured terminal turns most of this into **compile errors** --- `isEphemeraObjectId(edge.from)` no longer typechecks. **What survives is equality**: `edge.from === id` still compiles and still misses port terminals, which is the original hazard and now the whole of it | [PQ-10](AGENT.abstractionLayers.proposals.planning.md#open-questions-ab-34-sub-questions-ids-stable-never-reused) |
| 6 | **A port must be navigable from both sides.** One that records its egress target but not what attaches from inside makes walks succeed in one direction and **silently return nothing** in the other | ~~Open --- generated by the reducer 2026-08-15, owed to P7~~ **Largely satisfied by premise 12, recognised 2026-08-15:** the egress list supplies the outward half and the interior's own edges supply the inward half, so both directions exist without a new structure. **What remains is only that both halves be *looked up*, not that either be built** | [LC staged finding 5](AGENT.abstractionLayers.ludicCache.corpus.planning.md#mechanism-findings-staged-for-p7) |
| 7 | **Is there ever a pass-through with no nameable mediator?** --- whether a bare port-to-port interior edge must exist at all | ~~**Open.** A `ludicGraph` representation question, answerable **without** P7~~ **Answered for the cable 2026-08-20 by [LP5](#recommended-order), upstream at LC7, and *by rejecting the question's setup rather than choosing a branch*: a spanning object is multi-hosted membership, not a boundary crossing, so no box-port terminals exist to join and the bare form never arises.** Cite LC7, not this row, for the content. **Not yet general** --- LC8 reaches the same shape through a containment diamond and is now the sole remaining route to it; the verdict generalises only if LC8 agrees | [LC7](AGENT.abstractionLayers.ludicCache.corpus.planning.md#lc7-the-cable-that-passes-straight-through-the-box), [LC gap 6](AGENT.abstractionLayers.ludicCache.corpus.planning.md#coverage-gaps-recorded-up-front) |
| 8 | Declared versus derived ports | **Open, and not this plan's** --- explicitly left with PQ-9 | [PQ-9](AGENT.abstractionLayers.proposals.planning.md#open-questions-ab-34-sub-questions-ids-stable-never-reused) |
| 9 | ***In* and *part of* are two non-exclusive containment kinds**, and the level-crossing relation is a new edge kind `PartOf`, root to part, inside the whole's own graph | **Decided 2026-08-09 and never implemented. The *root to part* clause was flagged 2026-08-19 and re-confirmed the same day --- but as an iteration-1 constructor discipline rather than a lock; see the resolution at the end of this cell.** Worked example on the row: `Theseus -Held-> thread:1` and `thread:2 -In-> room`, both true, of different parts, in different graphs. **What is flagged is only the root restriction, not the two-kinds decision** --- `In` and `PartOf` being distinct and non-exclusive is untouched. No type-level or invariant-level reason to force root-to-part was found (checked against `sameHost` specifically, which does not depend on the root), and `ludicCache` is expected to produce containment **nesting** rather than a flat root-to-child fan. **The verdict is owed upstream, and as of 2026-08-19 it has a row there: [AB-53](AGENT.abstractionLayers.planning.md#settled-register).** Precision corrected the same day --- this premise cited *AB-48/AB-4* for the whole claim, but **only [AB-4](AGENT.abstractionLayers.planning.md#settled-register) carries *root to part***, and it now states that as a named re-open trigger; **AB-48 (two non-exclusive containment kinds) is untouched and not in question**; [LD-11](#open-decisions-implementation--plan-only) records what changes in this plan's own code either way, and [LD-12](#open-decisions-implementation--plan-only) records the cycle property the restriction was silently providing. **Resolved upstream 2026-08-19 --- [AB-53](AGENT.abstractionLayers.planning.md#settled-register) closed, and *root to part* holds as **how the constructor builds graphs in iteration 1**, not as a structural lock.** The nesting `ludicCache` needs comes from **nesting graphs**, not from node-to-node edges: a hosted thing is a node in **its host's own shard**, and each shard stays internally root-to-part. **The type does not forbid a node-to-node containment edge and nothing asserts the star topology at runtime** --- if a use case for multiple levels in one graph arrives, the discipline is dropped rather than defended, and [AB-5](AGENT.abstractionLayers.planning.md#settled-register)'s **mint / move / dissolve** already covers the carry behaviour that the restriction currently makes easy. **So this premise is unflagged and may be relied on for building**, but a row reasoning *from* the star topology as a guaranteed property should say so out loud and check AB-53 first --- with one addition it did not previously carry, from [AB-54](AGENT.abstractionLayers.planning.md#settled-register): **`On` is a hosting kind too**, so `PartOf` is no longer the only relation that puts a node in another node's shard. Cite AB-54 for that, not this row | [AB-48](AGENT.abstractionLayers.planning.md#settled-register), [AB-4](AGENT.abstractionLayers.planning.md#settled-register), [AB-53](AGENT.abstractionLayers.planning.md#settled-register), [AB-54](AGENT.abstractionLayers.planning.md#settled-register) |
| 10 | **`rootId` is recorded, never derived** --- it must be an input, not read back off the edges | **Constraint, shipped-code-derived.** `computeCarryClosure`'s BFS absorbs a doubly-reachable object via whichever edge it traversed first, so the traversal *tree* is order-dependent while the induced edge set is not | [`ludicGraph/AGENT.md`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/AGENT.md) |
| 11 | Whether **Room, Area or Feature** belong in the part-of ladder | **Explicitly unsettled, and named as the thing most likely to be read as included.** Adding a node *tag* must not be taken to answer it | [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md) |
| 12 | ~~**The port record is split into *complementary* halves, not duplicated ones, and the interior is authoritative.**~~ **Amended 2026-08-22 --- both clauses of that sentence were too strong, and the second was being read far past what locked it. See the Standing cell for the replacement.** ~~The original:~~ **The port record is split into *complementary* halves, not duplicated ones, and the interior is authoritative.** The **egress list** is a **new top-level element on the interior graph** --- port to outbound host. The **ingress half carries no place in truth**: it was moved off the exterior node the same day it was settled, because *"the room's own edges already carry what a coarse read needs"* and a port-address reference names the host and the port inherently | **Settled 2026-08-06, reversed once the same day, and never implemented.** The back-sweep splits its standing: *which side is authoritative* is **locked** (locked-frame clause 2), *where any ingress denormalization lives* is **provisional** and rests on AB-35. **Amended 2026-08-22, from conversation --- three corrections, and the third is the substantive one.** **(a) *Complementary, not duplicated* is false, and this cell's own justification is what falsifies it.** The ingress half was dropped because *"a port-address reference names the host and the port inherently"* --- which **is** the statement that `fromHostId` is reconstructible from the exterior side, since the referencing edge names the port and the graph holding it names the host. **Worked counter-example:** `OBJECT#ROPE` carries `port abcd123` with `fromHostId: ROOM#A` while `ROOM#B`'s graph holds an edge to `OBJECT#ROPE#abcd123`. Both cannot hold, nothing prevents it, and no shipped code would notice. **(b) The citation of locked-frame clause 2 is an over-reading.** Clause 2 says boundary crossings are mediated by *"an explicit binding **the interior owns**"* --- **ownership of the binding, not authority over every field on it.** What [PQ-1](AGENT.abstractionLayers.proposals.planning.md#the-port-record-split-complementary-with-the-interior-authoritative-settled-2026-08-06) actually locked was **egress list versus ingress list**, two denormalizations of one topology --- **and the ingress list was deleted from truth the same day**, so the contest that sentence settled no longer has two sides. **(c) The replacement, and it is a scoping rather than a reversal: *the interior is authoritative about things within its scope*.** **Interior scope:** the port's existence, its `portId`, its single-use lifecycle, its `kind`, and its interior edge and label --- PQ-1's surviving reason, *"the half whose lifecycle invalidates ports"*, argues for exactly these and nothing wider. Its other reason, footprint completeness, argues for the egress list being **local**, not for it **winning**, and a denormalized copy is equally local. **Exterior scope:** the port-to-host pairing (`fromHostId`) and, under [PR-11](AGENT.presence.planning.md#settled-register) reading (B), the exterior relation label --- both facts *about* the exterior relationship, held interior-side as denormalized copies. **The conflict rule is conditional and is the whole of it: compare where comparison is possible; where no exterior reference exists the stored value stands.** **This is not a witness requirement** --- an uncontested value needs no exterior instance to justify it, and reading it as one wrongly couples this to [PR-C1](AGENT.presence.corpus.planning.md#pr-c1-the-contraption-in-two-rooms-and-whether-covers-nest) residual (ii). **It is the same shape [PR-11](AGENT.presence.planning.md#settled-register) already locked for `kind`** (*checked wherever an edge exists, vacuously true where none does*), so the port record has **one** consistency rule, not one per field. **Owed to [LP6](#recommended-order):** the response to a detected mismatch --- throw, repair, or tolerate --- is **not** settled by this amendment, and note that *believe the interior* would have resolved a partial write **toward the stale value**, which is the argument for reversing the direction in the first place. **Also owed to LP6:** [`ephemeraMeta.ts`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts)'s port comment still reads *"the interior-authoritative half"* and describes `fromHostId` as having *"no interior witness"* --- the second half stays true, the first needs this scoping | [PQ-1](AGENT.abstractionLayers.proposals.planning.md#the-port-record-split-complementary-with-the-interior-authoritative-settled-2026-08-06) |

## What we are changing

**Three code areas, and their blast radii differ enough to sequence by.**

- **[`packages/mtw-interfaces/ts/ephemeraMeta.ts`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts) --- published.** `EphemeraLudicRelationalEdgeData.from`/`to` are `EphemeraObjectId`. **`ROOM#A` does not match `OBJECT#${string}` under any reading**, so a room cannot be an edge terminal today --- and `Room` becoming a legal *node* in CC0b did not make it a legal *terminal*.
- **[`packages/mtw-interfaces/ts/baseClasses.ts`](../../../../../packages/mtw-interfaces/ts/baseClasses.ts) --- published, and touched only if a discriminator lands beside the tagged-id family.** `isEphemeraTaggedId` splits on `#` and **throws** `Illegal nested EphemeraId` above two sections.
- **[`ludicGraph/`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/) --- local.** The matchers, `rootId` and the `CarryClosureFragment` collapse, and eventually the port record itself.
- **The graph *shape* changes too, and it is not only about ports.** `EphemeraLudicGraphData` has no root and no `ports` element; `EphemeraLudicGraphNode` is `Character | Object | Room`; `HostRelationalEdgeKind` is `'On' | 'Under' | 'Against' | 'Custom'` with **no containment kind at all**. LP4a--LP4d close those, and all three are **decided design that was never implemented** rather than open questions.

**One correction carried in from 2026-08-15 so it is not re-derived:** it is **not** true that a port address cannot be typed as an `EphemeraObjectId`. `EphemeraWrappedId<T>` is `` `${T}#${string}` `` and `${string}` matches `ROPE#ab6129d`, so **the nested form satisfies the type statically and assigning one compiles clean** ([PQ-9](AGENT.abstractionLayers.proposals.planning.md#open-questions-ab-34-sub-questions-ids-stable-never-reused), limit (i)). The entire separator difference is **runtime**, inside the predicate. The room widening is a real typing change; the port widening is about honesty and runtime behaviour, not about making code compile.

## The widenings, concretely

**Target declarations, so the steps below are implementable rather than descriptive.** Written as end-state; Stage 2's port-address terminal is deliberately absent and lands in [LP7](#recommended-order).

**Two of these are not simple union additions, and both were found by checking rather than assumed:**

**(a) `EphemeraAreaId` does not exist.** `'AREA'` is not in [`EphemeraId`](../../../../../packages/mtw-interfaces/ts/baseClasses.ts)'s tag union --- `'ASSET' | 'FEATURE' | 'KNOWLEDGE' | 'ROOM' | 'MAP' | 'CHARACTER' | 'OBJECT' | 'MESSAGE' | 'MOMENT' | 'IMAGE' | 'SITUATION'` --- so **Area is a new component identity, not a widening.** The `OBJECT# / AREA# later` comment anticipated a type that was never minted. `Object` and `Feature` cost one union entry each; **Area costs an id type, a guard, an `EphemeraId` entry, and whatever asserts on that union.** Consider splitting it into its own slice.

**(b) `EphemeraPositionAdjacencyContainedId` is a fourth narrow union, and nothing above named it.** It is `Character | Object`, and it types the **`EphemeraId` of an adjacency row** --- the *contained* side of membership. `FEATURE#Niche` being a member of `FEATURE#Wall` makes a Feature a contained id, so this widens too or membership cannot be stored for the case [LD-8](#open-decisions-implementation--plan-only) decided.

```ts
// packages/mtw-interfaces/ts/baseClasses.ts --- new; Area only
export type EphemeraAreaId = EphemeraWrappedId<'AREA'>
export const isEphemeraAreaId = isEphemeraTaggedId<'AREA'>('AREA')
// ...and add 'AREA' to EphemeraId's union and to isEphemeraId's disjunction.

// packages/mtw-interfaces/ts/ephemeraPositionAdjacency.ts
export type EphemeraMembershipHostId =
    | EphemeraRoomId | EphemeraCharacterId
    | EphemeraObjectId | EphemeraFeatureId | EphemeraAreaId

export const isEphemeraMembershipHostId = (value: string): value is EphemeraMembershipHostId =>
    isEphemeraRoomId(value) || isEphemeraCharacterId(value) ||
    isEphemeraObjectId(value) || isEphemeraFeatureId(value) || isEphemeraAreaId(value)

// the contained side --- Feature added for FEATURE#Niche in FEATURE#Wall
export type EphemeraPositionAdjacencyContainedId =
    EphemeraCharacterId | EphemeraObjectId | EphemeraFeatureId

// packages/mtw-interfaces/ts/ephemeraMeta.ts
export type EphemeraLudicGraphNode =
    | { tag: 'Character'; universalKey: EphemeraCharacterId }
    | { tag: 'Object';    universalKey: EphemeraObjectId }
    | { tag: 'Room';      universalKey: EphemeraRoomId }
    | { tag: 'Feature';   universalKey: EphemeraFeatureId }
    | { tag: 'Area';      universalKey: EphemeraAreaId }

// Written out rather than aliased to EphemeraMembershipHostId, deliberately: the two are
// extensionally equal today and have different jobs, so drift must be visible. Assert the
// equality in a type-level test -- the divergence is the bug, not the duplication.
export type EphemeraLudicTerminalPrimitive =
    | EphemeraRoomId | EphemeraCharacterId
    | EphemeraObjectId | EphemeraFeatureId | EphemeraAreaId

// `owner` is the whole that ALLOCATED this port. Named `owner` rather than `host` on purpose:
// hostId means "the graph's owner" and ports[].fromHostId means "the exterior host referred in
// from", so a third `host` would be the fourth sense of the word in one file. See LD-10.
export type EphemeraLudicPortAddress = {
    owner: EphemeraLudicTerminalPrimitive;
    port: string;
}

export type EphemeraLudicTerminalId =
    | EphemeraLudicTerminalPrimitive
    | EphemeraLudicPortAddress

export type HostRelationalEdgeKind =
    | 'On' | 'Under' | 'Against' | 'Custom'   // spatial, player-driven -- unchanged
    | 'In' | 'PartOf'                          // containment, non-exclusive (premise 9)

export type EphemeraLudicRelationalEdgeData = {
    tag: 'Relational';
    from: EphemeraLudicTerminalId;   // was EphemeraObjectId
    to: EphemeraLudicTerminalId;     // was EphemeraObjectId
    kind: HostRelationalEdgeKind;
    relationLabel?: string;
}

export type EphemeraLudicGraphPort = {
    portId: string;
    fromHostId: EphemeraMembershipHostId;
}

export type EphemeraLudicGraphData = {
    hostId: EphemeraMembershipHostId;
    rootId: EphemeraLudicTerminalId;              // recorded, never derived (premise 10)
    nodes: EphemeraLudicGraphNode[];
    edges?: EphemeraLudicRelationalEdgeData[];
    ports: EphemeraLudicGraphPort[];              // egress list; interior-authoritative (premise 12)
}
```

**Why the terminal is structured rather than a `ROOM#A#portKey` string, decided 2026-08-15.** The string form was the plan of record, and [PQ-9](AGENT.abstractionLayers.proposals.planning.md#open-questions-ab-34-sub-questions-ids-stable-never-reused)'s separator lean existed to make it fail **loudly** at runtime, because *"there is no compile-time protection either way"* --- `EphemeraWrappedId<T>` is `` `${T}#${string}` ``, and `${string}` swallows `ROPE#ab6129d`, so a port address satisfies `EphemeraObjectId` statically. **The structured form expresses the distinction the string form could not**, which makes the runtime workaround unnecessary rather than better-implemented. Three consequences: [LP2](#recommended-order)'s discriminator stops being a parser and becomes `typeof x === 'string'`; the parsed shape **is** the stored shape, with no round trip; and `isEphemeraObjectId(edge.from)` becomes a **type error** rather than the vacuous pass PQ-10 caught. **It is also additive** --- stored edges with string terminals remain valid as the primitive branch, so no migration.

**Equality does not fix itself, and this is the residue to watch.** `edge.from === someObjectId` still compiles against `string | EphemeraLudicPortAddress` and still returns `false` for every port terminal --- PQ-10's original bug wearing new clothes. **What changes is that the *other* sites now fail at compile time**, so the residue is small and enumerable rather than diffuse. It needs a comparison helper, and [LP3](#recommended-order)'s regression test still earns its place.

**The string form survives at the storage boundary, deferred deliberately and with a gate.** `ROOM#A#{portKey}` remains the right **Dynamo** representation, and serde in and out of it may well beat storing a POJO. **Not needed immediately** --- edges live in `Meta::*.ludicGraph` JSON, which holds an object fine. **The gate that re-opens it: the first time a port address must stand free of an edge** --- a Dynamo key, an adjacency row, a cache key, a log line, an LLM prompt. **This is deferred, not unassigned**, and the distinction is the one this whole plan exists to fix.

**Which means PQ-9's separator is relocated, not retired --- correcting a claim made in conversation before this note.** Scoped to serde, its argument still holds and possibly harder: a `:` is *affirmed* by every id predicate and narrows to `EphemeraObjectId` with the compiler's endorsement, while a second `#` throws. **The question that decides how much it matters:** is the string form purely a serialization detail that never escapes the read boundary, or an addressable identity someone can hold? **Contained, the throw is harmless. Free-standing, PQ-9's hazard is live** --- and *free-standing* is exactly what the gate above describes. Keep [LP1](#recommended-order); narrow its scope to the boundary.

**`HOST_RELATIONAL_EDGE_KINDS` must gain both new kinds in the same change** --- it is a runtime `Set` used by `isEphemeraLudicRelationalEdgeData`, so a union widened without it rejects every containment edge at the guard while typechecking clean. **That is the silent-mismatch shape again**, and it is one line.

**Required-versus-optional, stated so it is not defaulted:** `ports` is **required and possibly empty**, on CC0's own argument for `crossings` --- an optional field re-introduces a two-case branch at every read site. `edges` stays optional only because it already is; **do not make `ports` match it.**

**And *required* here means required, with no read-boundary default --- which is [LPM](#recommended-order)'s whole job.** `rootId` and `ports` are absent from every stored payload, and `EphemeraLudicGraphFieldPayload` is `Omit<EphemeraLudicGraphData, 'hostId'>`, so making them required in the domain type makes them required in the stored shape. **The migration is a reset, not a backfill and not a tolerant guard:** clear the stored graphs first, then widen. Defaulting was considered and rejected --- see LPM's first bullet for the two defaults that *would* have been honest (`ports ??= []`, `rootId ??= hostId`) and why permanent tolerance for a never-again shape lost to a one-time wipe on a single dev instance.

## Recommended order

Pending work uses `- [ ]` and completed work uses `- [X]`; nested bullets carry their own boxes, so mark each `[X]` as it lands rather than only the parent.

**Stage 1 --- startable now. Nothing here bets on P7's mechanism.**

- [X] **LP0. Widen `EphemeraMembershipHostId` --- first, because several steps below need it and none of it depends on them.** Today it is `Room | Character`, and [LP4d](#recommended-order) cannot type `fromHostId` for an object-hosted port without this. **The type carries its own roadmap and it agrees:** *"v1 eligible membership hosts (room + character inventory; **OBJECT# / AREA# later**)"* --- so Object and Area are deferred scope being taken up, not a new claim. **Landed 2026-08-16 for `Object`/`Feature`; `Area` landed the same day as its own slice per LD-9 --- see its bullet below. All sub-items closed.**
  - [X] **Separate the two changes, and do them in this order.** Widening the **type** is mechanical and additive: 200 non-test references across 50 files, none of which narrow on the union. Widening the **guard** is a behaviour change --- `isEphemeraMembershipHostId` is used as an **admission test at boundaries**, so every widened kind starts being *accepted* where it was previously refused. **Land the type first and the guard deliberately.** *Correction found while landing this: the "none of which narrow on the union" premise was wrong.* Non-test production code narrowed in two places (see the cache/kernel gap bullet below), and **six test files independently narrowed too** --- mocks typed as `(hostId: EphemeraCharacterId | EphemeraRoomId) => ...` stopped satisfying callers now typed on the widened union, caught only by running the full suite (`dataSource/actions/enrich/objectManipulation/synthesize/expandSameHost.test.ts`) and by literal `'OBJECT#...'`/`'FEATURE#...'` strings used as **rejection** fixtures in `ephemeraPositionAdjacency.test.ts`, `ephemeraMeta.test.ts`, `ludicCache/types.test.ts`, `positions/publishedEvents.test.ts`, `actions/publishedEvents.test.ts` that silently became **acceptance** cases once those tags became legal hosts. All fixed and reused as positive-case coverage instead.
  - [X] **The guard's 18 call sites are not uniform, and two classes need different care.** *Validators* --- [`isEphemeraLudicGraphData`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts), [`publishedEvents`](../../../../../lambda/ephemera/dataSource/actions/publishedEvents.ts) on both `hostId` and `transferFromHostId` --- widen to admit new host kinds, which is the intent. *Filters* are the hazard: [`groundChange.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/synthesize/groundChange.ts) uses `if (!isEphemeraMembershipHostId(candidate)) continue` to **skip** candidates, so widening silently lets previously-skipped candidates through into LLM grounding. **Enumerate all 18 and classify each as validator or filter before changing the guard.** *Done --- all 12 non-definition call sites classified; `groundChange.ts`'s two filters are the only behaviour change, and it is the intended one, now covered by a regression test.*
  - [X] **It widens a DynamoDB key template.** `EphemeraPositionAdjacencyDataCategory` is `` `POSITION#${EphemeraMembershipHostId}` ``, so this enlarges the key space. Additive for existing rows; **check nothing parses that category assuming exactly two tags.** *Checked --- only `parsePositionAdjacencyDataCategory` decodes it, and it delegates to the widened guard; no other hardcoded two-tag parse found.*
  - [X] **`Object`** --- **forced**, and the reason is premise 1's recursive hosting: *"the rope hosts its parts exactly as the box hosts its contents."* A spring's port egressing to `OBJECT#BOX` is the ordinary case.
  - [X] **`Area`** --- named in the type's own comment, **but it is the expensive one and the comment concealed that.** `EphemeraAreaId` does not exist and `'AREA'` is not in `EphemeraId`'s tag union, so this mints a **new component identity** rather than widening one. See [the declarations](#the-widenings-concretely). **Landed 2026-08-16, as its own slice per LD-9's decision.** Minted `EphemeraAreaId`/`isEphemeraAreaId` in `baseClasses.ts`, added `'AREA'` to `EphemeraId`'s tag union and `isEphemeraId`'s disjunction, then widened `EphemeraMembershipHostId`/`isEphemeraMembershipHostId` the same way Object/Feature landed. **Deliberately did not widen `EphemeraPositionAdjacencyContainedId`** --- Area is a host only, never a contained member, so it has no counterpart on that union; confirmed against the target code block before touching it rather than assumed. The `hostDataCategory`/`graphFromMeta` and `assertForwardHostId` `KNOWN GAP` comments were extended to name Area alongside Object/Feature --- no logic change, since both already throw/misroute for anything that isn't Room or Character.
  - [X] **Widen `EphemeraPositionAdjacencyContainedId` too --- the *contained* side, which nothing above named.** It is `Character | Object` and types an adjacency row's `EphemeraId`. `FEATURE#Niche` as a member of `FEATURE#Wall` makes a Feature a contained id, so **without this, membership for the case LD-8 decided cannot be stored at all.** *Also found and fixed in the same change: `isEphemeraPositionAdjacencyRow` independently hardcoded `Character \| Object` inline rather than through this type --- would have silently rejected valid Feature-contained rows if left alone.*
  - [X] **`Feature`** --- **in, decided 2026-08-15 ([LD-8](#open-decisions-implementation--plan-only)) on a worked case: `FEATURE#Wall` hosts `FEATURE#Niche` with `PartOf`.** The type's comment did not anticipate it, so this one is a genuine extension of the roadmap rather than deferred scope --- **and it survives the constraint that made it look doubtful.** *Never membership-moved* is untouched: a niche is part of a wall and neither of them goes anywhere. The literal reading of *"more like the walls of a room than its contents"* now has a wall with a part, which is the point.
  - [X] **Guard against the inference premise 11 names.** Widening the **host** union does not put any kind into the **part-of ladder**, and `AGENT.concepts.md` calls that the reading most likely to be wrongly drawn. Say so in the change. *A code comment on the widened type in `ephemeraPositionAdjacency.ts` now says so directly; `AGENT.concepts.md:351` already carried the design-level answer.*
  - [X] **New, found while landing this --- the widened type also reaches the already-shipped membership-transfer kernel, not only future ports.** `EphemeraMembershipHostId` types `hostDataCategory`/`graphFromMeta` (`ludicGraph/index.ts`) and `EphemeraLudicGraphCacheData.getLudicGraph` (`internalCache/ludicGraphCache.ts`), both of which only know `Room`/`Character` (`isEphemeraRoomId(hostId) ? A : B`) and are on the live `commitStepSequence` write path, not gated behind ports. An Object- or Feature-hosted `transferMembership`/`establishRelation` step now passes grounding and type-checks, then **throws at commit** --- `MultiKeyUpdate fetch missing footprint host` from the kernel, or the cache's own forward-host assertion, whichever is reached first. **Decision (user, 2026-08-16): leave it throwing, document it** --- fail loud rather than silently misread a row, and treat "teach the kernel/cache about Object/Feature storage" as a gap owned by a later slice rather than blocking this one. **Closed 2026-08-16.** `Meta::Object` / `Meta::Feature` / `Meta::Area` storage landed (each paired with play-position cache-gateway widening in the same slice, because `seedGraphMemos` calls `internalCache.Positions.set` unconditionally post-commit --- a Dynamo-only fix would have traded a safe pre-commit throw for an unsafe post-commit one); `hostDataCategory`/`graphFromMeta` and `assertForwardHostId` now dispatch all five host kinds; both gap comments retired. The serde rule that came out of it --- **one shared plain body for all five, with Room's `activeCharacters` fallback as the single documented exception** --- is now durable at [Host storage](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md#host-storage-one-shared-serde-one-documented-exception). The planning document that tracked this work has been deleted per [`taskPlanning/AGENT.md`](../../../../AGENT.md)'s *When the task finishes*.
- [X] **LP1. Graduate PQ-9's separator from a lean to a decision.** Everything else keys off it. The argument and the shipped-code verification already exist; what is missing is the graduation. **Done means:** the row's verdict cell reads as decided, and the [Open decisions](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) row upstream reflects it. **No code.** **Closed 2026-08-17.** PQ-9's row now reads decided on the separator, scoped exactly to the serde/Dynamo string-form boundary the 2026-08-15 rescoping already drew --- not widened by the graduation, since the domain-layer terminal is structured and does no parsing. The upstream ["not locked" table](AGENT.abstractionLayers.planning.md#what-is-not-locked-and-must-not-ride-in-on-this) row was narrowed to name only declared-vs-derived, which stays open and stays PQ-9's (premise 8, unchanged). Numbered-vs-named is untouched by this slice --- it was already retired to PQ-4 on 2026-08-13.
- [X] **LP2. Build the port-aware discriminator, and treat it as a first-class primitive rather than a helper.** PQ-9's limit (ii) is the requirement: under `#`, `isEphemeraObjectId(x)` **crashes** on a port address instead of returning `false`, so a port address cannot be handed to the tagged-id family even to ask *what kind of thing is this*. PQ-9 concludes a port-aware discriminator must land **in the same change as ports**. **Closed 2026-08-17.** `EphemeraLudicTerminalPrimitive`, `EphemeraLudicPortAddress` and `EphemeraLudicTerminalId` are now defined in `ephemeraMeta.ts`, alongside `isEphemeraLudicTerminalPrimitive`, `isEphemeraLudicPortAddress`, `ephemeraLudicTerminalOwner`, `ephemeraLudicTerminalsEqual` and `ephemeraLudicTerminalRefersTo` (the last built now for [LP3](#recommended-order) rather than invented ad hoc there, per this bullet's own title). Nothing downstream is wired up: `EphemeraLudicRelationalEdgeData.from`/`to` are untouched, staying `EphemeraObjectId` until LP4.
  - [X] **Decide where it lives** --- beside the tagged-id family in `baseClasses.ts` (published, and the natural home for something that fixes a hole in that family) or local to `positions/`. ~~Published is the recommendation, because the hazard is in the published predicate, and a local fix leaves every other caller exposed.~~ **Resolved 2026-08-17, and not to either listed option: `ephemeraMeta.ts`.** The recommendation above was reasoned when the discriminator was imagined as a fix to `isEphemeraObjectId`'s throw on `#` --- that fix now belongs to the deferred serde boundary (premise 4's rescoping), not here. What actually got built operates on `EphemeraLudicTerminalId`, a type that belongs beside the other ludicGraph types (`EphemeraLudicGraphNode`, `EphemeraLudicRelationalEdgeData`) in `ephemeraMeta.ts`. `ephemeraMeta.ts` imports from `baseClasses.ts`, not the reverse, so `baseClasses.ts` would need either a new import cycle or domain-specific ludicGraph types moved into a file that otherwise knows nothing about the ludicGraph domain. `ephemeraMeta.ts` is still published, which is LD-1's underlying reasoning; it is simply not `baseClasses.ts`. See [LD-1](#open-decisions-implementation--plan-only).
  - [X] ~~Split a terminal into `{ kind, base, port? }` without ever calling a throwing predicate.~~ **Superseded 2026-08-15 --- the structured terminal makes the parsed shape the stored shape.** The discriminator narrows `EphemeraLudicTerminalId` by `typeof x === 'string'`, and what is actually needed is **an accessor for the base primitive** (`x` or `x.owner`), which every consumer calls constantly, plus **a comparison helper** --- see the equality residue in [the declarations](#the-widenings-concretely). **No parsing in the domain layer at all**; parsing belongs to the deferred serde boundary if and when it lands. **Built as `ephemeraLudicTerminalOwner`** (the accessor) **and `ephemeraLudicTerminalsEqual` / `ephemeraLudicTerminalRefersTo`** (full-equality and owner-only-match comparison, the latter being the `edge.from === id` drop-in LP3 needs).
  - [X] **Unit tests are the deliverable here, not an afterthought:** unqualified id, port-qualified id, room id, malformed multi-section id, and the empty-port-segment edge case. **Reconciled 2026-08-17: the last two entries predate the 2026-08-15 supersession two bullets up and describe string-parsing failures that cannot occur once "no parsing in the domain layer at all" is true --- the same staleness pattern this plan names elsewhere (premise 2, the Feature-subject rule, `AGENT.concepts.md`).** Reinterpreted against the structured design and built as shape-guard cases instead: *malformed* is `isEphemeraLudicPortAddress({ owner: 'BOGUS#X', port: 'ab6129d' })` rejecting an owner that isn't a legal terminal primitive; *empty-port-segment* is `isEphemeraLudicPortAddress({ owner: 'OBJECT#BOX', port: '' })` rejecting a blank `port`. *Unqualified id*, *port-qualified id* and *room id* are covered as originally intended, on `ephemeraLudicTerminalOwner` and `isEphemeraLudicTerminalPrimitive`. All in `ephemeraMeta.test.ts`.
- [X] **LP3. Fix the matchers (PQ-10).** Confirmed sites, all reached from `ludicGraph`: [`assertNoRelationalEdgesReferencing`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/index.ts) (`index.ts:307`), [`bothObjectsOnGraph`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/index.ts) (`index.ts:337`), and `edgeReferencesObjectId` in [`baseClasses.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/baseClasses.ts). **Closed 2026-08-17.** All three, plus two same-shape sites found while landing this (`nodeHasRelationalEdge`, `edgesMatch` in `baseClasses.ts`), now route through LP2's terminal-aware helpers (`ephemeraLudicTerminalRefersTo`/`ephemeraLudicTerminalsEqual`/`ephemeraLudicTerminalOwner`) instead of raw `===`/`Set.has`. **A scoping fact surfaced while implementing, worth recording:** `EphemeraLudicRelationalEdgeData.from`/`.to` and the local `HostRelationalEdge.from`/`.to` are still `EphemeraObjectId`-typed (LP4/LP7 haven't widened them), so no production call path can hand these matchers a real port-qualified terminal yet — this fix is deliberately preemptive, so LP4/LP7 don't have to revisit these five call sites when they widen the field types. **Found and fixed in the same change, not this plan's fix but blocking a meaningful test of it:** `isEphemeraLudicRelationalEdgeData` (`packages/mtw-interfaces/ts/ephemeraMeta.ts`) called `isEphemeraObjectId(edge.from)` unconditionally and **crashed** (`value.split is not a function`) on a non-string terminal instead of returning `false` — hardened to check `typeof edge.from === 'string'`/`typeof edge.to === 'string'` first. **Found but explicitly left out of this slice:** [`expandValidate/interactionUnderTransfer.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/expandValidate/interactionUnderTransfer.ts) (`roleOfObjectInEdge`, `computeCarryClosure`'s set-membership checks) has the identical `edge.from === objectId` shape over the same `HostRelationalEdge` type — it's `CarryClosureFragment` machinery that LP4a is already collapsing into a rooted `ludicGraph`, so fixing it here would be scope creep into a step this plan already owns; **LP4a should pick it up** rather than it being silently missed a second time.
  - [X] **Settle PQ-10's open half first:** resolution at **parse time** (one place, but it discards the port and every downstream consumer loses it) versus **at each comparison** (preserves the port, but every new comparison site is a new chance to forget). **Recommendation: at comparison, via LP2's discriminator**, because parse-time resolution destroys exactly the information the reducer needs. **Decided 2026-08-17 per LD-2: at comparison** — no new argument needed, the recommendation stood.
  - [X] Regression tests that would have caught the original hazard: a port-qualified edge must be **found** by `assertNoRelationalEdgesReferencing(baseId)`, which is the assertion that passed vacuously. **Reconciled against the scoping fact above:** since a port-qualified terminal can't reach the *validated* stored-edge read path yet (`isEphemeraLudicRelationalEdgeData` correctly rejects it until LP4/LP7 widen the schema), the test coverage lands at the level that's actually reachable today and exercises the identical comparison logic — `bothObjectsOnGraph`, `nodeHasRelationalEdge`, `edgesMatch` in `ludicGraph/index.test.ts` (`describe('port-qualified terminals (LP3/PQ-10)')`), plus a boundary test proving `edgeReferencesObjectId`/`isEphemeraLudicGraphFieldPayload` reject a port-qualified raw edge cleanly rather than throwing (`ephemeraMeta.test.ts`).
- [X] **LP4. Widen edge terminals --- to *every legal host kind*, not only rooms.** ~~Admit rooms.~~ **Widened 2026-08-15:** the original scope came from [LC8](AGENT.abstractionLayers.ludicCache.corpus.planning.md#lc8-the-spring-in-the-box-and-in-the-contraption)'s edge beginning at `ROOM#A`, which is still forcing and still carries no bet on P7 --- but rooms were never the general case. `from`/`to` are `EphemeraObjectId`; they must admit whatever [LP0](#recommended-order) makes a host. Update `isEphemeraLudicRelationalEdgeData` and `ephemeraMeta.test.ts` in the same change, as CC0b did for the `Room` node. **Closed 2026-08-17.** `EphemeraLudicRelationalEdgeData.from`/`.to` (and both `HostRelationalEdge` copies) retyped to `EphemeraLudicTerminalPrimitive` (LP2's five-kind union, reused rather than reinvented), `isEphemeraLudicRelationalEdgeData` switched to `isEphemeraLudicTerminalPrimitive`. Node kinds (`EphemeraLudicGraphNode`) deliberately stay `Character | Object | Room` --- LP4b's job --- so an edge can validate with a Feature/Area terminal before any graph can produce such a node; nothing does yet, so this is inert, matching LP2's own "nothing downstream wired up" precedent.
  - [X] **The three unions are linked by entailment, so derive them rather than enumerating each separately.** (a) Concepts clause 3 puts a host's **root node in its own node list**, so **every host kind is a node kind.** (b) Containment edges run **root to member**, so every host kind and every member kind is a **terminal** kind. (c) [LC8](AGENT.abstractionLayers.ludicCache.corpus.planning.md#lc8-the-spring-in-the-box-and-in-the-contraption)'s referential-integrity requirement means a terminal must **name a node**, so terminal kinds cannot exceed node kinds. **Therefore terminal kinds and node kinds are the same set, and host kinds are a subset of it.** This is what [LP4b](#recommended-order) should enumerate against --- it turns *"enumerate against the corpus"* into a derivation. *No change needed here --- confirmed the derivation is LP4b's premise, not this step's obligation.*
  - [X] **Keep the three type names distinct, and assert the relationship in a test rather than merging them.** They have different jobs --- `EphemeraMembershipHostId` also spells a Dynamo key (`` `POSITION#${...}` ``) --- and a future kind may be a node without being a host. **A test that fails when the unions drift apart is worth more than a single merged type**, because the divergence is the bug, not the duplication. *Left as-is; no merge attempted.*
  - [X] **Blast radius, measured 2026-08-15 --- narrow in types, wide in assumptions.** Only **five** non-test files are typed on `EphemeraLudicRelationalEdgeData`: [`ephemeraMeta.ts`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts), [`gateways/positions/project.ts`](../../../../../packages/mtw-gateways/ts/ephemera/positions/project.ts), [`ludicGraph/index.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/index.ts), [`ludicGraph/baseClasses.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/baseClasses.ts), [`ludicCache/types.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicCache/types.ts). **The 22 files matching `edge.from`/`edge.to` are mostly a false positive** --- `mtw-wml` and `mtw-utilities` graph code sharing field names for a different abstraction --- **so do not scope the change from that grep.** *A sixth site missed by the five-file list: `manipulation/types.ts` independently redeclares `HostRelationalEdge` (same shape, not imported from `baseClasses.ts`) --- a pre-existing duplication, widened in lockstep rather than consolidated; noted in `ludicGraph/AGENT.md`'s "Relational edge names" table.*
  - [X] **What is actually wide is the object assumption**: 101 non-test `isEphemeraObjectId` call sites, and signatures that hard-code the narrow type --- `bothObjectsOnGraph(from: EphemeraObjectId, to: EphemeraObjectId)` is the clearest. **Widening the terminal type without widening these leaves the assumption in place while the type says otherwise**, which is the silent-mismatch class rather than a compile error. Overlaps [LP3](#recommended-order); do them together or in immediate sequence. **Closed.** `bothObjectsOnGraph`/`nodeHasRelationalEdge` widened to check a new kind-indifferent `nodeIds` getter (not `objectIds`, which stays membership-scoped and untouched). `tsc --noEmit` surfaced 21 compile errors across 8 downstream files where a widened `edge.from`/`.to` flowed into an `EphemeraObjectId`-typed slot (`detectRelationalCycle.ts`, `executor.ts`, `applyTransferSet.ts`, `interactionUnderTransfer.ts`, `compilePositionKernelOp.ts`, `applyObjectClearMembership.ts`, `applyObjectRelationalChange.ts`, `applyObjectRoomMembership.ts`) --- the loud, enumerable residue this bullet predicted, one silent-mismatch class turned into a compile-time one. Each fixed with an explicit `isEphemeraObjectId` narrow at the boundary (throw for authoritative kernel-step builders; filter/skip for best-effort walks like cycle detection and carry-closure), since production ingress can only produce Object-typed relational edges today --- widening those call sites' own input types is a deferred, separate decision (character/Feature-relation authoring), not this slice's.
- [X] **LPM. Reset the stored graphs, so `rootId` and `ports` can be *required* rather than defaulted. Lettered out of sequence deliberately** --- it is a one-time operational step rather than a code slice, and it must **execute before [LP4a](#recommended-order) and [LP4d](#recommended-order)**, which are the steps that make the two fields required. **Closed 2026-08-19.** All three steps landed; the "noted and out of scope" bullet below is a deliberately unresolved side question, not a blocker. `rootId`/`ports` may now be made required with no default.
  - [X] **The alternative, for the record, and why it lost.** The read boundary is already tolerant --- [`fromRoomMeta`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/index.ts) falls back to `seedFromActiveCharacters` and `fromCharacterMeta` to `{ nodes: [], edges: [] }` --- so both fields *could* be defaulted in `fromFieldPayload`, where `hostId` is in scope: `ports ??= []` (an empty egress list is a **correct** state, not a placeholder) and `rootId ??= hostId` (true of the whole existing corpus, since every graph shipped today is a room-or-character host graph whose root is its host --- **not** the edge-derivation premise 10 forbids). **Rejected because it is permanent tolerance for a shape that will never be written again.** Resetting costs once; defaulting costs a `??` at the read boundary and a loosened payload guard forever. **Pre-rollout standing --- single development instance, no external consumers --- is what makes the one-time cost the cheaper side, and it is the load-bearing premise here rather than a passing remark.** It is *not* recorded in any plan's settled register (checked 2026-08-15); if that ever stops being true, this decision is the one to revisit first.
  - [X] **Order is load-bearing: clear first, land the required fields second.** Reversed, every read between deploy and reset hits an old-shape row against a tightened guard. **The clearing tool is shape-agnostic** --- it removes an attribute --- so it can be built and run against the *current* code, before anything is widened. **Order held**: the tool was built and run before LP4a/LP4d, which have not started.
  - [X] **Step 1 --- extend the wipe to clear graphs, because today nothing does.** [`persistClearCoyoteGameImprovisationObjects`](../../../../../lambda/ephemera/dataSource/objects/persistImprovisationObject.ts) states in its own header that it *"does not mutate graphs"*: it enumerates object ids **from** the stored `ludicGraph`, deletes the `Meta::Object` and pair rows, and leaves the graph standing with nodes naming objects that no longer exist. **This is not an oversight in one function.** [`removeObject`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/index.ts) exists and is rigorous --- it strips node and play-only edges behind the BD-33/BD-35 assert-and-throw contract --- but **its only callers are the membership-*transfer* path** ([`applyStepSequenceCore.ts:151`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/applyStepSequenceCore.ts), [`applyTransferSet.ts:72`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/expandValidate/applyTransferSet.ts)). Cleanup answers *object moved out*, never *object ceased to exist*. **The confirming evidence is that [`orphanedImprovisedObjectSweep`](../../../../../lambda/diagnostics/orphanedImprovisedObjectSweep/index.ts) exists at all** --- it detects graph nodes with no surviving pair row, which would be undetectable-because-impossible if deletion cleaned graphs. **Closed 2026-08-18.** **Premise correction found while landing this: `persistClearCoyoteGameImprovisationObjects` is dead code** --- it is called from nowhere but its own test file. The production "await roadrunner" path is a same-named-but-different sibling, [`clearCoyoteGameImprovisationObjects`](../../../../../lambda/ephemera/dataSource/objects/clearCoyoteGameImprovisationObjects.ts) (no `persist` prefix), which *does* touch graphs via `applyObjectClearMembership` → `commitStepSequence` → `removeObject`, but only ever **rewrites** the attribute to a smaller object (`commitStepSequence.ts:133` always does `entry.ludicGraph = graph.toStored()`) --- it never REMOVEs `ludicGraph` outright. That REMOVE is the actual gap this step closes. Built [`persistClearStoredLudicGraphs.ts`](../../../../../lambda/ephemera/dataSource/objects/persistClearStoredLudicGraphs.ts), exporting `persistClearStoredLudicGraphs`: for every room in `CoyoteGame.gameRooms` and every character `collectActiveCharactersInCoyoteRooms` reports active in one of those rooms, issues `ephemeraDB.optimisticUpdate({ updateKeys: ['ludicGraph'], updateReducer: (draft) => { delete draft.ludicGraph } })` --- the `delete draft.field` idiom already established at `renderCache/perspectivePointer.ts:46-52`, which emits a real DynamoDB `REMOVE ludicGraph` rather than an empty-object overwrite. **Scope decision (user, 2026-08-18): kept local to CoyoteGame** --- the same rooms/characters the existing wipe sequence already touches, enumerated via the same functions rather than a full `Meta::Room`/`Meta::Character` table scan. If a room or character outside that scope ever turns out to carry a stored `ludicGraph`, that is the "reset is not total" case this plan's next bullet already names as a separately-checked risk, not something this tool's scope was built to solve. Covered by `persistClearStoredLudicGraphs.test.ts`.
  - [X] **Clear both categories, not just game rooms.** `Meta::Character.ludicGraph` is inventory and the coyote path only walks `CoyoteGame.gameRooms`. **Mirror the sweep's two-category scan** --- `orphanedImprovisedObjectSweep` already queries `Meta::Room` **and** `Meta::Character`, so the coverage question has a shipped answer rather than needing one invented. **Also check whether any room outside `gameRooms` carries a stored graph**; if so the reset is not total and the defaulting fallback comes back into scope for those rows only. **Closed 2026-08-19, resolved manually rather than by tool scope.** [LPM's Step 1](#recommended-order) deliberately scoped `persistClearStoredLudicGraphs` to CoyoteGame's rooms/active-characters, not a full table scan (user decision, 2026-08-18) --- so this bullet's coverage question was left open by the tool. **User confirmed 2026-08-19: manually swept and cleared every remaining row**, including several carrying the pre-rename `positionGraph` field rather than `ludicGraph` (a legacy field name; confirmed zero `.ts` files anywhere in the repo still read or write `positionGraph`, so it cannot reappear --- purely leftover data, not a live code gap). No row outside the tool's scope was left carrying either field.
  - [X] **Remove the attribute; do not write an empty payload.** Absence routes rooms to the seed path and characters to the empty default, and it makes the guard behave: `!isEphemeraLudicGraphFieldPayload(undefined)` returning `[]` in [`classification.ts:25`](../../../../../lambda/diagnostics/roomOccupancyDriftSweep/classification.ts) is the **correct** answer for an empty room. A present-but-old-shaped payload is the case that breaks --- it takes `fromRoomMeta`'s `if (record.ludicGraph)` branch and, once the guard tightens, classifies every room as zero-characters **silently**, with no throw. **Followed exactly**: `persistClearStoredLudicGraphs` uses `delete draft.ludicGraph` via `ephemeraDB.optimisticUpdate`, which emits a real DynamoDB `REMOVE`, never an overwrite with `{}`.
  - [X] **Step 2 --- execute manually**, in the established sequence: wipe objects (`CoyoteGame` await-roadrunner), disconnect all characters, **clear the graphs**, then an S3 Stale finding if the asset side needs it. Note the Stale finding is *not* what fixes this: `ludicGraph` has **no asset provenance** --- nothing in `lambda/assets`, `lambda/wml`, or the ephemera cache-asset path ever writes it --- so the `.ndjson` rebuild neither regenerates nor corrects it. **Closed 2026-08-19 (user, manual execution).**
  - [X] **Step 3 --- verify the reset landed before widening anything.** Confirm no `Meta::Room` or `Meta::Character` row carries a `ludicGraph` attribute. **This is the gate on LP4a/LP4d**; with it green, the two fields are required with no default and no tolerant payload guard. **Closed 2026-08-19 --- user confirmed manually.**
  - [ ] **Noted and out of scope: should object *deletion* clean graphs at all?** The move path's assert-and-throw contract is strict; the delete path's silence is a visible asymmetry, compensated by a diagnostic sweep rather than prevented. **A real question, and not this plan's** --- record it upstream if it wants an owner.
- [X] **LP4a. Add `rootId` to the graph shape --- and collapse `CarryClosureFragment` into it rather than leaving parallel duplication.** Concepts clause 3 requires exactly one root node, **present in the graph's own node list** and usable as an endpoint like any other, with the graph carrying a designation of which node that is. `EphemeraLudicGraphData` is still `{ hostId, nodes, edges? }` and has none. **This is the prerequisite for LP4c**, since a containment edge runs root -> member and the root must be nameable. **Closed 2026-08-19.** `rootId: EphemeraLudicTerminalId` added to `EphemeraLudicGraphData`/`EphemeraLudicGraphFieldPayload` (required, in the field payload since only `hostId` is `Omit`-ed) and threaded through `EphemeraLudicGraph` (constructor, all factories, `toJSON`/`toStored`, `equals()` --- which previously did not compare roots at all). `isEphemeraLudicGraphFieldPayload` validates it inline against the full `EphemeraLudicTerminalId` union (primitive or port address) without shipping the named `isEphemeraLudicTerminalId` guard, which stays LP7's to build. `computeCarryClosure` now returns an actual `EphemeraLudicGraph` (`hostId = rootId = startId`) instead of the standalone `CarryClosureFragment` shape, which is deleted; every consumer (`expansionEnvironment.ts`, `executorTypes.ts`, `applyObjectRelationalChange.ts`, `positionKernelOp.ts`, `buildObjectMoveOp.ts`, `compilePositionKernelOp.ts`, `executor.ts`) swapped `.members`/`.edges` for `.objectIds`/`.relationalEdges`. `roleOfObjectInEdge`'s `edge.from === objectId` matcher shape (flagged by LP3 for this slice) now routes through `ephemeraLudicTerminalRefersTo`.
  - [X] **`rootId` is recorded, never derived** (premise 10). Take it as an input; do not infer it from the edges. **Every host-bound factory (`empty`, `seedFromActiveCharacters`, the five `from*Meta` wrappers, `fromPlayEnvelope`) sets `rootId = hostId` explicitly** --- the one true value for a host's own interior graph (concepts clause 3), not a read-boundary default; `computeCarryClosure` sets it from its `startId` argument, unchanged.
  - [X] **Gated on [LPM](#recommended-order): required, with no default.** Do not add `rootId?` and do not default it at the read boundary. With the reset verified, every graph written from here is written by this code and supplies it natively. **No `??` at any read boundary** --- `fromFieldPayload` reads `payload.rootId` directly, now a required field on the type.
  - [X] **A revisit trigger fires here, and it was pinned in two places for this moment.** [`CarryClosureFragment`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/expandValidate/interactionUnderTransfer.ts) is `{ rootId, members, edges }` --- *"a rooted ludic graph in all but name"*, which *"exists as a separate shape only because this class has no root to lend it."* [`ludicGraph/AGENT.md`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/AGENT.md) states the instruction plainly: **if `ludicGraph` ever gains a root concept, the fragment should collapse into it rather than persist as parallel duplication.** **Done --- the fragment type is deleted, not kept as an alias**, and both docs updated to record the collapse rather than the trigger.
  - [X] Name and type the root-designating field. [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md) lists this as unsettled; `rootId` matches the shipped precedent and is the recommendation. **Named `rootId`, typed `EphemeraLudicTerminalId`** (the full primitive-or-port union, per this plan's own target declarations) --- recorded in `AGENT.concepts.md`.
- [X] **LP4h. Widen [`applyTransferSet`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/expandValidate/applyTransferSet.ts)'s transfer set to `Object | Character` --- catching up to a widening that already happened.** Added 2026-08-17. The function takes `transferSet: ReadonlySet<EphemeraObjectId>`, but its own caller's step type `MutationKernelTransferStep.entityIds` has been `Object | Character` since BD-36. [`applyStepSequenceCore.ts:79-88`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/applyStepSequenceCore.ts) splits that set back apart into `objectIds`/`characterIds` and routes characters **around** `applyTransferSet` entirely, handling them inline with `addCharacter`/`removeCharacter`. **The split is a workaround for this signature, not a design** --- widening collapses it. **Closed 2026-08-19.** `transferSet` is now `ReadonlySet<EphemeraObjectId | EphemeraCharacterId>`; the caller's real-transfer branch calls `applyTransferSet(nextSourceGraph, nextDestGraph, step.entityIds)` directly (its staleness pre-check generalized to one loop over `graph.nodeIds`), and the caller's separate character add/remove loop is deleted --- the split itself stays, since the pure-remove/pure-add branch below still needs it. New tests added directly against `applyTransferSet` (character-only, mixed object+character); the existing `applyStepSequenceCore.test.ts` BD-36 coverage (character-only, mixed, stale-character) passed unchanged, confirming the refactor is behaviour-preserving. Full `dataSource/positions/` suite green (388 tests), including integration tests.
  - [X] **Not the full terminal union, and the limit is a real one rather than caution.** Transfer means *changes host*. Objects move and characters move; Rooms, Features and Areas **are** hosts and never relocate. This is one of the few Object-ish narrows in the positions layer that is defensible on the merits --- it is simply drawn one kind too tight. Widen to `Object | Character`, and **write down why it stops there**, or the next reader will widen it to `EphemeraLudicTerminalPrimitive` for symmetry with LP4. **Done** --- a comment on the parameter records the limit.
  - [X] **The two `isEphemeraObjectId` guards [LP4](#recommended-order) added at [`applyTransferSet.ts:61-63`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/expandValidate/applyTransferSet.ts) are semantically dead and should simply go.** Given an Object-only `transferSet`, `transferSet.has(nonObject)` is already `false`; the guards exist **only** because TypeScript will not pass a wide union to `Set<Narrow>.has`. They change no runtime behaviour, and the comment above them overstates their significance --- treat them as compiler bookkeeping to delete, not as a narrowing to preserve. **Resolved 2026-08-19, and not by deletion:** `tsc` showed they're still required as-is once `transferSet` widens --- `edge.from`/`.to` (the five-kind `EphemeraLudicTerminalPrimitive`) still need narrowing down to `EphemeraObjectId` before `.has()` will accept them against the now-two-kind set, and no character-endpoint edge exists to widen the narrow to. **A second guard this step's own widening newly required, and not anticipated by this bullet:** `applyTransferSet`'s call to `boundaryEdgeOutcomes` (`interactionUnderTransfer.ts`, still `ReadonlySet<EphemeraObjectId>`-typed and out of this step's scope) does **not** type-check against the widened set --- `tsc` confirmed `ReadonlySet<Object|Character>` is not assignable to `ReadonlySet<EphemeraObjectId>`, no bivariance rescue. Fixed by filtering to an object-only subset before that one call.
  - [X] **Independent of containment --- do not let it get bundled into [LP4c](#recommended-order)'s gate.** This step would be worth doing if `In`/`PartOf` never landed at all. It shares a file family with [LP4a](#recommended-order), which is why it sits here.
  - [X] **Note what it does *not* reach.** The `removeObject`/`addObject` loop (`:74-77`) is genuine Object machinery, so admitting characters means dispatching to `addCharacter`/`removeCharacter` --- which is exactly the code the caller-side split currently holds. **Move it down here; do not duplicate it.** **Done.** **Also found and left alone, matching this bullet's own boundary:** `interactionUnderTransfer.ts`'s `computeCarryClosure`/`boundaryEdgeOutcomes` carry code comments claiming their Object-only `isEphemeraObjectId` guards "retire via LP4g/LP4h" --- checked against this step's actual scope and **they do not**; corrected in place (`interactionUnderTransfer.ts`, `ludicGraph/AGENT.md`'s "Character-relation widening, deferred (BD-36)" note) to say so, rather than leaving a second stale forward-reference. That narrow is now recorded as unowned, not assigned to LP4g either (LP4g's own bullets are the kernel's step terminals and `findHostOf`, not this file) --- a real gap surfaced by checking rather than resolved.
- [X] **LP4b. Widen `EphemeraLudicGraphNode` to the component kinds the graph actually needs.** Today the union is `Character | Object | Room` (CC0b added `Room`). ~~Enumerate against the corpus rather than by guesswork~~ **--- superseded 2026-08-15: [LP4](#recommended-order) derives it.** Every host kind must be a node kind (clause 3 puts the root in its own node list), so `Feature` and `Area` follow from [LP0](#recommended-order) without a separate argument. Update `isEphemeraLudicGraphNode` plus `ephemeraMeta.test.ts` in the same change, exactly as CC0b did. **Closed 2026-08-19.** `EphemeraLudicGraphNode` now carries `Feature`/`Area` variants alongside the existing three, `isEphemeraLudicGraphNode` extended with matching branches, and a comment records this is not a part-of-ladder claim (mirroring `EphemeraPositionAdjacencyContainedId`'s guard). Type-level only and inert --- no factory (`featureNode`/`areaNode`) and no production path constructs one yet, matching LP4/LP2's own "nothing downstream wired up" precedent. `ephemeraMeta.test.ts` gained accept/reject cases for both new tags, a corrected "rejects unrecognized tag" case (it previously used `'Feature'`, which is now valid), and a test asserting the node-tag set and `EphemeraLudicTerminalPrimitive`'s kind set stay in lockstep. Two stale in-code comments found and fixed along the way: `ludicGraph/index.ts`'s `nodeIds` getter doc (claimed `nodeIds` differs from `thingIds` "because Feature is not a graph node," no longer true) and `ludicGraph/AGENT.md:60`'s node-tag-union parenthetical (still said `'Character' | 'Object'`, already stale before this slice since Room shipped in CC0b).
  - [X] **`Feature` is a structural extension, not a widening, and it is not symmetric with the others.** [`ludicGraph/AGENT.md`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/AGENT.md) (confirmed in conversation 2026-07-24): a Feature node participates in a **subset** of the instruction structure --- never the **subject** of a relation, never **membership-moved**, *"more like the walls of a room than its contents"*, look-at-able and possibly a relation **target**. **Do not let it inherit Object-node capabilities by sharing a union with them.** *No code change needed --- the union stays written out (not merged/aliased), per LP4's own instruction; nothing grants Feature Object-node capabilities by this widening.*
  - [X] **Re-scope *never the subject of a relation* before implementing it, because taken literally it now contradicts a decided case.** `PartOf` runs **root to part**, so `FEATURE#Wall -PartOf-> FEATURE#Niche` puts a Feature in the `from` position --- a subject. **The rule is not wrong; it is older than the vocabulary it is being read in.** It was confirmed 2026-07-24, when the only relation kinds were `'On' | 'Under' | 'Against' | 'Custom'` --- player-driven spatial relations, where the constraint means *a wall does not get put on things*. Containment kinds arrived 2026-08-09. **Re-scope it to the non-containment kinds and record why**, or someone will later "fix" the contradiction by removing containment support for Features, which is the opposite of what was decided. **This is the third instance of the same pattern in two days** --- a verdict reasoned in an earlier vocabulary being inherited into a later one --- after premise 2 and the *Part* definition. **The type-level half of this is [LP4g](#recommended-order)'s** (added 2026-08-17): a Feature cannot occupy the `from` position while the kernel's step terminals are `EphemeraObjectId`, so the re-scoping recorded here and the widening done there are one idea landed in two places. **Closed.** `ludicGraph/AGENT.md`'s `## Known limitation (deferred)` section rewritten: the rule now reads *never subject of the four non-containment kinds*, with the `PartOf`/LD-8 exception spelled out and dated, and the "Features are not graph nodes yet" framing retired since this slice makes that false.
  - [X] **Premise 11 is partly answered by this, and the verdict is owed upstream rather than here.** *Whether Room, Area or Feature belong in the part-of ladder* was explicitly unsettled; `FEATURE#Wall -PartOf-> FEATURE#Niche` settles **Feature**, and says nothing about Room or Area. **Record it in [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md) and the abstraction-layers plan**, per this plan's rule that design verdicts live upstream --- leaving it only in an implementation step is how the last three staleness bugs were made. **Already current --- checked, not re-done.** `AGENT.concepts.md:362` already records Feature settled 2026-08-15 (`FEATURE#Wall -PartOf-> FEATURE#Niche`) with Room/Area explicitly left open; this predates LP4b landing and needed no change.
  - [X] **Guard, and it is the one most likely to be violated silently (premise 11):** adding a node tag **does not** put that kind into the part-of ladder. `AGENT.concepts.md` names *"whether Room, Area or Feature belong in the part-of ladder at all"* as the inference most likely to be wrongly read in, and warns that **a uniform graph interior is not a uniform containment ladder.** Say so in the change. **Done** --- the guard comment added on `EphemeraLudicGraphNode` in `ephemeraMeta.ts` says so directly, matching the pattern already on `EphemeraPositionAdjacencyContainedId`.
- [X] **LP4g. Widen the relational kernel's step terminals --- the prerequisite for [LP4c](#recommended-order), and the step that *retires* LP4's throw-narrows rather than propagating them.** Added 2026-08-17. [`ExecutorEstablishRelationStep`/`ExecutorDissolveRelationStep`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/synthesize/executorTypes.ts) carry `subjectId`/`targetId: EphemeraObjectId`, and every kernel consumer is typed through them. **Verified 2026-08-17 by reading the whole write path end to end: not one consumer branches on entity kind.** [`computeStepSequenceFootprint`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/computeStepSequenceFootprint.ts) resolves both endpoints' hosts and adds them to the lock set; [`applyStepSequenceCore`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/applyStepSequenceCore.ts) (`:187-206`) finds each endpoint's host, throws on mismatch, and calls `applyRelationalPatch` --- already widened by LP4; [`factsForStep`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/factsForStep.ts) re-derives the host and calls [`buildObjectRelationalFact`](../../../../../lambda/ephemera/dataSource/positions/manipulation/relational/buildObjectRelationalFact.ts), a nine-field payload constructor with `Object` in its **name only**. There is no `isEphemeraObjectId`, no `OBJECT#` prefix logic, and no Object-specific validation anywhere in the path. **The Object-ness is annotation, not behaviour**, so this is a retype plus two lines --- not a design change. **Closed 2026-08-19.** `ExecutorEstablishRelationStep`/`ExecutorDissolveRelationStep.subjectId`/`.targetId` retyped to `EphemeraLudicTerminalPrimitive`; both `findHostOf` copies switched from `graph.objectIds` to `graph.nodeIds`; the `getCurrentHost` callback chain (`computeStepSequenceFootprint`, `CommitStepSequenceDeps`, and the concrete closures in `applyObjectRelationalChange.ts`/`applyObjectClearMembership.ts`/`applyObjectRoomMembership.ts`, including their `hostByReferencedId` maps) widened in lockstep --- a compile-forced chain the original bullets didn't spell out, found by widening and letting `tsc` locate every downstream site. `buildObjectRelationalFact` renamed to `buildRelationalFact` per the payoff test's own suggestion; its args and `ObjectRelationChangedPublishedPayload` (`positions/publishedEvents.ts`, plus its runtime guard) widened to match --- the published `type` string and exported payload type name left alone (load-bearing for existing stream consumers). One real, non-mechanical fork found and resolved along the way: `perception/objectManipulationPresentationLegAdapters.ts` builds its `relationalFact` leg straight off the now-widened payload, but the whole presentation/narration fan-in stack (`objectManipulationPresentationFanIn.ts`) stays `EphemeraObjectId`-only --- rather than widening that separate stack in this slice, the adapter now filters a non-Object subject/target to `[]`, mirroring its existing character-hosted-narration precedent (recorded in `ludicGraph/AGENT.md`'s BD-36 paragraph, not left silent). A second, symmetric boundary was found on the ingress side: `compileRelationalFromSkeleton.ts` converts a (now-widened) executor step back into the ingress-facing `EstablishRelationStep`/`DissolveRelationStep` (`parsePlanStep.ts`), which stays `EphemeraObjectId`-typed by design per LD-13 --- added a narrow-and-drop guard there, matching the file's existing "no fallback path today, drop the candidate" idiom.
  - [X] **The only structural Object assumption is two lines, and it is the identical defect LP4 already fixed once.** `findHostOf` --- duplicated in [`applyStepSequenceCore.ts:10-20`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/applyStepSequenceCore.ts) and [`factsForStep.ts:16`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/factsForStep.ts) --- tests `graph.objectIds.has(id)`, exactly the narrowness `bothObjectsOnGraph` had before LP4 replaced it with the kind-indifferent `nodeIds` getter. **Same fix, same getter, both copies.** **Done.**
  - [X] **The sameHost invariant generalises rather than breaking, and this is what makes the step safe rather than merely typeable.** `applyStepSequenceCore` throws when `subjectHost !== targetHost` --- the one genuinely structural rule relational edges have. Concepts clause 3 puts a host's **root node in its own node list**, so for a containment edge `ROOM#A -In-> OBJECT#Table` *both* endpoints resolve to `ROOM#A`'s graph. **Containment satisfies the invariant natively; no exemption, no special case.** **Confirmed unchanged --- no code needed here.**
  - [X] **Do this before [LP4c](#recommended-order), not after --- the ordering is load-bearing.** LP4 left five throw-on-non-Object narrows at authoritative kernel-step builders. They are correct while only spatial kinds exist and **wrong the moment a containment edge is legal**: a `PartOf` edge with a Feature endpoint would throw on a *legitimate* edge. Landing this step deletes them; landing LP4c first turns all five into live bugs. **Done --- all five deleted:** `applyObjectRelationalChange.ts`, `compilePositionKernelOp.ts`, `applyObjectClearMembership.ts`, `applyObjectRoomMembership.ts`, `executor.ts`. Each covered by a new non-Object-endpoint test proving the flow now succeeds instead of throwing.
  - [X] **This is also what dissolves the one gap that was never even a deferral.** Character relational subjects have BD-36's named gate; **Room, Feature and Area subjects had no note anywhere** --- *unassigned* in this plan's own vocabulary, the exact failure mode this plan exists to fix. [LD-8](#open-decisions-implementation--plan-only) already makes `FEATURE#Wall` as a subject a **decided, live** case, so the honest resolution is to widen rather than to write a deferral for something already decided. Do not add a gate here; close the gap. **Done at the kernel layer.** `computeCarryClosure`/`boundaryEdgeOutcomes` (`interactionUnderTransfer.ts`) and presentation-leg narration remain Object-only, but both are now explicitly recorded as such (`ludicGraph/AGENT.md`), not silently assumed.
  - [X] **Payoff test:** an `establishRelation` step with a non-Object subject commits and emits its fact, with no `isEphemeraObjectId` narrow anywhere in the path. **Consider renaming `buildObjectRelationalFact`** in the same change, or leave a note --- it is the last place the word `Object` claims a restriction the code does not have. **Done.** Renamed to `buildRelationalFact`; new Character-subject cases added to `applyStepSequenceCore.test.ts`, `factsForStep.test.ts`, and `computeStepSequenceFootprint.test.ts`. Full `dataSource/positions/` suite green (395 tests); `dataSource/positions/manipulation/` + `dataSource/actions/enrich/objectManipulation/` + `dataSource/perception/` green (808 tests); `packages/mtw-interfaces` `ephemeraMeta.test.ts` green (74 tests); `tsc --noEmit` clean.
- [ ] **LP4c. Add the containment kinds to `HostRelationalEdgeKind` --- closing the phantom edge.** The union is `'On' | 'Under' | 'Against' | 'Custom'`, so **the model cannot say *in* about anything**, and hosting's implicit *contents-of* link is *"not merely unlabeled but untypeable."* Premise 9 decided the fix on 2026-08-09 and nothing implemented it. **Two prerequisites, both recorded 2026-08-17:** [LP4a](#recommended-order) (a containment edge runs root to member, so the root must be nameable) --- **satisfied**, and [LP4g](#recommended-order) (the kernel's step terminals must admit non-Objects first, or LP4's throw-narrows fire on legitimate containment edges) --- **satisfied, landed 2026-08-19.** **One blocker remains: [LD-11](#open-decisions-implementation--plan-only)**, which is undecided --- see the bullet below. **Re-scoped 2026-08-19, and the scope grew rather than shrank:** LD-11 now waits on an **upstream** question --- whether premise 9's *root to part* restriction stands at all --- because dropping it makes a containment edge routable as an **internal** transfer edge as well as a boundary one, which the row's original framing assumed impossible. **Note the prerequisite wording above inherits the same assumption:** *"a containment edge runs root to member"* is premise 9's restriction restated, not an independent fact, so if that restriction lifts, LP4a's root remains necessary for the rooted-graph shape but stops being what makes containment edges nameable. **Split 2026-08-19 into two halves, and only one of them is gated --- the bullets below are grouped accordingly.** Checking the actual blast radius (rather than trusting this step's 2026-08-17 notes, which predate LP4a/LP4b/LP4g) found that **exactly one site breaks the build**, and it is the one LD-11 owns; everything else either fails *silently* or must deliberately not change at all. **[LP4c-i](#recommended-order) (representation) is startable now with no LD-11 dependency; [LP4c-ii](#recommended-order) (behaviour under transfer) keeps the gate.** The step keeps the name `LP4c` so existing references from [LD-5](#open-decisions-implementation--plan-only), [LD-11](#open-decisions-implementation--plan-only), [LD-12](#open-decisions-implementation--plan-only) and C14 still resolve. **LP4c-i landed 2026-08-19** --- `HostRelationalEdgeKind` now admits `In`/`PartOf`, all persistence-lane sites widened and all ingress-lane sites left narrow and commented, the classifier placeholder throws naming LD-11. **LP4c-ii (the classifier's actual behaviour) remains open and gated on LD-11**, unchanged by this slice.
  - [ ] **This step is a gate on an upstream slice, recorded 2026-08-17 --- do not treat it as self-contained.** [C14](AGENT.abstractionLayers.corpus.planning.md#c14-the-tray-its-cups-and-how-many-times-it-gets-picked-up)'s candidate (c) --- ephemeral-abstraction construction, the abstraction plan's [Phase 5(b)](AGENT.abstractionLayers.planning.md#recommended-order) and its **stronger first choice** for a first vertical slice --- turns carry closure into construction of a rooted, ported whole. Its independence criterion is *reduce the room-side attachment to exactly one `In` edge*, **so it cannot be built until `In` exists.** C14 was written 2026-08-07, two days before premise 9 decided the containment kinds, which is why that gate was missing from Phase 5(b) until now. **Sharpened 2026-08-19 by the LP4c-i/LP4c-ii split, and it clears the gate earlier than this bullet assumed:** C14's independence criterion needs an `In` edge to be **representable**, not to be carry-classified, so **[LP4c-i](#recommended-order) alone unblocks Phase 5(b)** --- it does not have to wait on [LD-11](#open-decisions-implementation--plan-only) or on premise 9's upstream question. **This is owed upstream:** Phase 5(b) and C14 Finding 6 both currently name plain "LP4c" as the gate, and should be corrected to name the representation half, or that work will look blocked for longer than it is.
  - [X] **=== LP4c-i. Representation --- startable now, no [LD-11](#open-decisions-implementation--plan-only) dependency. ===** Make the model able to *say* `In`/`PartOf`. Deliberately inert, exactly as [LP2](#recommended-order), [LP4](#recommended-order) and [LP4b](#recommended-order) each landed: the type widens, nothing authors one yet. **Independent payoff, not a manufactured split** --- [C14](AGENT.abstractionLayers.corpus.planning.md#c14-the-tray-its-cups-and-how-many-times-it-gets-picked-up) Finding 6's *"candidate (c) cannot be built before `In` exists"* needs `In` to **exist**, not to be carry-classified, so this half alone clears that gate. **Closed 2026-08-19.**
  - [X] **Eight drift sites in two lanes, and the lane decides the action --- half of them must deliberately NOT widen.** Verified 2026-08-19. The union is spelled out identically in both lanes, so **nothing in the code tells an implementer which lane a site belongs to** --- that indistinguishability is the actual hazard here, more than any single stale set. **Widen (positions/persistence lane):** (1) [`ephemeraMeta.ts:314`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts) the canonical type --- the widening itself; (2) `ephemeraMeta.ts:330` `HOST_RELATIONAL_EDGE_KINDS`, which gates `isEphemeraLudicRelationalEdgeData` (**the stored payload** --- stale here means every containment edge is silently dropped); (3) [`ludicGraph/baseClasses.ts:27`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/baseClasses.ts), the parsed in-memory view feeding `extractRelationalEdgesFromStored`; (4) [`positions/publishedEvents.ts:51`](../../../../../lambda/ephemera/dataSource/positions/publishedEvents.ts), gating `isObjectRelationChangedPublishedPayload`, so a kernel-emitted containment fact validates. **Deliberately leave narrow (actions/ingress lane, per [LD-13](#open-decisions-implementation--plan-only)):** (5) [`relationKind.ts:1`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/relationKind.ts); (6) [`actions/baseClasses.ts:337`](../../../../../lambda/ephemera/dataSource/actions/baseClasses.ts) `ParseCommandEstablishRelationResult.relationKind`; (7) `actions/baseClasses.ts:676`, the `Set<string>` guarding it; (8) [`actions/publishedEvents.ts:68`](../../../../../lambda/ephemera/dataSource/actions/publishedEvents.ts) `HostRelationalEdgeKindPublished` **and its own `HOST_RELATIONAL_EDGE_KINDS_PUBLISHED` set just below it**. **Add a comment at each of (5)--(8) saying it is narrow on purpose**, or the next widening sweep will "fix" them and quietly undo BD-2's ingress exclusion. **Closed 2026-08-19; all eight landed as specified.** **A ninth site surfaced by `tsc`, not by grep, because it referenced the type by import rather than spelling it out:** [`parsePlanStep.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/parsePlanStep.ts)'s `EstablishRelationStep`/`DissolveRelationStep.relationKind` imported `HostRelationalEdgeKind` straight from `ephemeraMeta.ts` --- ingress-lane by every other signal (`subjectId`/`targetId` are `EphemeraObjectId` there, LD-13/LP4g) but coincidentally identical to the persistence-lane type until this widening made them diverge. Repointed its import to `relationKind.ts`'s narrow type, which then surfaced two producers assigning a widened `relationKind` into it --- [`compileRelationalFromSkeleton.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/compileRelationalFromSkeleton.ts) and [`groundChange.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/synthesize/groundChange.ts) --- both fixed with the same drop-the-candidate idiom the endpoint-id guards already use, both unreachable today for the same reason (`isContainmentSpan` -> `nestingDefer`). **The lesson for LP4c-ii or any later widening sweep: grep-for-the-literal-union undercounts sites that reference the type by import; `tsc --noEmit` after the widening is the actual completeness check.**
  - [X] **Method note, because it changed the count: a one-line grep undercounts these.** `HOST_RELATIONAL_EDGE_KINDS_PUBLISHED` spells its members on **separate lines**, so a `grep` requiring `'On'`/`'Under'`/`'Against'` on one line misses it --- which is how the earlier pass reported four sets rather than five. Grep for the identifier **and** for `'Against'` alone when re-checking.
  - [X] **The one hard break still has to compile: use an explicit `throw`, not a semantic default, and give it an expiry.** `classifyInteractionUnderTransfer` (see the LP4c-ii bullet below) will refuse to compile. LP4c-i must put *something* there. **A `throw` naming LD-11 is the honest placeholder:** it is unreachable today --- [`normalizeRelationSpan.ts:53`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/normalizeRelationSpan.ts)'s `isContainmentSpan` returns `nestingDefer`, propagated through `matchRelationalTemplate` -> `compileUngroundedPlan` -> `compileRelationalFromSkeleton:88` into player-facing defer copy, so **no ingress path can author a containment edge**; it matches this codebase's fail-loud discipline (BD-33/BD-35, *"failing loudly is the point"*); and unlike returning `'defer'` or `'dissolve'` it **does not silently answer LD-11**. **Its expiry is named and is not the ingress path:** `ludicCache` is expected to author containment nesting directly (2026-08-19), so **LD-11 must be answered before ludicCache authoring lands, not before LP4c-i does.** Record the throw as owned by LP4c-ii, not as a permanent guard. **Closed 2026-08-19** --- `interactionUnderTransfer.ts` throws `classifyInteractionUnderTransfer: containment kind '${relationKind}' not yet classified (LD-11)` for both new cases, covered by a regression test asserting the throw (not a return value) for both roles.
  - [X] **Both kinds, not one.** `PartOf` alone leaves contents implicit and recreates the phantom edge for exactly the case that exposed it --- a box's crystal ball. AB-48 says *in* **and** *part of*. **Both landed together.**
  - [X] **Non-exclusive.** A thing can be *in* something and *part of* it at once. **Must not be written as a mutually-exclusive switch** anywhere, including in guards and in narration. **Verified with a test asserting both edges coexist between the same pair.**
  - [X] **The distinction lives on the edge, never on the node.** This is what keeps it compatible with the locked role claim --- *"whole and part are roles relative to a level, not kinds"*, and *"any rule that gives parts and wholes different behaviour is not a rule at all."* The crystal ball is not a different kind of object from the lid; it is joined by a different kind of edge, and it is simultaneously a whole of its own interior. **Type the edge and the claims coexist; type the node and they collide.** **No node-level change made --- confirmed by construction.**
  - [X] **Widening the union also breaks something the compiler *cannot* catch --- update [`HOST_RELATIONAL_EDGE_KINDS`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts) (`:312`) in the same change.** It is a runtime `Set<HostRelationalEdgeKind>` literal duplicating the type union, and `isEphemeraLudicRelationalEdgeData` gates on it. A `Set<T>` literal has no exhaustiveness requirement, so adding `'In'`/`'PartOf'` to the type leaves this set stale with **no compile error** --- and the guard's failure mode is `return false`, meaning **every containment edge is silently dropped from the stored payload** rather than rejected loudly. Found 2026-08-17. Add a test asserting the set and the union agree, since nothing else can. **Superseded in scope 2026-08-19 --- this bullet found one set; there are five, plus three duplicate type declarations.** See the eight-site bullet under [LP4c-i](#recommended-order) for the full list and which lane each belongs to. Two corrections to this bullet's own text while re-checking: the set is at **`:330`**, not `:312` (`:314` is the type union), and the agreement test it asks for should be written **per persistence-lane set**, since the three outside `ephemeraMeta.ts` are separate literals that can drift independently. **Closed 2026-08-19** --- all three persistence-lane sets widened, each covered by an acceptance test per kind (agreement-by-behavior, since the sets themselves are not exported), plus explicit round-trip tests for `extractRelationalEdgesFromStored`'s fallback branch and `isObjectRelationChangedPublishedPayload`'s publish-path guard, per the plan's own silent-drop-check discipline.
  - [ ] **Widening the union breaks the build in exactly one place, and that break is a decision rather than a fix ([LD-11](#open-decisions-implementation--plan-only)).** `classifyInteractionUnderTransfer` in [`interactionUnderTransfer.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/expandValidate/interactionUnderTransfer.ts) switches over exactly `'On' | 'Under' | 'Against' | 'Custom'` with **no `default`** --- exhaustive only because that is currently the whole union. Adding two kinds makes it non-exhaustive and TypeScript will refuse to compile it, **which is the good outcome**: the gap surfaces loudly. What the new cases should *return* is genuinely undecided and is not this step's to invent. **Do not add a `default` to make the build pass.** **=== This bullet is LP4c-ii. Behaviour under transfer --- still gated on [LD-11](#open-decisions-implementation--plan-only), which now waits on premise 9 upstream. ===** **Re-verified 2026-08-19 and the claim holds exactly as written:** `case 'Against'` appears in **one** file repo-wide, and both `lambda/ephemera` and `packages/mtw-interfaces` compile with `"strict": true`, so the fall-through is a real compile error rather than an implicit `undefined` return. **What changed is only that LP4c-i may now land ahead of this bullet** by putting an explicit `throw` in the two new cases (see LP4c-i's placeholder bullet) --- that is *refusing* to decide in the one place that would otherwise decide silently, not a `default`, and this bullet's prohibition stands undiluted. **Note also that `'illegal'` is not available as an answer here:** `InteractionUnderTransferOutcome` is `'dissolve' | 'carry' | 'defer'`, so [LD-11](#open-decisions-implementation--plan-only)'s option (a) (*"always `illegal`"*) is not a classifier return value at all --- it is a downstream `applyTransferSet` verdict reached *via* `carry` (`incompleteTransferSet`) or `dissolve` (`unresolvedDissolveEdge`). **Whoever settles LD-11 must translate option (a) into this three-value vocabulary before it can be implemented**; as written it names a layer this function does not speak.
  - [X] Check whether the storage row needs the column back: [`EphemeraPositionAdjacencyRow`](../../../../../packages/mtw-interfaces/ts/ephemeraPositionAdjacency.ts) is *"a from, a to, and no third column, which shows the shape was chosen rather than overlooked."* ~~**Open**: whether containment kinds ride on the graph's `edges` (no storage change) or on adjacency rows (a published change).~~ **Stale --- closed 2026-08-15 by [LD-5](#open-decisions-implementation--plan-only), and this bullet was never updated to match; found 2026-08-19 while splitting this step.** The verdict is **graph edges only, no storage change**: adjacency is the reverse *index*, not truth, and its question (*which host contains X*) is unchanged by whether X is a part or contents. **The row's missing third column stays missing --- it was "chosen rather than overlooked."** Nothing to do in LP4c; what LD-5 does require is [LP4f](#recommended-order). **Confirmed 2026-08-19 on landing LP4c-i: no storage-row change made, matching the verdict.** **Amended 2026-08-20: read *"what LD-5 does require is LP4f"* only as LD-5 wrote it --- as a pointer, not as a description of LP4f's content.** [LP4f](#recommended-order) was re-scoped that day because it had been written from this sentence rather than from the index, and quantified its invariant over containment **edges** when the adjacency row indexes node **presence**. LD-5's verdict is untouched and was never the error.
- [X] **LP4i. A structural-staleness diagnostic, an `ephemera` self-heal path --- and clause 3's missing root node as its proving case. Added 2026-08-20 (user); re-cut the same day to run *before* [LP4d](#recommended-order) and to absorb root-in-nodes.** **The framing is the user's and it is the honest one: this is catch-up. The implementation has outpaced its resiliency systems.** Each change to the graph shape so far has been paid for by **rejiggering existing table items by hand** --- [LP4a](#recommended-order)'s `rootId`, [LP4b](#recommended-order)'s node kinds, and `ports` next. **The tedium is the symptom rather than the problem: the system has no way to identify stale-structured data itself, and no way to recover from it.** Every shape change is therefore a manual migration with no detector and no repair --- a standing brittleness independent of what the shape happens to be. **Closed 2026-08-20.** Three layers landed together: (1) the root-cause fix --- a new `nodeFromId` dispatcher in [`ludicGraph/index.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/index.ts) wired into every fresh-construction factory (`empty`, `seedFromActiveCharacters`, `fromPlainHostMeta`'s default, `fromPlayEnvelope`) so a newly-built graph's root is always in `nodes`, plus `isEphemeraLudicGraphFieldPayload` (`ephemeraMeta.ts`) extended to assert root-in-nodes so a stale stored payload fails loudly at the read boundary rather than only in a diagnostic; (2) the diagnostic sweep, [`lambda/diagnostics/ludicGraphStaleStructureSweep/`](../../../../../lambda/diagnostics/ludicGraphStaleStructureSweep/), read-only, scanning all five `Meta::*` host `DataCategory`s and classifying via that same guard (a caller, not a second theory); (3) the self-heal, [`healLudicGraphStructure.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/healLudicGraphStructure.ts), idempotent and dry-run-capable, wired to the new `Ludic Graph Stale Structure Finding` on `mtw.ephemera.positions`. Full `lambda/ephemera` suite green (2677 passed, `--clearCache` run per Getting Started item 6), `lambda/diagnostics` and `packages/mtw-interfaces` suites green, `tsc --noEmit` clean in all three.
  - [X] **Why now, and the argument is a closing window rather than a preference.** The current answer to stale structure is [LPM](#recommended-order)'s clearing tool --- **wipe it** --- which is viable only because there is no data worth keeping. **That stops being true exactly once, silently, and cannot be got back.** Build the replacement while the wipe is still available as the fallback if the self-heal is wrong; afterwards there is no fallback to test against. **Landed within the wipe's window, as intended.**
  - [X] **The proving case, moved here from [LP4d](#recommended-order) on 2026-08-20 so this step has something real to find: the root is designated and absent.** Clause 3 requires exactly one root **present in the graph's own node list**, *"and therefore usable as an endpoint of relations like any other node."* **Shipped code designates it and never adds it.** Three construction paths in [`ludicGraph/index.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/index.ts): `EphemeraLudicGraph.empty()` returns `new EphemeraLudicGraph(hostId, hostId, [], undefined)` --- **its own doc comment cites clause 3 while omitting the node clause 3 asks for**; `fromPlainHostMeta`'s lazy default is `{ rootId: hostId, nodes: [], edges: [] }`; and `seedFromActiveCharacters` builds `nodes` from the active characters only, **so even a fully populated Room graph has no node for the room.** `fromFieldPayload` performs no root-in-nodes check and [`isEphemeraLudicGraphFieldPayload`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts) validates `rootId`'s **shape** only. Every fixture in the corpus matches. **So [LP4a](#recommended-order)'s `rootId` is a designation without a referent.** **Why it matters now rather than eventually:** clause 3's stated *reason* for root-in-nodes is endpoint-usability, and [LD-16](#open-decisions-implementation--plan-only) has just made containment run **member -> root** --- so every containment edge points at a node that does not exist. **[LP4b](#recommended-order) closed a referential-integrity gap in node *kinds*; this is the same class, still open, directly beneath the newest decision.** **Not a live bug** --- LPM wiped the corpus, no producer authors a containment edge, and the classifier throws on both kinds --- **so it is latent, in the direction being built.** **Closed --- confirmed as the actual bug, and it was live in one more place than named here:** `bothObjectsOnGraph` (`applyRelationalPatch`'s gate) failed for any edge naming a graph's own root as an endpoint, which is exactly the shape a containment edge to a room's own root takes; a payoff test in `ludicGraph/index.test.ts` proves it now succeeds.
  - [X] **Why this case and not a synthetic fixture, which is the whole argument for the re-ordering.** It is **real, non-synthetic staleness already in the table** --- every graph fails it today --- and its repair is **fully canonical**: `EphemeraLudicGraphNode` is `{ tag, universalKey }` and both fields fall out of `rootId` alone (tag from the id prefix, key from the id), so healing involves **zero judgement** and waits on no open row. **And it buys a null-case assertion that is available exactly once:** the table is a known-good baseline post-LPM, so *the sweep finds the root-node gap and nothing else* is provable now and **not** after `ports` lands. **Assert root-in-nodes in the payload guard, not only in tests**, so a stale stored row fails loudly at the read boundary rather than producing a graph with a dangling root. **Done** --- the guard check landed in `isEphemeraLudicGraphFieldPayload`, checked by owner via `ephemeraLudicTerminalOwner` so a port-qualified root (Stage 2/LP7, not reachable yet) is handled correctly too.
  - [X] **(a) The diagnostic sweep: given a `ComponentUUID`, judge whether its entry is stale-structured.** **Most of the judgement already exists and is uncalled** --- [`isEphemeraLudicGraphFieldPayload`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts) is precisely the predicate *is this the current shape?*, and after LP4d it also answers the root-in-nodes question. **So the sweep is a caller, not a new theory of staleness**, which keeps it honest: a row is stale iff the shipped guard rejects it, with no second definition to drift. **Precedent for the lambda shape:** `lambda/diagnostics/roomOccupancyDriftSweep` is an existing diagnostics sweep in the same table; follow it rather than inventing a harness. **Report, do not repair** --- read-only, and separately invocable. **Closed, and one clause corrected on landing: root-in-nodes is answered by *this step's own* guard change, not LP4d's** --- LP4d never touched root-in-nodes (it was moved out to here specifically so LP4d wouldn't have to). Built [`lambda/diagnostics/ludicGraphStaleStructureSweep/`](../../../../../lambda/diagnostics/ludicGraphStaleStructureSweep/) (`index.ts` + `classification.ts` + tests, matching `roomOccupancyDriftSweep`'s and `orphanedImprovisedObjectSweep`'s shape exactly), scanning all five `Meta::*` `DataCategoryIndex`s (`Room`/`Character`/`Object`/`Feature`/`Area` --- the full `EphemeraMembershipHostId` set) rather than the two `roomOccupancyDriftSweep` scans, since the staleness this sweep detects is shape-general across all host kinds, not room-specific. Emits `Ludic Graph Stale Structure Finding` (new finding type, added to `@tonylb/mtw-interfaces/ts/eventBridge/diagnostics` alongside its serializer round-trip). Wired into `dataSource/apiDiagnostics.ts`, `dataSource/subscribedEvents.ts`, `dataSource/index.ts`, `app.ts`'s direct-invoke allow-list.
  - [X] **(b) The self-heal path: keep what is good, default what is missing.** Must be **idempotent** and must have a **dry-run** mode that reports the exact write it would make before making it --- the codebase already runs that pattern on the manipulation side (`validatePlanDryRun`), and a repair pass is the case it was designed for. **Closed.** Built [`healLudicGraphStructure.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/healLudicGraphStructure.ts): given an `ephemeraId` and `{ dryRun }`, reads the stored payload, reports `{ stale: false }` unchanged (absent or already-valid), computes the scoped repair (default `rootId` to `hostId` when missing/invalid, insert the missing root node via `nodeFromId`) and either reports it (`dryRun: true`) or writes it via `ephemeraDB.optimisticUpdate` (`dryRun: false`, matching `persistClearStoredLudicGraphs`'s `{ Key, updateKeys: ['ludicGraph'], updateReducer }` idiom, whole-field replace not a partial merge). A row stale for a reason outside the healable set reports `{ stale: true, healable: false }` and is left untouched --- not force-healed. Wired to the new finding on `mtw.ephemera.positions` (`positions/index.ts`, `positions/subscribedEvents.ts`), always committing (`dryRun: false`) from that path; dry-run is for manual/operator invocation.
  - [X] **State the LPM tension explicitly, or this reads as backsliding into what LPM removed.** LPM's whole point was that a **read-boundary default** (`??= []`) makes a stale row **indistinguishable from a current one, on every read, forever** --- and LP4d's own gate bullet forbids reintroducing one. **A repair pass is the opposite construct wearing similar clothes:** it is **explicit, one-time, and it writes**, so after it runs the row genuinely *is* current and the strict guard stays strict. **The rule that keeps them distinct: the heal may only ever run from a sweep or an explicit invocation, never from a read.** **Do not let it grow a read-time fallback** --- that is LPM undone, arriving as a convenience. **Followed, and stated in code, not only here:** `fromFieldPayload` was deliberately left untouched (no default injected there); the rule is written directly on `healLudicGraphStructure`'s own doc comment and in `AGENT.contract.md`'s new subsection so it survives independent of this plan.
  - [X] **Which fields are healable is not a matter of taste, and the line is already drawn: premise 10's *recorded, never derived*.** A default is only sound where the missing value is **canonically recoverable from what is present**. **Healable:** `rootId` on a host-bound graph (`rootId === hostId` is already the invariant), and the root **node** itself, whose tag follows from the host id's kind. **Not healable: `ports`.** LP4d's own bullet 2 is explicit that `fromHostId` **has no interior witness** --- nothing inside the graph mentions who refers to it --- so a `ports` repair must read the **exterior**, which today means `EphemeraPositionAdjacency`. **That makes the `ports` half of this step depend on [AB-55](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only)/[LD-17](#open-decisions-implementation--plan-only)**, and it is a genuine finding rather than a scheduling nuisance: **the healable set and the un-healable set are separated by exactly the recorded-versus-derived line**, which is a useful check that premise 10 is drawn in the right place. **Land the structural half (root, root node) without waiting; leave `ports` repair behind those rows.** **Done exactly as scoped** --- `healLudicGraphStructure`'s repair touches only `rootId` and the root node; `ports` repair is left to LP4d/LD-17, noted in that row. **Narrowed 2026-08-23 by [LD-18](#open-decisions-implementation--plan-only), and the narrowing vindicates this bullet's own test rather than overturning it.** *Not healable: `ports`* was drawn one notch coarser than the recorded-versus-derived line it claims to follow. **Repairing a *missing* `fromHostId` does need the exterior and therefore the reverse index** --- that stays gated here. **Repairing a *disagreement* does not:** the port already names the host to read, so the check is one named fetch and a comparison. LD-18 takes the second case out of this gate and leaves the first in.
  - [X] **Not gated on [LD-15](#open-decisions-implementation--plan-only).** Detecting and repairing graph *shape* nests nothing and removes nothing from the flat catalogs, so this is available work during the flat period alongside [CC1a](AGENT.abstractionLayers.planning.md#recommended-order)/[CC3](AGENT.abstractionLayers.planning.md#recommended-order) and [LD-17](#open-decisions-implementation--plan-only)'s (a). **Confirmed --- landed with no dependency on LD-15.**
  - [X] **Go in knowing this is a behaviour-changing slice, not a read-only diagnostic --- a different risk profile from the one the step's name suggests.** Adding a root node to every graph changes **node counts everywhere**: `graph.nodeIds` is iterated by [`applyTransferSet`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/expandValidate/applyTransferSet.ts), [LP3](#recommended-order)'s matchers, and both `findHostOf` copies. `objectIds` filters by tag, so **room graphs are unaffected** (their root is a `Room`), but an object's own graph gains an Object node that is itself --- check the enumerating callers rather than assuming. **Run the full `lambda/ephemera` suite, not just `dataSource/positions/`** --- per [Getting Started](#getting-started) item 6, ts-jest caches type-check results for files a slice does not touch, which is exactly how the last node-shape change hid two broken suites. **Confirmed and one new risk found along the way:** `applyTransferSet.ts` reads `_nodes`/`characterIds`/`objectIds` directly and never calls `nodeIds`, so it was unaffected as predicted; `bothObjectsOnGraph`/both `findHostOf` copies/the `staleTransferCandidate` check all audited and covered by the existing and new test suites. **Found by running the full suite, exactly as this bullet warned:** `internalCache/ludicGraphCache.test.ts` (outside `dataSource/positions/`) broke on two counts --- four fixtures missing the root node (stale, fixed the same way as every other fixture), and a real duplicate-node bug in `fromPlayEnvelope` for Object-hosted graphs, whose root shares the projected `Object` tag with ordinary members and was being round-tripped back in twice; fixed by filtering the root's own id out of the extracted Character/Object lists before prepending `nodeFromId(hostId)`. `lambda/diagnostics/roomOccupancyDriftSweep`'s own fixtures (a different lambda package entirely) needed the same root-node fixture fix, caught by running that package's suite too.
  - [X] **Scope boundary, so this does not become a migration framework.** One component kind's `ludicGraph` shape, one guard, one repair. **Explicitly not in scope:** a schema-version marker on table items, batch/whole-table migration orchestration, and any repair of non-`ludicGraph` attributes. If a version marker turns out to be wanted, it is a design question and belongs upstream, not inside this step. **Held.** No version marker, no migration framework, no non-`ludicGraph` repair; the sweep spans all five host kinds because the `ludicGraph` shape itself is shared across them (one guard, one shape), not because the scope boundary was read as "per host kind."
- [X] **LP4d. Add the egress list --- `ports` --- as a top-level element on `EphemeraLudicGraphData`.** Premise 12's shape, unimplemented since 2026-08-06. Proposed form: `ports: { portId: string; fromHostId: <host id> }[]`. **Re-scoped twice on 2026-08-20 (user), and the second move undid the first --- recorded because the reasoning is what matters, not the boundary.** First it absorbed [locked-frame clause 3](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md#wholes-parts-and-ports)'s **root-in-nodes** half, on the finding that `ports` was never the only field missing from the minimum structure. Then **root-in-nodes moved out again, into [LP4i](#recommended-order)**, which now runs **first** --- because LP4i's own staleness rule is *a row is stale iff the shipped guard rejects it*, and with root-in-nodes sitting here LP4i would have shipped a detector with **nothing to detect** against a table [LPM](#recommended-order) had just wiped clean. **What LP4d becomes as a result, and it is the better job: the first shape change that *exercises* the resiliency discipline rather than being hand-migrated.** Extend the guard, extend the heal --- or record deliberately that the `ports` heal defers behind [AB-55](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only)/[LD-17](#open-decisions-implementation--plan-only), which is a documented decision inside a working framework rather than a gap. **Closed 2026-08-20.** `EphemeraLudicGraphPort`/`ports: EphemeraLudicGraphPort[]` (required, possibly empty) landed on `EphemeraLudicGraphData`/`EphemeraLudicGraphFieldPayload` in `ephemeraMeta.ts`, with a companion `isEphemeraLudicGraphPort` guard and an unconditional `ports` check added to `isEphemeraLudicGraphFieldPayload` (mirroring the `nodes` block, not the optional `edges` block). `EphemeraLudicGraph` (`ludicGraph/index.ts`) threads `ports` through the constructor, `empty`/`fromJSON`/`fromFieldPayload`/`fromPlayEnvelope`/`toJSON`/`toStored`/`storedRelationalEdges`/the `with*` chain, plus a new `ports` getter (inert until a producer exists, same precedent as LP4b). `healLudicGraphStructure.ts`'s `computeRepairedPayload` extended to default a missing/malformed `ports` to `[]` --- LP4d exercising LP4i's discipline exactly as scoped, rather than a second manual wipe; `ludicGraphStaleStructureSweep` picks up ports-missing rows as stale with no change (pure pass-through to the guard), confirmed by a new proving-case test in both the sweep and its classification module. All downstream production literals updated (`mtw-gateways/positions/factory.ts`'s five lazy-default sites, `interactionUnderTransfer.ts`'s carry-closure reconstruction) and the full four-package suite (`mtw-interfaces`, `mtw-gateways`, `lambda/ephemera` including integration tests and a `--clearCache` run, `lambda/diagnostics`) plus `tsc --noEmit` in all four are green.
  - [X] **The asymmetry is the point, and it must be written down where implementers will read it.** If `ROOM#A`'s graph holds a reference to `OBJECT#BOX#ac123e6`, then **`OBJECT#BOX`'s graph carries `{ portId: 'ac123e6', fromHostId: 'ROOM#A' }` and `ROOM#A`'s node carries nothing.** The association is **inherent in the reference** --- the address already names both the host and the port --- so exterior-side port data would be a denormalization of something the reader already holds. **This is not a new decision**; it is PQ-1's reversal, whose reasoning was that the ingress list *"surfaces information from one item, saving one read of an object already known to be present"* --- weak at 1:1, where the membership node is strong at N:1. **Recorded as-is on the shipped type's own doc comment; no code enforces the asymmetry since nothing writes the exterior half (out of scope, see below).**
  - [X] **Only the pairing is new information.** The `portId`s are recoverable from the interior graph's own edges; **`fromHostId` is not recoverable from inside at all**, because nothing in the interior mentions who refers to it. So `ports` joins `rootId` in the *recorded, never derived* family (premise 10), and for the same reason: the fact has no interior witness. **`EphemeraLudicGraphPort`'s doc comment states this explicitly, citing premise 10.**
  - [X] ~~**The host-id type is too narrow and this is the step that discovers it.**~~ **Already satisfied, verified 2026-08-20 --- [LP0](#recommended-order) widened it on 2026-08-16 and this bullet was never updated, so [LD-7](#open-decisions-implementation--plan-only) closes as *already answered* rather than being decided.** [`EphemeraMembershipHostId`](../../../../../packages/mtw-interfaces/ts/ephemeraPositionAdjacency.ts) reads `Room | Character | Object | Feature | Area` today, with the *"admission here is not a part-of-ladder claim"* caveat already carried in its own doc comment. The reasoning below stands as the **rationale** for a widening that has happened; do not re-do it. ~~is `Room | Character`.~~ But hosting is **recursive** --- *"the rope hosts its parts exactly as the box hosts its contents"* --- so a spring's port egresses to `OBJECT#BOX`, and an object must be a legal `fromHostId`. See [LD-7](#open-decisions-implementation--plan-only). **Carry `AGENT.concepts.md`'s warning with the widening:** the room boundary is *"a seam of different provenance"*, so a wider host id is not a claim that every level is uniform. **No further work needed --- `fromHostId: EphemeraMembershipHostId` used as-is.**
  - [X] **A port with no exterior edge is a pure presence indicator**, and the entry still exists --- PQ-1's section is explicit that *"the absence of an edge **is** the signal"*, with no second record shape. **`ports` is therefore non-empty for anything hosted anywhere**, which is the concrete, testable form of premise 2's *forced yes*. Assert it. **Blocked as written, found 2026-08-20 --- there is nowhere for the simple case's port to live.** Object graphs are **lazy**: [`fromPlainHostMeta`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/index.ts) returns `fromFieldPayload(hostId, record.ludicGraph ?? { rootId: hostId, nodes: [], edges: [] })`, so a cup in a room --- hosted by any reading --- has no stored interior graph and reads back with **empty `ports`**. The assertion is right and the **write** path is what fails it. **Scope this bullet to graphs that exist and split the write-path question out**, rather than weakening the invariant to fit the read path. (Naming collision, flagged so it is not mistaken for a connection: `fromPlainHostMeta`'s *plain* means a plain Dynamo record, unrelated to premise 2's *plain object*.) **Scoping decision held as written: no non-empty assertion added this slice** --- `ports: []` is a legal value everywhere; the write-path question (AB-55/LD-17) stays open.
  - [X] **Do not settle who is authoritative in this slice --- that is [AB-55](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only), opened 2026-08-20.** An unlinked presence port is an **in-graph reflection of an adjacency record**: both answer *contained -> host*, so the egress list contests `EphemeraPositionAdjacency` rather than the host's own graph, and **I5's *stored room ludicGraph wins* is untouched** by this step. That row leans toward adjacency becoming derived-or-deleted, on a tiny consumer surface (one read shape, three write sites) and a diagnostics lambda that exists only because the fact is stored twice --- **but it is open, and it holds an unmeasured cost objection.** LP4d adds the field; it does not retire anything. **Held --- `EphemeraPositionAdjacency` untouched, AB-55/LD-17 left open.**
  - [X] **Consistency invariant, not a derivation:** C9's *present iff at least one port egresses here* holds **between the egress list and membership**, and PQ-1 is explicit that no reader performs it as a derivation. Test it as an invariant; do not compute membership from `ports`. **Not actionable this slice: nothing writes a populated `ports` entry yet (AB-55/LD-17), so there is no real data to test the invariant against. Left for whichever slice adds a producer.**
  - [X] **Gated on [LPM](#recommended-order), same as `rootId`: required and possibly empty, with no `??= []` at the read boundary.** The empty default would have been *correct* rather than a placeholder, which is exactly why it is tempting; the reset makes it unnecessary and keeps the payload guard strict. **Satisfied without a second manual wipe:** every currently-stored row predates `ports` and is now correctly classified stale by the guard; `healLudicGraphStructure` heals it to `ports: []` (LD-17's interim posture (b), a one-time write-carrying repair, not a read-boundary default) --- LP4i's discipline doing the job LPM's clearing tool did for `rootId`/the root node.
  - [X] **Out of scope, deliberately:** the ingress half in `positionCache`. It is a cache-side denormalization resting on a provisional row, and nothing in this plan needs it. **Held --- `PlayLudicGraph`/`positionCache` untouched; confirmed `projectComponentGraphFromStoredLudicGraph`'s output does not carry `ports`.**
- [X] **LP4f. Make the adjacency row's existence an *existential invariant* rather than a lifecycle event. Re-scoped 2026-08-20 (user, from conversation) --- the title survives intact and the body did not: every bullet quantified the invariant over *edges*, and the adjacency index does not index edges.** ~~[LD-5](#open-decisions-implementation--plan-only) keeps the index kindless, which is only sound if row death follows the **last** containment edge rather than **an** edge. **Non-exclusivity is what breaks the current rule:** a hinge may be both `PartOf` and `In` the same box, so two edges map to **one** row --- same `EphemeraId`, same `DataCategory`.~~ *Original step text, retained for the record, immediately above.* **What the index actually indexes: node presence.** [`EphemeraPositionAdjacencyRow`](../../../../../packages/mtw-interfaces/ts/ephemeraPositionAdjacency.ts) is `{ EphemeraId: <contained entity>, DataCategory: 'POSITION#<hostId>' }`, and its one reader --- [`queryMembershipContainersFromDynamo`](../../../../../packages/mtw-gateways/ts/ephemera/positions/adjacency.ts) --- asks exactly one question: *which hosts hold X as a member.* Membership in a `ludicGraph` **is** node presence and nothing else: [`addObject`/`addCharacter`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/index.ts) call `withNodes` and touch no edge, and `removeObject`/`removeCharacter` *assert there is no relational edge referencing the node* before removing it --- so the normal state of a member is **edge-free**. An object lying on a room's floor is a node with no relational edge to anything and it has an adjacency row. **Edges of every kind --- containment, hosting, peer --- are orthogonal to this index, and [`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) has said so in normative text the whole time: *"Must not write adjacency rows for relational edges (forward-graph only)."*** **How the error got in, which is the durable lesson: [LD-5](#open-decisions-implementation--plan-only) ends *"what the decision does require is LP4f,"* and this step was written from that sentence rather than from the index.** LD-5 kept the index **kindless**; that was read as *a kindless index needs a rule for when a kind-bearing row dies*, when in fact kindlessness is not a hazard to defend against --- the index never counted kinds because it never counted edges. **A step generated from a verdict's phrasing instead of from the structure the verdict is about**, and then a `commitStepSequence` defect invented downstream to justify it (see the struck bullet below).
  - [X] ~~**State the rule as a predicate:** *an adjacency row exists iff **at least one** containment edge runs from that host to that member.*~~ **Superseded 2026-08-20. The predicate is over nodes:** *an adjacency row `(X, POSITION#H)` exists iff **X is a node in H's `ludicGraph`***. **Still not a refcount, and now trivially so** --- a node is present or it is not, so there is nothing to tally and the "at least one" hedge the original needed disappears with the edges it ranged over. **This is the one part of the step that is real work**, and it is documentation work: the *iff* is implemented but nowhere written as an iff. `AGENT.contract.md` states the halves separately (graph + adjacency in the same `transactWrite`; `ludicGraph` wins on conflict; no adjacency dual-write for relational edges) without ever closing them into a single existential rule --- which is precisely the gap that let this step be written over the wrong structure. Land it as a contract clause, per [`taskPlanning/AGENT.md`](../../../../AGENT.md)'s *"add falsifiable rules to `AGENT.contract.md`"*. **Landed 2026-08-20** --- a new bullet on [`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md), immediately after the character-route *"`ludicGraph` wins"* clause, states the *iff* over node presence, cross-references the existing *"must not write adjacency rows for relational edges"* line rather than duplicating it, and carries the forward note from the bullet below.
  - [X] **Audit that the invariant holds today --- done 2026-08-20 as part of the re-scope, and it does.** Every non-test caller that mutates `_nodes` is `applyTransferSet.ts` or `applyStepSequenceCore.ts`, both reached only from a `transferMembership` step, which is exactly the step [`commitStepSequence.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/commitStepSequence.ts) filters on when building adjacency items. The one other hit, [`compileRelationalFromSkeleton.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/compileRelationalFromSkeleton.ts) (`:274`), builds a throwaway simulation graph for cycle detection and never persists. **Keep the audit as a bullet rather than deleting it: its value is that it is re-runnable, and it is the check that would have caught the original framing.**
  - [X] **Note it is the same shape as a rule this design already has:** PQ-1's C9 invariant is *present iff at least one port egresses here*. Both are existential, both hold **between** a graph and a derived structure, and neither is a derivation any reader performs. **Corrected 2026-08-20: implement them as one *idea*, not as one mechanism.** The analogy is about the shape of the rule, not a shared quantity --- C9 genuinely ranges over a list (`ports`, where "at least one" is load-bearing), adjacency ranges over node presence, where it is not --- see the struck predicate above. **Reading the analogy as *both count things* is how this step acquired its refcount framing in the first place.** **No separate action needed** --- the correction is the note itself; nothing in the landed contract clause states the two as one mechanism.
  - [X] ~~**Fix [`commitStepSequence.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/commitStepSequence.ts) (`:154`), which encodes the old rule.** It pairs an unconditional `Delete` of the row at `fromHostId` with a `Put` at `step.toHostId`. **Sound under a move** --- leaving a host takes all its edges --- **and quietly wrong under a dissolve** that drops one containment kind while the other survives: the row goes, the member stays in the graph, and reverse lookup loses it.~~ **Struck 2026-08-20: the defect is not real, and `:154` is correct as shipped.** The adjacency items are built from `steps.filter((step) => step.kind === 'transferMembership')`, so a `dissolveRelation` step contributes **no adjacency item at all** and never reaches `:154`. [`applyObjectRelationalChange`](../../../../../lambda/ephemera/dataSource/positions/manipulation/relational/applyObjectRelationalChange.ts) confirms it from the producer side: an unrepaired dissolve emits `steps = [relationalStep]`, one step, zero adjacency writes. The unconditional `Delete` is paired with a step that by definition removes the node from the source graph, so it is right, not "sound under a move and wrong under a dissolve." **The bullet's own supporting quote pointed the other way and was read past:** `roomOccupancyDriftSweep`'s header says *"does not detect stale adjacency without a graph **node**"* --- the shipped code has always described this invariant in nodes; this step is what reworded it into edges.
  - [X] **Note the future coupling, which is the one live thing the struck bullet was near.** When [AB-54](AGENT.abstractionLayers.planning.md#settled-register)'s hosting-as-shard actually lands, dissolving a hosting edge (`On`/`In`/`PartOf`) **will** mean a membership change --- the object leaves the tray's shard and rejoins the room --- so a dissolve that today emits no membership step will then have to emit one. **The correct fix at that point is to route it as a `transferMembership` step so adjacency keeps following nodes; it is *not* to teach adjacency to inspect edges.** That distinction is the whole content of this step, and it is worth writing into the contract clause above rather than left here, since the plan file is disposable and the trap is not. **Held behind [LD-15](#open-decisions-implementation--plan-only)'s gate** (the constructor stays flat until `ludicCache` serves referent search), so there is nothing to build now. **Landed 2026-08-20** --- carried into the contract clause's forward note verbatim in substance, so the trap survives independent of this plan file.
- [X] **LP4j. Retire *containment kind* as a named subset of relation kinds, then sweep the type contracts that still encode the superseded partition.** **Opened 2026-08-20 from conversation, ordered immediately after [LP4f](#recommended-order). Landed 2026-08-20** --- the vocabulary rule shipped on `AGENT.concepts.md`, LD-12 got its future-obligation note, and all four sweep-hit sites (`ephemeraMeta.ts`, `interactionUnderTransfer.ts`, and the three ingress-lane doc comments across `relationKind.ts`/`publishedEvents.ts`/`baseClasses.ts` x2) were reworded. Documentation and commentary only, as scoped --- no type or behaviour change except the one `Error` message string, whose trigger condition is unchanged. **The finding: this design carries two terms of art for one mechanism, and [AB-54](AGENT.abstractionLayers.planning.md#settled-register) already said so in its own verdict --- *"Hosting and containment are one mechanism, and relation kinds partition in two."*** **Containment kind** was fixed at `{In, PartOf}` by [AB-48](AGENT.abstractionLayers.planning.md#settled-register) on **2026-08-09**; `On` joined the same mechanism on **2026-08-19**, and AB-54 calls that *"the whole of what is new."* **The widening landed under a new label instead of on the existing one, so the older term kept its narrower extension in the same documents that state the merge.** **The risk, and why this is a step rather than a note: keeping both terms alive invites code and docs to *codify* the drift** --- to encode a distinction that a settled verdict has already dissolved, or to keep tracking the obsolete grouping because a type still names it.
  - [X] **State the rule, in [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md):** **hosting kind / peer kind is *the* partition of relation kinds**, and **there is no second, narrower partition.** Where a doc or a comment genuinely means the pair `In`/`PartOf`, **name the two kinds explicitly** rather than reaching for a collective noun --- an unnamed pair cannot silently acquire a third member, which is exactly how this drift happened. **Keep the word *containment* for the phenomenon** (AB-53's *"the containment subgraph is a star"* is a real claim and stays true once `On` joins); it is **containment-kind-as-a-named-set** that retires, not the word. **Landed 2026-08-20** --- appended to the existing Hosting kind / peer kind row.
  - [X] **Do not let the merge orphan `Part` and `Contents`.** Those rows are defined off `PartOf` and `In` and give `On`-hosted members no noun. **They are per-kind *narration* vocabulary, not a partition** --- concepts.md already insists the distinction *"lives on the edge, never on the node"* --- so they need no collective term and no third sibling invented for `On`. **Say that in the rows themselves**, or the next reader re-derives a structural split from a narration one, which is this step's own failure mode running a second time. **Checked 2026-08-20, no change needed** --- both rows already name `PartOf`/`In` explicitly rather than reaching for a collective noun.
  - [X] **Landed 2026-08-20** --- note appended to LD-12's row, and the same pointer added to `detectRelationalCycle.ts`'s docblock. **Record the future obligation on [LD-12](#open-decisions-implementation--plan-only)** (cycle detection), which the merge reaches directly: [`detectRelationalCycle`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/synthesize/detectRelationalCycle.ts) takes `kind: 'On' | 'Under'` and describes them as *"the two kinds whose real-world semantics imply a hierarchy that cannot loop back on itself"* --- **a third grouping, which cuts across AB-54's partition** (`On` hosting, `Under` peer) and excludes both kinds LD-12 is about. **The obligation to record, not to resolve here:** when LD-12 is finally scoped it must decide what the **`On` arm** becomes, not only whether `PartOf` joins. If AB-54's target holds, an `On` edge is root-incident inside its host's shard and an `On` edge-cycle is unrepresentable for the same reason the row already records for containment --- so the `On` arm is **iteration-1 scaffolding for the flat constructor**, and `Under`, a peer kind, is left as the only genuine edge-cycle case. **LD-12 already anticipates the shape of this** (its closing note moves the likely fix from a kind-filtered DFS to a host-nesting membership invariant); what it does not yet say is that its own insulating parameter encodes the retired partition.
  - [X] **Then sweep the code for converged or obsolete type contracts --- and run the sweep in that order, after the vocabulary is settled, so there is a rule to sweep against.** Three confirmed starting points, found 2026-08-20 while opening this step (**not an exhaustive inventory --- the step owes its own grep**):
    - [X] **[`ephemeraMeta.ts`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts)'s canonical `HostRelationalEdgeKind`** annotates its own union with the superseded split --- `'On' | 'Under' | 'Against' | 'Custom'` marked *spatial, player-driven*, `'In' | 'PartOf'` marked *containment*. **This is the highest-value hit: the partition is inscribed on the canonical type declaration, and AB-54's real partition cuts it in a different place** (`On` belongs with `In`/`PartOf`, not with `Under`). Comment-only, no type change --- the union's *members* are right, its *grouping commentary* is a year-zero artifact. **Landed 2026-08-20** --- inline comments now read `On: hosting kind (AB-54); Under/Against/Custom: peer kinds` and `hosting kinds (AB-54), non-exclusive (premise 9)`.
    - [X] **[`detectRelationalCycle`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/synthesize/detectRelationalCycle.ts)'s `kind: 'On' | 'Under'` parameter** --- an actual type union encoding the third grouping. **Do not change it in this step**; it is LD-12's to scope, and today's behaviour is correct for a flat constructor. Record it, point it at LD-12, leave it. **Landed 2026-08-20** --- docblock pointer added, type left untouched.
    - [X] **[`interactionUnderTransfer.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/expandValidate/interactionUnderTransfer.ts) (`:47`)** throws `containment kind '...' is a hosting kind` --- **a message that only parses if the two terms differ**, and its stated justification (*a hosting kind puts the subordinate node in its host's own shard, so the edge is not on the exterior graph*) **applies verbatim to `On`, which the same switch classifies normally four lines above.** **The `On` behaviour is not the bug** --- AB-54 is marked *not yet enforced anywhere in code*, [LD-15](#open-decisions-implementation--plan-only) holds the constructor flat, so `On` edges genuinely do live on exterior graphs today. **The bug is the justification:** the throw's real ground is *iteration 1 has no producer for `In`/`PartOf`*, and pinning it to the target-state shard argument is what makes one mechanism read as two. Re-justify on iteration-1 grounds and keep the throw. **Landed 2026-08-20** --- message now reads *"'`${relationKind}`' has no producer on an exterior graph in iteration 1 (AB-53/AB-54); reaching here means a producer built a graph the constructor does not author"*; no test asserted the old string.
  - [X] **Expect false positives, and know them before starting --- the ingress-lane narrows are not drift.** [`relationKind.ts`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/relationKind.ts), [`actions/baseClasses.ts`](../../../../../lambda/ephemera/dataSource/actions/baseClasses.ts) and [`actions/publishedEvents.ts`](../../../../../lambda/ephemera/dataSource/actions/publishedEvents.ts)'s `HostRelationalEdgeKindPublished` all narrow to the pre-AB-48 four, **deliberately, per [LD-13](#open-decisions-implementation--plan-only)/BD-2, and they stay narrow.** **But their doc comments are a real hit and the subtlest one in this step:** each says *"containment (`In`/`PartOf`) must not parse into `establishRelation`."* **Once *containment kind* means what *hosting kind* means, that sentence starts asserting that `On` must not parse either --- which is false, and would be a live bug the moment someone believed the comment and narrowed the union.** **Rewrite them to name `In` and `PartOf` explicitly**, and note that the exclusion is an ingress-lane authoring restriction, not a statement about hosting. **This is the clearest instance of the risk this step exists for: merging the term carelessly would *create* the defect the merge is meant to prevent.** **Landed 2026-08-20** --- all four sites reworded to name `In`/`PartOf` explicitly and state the ingress-lane-restriction framing; the narrows themselves untouched.
  - [X] **Out of scope, stated so the sweep does not grow into them:** resolving [LD-12](#open-decisions-implementation--plan-only) (record only); widening or narrowing any ingress-lane union (LD-13 stands); anything behind [LD-15](#open-decisions-implementation--plan-only)'s gate, including making AB-54's partition actually enforced in code --- **this step aligns the vocabulary and the commentary with a verdict already settled, and ships no behaviour change.** **If a sweep hit turns out to require a behaviour change, that is a finding to record, not a licence to widen the slice.**
- [X] **LP4e. Refresh [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md), which is stale on exactly this. Taken out of sequence 2026-08-20 (user), ahead of [LP4d](#recommended-order) --- it is documentation-only, it sits entirely on the safe side of [LD-15](#open-decisions-implementation--plan-only)'s gate, and the vocabulary had just changed twice under it. Closed 2026-08-20.** **Two of the three staleness claims in these bullets were themselves stale and needed no work** --- the `In`/`PartOf` vocabulary (**Part** and **Contents** as non-exclusive sibling rows) and the port-record split were **both already recorded** in `AGENT.concepts.md`, dated 2026-08-15, before this step ran; the [Progress](#progress) note claiming they remained open was written from this step's bullets rather than from the file. **Same class of defect this step exists to fix, one level up: a *plan* describing a durable doc it had not re-read.** **What was actually stale, found by reading the file:** (a) *"`HostRelationalEdgeKind` is still `'On' | 'Under' | 'Against' | 'Custom'` in shipped code, so the model cannot yet say *in* about anything"* --- false since LP4c-i, now recorded as closed with the ingress-lane exclusion noted so the widening is not over-read as live; (b) **`EphemeraMembershipHostId` is `Room | Character`** asserted in **two** places (the Abstraction-Fractal departures table and the room-boundary warning) --- false since LP0 on 2026-08-16, and the more interesting of the two, because the departure it supports (*earned below the room, aspirational above it*) **survives the correction** --- a wide host union is not an earned ladder, and the entry now says so rather than resting on a type that no longer reads that way. **Added, as new target-model content rather than staleness repair:** the **hosting kind / peer kind** vocabulary row (AB-54), and the paragraph recording that root-to-part is an **iteration-1 constructor discipline** rather than a structural claim (AB-53), including what would make dropping it cheap (AB-5's mint/move/dissolve) and the two things that currently rest on the star topology. **Surfaced while writing it and deliberately not answered here: [LD-16](#open-decisions-implementation--plan-only)**, the hosting-kind direction disagreement. ~~It still lists~~ *Original bullet text, retained for the record:* it still lists *"what relation kind joins a root to its members --- the model has no containment kind at all"* as **unsettled**, and still defines **Part** as any non-root node reached by *a* containment edge, with no sibling term for contents. **Both were superseded on 2026-08-09 by AB-4 and AB-48 and the update never propagated.** Add **Contents** as the sibling vocabulary row, and state that the two kinds are non-exclusive.
  - [X] **Already done before this step ran --- checked 2026-08-20, no change needed.** The entry records the split as *"settled 2026-08-06, corrected here 2026-08-15 because this entry still listed it as open and a reader believed it"*, with the asymmetry spelled out. **The bullet below is the 2026-08-15 correction's own description, not a live task.** ~~Same file, same failure, a third instance: it lists *"where a port record's two halves are stored"* as unsettled.~~ *Original text:* it lists *"where a port record's two halves are stored"* as unsettled. [PQ-1 answered it on 2026-08-06](AGENT.abstractionLayers.proposals.planning.md#the-port-record-split-complementary-with-the-interior-authoritative-settled-2026-08-06) --- egress list on the interior graph, authoritative; no ingress half in truth. **Record the answer and the asymmetry**, since a reader who believes the doc will reasonably put port data on the referencing node. **This is the same failure mode this plan exists to fix** --- a design answer that never reached the durable doc, invisible until someone read the doc and believed it. It caused a live misreading on 2026-08-15.
- [X] **LP5. Answer premise 7 --- *is there ever a pass-through with no nameable mediator?*** ~~A fiction and representation question, not a reducer question, so it is answerable now and it **closes or opens LP7 before LP7 is scheduled**. Give the conduit a node and *look in the tube* becomes a move a player can make; leave it bare and it does not.~~ **Record the verdict upstream on the `ludicGraph` side, not here.** **Landed 2026-08-20, from conversation, as documentation only --- no code, and the fork above was never taken.**
  - [X] **The verdict, and it is a premise rejection rather than a branch choice.** The question presumes the cable reaches *through* the box, so that two exterior edges terminate at box ports and something inside must join them. **It does not.** `OBJECT#Cable` is a member of `OBJECT#Box`'s `ludicGraph` *and* of the room's --- **multi-hosted membership carries "the cable passes through the box" entirely** --- so the box's boundary is never crossed by the connectivity and there are no port terminals to join, bare or mediated. **Neither of LC7's two bullets is what anyone would build**, which is a stronger result than the case anticipated (it expected to dissolve into its own first bullet, but that bullet also puts the mediator inside the box). **Recorded upstream at [LC7](AGENT.abstractionLayers.ludicCache.corpus.planning.md#lc7-the-cable-that-passes-straight-through-the-box)** per this step's own instruction, with [LC8](AGENT.abstractionLayers.ludicCache.corpus.planning.md#lc8-the-spring-in-the-box-and-in-the-contraption) and [LC gap 6](AGENT.abstractionLayers.ludicCache.corpus.planning.md#coverage-gaps-recorded-up-front) updated in lockstep.
  - [X] **The mediator question relocates rather than vanishing, and lands on an already-settled row.** With the cable multi-hosted, what remains is whether an exterior edge terminates at `OBJECT#Cable` or at `OBJECT#Cable#{port}` leading inbound to a minted span-part --- **a decomposition question about the cable, not a pass-through question about the box.** [C7](AGENT.abstractionLayers.corpus.planning.md#c7-ariadnes-thread)'s H1 clause 3 already answers it on the bystander test: **spanning things are modelled as parts**, because one multi-hosted node means reaching into the box grabs the *entire* cable.
  - [X] **Not general, and the limit is stated rather than glossed.** LC8 reaches the same bare-port-to-port shape through a **containment diamond**, where the membership reframe does not obviously transfer, and LC7's own text requires the two be worked together. **LC8 is now the sole remaining route to a bare port-to-port edge**; until it runs this is the cable's answer.
  - [X] **LD-3 removed from [Open decisions](#open-decisions-implementation--plan-only)** --- resolved to its *"always nameable --- no bare port-to-port edge"* option, by the route above. Row deleted per the base framework rather than graduated; the verdict lives at LC7.
  - [X] **A rendering concern was raised and parked upstream, not resolved here.** Reading the connection appears to need a walk *down* into `OBJECT#Cable` and back *up* to its hosts. **For connectivity the cost is illusory** --- at room level the cable is one node, and C7's Test 2 has a relation on a constituent **port through** the whole, so reading the cable alone already reports a boundary-crossing relation exists, *omittably*. **What is genuinely open is presence projection** (*where all is this cable*), which is C7's "three live options, no choice made" plus AB-6 at the presence level. Recorded at LC7 because the split --- **connectivity free, presence open** --- was written down nowhere.
  - [X] **Failure mode, and this is the second instance in four days.** LP5 was generated from a corpus case's **edge-shaped setup** rather than from the membership structure that actually models the case --- the same class [LP4f](#recommended-order)'s re-scope named on 2026-08-20 (*a step generated from a verdict's closing pointer rather than from the structure the verdict was about*). **Two instances is a pattern, not a coincidence: this plan's steps are being derived from the framing of the document that raised them.** The cheap check, worth running when a step is opened: *what structure is this quantified over, and is that the structure the system actually uses?* Noted also that the first answer offered in the 2026-08-20 conversation made the identical error, arguing for a mediator from the one-interior-edge-per-port rule --- **still reasoning from edge shape.**

**Stage 2 --- gated on P7. Do not start these before the merge-reduce proposal lands.** **Narrowed 2026-08-22: the gate is [LP6](#recommended-order)'s (the port record's shape depends on P7's reducer), not [LP7](#recommended-order)'s (a mechanical edge-terminal type widening with no port-record dependency of its own) --- LP7 landed ahead of P7 on that basis; see the Progress table's Stage 2 row.** **Narrowed again 2026-08-23, to nothing: the gate is released for [LP6](#recommended-order) too.** Both requirements it was drawn over are discharged; see the recheck under [*What is not blocked, and must go first*](#why-this-plan-exists). **What survives is the reverse dependency** --- premise 6's both-halves lookup is an obligation P7 must honour when it is written. **Third time this gate has been read at a coarser grain than its own justification supports** (LP7 on 2026-08-22, LP6's whole scope, and the `kind` field within it), which is the `overbroad-inheritance` pattern doing what it was minted to catch.

- [X] **LP6. ~~Design and build the port record~~ Narrowed 2026-08-15 --- most of this moved to [LP4d](#recommended-order) and is no longer gated.** Premise 12 already fixes the record's shape and premise 6 falls out of it, so there is nothing left to *design* here. **What remains gated on P7 is only the interior-side lookup discipline** --- that both halves are consulted, in whichever order the reducer needs, and that neither direction is quietly built one-way. ~~If P7 lands and adds no requirement beyond premise 12, **this step closes empty**, which is the expected outcome.~~ **That expectation is void as of 2026-08-21: this step does not close empty, and the requirement came from the presence plan rather than from P7.** [PR-10](AGENT.presence.planning.md#settled-register) settled that presence may be left implicit for a **single-*presence*-ported** whole --- and **ports carry relational pass-throughs as well as presence**, so `ports.length` is not that count and `EphemeraLudicGraphPort` has **no discriminator** to make it one. **Settled in conversation the same day: add a kind field to the port record**, so a graph can answer *how many buckets do I have* from inside its own shard rather than by fetching its parent. **What LP6 now owes:** the field's shape; the fact that it is a requirement **beyond premise 12** and so widens the record LP4d shipped; validation of the interior kind against the exterior edge's class, which are **two facts that must agree** in the same shape as the existential invariant; and ~~one vocabulary shared with [PR-4](AGENT.presence.planning.md#settled-register)'s partitioning edge class, since the port kind and the edge class are the two halves of one distinction and must not be named twice.~~ **Resolved 2026-08-22 by [PR-11](AGENT.presence.planning.md#settled-register): the field's shape *is* the vocabulary-sharing mechanism** --- `kind: HostRelationalEdgeKind`, the same type the edge's own `kind` field carries, not a second enum needing to agree with it by convention. There is no separate naming step left for LP6 to do; typing the field is the whole of "sharing the vocabulary." **The interior-authoritative reading is inferred from `fromHostId`'s precedent, not settled** --- arbitration is open. **Read [PR-10](AGENT.presence.planning.md#settled-register)'s discriminator note before building this.** ~~Note the sequencing: it is a presence-side decision landing in a ports-side slice, so the vocabulary is PR-4's to fix and LP6's to implement.~~ **What remains PR-4's to fix is narrower than "the vocabulary": only the literal `'Present'` itself, which must land in `HostRelationalEdgeKind` before LP6 can construct a presence-marked port.** **Cleared 2026-08-22: `'Present'` shipped ([PR-4](AGENT.presence.planning.md#settled-register)).** LP6's own blocker is gone --- what remains is LP6's own work, unaffected by this slice: the field's shape (`kind: HostRelationalEdgeKind`) on `EphemeraLudicGraphPort`, interior/exterior kind-agreement validation, and the still-open interior-authoritative-reading arbitration. **Restated 2026-08-23, because three clauses above are now stale and one open question closed.** **(i) The field is three fields, not one.** [PR-11](AGENT.presence.planning.md#settled-register) landed reading (B): `kind`, plus an **exterior** and an **interior** `Custom` payload, both required non-empty when `kind` is `'Custom'`. The single-`kind` phrasing above predates the flashlight/cord case ([PR-C2](AGENT.presence.corpus.planning.md)), which widened the answer rather than confirming it. **(ii) *"The interior-authoritative reading is inferred ... arbitration is open"* is closed.** [Premise 12](#premises-and-where-each-is-recorded)'s 2026-08-22 amendment scoped it: `kind` is interior scope, `fromHostId` and the exterior label are exterior scope, and the conflict rule is conditional --- **compare where comparison is possible; where no exterior reference exists the stored value stands.** That is explicitly **not** a witness requirement. **(iii) The disagreement *response* is settled 2026-08-23 by [LD-18](#open-decisions-implementation--plan-only)** --- repair on write, tolerate-and-report on read, heal out of band via a targeted sweep. **So what remains for LP6 is code, with no design question in front of it:** the three fields on `EphemeraLudicGraphPort`; the guard requiring both labels for `Custom`; the kind-agreement check made **conditional on an exterior edge existing**; LD-18's sweep and self-heal; and two comment repairs --- `ephemeraMeta.ts`'s port doc comment (*"no interior witness"* survives; *"the interior-authoritative half"* needs the scoping) and [`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md)'s *"Not healable ... `ports`"* bullet. **The P7 gate on this step was rechecked 2026-08-23 and released --- LP6 is not blocked, and may be scheduled now.** The gate's own justification names premises 6 and 7 and nothing else; both are discharged, and premise 6's remainder (*both halves looked up*) is a discipline on the reducer with no LP6 code able to violate it, so it inverts into an obligation on P7. Full argument at [*What is not blocked, and must go first*](#why-this-plan-exists); the *only consumer* premise is stale on two counts recorded there. **Split three ways 2026-08-23 (user), and LP6 is now the types alone.** The bullet above bundled work with very different readiness: the fields are single-record and local, while **the kind-agreement check is cross-record and therefore has no read-boundary home at all** --- it compares an interior port against an exterior edge, and a read of one graph does not hold the other, so it cannot live in a payload guard the way [LP4i](#recommended-order)'s root-in-nodes check does. **What stays in LP6:** the three fields on `EphemeraLudicGraphPort`, and the shape guard requiring both labels when `kind` is `'Custom'` --- inert type machinery in the same rhythm as [LP2](#recommended-order)/[LP4](#recommended-order)/[LP7](#recommended-order), with no producer needed. **Moved to [LP6a](#recommended-order):** [LD-18](#open-decisions-implementation--plan-only)'s sweep and self-heal. **Moved to [LP6b](#recommended-order):** validation and write-time repair, which genuinely wait on a producer. **Also LP6's, because it is a one-line type-shape consequence:** `ephemeraMeta.ts`'s port doc comment (*"no interior witness"* survives; *"the interior-authoritative half"* needs premise 12's scoping). **Trimmed 2026-08-23 (user): TWO fields, not three.** [PR-11](AGENT.presence.planning.md#settled-register) is amended --- `kind` plus the **exterior** relation label; the interior label is cut, on locality (`ports` and `edges` are sibling fields of one stored attribute, so the interior label is a filter over data the reader already holds) and on **interior fan-out** (single-use quantifies crossings, not interior degree, so *the* interior label is not a single value). **The guard shrinks with it:** `kind === 'Custom'` requires the **exterior** label, non-empty --- a one-for-one mirror of [`ephemeraMeta.ts:351-353`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts), not a doubling. **LANDED 2026-08-23.** [`ephemeraMeta.ts`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts): `EphemeraLudicGraphPort` gains `kind: HostRelationalEdgeKind` (required) and `exteriorRelationLabel?: string`; `isEphemeraLudicGraphPort` gains the kind check --- **reusing the module's existing `HOST_RELATIONAL_EDGE_KINDS` Set rather than minting a fourth copy** --- and the `Custom` conditional, mirroring the edge guard one-for-one. **The label is named `exteriorRelationLabel`, not `relationLabel` (user, 2026-08-23):** with the interior half trimmed, a scope-neutral name sitting beside an interior-scoped `kind` would re-create exactly the conflation [PR-11](AGENT.presence.planning.md#settled-register) was amended to remove. **`kind` is required, and that is a stricter guard than the shipped one:** there is no producer and **no port literal anywhere in non-test source**, so there is no legacy shape to tolerate, and an optional `kind` would leave `ports.length` ambiguous --- the defect the field exists to remove. **No plumbing changed:** `EphemeraLudicGraph` carries ports opaquely (stored, copied by the getter, round-tripped), and `healLudicGraphStructure`'s port filter simply gets stricter for free. **Verified:** `tsc --noEmit` clean; `ephemeraMeta.test.ts` 111 passing (new cases: missing `kind`, out-of-union `kind`, an `it.each` over the six label-free kinds, `Custom` with/without/empty label, non-string label on a non-`Custom` port); `dataSource/positions/` 47 suites / 419 tests passing, including a `Custom`-port round trip through `toJSON`/`fromJSON` carrying its label. **Doc repaired:** the port type's comment no longer calls the record *the interior-authoritative half* --- it now says the record mixes scopes, with `fromHostId`'s *no interior witness* clause kept verbatim. [`ludicGraph/AGENT.md`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/AGENT.md)'s `get ports()` gloss updated. **Deliberately NOT done here, and not owed by this step:** [`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md)'s *"Not healable ... `ports`"* bullet, which is [LP6a](#recommended-order)'s to re-scope.
- [X] **LP6a. [LD-18](#open-decisions-implementation--plan-only)'s mismatch sweep and self-heal --- built on the types alone, and deliberately *ahead* of the producer. Added 2026-08-23 (user).** **Buildable now:** the sweep reads stored rows and compares a port's recorded exterior values against the edge held by the host the port itself names. It needs [LP6](#recommended-order)'s fields to exist and needs no producer. **Why early is the point rather than a scheduling convenience, and [LP4i](#recommended-order)'s own argument is the precedent:** LP4i built its detector *before* `ports` landed, on the ground that *"the table is a known-good baseline post-LPM, so **the sweep finds the root-node gap and nothing else** is provable now and **not** after `ports` lands."* **The same window is open one shape-change later** --- while no producer exists, *the sweep finds nothing* is provable, and the producer's first port is then covered from its first write rather than after a backfill. **Recorded because this plan argued the opposite earlier the same day and cited LP4i while doing it:** the objection was that a sweep with nothing to find *"encodes a theory nobody checked."* That is an argument for testing the classifier hard against fixtures, **not** for shipping it late --- and LP4i's bullet says so directly, in the sentence quoted above. **Shape, per LD-18:** a **separate** sweep, not an extension of [`ludicGraphStaleStructureSweep`](../../../../../lambda/diagnostics/ludicGraphStaleStructureSweep/) --- that one is honest because it is a *caller* of the shipped guard, one row in and a verdict out, and a mismatch needs two graphs. Follow [`roomOccupancyDriftSweep`](../../../../../lambda/diagnostics/roomOccupancyDriftSweep/) (already cross-record), with its own finding type on `@tonylb/mtw-interfaces/ts/eventBridge/diagnostics` plus a serializer round-trip, and the **inline `EventBridgeClient`** the sibling sweeps use rather than the shared wrapper. **Self-heal:** idempotent, dry-run-capable, and **never invoked from a read** --- the rule already shipped at [`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) and `healLudicGraphStructure.ts:96`, and LD-18 is a second finding routed through it, not a second mechanism. **Scope boundary, drawn where LD-18 drew it:** repairs a **disagreement with a named referrer** only. A port missing `fromHostId`, or one whose named referrer holds no matching edge at all, asks *who **should** refer here* --- reverse-index work, still behind [LD-17](#open-decisions-implementation--plan-only). **Owed to shipped docs in this step:** re-scope the contract's *"Not healable ... `ports`"* bullet to that narrower gate, and give the new sweep its own subsection beside LP4i's. **Unaffected by the 2026-08-23 trim, and the trim makes this step *cleaner*.** Everything the sweep compares is now denormalized-**exterior** (`fromHostId` and the exterior label) against the referring edge in the named host's graph --- **one comparison shape, all of it cross-shard, none of it with a local witness.** Under the three-field reading it would also have owed an interior-label check against a local edge: a second, differently-shaped comparison inside one classifier, which is exactly the *second theory of staleness* this step declined when it refused to fold into `ludicGraphStaleStructureSweep`. **Unblocked 2026-08-23: LP6's fields shipped**, so the values this sweep compares (`fromHostId`, `kind`, `exteriorRelationLabel`) now exist on the record. This step is next. **LANDED 2026-08-23.** Sweep [`ludicGraphPortMismatchSweep/`](../../../../../lambda/diagnostics/ludicGraphPortMismatchSweep/) (own finding type `Ludic Graph Port Mismatch Finding` with serializer round-trip, inline `EventBridgeClient`, operator-invoked, five-category scan), self-heal [`healLudicGraphPortMismatch.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/healLudicGraphPortMismatch.ts), consumer branch in `positions/index.ts` beside LP4i's, and the detail-type added to `template.yaml`'s `EphemeraFunction` rule. **The finding names the port, not the row (user, 2026-08-23):** `{ ephemeraId, portId }`, because a mismatch is one crossing being wrong and triage needs to know which. **One definition of *disagreement*, and it lives in neither caller:** the comparison is [`classifyLudicGraphPortMismatch`](../../../../../packages/mtw-gateways/ts/ephemera/positions/classifyLudicGraphPortMismatch.ts) in `mtw-gateways`, used by the sweep **and** re-run by the heal --- the `classifyAuthoredCatalogDrift` precedent. Two copies would have been two theories of mismatch, which is the exact failure this step refused when it declined to fold into `ludicGraphStaleStructureSweep`. **The rule the classifier settles, and it was not in the plan text:** a mismatch needs the matching exterior edges to be **unanimous**; a fan that disagrees with *itself* is reported but carries **no correction**, and the heal reports it `healable: false` rather than picking an edge to believe. Single-use quantifies crossings, so a split fan is broken exteriorly and repairing it would invent an answer. **Four gated non-findings, each pinned by a test:** no matching edge, absent referrer graph, shape-stale referrer graph (that is the *structure* finding, which orders the two sweeps instead of duplicating them), and an edge naming the same owner but a different port. **Verified:** `tsc --noEmit` clean; `mtw-gateways` 34 suites / 262 tests (20 of them the new classifier fixtures); `lambda/diagnostics` 16 suites / 115; `mtw-interfaces` diagnostics 81; `positions/` 48 suites / 431. **Docs:** [`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md)'s *"Not healable ... `ports`"* bullet **re-scoped** (a named-referrer disagreement is healable out of band; a missing `fromHostId` or an unreferenced port stays reverse-index work) plus a new self-heal subsection; [`lambda/diagnostics/AGENT.md`](../../../../../lambda/diagnostics/AGENT.md) sweep section beside LP4i's; `ludicGraph/AGENT.md` module map. **Still open, and this step does not touch it:** [LP6b](#recommended-order)'s write-time repair, which travels with the producer.
- [ ] **LP6b. Kind-agreement validation and write-time repair --- aligned with the producer, not scheduled here. Added 2026-08-23 (user).** **Why this half and not [LP6a](#recommended-order)'s waits:** validation is for *producers and consumers*, and write-time repair is a producer behaviour --- both need code that holds an interior port and an exterior edge in the same operation, which is exactly what does not exist while `ports` is *"inert until a producer exists"* ([`ludicGraph/AGENT.md`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/AGENT.md)). **Who the producer is remains [LD-17](#open-decisions-implementation--plan-only)'s open question** (*does every hosted thing get a materialized interior graph carrying its presence port, and who writes it?*), with the kernel rewrite the likely answer. **Schedule this step with that one rather than in this stage** --- and if the producer arrives in a different step than expected, this step moves with it. **What it must implement, all already decided and none of it re-opened here:** the kind-agreement check made **conditional on an exterior edge existing** (premise 12's 2026-08-22 amendment --- *compare where comparison is possible; where no exterior reference exists the stored value stands*, explicitly not a witness requirement); and [LD-18](#open-decisions-implementation--plan-only)'s **repair-on-write** --- a mismatch noticed while the records are being rewritten is fixed in that write. **The one thing left to specify at implementation time**, and it is ordering rather than design: the sequencing inside a single write when the write is *to the port* rather than to the exterior edge. Premise 12's amendment already fixes which **value** is right. **Quantifier widened 2026-08-23:** the kind-agreement check is the port against **every edge incident to it**, interior and exterior --- not *the* interior edge. A port's interior degree is unbounded (single-use governs crossings, not interior edges; a `Present` port fans out across its bucket). **Cheap** --- the interior edges are a filter on the same payload --- and **well-defined precisely because `kind` passes through**, so N incident edges must carry N equal values. **The condition under which that stops holding is already PR-11's re-open trigger** (*one port fronting both a `Present` edge and a non-`Present` one*), so this check failing at interior grain is a design signal, not just a data fault.
- [X] **LP7. Widen edge terminals to admit port addresses**, in whatever shape LP6 lands. **Closed 2026-08-22.** ~~**Conditional on LP5:** if there is never a pass-through without a nameable mediator, the bare port-to-port edge is not needed and this step shrinks.~~ **The conditional was wrong, and is retired 2026-08-20 on landing [LP5](#recommended-order).** LP5 answered *no bare port-to-port edge* --- **and this step does not shrink at all.** Every bullet below concerns port-*qualified* terminals in general, not the both-ends-are-ports case: a node-to-port edge is still entirely real (`AGENT.concepts.md`'s own `PowerCord -[ThreadsInto]-> FLASHLIGHT:1`), so the two silent `typeof` pre-checks, the missing union guard and the round-trip regression are all needed either way. **Recorded because the plan promised a payoff that does not exist**, and a reader sequencing on LP5 would have expected one. ~~Gated on [LD-14](#open-decisions-implementation--plan-only) for its *scope*, recorded 2026-08-17 and unchanged --- the stored terminal's form decides which direction two shipped guards get fixed in, and they get fixed in opposite directions depending on the answer.~~ **Ungated 2026-08-22: [LD-14](#open-decisions-implementation--plan-only) was already decided by PQ-9 the same day it opened and had simply gone unnoticed.** The stored form is the structured `{ owner, port }` map, so this step's direction is fixed: delete the two `typeof === 'string'` pre-checks (they reject a valid object), ship the missing `isEphemeraLudicTerminalId` union guard, and add the round-trip regression test.
  - [X] **Two `typeof === 'string'` pre-checks stand between a port address and validation, and both fail *silently*.** [`isEphemeraLudicRelationalEdgeData`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts) (`:322`) and the legacy fallback parse in [`baseClasses.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/baseClasses.ts) (`:69-73`) both read `typeof x === 'string' && isEphemeraLudicTerminalPrimitive(x)`. A port address is an object, so the pre-check rejects it and the guard returns `false` --- **the edge is dropped from the payload, not thrown on**, and in the fallback it is simply never pushed. **Do not confuse these with [`isEphemeraLudicTerminalPrimitive`'s own `typeof`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts) (`:242`), which is definitional** --- the primitive union *is* a string union --- **and must not be touched.** **Closed 2026-08-22:** both pre-checks deleted and replaced with `isEphemeraLudicTerminalId`, `isEphemeraLudicTerminalPrimitive` left untouched.
  - [X] **Both pre-checks are already redundant today, and the history is the point.** `isEphemeraLudicTerminalPrimitive` does its own `typeof` internally, so they add nothing. [LP3](#recommended-order) added them because the guard *then* called `isEphemeraObjectId`, which **crashed** on a non-string; [LP4](#recommended-order) swapped in the crash-safe helper and **kept the pre-check anyway** as the conservative call. So LP3 correctly traded a loud crash for a quiet `false` --- and that same trade is what makes this step's failure invisible instead of obvious. **The general lesson worth carrying: a hardening fix that converts a crash into a safe rejection also removes the signal, and the removal outlives the reason.**
  - [X] **`isEphemeraLudicTerminalId` exists as a type but has no guard --- LP2 never shipped one.** [`ephemeraMeta.ts:261`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts) declares the union; `isEphemeraLudicTerminalPrimitive` and `isEphemeraLudicPortAddress` both exist, the union guard does not. Add it here. With it, each site is **one** edit: swap the call and delete the `typeof` pre-check *together*, since `isEphemeraLudicPortAddress` already object-checks safely. Deleting the pre-check without swapping the call is the failure mode to avoid. **Closed 2026-08-22** --- `isEphemeraLudicTerminalId` added beside the other two guards in `ephemeraMeta.ts`, also swapped in for `EphemeraLudicGraphFieldPayload`'s own inline `rootId` check (`isEphemeraLudicGraphFieldPayload`), which had been validating the union by hand since LP4a rather than waiting for this guard.
  - [X] **Regression test the drop, not just the accept.** The hazard is a valid edge vanishing, so the test that matters asserts a port-qualified edge **survives** a stored-payload round trip --- both through `isEphemeraLudicRelationalEdgeData` and through `extractRelationalEdgesFromStored`'s fallback branch. An accept-only test on the primary path would pass while the fallback silently ate the edge. **Closed 2026-08-22.** Added to `ephemeraMeta.test.ts`: a dedicated `isEphemeraLudicTerminalId` describe block, and the field-payload test that used to assert clean *rejection* of a port-qualified terminal (pre-LP7 behaviour) now asserts *acceptance*, plus a both-ends-port-qualified case and a still-rejects-malformed case. Added to `ludicGraph/index.test.ts`: `extractRelationalEdgesFromStored` survival on the primary path (a well-formed port edge now satisfies `isEphemeraLudicRelationalEdgeData` directly) and, separately, through the manual fallback branch specifically (forced there with a `Custom`-kind edge carrying an empty `relationLabel`, which fails the primary guard's non-empty-label rule but not the fallback's looser one --- the one input shape that actually exercises the fallback's own fixed pre-check). `edgeReferencesObjectId`'s existing port-qualified test flipped from asserting rejection to asserting it finds the edge by owner.
  - [X] **Found while widening, not named by this row's own bullets: the field-type widening itself.** The three bullets above fix the two guards, but a widened guard checking a field still typed `EphemeraLudicTerminalPrimitive` is dead code --- this row's own title ("widen edge terminals") requires widening `from`/`to` on `EphemeraLudicRelationalEdgeData` (`ephemeraMeta.ts`), `HostRelationalEdge` (`ludicGraph/baseClasses.ts`), and its pre-existing, deliberately-not-consolidated duplicate in `manipulation/types.ts` (see LP4's own blast-radius note) from `EphemeraLudicTerminalPrimitive` to `EphemeraLudicTerminalId`. Done, and the blast radius was checked by type reference before widening (LP4's own method), then confirmed complete by `tsc --noEmit`, which surfaced eleven more sites than the direct check found: five kernel-step *producers* that flow a boundary/carry-closure edge straight into a `dissolveRelation` step's still-`EphemeraLudicTerminalPrimitive`-typed `subjectId`/`targetId` (`executor.ts`, `compilePositionKernelOp.ts`, `applyObjectRelationalChange.ts`, `applyObjectClearMembership.ts`, `applyObjectRoomMembership.ts` --- each fixed by filtering to primitive-endpoint edges before building the step, since no producer can build a port-qualified boundary edge yet, matching the ludicGraph/AGENT.md BD-36 precedent); four sites calling the **string-only** `isEphemeraObjectId` directly on a now-wider terminal without a `typeof` guard first (`interactionUnderTransfer.ts` x3, `applyTransferSet.ts`, `detectRelationalCycle.ts` --- fixed by adding the guard back, the same idiom `ephemeraMeta.ts`'s own pre-LP7 hardening used); and `bothObjectsOnGraph` (`ludicGraph/index.ts`), whose parameter type was narrower than its own body already assumed (it already resolved a terminal to its owner via `ephemeraLudicTerminalOwner` before this slice) --- widened to `EphemeraLudicTerminalId` rather than narrow-and-drop, since node-presence-by-owner is exactly the semantics a port-qualified edge terminal needs and PR-4's presence work depends on it working. **A second bug found by the same check, not by `tsc` (a type error would not have caught it): `EphemeraLudicGraph.equals()` compared `edge.from`/`.to` with raw `!==`** --- the identical "raw `===`" shape [LP3](#recommended-order) swept everywhere else, missed here because `from`/`to` were always strings until this slice. A port address is an object, so two structurally-identical port terminals built as separate literals now compare unequal by reference; fixed by routing through `ephemeraLudicTerminalsEqual`, matching `edgesMatch`. Both fixes covered by new tests in `ludicGraph/index.test.ts`. Full `dataSource/` suite green (292 suites, 2347 passed, 8 pre-existing skips); `packages/mtw-interfaces` green (98 tests); `tsc --noEmit` clean on both packages.
- [ ] **LP8. Unblock CC1.** Return to the abstraction-layers plan, clear CC1's blocked-by line, and confirm CC1b's port-identity seam still reads correctly against what was built.
- [ ] **LP9. Update checkboxes, Progress and Verification in this document** as the last step of each slice, after tests pass.

## Open decisions (implementation --- plan only)

| # | Decision | Blocks | Options | Recommendation |
| --- | --- | --- | --- | --- |
| **LD-1** | ~~Where does the port-aware discriminator live?~~ | ~~LP2, and LP3 through it~~ | ~~Published, beside the tagged-id family in `baseClasses.ts`; or local to `positions/`~~ | **Decided 2026-08-17, and not either listed option: `ephemeraMeta.ts`.** `EphemeraLudicTerminalId` belongs beside the ludicGraph types it discriminates, not beside the generic tagged-id family --- `ephemeraMeta.ts` imports from `baseClasses.ts`, so the reverse would need a new import cycle or the ludicGraph domain leaking into generic id infra. Still published, which was the underlying reasoning; just not `baseClasses.ts` |
| **LD-2** | ~~Parse-time or comparison-time port resolution?~~ | ~~LP3~~ | ~~Resolve once at parse; resolve at each comparison~~ | **Decided 2026-08-17: at comparison.** Parse-time discards the port, which is the information the reducer exists to use. LP3's five matcher sites now route through LP2's `ephemeraLudicTerminalRefersTo`/`ephemeraLudicTerminalsEqual`/`ephemeraLudicTerminalOwner` rather than raw `===`/`Set.has` |
| **LD-4** | Does the terminal widening go out before ports, or in one release with them? | LP4 sequencing | Ship LP4 alone; or hold it and ship the whole widening together | **Ship alone.** It is forced independently of P7, and bundling it makes a settled change wait on an unsettled one. **Restated 2026-08-15** --- this row said *the room widening* when LP4's scope was rooms only; it now covers every host kind |
| **LD-5** | ~~Do containment kinds ride on the graph's `edges`, or on `EphemeraPositionAdjacencyRow`?~~ | ~~LP4c~~ | ~~Graph edges only; or restore the third column~~ | **Decided 2026-08-15: graph edges only --- no storage change.** Adjacency is the **reverse index**, not truth (`AGENT.contract.md`: *"On graph/adjacency conflict, `ludicGraph` wins"*), and its question is *which host contains X* --- unchanged by whether X is a part or contents. **Putting the kind in the index would make it answer a question it was never asked**, and is the *materialise-a-detail-into-the-coarse-layer* shape the proposals file has already caught itself in twice. The row's missing third column stays missing; it was *"chosen rather than overlooked."* **What the decision does require is [LP4f](#recommended-order)**. **Amended 2026-08-20 --- the verdict stands, and this closing pointer was misread downstream.** [LP4f](#recommended-order) was written from this sentence rather than from the index, taking *the index stays kindless* to mean *a kindless index needs a rule for when a kind-bearing row dies*. It does not: the index never counted kinds because **it never counted edges at all** --- an adjacency row is the reverse of **node presence** in a host's `ludicGraph` (`addObject`/`addCharacter` touch `_nodes` only; a member's normal state is edge-free). LP4f was re-scoped over nodes that day. **Recorded here because the failure is a *plan step generated from a verdict's phrasing instead of from the structure the verdict is about*** --- adjacent to, but distinct from, this plan's four *verdict inherited into a later vocabulary* instances (see [LD-16](#open-decisions-implementation--plan-only)). **Second instance 2026-08-20, and it makes this a pattern rather than a one-off: [LP5](#recommended-order)** was quantified over a corpus case's **edge-shaped setup** (two exterior edges terminating at box ports) rather than over the membership structure the system actually uses for a spanning object. **The generalisation both instances support:** a step inherits the *shape* of whatever document raised it, and the framing survives into the step even when the structure underneath is different. **The cheap check when opening a step --- *what structure is this quantified over, and is that the structure the system actually uses?*** **Root cause identified 2026-08-20 and it is not a plan-hygiene problem --- see [AB-57](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only), which owns it.** For the LP5 instance specifically, the framing recurs because a **crossing is real, useful, edge-shaped, and currently neither represented nor derivable**, so the edge is the only structure that would work --- a **desire path**, not carelessness. **This row keeps only the plan-hygiene observation (steps inherit their raising document's shape) and the two-instance count; the mechanism and its resolution are AB-57's**, deliberately, since this row is closed and lives in a plan whose resolved rows are deleted on disposal |
| **LD-8** | ~~Does `Feature` join `EphemeraMembershipHostId`?~~ | ~~LP0's scope~~ | ~~Widen to five; or to four and add Feature when a case needs it~~ | **Decided 2026-08-15: yes, widen to all five.** The case that was missing arrived --- **`FEATURE#Wall` hosts `FEATURE#Niche` with a `PartOf` relationship.** A wall is exactly the sort of thing that has a niche as a part, and the niche is exactly the sort of thing that is not an object and cannot be picked up. **Note it forces [LD-6](#open-decisions-implementation--plan-only) as well**, since the member is itself a Feature |
| **LD-10** | `owner`, `whole`, or `host` for the port address's base field? | LP4, LP2 | `owner` (recommended in the declarations); `whole`; or `host` for symmetry with `hostId` | **Not `host`.** `hostId` means *the graph's owner*, `ports[].fromHostId` means *the exterior host referred in from*, and this means *the whole that allocated the port* --- a third `host` makes it the fourth sense in one file. **Three of the last four defects here came from a term read in a later vocabulary than it was written in**, so the cheap fix is a distinct word |
| **LD-9** | Does `Area` ship with this plan, or as its own slice? | LP0 scope | Take it with `Object` and `Feature`; or split it out | **Split it out --- and it did, landing 2026-08-16 as its own slice, same day as the rest of LP0.** It is the only one of the five that mints a new component identity (`EphemeraAreaId`, an `EphemeraId` tag, a guard), and **nothing in this plan needs an Area host** --- `Object` is forced by ports, `Feature` by LD-8, Area by neither |
| **LD-7** | ~~What type is `ports[].fromHostId`?~~ | ~~LP4d~~ | ~~Keep `EphemeraMembershipHostId` (`Room \| Character`) and forbid object-hosted ports; or widen to admit `EphemeraObjectId`~~ | **Decided *widen* --- and closed 2026-08-20 as *already satisfied* rather than newly decided.** [LP0](#recommended-order) widened `EphemeraMembershipHostId` to `Room \| Character \| Object \| Feature \| Area` on **2026-08-16**, **before this row was ever picked up** --- so the verdict was implemented by a step that never cited it, and the row then spent four days offering a fork whose narrow option no longer existed. **[LP4d](#recommended-order)'s own bullet inherited the stale claim and asserted `Room \| Character` in the present tense**; struck 2026-08-20. **The pattern, and it is this plan's most frequent one:** a row stating a fact about shipped code, not re-checked against the code before being relied on --- see [Getting Started](#getting-started). *Verdict, as it stood, retained as the rationale for a widening that has happened:* **Widen.** Hosting is recursive --- *"the rope hosts its parts exactly as the box hosts its contents"* --- so a spring's port egressing to `OBJECT#BOX` is the ordinary case, not an exception. `AGENT.concepts.md`'s *"seam of different provenance"* warning travels with the change rather than blocking it, and the shipped type now carries that caveat in its own doc comment |
| **LD-11** | Do containment edges participate in carry-closure, and how? | ~~**LP4c --- blocking**~~ **Nothing, as of 2026-08-19 --- largely dissolved rather than answered; see the resolution at the end of this cell** | (a) Never a carry candidate --- containment is structural, so a boundary containment edge is always `illegal`; (b) always carries --- a part moves with its whole; (c) split by kind --- `PartOf` carries, `In` does not | **None yet, and this is the one genuinely blocking decision for [LP4c](#recommended-order).** Opened 2026-08-17. `classifyInteractionUnderTransfer` ([`interactionUnderTransfer.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/expandValidate/interactionUnderTransfer.ts)) switches over exactly the four spatial kinds with **no `default`**, so LP4c breaks the build there --- loudly, which is what should happen. What is undecided is what the new cases return. **Note where containment actually lands, because it is counter-intuitive:** a containment edge can never be *internal* to a transfer --- [`applyTransferSet.ts:61`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/expandValidate/applyTransferSet.ts)'s filter needs **both** endpoints in the transfer set, and a containment root (a Room or Feature) is never in one --- so it is **always** a boundary edge routed to `boundaryEdgeOutcomes`. Every containment-under-transfer interaction funnels through this single switch, which is what makes one decision sufficient. **Widened 2026-08-17 --- containment is not the only pending consumer of this switch, and deciding it containment-only will not hold.** [C14](AGENT.abstractionLayers.corpus.planning.md#c14-the-tray-its-cups-and-how-many-times-it-gets-picked-up)'s Finding 5 (2026-08-07) reads *"`Against` and `On`-subject dissolving is very likely still correct regardless of ports"*; [the tranche-B back-sweep](AGENT.abstractionLayers.proposals.planning.md#the-parts-and-ports-back-sweep-2026-08-10) (2026-08-10) reads *"a rope `Against` a tree is **untied** by recognizing the snare"*, cited as this classifier deciding **wrongly**. **Same edge kind, opposite verdicts, three days apart, and neither document reconciles them.** The candidate reconciliation (C14 Finding 7, recorded 2026-08-17): C14's beat is a **physical displacement** --- the tray really moves, so `Against` really should break --- and the back-sweep's is **recognition**, where nothing moved and a level was interposed around things that stayed put. **If that holds, the classifier needs to know *why* the boundary moved, which is a new *input* rather than two new cases** --- materially bigger than widening the switch, and the reason this row must be decided with both consumers in view. **Take the two back-sweep cases (rope/tree, lantern/stick) as inputs to this decision alongside the containment cases**, or ephemeral-abstraction construction ([Phase 5(b)](AGENT.abstractionLayers.planning.md#recommended-order), AB-41) will reopen it. **Re-scoped 2026-08-19 --- this row's central premise is conditional on a premise that is itself now in question, and the three options above no longer partition the space.** Conversation surfaced that premise 9's **root to part** restriction may not hold: `ludicCache` is expected to produce containment **nesting** rather than a flat root-to-child fan, and no type-level or invariant-level reason to force the restriction was found. Checked specifically against `sameHost` --- the invariant most likely to bite --- and it does not depend on the root: two ordinary members of one graph satisfy it exactly as trivially as a root and a member ([LP4g](#recommended-order)'s own bullet argued this *via* the root, but the root is not what makes it true). **Three consequences, all verified against shipped code 2026-08-19:** **(i) This row's "always a boundary edge" claim is false without the restriction.** [`applyTransferSet.ts:68-93`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/expandValidate/applyTransferSet.ts) moves an edge whose **both** endpoints are in the transfer set **intact** --- stripped from the source graph, re-added to the destination --- with `classifyInteractionUnderTransfer` **never consulted at all**. Under root-to-part that path is unreachable for containment (the root is never in a transfer set, which is exactly what this row asserted); node-to-node makes it reachable, so containment routes through **two** paths and settling the switch alone settles half the question. **(ii) The candidate answer is not option (b) as written, but `On`'s existing rule mirrored.** `On` runs content->surface (`glass -On-> tray`) and reads `movedRole === 'subject' ? 'dissolve' : 'carry'`. `In` runs container->content (premise 9's own worked example is `room -In-> thread`), so the *same* semantics on the opposite arrow direction is `subject ? 'carry' : 'dissolve'` --- the box moving carries the money, the money being taken out dissolves the edge. **The classifier returning `carry` is also what populates the transfer set that makes (i)'s internal path fire**, so the two are one mechanism in sequence rather than competing answers. **(iii) [LD-12](#open-decisions-implementation--plan-only) sharpens from hypothetical to reachable** --- see that row. **What this row now waits on:** whether premise 9's *root to part* stands. **That is an upstream verdict --- now [AB-53](AGENT.abstractionLayers.planning.md#settled-register), opened 2026-08-19, with the trigger recorded on [AB-4](AGENT.abstractionLayers.planning.md#settled-register)'s verdict cell --- not this plan's** --- per this plan's own rule that a premise which looks wrong is fixed upstream rather than restated here. **Also opened 2026-08-19 and deliberately not answered:** if node-to-node containment lands, box-and-money becomes representable **two** ways --- Box as its own membership host with an interior graph (already shipped: LP0 made `Object` a legal `EphemeraMembershipHostId`, and `graphFromMeta` dispatches `Meta::Object`), or an `In` edge on the room's own graph. [H3 clause 1](AGENT.abstractionLayers.proposals.planning.md#h3-ports-as-the-boundary-mechanism-decomposition-as-a-modellers-choice) makes decomposition a modeller's choice **by design**, so this is not a defect --- but something must say which representation a given fiction gets, or the `ludicCache` rebuild ([CC1](AGENT.abstractionLayers.planning.md#recommended-order)) meets the same situation in two shapes. **The user holds a position on this that is not yet recorded, and it may belong upstream rather than in this plan** --- capture it before LP4c's authoring paths exist, not after. **That position was captured 2026-08-19, and it dissolves most of this row rather than choosing among (a)/(b)/(c).** **Upstream verdict: [AB-53](AGENT.abstractionLayers.planning.md#settled-register) resolved (root-to-part stands; nesting comes from nesting graphs) and [AB-54](AGENT.abstractionLayers.planning.md#settled-register) opened and decided (hosting and containment are one mechanism; `On`, `In` and `PartOf` are **hosting kinds** that put the subordinate node in the superior's own shard, while `Under`, `Against` and `Custom` are **peer kinds**).** **What that does to this row, point by point:** **(1) The question "do containment edges participate in carry-closure" stops having a subject.** A hosting edge is never present in the exterior graph, so `classifyInteractionUnderTransfer` never receives `In` or `PartOf` --- **LP4c-i's placeholder `throw` is therefore not a placeholder but a permanent invariant assertion**, and its comment should be corrected from *"replace before ludicCache authors containment nesting"* to say so. **(2) Consequence (ii)'s mirrored-`On` candidate answer is withdrawn**, not adopted: there is no classifier case to give it to. **And its premise was wrong twice over --- [LD-16](#open-decisions-implementation--plan-only) (2026-08-20) found that `In` does *not* run container->content**; containment runs member -> root, the same subject-first direction as `On`, so the *"same semantics on the opposite arrow"* reasoning had no opposite arrow to work with. **(3) Consequence (i)'s two-paths worry lapses** --- it was conditional on node-to-node containment, which AB-53 declined. **(4) `carry` disappears from `InteractionUnderTransferOutcome` entirely** --- `On`/target was the only `carry`, and `On` is now a hosting kind --- so the union shrinks to `'dissolve' \| 'defer'` and `computeCarryClosure` collapses from a BFS-to-fixpoint into a read of the shard. **This is the largest code consequence in the row and it is a deletion, which is why it is believed.** **And the deletion is not a bet on root-to-part surviving:** if multi-level graphs ever arrive, [AB-5](AGENT.abstractionLayers.planning.md#settled-register)'s **mint the whole, move the whole, dissolve the whole** reproduces carry behaviour without any traversal to generalise --- so the traversal is being retired because something better already exists, not because the current topology happens to make it unnecessary. **(5) The two-representations question closes: there is one representation, hosting.** **What genuinely survives, and must not be read as closed:** the **`Against` reconciliation** (C14 Finding 5 versus the tranche-B back-sweep) --- `Against` stays a peer kind, so this verdict does not touch it. **Re-homed 2026-08-20: that half is now [AB-56](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only)**, split out as [AB-54](AGENT.abstractionLayers.planning.md#settled-register)'s graduation precondition and carrying the full argument --- the re-hosting lead (recognition forms a Snare shard and the rope moves into it, rather than the edge dissolving) **must be tested against rope/tree and lantern/stick rather than assumed.** This row remains the *implementation-side* home; the design verdict is owed upstream at AB-56. **And the sequencing, which is the reason nothing in this list is startable as code today: see [LD-15](#open-decisions-implementation--plan-only).** **This row stays open, narrowed to the `Against` half, rather than being struck through** |
| **LD-12** | Does `PartOf` need cycle detection, and who owns it? | Nothing yet --- [LP4c](#recommended-order) surfaces it; **[LP4j](#recommended-order) owes this row a note, not a resolution (opened 2026-08-20)** | Widen `detectRelationalCycle`'s kind parameter to cover it; add a separate part-of-ladder check; or argue containment cycles are structurally impossible and assert that instead | **None yet.** Opened 2026-08-17. [`detectRelationalCycle`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/synthesize/detectRelationalCycle.ts) is insulated from containment **by accident rather than by design** --- it takes `kind: 'On' \| 'Under'` as a parameter and filters to it, so containment kinds simply never reach it. But a `PartOf` hierarchy can cycle (`A PartOf B PartOf A`) and nothing checks. **Do not read the existing insulation as an answer to the question**; it predates the question. Related to premise 11's *whether Room, Area or Feature belong in the part-of ladder*, which is owed upstream by [LP4b](#recommended-order). **Sharpened 2026-08-19, and the insulation turns out to be structural rather than accidental after all --- but only while premise 9's root restriction holds.** Under *root to part*, every containment edge emanates from the graph's single root, so the containment subgraph is a **star**, and a cycle needs an edge between two non-root nodes: cycles are **unrepresentable within one graph**, which is the real reason nothing checks. **If the root restriction is dropped (see [LD-11](#open-decisions-implementation--plan-only)), `A PartOf B PartOf A` becomes representable and this row stops being optional.** The fix stays small --- [`detectRelationalCycle`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/synthesize/detectRelationalCycle.ts) is already a generic DFS parameterised by `kind` and only ever filters to it, so widening the parameter is the whole change --- but it moves from *deferred* to *required*, and it acquires an owner it does not have today. **Updated 2026-08-19, and the row changes shape rather than resolving --- deliberately not answered here.** [AB-53](AGENT.abstractionLayers.planning.md#settled-register) kept the root restriction, so **the conditional above does not fire yet**: while the constructor builds root-to-part, the containment subgraph within a single graph is a star, `A PartOf B PartOf A` is unrepresentable, and `detectRelationalCycle`'s insulation holds --- so this row stays optional **in the form it is written in**. **But that insulation is now known to be a *constructor* property rather than a structural one** (AB-53 resolved for iteration 1 and explicitly declines to lock the restriction), so **it is a property to re-check whenever the constructor changes, not one to rely on indefinitely** --- which is a weaker guarantee than the *"structural rather than accidental"* reading recorded above, and supersedes it. **But [AB-54](AGENT.abstractionLayers.planning.md#settled-register) relocates the hazard rather than removing it.** If a hosting edge puts a node in its host's shard, then the cyclic arrangement that becomes representable is not an edge cycle but a **host-nesting cycle** --- the tray's shard holds the cup and the cup's shard holds the tray. **That is a membership invariant, checked over `Meta::*` host containment, not a kind-filtered DFS over one graph's edges**, so the *"widening `detectRelationalCycle`'s kind parameter is the whole change"* fix recorded above is **no longer the likely fix**. **Whether it is cheaper or dearer is unassessed** --- it is plausibly cheaper, since membership already has a single write path, but that is a guess and this row should not be closed on it. **What this row needs before it can be scoped: AB-54's implementation gate ([LD-15](#open-decisions-implementation--plan-only)) to clear**, since nothing can author a nested host until then and the invariant has nothing to guard. **Note added 2026-08-20 per [LP4j](#recommended-order), not a resolution:** `detectRelationalCycle`'s `kind: 'On' \| 'Under'` parameter is a **third grouping**, cutting across AB-54's hosting/peer partition (`On` hosting, `Under` peer) rather than tracking it. Whenever this row is finally scoped, it must decide the **`On` arm** too, not only whether `PartOf` joins --- if AB-54's target holds, an `On` edge is root-incident inside its host's shard and an `On` edge-cycle is unrepresentable for the same reason already recorded above for containment, so `On` is likely **iteration-1 scaffolding for the flat constructor** and `Under`, a peer kind, is the only genuine edge-cycle case left. This sharpens the shape already anticipated above (kind-filtered DFS → host-nesting membership invariant); it does not change which gate the row waits on |
| **LD-13** | Is BD-2's containment exclusion about *ingress*, or about *persistence*? | [LP4c](#recommended-order), and [LP4g](#recommended-order)'s framing | Ingress-only --- parsing must not route containment language to `establishRelation`, but the kernel may persist containment edges; or persistence-level --- containment needs its own operator end to end | **Ingress-only, recommended --- and the evidence is [LP4g](#recommended-order)'s.** Opened 2026-08-17. [`AGENT.contract.md:297`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) reads *"`In`, `inside`, and other containment language --- **must not** persist as `establishRelation` v1; actions routes to future **nested container** operator with player-facing defer copy."* Read as an **ingress** exclusion --- do not let *put the key in the box* get parsed into an `establishRelation` before containment semantics exist --- it is sound and probably still right. **Read as a claim about the persistence layer, it mandates duplicating a kernel that is already kind-agnostic** (LP4g verified the whole write path branches on nothing), i.e. it mandates the second and more complicated kernel this row exists to prevent. **Whichever way it lands, record that *nested container operator* is named exactly once in the entire corpus, with no owner and no step** --- if it survives as a real thing it needs one, and if it does not, BD-2's sentence should stop pointing at it |
| **LD-14** | What form does a port-qualified terminal take **in the stored payload** --- structured map, or `#`-separated string? | **[LP7](#recommended-order)'s scope --- gating** | Store the structured `{ owner, port }` map, matching the domain type exactly; or store PQ-9's `#`-separated string form and parse at the serde boundary | ~~**None yet.** Opened 2026-08-17... **This is PQ-9's question, not this plan's, and the verdict is owed upstream** --- but LP7 cannot be scoped until it is answered, which is the dependency this row exists to make visible~~ **Decided 2026-08-22, from conversation: this row was stale, not open.** [`isEphemeraLudicRelationalEdgeData`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts) validates `EphemeraLudicGraphFieldPayload` --- **the stored shape** --- so it sits astride the serde boundary [LP1](#recommended-order) deliberately deferred: the file's own comment (`:232-234`) says the domain-layer terminal is structured and does no parsing, and *"the serde/Dynamo string boundary is deferred to a later slice."* **PQ-9 already answered this, the same day this row opened:** *"Graduated to a decision 2026-08-17, via `ludicGraphPorts` LP1 --- the terminal is now structured, so the separator governs only the serde/Dynamo string form, deferred behind a gate (the first time a port address must stand free of an edge). Within that scope, `#` is decided... Outside that scope there is nothing to decide yet, because the domain layer does no parsing at all."* **So the stored payload is the structured `{ owner, port }` map**, matching the domain type exactly; the `#`-separated string form is decided **for a later, not-yet-reached serde boundary**, not for this row's question. This row just never noticed PQ-9 had closed under it |
| **LD-16** | ~~**Which endpoint of a hosting edge is the host --- and do the three hosting kinds agree?**~~ | ~~Anything that treats [AB-54](AGENT.abstractionLayers.planning.md#settled-register)'s hosting kinds **as a group**: the shard constructor, [LP4d](#recommended-order)'s `ports` pairing, [LP4f](#recommended-order)'s adjacency predicate~~ | ~~(a) Normalise `On` to host-first, matching `In`/`PartOf`; (b) keep the shipped directions and make every group-consumer direction-aware; (c) drop the group and handle three kinds separately~~ | **Decided 2026-08-20 --- opened and closed the same day, and NOT as the fork it was opened as. The options above are retained for the record; (a)/(b)/(c) mis-framed the question.** **Opened** while landing [LP4e](#recommended-order), from reading the vocabulary rather than the code. **Re-framed immediately on the user's challenge --- *`In`/`PartOf` are one day old and untested; is this an implementation error rather than a design fork?* --- and checking that reframing is what produced the real diagnosis.** **The actual defect: a relation kind is a predicate on its *subject*, and the two containment kinds were specified backwards against their own names.** All four shipped kinds obey the convention --- `glass -On-> tray` is *"the glass is on the tray"*, `boots -Under-> table`, `rope -Against-> tree`. Read the same way, [LP4c-i](#recommended-order)'s fixture `ROOM#Kitchen -PartOf-> OBJECT#crystalBall` says *"the kitchen is part of the crystal ball"*, and [LD-8](#open-decisions-implementation--plan-only)'s `FEATURE#Wall -PartOf-> FEATURE#Niche` says *"the wall is part of the niche"*. **Resolution: containment runs member -> root** --- `crystalBall -In-> kitchen`, `niche -PartOf-> wall` --- so all three of AB-54's hosting kinds agree, with the subordinate at `from`. **The leaning recorded when this row was opened --- normalise `On` to host-first --- was exactly backwards**, and is kept here because it shows the fork framing was itself the error: asking *which end is the host* invited a choice, when a convention was already in force to check against. **What *"root to part"* was right about, and it is the load-bearing half: incidence, not direction.** Every containment edge is incident to the root, which is what makes the containment subgraph a **star**, which is what makes cycles unrepresentable. **[AB-53](AGENT.abstractionLayers.planning.md#settled-register)'s resolution, [LD-12](#open-decisions-implementation--plan-only)'s reasoning and the star topology are untouched --- only the arrow moved.** **Cost, which is why the reframing was worth making: four test fixtures.** No producer, no consumer and no stored row ever carried a containment edge ([LPM](#recommended-order) wiped the corpus), and the classifier throws on both kinds. **Landed 2026-08-20:** fixtures flipped in `ephemeraMeta.test.ts` (x2), `ludicGraph/index.test.ts` and `positions/publishedEvents.test.ts`; [AB-4](AGENT.abstractionLayers.planning.md#settled-register)'s verdict amended upstream; the `thread:2 -In-> room` worked example corrected in both plans; `AGENT.concepts.md` and `ludicGraph/AGENT.md` corrected. **The durable fix, and the actual deliverable: the subject-predicate-object convention is now written down** in [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md). **This is the fourth instance of *a verdict inherited into a later vocabulary* in this plan --- and the first where the vocabulary that shifted was an unwritten convention.** The other three were caught by re-reading a durable doc; **this one could not have been, because there was no doc to re-read.** *Superseded framing follows:* the disagreement was originally stated as `PartOf`/`In` running **host-first** while `On` runs **content-first** (`glass -On-> tray`, and `classifyInteractionUnderTransfer` reads `On`/subject as the *content* being moved). **So AB-54's *hosting kinds* group puts the host at `from` for two members and at `to` for the third.** **Why this is a trap rather than a detail:** the whole value of the partition is that a consumer can ask *is this a hosting kind?* and act uniformly; a group whose members disagree about which end is the host gives that consumer a silently-wrong answer half the time, and the failure is a mis-parented node rather than an exception. **The leaning, recorded as a leaning:** the in-shard representation appears to force (a) --- inside the tray's own graph the tray is the root, and a root-to-member edge is host-first by construction, so an `On` edge that has moved into the tray's shard is already host-first whatever the exterior spelling was. **What makes it cheap if (a) is right:** `On`'s shipped direction semantics live almost entirely in the classifier, and [AB-54](AGENT.abstractionLayers.planning.md#settled-register) **deletes** that row (`carry` goes away with it) --- so the flip may cost nothing beyond the constructor and a data migration that [LPM](#recommended-order) has already made trivial. **What makes it not free:** *on* and *in* are not interchangeable in narration, and whichever way the arrow points, the **relation text** presented at a crossing (`EphemeraLudicCacheCrossing.edgeText`) must still distinguish them --- so normalising direction must not be allowed to look like normalising **meaning**. **Do not settle this inside an implementation slice** --- it is AB-54's own loose end and belongs upstream with it |
| **LD-15** | **When may the `ludicGraph` constructor actually nest --- that is, when may a hosted thing stop being a member of the exterior graph?** | Everything downstream of [AB-54](AGENT.abstractionLayers.planning.md#settled-register): `carry`'s removal, `computeCarryClosure`'s collapse, [LD-12](#open-decisions-implementation--plan-only)'s relocated invariant | Nest as soon as the representation admits it; or **hold the constructor flat** until `ludicCache` serves referent search | **Decided 2026-08-19, at the same time as AB-54 and by the same conversation: hold it flat. Lay down the structures that make `ludicCache`'s referent search sensible and possible; defer the constructor refactor that makes it live.** **Why the gate is hard rather than prudent:** [`roomObjectCatalogForCharacter.ts:74-75`](../../../../../lambda/ephemera/dataSource/actions/roomObjectCatalogForCharacter.ts) builds the referrable-object catalog by enumerating one room's `ludicGraph.objectIds`, and [`heldInventoryCatalogForCharacter.ts`](../../../../../lambda/ephemera/dataSource/actions/heldInventoryCatalogForCharacter.ts) does the equivalent for a Character host. **The first nested cup vanishes from the player's vocabulary**, and it vanishes by being **absent from a list** rather than rejected by a guard --- **the same silent-drop class this plan already calls out in [Verification](#verification)**, one layer up and with no guard to test. **What may land before the gate, and this is the useful half:** (a) the hosting/peer partition stated in code as a named split rather than left implicit in the classifier's switch, which changes no behaviour; (b) [CC1a](AGENT.abstractionLayers.planning.md#recommended-order) and [CC3](AGENT.abstractionLayers.planning.md#recommended-order), the cache and its referent wiring, including the catalog builders. **And (b) is verifiable for free precisely because the constructor is still flat:** while nothing nests, a cache-backed catalog **must** produce the identical id set to the graph-backed one, so the migration can be proved equivalent **before** the day it starts mattering. **That equivalence test is the real deliverable of the deferral** --- take it, rather than treating the flat period as dead time. **What must wait behind the gate:** the constructor change itself, deleting `carry` from `InteractionUnderTransferOutcome`, collapsing `computeCarryClosure`, and LD-12's host-nesting invariant --- **all four are behaviour changes on a live path and none of them are safe while the catalogs still read the graph directly** |
| **LD-17** | **Does every hosted thing get a *materialized* interior graph carrying its presence port --- and who writes it?** | [LP4d](#recommended-order)'s non-empty assertion **as written**; premise 2's realization in data rather than only in the type; [AB-55](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only)'s subsumption claim, which cannot land without it | (a) **Materialize on hosting** --- the membership write path also writes the member's own graph with its egress; (b) **keep graphs lazy** and scope the invariant to graphs that exist, treating an absent graph as *port not yet written*; (c) **derive the port at read time** from the exterior and store nothing | **Open. Opened 2026-08-20 while scoping [LP4d](#recommended-order), from code rather than from the corpus.** **The finding:** [`fromPlainHostMeta`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/index.ts) returns `fromFieldPayload(hostId, record.ludicGraph ?? { rootId: hostId, nodes: [], edges: [] })`, so object graphs are **lazy**. A cup in a room is hosted by any reading, has no stored interior graph, and reads back with **empty `ports`** --- which makes premise 2's *a plain object carries a presence port* true in the **type** and false in the **data**. **Why this is a decision and not a defect report:** all three options are coherent, and they differ on where the fact lives rather than on whether it is true. **(a) is the only option that makes premise 2 true in data**, and [AB-55](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) depends on it --- adjacency cannot be retired in favour of a record that is not always written. **Its apparent cost is a second partition in the membership transaction, and that cost may be near zero:** [`commitStepSequence.ts:155-158`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/commitStepSequence.ts) **already** writes adjacency rows keyed `EphemeraId: entityId` --- the *member's* partition --- so the transaction reaches there today and (a) may be a change of payload rather than of shape. **Check that before pricing it; it is the cheapest thing in this row to verify.** **(b) is the cheap read and the interim posture**, but it makes empty `ports` ambiguous between *not hosted* and *not yet written* --- **the absent-from-a-list failure mode this plan keeps naming** ([LD-15](#open-decisions-implementation--plan-only), [Verification](#verification)), where nothing throws and a thing is merely missing. **(c) contradicts [LP4d](#recommended-order)'s own bullet 2:** `fromHostId` has **no interior witness**, so deriving it means reading the exterior --- which is adjacency --- and that forecloses AB-55 in the opposite direction rather than answering it. **What locked-frame clause 3 does and does not contribute here --- stated carefully, because a first draft of this row overstated it.** [Clause 3](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md#wholes-parts-and-ports) carries an explicit scope note --- ***"it constrains graphs that exist and mints none"***, and upstream, ***"it is conditional on having a graph at all, which is what keeps it cheap: it decides no inventory."*** **That is silence, not prohibition:** (a) does not violate clause 3, it goes beyond it. **So the clause does not argue for (b) --- what it establishes is that minting is genuinely unanswered**, which is stronger than it sounds, because it means **premise 2 cannot settle this row on its own.** Premise 2 says a hosted thing *carries a presence port*; clause 3 deliberately declines to say a hosted thing *has a graph*; and until this row chooses, nothing bridges them. **The specific misreading clause 3 names is the one to avoid here:** *"a uniform graph interior is not a uniform containment ladder --- reading an inventory claim out of a structure claim is the specific error to avoid."* **(a) is an inventory claim** and must be argued as one. **The reconciliation to test before choosing:** clause 3 governs graph **structure** and disclaims minting; premise 2 governs **presence**. If a port can be a property of the member's **item** without obliging a populated graph --- an egress list on `Meta::Object` while `nodes` stays empty --- both hold, and (a) mints nothing clause 3 was protecting. **That is a shape question [LP4d](#recommended-order) must not answer by accident**, which is a further reason its assertion is scoped rather than weakened. **Leaning: (a), with (b) as LP4d's interim posture**, which is compatible --- asserting over graphs that exist stays correct when (a) later makes every graph exist. **Sequencing note, and it is the useful finding in this row: this is *not* gated by [LD-15](#open-decisions-implementation--plan-only).** Materializing an interior graph for a cup does **not** nest the cup --- it remains a member of the room's graph, `roomObjectCatalogForCharacter` still enumerates it, and no referent vanishes. **Nearly every other consequence of [AB-54](AGENT.abstractionLayers.planning.md#settled-register) waits behind that gate and this one does not**, which makes it available as real work during the flat period alongside [CC1a](AGENT.abstractionLayers.planning.md#recommended-order)/[CC3](AGENT.abstractionLayers.planning.md#recommended-order). **Scope carried from AB-55:** `EphemeraPositionAdjacencyContainedId` is `Character \| Object \| Feature`, so (a) obliges a **Character's** graph to carry an egress too --- consistent with locked-frame clause 3, untested for Characters. **What would settle it:** the transaction-shape check above, plus the write-path enumeration AB-55 already owes --- **enumerate once for both rows, not twice**. **Noted 2026-08-20 on landing [LP4i](#recommended-order), not a resolution of this row:** the self-heal infrastructure now exists ([`healLudicGraphStructure.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/healLudicGraphStructure.ts)), scoped deliberately to `rootId`/root-node only --- whichever option this row settles on, a `ports` repair (if one is ever needed under (b)'s interim posture) can extend that module rather than building a second detect-and-heal pipeline; it does not answer materialize-vs-lazy-vs-derive. **Landed 2026-08-20 on [LP4d](#recommended-order), still not a resolution of this row:** `computeRepairedPayload` now defaults a missing/malformed `ports` to `[]`, exactly the interim posture (b) anticipated above --- a graph that exists but carries no recorded `ports` is treated as *not yet written*, healed once, not defaulted at the read boundary. This is scaffolding for whichever option wins, not a lean toward (b) over (a)/(c): the moment this row settles on (a), the heal default becomes dead code for newly-hosted things (still correct for anything hosted before that slice lands) rather than something to unwind. **Upstream dependency recorded 2026-08-20: this row presumes a port is the right structure and asks who writes it --- [AB-57](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) asks whether it delivers what a crossing needs at all**, and its *manner* residue could change what a written port has to contain. **Do not settle (a)/(b)/(c) as a pure plumbing choice before AB-57 sizes the payload.** Note also that this row is load-bearing for AB-57's own part (2): under (b), the derivation a crossing edge substitutes for **does not exist**, which is why the desire path has nothing to lose to |
| **LD-6** | ~~Does `Feature` land in this pass, or only the kinds the corpus forces?~~ | ~~LP4b scope~~ | ~~Include it; or defer until something needs a Feature node~~ | **Decided 2026-08-15: include it --- the corpus forced it.** [LD-8](#open-decisions-implementation--plan-only)'s `FEATURE#Wall -PartOf-> FEATURE#Niche` needs a Feature on **both** ends, so the node tag is required and the earlier *defer unless forced* recommendation is spent. **The asymmetry it was hedging still applies** and moves into LP4b as a constraint rather than a reason to wait |
| **LD-18** | **When an interior port's stored value and the exterior edge disagree, what is the response?** Opened and decided 2026-08-23. [Premise 12](#premises-and-where-each-is-recorded)'s 2026-08-22 amendment settled *which side is right* --- compare where comparison is possible, and where an exterior reference exists it governs --- and explicitly left *what to do about a disagreement* owed to [LP6](#recommended-order). | **Split across two steps 2026-08-23 (user): the sweep and self-heal are [LP6a](#recommended-order)** (buildable on [LP6](#recommended-order)'s types, deliberately ahead of the producer), **the write-time repair is [LP6b](#recommended-order)** (travels with the producer). [LP6](#recommended-order)'s kind-agreement validation, which cannot be written without it; and the `ports` line in [LP4i](#recommended-order)'s **shipped** repair model ([`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md): *"Not healable ... `ports`"*), which this narrows **Half discharged 2026-08-23: [LP6a](#recommended-order) landed the out-of-band arm** --- the targeted sweep and its self-heal, both scoped to a disagreement with a named referrer. **The on-write arm ([LP6b](#recommended-order)) is still open** and still travels with the producer. **The on-read arm needed no code and got none:** *tolerate and report* is what a read boundary that never calls the heal already does. | (a) Throw at the read boundary, the way `isEphemeraLudicGraphFieldPayload` already does for shape staleness; (b) repair in place wherever noticed, including on read; (c) split by operation --- repair on write, tolerate-and-report on read, heal out of band | **Decided 2026-08-23 (user): (c), split by operation.** **On write:** if the mismatch is noticed while the records are being rewritten, **repair it in that write.** No new failure mode is created --- the transaction is already open and already writing that record, so the repair costs a value substitution, not a round trip. **On read:** **tolerate, and report a diagnostic finding.** The read returns what it read. The stated reason is the user's and it is the binding constraint on the whole answer: **no read operation may become a potential synchronous write operation.** **Out of band:** a **targeted sweep with a corresponding self-heal**, which repairs the system without any read having to. **This invents no mechanism --- it routes a second finding through the one [LP4i](#recommended-order) already built**, and lands on a rule that is already shipped in two places rather than newly asserted here: [`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) (*"Never called from a read boundary ... growing a read-time fallback here is LPM's reset undone"*) and [`healLudicGraphStructure.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/healLudicGraphStructure.ts) `:96`. **The user's reason and LPM's are the same rule reached from opposite directions** --- LPM's was *a read-time default makes a stale row indistinguishable from a current one, forever*; this one is *a read-time repair makes every read a write*. That agreement is the main evidence this answer is right, and it is why (b) is refused even though (b) is the shortest path to a consistent table. **Why not (a), which is the shipped precedent and so the default worth arguing against explicitly.** Shape staleness throws because there is **no usable value** --- the record does not parse, and returning it would propagate an unvalidated shape. A mismatch is the opposite case: **two well-formed values with a known correction direction.** Throwing there destroys availability to buy nothing that the sweep does not buy later, and it would make a stale exterior graph capable of failing an unrelated interior read. **Why a separate sweep, and not an extension of [`ludicGraphStaleStructureSweep`](../../../../../lambda/diagnostics/ludicGraphStaleStructureSweep/).** That sweep's honesty comes from being a **caller** of the shipped guard --- one row in, verdict out, no second theory of staleness to drift. **A mismatch cannot be judged from one row:** it needs the interior graph and the exterior host's graph together. Folding it in would give that sweep exactly the second definition its own design note refuses. Follow [`roomOccupancyDriftSweep`](../../../../../lambda/diagnostics/roomOccupancyDriftSweep/) instead --- already cross-record, already the shape LP4i cited as precedent --- with its own finding type on `@tonylb/mtw-interfaces/ts/eventBridge/diagnostics` and its own serializer round-trip, and the **inline `EventBridgeClient`** the sibling sweeps use rather than the shared wrapper. **What this narrows in [LP4i](#recommended-order), and the line moves to exactly where premise 12's amendment put it.** LP4i deferred `ports` repair wholesale, on the ground that `fromHostId` **has no interior witness**, so any repair must read the exterior --- which meant the reverse index, and therefore [AB-55](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only)/[LD-17](#open-decisions-implementation--plan-only). **A disagreement is not that case.** The port already **names** the host to check, so the sweep fetches one named graph and compares: no reverse index, no LD-17 dependency. **Still gated, and for LP4i's original reason:** a port whose named referrer holds no matching edge at all, and a port missing `fromHostId` entirely --- both ask *who **should** refer here*, which only the reverse index answers. **Checking a named referrer is cheap; discovering the absence of any referrer is not.** That is the line, and it is the same recorded-versus-derived line LP4i said its healable set was testing --- it just sat one notch coarser than the premise supported. **Owed to [LP6a](#recommended-order) when it lands, not open here:** the `AGENT.contract.md` repair-model bullet above must be re-scoped from *`ports` is not healable* to the narrower gate this row draws, and the new sweep given its own subsection beside LP4i's. **Also LP6's to specify rather than a question for this row:** the sequencing inside a single write when the write is *to the port* rather than to the exterior edge --- premise 12's amendment already governs which **value** is right, and the ordering is implementation. |

## Verification

Per-slice, from `lambda/ephemera` unless stated:

```bash
# Stage 1, after LP2/LP3
cd lambda/ephemera && npm run test -- --watchAll=false dataSource/positions/ludicGraph/

# After LP4 (published package)
cd packages/mtw-interfaces && npm run test -- --watchAll=false ts/ephemeraMeta.test.ts

# Full positions suite before calling any slice done --- integration tests live outside tsconfig
cd lambda/ephemera && npm run test -- --watchAll=false dataSource/positions/
```

**Silent-drop check, added 2026-08-17 --- the suite passing is not evidence for this class.** Two hazards in this plan fail by *discarding valid data and returning `false`*, not by throwing or failing to compile: [LP4c](#recommended-order)'s stale `HOST_RELATIONAL_EDGE_KINDS` set (a `Set` literal has no exhaustiveness requirement) and [LP7](#recommended-order)'s `typeof === 'string'` terminal pre-checks. **Both live inside `isEphemeraLudicRelationalEdgeData`, whose whole contract is to return `false` rather than complain.** For either slice, the test that matters asserts the new edge shape **survives a stored-payload round trip** --- and covers `extractRelationalEdgesFromStored`'s fallback branch as well as the primary path, since an accept-only test on the primary path passes while the fallback silently eats the edge.

**Documentation check per slice:** confirm cross-file links still resolve, in both directions --- this plan cites PQ rows upstream, and [CC1](AGENT.abstractionLayers.planning.md#recommended-order) cites this plan as its blocker.

## Progress

| Stage | Status | Next |
| --- | --- | --- |
| **Stage 1** (LP0--LP5, including LPM and LP4a--LP4j) | **In progress.** LP0 fully landed 2026-08-16 (Object + Feature, then Area as its own slice per LD-9 --- same day); LP1 landed 2026-08-17 (PQ-9's separator graduated, scoped to the serde boundary); LP2 landed 2026-08-17 (structured terminal primitives + discriminator, in `ephemeraMeta.ts` per LD-1's resolution); LP3 landed 2026-08-17 (matchers fixed, LD-2 decided, `isEphemeraLudicRelationalEdgeData`'s crash-on-non-string guard hardened as a found-along-the-way fix); LP4 landed 2026-08-17 (edge terminals widened to `EphemeraLudicTerminalPrimitive`, `bothObjectsOnGraph`'s object assumption fixed via a new `nodeIds` getter, 21 downstream compile breaks across 8 files narrowed at their Object-only boundary); LPM landed 2026-08-19 (reset executed and verified); LP4a landed 2026-08-19 (`rootId` shipped, `CarryClosureFragment` collapsed); LP4h landed 2026-08-19 (`applyTransferSet` widened to `Object \| Character`, caller-side split collapsed); LP4b landed 2026-08-19 (`EphemeraLudicGraphNode` widened to `Feature`/`Area`, closing LP4's referential-integrity gap, inert until a producer exists); LP4g landed 2026-08-19 (kernel step terminals widened to `EphemeraLudicTerminalPrimitive`, five throw-on-non-Object narrows deleted, the `getCurrentHost` callback chain widened in lockstep, `buildObjectRelationalFact` renamed to `buildRelationalFact`, and two new boundary forks found and resolved --- the presentation-leg adapter now filters a non-Object relational subject to `[]` rather than widening the whole narration stack, and `compileRelationalFromSkeleton.ts` gained a matching narrow-and-drop guard at the ingress-facing `parsePlanStep.ts` boundary per LD-13); **LP4c-i landed 2026-08-19** (`HostRelationalEdgeKind` widened to `In`/`PartOf`; all four persistence-lane sites widened, all four ingress-lane sites left narrow and commented per LD-13; a ninth site --- `parsePlanStep.ts`'s import-based reference, invisible to the eight-site grep --- surfaced via `tsc --noEmit` and was narrowed to match, with drop guards added at its two producers, `compileRelationalFromSkeleton.ts` and `groundChange.ts`; `classifyInteractionUnderTransfer` throws naming LD-11 for both new kinds rather than answering it) | ~~**LP4c-ii** is next --- the classifier's actual behaviour under transfer --- and it is still gated on **[LD-11](#open-decisions-implementation--plan-only)**, unchanged by LP4c-i landing.~~ **Superseded 2026-08-19: LP4c-ii is not next and not scoped as written.** [AB-53](AGENT.abstractionLayers.planning.md#settled-register)/[AB-54](AGENT.abstractionLayers.planning.md#settled-register) made hosting and containment one mechanism, which dissolves most of LD-11 (the classifier never sees a containment kind) and **replaces LP4c-ii's "add two cases" with a *deletion*** --- `carry` leaves `InteractionUnderTransferOutcome` and `computeCarryClosure` collapses into a shard read. **That deletion is behind [LD-15](#open-decisions-implementation--plan-only)'s gate**, which holds the constructor flat until `ludicCache` serves referent search, so **the next work in this stream is [CC1a](AGENT.abstractionLayers.planning.md#recommended-order)/[CC3](AGENT.abstractionLayers.planning.md#recommended-order) in the abstraction plan, not a step here.** `expandValidate/interactionUnderTransfer.ts` still has LP3's matcher shape unfixed --- flagged for LP4a to pick up alongside the `CarryClosureFragment` collapse; LP4 additionally left it and four sibling files (`applyTransferSet.ts`, `applyObjectClearMembership.ts`, `applyObjectRoomMembership.ts`, `applyObjectRelationalChange.ts`/`executor.ts`/`compilePositionKernelOp.ts`) with fresh `isEphemeraObjectId` narrows at the widened-edge boundary --- LP4a's `CarryClosureFragment` collapse should look at whether those narrows can retire rather than persist alongside a rooted graph. **Answered 2026-08-17, and it produced two new steps rather than an LP4a sub-item:** reading the relational write path end to end showed the kernel branches on entity kind **nowhere**, so the narrows are retired by **[LP4g](#recommended-order)** (retype the executor step terminals + two `findHostOf` copies; **must precede LP4c**, or the narrows become live bugs on legitimate containment edges) and **[LP4h](#recommended-order)** (`applyTransferSet`'s set to `Object \| Character`, independent of containment). **Corrected 2026-08-19, on landing LP4h:** LP4h's actual scope never touched `interactionUnderTransfer.ts` --- `computeCarryClosure`/`boundaryEdgeOutcomes`'s Object-only narrows are **not** retired by either LP4g or LP4h and are now recorded as unowned in `ludicGraph/AGENT.md`, not silently assumed closed. **Confirmed again on landing LP4g 2026-08-19: LP4g's own bullets never reached `interactionUnderTransfer.ts` either**, so that narrow stays exactly as unowned as LP4h left it. **Three decisions opened alongside them --- [LD-11](#open-decisions-implementation--plan-only) blocks LP4c-ii**, [LD-12](#open-decisions-implementation--plan-only) and [LD-13](#open-decisions-implementation--plan-only) are unowned (though LD-13's "ingress-only" recommendation is now also confirmed twice more --- LP4g's `compileRelationalFromSkeleton.ts` fix and LP4c-i's `parsePlanStep.ts`/`groundChange.ts` fixes). **LP4i landed 2026-08-20** (structural-staleness sweep + self-heal, proven on root-in-nodes --- see its own row). **LP4d landed 2026-08-20** (`ports` --- the egress list --- shipped as a required, possibly-empty field, exercising LP4i's self-heal rather than a second manual wipe; see its own row). **[LP4f](#recommended-order) is next** --- the adjacency-row existential-invariant fix --- then [LP5](#recommended-order) (premise 7, the pass-through-mediator question). **LP4f re-scoped 2026-08-20 (see its own bullets and the status line):** it quantified its invariant over containment **edges**, but an adjacency row is the reverse index of **node presence**; the `commitStepSequence.ts:154` defect it was built around does not exist (adjacency items filter to `transferMembership`, which a dissolve never produces), and the node-grained invariant already holds. **It shrinks to a contract clause plus an AB-54 forward note, and stays next.** **LP4f landed 2026-08-20** --- the contract clause and forward note shipped exactly as scoped, no code touched. **[LP4j](#recommended-order) opened 2026-08-20 and follows it** --- retire *containment kind* as a named subset of relation kinds (AB-54 already merged the mechanism; only the vocabulary lagged), record the consequent obligation on [LD-12](#open-decisions-implementation--plan-only), then sweep the type contracts that still encode the superseded partition. **LP4j landed 2026-08-20** --- `AGENT.concepts.md`'s hosting/peer row states the exhaustive partition, LD-12 carries the future-obligation note (also mirrored into `detectRelationalCycle.ts`'s docblock), and the three confirmed hit sites (`ephemeraMeta.ts`'s union comments, `interactionUnderTransfer.ts`'s throw message, four ingress-lane doc comments) were reworded. Documentation and commentary only, no behaviour change beyond the reworded `Error` string. **LP5 landed 2026-08-20** --- premise 7 answered by **rejecting its setup**: a cable through a box is multi-hosted membership, not a boundary crossing, so no port terminals exist to join and the bare-vs-mediated fork was never live. Verdict recorded upstream at LC7 (with LC8 and gap 6 updated), LD-3 removed, and **LP7's promised shrink retired as illusory**. **The remaining thinking is LC8's**, now the sole route to a bare port-to-port edge. **[LP6](#recommended-order)/[LP7](#recommended-order) are Stage 2 and still gated on P7**, so there is no unblocked step left in this plan. |
| **LP4a--LP4j** (graph shape) | **LP4a landed 2026-08-19. LP4h landed 2026-08-19. LP4b landed 2026-08-19. LP4g landed 2026-08-19. LP4c-i landed 2026-08-19. LP4i landed 2026-08-20.** Added 2026-08-15 --- **implementing 2026-08-06 and 2026-08-09 decisions that never reached code or the durable doc.** LP4g/LP4h added 2026-08-17, both retiring narrows LP4 introduced rather than implementing a stale decision --- **though LP4h's landing found its own scope narrower than the retirement claim on `interactionUnderTransfer.ts`; see the Stage 1 row.** | ~~**LP4c-ii next (containment kinds, behaviour under transfer) --- gated on [LD-11](#open-decisions-implementation--plan-only).**~~ **Re-aimed 2026-08-19 --- see the Stage 1 row: LP4c-ii's content became a deletion behind [LD-15](#open-decisions-implementation--plan-only)'s gate, and the next work is on the cache side.** LP4c-i (representation) landed 2026-08-19; the stale bullet it carried (*"**Open**: graph edges or adjacency rows"*, already closed by [LD-5](#open-decisions-implementation--plan-only) on 2026-08-15) was corrected in the same pass. ~~`AGENT.concepts.md` was stale on **three** counts, now down to two --- the root-designating field is recorded, `In`/containment vocabulary and the port-record split remain to close via LP4e~~ **Wrong, and corrected 2026-08-20 by actually reading the file: the `In`/containment vocabulary and the port-record split were *already recorded* there, both dated 2026-08-15.** This cell was written from LP4e's bullets rather than from `AGENT.concepts.md`, which is the same never-propagated-to-the-durable-doc defect one level up. **[LP4e](#recommended-order) landed 2026-08-20**, out of sequence and ahead of LP4d (documentation-only, entirely on the safe side of [LD-15](#open-decisions-implementation--plan-only)'s gate). What was genuinely stale: the `HostRelationalEdgeKind` implementation-gap sentence (false since LP4c-i) and **two** assertions that `EphemeraMembershipHostId` is `Room \| Character` (false since LP0). Added as new content: the **hosting kind / peer kind** vocabulary row and the **iteration-1 constructor discipline** paragraph. Surfaced and deliberately left open: **[LD-16](#open-decisions-implementation--plan-only)**, the hosting-kind direction disagreement --- **closed the same day; see its row.** **Next is [LP4i](#recommended-order), then [LP4d](#recommended-order) --- re-ordered 2026-08-20 (user) as an explicit recognition that *the implementation has outpaced its resiliency systems*.** LP4i builds the staleness sweep and self-heal **first**, taking clause 3's missing root node as its proving case; LP4d then becomes the first shape change that **exercises** that discipline instead of being hand-migrated. **The ordering is not cosmetic:** LP4i's staleness rule is *a row is stale iff the shipped guard rejects it*, so with root-in-nodes left in LP4d it would have shipped a detector with **nothing to detect** against a table [LPM](#recommended-order) had just wiped clean --- and the null-case assertion (*the sweep finds this gap and nothing else*) is provable only while that baseline is clean. **Note LP4i is behaviour-changing rather than read-only** --- adding a root node to every graph shifts node counts through `nodeIds`, `applyTransferSet` and the [LP3](#recommended-order) matchers. **LP4d itself is ready and unblocked.** Its two earlier blockers dissolved on inspection rather than being decided: **[LD-7](#open-decisions-implementation--plan-only) closes as already-satisfied** ([LP0](#recommended-order) widened `EphemeraMembershipHostId` on 2026-08-16 and the bullet was never updated), and **premise 2's flag turned out to be a narrow definitional wrinkle** about the word *plain*, not a question about mechanism. **What grew instead:** scoping the step found that `ports` was never the only field missing from [locked-frame clause 3](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md#wholes-parts-and-ports)'s minimum --- **the root node is designated by `rootId` and absent from `nodes` in all three construction paths** --- so LP4d now lands the whole minimum structure. **Two rows and one step opened out of it, none of them gated on [LD-15](#open-decisions-implementation--plan-only):** [AB-55](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) (does the presence port subsume the adjacency reverse index?), [LD-17](#open-decisions-implementation--plan-only) (does hosting mint a materialized graph?), and **[LP4i](#recommended-order)** (a structural-staleness diagnostic plus an `ephemera` self-heal path, so shape changes stop being hand-migrations). **LP4i landed 2026-08-20**, exactly as scoped: construction-time fix (every fresh-construction factory now includes the root node via a new `nodeFromId` dispatcher), the payload guard now asserts root-in-nodes, [`ludicGraphStaleStructureSweep`](../../../../../lambda/diagnostics/ludicGraphStaleStructureSweep/) (diagnostics, report-only) and [`healLudicGraphStructure`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/healLudicGraphStructure.ts) (`ephemera`, idempotent, dry-run-capable) ship the detect-and-heal infrastructure as durable, reusable machinery rather than a one-off --- the `ports` heal LP4d/LD-17 will eventually need can build on the same module rather than inventing a second one. **[LP4d](#recommended-order) landed 2026-08-20** --- `ports` shipped as a required, possibly-empty field on `EphemeraLudicGraphData`, threaded through `EphemeraLudicGraph`'s full construction/serialization surface, and `healLudicGraphStructure`'s `computeRepairedPayload` extended to default a missing/malformed `ports` to `[]` (LD-17's interim posture (b)) --- LP4d exercising LP4i's discipline exactly as that row anticipated, rather than a second manual wipe. Its two blockers dissolved as scoped: LD-7 stayed closed-as-already-satisfied, and the non-empty-for-anything-hosted invariant stayed deliberately unenforced (blocked as written, per that bullet's own scoping). ~~**`LP4a--LP4i` (this row's own scope) is now fully landed.**~~ **Corrected 2026-08-20, and the row's scope re-labelled `LP4a--LP4j` with it: the *graph-shape* steps `LP4a--LP4e` and `LP4g--LP4i` are all landed, which is what that sentence meant, but [LP4f](#recommended-order) sits inside the lettered range and is *not* landed** --- it is the adjacency-row invariant, re-scoped 2026-08-20 over nodes rather than edges and still open --- **and [LP4j](#recommended-order) opened the same day** (retire *containment kind* as a named subset, record the obligation on [LD-12](#open-decisions-implementation--plan-only), sweep the type contracts that encode the superseded partition; documentation and commentary only). **Read the lettered range as a naming sequence, not as a landed set.** **LP4f landed 2026-08-20**, closing out the range's last open step --- a single falsifiable clause on `AGENT.contract.md` (node-presence *iff*, plus the AB-54 forward note), no code touched. **LP4j landed 2026-08-20** too, same day --- the vocabulary rule, LD-12 note, and sweep hits described in the Stage 1 row. **`LP4a--LP4j` is now fully landed.** |
| **LPM** (graph reset) | **Closed 2026-08-19.** Added 2026-08-15. **Operational, one-time, and it gated LP4a/LP4d** --- the required-field migration was a wipe rather than a backfill | Done --- `rootId` (LP4a) is required with no default; `ports` (LP4d, landed 2026-08-20) is required too, satisfied by LP4i's self-heal rather than a second wipe |
| **Stage 2** (LP6--LP9) | **LP7 landed 2026-08-22, ahead of the section header's own "gated on P7" line (see below) --- checked, not overlooked.** ~~LP6 remains open and does carry the P7 gate for real (the port record's `kind` field, per [PR-11](AGENT.presence.planning.md#settled-register), and premises 6/7 in this plan's own [Premises](#premises-and-where-each-is-recorded) table, both about the port record's shape).~~ **The Stage 2 header's gate was stated over the whole LP6--LP9 range, but its own rationale ("building the port record before P7 lands would build it blind to its only consumer") is about the *port record*, which is LP6's --- LP7 only widens edge-terminal types to admit an already-real terminal shape (`AGENT.concepts.md`'s `PowerCord -[ThreadsInto]-> FLASHLIGHT:1`), with no port-record-shape dependency of its own.** Same pattern this plan's own performance tally already named `overbroad-inheritance` earlier the same day, on a different plan --- a group-level gate inherited by a member that doesn't carry the group's stated reason. **Rechecked 2026-08-23 (user); the struck sentence above is withdrawn --- LP6 does not carry the gate either.** Premises 6 and 7 --- the gate's own two stated requirements --- are both discharged, the record shipped in [LP4d](#recommended-order) without P7, and P7's six staged findings never mention the port's relational `kind`. **Stage 2 now has no P7 gate on any member.** Full argument at [*What is not blocked, and must go first*](#why-this-plan-exists). **LP6 landed 2026-08-23** --- the port record's two fields and their guard, types alone. **Next is [LP6a](#recommended-order)** (LD-18's mismatch sweep and self-heal, deliberately ahead of the producer); [LP6b](#recommended-order) still travels with the producer step ([LD-17](#open-decisions-implementation--plan-only)), outside this stage. **LP6a landed 2026-08-23** --- LD-18's mismatch sweep and self-heal, built ahead of the producer exactly as [LP4i](#recommended-order) was, with the classifier carrying the fixture weight. **Next in this stage is [LP8](#recommended-order)**; [LP6b](#recommended-order) remains outside it, aligned with the producer step ([LD-17](#open-decisions-implementation--plan-only)). | ~~LP7 carries a second gate as of 2026-08-17: LD-14 --- the stored terminal's form (structured map vs. PQ-9 string) decides which *direction* two shipped `typeof === 'string'` guards get fixed in, so the step cannot be scoped until PQ-9 answers it upstream~~ **LD-14's gate cleared 2026-08-22: PQ-9 had already answered it on 2026-08-17, unnoticed. LP7 landed the same day** --- both guards fixed, `from`/`to` widened to `EphemeraLudicTerminalId` on `EphemeraLudicRelationalEdgeData`/`HostRelationalEdge` (and its `manipulation/types.ts` duplicate), plus two bugs `tsc`/inspection surfaced along the way (five kernel-step producers assuming a primitive boundary-edge endpoint, and a raw-`!==` edge comparison in `EphemeraLudicGraph.equals()`) --- see LP7's own bullets for the full list. ~~**Next is [LP8](#recommended-order)** (unblock CC1).~~ **Re-sequenced 2026-08-23 (user): [LP6](#recommended-order) split three ways --- LP6 (types) then [LP6a](#recommended-order) (LD-18's sweep and heal, deliberately ahead of the producer), with [LP6b](#recommended-order) (validation and write-time repair) moved out of this stage to travel with the producer step, per [LD-17](#open-decisions-implementation--plan-only). **Next is LP6.** |
| **Upstream effect** | CC1 blocked; CC0b landed and is unaffected. **Reversed in one direction 2026-08-19: the dependency now runs both ways.** This plan still blocks CC1 (ports do not exist in code), but [AB-54](AGENT.abstractionLayers.planning.md#settled-register) makes [CC3](AGENT.abstractionLayers.planning.md#recommended-order) block **this** plan's constructor, via [LD-15](#open-decisions-implementation--plan-only) --- referent search reads the flat graph today and would silently lose hosted objects the day it nests | LP8 clears the outbound half. **The inbound half clears when CC3 lands** --- and note the two are not deadlocked: CC3 wires the referent path to the cache, which does not require ports, while CC1b's port-joining does. **Sequence CC3 ahead of CC1b if the cache work is picked up before LP7** |
