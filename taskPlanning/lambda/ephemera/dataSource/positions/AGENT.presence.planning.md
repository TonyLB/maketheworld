# Presence: what it means for a thing to be somewhere

**Status: STUB, opened 2026-08-21 from conversation. Design stage, Phase 0 not started.** Nothing below is decided; the open rows are named so they can be worked, not answered. **No code is in scope yet**, with one deliberate qualification carried from the parent: *no code yet* gates **provisional** decisions, not decisions generally --- see [Graduation tiers](../../../../AGENT.designVariant.md#graduation-tiers-when-a-decision-licenses-code). This plan is expected to reach for an implemented **interface** earlier than most design work does, because grounding the next step is its stated purpose.

**Read [Presence as a cover](#presence-as-a-cover) before the rows** --- it is a working formulation, not a settled one, but every open row is phrased in its vocabulary.

**Next step: ~~Phase 0~~ Phase 1 --- [PH0 closed 2026-08-21](#recommended-order) with all four sub-steps disposed of, and further corpus cases are now discovered in development rather than scheduled.** ~~**The live blocker is no longer corpus work: [PR-9](#settled-register) and [PR-10](#settled-register) are answered and unblocked, and what stands between them and the [Settled register](#settled-register) is that both are denominated in the [cover formulation](#presence-as-a-cover), which has not itself graduated.**~~ **Both graduated 2026-08-21**, the blocker having been cleared the same day: the cover formulation's semantics are now a **Target mental model** in [`positions/AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md#presence-as-a-cover), so the vocabulary they are denominated in is no longer plan-local. **Two IDs were minted on the way out, and the reason is worth keeping:** [PR-11](#open-decisions-design--plan-only) and [PR-12](#open-decisions-design--plan-only) carry PR-10's remainders, one of which pointed at **LP6/PR-4 and was not there** --- LP6 had been narrowed on 2026-08-15 --- so graduating the shell without the split would have stranded it. ~~**[PR-4](#open-decisions-design--plan-only), [PR-9](#settled-register) and [PR-10](#settled-register) were all answered on 2026-08-21, ahead of the corpus, and none is graduated** --- PR-9 and PR-10 have nothing open and wait only on the corpus confirming them;~~ **PR-4 is the one still open of the three;** PR-4 owes [PR-7](#open-decisions-design--plan-only) and shipped work in the parent's [Channel D](AGENT.abstractionLayers.planning.md#recommended-order). **Answering three rows before writing a single case inverted this plan's own order, and the first case written --- [PR-C1](AGENT.presence.corpus.planning.md#pr-c1-the-contraption-in-two-rooms-and-whether-covers-nest), 2026-08-21 --- falsified PR-10's predicate and re-opened half of PR-9 immediately.** **Read that as the corpus working rather than as the inversion failing:** answering the rows is what told the corpus where to aim, since [PR-10](#settled-register) is what revealed that character cases test nothing. **The risk it leaves is concentrated rather than gone** --- all three answers lean on the same [cover formulation](#presence-as-a-cover), so a fault there fails them together and no amount of case-writing in this vocabulary would catch it. Do not open a representation row before the requirement rows have an answer; that is the conflation this plan's sibling exists because of. **PR-10 is the one deliberate exception and says why in its own cell.**

**Sub-plan of [`AGENT.abstractionLayers.planning.md`](AGENT.abstractionLayers.planning.md)**, split out 2026-08-21. It owns **the semantics of being somewhere** --- what it means to say a thing is in a host, what carries that fact, what may read it, and what may not. The parent keeps the graph model, the locked frame, ports, and everything about `ludicGraph`'s shape.

This document is task-scoped and follows [`taskPlanning/AGENT.md`](../../../../AGENT.md) and the design variant [`AGENT.designVariant.md`](../../../../AGENT.designVariant.md).

**Companion files.** ~~Only the corpus is planned~~ **Two, as of 2026-08-21:** the [evidence corpus](AGENT.presence.corpus.planning.md), and the [reasoning trail](AGENT.presence.discussion.planning.md) --- **earned by the first graduation rather than created in advance**, which is what the [ladder](../../../../AGENT.designVariant.md#the-companion-file-ladder) intends. No proposal record; nothing in this plan has been a candidate scheme. **Forwarding pointer, which the ladder requires in both files:** graduated row detail and chronological reasoning go in the **trail**, not in [Progress](#progress) and not back into a row.

| File | Class | State |
| --- | --- | --- |
| **This file** | Control surface | Stub |
| [`AGENT.presence.corpus.planning.md`](AGENT.presence.corpus.planning.md) | Evidence corpus | **Created 2026-08-21** with [PR-C1](AGENT.presence.corpus.planning.md#pr-c1-the-contraption-in-two-rooms-and-whether-covers-nest), which falsified one row and re-opened half of another on arrival |
| Reasoning trail | --- | **Not split out.** Entries go in the parent's [`AGENT.abstractionLayers.discussion.planning.md`](AGENT.abstractionLayers.discussion.planning.md) until this plan earns its own; say so rather than defaulting them into Progress |

---

## Getting Started

1. **Read [`taskPlanning/AGENT.md`](../../../../AGENT.md) and [`AGENT.designVariant.md`](../../../../AGENT.designVariant.md) once.** This is a design-stage plan: resolved rows [graduate](../../../../AGENT.designVariant.md#graduating-a-resolved-row) rather than being deleted, rows may block no slice at all, and code is licensed by tier.
2. **Read the parent's [locked frame](AGENT.abstractionLayers.planning.md#locked-frame-parts-and-ports-2026-08-06) and its [exclusion list](AGENT.abstractionLayers.planning.md#what-is-not-locked-and-must-not-ride-in-on-this).** Its three shape claims are this plan's premises, not its subject matter. If you find yourself arguing whether a whole has its own graph, you are in the wrong document.
3. **Read [H3](AGENT.abstractionLayers.proposals.planning.md#h3-ports-as-the-boundary-mechanism-decomposition-as-a-modellers-choice) and [ports are single-use](AGENT.abstractionLayers.proposals.planning.md#ports-are-single-use-and-that-is-not-a-detail-clarified-2026-08-06).** A **presence port** --- a port with no exterior endpoint --- is the mechanism most of these rows will argue over, and it is now shipped rather than proposed.
4. **Read the shipped side before proposing anything.** There is already a presence predicate in the code and it is normative: [`positions/AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) --- *"Adjacency row existence is an existential invariant, not a lifecycle event: a row `(EphemeraId: X, DataCategory: POSITION#<hostId>)` exists **iff** X is a node in `<hostId>`'s `ludicGraph`."* **That sentence is a presence design**, made and shipped, and this plan either absorbs it or supersedes it deliberately.
5. **Read [Presence and perspective are orthogonal](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md#presence-and-perspective-are-orthogonal)** in the durable concepts doc. It is the one durable statement using this plan's word today, and **it uses it in the narrower, character-only sense** --- see [Two things are called presence](#two-things-are-called-presence-and-whether-they-are-one-thing-is-pr-2) below.
6. **Then write the corpus.** The open rows below are deliberately unargued; Phase 0 is what makes them arguable.

**Test orientation and baseline, for when this eventually licenses code.** From `lambda/ephemera`:

```bash
cd lambda/ephemera && npm run test -- --watchAll=false dataSource/positions/
```

**`npm run test`, not `npm test`, and `npx tsc --noEmit` is not sufficient** --- `*.integration.test.ts` files sit outside the tsconfig include and mock by module *path*, so a rename can typecheck clean and still break the suite. Commands live in [Verification](#verification), which is the authority if anything here conflicts with it.

**Nothing in this plan should change that baseline yet.**

## Why this is a separate plan, and why it is opened now

**The trigger, from conversation 2026-08-21: presence is the missing articulation that [P7 --- merge-reduce](AGENT.abstractionLayers.ludicCache.corpus.planning.md#mechanism-findings-staged-for-p7) needs before it can be authored.**

The staged findings state the reducer's unit as *"a merge resolves a set of terminals; each resolved terminal is queried against the cache and against the retained graphs; each hit is composed or walked onward"* --- and **every clause of that is a presence operation.** *Queried against the retained graphs* is a presence predicate. Finding 4's sub-graphing **drops** things, and what may be dropped is a presence judgement. Finding 5's recovery is *"a keyed walk, not a scan"*, and the key is a presence key. The parent's [flag on CC1b](AGENT.abstractionLayers.planning.md#recommended-order) put this in the negative form already: under the reducer, **node presence and edge composition are the same operation** --- which is precisely why the reducer cannot be specified while presence is a word rather than a system.

**Writing P7 first would decide presence by implication**, in the vocabulary of a reducer, in whichever form made that reducer easy to write. That is the failure mode this initiative has a name for.

**The second reason is the parent's own methodological one**, and it applies here more sharply than anywhere it has been applied yet:

> *What must the model be able to express?* is answerable from the fiction with confidence. *What is the representation?* is not, and depends on the first. **A row asking both makes the answerable half unreachable.**

**[AB-8](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) has been open since 2026-08-05 and has been re-framed at least four times** --- through H1's removal of multi-hosting, through P3's single-use ports, through the port-list carrier, through C10's apprehension-scale qualifier. **It is a representation row that has never had a requirement half.** This plan exists to supply it.

## Two things are called presence, and whether they are one thing is PR-2

**Stated first because the ambiguity is live in the corpus, in the durable docs, and in the shipped code at once**, and a reader who does not have it will merge two systems by accident.

| | **Character presence** (shipped, named) | **Extent** (design, unnamed) |
| --- | --- | --- |
| What it says | Which **room** a character occupies, and who shares it | Which **graphs hold a qualified reference** to a whole |
| Where it is stated | [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md) --- *"At play time, which room a character occupies"* | [AB-8](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only)'s (a)-with-ports re-frame, 2026-08-09 |
| Cardinality | **One** --- but ~~`evaluateComplexityPreGates` terminally errors on `containers.length > 1`~~ **is the wrong citation, corrected 2026-08-21.** That function is typed on `EphemeraObjectId`, lives under `objectManipulation/`, and gates **objects**. **Nothing enforces character cardinality anywhere**: `resolveCharacterRoomId`, `roomObjectCatalogForCharacter`, `roomExitTargetsForCharacter` and `resolveHomeTargetForCharacter` each simply take `containers[0]`. It is an assumption repeated at every read site, and since [PR-2](#open-decisions-design--plan-only) it is a **consequence** of hosting being restricted to rooms | **Many, by design.** Ariadne's thread is *supposed* to bind into several rooms --- [C7](AGENT.abstractionLayers.corpus.planning.md#c7-ariadnes-thread) |
| Carrier today | Membership API, eviction ladder, adjacency rows, `handleConnectionsCharactersPresence.ts` | Port list in the whole, per H3 --- shipped as ports, undesigned as presence |
| Mutation rate | The **highest-frequency mutation in the system** | Rare |

**They are not obviously the same and not obviously different.** Under [locked-frame clause 3](AGENT.abstractionLayers.planning.md#locked-frame-parts-and-ports-2026-08-06) every host has a graph of the same internal structure whatever kind it is, so *a character in a room* and *a rope in a room* are both **a node in a room's graph** --- which argues one relation. But the shipped machinery for the first is entirely separate from the mechanism proposed for the second, one is single-valued and terminally enforced while the other exists to be multi-valued, and their write rates differ by orders of magnitude.

**Do not resolve this in prose.** It is [PR-2](#open-decisions-design--plan-only), it is a **requirement** question before it is a representation one, and it is probably the row that decides the shape of everything else here.

**One thing has already shrunk it, 2026-08-21.** [PR-10](#settled-register) was answered *yes* --- presence is left implicit wherever a thing can only be in one place --- the right-hand column of this table is now the only one with a representation question in it: characters, being single-valued and terminally gated, carry no presence record to compare against. **This does not answer PR-2** --- the two might still be one relation or two in the fiction --- but the answer now costs nothing in storage, which is a materially smaller row to argue than the one this section was written for.

**[PR-2](#open-decisions-design--plan-only) was then answered 2026-08-21: one relation, with character hosting restricted to `ROOM`.** **This section is kept rather than rewritten, because the ambiguity it warns about is still live in the durable docs and the shipped code**, and a reader still needs the two columns to see what was merged. **Read it now as the statement of a resolved question, not an open one** --- and note that the resolution was a **scoping decision** about how much complexity the character path should carry today, not a discovery about the fiction. The row states what lifts it.

## What is inherited as settled, and must not be re-argued here

| Inherited | From | Consequence for this plan |
| --- | --- | --- |
| A whole has **its own graph, with a root node**; parts are nodes in it | [Locked frame](AGENT.abstractionLayers.planning.md#locked-frame-parts-and-ports-2026-08-06), clause 1 | "Where is it" is always askable *of a graph*. There is no placeless thing |
| Boundary crossings are mediated by an **explicit binding the interior owns** | Locked frame, clause 2 | Presence is never read by addressing a foreign interior node directly |
| **Every graph has the same internal structure**, whatever kind of host it belongs to | Locked frame, clause 3 | A rule that holds for rooms must hold for characters and objects, or it is not a presence rule |
| **Ports are single-use**; a port's exterior end is a graph plus an *optional* endpoint | [P3](AGENT.abstractionLayers.proposals.planning.md#ports-are-single-use-and-that-is-not-a-detail-clarified-2026-08-06) | A **presence port** is a port with no endpoint. This is the shape most rows will argue over --- and it is **shipped**, address form `OBJECT#ROPE#ab6129d` |
| **Multi-host presence is legitimate**, not drift | AB-8 re-frame 2026-08-09; [C9](AGENT.abstractionLayers.corpus.planning.md#c9-coiling-the-rope-back-in) | A design that reconciles multi-hosting away is disqualified before the corpus is consulted. Carries an outstanding doc-correction debt on `evaluateComplexityPreGates`' `multiPresent` error and on `repairObjectPlacementDrift` |
| `ludicCache` is **derived, authoritative over nothing**; a miss falls through | [P6 clause 5 / clause 4](AGENT.abstractionLayers.proposals.planning.md#the-five-clauses) | **Presence is truth and the cache is not.** Errors in the cache degrade to slow; errors here degrade to wrong. This plan does **not** inherit attention's weak correctness burden |
| A cache hit returns **a handle, not a subgraph**; reachability stays the graph's job | [P6 clause 3](AGENT.abstractionLayers.proposals.planning.md#the-five-clauses) | Presence must not quietly become a reachability oracle --- [PR-7](#open-decisions-design--plan-only) |

### The one inherited clause that most changes how this plan should be read

**Presence is truth.** The sibling sub-plan on attention is licensed to be lossy, sampled, heuristically pruned and aggressively compacted, because *a miss falls through* and the worst outcome is slow. **None of that license transfers here.** Presence answers *where is it*, and a wrong answer is a wrong world --- an object in a room it is not in, or absent from one it is in. Designs that would be perfectly reasonable for the attention ledger are **disqualified** in this plan, and the argument for them will sound familiar because it was accepted one construct over.

## Recommended order

Pending work is `[ ]`, completed work is `[X]`; mark each nested line `[X]` as it is done. **Nothing here is started.**

- [X] ~~**PH0. Write the corpus**~~ **Closed 2026-08-21 from conversation, with all four sub-steps disposed of.** --- `AGENT.presence.corpus.planning.md`, cases `PR-C1`... Cases are written against **today's** model and state which limit they hit; **do not propose solutions in the corpus.** **Closed on a stated rule rather than on the corpus being finished: further corpus cases are *discovered in development*, not blockers for calling this done.** **Note what the four sub-steps actually cost, because it is the reusable part** --- PH0b and PH0c were retired as unwritable, PH0d closed without a case on the finding that no falsifier can be written, and **the one case that *was* written, [PR-C1](AGENT.presence.corpus.planning.md#pr-c1-the-contraption-in-two-rooms-and-whether-covers-nest), falsified [PR-10](#settled-register)'s predicate immediately.** **The lesson is not that corpus work was wasted; it is that a brief should say what it would falsify before it is scheduled**, since three of these four could have been graded unwritable at booking time. **Re-opening this step needs a case that names its falsification target, not a gap in coverage.**
  - [X] **PH0a. Harvest before writing.** **Done 2026-08-21, out of order --- after PR-C1 rather than before it --- and the delay cost something: see [H-1](AGENT.presence.corpus.planning.md#ph0a-harvest--the-presence-cases-that-already-exist).** The write-up is [the harvest section](AGENT.presence.corpus.planning.md#ph0a-harvest--the-presence-cases-that-already-exist); it added **C11** to the list below and corrected two claims in [PR-10](#settled-register). **Two of its own findings were then rewritten the same day** --- [H-3](AGENT.presence.corpus.planning.md#ph0a-harvest--the-presence-cases-that-already-exist) (the cover formulation reframes PQ-13, rather than PQ-13 objecting to PR-10) and [H-4](AGENT.presence.corpus.planning.md#ph0a-harvest--the-presence-cases-that-already-exist) (twice, and then withdrawn with PR-10's trigger). **Read the harvest's corrections-of-itself as the point rather than as noise:** every one ran the same direction --- a claim true of a narrower object than it was asserted about --- and **no additional corpus case would have caught any of them.** The parent corpus already contains presence cases under other names --- at minimum [C2](AGENT.abstractionLayers.corpus.planning.md#c2-the-rope-in-two-rooms-refused-at-the-gate), [C7](AGENT.abstractionLayers.corpus.planning.md#c7-ariadnes-thread), [C9](AGENT.abstractionLayers.corpus.planning.md#c9-coiling-the-rope-back-in), [C10](AGENT.abstractionLayers.corpus.planning.md#c10-the-moonbase-computer-system). **Cite them; do not restate them.** A case that already exists gets a pointer and a note on which PR row it grades.
  - [X] ~~**PH0b. Write the cases the parent corpus lacks**, which are the **character-side** ones --- movement, connect, disconnect, eviction --- because the parent's corpus was built to grade structure and never had to grade the highest-frequency mutation in the system.~~ **Re-aimed 2026-08-21, and the original was answering the wrong question: it asked what the system does *most*, not what this design *decides*.** [PR-10](#settled-register) makes characters single-ported, so **the presence design never meaningfully touches them** and a character corpus would test nothing. **Write multi-host and nested-cover cases instead** --- [PR-C1](AGENT.presence.corpus.planning.md#pr-c1-the-contraption-in-two-rooms-and-whether-covers-nest) is the first and it broke two rows on arrival. **Keep exactly one character case**, and only as the boundary marker: the case that would falsify *characters are single-ported*. ~~**[PH0a](AGENT.presence.corpus.planning.md#ph0a-harvest--the-presence-cases-that-already-exist) gave this step a brief it did not have (H-4): write the *nested whole with no crossing edges*, the one case that would fire [PR-10](#settled-register)'s revisit trigger.**~~ **Withdrawn 2026-08-21 from conversation, hours after it was written, with the trigger it served.** **The brief asked for a case built to satisfy a predicate that could not be stated** --- see PR-10's withdrawal note --- **and a case written that way illustrates the row it came from rather than testing it.** **What replaced the trigger is an obligation against [`MutationKernelTransferStep`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/kernelStep.ts), which no corpus case can grade**, because the gap is in the planner -> kernel types rather than in the fiction. **PH0b is therefore smaller than it looked**: multi-host and nested-cover cases as originally re-aimed, plus the one character boundary marker, and **nothing owed to PR-10**. **Closed 2026-08-21 on the corpus that already exists, with no new case written, and the check is what closed it: each of the three remaining demands is served and the fourth was withdrawn.** **Multi-host** is served by [C2](AGENT.presence.corpus.planning.md#ph0a-harvest--the-presence-cases-that-already-exist), C7, C9, C10, C11 and [PR-C1](AGENT.presence.corpus.planning.md#pr-c1-the-contraption-in-two-rooms-and-whether-covers-nest). **Nested cover** is served three times independently --- [C11](AGENT.abstractionLayers.corpus.planning.md#c11-the-rube-goldberg-machine) requires chaining, [C7](AGENT.abstractionLayers.corpus.planning.md#c7-ariadnes-thread)'s `thread-span` nests, and PR-C1 adds the alignment failure neither reaches --- and PR-C1 also supplies the bucket shapes a written case would have aimed at: **disjoint**, **non-uniform**, and a single-bucket part inside a multi-bucket parent. **The character boundary marker is present as prose rather than as a case, deliberately.** PR-C1's Finding 1 states the falsifier outright: characters are single-ported *because* a character's host is a room and rooms are not parts of a spanning whole, which is **true of today's tag graph rather than guaranteed by it**. **Writing that as fiction would require a character hosted inside a composed object, which the tag graph does not permit** --- the case would illustrate the row instead of testing it, which is precisely what [H-4](AGENT.presence.corpus.planning.md#ph0a-harvest--the-presence-cases-that-already-exist) was withdrawn for. **The marker's job is to name what would falsify the claim, and it is named.**
  - [X] ~~**PH0c. Write at least one case that would grade PR-2 either way.** A corpus that only contains rope cases will answer *one relation or two* by omission.~~ **Closed 2026-08-21 with no case written, and the reason matters more than the closure.** [PR-2](#open-decisions-design--plan-only) was **not** answered by omission --- it was answered by an explicit **scoping decision**, that character hosting is restricted to `ROOM` until the non-character graph work matures. **A corpus case cannot grade a scoping decision**, because what would change it is the maturity of other work rather than any fiction. **The case that got there is recorded in the row and is worth keeping in view: a character strapped to a torture device, or standing on a table** --- peer `Custom` versus hosting `On`, which is the only reachable form of the character boundary marker [PH0b](#recommended-order) declined to write. **PH0b's reason for declining stands and this is its counterpart:** PH0b could not write *a character hosted inside a composed object* because the tag graph forbids it; this case stays inside what the tag graph permits and asks which edge kind the author should reach for. **The standing caution: PR-2's answer is a scope decision and must not later be cited as evidence about the fiction.** If the restriction lifts, this line does **not** become a debt to write the case --- it becomes a debt to re-ask the row.
  - [X] ~~**PH0d. Write the cases that would falsify totality**~~ **Closed 2026-08-21 without writing a case, on the finding that no case can be written --- see the three dispositions at the end of this item.** --- a part the fiction wants in a whole's graph that **falls in no bucket**, ~~and separately a part the fiction wants in **two** buckets at once.~~ **Struck 2026-08-21: that half was never a falsifier.** The [cover formulation](#presence-as-a-cover) says buckets **may overlap** in its own statement of the condition, so a part in two buckets confirms the formulation rather than breaking it --- and [PR-9](#settled-register) point (4) makes the root the **maximal** instance of exactly that. **A brief that asks for a case which cannot fail is spending corpus effort on nothing; only the *no bucket* half was ever the falsifier.** **If no falsifying case can be written, that is the finding**, and [PR-9](#settled-register) closes on it rather than on the lean. ~~**Also write the doorway case** --- an ordinary edge whose endpoints sit in different buckets --- because PR-9's definitional sub-question cannot be answered from rope cases that stay inside one room.~~ **Retired 2026-08-21: the doorway edge does not exist as one edge**, per PR-9's answer --- CC1b's port-joining, read in reverse, splits it into two edges each lying wholly inside a bucket. **The root question that briefly replaced it was settled the same day** --- the root is in every bucket by definition --- **so what PH0d now writes is a case that would falsify *that*: a whole whose root the fiction wants present in some of its hosts but not others.** Write it against the rope, which needs no new fiction. ~~**If it cannot be written, PR-9's totality half graduates**; the falsifier hunt above is now checking an answer rather than forming one.~~ **Ran 2026-08-21 from conversation: it cannot be written, and the reason is structural rather than a failure to find one.** The cover is indexed by the hosts the whole is present in (**read as *presence bindings* after the [2026-08-21 correction](#presence-as-a-cover) --- the closure is unaffected**) and the root **is** the whole, so the falsifier would have to be a bucket for a host the whole is not present in --- **the index cannot produce one.** Recorded in [PR-9](#settled-register) point (4), which is upgraded from a stated rule to a consequence of the indexing and **therefore owes no re-open trigger.** **What this does *not* do, stated because *totality graduates* would over-read it, is three things.** ~~**(1) It reaches the root only** --- the argument runs *because* the root is the whole, and no other node is. **The non-root half of this brief is untouched and is still the live one: a part the fiction wants in the graph and in no bucket.**~~ **(1) Closed 2026-08-21 from conversation: the non-root half falls too, by a *different* argument that reaches the same place.** The root's is an **identity** argument and genuinely does not generalise. The non-root one is a **construction** argument, and [C10](AGENT.abstractionLayers.corpus.planning.md#c10-the-moonbase-computer-system) supplies the evidence in its own table --- `Port 1 -> ROOM#Lab, presence-linked to Terminal1`. **The port into the lab exists *because* `Terminal1` is there**, so the host index is not independent of where the parts are; it is **minted from them**. A part in a room with no port into it is therefore **not** a part in no bucket --- it is a whole whose extent into that room **has not been authored yet**. **The only genuine falsifier would be a part that is nowhere at all** --- part of the whole, yet not anywhere the whole is --- **and the fiction has no way to mean that, because being a node of the whole's graph *is* being part of the whole.** **[PR-1](#settled-register) demand (3) is not the counter-example it looks like**, on two counts: PR-1 already resolved it into the zero-host question (*"not a fourth demand at all --- it is the zero-host question wearing another name"*), now locked as (ii); and C10's actual complaint was never that the system is absent from the lab but that *"there is a computer system here"* is the wrong answer to **what is here** at room scale --- **apprehension scale, which PR-1 split off deliberately.** ~~**(2) It does not settle the zero-host case**; it hands [PR-1](#settled-register) a sharper question instead --- no hosts means no buckets, totality fails for every node, and the root rule holds vacuously, so the fault is in the **hosting**, not the cover.~~ **(2) Settled 2026-08-21 from conversation, not by this brief but alongside it: [PR-1](#settled-register)'s fork takes (ii).** Hostless wholes are **representable**, totality is **over hosted wholes**, and a whole with no hosts is **out of the invariant's domain** rather than in violation of it. **This brief no longer grades it, and no zero-host case is owed.** **(3) It graduates nothing into the [Settled register](#settled-register)**, because a consequence of the indexing is only as firm as the indexing, and the [cover formulation](#presence-as-a-cover) is not itself settled. ~~**This brief stays open on its non-root half alone.**~~ **Closed entirely 2026-08-21. All three halves are disposed of --- root by identity, non-root by construction, zero-host by [PR-1](#settled-register)'s (ii) --- and the overlap half was struck as unfalsifiable.** **The finding this brief actually produces is that totality is an invariant *maintained by construction*, not a claim about the fiction that a case could break**, and [PR-9](#settled-register) is restated accordingly. **No case is owed and none should be written**; a case asking the fiction to violate totality is asking it to mean something it cannot mean. **What replaces the case, and it is a test rather than a corpus entry:** the invariant can still fail through an **incomplete constructor**, so the thing to exercise is a **nested straddling whole** --- [PR-10](#settled-register)'s Obligation A cascade, where *"the top-level cover will be well-formed and the interior ones will be silently unaligned."* ~~**PR-9's *edge* half needs a different case and [PR-C1](AGENT.presence.corpus.planning.md#pr-c1-the-contraption-in-two-rooms-and-whether-covers-nest) already supplies it** --- the cable spanning two buckets of one graph. **What PH0d owes there is not another case but a choice between PR-C1's readings (a) and (b)**, and the thing that would decide it is a case where minting a port at the bucket boundary is *obviously wrong* fictionally. Write that one.~~ **Retired 2026-08-21: there is no (a)-versus-(b) to choose.** The fork restated PR-9's point (1) as one of its own alternatives, and the case this brief asked for **already exists and already decided it** --- [C7](AGENT.abstractionLayers.corpus.planning.md#c7-ariadnes-thread)'s thread connects span-parts across rooms, so minting a port at every bucket boundary would split exactly the connections that make its route recoverable. **PH0d owes nothing to PR-9's edge half**, which is closed. **What inherited the residual is [PR-8](#open-decisions-design--plan-only)** --- what a *reduction* does with a straddling edge --- **and that is a convention to choose rather than a case to write**, so it does not come back here either.
- [ ] **PH1. Answer the requirement rows** --- ~~**PR-1, PR-2, PR-7**~~ **[PR-2](#open-decisions-design--plan-only) and PR-7 only** --- from the corpus. **[PR-1](#settled-register) left this list 2026-08-21 by graduating**, answered from conversation rather than from cases: four candidate demands in, three answered and the fourth split, with the zero-host fork it inherited resolved to **(ii)**. **What PH1 owes it is the same thing it owes PR-9 --- confirmation, not formation** --- and the two obligations it opened went to the parent as [AB-59](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) and [AB-60](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) rather than staying here. **PR-7 is still unformed, and is now the only requirement row in this phase that is.** **[PR-9](#settled-register) is no longer among them either: it was answered in full on 2026-08-21 and needs only confirmation**, ~~which [PH0d](#recommended-order) supplies~~ **and it graduated the same day --- PH0d closed without a case, on the finding that no falsifier can be written.** **PR-1 inherited one thing from it** --- whether a whole with **zero** hosts is representable, which PR-9 declined and routed here. **PR-9 was answered provisionally 2026-08-21 ahead of the corpus**, so what PH1 does with it is **confirm or falsify, not form** --- and PR-7 arrived here carrying [PR-4](#open-decisions-design--plan-only)'s graduation with it. ~~**Nothing about representation, with one exception:** take **PR-10** here too, immediately after PR-9.~~ **PR-10 was answered 2026-08-21, before PH0 --- *yes*, implicit for single-*ported* --- and its narrowing of PR-2 and PR-3 is already written into both rows.** What PH1 owes it is confirmation, plus the judgement the answer deferred: **whether its host-arity obligation is still correctly parked**, which depends on nothing PH1 does and everything on whether [C2](AGENT.abstractionLayers.corpus.planning.md#c2-the-rope-in-two-rooms-refused-at-the-gate)'s gate has moved. **That judgement got larger on 2026-08-21**: the obligation is now two, because port arity is **inherited**, and the parking decision has to cover the cascade as well as the top level. **[PR-1](#settled-register) was answered 2026-08-21, ahead of the corpus and from conversation, so PH1 owes it confirmation rather than formation** --- and owes specifically the one thing it declined to choose, the **zero-host fork**, ~~which [PH0d](#recommended-order) grades~~ **resolved to (ii) on 2026-08-21 from conversation rather than by a case; PH0d is closed and grades nothing**. ~~**The requirement rows still to be *formed* are PR-2 and PR-7.**~~ **[PR-2](#open-decisions-design--plan-only) was answered 2026-08-21 as well, from conversation, and [PH0c](#recommended-order) closed with it** --- so **the only requirement row still to be formed is PR-7**, and PH1 is now three confirmations and one formation. **What PH1 owes PR-2 is narrower than confirmation, because the answer is a scoping decision rather than a corpus finding:** confirm only that nothing in the corpus *contradicts* one-relation --- the restriction itself is not the corpus's to grade --- and check the one thing the row left open, that the restriction is enforced today only by read-side `isEphemeraRoomId` filters and has no stated rule.
- [ ] **PH2. Answer the remaining representation rows** --- PR-3, PR-5, and whichever parent rows transfer. Only after PH1. **PR-4 was answered out of order on 2026-08-21** (reading (d), provisional) and what remains for it here is **graduation, not argument** --- it needs PR-9's edge/node half, PR-7, and [Channel D](AGENT.abstractionLayers.planning.md#recommended-order)'s CD2/CD3. **That is a dependency on shipped-code work, which no other row in this plan has**, so PR-4 may sit answered-but-open longer than the rows around it; that is expected rather than drift.
- [ ] **PH3. Ground it: the interface.** The stated purpose of this plan is to unblock the next step, and the next step needs something callable. Decide at PH3 --- not before --- whether that is a **locked** interface (with an exclusion list and a re-open trigger) or a **Prototype** (with a dependency tag and a rollback trigger named in advance). **Classify it out loud before the code exists**; see [Graduation tiers](../../../../AGENT.designVariant.md#graduation-tiers-when-a-decision-licenses-code).
- [ ] **PH4. Discharge the founding premise.** Answer [PR-8](#open-decisions-design--plan-only) explicitly: state what the merge-reduce reducer may assume about presence, in the register's verdict cells, so P7 can be authored against it. **This plan is not done when the rows close; it is done when P7 can cite them.**

### What is deliberately not in scope

- **Writing P7.** This plan unblocks it; it does not absorb it.
- **Attention and salience.** [`AGENT.attentionHistory.planning.md`](AGENT.attentionHistory.planning.md) owns what is *remembered*; this plan owns what is *true*. A case about whether a referent is still salient is an attention case.
- **Perspective.** Already settled as orthogonal in the durable concepts doc; a case about second-person copy is not a presence case.
- **The graph's shape.** Locked frame, parent plan.

## Presence as a cover

~~**Working formulation, from conversation 2026-08-21. Not settled** --- nothing is in the [Settled register](#settled-register) and this section is not a graduated row.~~ **Corrected 2026-08-21 later the same day: the second clause went stale within hours and it matters, because rows are told to treat this section as freely revisable.** **This section is still not itself a graduated row** --- it has no ID and no verdict cell --- **but two rows phrased in its vocabulary have now graduated**, and they pin parts of it: [PR-9](#settled-register) settles **totality** (over nodes, over hosted wholes) and [PR-10](#settled-register) settles the **at-most-one-binding** predicate, which is phrased in the index this section defines. **So the revisable surface has shrunk to what those two verdicts do not touch.** Changing what the cover is **indexed by**, or what its **domain** is, now contradicts a settled row rather than merely amending a working note --- and would have to go back through the register. It is written down because the open rows below are phrased in its vocabulary and are unreadable without it.

**Nothing in this formulation is ever serialised, hoisted here 2026-08-21 from conversation because it was stated only in [PR-1](#settled-register)'s method note and a reader meets the vocabulary first.** **There is no `buckets` field and no `cover` record.** Stored: nodes, edges, presence bindings. **Derived: buckets and the cover** --- and for an at-most-one-binding whole, not even derived, since the single bucket **is** the node list. **A stored bucket set would be a second encoding of what edges and bindings already determine, so it could disagree with them, with no tiebreak available because the derived answer is the definition.** That is the same argument that makes presence implicit under [PR-10](#settled-register). **The cover is what the storage is graded against, never a copy of it.**

**Two senses of *present*, disambiguated 2026-08-21 from conversation, and this must be read first because the formulation below uses the word in only one of them.** **Membership --- *is the whole there at all* --- is DERIVED**, from where its parts are: [C10](AGENT.abstractionLayers.corpus.planning.md#c10-the-moonbase-computer-system)'s port into the lab exists **because** `Terminal1` is there. **Apprehensibility --- *does the whole answer "what is here" at that host's scale* --- is DECLARED**, and cannot be derived: rope and moonbase computer have identical presence-port topology and need different room-scale answers, which is C10's finding and is why **apprehension scale** is a declared field. **Everything below is the membership sense.** **The two were fused in C10's own opening wording and briefly re-fused on 2026-08-21** while [PH0d](#recommended-order) was closing --- the closure argument (*the index is minted from where the parts are*) is a membership claim, and C10's *"that is the wrong answer"* is an apprehensibility claim, **so they never actually disagreed.** **Do not let this collapse back into one rule; the corpus case that looks like a counter-example to derived membership is [annotated at the source](AGENT.abstractionLayers.corpus.planning.md#c10-the-moonbase-computer-system) as not being one.**

~~**Presence is a cover of a whole's graph, indexed by the hosts the whole is present in.** For each host, presence names a subset of the whole's nodes: the part of the whole that is *there*.~~ **Corrected 2026-08-21 from conversation: the index is *finer than the host set*, and this sentence had it backwards.** **Presence is a cover of a whole's graph, indexed by the whole's *presence bindings* --- the distinguishable ways it is present, mechanised today as presence ports.** For each binding, presence names a subset of the whole's nodes: the part of the whole that is *there*. **The host is a *property* of a bucket, not its key.** **[PR-C1](AGENT.presence.corpus.planning.md#pr-c1-the-contraption-in-two-rooms-and-whether-covers-nest) tabulates the counter-example to host-indexing and this formulation never absorbed it:** `GrapplingHookGun` has `port_1` and `port_2` with **both** `fromHostId: OBJECT#Contraption`, and buckets `{Gun, CableSpan1}` / `{CableSpan2, Hook}` that are **disjoint**. **Index those by host and they merge, destroying the split the case exists to demonstrate.** **Note how the error survived: PR-C1's amendment was applied to [PR-10](#settled-register)'s predicate --- *port count, not host count* --- and never to the formulation the predicate is phrased in**, so the corrected row and the uncorrected vocabulary sat two screens apart for a day, **and the stale half is the one that got transcribed into a durable doc.** **Why the index cannot be the host set, stated so it does not regress:** port arity is **inherited**, so the ways a whole is present are inherited too; the host set is not, and collapsing to it discards exactly the inherited distinctions [PR-10](#settled-register) is about. **What is preserved --- checked rather than assumed:** *the root is in every bucket* survives untouched, since it turns on the root **being** the whole and not on what keys the index, and [PR-C1](AGENT.presence.corpus.planning.md#pr-c1-the-contraption-in-two-rooms-and-whether-covers-nest) already says so directly (*"each root is in every bucket of its own graph by definition"*) --- its *disjoint* refers to the non-root members it lists. **Likewise the PH0d closure argument holds**: a bucket exists because the whole is present at that binding, so a bucket the whole is absent from is still unproducible. **The semantics/mechanism line is preserved by naming the index *presence binding* rather than *port*.** That the index is **finer than the host set** is semantics, and survives the walk being abandoned; that a binding is realised as a **port** is [PR-4](#open-decisions-design--plan-only)'s mechanism and stays swappable. Buckets **may overlap**. Their union **must be every node** --- that totality condition is [PR-9](#settled-register).

Three levels, and keeping them apart is what makes the rows below separable:

| Level | What it fixes | Row |
| --- | --- | --- |
| **The whole** | An abstraction-fractal level. Has its own graph and a root, per locked-frame clause 1 | Parent plan, settled |
| **A node in that graph** | A **part** at this level --- one of the elements the whole is constructed from in aggregate | Parent plan, settled |
| **The cover** | How that breakdown plays out **relative to the hosting of the whole**: which parts are present in which host | **This plan** |

**The candidate mechanism** is a **walk over presence-bearing relational edges**: each bucket is the reachability walk from a host's presence port, following only edges whose kind carries presence. ~~The aggregation of the buckets from every host the whole is present in reconstitutes the whole graph.~~ **Corrected 2026-08-21: it reconstitutes every *node* of the graph, which is all the cover ever claimed** --- the cover ranges over nodes only, so **edges are not recovered by aggregation and were never in a bucket to recover.** Edges come from the graph, which is what is stored. **The mis-reading this guards against is not benign:** a reader who takes *reconstitutes the graph* literally builds aggregation as a per-bucket edge union, **which silently drops every edge spanning two buckets** --- and those are required by this design, not tolerated by it ([C7](AGENT.abstractionLayers.corpus.planning.md#c7-ariadnes-thread)'s cross-room span connections are what make its route recoverable). **So the aggregate of the buckets is the node list, and the graph is node list plus edges.** Under this mechanism, PR-9's totality stops being a lean and becomes **a checkable invariant** --- every node reachable from at least one presence port over presence-bearing edges.

**What that check is aimed at, sharpened 2026-08-21 and recorded here because this sentence is what other rows cite.** Totality is maintained **by construction** --- ports are minted from where the parts are --- so **the fiction cannot produce a violation**, and [PH0d](#recommended-order) closed on being unable to write one. **That is a theorem, not a gap.** What the check earns its keep against is an **incomplete constructor**: *by construction* here is a claim about a **constructor discipline** rather than about arithmetic, and [PR-10](#settled-register)'s Obligation A names the way it falls short --- a nested straddling whole whose top-level cover is well-formed while *"the interior ones are silently unaligned."* **So read *checkable* as verifying the write paths and the cascade, never as interrogating the world.**

**This is a new *kind* on the existing relational edge, not a parallel edge layer.** `ludicGraph` already carries uuid-keyed, typed, **directed** `{ from, to, kind }` relational edges through one shared serde across every host kind, with a WML convergence path staked out for the wire types. A separate structure walked independently would duplicate all of that. **Which kind or kinds the walk follows is [PR-4](#open-decisions-design--plan-only), answered provisionally 2026-08-21 as reading (d): a `Present` kind, and the walk is distinguished from composition and peer edges by *where the edge starts* --- port -> node --- rather than by a hierarchy among kinds.** Read PR-4 before treating that as settled; it is not graduated, and it carries a dependency on shipped-code work.

**Two derivations have already failed, and both failed the same way.** [C10](AGENT.abstractionLayers.corpus.planning.md#c10-the-moonbase-computer-system) falsified *at least one port egressing here*; plain graph connectivity falsifies *reachable from the port*, because a connected whole makes every bucket the entire graph and the cover says the rope is entirely in both rooms at once. **Both tried to read presence off structure drawn for other purposes.** A dedicated edge type is declared structure whose only job is to carry this --- which is why [PR-4](#open-decisions-design--plan-only) is no longer a derived-versus-declared question.

**What this formulation buys, recorded so the rows are not re-argued from scratch:** [PR-1](#settled-register)'s hardest demand --- *where along a spanning thing an attachment sits* --- is answered structurally, as *which bucket*. [PR-10](#settled-register) gets a reason rather than a lean: single-**presence**-ported means a **one-bucket cover**, which is total by construction and identical to membership. (*Single-hosted* until [PR-C1](AGENT.presence.corpus.planning.md#pr-c1-the-contraption-in-two-rooms-and-whether-covers-nest); a whole may hold two ports from one host, so **port count is the predicate and it is inherited from the parent, not read off the thing**.) [PR-2](#open-decisions-design--plan-only)'s third answer gets its argument: character presence and extent become **one relation at different cardinality**, one bucket versus many. And [PR-8](#open-decisions-design--plan-only) gets its first candidate sentence, because a merge-reduce **sub-graph is a bucket**, what sub-graphing *drops* is everything outside it, and *walked onward* is crossing into an adjacent bucket through the port.

## Open decisions (design --- plan only)

Plan-only. Rows [graduate](../../../../AGENT.designVariant.md#graduating-a-resolved-row) rather than being deleted; the [Settled register](#settled-register) is below. **IDs are stable and never reused.**

**Every row below is Open and unargued.** The *Constrained by* column names what a row must answer to, not what it has already been tested against. **Requirement rows come first deliberately** --- PR-1, PR-2 and PR-7 are answerable from the fiction; the rest depend on them.

**The table is in dependency order, not ID order.** PR-9 and PR-10 were added 2026-08-21, after the rest were written, and are **more foundational than PR-2** --- they are placed where they must be worked. IDs are not renumbered to match; they are stable and never reused.

| ID | Question | Constrained by | Status |
| --- | --- | --- | --- |
| **PR-11** | **Does a port carry a `kind`, and what discriminates a *presence* binding from a purely relational one?** **Split out of [PR-10](#settled-register) on graduation, 2026-08-21, because its stated destination was stale** --- PR-10 booked it *on LP6/PR-4*, but [LP6](AGENT.ludicGraphPorts.planning.md#recommended-order) was narrowed 2026-08-15 (*"most of this moved to LP4d"*) and PR-4's discriminator talk is about **hosting kinds**, a different question. **Nothing owned it, and graduating PR-10 without this ID would have stranded it.** [`EphemeraLudicGraphPort`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts) is `{ portId, fromHostId }` with no discriminator, so **`ports.length` is not the binding count**: a cord threading into a flashlight is a crossing that locates nothing, and a whole may hold three ports and one bucket. **Two shipped consumers wait on it** --- PR-10's implicitness predicate, and PR-9's totality checker, which must read arity **before** grading or every disconnected character reports as a mass violation. **Not merely a field:** decide whether presence-bearing is a property of the **port** or of the **edge kind** it fronts, since [AB-54](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) already partitions kinds and a second, disagreeing partition on ports would be the drift that section warns about. | PR-9; PR-10; AB-54; LP4d | **Open, and unblocked** --- needs no corpus case, only a decision. **Gated in *practice* on ports existing in code ([CC1](AGENT.abstractionLayers.planning.md#recommended-order), BLOCKED), but the design question can be answered now** |
| **PR-12** | **How does the write side maintain the cover, at every level rather than the top one?** **Split out of [PR-10](#settled-register) on graduation, 2026-08-21**, carrying its Obligations A and B intact. **A: the cascade.** Because arity is inherited, splitting a whole across a second host obliges partitioning its parts, and *their* parts, **to the depth of the composition** --- so *add a host* is an authoring act at every level, not a derivation. A constructor that stops at the top leaves a well-formed outer cover over **silently unaligned** inner ones. **This is the one way totality can actually fail**, per [PR-9](#settled-register): the invariant is by-construction, so **the constructor is what the check is aimed at**, and a nested straddling whole is the test that would catch it. **B: the read side**, cheaper --- traversal must follow **binding-to-binding, never binding-to-host**, or it re-introduces the one-to-many hop below the first level. **Also here:** the top-level arity change, gated on [C2](AGENT.abstractionLayers.corpus.planning.md#c2-the-rope-in-two-rooms-refused-at-the-gate)'s refusal being lifted; the planner -> kernel boundary needing to carry a **redistribution** (which entities to which binding, not merely which host) since [`MutationKernelTransferStep`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/kernelStep.ts) has a singular `toHostId` and cannot express a pure add; and the reverse direction --- collapsing to one host makes interior bindings unreachable, which is [C9](AGENT.abstractionLayers.corpus.planning.md#c9-coiling-the-rope-back-in)'s question asked recursively. | PR-9; PR-10; C2; C9; AB-55 | **Open. Design-only here, worked on the action side** --- this plan does not reach across to write it there. **Recursion depth is not the worry and should not be re-litigated:** bounded by composition depth, on rare authoring acts, and the read side is one hop |
| **PR-2** | **Are character presence and whole extent one relation or two?** See [the table above](#two-things-are-called-presence-and-whether-they-are-one-thing-is-pr-2). Locked-frame clause 3 argues one; single-valuedness, ~~the terminal `multiPresent` gate,~~ the separate shipped machinery and a mutation rate differing by orders of magnitude argue two. **The gate is struck 2026-08-21 --- it was a mis-citation and never argued anything about characters; see [the table above](#two-things-are-called-presence-and-whether-they-are-one-thing-is-pr-2), where it is corrected at source.** **A third answer is available and should not be reasoned past: one relation with a declared cardinality constraint on the character case.** **Work [PR-9](#settled-register) and [PR-10](#settled-register) first** --- if PR-10 holds, this row loses its representation half entirely (characters would carry no presence record to compare) and what remains is purely a question about the fiction. **Answered 2026-08-21 from conversation: the third answer, one relation --- but the constraint is on host *kind*, not on host *count*, and that difference is the whole content of the answer.** **The restriction: a character's membership host is a `ROOM`, and only a `ROOM`.** Objects are unrestricted and may be hosted by objects, features and areas as they already are. **Cardinality is not declared separately because it is a consequence:** a room is never a *member* of any graph --- the kernel's [`MutationKernelTransferStep`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/kernelStep.ts) admits only objects and characters as `entityIds` --- so a room never inherits ports, so a character hosted in a room is single-**presence**-ported by construction. **This upgrades the premise [PR-10](#settled-register) flagged as *a fact about the current tag graph, not a theorem*.** It is still not a theorem, but the residual premise moved from *no character's host happens to be partitioned* --- an accident of what has been authored --- to *rooms are never members*, which is enforced in a type. **Why the restriction, in the terms it was given, because the reason governs when it lifts: it is a scoping decision taken to keep the character path simple until there is a working graph system for non-characters.** **It is not a finding that characters are ontologically unlike objects, and must never be cited as one.** Read against [the scoped-boundary rule](../../../../AGENT.designVariant.md): the boundary was drawn against the immaturity of the non-character graph work, so what lifts it is that work maturing --- not new fiction, and not a corpus case. **The revisit is expected rather than hypothetical.** **Three of the four arguments for *two* dissolve, and the fourth changes job.** *Single-valuedness* is now port arity 1, the same property that makes a lantern's presence implicit --- a different count, not a different relation. *The `multiPresent` gate* was never a character gate: [`evaluateComplexityPreGates`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/complexityPreGates.ts) is typed on `EphemeraObjectId`, sits under `objectManipulation/`, and its only caller is `compileMembershipAtomic` --- **it gates objects, and is [C2](AGENT.abstractionLayers.corpus.planning.md#c2-the-rope-in-two-rooms-refused-at-the-gate)'s refusal on the object side, so it argued sameness all along.** *Separate shipped machinery* is thinner than it looks: connect and disconnect compile through `buildCharacterMoveOp` -> `compilePositionKernelOp` into ordinary kernel steps, and what is genuinely character-specific is the eviction ladder and the narration, not the presence write. *Mutation rate* survives, but **not as an argument about the fiction** --- it is a cost, and it lands on the option this row declined; see the torture-device case below. **The answer is already implemented and was not recognised as an answer.** [`resolveCharacterRoomId`](../../../../../lambda/ephemera/dataSource/positions/membership/resolveCharacterRoomId.ts) narrows containers with `isEphemeraRoomId` before reading them, and `syncMembershipAdjacency` does the same. **That filter is this restriction, shipped and undocumented** --- the same shape as PR-10 finding the existential invariant was its own answer already in code. **Consequence worth stating plainly: hosting a character in an object's graph is *type-legal today* and would not fail loudly.** An Object is a legal `EphemeraMembershipHostId` and the kernel already admits a character as a transferable entity, so the transfer would succeed --- and then the character would have zero *room* containers, fall through to the `RoomStack`, and read as **out of play**. **A restriction enforced by a read-side filter fails silently by construction**, so if this row's restriction is to be relied on it needs a stated rule rather than four independent narrowings; **booked, not written here.** **The case that decided it, and it is better than the one this row was going to be given: a character strapped to a torture device, or standing on a table.** The choice is between a **peer** kind (`Custom`, with a `relationLabel`, character stays a room-level node) and a **hosting** kind (`On`, character becomes a member of the object's graph) --- which is [AB-54](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only)'s own partition, so the case tests exactly one variable. **Answered: `Custom`.** **And the reason inverts what this plan had assumed about `Custom`.** Its opacity --- [the validator requires a non-empty author-supplied `relationLabel`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts) that no generic rule can read --- was first written up here as its *cost*. **It is the mechanism.** An opaque peer edge is what routes *what happens if someone lifts the table* to the reasoning engine; a hosting edge pre-answers it structurally, and answers it silently. **The engine firing on that question is the design, so the edge that provokes it is the right edge.** **What this therefore rules out for now, named as a feature rather than as a gap: structural immobilization.** A hosting edge would make *leave the room* inexpressible without first transferring the character out --- [PR-1](#settled-register)'s mutation grammar applied to a character, and a real thing to want. **It is deferred with the restriction and returns with it.** Until then a restraint is a peer edge plus whatever the reasoning engine makes of it, and **nothing in the presence graph enforces it.** **The cost of the option declined, recorded so the revisit does not have to rediscover it:** under hosting, a strapped character sits in the device's bucket, so *who is in this room* stops being one adjacency query and becomes an aggregation walk --- on the highest-frequency read in the system. **That is where the mutation-rate argument actually bites.** Note it does **not** make the character un-apprehensible: apprehensibility sits on the whole ([PR-1](#settled-register)), and a person strapped to a device is an answer at room scale in a way [C10](AGENT.abstractionLayers.corpus.planning.md#c10-the-moonbase-computer-system)'s router is not. **The cost is in the read path, not in the fiction.** **One reclassification this row makes on the way past: `RoomStack` is not presence.** [`resolveCharacterRoomId`](../../../../../lambda/ephemera/dataSource/positions/membership/resolveCharacterRoomId.ts) consults it **only** when membership is empty, and what it returns is the top frame after trimming to accessible assets --- *where should this character be placed on re-entry*, not *where is it*. **It is a different relation sharing a read path, and unifying presence does not touch it.** **Falls out to other rows.** **[PR-3](#open-decisions-design--plan-only) loses characters entirely** for as long as the restriction holds, since a room-hosted character is single-ported by construction and PR-3 is the multi-ported carrier question. **[PR-1](#settled-register)'s zero-host fork gains a shipped witness**, since [`repairCharacterLegalPlacement`](../../../../../lambda/ephemera/dataSource/positions/membership/repairCharacterLegalPlacement.ts) reads `containers.length > 0` as *in play* --- a disconnected character **is** a whole with zero hosts, represented and persisted; it grades toward fork (ii), ~~**with the honest caveat that fork (i) may answer that a disconnected character is *out of the world* rather than *in the world at nowhere*, and that distinction is exactly what (i) would owe.**~~ **Caveat discharged 2026-08-21: (ii) was taken, so this witness is now confirming evidence rather than a lean.** A disconnected character is **in the world at nowhere** --- representable, and simply outside totality's domain. **[AB-54](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) gains a second amendment** alongside the one already booked for PR-4's partitioning class: character-to-object relations take **peer** kinds, so AB-54's partition is now constrained on the character side and should say so. **And a terminology hazard, flagged because it will be conflated: `kind: 'On'` is a *hosting* kind, while a character on a table is `kind: 'Custom'` with `relationLabel: 'On'`.** The same word on two fields means opposite things about where the character lives. **Prefer a different label in examples, or state the distinction wherever `On` appears.** **What this row does not do:** it does not merge the shipped code. [`syncMembershipAdjacency.ts`](../../../../../lambda/ephemera/dataSource/positions/membership/syncMembershipAdjacency.ts) and [`syncObjectMembershipAdjacency.ts`](../../../../../lambda/ephemera/dataSource/positions/membership/syncObjectMembershipAdjacency.ts) are identical line-for-line apart from the identifier name, which is now justified as duplication rather than as coincidence --- **but the dedup has a shelf life**, because both filter to rooms and only the character one is *supposed* to. **Whether the object one filtering to rooms is already wrong for a boxed object is a question this row raises and does not answer**; it has not traced that function's callers. **Design-only, no code in scope.** | Locked-frame clause 3; `AGENT.concepts.md`; PR-9, PR-10; ~~PH0c~~ | **Answered 2026-08-21 --- one relation, character hosting restricted to `ROOM`.** **Not graduated:** the restriction is a **scoping decision, not a finding**, and lifts when the non-character graph work matures. **Structural immobilization is deferred with it.** Shrinks [PR-3](#open-decisions-design--plan-only) (characters leave it entirely), supplies [PR-1](#settled-register) a zero-host witness, and owes [AB-54](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) an amendment. **Owed: a stated rule, since the restriction is enforced today only by read-side filters that fail silently** |
| **PR-3** | **Representation: what carries presence?** The parent's live candidates are a **port list in the whole**, a **stored set**, and a **predicate derived over graph edges**. The shipped answer is a **fourth**: node membership in the host graph, with `EphemeraPositionAdjacency` as a reverse index under an existential invariant. **This row is [AB-8](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only)'s representation half and may take it by transfer --- see [the transfer question](#which-parent-rows-this-plan-serves) --- keeping the AB-8 id if it does.** ~~**[PR-10](#settled-register) may shrink this row before it is argued**~~ **--- it did, 2026-08-21.** This row is now *what carries presence **for multi-*ported* things***, which is materially smaller than what AB-8 has been carrying since 2026-08-05. **It shrank by less than it first looked**, though: port arity is inherited, so a single-hosted part of a spanning whole is in scope for this row. **The shipped fourth answer is confirmed rather than displaced for the single-*ported* case**, since PR-10 settled that membership *is* the representation there. **Shrank again 2026-08-21 when [PR-2](#open-decisions-design--plan-only) was answered: characters leave this row entirely.** A character's host is a `ROOM`, a room is never a member of any graph and so never inherits ports, and a character is therefore single-presence-ported **by construction rather than by observation** --- which is a stronger exemption than PR-10 could give, since PR-10's version rested on no character's host *happening* to be partitioned. **The exemption is only as durable as PR-2's restriction**, which is a scoping decision with a stated lift condition, so **this row inherits the revisit**: if character hosting widens beyond rooms, characters re-enter PR-3 and they re-enter it as the highest-frequency read in the system. | PR-1, PR-2, PR-10; [AB-6](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only); [AB-55](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) | Open --- **representation row**, blocked on PR-1. **Narrowed twice: to multi-*ported* things by PR-10, and clear of characters entirely by PR-2** |
| **PR-4** | **What structure does the cover walk over?** **This row is no longer *derived versus declared*** --- the [cover formulation](#presence-as-a-cover) makes it both, and the dichotomy was the reason two derivations failed. The mechanism is **a kind on the existing relational edge**, walked from the presence port. **Two facts from the shipped code, neither of which is in the durable docs:** the kind list is `['On', 'Under', 'Against', 'Custom', 'In', 'PartOf']` ([`ludicGraph/baseClasses.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/baseClasses.ts)) --- **`In` and `PartOf` already ship** --- and relational edges are **already directed**, `{ from, to, kind }`. **The open question is which kind or kinds the cover walks, and there are three readings:** (a) **a new `Present` sibling kind** --- cleanest to reason about, but leaves three containment-ish kinds needing a rule, because a part `PartOf` a whole that is present in a room is hard to argue is *not* covered; (b) **walk `In` / `PartOf` as they stand** --- no new kind, but then presence is a reading of edges drawn for another purpose, **which is the failure mode that killed the previous two derivations**; (c) **`Present` is the generic and `In` / `PartOf` / `On` are specializations that imply it**, with the walk following any presence-implying kind --- **which was the reading nominated as most likely to survive, until (d)**; it remains [PR-7](#open-decisions-design--plan-only)'s live sub-question with shipped code attached rather than fiction; (d) **discriminate by *source endpoint*, not by kind** (added 2026-08-21 from conversation): composition edges are **root-incident** (`In`, `PartOf`, and --- after [Channel D](AGENT.abstractionLayers.planning.md#recommended-order) --- `On`, all running member -> root per [LD-16](AGENT.ludicGraphPorts.planning.md#open-decisions-implementation--plan-only)); peer edges run **node -> node** (`Under`, `Against`, `Custom`); presence edges run **port -> node**. Three structurally distinguishable classes, and **no conceptual hierarchy is required at all.** **Decided 2026-08-21: (d), provisionally --- see the verdict paragraph below.** **Directedness is settled by the shipped shape and is load-bearing:** were the walk undirected, a connected presence subgraph --- a chain running the length of a rope, the natural way to model one --- would let every port reach everything and **the cover would trivialize exactly as plain connectivity did**. Fanning outward from the port, buckets stay distinct structurally and overlap acquires a clean meaning (a node reachable from two ports). **The verdict, taken 2026-08-21: (d), provisional and not [graduated](#settled-register).** **Three reasons, in order of weight.** **First, (d) widens the existing taxonomy where (c) crosses it with a second one --- and the distinction is *additive versus multiplicative*, not *some versus none*.** **Stated precisely, because the first draft of this row overstated it (corrected 2026-08-21 from conversation): (d) does expand the kind taxonomy, from two classes to three.** [AB-54](AGENT.abstractionLayers.planning.md#settled-register) partitioned kinds **hosting** (`On`, `In`, `PartOf`) versus **peer** (`Under`, `Against`, `Custom`); (d) adds a third class --- call it **partitioning** --- alongside them. What it does **not** do is lay a *presence-implying* generic **across** that cut, which is (c): under (c) every kind answers **two** questions (hosting or peer? *and* presence-implying or not?) and the classes multiply; under (d) every kind answers **one**, and the answer is read off **where the edge starts**. **So the claim is 3 classes rather than 2, against 2 x 2 --- an extra bucket, not an extra dimension.** **This is the cheaper expansion, not a free one, and it carries a bill:** see the AB-54 amendment obligation below. **Second, (d) escapes the failure mode that killed (b) and the two prior derivations** --- a `Present` edge is drawn **for** presence, so nothing is being inferred from structure authored for another purpose, while (a) still owes the containment rule and (b) is the failure mode by construction. **Third, the cost is a slice that is already planned rather than a new mechanism.** [`EphemeraLudicPortAddress`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts) (`{ owner, port }`) and `EphemeraLudicTerminalId` (primitive \| port address) **already ship**; relational edges admit only the primitive branch, and the type's own comment says why --- *"Port-address terminals are Stage 2/LP7, deliberately not admitted here yet."* So (d) costs **widening the relational endpoint from `EphemeraLudicTerminalPrimitive` to `EphemeraLudicTerminalId`**, which is a deferred-on-purpose slice with a name, not an invention. **The bill for the third class: (d) puts an amendment obligation on a Settled row.** [AB-54](AGENT.abstractionLayers.planning.md#settled-register)'s verdict says in terms that *"relation kinds partition **in two**"*, and it reached that partition by **reading it off shipped behaviour** rather than imposing it. A third class **falsifies that sentence as written** --- not its content, which survives intact for the six kinds it ranges over, but its **exhaustiveness**. **Do not let this ride in silently as a consequence of PR-4 closing**, which is precisely how a settled row goes stale: AB-54 is amended, in its own plan, with the two-way claim rescoped to *the kinds that connect nodes to nodes* and the third class named. **Booked here rather than done here**, because this plan does not edit the parent's register --- and it is a real cost of (d) that (a) and (c) do not pay, since neither adds a class beside AB-54's cut. **What would make this bill bigger than an edit:** if the partitioning class turns out to need **its own** hosting-versus-peer answer --- a `Present` edge that hosts --- then (d) has quietly become a second dimension after all, and the reason to prefer it over (c) is gone. **[PR-7](#open-decisions-design--plan-only) is where that gets checked**, and it is the sharpest form of why that row now blocks this one. **(d) acquired an obligation from [PR-C1](AGENT.presence.corpus.planning.md#pr-c1-the-contraption-in-two-rooms-and-whether-covers-nest) on 2026-08-21: nested covers are representable but not *aligned*.** Presence edges run port -> **node**, so when a partitioned whole is itself a part of a partitioned whole, the parent's two ports both point at the one child node and **nothing states which of the child's interior ports pairs with which of the parent's**. A well-formed child bucket is then not attached to one host rather than the other. **The repair, settled 2026-08-21 from conversation: the presence edge's target is a *port-qualified terminal*.** Contraption's graph carries `port_A -[Present]-> GHG#port_1`, where `GHG#port_1` is `{ owner: GHG, port: 'port_1' }` --- **the same shape as [LP7](AGENT.ludicGraphPorts.planning.md#recommended-order)'s own example, `PowerCord -[ThreadsInto]-> FLASHLIGHT:1`.** **This adds no mechanism and needs only LP7, which (d) already depends on for edge endpoints** --- so it is genuinely one slice, and that convergence is evidence for (d). **Three things a reader will want to re-raise, pre-answered:** **(i) it is not a cross-shard reference** --- `owner` is `GHG`, which is already a node in Contraption's graph, so the terminal qualifies a local node rather than reaching into another shard; **(ii) it is not [LD-3](AGENT.ludicGraphPorts.planning.md#open-decisions-implementation--plan-only)'s bare port-to-port edge**, and that row's *"bare"* was quantified over the **mediator**, not the endpoints --- [LC7](AGENT.abstractionLayers.ludicCache.corpus.planning.md#lc7-the-cable-that-passes-straight-through-the-box) restated the question as *is there ever a pass-through with **no nameable mediator**?*, about one host's own port A joined to its own port B with nothing named between; here the far end **is** named, as `owner`; **(iii) it does not duplicate the port record** --- GHG's `fromHostId: OBJECT#Contraption` and Contraption's edge to `GHG#port_1` are premise 12's **interior half and exterior half** of one record, which is how every port already works. **Considered and rejected: widening the port record's `fromHostId` to name the parent *port*.** It would move an exterior-owned fact into the interior record, inverting premise 12, and it makes a port address **stand free of an edge**, which trips the serde deferral in [`ephemeraMeta.ts`](../../../../../packages/mtw-interfaces/ts/ephemeraMeta.ts) and pulls [LD-14](AGENT.ludicGraphPorts.planning.md#open-decisions-implementation--plan-only) forward for no gain. **The obligation is real and (d) does not graduate without LP7**, but it is a scheduled slice rather than a design gap. **What (d) depends on, and it is a hard dependency rather than a note: [Channel D](AGENT.abstractionLayers.planning.md#recommended-order).** The source-endpoint discriminator is only clean once **all three** hosting kinds are root-incident. `On` ships peer-to-peer today, so **against shipped code the discriminator is ambiguous and (d) is unimplementable** --- an `On` edge is structurally indistinguishable from `Under`. **PR-4 therefore cannot graduate before CD2/CD3 land**, and that is the first time this plan's design has taken a dependency on shipped-code work rather than on another row. **What would falsify (d), stated so the corpus can aim at it:** a part the fiction wants present that is **reachable from no port** (which falsifies (d) and [PR-9](#settled-register) together --- [PH0d](#recommended-order) already writes this case); a **composition** edge that must run node -> node inside one graph, which [AB-53](AGENT.abstractionLayers.planning.md#settled-register) explicitly leaves droppable and whose drop would collapse the composition/peer distinction the discriminator rests on; or the LP7 widening being refused or reshaped. **Not decided by (d), and do not read it in:** whether the cover ranges over **edges** as well as nodes (PR-9's definitional half --- (d) covers nodes reached, and says nothing about the doorway edge), and whether a hosting edge *also* implies presence, which stays [PR-7](#open-decisions-design--plan-only)'s. Inherited and still live: whether [C10](AGENT.abstractionLayers.corpus.planning.md#c10-the-moonbase-computer-system)'s **apprehension scale** is world truth or perspective-relative, and whether [P3's reserved field](AGENT.abstractionLayers.proposals.planning.md#h3-ports-as-the-boundary-mechanism-decomposition-as-a-modellers-choice) is where it lands --- ~~with a candidate that needs no new mechanism: apprehension as a cost or threshold *on a `Present` edge*. A part reachable only across a high-cost edge is present-but-only-by-traversal, which is what apprehension scale was invented to say. Hold that as a candidate, not a claim, until apprehension is actually worked.~~ **Struck 2026-08-21 from conversation, and the reason is worth more than the candidate was.** It graded a `Present` edge by **how noticeable** the part at its far end is --- but a `Present` edge is a **truth claim**, and if a part is in the bucket it is in the host. Putting a noticeability dial on it makes presence answer *what is immediately apprehensible* rather than *what is true*. **[PR-1](#settled-register)'s split is what separates the two, and only half of it lands here:** *present-but-only-by-traversal* is noticeability and leaves presence altogether (rehomed to the parent's apprehension-versus-description axis), while **apprehension scale is scale-relative truth and stays**. **So the inherited question in the sentence above is unaffected by this strike and remains live** --- what is gone is a proposed mechanism, not the question it was proposed for. **One further obligation, added 2026-08-21: the partitioning class now has a second half.** [PR-10](#settled-register) settled that the **port record carries a kind field** so a whole can count its presence ports locally --- so the distinction (d) draws on the **edge** is the same distinction the **port** must declare. **Name it once.** If the edge class and the port kind end up with two vocabularies for one idea, that is this row's failure rather than [LP6](AGENT.ludicGraphPorts.planning.md#recommended-order)'s, because (d) is where the class is defined. | C10; PR-1; PR-9; PR-7; [Channel D](AGENT.abstractionLayers.planning.md#recommended-order) (CD2/CD3); LP7 endpoint widening; LP6 port kind | **Answered provisionally 2026-08-21 --- reading (d).** Not graduated: blocked on PR-9's edge/node half, on PR-7, and on Channel D making `On` root-incident. **Owes LP6 a shared vocabulary for the partitioning class** |
| **PR-5** | **What is the query interface, and which shapes must resolve in one keyed read?** At minimum: *what is present in H*, *where is X present*, and --- the one merge-reduce actually needs --- *is X present in H* as a decidable predicate over a graph the reducer already holds. **A candidate that cannot serve the hot shape in one read is disqualified on latency before the corpus is consulted**, which is the sibling plan's rule and transfers cleanly. | PR-3; [PR-8](#open-decisions-design--plan-only) | Open |
| **PR-6** | **Who writes presence, and is the write transactional with the mutation kernel?** The shipped invariant makes the adjacency row and graph membership two records that must agree, and the kernel already re-validates presence at commit time against freshly-fetched host graphs. **The question is whether presence gets its own write path or stays a consequence of graph mutation** --- and, if the former, what stops the two disagreeing. | `AGENT.contract.md`; PR-3 | Open |
| **PR-7** | **What does presence deliberately *not* answer?** Nominated boundaries, to be confirmed or rejected rather than assumed: not **reachability** (P6 clause 3 keeps that with the graph), not **salience** (the attention sub-plan), not **perspective** (already settled orthogonal), and not **containment semantics** --- *in* versus *held by* versus *attached to* may be presence with a manner, or a different relation entirely. **The last one is the live one**, and it touches [AB-57](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) and [AB-58](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only). **It is no longer a purely fictional question: `In` and `PartOf` already ship as relational edge kinds**, so *presence with a manner versus a different relation* has a committed answer in code that this plan has not examined. ~~**If [PR-4](#open-decisions-design--plan-only) takes reading (c) --- presence as the generic that `In` / `PartOf` / `On` specialize --- this row supplies the implication rule**, and the two close together or not at all.~~ **Rewritten 2026-08-21: PR-4 took reading (d) instead, and that does not relieve this row --- it isolates it.** Under (d) the three classes are told apart by source endpoint, so **nothing in the mechanism says whether a hosting edge also implies presence**; the question survives (d) intact and is now *only* this row's. **The concrete form it takes: is a part `PartOf` a whole, where the whole is present in a room, itself in that room's bucket?** Under (d) it is **only if a `Present` edge reaches it from the port** --- so either the constructor authors both edge classes together, or composition and presence can disagree and the fiction has to say which is true. **Answer this before PR-4 graduates.** | P6 clause 3; AB-57; AB-58; PR-4; `ludicGraph/baseClasses.ts` | Open --- **requirement row; PR-4 now blocks on it** |
| **PR-8** | **What may the merge-reduce reducer assume about presence?** The row this plan was opened to answer. Findings 3, 4, 5 and 6 each make a presence demand: *may the reducer consult a graph it has already merged* is a question about accumulated presence state; sub-graphing **drops**, and what may be dropped is a presence judgement; recovery is *a keyed walk*, and the key is a presence key. **The [cover formulation](#presence-as-a-cover) supplies the first candidate sentence** --- a sub-graph *is* a bucket, what sub-graphing drops is what falls outside it, and *walked onward* is crossing into an adjacent bucket through the port. **That is a candidate, not the verdict**, and it is only as good as PR-9 and PR-4 turn out to be. **Sharpened 2026-08-21, once PR-9 answered: a merge-reduce sub-graph is the root, unconditionally, plus the nodes reached from that host's port.** Two things that were previously open no longer complicate it --- **the reducer never has to decide an edge's bucket**, because a boundary-spanning edge is already two edges split at ports, and **it never has to walk to find the root**, because the root's membership is definitional. **Still a candidate**: it inherits PR-4's dependency on [Channel D](AGENT.abstractionLayers.planning.md#recommended-order) and on [PR-7](#open-decisions-design--plan-only), and PR-5 is untouched by any of this. **And one of the two simplifications was withdrawn the same day.** [PR-C1](AGENT.presence.corpus.planning.md#pr-c1-the-contraption-in-two-rooms-and-whether-covers-nest) re-opened PR-9's edge half, so **the reducer's freedom from edge-bucket questions holds only for edges crossing a *graph* boundary** --- an edge spanning two buckets **inside one graph** is undecided, and *taking bucket 1, do I get the cable?* is this row's question again under PR-9's reading (b). **The root simplification is unaffected.** **Restated 2026-08-21, once PR-9's fork was struck, and the question is this row's *outright* rather than on loan from PR-9.** **The cover ranges over nodes only and always did**, so no edge is ever a bucket member and the reducer is never owed an edge's bucket. **What the reducer is owed is a *reduction convention*:** a bucket is a set of nodes, and the sub-graph it induces has to say what becomes of an edge with **one endpoint outside** --- keep it with a dangling terminal, drop it, or emit something that records the cut. **Three candidates, and the third is favoured --- but on its *content* only, and the qualification is not optional:** [CC1b](AGENT.abstractionLayers.planning.md#recommended-order)'s `crossings`, which joins a boundary-spanning edge out of two halves. **Its proper home is here, at reduction time, describing a straddle** --- it was previously read in reverse, at authoring time, to *prevent* one, which is what PR-9's struck reading (a) proposed and [C7](AGENT.abstractionLayers.corpus.planning.md#c7-ariadnes-thread) falsified. **The qualification, added 2026-08-21 on a check of CC1b's own text: CC1b is an unstarted sub-step of [CC1](AGENT.abstractionLayers.planning.md#recommended-order), which has been BLOCKED since 2026-08-15, and it carries a flag dated 2026-08-20 that this row must not read past.** The flag says CC1b *"spells out, in implementable detail, the algorithm [P7's staged finding 2](AGENT.abstractionLayers.ludicCache.corpus.planning.md#mechanism-findings-staged-for-p7) argues against building"*, and forbids *"reading CC1a/CC1b as the specification and implementing them directly."* **What is at risk there is the *shape* --- batch concatenation --- and what is explicitly not retracted is the *content*:** *"crossings per hop, never merged, never tie-broken, port-address terminals **is what P7's reducer must also produce**."* **So `crossings` is a candidate here as a record of what a cut edge carried, and emphatically not as an endorsement of a concatenate-then-join pass.** **The hazard is sharper for this row than for most, because P7 is the thing that discharges it** --- taking CC1b's shape here would import into PR-8 the premise P7 exists to revisit. **Re-read CC1b against P7 before this row closes, as its flag instructs.** **C7 also constrains the answer before this row argues it:** the thread's span-parts connect across rooms, and the case turns on the connection recording *"which exit each connection traverses"*, so **a convention that simply drops straddling edges makes C7's route unrecoverable and is not available.** **Note what this costs and does not cost: nothing here reaches back into PR-9**, since the cover's domain was never in question; the reducer inherits a convention to choose, not a semantics to repair. **This row does not close until its verdict is a sentence P7 can be written against.** | [Staged findings for P7](AGENT.abstractionLayers.ludicCache.corpus.planning.md#mechanism-findings-staged-for-p7); PR-5 | Open --- **the discharge row; PH4** |

### Which parent rows this plan serves

**Stated as a question rather than executed, because a transfer that happens quietly leaves the parent's readers pointing at rows that moved.**

Three parent rows are candidates for transfer into this plan: **AB-8**'s presence half, **AB-6**'s presence half (the row already carries a note that its member-set and presence halves *"are different things and the answers may legitimately differ"*), and **AB-55** (*does the presence port subsume `EphemeraPositionAdjacency`*), which is a presence row in all but filing.

**Two things are settled about how a transfer would work, if one happens.** A transferred row **keeps its ID** --- IDs are stable and never reused, and cross-references outlive rows. And the parent must retain a pointer where the row was, because a design row closes into *other rows that argue from it*, and a silently relocated row strands them.

**Whether to transfer at all is the author's call and is not made here.** The alternative is real and may be better: this plan supplies the requirement half, the parent's rows stay where their dependents can see them, and PR-3 cites AB-8 rather than absorbing it.

### Settled register

**Three rows settled 2026-08-21** --- PR-9 and PR-10 first, then PR-1 later the same day, once the two obligations it carried had somewhere to live ([AB-59](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only), [AB-60](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only)). Rows land here with a **self-sufficient verdict cell** --- everything a live row needs, with the link reserved for readers asking *why*.

| ID | Question | Verdict | Settled | Detail |
| --- | --- | --- | --- | --- |
| **PR-1** | **Requirement: what must "X is present in H" be able to express?** | **Four candidate demands in; three answered, the fourth split.** **(1) Extent without ranking, and presence not keyed to host kind.** Multi-host extent and *held-and-present* are **one** demand: a thing may be present in several hosts with **none privileged as *the* location**, and `EphemeraMembershipHostId` is already **one union of five kinds** (`Room`, `Character`, `Object`, `Feature`, `Area`), so `Theseus -Held-> thread:1` alongside `thread:2 -In-> room` is a two-host extent over a heterogeneous index set and needs no separate device. **What would now need arguing is *exclusivity*, and that inversion is the gain.** **(2) Where along a spanning thing an attachment sits**, stated walk-free: presence must locate an attachment **relative to the extent**, not only relative to the whole --- answered structurally as *which bucket*. **(3) A whole is not necessarily present where its parts are** ([C10](AGENT.abstractionLayers.corpus.planning.md#c10-the-moonbase-computer-system)), and **apprehension scale stays, as a *truth* question:** *at which level is this thing the answer to what is here* is **scale-relative truth**, not a lossy view --- *"there is a terminal here"* is correct at room scale. **(4) Grading by noticeability leaves.** *Present but only by traversal* grades how hard a part is to notice **given that it is already in the bucket**; keeping it would make presence answer *what is apprehensible* instead of *what is true*. Rehomed to the parent's apprehension-versus-description axis --- a homing the ludicCache corpus had **already ruled**, not one chosen here. **Zero-host wholes are representable, and totality reads over *hosted* wholes** (the inherited fork resolved to **(ii)**; shipped witness is [`repairCharacterLegalPlacement`](../../../../../lambda/ephemera/dataSource/positions/membership/repairCharacterLegalPlacement.ts)'s `containers.length > 0`, so a disconnected character is a zero-host whole persisted today). **The row's own instruction, which nearly got broken and is worth carrying: do not answer this with a data structure.** The [cover formulation](#presence-as-a-cover) is not one --- the test is *would you serialise it?* and you would not. **So the swap-out test for anything added here is *would this survive the **walk** being abandoned*, not the cover.** **Two obligations left this plan rather than closing with the row:** [AB-59](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) (`PartOf`'s mutation grammar) and [AB-60](AGENT.abstractionLayers.planning.md#open-decisions-design--plan-only) (apprehensibility declared on the whole), both opened 2026-08-21 so that graduation would not strand them | 2026-08-21 | [record](AGENT.presence.discussion.planning.md#pr-1-requirement-what-must-x-is-present-in-h-be-able-to-express) |
| **PR-9** | **Requirement: is the cover total, and what does it range over?** | **The cover is TOTAL and ranges over NODES ONLY.** Every node of a hosted whole's graph falls in at least one bucket; **edges are not bucket members**, so an edge may span two buckets --- and cross-bucket edges are **required** by this design, not tolerated ([C7](AGENT.abstractionLayers.corpus.planning.md#c7-ariadnes-thread)'s cross-room span connections are what make its route recoverable). **The root is in every bucket**, exempt not from coverage but from needing an edge to establish it. **Domain: *hosted* wholes** --- a hostless whole has an empty cover and is outside the invariant, not in violation of it. **Totality is maintained by construction** (the index is minted from where the parts are), so **no fiction can violate it and no corpus case should be written to try**; the check verifies the **constructor**, and the known shortfall is the nested cascade ([PR-12](#open-decisions-design--plan-only)). **Three anti-reclassification guards:** a node in no bucket is never grounds for deciding its tag lacks presence, nor for deciding its whole is hostless --- **hostlessness is read from the stored presence-binding list, never inferred from an empty cover** --- nor for excusing the absence because the node **is a member of some other whole's graph too** (a part may be a node in several graphs; [LC8](AGENT.abstractionLayers.ludicCache.corpus.planning.md#lc8-the-spring-in-the-box-and-in-the-contraption)'s spring is in the box's and the contraption's, and **totality is stated per graph**). **The third guard added 2026-08-21**, after the trail's contrary claim --- *a part is a node in exactly one whole's graph* --- was falsified in conversation. | 2026-08-21 | [record](AGENT.presence.discussion.planning.md#pr-9-is-the-cover-total-and-what-does-it-range-over) |
| **PR-10** | **May presence be left implicit where a thing has at most one presence binding?** | **Yes --- implicit for AT-MOST-ONE presence binding, and the test is binding count, never host count.** There is no point representing a cover with one bucket: the single bucket **is** the node list, and totality guarantees a separate record could never disagree with it. The zero case behaves the same, so **the implicit/explicit boundary sits between one and two**, and 0 -> 1 needs no backfill. **Binding arity is INHERITED from the parent, so it is not readable off the thing itself** --- a part of a two-host whole has two bindings without doing anything to earn them, and **every call site must ask the binding list, never the host list**, even where hosting currently returns the right answer. **Characters stay exempt**, now on a scoping decision plus a type (hosting restricted to rooms; rooms are never graph members) rather than on an authoring accident. **Qualified: *conceptually settled, scaling untested*.** Write-side work is [PR-12](#open-decisions-design--plan-only); the `kind` field the count depends on is [PR-11](#open-decisions-design--plan-only). | 2026-08-21 | [record](AGENT.presence.discussion.planning.md#pr-10-may-presence-be-left-implicit-where-a-thing-has-at-most-one-presence-binding) |

## Verification

**Nothing to verify yet** --- this plan licenses no code. When it does, these are the checks that close a slice:

1. **Suite, from `lambda/ephemera`:** `npm run test -- --watchAll=false dataSource/positions/`. **Not `npm test`**, and **`npx tsc --noEmit` is not sufficient** --- integration tests sit outside the tsconfig include and mock by module path.
2. **Anchor resolution.** Every `](Other.md#anchor)` in this file resolves to a real heading. A broken cross-file anchor fails **silently**. **Prefer copying an existing link to deriving a slug** --- a `---` in a heading is an em dash, so its anchor takes two hyphens.
3. **Contract diff.** No clause reaches [`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) for behavior that has not shipped.
4. **Doc-correction debt: the relational edge kind list in [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md) is stale.** It lists `On`, `Under`, `Against`, `Custom`; the shipped set in [`ludicGraph/baseClasses.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/baseClasses.ts) also contains **`In`** and **`PartOf`**. **Fix this independently of any decision here** --- it is the durable doc, and the two missing kinds are the two most relevant to presence, so anyone reasoning about presence from that table is reasoning without them. Found 2026-08-21.
5. **The doc-correction debt inherited with multi-host presence** --- `evaluateComplexityPreGates`' terminal `multiPresent` error and `repairObjectPlacementDrift`'s purpose --- is discharged or explicitly re-deferred whenever PR-3 moves.

## Progress

Phase grain, per the variant. **The day-by-day does not go here** --- ~~it goes in the parent's reasoning trail until this plan splits its own.~~ **This plan split its own on 2026-08-21: it goes in [`AGENT.presence.discussion.planning.md`](AGENT.presence.discussion.planning.md).** **Graduated row detail goes there too**, keyed by ID, and **not** in this table --- the ladder costs the one initiative that split without saying so **58 argument-carrying entries** stranded in Progress.

| Phase | State |
| --- | --- |
| **PH0 --- corpus** | Not started. File not yet created |
| **PH1 --- requirement rows** | Not started |
| **PH2 --- representation rows** | Not started |
| **PH3 --- interface, tier decided out loud** | Not started |
| **PH4 --- discharge PR-8 for P7** | Not started |

## Lifecycle

**Disposes with the parent initiative**, per the ladder --- the split is an organizational convenience, not a separate lifecycle. Two things to decide at disposal:

1. **The Settled register is the disposal checklist.** Each row routes to `AGENT.contract.md` (a falsifiable rule), `AGENT.implementation.md` (paths and behavior), `AGENT.concepts.md` (vocabulary or a graduated mental model), or is task-only. **Presence is the plan most likely to owe `AGENT.concepts.md` a vocabulary entry**, since the durable doc currently defines the word in only its narrow, character-only sense --- see PR-2.
2. **Harvest the method findings before deleting anything**, per [the variant](../../../../AGENT.designVariant.md#method-findings-are-an-output-not-a-byproduct). **One is already predictable and should be watched from the start:** this plan was opened because a *word* was being used across two systems as though it named one, for two weeks, in three files. Whether that is a general failure mode worth a rule --- or one vivid instance --- is exactly the [admission bar](../../../../AGENT.designVariant.md#what-it-takes-to-add-a-rule) question, and the answer needs a second occurrence, not this one.
