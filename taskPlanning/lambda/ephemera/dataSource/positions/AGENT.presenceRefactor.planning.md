# Presence port population --- refactor planning

**Status: RD-1 and RD-2 decided by the user 2026-09-04; step 1 ("Split the presence step") shipped the same day, graduated into `AGENT.presence.planning.md`. RD-3 resolved 2026-09-04 (dissolved by extraction, not decided either way --- see its row). Step 2 (populate objects and characters) shipped 2026-09-04, graduated into `AGENT.presence.planning.md` (PR-2, PR-12, PR-13, plus the banner and PH2's Progress row) the same day. `AGENT.contract.md` owed nothing --- it never stated a population-scope claim for step 2 to update. RD-4 decided 2026-09-04/05; step 3 (populate rooms, features, areas) shipped 2026-09-05 for Room-in-Area and Feature-in-Room, graduated into `AGENT.presence.planning.md` (PR-7, PR-12) the same day --- Feature-in-Feature deferred on a separate `mtw-wml` prerequisite (`StandardFeaturePayload` has no `_ludicGraph` yet). `AGENT.contract.md` updated with a new Ingress subsection for the `Component Updated` trigger. Step 4 remains open.**

This plan exists because a set of **assertions about presence ports were made confidently in chat on 2026-09-04, several of them wrong**, and there were too many to unpick in conversation. They are written down here **as assertions, not as findings**, so they can be worked through one at a time. **Read every row of the register below as "this was claimed," never as "this is so."** Rows only become findings by being checked, and the check is recorded in the row.

This is a **standard-variant** task plan (see [`taskPlanning/AGENT.md`](../../../../AGENT.md)) --- an implementation plan with an unusually fat front section, not a design-stage plan. The design-stage sibling is [`AGENT.presence.planning.md`](AGENT.presence.planning.md), which owns the settled model (presence-as-a-cover, PR-1..PR-16) and is **authoritative over this file** wherever the two touch. **When a row here resolves into a design answer, it graduates into that plan, not into this one.**

## Getting Started

1. **Read the testing doc first:** [`lambda/ephemera/AGENT.testing.md`](../../../../../lambda/ephemera/AGENT.testing.md). It is the command authority for this area --- if anything here conflicts with it, follow it. Note in particular that `npx tsc --noEmit` is **not** sufficient (integration tests sit outside `tsconfig`), and that `npm run test -- --clearCache` is needed once after making a shared field required.
2. **Baseline before editing:** from `lambda/ephemera/`, run `npm run test -- --watchAll=false` and confirm green.
3. **Read the model, not this file's summary of it:** [`AGENT.presence.planning.md`](AGENT.presence.planning.md) --- especially **PR-12** (write-side maintenance of the cover; the row that records `MutationKernelTransferStep`'s singular `toHostId` *"cannot express a pure add"*), **PR-14** (what a transfer may say about dividing members between landing buckets), **PR-10** (at-most-one binding is left *implicit* --- settled), PR-13 (object-hosted characters) and PR-15 (presence ports are never crossings). **Correction, 2026-09-04: earlier passes of this file cited "PR-7's `toPorts` widening" for the write-side step shape. Both halves were wrong** --- PR-7 is the *scope-boundary* row (*"what does presence deliberately not answer?"*), and the string `toPorts` appears nowhere in that plan. **It was this author's coinage read back as the corpus's vocabulary.** The write-side row is PR-12.
4. **Read the scale-invariance material before reasoning about which entity kinds "can" be present.** [`AGENT.abstractionLayers.planning.md`](AGENT.abstractionLayers.planning.md) and its [discussion sibling](AGENT.abstractionLayers.discussion.planning.md). **This is not optional context --- RA-3 below was a direct re-introduction of a distinction that corpus falsified on 2026-08-07**, and the same error is available to any reader who starts from a storage-row type.
5. **Read the write path in code, not in summary:** [`compilePositionKernelOp.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/compile/compilePositionKernelOp.ts), [`executeObjectMove.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/membership/executeObjectMove.ts), [`kernelStep.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/kernelStep.ts).

## The assertion register

**Held lightly. `Standing` is what the assertion is currently worth, not a verdict on the underlying question.** An assertion can be refuted while the question it was reaching for stays wide open --- most of these are exactly that.

| ID | Assertion, as made 2026-09-04 | What it was actually derived from | Standing |
| --- | --- | --- | --- |
| **RA-1** | The **only** producer of a presence port is [`compilePositionKernelOp.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/compile/compilePositionKernelOp.ts), gated on `op.moved.kind === 'closure' && op.to`; nothing else in the repo emits `setPresencePort` | Repo-wide grep for `setPresencePort`, plus reading the one emitting site | **Re-checked 2026-09-04, before step 1 landed: confirmed.** `grep -rn "kind: 'setPresencePort'" --include="*.ts" lambda packages | grep -v ".test.ts"` returned only `compilePositionKernelOp.ts` (producer) and `kernelStep.ts` (type declaration) --- no other emitter. (Post-step-1, the kind is renamed to `addPresencePort`/`removePresencePort`; the single-producer property carries over unchanged.) |
| **RA-2** | Characters never receive a presence port, and this is a **deliberate design decision with standing** --- *"this isn't a hole to plumb; someone decided against it and left a tripwire"* | The `kind === 'closure'` gate, plus the confident register of the comment sitting above it (*"keeps a bare `compilePositionKernelOp` widening from porting a character on every navigate"*) | **The mechanical half stands; the standing half is withdrawn.** User: that "someone" was a code agent improvising with no remit. **No plan row, contract clause or discussion entry was checked for before granting the comment authority.** A comment is written by the same process that writes the code and inherits none of the corpus's authority --- the general lesson, not just this instance. **What the row leaves behind is a real question, RD-1 --- answered 2026-09-04, see that row.** |
| **RA-3** | *"Rooms and Features: not a population gap at all. They're host kinds, never contained ones"* --- so Room-in-Area is a different and larger question than the one posed | The union `EphemeraPositionAdjacencyContainedId = Character \| Object \| Feature` in [`ephemeraPositionAdjacency.ts`](../../../../../packages/mtw-interfaces/ts/ephemeraPositionAdjacency.ts), read as an ontology | **Refuted twice over, and the second refutation is the important one.** *(a)* **The cited union literally contains `Feature`**, so the claim was falsified by the line it was read off. *(b)* **There is no host-kind/contained-kind distinction in this design at all** --- it is scale-invariant, and *whole* and *part* are consequences of point of view and positioning, not attributes of an item. **That exact distinction was already falsified in this corpus on 2026-08-07** (C11 against H1 clause 1a) on exactly these grounds. *(c)* Factually: **Rooms are hosted in Areas; Features are hosted in Rooms and in Features.** **A storage-row type was promoted to an ontological claim the design had retired a month earlier** |
| **RA-4** | The Coyote spawn path bypasses the compiler entirely: `spawnOneImprovisationObject` places via `executeMembershipTransfer`, which builds its own bare `transferMembership` step inline rather than compiling one, so spawned objects get no presence port | Reading [`spawnImprovisationObjectsBatch.ts`](../../../../../lambda/ephemera/dataSource/objects/spawnImprovisationObjectsBatch.ts) into `executeMembershipTransfer`'s body | **Re-checked 2026-09-04, before step 1 landed: confirmed.** `spawnOneImprovisationObject` calls `executeObjectMove.ts`'s `executeMembershipTransfer` directly, with no `compileMutationSteps` override, so it builds a bare `transferMembership` step and never emits a presence step. **The `compileMutationSteps` injection point still exists** on that function; every current injector happens to be a character route. Whether routing through it is the right fix is RD-3, not settled here |
| **RA-5** | Nothing reads presence ports for behaviour: `findRelationalChain` treats `kind: 'Present'` as identical to *no port at all*, `classifyLudicGraphPortMismatch` short-circuits it to `mismatch: false`, and no sweep detects an **absent** port because both sweeps compare ports that exist | Grep for `.ports` readers plus reading the two classifiers | **Re-checked 2026-09-04, before step 1 landed: confirmed.** `findRelationalChain.ts` treats `kind === 'Present'` as no-port; `classifyLudicGraphPortMismatch.ts` short-circuits it to `mismatch: false`; the heal/sweep paths (`healLudicGraphPortMismatch.ts`, `healLudicGraphStructure.ts`, `ludicGraphPortMismatchSweep/index.ts`) read `.ports` generically but assert nothing specific to presence semantics. **The observation is unchallenged. Every use it was put to is contested --- see RA-6.** Keep the fact; distrust anything built on it |
| **RA-6** | *"The sparseness is invisible today because presence ports are write-only"* --- therefore the cost of the gap is entirely prospective, and therefore the case for acting is weaker | RA-5, treated as a standing fact about the system | **Withdrawn as an argument. It is circular, and I am the loop.** Presence ports are write-only substantially because I declined to extend them on repeated occasions in the days prior, **each time citing the absence of consumers as the reason.** Refuse to extend, observe no consumers, cite no-consumers as grounds not to extend. User: *"quit arguing from present state to a perpetual authority."* **The general form is the thing to carry forward, since it is available to every row in this file: present implementation state is evidence about the present, never authority over the design** |
| **RA-7** | *"A presence port carries a singular `fromHostId`, so today it encodes strictly less than the reverse index's set"* --- hence the redundancy comparison is currently foregone | `setPresencePort`'s replace-all commit semantics in [`applyStepSequenceCore.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/applyStepSequenceCore.ts), read as a property of the representation | **Refuted.** True of **one** port; false of the representation. **N presence ports on a graph are exactly as expressive as an N-element reverse-index set.** What was read as a modelling limit is today's step semantics (PR-10's at-most-one), which is **precisely what PR-12 is open on** (*"cannot express a pure add"*) --- so the argument assumed away its own subject. **Row corrected 2026-09-04: originally cited PR-7, which is the scope-boundary row and not this question** |
| **RA-8** | Population-everywhere as a pure plumbing sweep would yield a *total but degenerate* index, and the redundancy comparison would still return "redundant," correctly and uninformatively | Entirely downstream of RA-7 | **Falls with RA-7 and is not independently supported.** It may still be true for other reasons; **nothing here establishes it and it should not be cited** |
| **RA-9** | Three decisions gate the sweep, and the list will not grow because they are the three entity classes: character meaning, `executeMembershipTransfer` routing, and whether rooms/features become contained entities | RA-2 and RA-3, plus the closed-list intuition | **Item 3 is void as phrased** (RA-3), and with it the *"three entity classes"* argument for the list being closed. **Items 1 and 2 survive as RD-1 and RD-3.** The non-growth claim is withdrawn rather than repaired --- it was resting on the bad taxonomy |

### What actually survives

**Three mechanical observations** --- RA-1, RA-4, RA-5 --- all re-checkable by grep, none of them loadbearing on their own.

**Every inference drawn from them on 2026-09-04 is withdrawn or refuted.** The shared mechanism in RA-2, RA-6 and RA-7 is one thing worth naming, because it will recur in this plan: **reading the current implementation as the design.** A comment's rationale read as a decision; a missing consumer read as a verdict on consumers; a step's commit semantics read as a representation's expressiveness.

**The split is not random, and it is the reason this plan is ordered the way it is.** The survivors are exactly the assertions answerable by grep. **Every refuted row was reasoning in the hypothetical --- what a comparison would show, what a decision must have meant, what a representation could express --- and the hypothetical register is one this plan's author has demonstrated repeatedly they cannot operate in productively as a collaborator** (user's call, 2026-09-04, and it is written here as a working constraint rather than as an apology). **So: code and population get sorted first, on the user's authority, and the assessment design is written afterwards against a system that exists.** See [Recommended order](#recommended-order) --- the first draft of this plan inverted that, putting RD-5 in the gating slot, which is the same error one layer up.

**And the question the original exchange was reaching for is untouched and still good:** presence ports are populated inconsistently, which makes it unattractive to build on them, which blocks an apples-to-apples comparison against the adjacency reverse index. **RA-7's refutation makes that comparison more interesting, not less** --- if multi-port presence is as expressive as the reverse index, then the two really are candidate substitutes and the choice has to be made on other grounds.

## Open decisions (implementation --- plan only)

Plan-only: decisions we are making in order to implement the next slice(s). Do not copy into package `AGENT.concepts.md`. When a decision ships, record it in [`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) / [`AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.implementation.md) and remove the row here. **Where a row resolves into design rather than code, it graduates into [`AGENT.presence.planning.md`](AGENT.presence.planning.md) instead.**

**RD-1 and RD-2 graduated 2026-09-04**, on shipping step 1 ("Split the presence step"): both rows are removed from this table per the rule above, and their content now lives in `AGENT.presence.planning.md`'s PR-12 (write-side maintenance), PR-10 (at-most-one implicit-vs-explicit), PR-2 (character host-kind restriction) and PR-7 (corrected write-side pointer). **RD-3 graduated the same way on shipping step 2**: dissolved by extraction (`presencePortStepsForMove`), not decided either way, and its content --- the planner -> kernel gap RA-4 found, and how extraction closed it without routing through `compileMutationSteps` --- now lives in PR-12 alongside RD-1/RD-2's content. **RD-4 graduated 2026-09-05, on shipping step 3** ("Populate rooms, features, areas"): Room-in-Area and Feature-in-Room now ship (`populateContainmentAtCache`/`containmentPopulationSteps`, `dataSource/positions/manipulation/containment/`); Feature-in-Feature does **not** --- `StandardFeaturePayload` has no `_ludicGraph` field, so a Feature has no upstream data to populate from yet, a prerequisite gap parallel to the one Room had before ISS8158, not closed here (user, 2026-09-05: ship the two that work now rather than widen this slice into `mtw-wml`). Its content now lives in `AGENT.presence.planning.md`'s PR-7 (the second write path PR-7's own verdict booked for PR-12, now shipped as this mechanism) and PR-12 (a third writer of presence bindings, still single-level). **Also found while shipping, and recorded rather than left implicit**: `MutationKernelTransferStep.entityIds` (`kernelStep.ts`) and `EphemeraPositionAdjacencyContainedId` (`ephemeraPositionAdjacency.ts`) both needed widening to admit `EphemeraRoomId`/`EphemeraFeatureId` as pure-add transfer entities --- `applyStepSequenceCore.ts`'s "real transfer" branch (`applyTransferSet`, LP4h) deliberately stays Object/Character-only (Room/Feature/Area never relocate), so a Room/Feature id reaching that branch is now rejected (`unsupportedTransferEntityKind`) rather than silently mishandled.

| ID | Decision | Blocks slice | Status |
| --- | --- | --- | --- |
| **RD-6** | **When a move is additive, is that a split, a duplicate, or an error?** Once RD-2 lands, an object at `[X]` told *"move from `[]` to `Y`"* becomes `[X, Y]`, and nothing decides what that means. **This is not a new row --- it is [`AGENT.presence.planning.md`](AGENT.presence.planning.md)'s PR-14** (*"what may a transfer instruction say about how members divide between landing buckets?"*, minted 2026-08-21), **and it stays there.** Kept here only as the pointer, because RD-2 is what makes the state expressible and therefore what turns PR-14 from a question about a hypothetical instruction into one about a shape that exists. **Do not answer it before step 3, and do not answer it in this file** | 4 | **Deferred --- owned by PR-14, not by this plan** |
| **RD-5** | **What would the presence-vs-reverse-index comparison actually measure?** **Deferred by construction, not by convenience** --- this is the assessment plan, and it is written against a populated system rather than a hypothetical one. Blocked on 1--3 shipping. **Do not answer it early**; the argument for answering it early was itself an instance of the failure this plan records | 4 | **Deferred** until population lands |

## Recommended order

Pending work uses `[ ]`; completed work uses `[X]`. Mark nested lines `[X]` as they are done, so partial progress on a multi-part step is visible.

**Code and population first; assessment last.** Steps 1--3 make presence ports actually populated, under decisions the user owns. Step 4 designs the comparison **afterwards**, against a system that exists. This ordering is the user's, reversing the first draft of this plan --- see [What actually survives](#what-actually-survives) for why.

- [X] **1. Split the presence step (RD-2).** Decided; this is now a write, not a fork. **Target: green suite with no observable behaviour change for any sequence a current caller emits** --- if an existing test's expectations change beyond step *names* and *counts*, something in the decision was misread. **New tests are expected** (the validator's throw path has no existing coverage); **changed ones are the warning sign.** **Shipped 2026-09-04.**
  - [X] Re-check RA-1, RA-4, RA-5 by grep and record the result in each row. Cheap, and they are the slice plan's only load-bearing facts.
  - [X] Rename `MutationKernelSetPresencePortStep` -> `MutationKernelAddPresencePortStep` (`kind: 'addPresencePort'`), drop the optional `port` --- the clear is now a separate step, not an omitted field.
  - [X] Add `MutationKernelRemovePresencePortStep` as `{ kind: 'removePresencePort', hostId, fromHostId }`. **Addressed by host, not by `portId`** --- unlike `removeCrossingPort`, because the compiler cannot know a `portId` without the prior-state read that replace-all existed to avoid.
  - [X] Reducer: the block at [`applyStepSequenceCore.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/applyStepSequenceCore.ts) narrows from `filter(port => port.kind === 'Present')` to the same filter plus `&& port.fromHostId === step.fromHostId`. `removePort` is a plain filter, so removing an absent binding is a silent no-op --- **which is the behaviour population needs while coverage is still sparse.**
  - [X] **Add the character at-most-one presence validator --- this is what makes the reducer change safe to ship, not a follow-up to it.** RD-2 removes the reducer's replace-all, which was the only thing enforcing at-most-one; this puts the guarantee back for the one kind that still needs it.
    - [X] **Throw, in both modes, not a verdict.** A character with two presence ports cannot arise from a race or a stale read --- only from a compiler emitting a bad sequence --- so it is a **structural-invariant violation** and takes the BD-33 path already documented at [`applyStepSequenceCore.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/applyStepSequenceCore.ts): *"Structural-invariant violations … throw, uniformly in both modes --- not a `MutationKernelApplyOutcome` verdict."* Throwing in the dry-run pass means a bad sequence fails **before** anything is written.
    - [X] **Check at end of sequence, over the character graphs in the footprint --- never per step.** The compiler emits remove-then-add, so a per-step check would pass today while **silently making the invariant depend on step order**; the first caller to emit add-before-remove would then take a spurious throw on a correct sequence. End-of-sequence is order-independent and constrains the compiler not at all.
    - [X] **Scope to characters only, and cite the expiry in the throw message.** Objects must not be constrained --- multi-presence is the point of this whole plan. The restriction traces to [`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md): *"it exists to keep the character path single-hosted while the general graph work matures, and it **lifts when that work does**."* **Name that clause in the error**, so whoever lifts the restriction finds the validator instead of reverse-engineering whether it is still load-bearing --- the dangling-marker anti-pattern [`taskPlanning/AGENT.md`](../../../../AGENT.md) already names.
    - [X] **Do not mistake it for coverage.** It is a **ratchet on new writes, not a repair**: a character already carrying a stale or duplicate port sits untouched until some sequence happens to include it. Pre-existing bad state is sweep territory ([`healLudicGraphStructure.ts`](../../../../../lambda/ephemera/dataSource/positions/ludicGraph/healLudicGraphStructure.ts) is the precedent) and belongs to step 2, not here.
  - [X] Compiler: emit `[...froms.map(removePresencePort), addPresencePort(to)]` in place of the single set step.
  - [X] Sweep the mechanical sites: `isKernelMutationStep` and `computeStepSequenceFootprint` (both gain one arm), `factsForStep` (still no facts), and `publishedEvents.ts`'s guard comment.
  - [X] **Graduate into [`AGENT.presence.planning.md`](AGENT.presence.planning.md) as part of this step, not after it.** The design plan owns the model; leaving it describing a reducer-enforced invariant the code no longer has is the failure this whole plan exists to correct. Three named targets, and **read each row before editing it --- rows in that file are single 5--20KB lines and a bare ID can match more than one table**:
    - [X] **PR-12** (Open decisions) --- its stated blocker is that `MutationKernelTransferStep`'s singular `toHostId` *"cannot express a pure add."* RD-2 makes a pure add expressible **without touching the transfer step**, so record that the blocker is dissolved by a separate step rather than by the widening the row anticipated. Obligation A (the cascade) is untouched.
    - [X] **PR-10** (Settled register) --- **the delicate one.** Its verdict is that at-most-one presence is left **implicit** (*"no point representing a cover with one bucket … 0 -> 1 needs no backfill"*). Population-everywhere writes those bindings explicitly anyway, and RD-2 moves the at-most-one guarantee from the reducer to compiler discipline. **Neither change falsifies the row** --- implicit-is-permitted is not explicit-is-forbidden --- **but the row's rationale now argues against something this plan is deliberately doing, and that must be visible in the cell rather than left for a reader to hit.** Note also that PR-10's *"characters stay exempt"* clause meets RD-1 head-on: characters are exempt from inheriting multiple bindings, and now carry an explicit port anyway.
    - [X] **PR-14** --- add the pointer that RD-2 makes its subject a shape that exists rather than a hypothetical instruction. **Do not answer it.**
    - [X] **PR-7 --- clean up, do not extend.** The 2026-09-04 reconciliation pass appended a write-side correction to *this* row's Status cell (`MutationKernelTransferStep`'s singular `toHostId`, the presence-port step, the multi-host cascade), under the same misattribution. **That paragraph is PR-12's content sitting on the scope-boundary row.** Move it, leaving a dated pointer rather than a silent deletion.
    - [X] Record RD-1's verdict where the plan keeps character presence, per PR-2's *"restriction on host kind, not host count"* framing --- it is the same claim, and RD-1 should read as its consequence rather than as a new rule.
  - [X] **Graduate step 2's consequences into [`AGENT.presence.planning.md`](AGENT.presence.planning.md), same day.** **PR-2**: RD-1 is now mechanised, not merely validated --- the `closure` gate's removal noted as this row's restriction read forward, not a new question. **PR-12**: the "Also here" planner -> kernel remainder (RA-4's uncovered callers) is closed by the same extraction that resolved RD-3, not by routing through the compiler; the missing-clear fix recorded as a defect this row never named; Obligation A (the cascade) restated as still untouched. **PR-13**: checked again against the wider write surface (`presencePortStepsForMove`, `executeMembershipTransfer`'s default path) --- same unenforced conclusion. **Banner and PH2's Progress row**: both updated with a second 2026-09-04 pass. **`AGENT.contract.md`**: checked (grepped for `presence`/`closure`/emission-site claims) and found to own no population-scope statement for step 2 to contradict or update --- the character host-kind restriction it states is untouched by which callers populate presence bindings.
- [X] **2. Populate objects and characters.** RD-1 decided; RD-3 resolved by extraction. **Shipped 2026-09-04.** Full suite green (349 suites, 2822 tests).
  - [X] **Extract the presence-port block first (RD-3).** Landed as [`presencePortStepsForMove.ts`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/compile/presencePortStepsForMove.ts): `presencePortStepsForMove(hostId, froms, to)`, taking only the three fields the block reads. `compilePositionKernelOp` calls it with `(primaryMovedId, op.froms, op.to)`; `executeMembershipTransfer`'s default path calls it with `(args.entityId, froms, args.target)`. Neither caller builds a `compileMutationSteps` closure just to get a presence port.
  - [X] **Fixed the missing clear.** The extracted function's `to` guard now governs only the `addPresencePort` append; the `removePresencePort`s over `froms` fire unconditionally, so a departure to no host (destroy/scrub, or a character going out of play under RD-1) clears every prior binding instead of leaving a stale one standing.
  - [X] Dropped the `moved.kind === 'closure'` gate in `compilePositionKernelOp` --- characters compile a presence binding on every move now, per RD-1.
  - [X] Wired `executeMembershipTransfer`'s default (non-`compileMutationSteps`) path to append `presencePortStepsForMove`'s output to the bare `transferMembership` step --- object spawn, clear/scrub, place, and drift-repair all get presence population with no per-caller change.
  - Tests: new `presencePortStepsForMove.test.ts` (pure add, pure remove, rehost, no-op, fresh `portId` per call); `executeMembershipTransfer.test.ts`'s spawn-shape and clear-shape cases now assert the committed object's own `ports` directly; `compilePositionKernelOp.test.ts`, `executeCharacterNavigate.test.ts`, `applyCharacterRoomMembership.test.ts`, `handleConnectionsCharactersPresence.test.ts` updated for the now-present presence steps on character routes (expected consequence of dropping the gate, not a regression).
- [X] **3. Populate rooms, features, areas:** RD-4, on the scale-invariant framing. **Shipped 2026-09-05** for Room-in-Area and Feature-in-Room; Feature-in-Feature deferred (no `_ludicGraph` on `StandardFeaturePayload` yet --- a separate, out-of-scope `mtw-wml` prerequisite). Full suite green (351 suites, 2848 tests).
  - [X] Room-in-Area and Feature-in-Room: additive-only node-plus-presence-port population at cache time, per RD-4's 2026-09-04 decision (provenance/removal-on-deauthoring stays deferred). Landed as [`containmentPopulationSteps.ts`](manipulation/containment/containmentPopulationSteps.ts) (pure step-computer, checks current state before emitting each of the three steps) and [`populateContainmentAtCache.ts`](manipulation/containment/populateContainmentAtCache.ts) (reads both graphs, batches every named child of one `cacheAsset` update into one `commitStepSequence` call), triggered from `dataSource/index.ts`'s `processComponentUpdated` --- duck-typed on `'ludicGraph' in component` rather than switched on `component.tag`, so a future host kind (Feature, once it gains `_ludicGraph`) starts participating with no change to the trigger.
  - [X] **Kernel widening this item required, found while building the node-membership half, not anticipated when this row was written**: `MutationKernelTransferStep.entityIds` (`kernelStep.ts`) admitted only `EphemeraObjectId | EphemeraCharacterId`; a Room/Feature pure-add needed it widened to include `EphemeraRoomId | EphemeraFeatureId`. `EphemeraLudicGraph` gained `addRoom`/`removeRoom`/`addFeature`/`removeFeature` (idempotent-add, mirroring `addObject`/`addCharacter` exactly) and `roomIds`/`featureIds` getters, plus `addNode`/`removeNode` --- a kind-dispatching pair added on review (the first pass wrote four parallel per-kind loops in `applyStepSequenceCore.ts`'s pure-add/remove branch; the reducer's own generic `nodeIds` getter already made that unnecessary, so it collapsed to one loop over `nodeIds`/`addNode`/`removeNode` instead). `applyStepSequenceCore.ts`'s pure-add/remove branch now dispatches all four entity kinds through that one loop; its "real transfer" branch (`applyTransferSet`, LP4h) stays deliberately Object/Character-only (Room/Feature/Area never relocate) and now rejects a Room/Feature id reaching it (`unsupportedTransferEntityKind`) rather than silently dropping it, since the type no longer forbids constructing such a step. `EphemeraPositionAdjacencyContainedId` (`ephemeraPositionAdjacency.ts`) widened to admit `EphemeraRoomId` too, so a Room's reverse adjacency row (`EphemeraId: ROOM#..., DataCategory: POSITION#AREA#...`) is a legal shape --- this is the union RA-3 read as excluding Room; the read was right about the gap even though RA-3's own reasoning about it was refuted.
  - [X] **Mint a `PartOf` runtime edge at cache time, routed through the live transfer kernel** (RD-4, decided 2026-09-05) --- not a bespoke `cacheAsset` write; the edge should be produced by the same kernel machinery PR-7 describes for live moves, so it shares that machinery's invariants and step-sequence semantics. **Always `PartOf`, never `In`, for these two relationships** --- Room-in-Area and Feature-in-Room are fixed/authored nestings, not mobile placement; `In` is reserved for more mobile members (e.g. Objects) and is out of scope here. Landed via the existing [`applyObjectRelationalChange.ts`](manipulation/relational/applyObjectRelationalChange.ts)-style route: `containmentPopulationSteps` builds one `establishRelation` step (`subjectId: childId, targetId: parentId, hostId: parentId, relationKind: 'PartOf'`), committed by `commitStepSequence` alongside the node-add and presence-port steps in the same transaction.
  - [X] **Assure idempotency of the edge-minting step(s).** `cacheAsset` re-runs frequently over state that is already cached and unchanged (or only partially changed); the kernel step(s) that mint `PartOf` must be safe to replay over a graph that already reflects the edge --- no duplicate edges, no spurious remove/re-add churn --- rather than assuming a from-empty first-write. Concrete mechanism (e.g. a step that no-ops when the edge already matches its intended source/target) is this item's to design when worked; per RD-4's note, don't over-invest in getting it "right" beyond that on the first pass. **Landed**: each of the three steps (node-add, presence-port-add, edge-establish) is independently pre-checked against already-fetched current state in `containmentPopulationSteps` before being emitted, so a fully-populated rerun emits zero steps and commits no transaction at all --- checked rather than assumed, since neither `transferMembership`'s pure-add branch nor `addPresencePort` is safe to replay unconditionally at the reducer level (both were confirmed to error/duplicate, respectively, before this landed).
- [ ] **4. Design the assessment** (RD-5) and only then run it. **Not before step 3 lands.**

## Progress

| Phase | State |
| --- | --- |
| Assertion register | **Written 2026-09-04.** Nine rows; three mechanical observations survive, six inferences withdrawn or refuted |
| 1 --- split the presence step (RD-2), with the character validator | **Shipped 2026-09-04.** `MutationKernelSetPresencePortStep` retired; `MutationKernelAddPresencePortStep`/`MutationKernelRemovePresencePortStep` in place, with the end-of-sequence character at-most-one validator in `applyStepSequenceCore.ts`. RA-1/RA-4/RA-5 re-confirmed by grep. Graduated into [`AGENT.presence.planning.md`](AGENT.presence.planning.md) (PR-12, PR-10, PR-14, PR-7, PR-2). Full suite green |
| 2 --- populate objects and characters | **Shipped 2026-09-04.** `presencePortStepsForMove` extracted and shared by `compilePositionKernelOp` and `executeMembershipTransfer`'s default path; `closure`-only gate dropped (RD-1); missing-clear fixed. Full suite green. **Graduated 2026-09-04** into `AGENT.presence.planning.md` (PR-2, PR-12, PR-13, banner, PH2 Progress row); `AGENT.contract.md` checked and owed no update |
| 3 --- populate rooms, features, areas | **Shipped 2026-09-05** for Room-in-Area and Feature-in-Room: `containmentPopulationSteps`/`populateContainmentAtCache` (`manipulation/containment/`), triggered from `processComponentUpdated`. Feature-in-Feature deferred (no `_ludicGraph` on `StandardFeaturePayload` yet). Kernel widened (`MutationKernelTransferStep.entityIds`, `EphemeraPositionAdjacencyContainedId`) to admit Room/Feature as pure-add transfer entities; `applyStepSequenceCore.ts`'s real-transfer branch stays Object/Character-only (LP4h) and now rejects the wrong-kind case explicitly. Full suite green. **Graduated 2026-09-05** into `AGENT.presence.planning.md` (PR-7, PR-12) |
| 4 --- assessment design (RD-5) and split semantics (RD-6) | **Deferred by construction** until 1--3 land |

## Verification

From `lambda/ephemera/`:

```bash
npm run test -- --watchAll=false
```

Per [`AGENT.testing.md`](../../../../../lambda/ephemera/AGENT.testing.md): `npx tsc --noEmit` is **not** sufficient here, and after making any shared field required run `npm run test -- --clearCache` once before believing a green result.

**Re-checking the register's mechanical rows** (run from the repo root):

```bash
# RA-1: is compilePositionKernelOp still the only producer?
grep -rn "kind: 'setPresencePort'" --include="*.ts" lambda packages | grep -v ".test.ts"

# RA-5: has anything started reading presence ports?
grep -rn "\.ports\b" --include="*.ts" lambda packages --exclude="*.test.ts"
grep -rn "kind === 'Present'" --include="*.ts" lambda packages | grep -v ".test.ts"
```
