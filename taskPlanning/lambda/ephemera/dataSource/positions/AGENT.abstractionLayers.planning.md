# Abstraction layers: fractal summary and detail above and below the object

**Status:** Design stage, not started. Seeded 2026-08-05 from conversation. **No code is in scope yet** --- the deliverable of the first several phases is a settled set of answers, recorded here, and only then a vertical slice. Nothing in this file belongs in a durable `AGENT.contract.md` until it ships; see [`taskPlanning/AGENT.md`](../../../../AGENT.md) on wishlist normative text.

This document is task-scoped and follows [`taskPlanning/AGENT.md`](../../../../AGENT.md).

**Sibling initiative, deliberately separate:** [`AGENT.objectManipulationIterations.planning.md`](../actions/AGENT.objectManipulationIterations.planning.md) is the standing index for the **parse and reasoning pipeline** (classify -> Parse -> Plan -> Synthesize -> kernel). That ladder's rungs *consume* the world model; this initiative changes *what the world model is*. They interact --- see [Relationship to the object-manipulation ladder](#relationship-to-the-object-manipulation-ladder) --- but a rung on that ladder is the wrong container for a data-structure question that also reaches WML authoring, perception, and diegetic state.

---

## Why this initiative exists

The shipped position model has exactly one granularity: the human-convenience scale of Object and Character nodes on a host graph, related by peer-to-peer edges (`On`, `Under`, `Against`, `Custom`) within a single host. That model is a fine starting point and is not in question. What is in question is whether it can carry complexity **without becoming unworkable to reason about** --- for an LLM in particular, but the burden is not only the LLM's.

The gap is structural, not ergonomic. **Membership is the only level-crossing relation the model has, and it is flat**: a node is in a host or it is not, with no way to be in it *as part of something*. Every other relation is intra-host by construction --- [`applyStepSequenceCore`](../../../../../lambda/ephemera/dataSource/positions/manipulation/kernel/applyStepSequenceCore.ts) derives a relational step's shared host live and **throws** on mismatch.

**Membership is not, however, single-host at the storage layer** --- a point that was overstated in an earlier draft of this file and is worth stating precisely, since AB-8 turns on it. See [Multi-host presence: what is actually true today](#multi-host-presence-what-is-actually-true-today) below.

So there is no way to say "these five things together are a thing," or "this thing has parts," or "this thing extends across those rooms," except by encoding it in a vocabulary that has no levels in it --- which pushes the level structure into every consumer's head, to be re-derived by inspection each time. That is the burden.

### The four seed examples

These are the origin cases, recorded verbatim in intent because the corpus in Phase 0 is built *from* them and must not lose their variety:

1. **Place setting.** Plate, cup, fork, knife, spoon `On` a table. At some point it should be possible to reason about and display "table with a place setting" rather than six nodes and five edges.
2. **Dismantled flashlight.** Improvise "parts" --- battery, bulb --- at a level *below* the object, on demand.
3. **Powered door.** A door that opens only when power is on, where power is an area-wide fact affected by repairs to a generator elsewhere. Demonstrates that abstractions likely convey **state** as well as position.
4. **Rope through several rooms.** Pulling it is mediated not by an individual room but by an Area or by an abstraction.

### They are probably not one structure

The examples separate along at least three axes, and conflating them is the main design risk this plan exists to manage:

| Axis | Seed examples | Character |
| --- | --- | --- |
| **Composition (part/whole)** | 1, 2 | Level-crossing, rooted, spatially coherent. The two examples are mirror images of **one** relation --- the difference is only which level pre-existed and which was improvised into being. Place setting: members primary, whole minted. Flashlight: whole primary, parts minted. |
| **Functional aggregate carrying state** | 3 | Members deliberately **not** co-located; membership is participation in a system, not containment. Carries state; the state gates behavior elsewhere. |
| **Multi-host extent** | 4 | One thing present across several hosts --- **representable today**, contrary to an earlier draft; see [Multi-host presence](#multi-host-presence-what-is-actually-true-today). Distinguished by needing **adjacency-mediated connections** among its parts (AB-26 --- *not* a path ordering; see that row), and by making the aggregate a candidate **manipulation host** peer to Room and Character. |

**Do not assume these collapse into one `Abstraction` type, and do not assume they stay separate.** Phase 1 decides it, against the corpus. Everything downstream reads differently depending on the answer, which is exactly why it is the root question.

### Clean fractal, dirty implementation

**Named 2026-08-06, alongside the *Abstraction Fractal* candidate on AB-0.** The fractal framing promises **scale-invariance** --- the same part-of relation at every level, no privileged one --- and the corpus does not fully deliver it. That is not one problem but two, and conflating them would either discard a good model or excuse a real gap.

| | What it is | How to treat it |
| --- | --- | --- |
| **Implementation dirt** | The clean model holds, and the implementation deviates from it deliberately, for efficiency: human-scale anchoring, caches, fast paths, shorthands | **Expected and legitimate.** Keep the clean fractal as the *specification of correctness*, and enumerate each deviation as an optimisation over it. A deviation that cannot be stated as "the clean answer, computed faster" is not dirt --- it is a model change wearing dirt's clothing |
| **Conceptual limit** | The clean model itself is not scale-invariant, independent of any implementation | **A real limit on the metaphor**, which the name must not paper over |

**Already-known instances of each.** *Dirt:* AB-15's default binding level is exactly human-scale anchoring --- the ontology privileges no level, the **pragmatics** privilege the authored one; and the render cache is a literal cache over the clean answer, so this is not hypothetical. *Conceptual limit:* **AB-13's DAG.** Multiple parents means the structure is not a tree-like recursion, and no amount of implementation care makes it one --- that is a property of composition, not of storage. **AB-16 is unclassified** and worth deciding deliberately: whether homogeneous extent versus heterogeneous separable parts is a genuine break in scale-invariance, or an implementation convenience, is exactly the kind of question this distinction exists to force.

**Why the split earns its keep:** it lets the clean model be held as correctness while the implementation is permitted to deviate, with every deviation named as an optimisation rather than quietly absorbed into the model. The failure mode it guards against is the opposite --- efficiency shortcuts accumulating until nobody can say what the model *was*, which is the state the position lane's four routes reached with single-host (see [C2](#c2-the-rope-in-two-rooms-refused-at-the-gate)).

### Multi-host presence: what is actually true today

Verified against the code 2026-08-05, because the rope case (AB-8) had been written up as breaking a hard invariant, and that is not accurate. **Multi-room object presence is representable today with no schema change.** What stands in the rope's way is narrower and more specific.

| Layer | Reality |
| --- | --- |
| **Adjacency storage** | Natively multi-host. `getMembershipContainers` returns an **array**; adjacency rows are `(OBJECT#, POSITION#ROOM#...)` with nothing unique-constraining them. `MembershipDiff.froms` is plural and [`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) explicitly permits `froms.length > 1`. |
| **Membership compile gate** | **The hardest pressure, and the earliest.** Corrected 2026-08-05 while mining C2 --- an earlier draft named the end-state planner as "the" pressure. [`complexityPreGates.ts:29`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/complexityPreGates.ts) returns `complexityClass: 'multiPresent'` for `containers.length > 1`, and `preGateOutcomeToTerminalError` maps it straight to player-facing error copy. A multi-present object is **refused before planning is reached**. |
| **End-state planner** | A second, softer pressure. [`computeEndStateRoomDiff`](../../../../../lambda/ephemera/dataSource/positions/manipulation/adapters/computeEndStateRoomDiff.ts) computes `froms = priorContainers.filter(!== to)`, so ordinary place / spawn / remove scrub every other room as **normal operation**, not as repair. A planner behavior, not an invariant. |
| **Relational compile** | A third, and different again: [`compileRelationalFromSkeleton.ts:168`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/compileRelationalFromSkeleton.ts) populates `hostByObjectId` only `if (containers.length === 1)`, so a multi-present object is **silently hostless** to Expansion rather than refused. |
| **Cross-host move route** | Bypasses the gate *and* the planner --- `executeObjectMove` grounds through the Synthesize executor from a host pair. So the constraint is **not one rule applied unevenly across routes; it is three different rules on three routes.** See [C2](#c2-the-rope-in-two-rooms-refused-at-the-gate) for the full table. |
| **Kernel step shape** | `MutationKernelTransferStep` has `fromHostIds: ReadonlySet<...>` but a **singular `toHostId`**. There is no add-to-many primitive. Three-room presence needs three pure-add steps or a widened field. |
| **Relational edges** | Strictly intra-host regardless. A rope in room A cannot relate to a bollard in room B under any current shape. |
| **Presentation** | Affordance compose reads `graph.objectIds` per room, so a multi-room rope renders as a **whole object in each room** with no notion of partial presence. Likely the first thing a player would notice. |
| **Drift repair** | [`repairObjectPlacementDrift`](../../../../../lambda/ephemera/dataSource/positions/membership/repairObjectPlacementDrift.ts) **has no production caller** --- only its own test. The `Object Placement Drift Finding` sweep that would feed it is deferred and does not exist in [`lambda/diagnostics/`](../../../../../lambda/diagnostics/). Nothing is eliminating multi-room presence at runtime. |

**A four-way disagreement to resolve deliberately, not silently** (three when first written; the compile gate is the fourth, added 2026-08-05). The durable docs currently assert the opposite of the intended design: [`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) says "**Steady state:** at most one room per `OBJECT#`; multi-room adjacency is drift," and [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md) says "Objects follow the same steady-state rule (nodes only)." **Storage** permits multi-host while the **membership compile gate** refuses it outright and the **relational** route silently ignores it; the stated design (2026-08-05) is that a rope should eventually be present in several rooms. Either the clause was scoped to **characters** and over-generalized to objects when object membership shipped, or the design moved and the docs did not. **Fix the docs as part of Phase 1**, whichever way it resolves --- an unwired repairer plus a contract clause forbidding what the design intends is exactly the kind of stale normative text that gets obeyed by a future reader who has no reason to doubt it.

### Improvisation is the objects lane's mechanism, generalized

Settled 2026-08-05, and it changes what several rows cost. **`ASSET#IMPROVISATION` is not a flag on a special kind of object --- it is an asset layer in the participation stack.** Improvised bodies merge through composite `ComponentData` like any other layer, and spawn-time `DEFAULT` situation prose deliberately routes objects through the **same** `ensureAuthoredCatalog` render path Feature / Knowledge / Character use rather than a bespoke stub. So the mechanism's actual purpose is already the general one this initiative needs: **mint a component nobody authored, such that nothing downstream can tell the difference.** What it lacked was a name for that purpose. Full detail: [`objects/AGENT.md`](../../../../../lambda/ephemera/dataSource/objects/AGENT.md).

**What arrives already built:** the four-way split (merge body / play meta / semantic embedding / placement), a two-step existence-then-placement sequence, the **S1** compensating delete, the orphan sweep and its delete-only repair, and a best-effort **LLM-in-the-loop discipline** (Bedrock embeddings and enrich-generated prose, both failure-tolerant and non-blocking) that AB-18's cost and latency policy should start from rather than re-derive.

**This sharpens AB-12 from a philosophical question into a cost question.** If Objects are already abstractions at every level, minting a battery or a place setting **is** minting an `OBJECT#` --- the lane needs no generalization at all, only use. The Room / Feature half is different in kind: every row key, module, and type in that lane is `OBJECT#`-shaped. The two bets therefore differ by roughly *no new storage* versus *a new storage shape*, which is far more decidable than the ontology argument.

**Three gaps, all bounded, all real:**

| Gap | Detail |
| --- | --- |
| **No composition relation** | Improvisation mints a **free-standing** object and places it in a room. A *part* needs the level-crossing edge as well --- so spawn+place becomes spawn+place+compose, or the compose **is** the place if AB-4 resolves part-of as a host relationship. Either way the S1 compensating-delete story needs revisiting, since it currently compensates a two-step sequence. |
| **Recognition fits less cleanly than Improvisation** | Improvisation creates a thing from nothing, so its existence rows are the whole story. Recognition creates an aggregate **over things that already exist**: its body is nearly empty and its **member set is the content**. The lane has no concept of a component whose identity is a set of other components. |
| **`EMBEDDING#IMPROMPTU` is `shortName`-derived** | Reasonable for a battery, questionable for "the place setting on the table." Whether an aggregate gets a semantic vector, and over what text, is undecided. |

**Naming consequence --- carried as AB-0's concrete form.** If Improvisation names the *mechanism*, then Improvisation and Recognition are **not peers**: Recognition is a trigger that invokes the mechanism, and operational necessity is the other trigger. The word is currently doing both jobs, which is the same conflation AB-14 was restructured to remove. Two ways out, unresolved: **(a)** Improvisation and Recognition stay the two triggers and the shared mechanism takes its own name; **(b)** Improvisation names the mechanism and the downward trigger is renamed.

**One speculative consequence worth keeping.** Because `ASSET#IMPROVISATION` is a *layer*, an improvised thing can later be **authored over**. A recognized snare trap could graduate into canon --- a real partial answer to AB-19's cost objection that an authored pattern vocabulary limits players to what someone anticipated.

### Coyote as prior art: bounded LLM reasoning that is not bounded structurally

Identified 2026-08-05 as the **existing first-iteration attempt** at reasoning about player-assembled contraptions --- deliberately flawed and ad hoc, and the closest thing to a working answer for AB-19. Read [`coyoteGame/AGENT.md`](../../../../../lambda/ephemera/dataSource/coyoteGame/AGENT.md), [`coyoteGame/AGENT.tropes.md`](../../../../../lambda/ephemera/dataSource/coyoteGame/AGENT.tropes.md), and [`coyotePlanAffinities.ts`](../../../../../packages/mtw-interfaces/ts/coyotePlanAffinities.ts) before designing anything in this area.

**What it does, structurally:** a `CoyoteStagedObject` snapshot assembled from `positionGraph` + `Meta::Object` + the improvisation pair, grouped by room; each object carrying `tropeAffinities` (roles with graded `aptness`, plus `environmentAffordances` / `affordancesProvided`) written at **Acme enrich time**; then a multi-hop LLM pipeline (candidates -> planSelect -> narrativeBeats -> outcome) with structured parse between hops and an internal-only `gimmick` narrative spine.

**What generalizes --- keep these:**

| Pattern | Why it matters here |
| --- | --- |
| **Annotate at mint time, not at reasoning time** | One Bedrock call at object creation writes durable structure; the reasoning step then *consumes* it. This is the direct answer to AB-18's cost concern and the reason AB-22 exists. |
| **Closed structural vocabulary, open narrative content** | Roles and tropes are enumerable; `gimmick` is deliberately free text with no closed enum. Matches the standing rule that only genuinely enumerable vocabulary should be hardcoded. |
| **Graded `aptness`, not booleans** | Affordance is a matter of degree, and downstream hops can rank rather than filter. |
| **Multi-hop with structured parse between hops** | Each hop's output is validated JSON, so a later hop reasons over structure rather than prose. |
| **Internal-only reasoning handoff** | `gimmick` never reaches the client. Reasoning scaffolding and player-facing narration are separate artifacts. |

**What is ad hoc --- and it is precisely the gap this initiative would fill:**

- **There is no persistent assembly-level object anywhere in it.** Every prediction reconstructs "what did the player build" from a **flat bag** of staged objects plus per-object role affinities. The contraption is never a thing.
- **Context is scoped to rooms, not to the contraption** (`loadCoyoteRoomObjectsByRoom`, across all Coyote game rooms). The LLM receives everything staged, relevant or not.
- **Therefore its boundedness is circumstantial, not structural.** It comes from the world being tiny and the genre closed --- six tropes, a hardcoded `boulder | cactus | ...` affordance enum, a handful of rooms. Remove any of those and nothing bounds the context.
- **The seam-label hack** (prompt labels vs. canonical `EphemeraRoomId`, an explicitly one-way contract carrying a six-item warning list about reversing it) reads as a symptom of the same cause: prompt context assembled ad hoc rather than projected from a real structure.

**The load-bearing conclusion:** an abstraction layer would supply the boundedness **structurally** --- the contraption is a named subgraph, and that subgraph *is* the context. Coyote answers the annotation question per-object and the scope question per-room; the missing piece is per-**assembly**.

### `thinking` as the scheduling substrate: shipped ledger, unbuilt queue

Identified 2026-08-05 as the intended home for deferred recognition, and **verified against code** because the distinction between what exists and what does not is load-bearing. Read [`thinking/AGENT.md`](../../../../../lambda/ephemera/dataSource/thinking/AGENT.md).

**Shipped and reusable:** durable `Meta::Schedule` rows on a `TASK#${workItemId}` partition, a status vocabulary of `scheduled | claimed | cancelled | completed`, job bootstrap and adjacency, rollup to `Job Completed`, client dashboards, and `fetchThinkingResult` lookup by `workItemId`. The retention policy already anticipates this exact use: **"in-flight rows --- `scheduled` / `claimed` schedules ... do not set `deleteAt`, so *opportunistically queued work* is not TTL'd while waiting."**

**Not shipped --- and this is the actual work:** **nothing writes `claimed`.** Only `scheduled`, `completed`, and `cancelled` have producers; `claimed` appears in the type union and in `persistThinkingJobError`'s `IN_FLIGHT_SCHEDULE_STATUSES` for cancellation, with **no claimer anywhere**. Every thinking job today is bootstrapped at the start of a synchronous pipeline (Coyote hypothesis, Acme enrich) and completed within that same Lambda invocation. **The schedule rows are observability records of work already happening, not a queue anything pulls from.** A worker, deferred dispatch, and promotion-on-demand all remain to be built.

**One guarantee that comes free, and it closes AB-18's convergence requirement.** `Meta::Result` is written with `ephemeraDB.nonCollidingPutItem` --- conditional on `attribute_not_exists`, **first write wins**, duplicate finalize returns `alreadyFinalized`. That is exactly what "the deferred pass and the promoted foreground path must not produce two snares" needs, already shipped and already retry-safe. Two independently-derived requirements (AB-18's convergence, and promotion-on-demand from the thinking side) landing on one existing mechanism is evidence the framing is sound.

---

## What already exists to build on

Read these before answering anything. Several are not background --- they are hooks the codebase has *already pre-registered* for this work, and re-deriving them from scratch would waste the design effort that put them there.

1. **The rooted-graph trigger, already written down.** [`positionGraph/AGENT.md` --- Known limitation (deferred)](../../../../../lambda/ephemera/dataSource/positions/positionGraph/AGENT.md#known-limitation-deferred) records that `EphemeraPositionGraph` is host-bound and unrooted, that [`CarryClosureFragment`](../../../../../lambda/ephemera/dataSource/positions/positionGraph/expandValidate/interactionUnderTransfer.ts) --- `{ rootId, members, edges }` --- is "a rooted position graph in all but name," and that **if a root concept ever arrives, the fragment should collapse into it rather than persist as parallel duplication.** A composition abstraction is a named, persistent carry closure. That note is pinned in two places specifically so whoever adds a root here finds it. This is the single strongest existing anchor for axis 1.
   - One constraint travels with it: **`rootId` is recorded, never derived.** `computeCarryClosure` takes it from its `startId` argument because the BFS guards with `closureSet.has(...)`, so the traversal *tree* is order-dependent while the induced edge set is not. Any root concept must be an input.
2. **Object-as-host, already deferred rather than rejected.** [`AGENT.concepts.md` --- Fractal position graphs](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md#fractal-position-graphs-container-scale-and-edges) states container scale shipped for Character hosts (D16) with "**Object `OBJECT#` / Area hosts deferred until needed**." Nesting is precisely the case that makes them needed. `EphemeraMembershipHostId` is Room | Character today.
3. **Improvisation as the minting precedent.** The objects lane already brings things into existence on demand, with a two-step existence-then-placement sequence, a compensating delete, and a diagnostics orphan sweep: [`objects/AGENT.md`](../../../../../lambda/ephemera/dataSource/objects/AGENT.md), and the contract's [Object room membership](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md#object-room-membership-nodes-only) section. Axis 1's minting question (Phase 3) should start from this shape, not invent one.
4. **Graph roles: same shape, different authority.** [`AGENT.concepts.md` --- Graph roles](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md#graph-roles-shared-shape-different-authority) separates authored blueprint / play manipulation / materialized presentation / ephemeral presentation for the *same* `{ nodes, edges }` shape. That table exists because conflating them cost something before. An `Abstraction` will face the same fourfold question and should be answered with the same discipline.
5. **Trim-to-valid as the decay precedent.** The eviction ladder ([`AGENT.concepts.md` --- Eviction ladder](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md#eviction-ladder)) answers "what survives when the world changes underneath a stored structure" by filtering to what is still valid, reading the top survivor, and **calling it a change only when the resolved answer differs**. AB-6 is structurally the same question.
6. **Projections are filters, not the graph.** Same concepts doc, core vocabulary. AB-9 leans hard on this: if level-selection is a projection concern, a large fraction of the LLM-scalability payoff lands in the presentation lane and never becomes stored truth.
7. **The step vocabulary an abstraction would have to join.** [`manipulation/AGENT.implementation.md` --- Step vocabulary](../../../../../lambda/ephemera/dataSource/positions/manipulation/AGENT.implementation.md#step-vocabulary) and the Synthesize executor's instruction language. AB-11 asks whether abstractions are new *operands* in that language or need new *step kinds*; the answer determines whether the kernel changes at all.
8. **Where state does *not* currently live.** There is no `Variable` / `Computed` in WML today, and `dataSource/state/` is asset-stack resolution, not diegetic state. AB-7 may therefore be filling a genuinely vacant slot rather than extending an existing one --- verify this before designing against it, and check [`diegeticLogic/AGENT.md`](../../../../../lambda/ephemera/dataSource/diegeticLogic/AGENT.md) for prior operator-design thinking.

**Testing authority:** [`lambda/ephemera/AGENT.testing.md`](../../../../../lambda/ephemera/AGENT.testing.md) --- `npm run test`, not `npm test`, run from `lambda/ephemera`. There is no `taskPlanning/lambda/ephemera/AGENT.development.md`.

**Hazard, inherited and repeatedly re-learned:** `*.integration.test.ts` files sit **outside** the tsconfig include and mock modules **by path**, so `npx tsc --noEmit` cannot catch a break in them. Run the full jest suite after any rename or deletion, and grep module *paths*, not only symbols.

Baseline (should pass before any edits, from `lambda/ephemera`):

```bash
npm run test -- --watchAll=false \
  dataSource/positions/ \
  dataSource/actions/enrich/objectManipulation/
```

---

## Proposal P1: single-host objects + hosting at the containing node

**Status: adopted provisionally as [H1](#working-hypotheses), 2026-08-05.** Recorded first as an unjudged proposal, then promoted --- **not** to a decision, but to a *working hypothesis whose job is to ground further design*. Every answer derived from it is tagged `H1-dependent` so the rollback set stays known; see [Working hypotheses](#working-hypotheses) for the tagging test, the falsification cases, and the named rollback triggers. It touches AB-4, AB-8, AB-9, AB-16, AB-17 and the AB-21 identity question, so it is written up here rather than inside any one row.

### The proposal

1. **Remove multi-host capability for Objects.** An Object has exactly one host. (Features may remain multi-host --- see the note below.)
2. **Give Objects a direct lookup of placement data on their `Meta`**, rather than a variable set of adjacency rows.
3. **Spanning things are modelled as parts.** "Tie rope to rock" then "drop rope into abyss" improvises **two rope parts**: one stays in the cliff-top room tied to the rock, one goes into the abyss.
4. **The whole is hosted at the node high enough in the containment tree to contain all of its parts.** The rope object itself hosts in the **Area** containing both rooms, with part connections (likely two-way) to its parts.
5. **Migration is automatic and bidirectional.** Haul the rope up, all parts in one room, and the rope migrates down from Area into that room.

### What it buys (assessed against code, 2026-08-05)

| Gain | Detail |
| --- | --- |
| **Every threatened invariant survives** | `computeEndStateRoomDiff`'s end-state scrub stays correct; adjacency stays single-valued; `MutationKernelTransferStep`'s singular `toHostId` needs no widening. |
| **The contract clause is vindicated, not stale** | "At most one room per `OBJECT#`" becomes **true as written** --- the design moves to meet the contract instead of the contract being rewritten. Materially cheaper than the widening path, and it retires the three-way disagreement recorded under [Multi-host presence](#multi-host-presence-what-is-actually-true-today). |
| **The relational-edge problem dissolves** | Rope tied to rock is an ordinary **same-host** edge between the cliff-top *part* and the rock. This was the sharpest structural objection to multi-room ropes; it needs zero new machinery. |
| **Presentation is correct by construction** | Each room's `graph.objectIds` holds the part actually present, so no room renders a whole rope it only partly contains. |
| **The drift class genuinely shrinks** | A single-valued host cannot exhibit multi-room drift. `repairObjectPlacementDrift`'s multi-room scrub branch becomes **structurally impossible**; only field-vs-graph lag survives. A deletion, not a relocation. |
| **Improvisation is reused unchanged for parts** | A part is an Object; minting one is minting an `OBJECT#` into `ASSET#IMPROVISATION`. Only the composition edge is new (already tracked as gap 1). |

### What it costs --- concentrated almost entirely in the host containment tree

| Cost | Detail |
| --- | --- |
| **Area is further away than "parked"** | `EphemeraMembershipHostId = EphemeraRoomId \| EphemeraCharacterId` ([`ephemeraPositionAdjacency.ts`](../../../../../packages/mtw-interfaces/ts/ephemeraPositionAdjacency.ts)), and **there is no `EphemeraAreaId` in `baseClasses.ts` at all**. Area exists only WML/asset-side (`AREA#downtown`, `AREA#region`). This introduces an ephemera-side identity, not merely a deferred host kind. |
| **Containment is authored downward, with no upward index** | `StandardAreaData` carries a `positionGraph` listing its members, so "Area contains Room" is nodes in the Area's graph. **Nothing maps room -> area.** The rule needs the upward direction on every part move --- structurally the same missing thing as the parked reverse-edge-reference index. |
| **Is it a tree at all?** | Area membership is authored blueprint **merged at participation order**, so containment may be **asset-stack dependent** --- while an object's host must be world truth, not perspective-relative. `AREA#region` beside `AREA#downtown` also suggests nesting depth. And two rooms with no common Area leave the LCA undefined without a world root. **This is the load-bearing unknown for P1.** |
| **Migration is an invariant needing transactional cascade** | Every part move recomputes its whole's host, which may move the whole, which may cascade to wholes-of-wholes. `computeStepSequenceFootprint` runs **once, up front**, and `MultiKeyUpdate` cannot be re-entered --- so plan time must walk composition *upward* to pre-lock every host the cascade might touch. |

### What it changes conceptually

- **Two kinds of hosting, needing a name.** Room-hosting means *physically there*; Area-hosting is *bookkeeping*. Area-hosted objects presumably never surface in affordances --- state it, or someone writes compose that surfaces them.
- **It makes AB-9 acute rather than dodging it --- and [C5](#c5-the-chain-mounted-at-both-ends) escalates that from acute to routine.** With all parts back in one room, the whole **and** its parts sit in that room's graph: "you see a rope, a rope end, and a rope end." Description must pick a level, unavoidably. C5 shows this is not a transient state during manipulation but the **permanent steady state of anything anchored**: a chain mounted at one end and attached to a chest at the other sits in the cell's graph as three nodes indefinitely. Level selection is therefore a default read-path concern, not an edge case.
- ~~**Migration-down and reabsorption may be the same event (AB-17).** Coiling the rope is all-parts-co-located, which is both "migrate the whole into the room" and "dissolve the parts."~~ **Falsified 2026-08-05 by [C5](#c5-the-chain-mounted-at-both-ends).** Co-location and load-bearing are independent: the chain's parts are all in one cell *and* still carry the relations anchoring it, so the whole migrates down while the parts must persist. **P1 collapsed two conditions into one** --- AB-17's criterion was already written as a disjunction ("no part is separately referenced **or** separately positioned") and clause 5 tests only the second. The two events stay separate; **AB-17** carries the split criteria. Struck rather than softened, per this plan's rule on recording supersessions.

### What it does not resolve

- **How parts *connect* is out of P1's scope.** Parts plus a whole give membership, not structure. **This is AB-26, and it is not a sequence** --- connections are adjacency-mediated edges ("the clifftop end connects to the shaft section by way of the 'into the abyss' exit"), which branch. P1 and AB-26 are complementary, not competing: P1 decides *where the whole is hosted*, AB-26 decides *how the parts relate*.
- **Multiple parents (AB-13) is fine**, incidentally: each whole computes its own LCA independently, so an object being part of two wholes creates no conflict.

### On the two sub-proposals

**`Meta` direct lookup:** single-host makes the reverse index single-valued, collapsing a variable row set into one field --- a genuine simplification of *shape*. It remains a dual write with the same atomicity discipline and the same "`positionGraph` wins" conflict rule, so it **shrinks** the drift class rather than removing it.

**Features stay multi-host:** this is **forward-looking, not preserved.** Features are not graph nodes at all today (the node `tag` union is `'Character' | 'Object'`). It is consistent with their documented narrowing --- never membership-moved, never a relation subject, therefore no transfer semantics to get wrong --- but it describes a capability yet to be built.

**And the phrasing may be wrong, not just unbuilt (added 2026-08-05, see AB-30).** "Features stay multi-host" presumes Features are the kind of thing that *has* hosts. If a wall between two rooms is better modelled as an **edge** than as a node, it is not multi-host --- it is a non-member, and the sub-proposal is answering a question that does not arise. Note the asymmetry this sub-proposal currently carries unremarked: H1's entire thrust is **removing** multi-host for Objects, while this clause **preserves** it for Features. That is either a real distinction with a reason, or the clause is describing the problem in the vocabulary H1 just abandoned. AB-30 decides which.

---

## Working hypotheses

**Why this section exists (2026-08-05).** This plan's governing posture has been that *deciding is premature* --- there is useful discussion left in most of these questions, and closing them early would foreclose it. That posture is unchanged. What changed is the recognition that a question can also be under-discussed for the opposite reason: with no grounding assumption, downstream rows have nothing to argue *against*, and the discussion stays abstract rather than premature.

**A working hypothesis is the middle move.** Adopt a candidate explicitly as a *test of how well it grounds further design*, tag every answer derived from it, and keep the derived set visible so it can be rolled back as a unit. If a hypothesis dies, the paper trail says exactly which answers die with it and where to branch from with a different candidate. **This is not a decision, and a hypothesis surviving is not evidence it is right** --- only that nothing has yet killed it.

### The tagging test, and why it is the point

Mark a row **`H1-dependent`** only if its answer is **derived from** the hypothesis --- it would not make sense, or would be answered differently, if the hypothesis were withdrawn. Do **not** mark a row merely *compatible* with it.

| | Example | Tag |
| --- | --- | --- |
| **Derived from** | "Part-connections need an Area-level graph" --- only holds because P1 hosts the whole at the Area | `H1-dependent` |
| **Merely compatible** | AB-26's "adjacency *licenses* an edge, never derives one" --- true under framing (a) as well | untagged |

**The distinction is not bookkeeping; the count is a measurement.** A tag applied to everything makes rollback total, which is the same as having no trail at all. Applied honestly, the size of the dependent set says how much work H1 is actually doing: a small set means the hypothesis was cheap and most of the design stands on its own; a large one means it is load-bearing and the lock deserves more scrutiny than it has had. Record that count at [Phase 3](#recommended-order)'s consistency pass, where the set is finally known.

### H1: single-host objects, hosting at the containing node

**Statement:** [Proposal P1](#proposal-p1-single-host-objects--hosting-at-the-containing-node), in full.

#### H1 is a conjunction, and its clauses are tested separately

Adopted 2026-08-05 after "H1 is untested against a serious attempt" turned out to be **too coarse a statement to be true or false.** [C7](#c7-ariadnes-thread) is a passed discriminating test --- of *one clause*. Tracking per-clause is what makes the next tests focused rather than another all-purpose scenario, and it makes the untested surface visible instead of averaged away.

**They are also not independent, which the first version of this table got wrong.** Corrected 2026-08-06: **clause 1 applied to *wholes* generates clause 4, and clause 4 generates clause 5.** Enforce single-host, decompose a spanning thing into parts, and the *whole* still exists --- if it is a graph node it needs a host, and its parts are by construction spread across rooms. So "where does the whole live" must be answered, and clause 4 is that answer. Then, since the host is defined as *the node containing all parts* and parts move, the host must change: **clause 5 is not a feature, it is what maintaining clause 4's invariant consists of.** Three of the five clauses are therefore one commitment with two entailments, and testing them as peers would have over-counted H1's independent content.

| # | Clause | Tests run | Status |
| --- | --- | --- | --- |
| **1a** | **Parts** have exactly one host | implied by C7's decomposition result | **Nearly free.** Follows from clause 3: a part spanning two rooms would be decomposed further, since the criterion *is* "a distinct locus is needed here" |
| **1b** | **Wholes** have exactly one host | **AB-32's same-mechanism requirement** (2026-08-06) --- an argument, not a scenario | **Survives, at a stated price.** Single-hosting is compatible with scale-invariance **only if reference is traversal-shaped as the clean rule** ([three positions](#three-positions-on-reference-and-what-clause-1b-actually-costs)). Still what generates clauses 4 and 5; see [the non-positional-whole alternative](#candidate-drop-clause-1b-and-the-whole-chain-with-it), now narrowed to extent |
| **2** | Placement is a direct lookup on `Meta` | **none, and nothing is queued** | **Unexamined.** The most conspicuous gap in the register --- this clause has attracted no scrutiny at all across the whole design conversation |
| **3** | Spanning things are modelled as **parts** | [C7](#c7-ariadnes-thread) --- discriminating, competitor **failed** | **Passed.** Framing (a) cannot represent Ariadne's thread; H1 can. The strongest result available from a discriminating test --- not survival, but the alternative breaking |
| **4** | The whole hosts at the node containing all its parts | none --- *used* in C5, C6, C7, never *stressed* | **Untested; entailed by 1b.** AB-25 is its test, and rollback trigger 1 |
| **5** | Migration is automatic and bidirectional | none directly; C5 established parts persist through it | **Untested; entailed by 4.** Rollback trigger 3; the queued concurrent-migration and thrash cases target it |

**Credit where it is due and no further: clause 3 is not uniquely H1's.** AB-8's framing (c) proposed decomposition before P1 existed, and H1 adopts it --- so a (c)-with-multi-host variant passes C7 identically, which is why C7 earns no `H1-dependent` tag. **C7 vindicates decomposition, which H1 contains.**

~~**But that line of reasoning yields a cost reduction on clause 1.** Parts are local by construction, so nothing needs multi-host and clause 1 weakens from *remove the capability* to *nothing uses it*.~~ **Half wrong, corrected 2026-08-06.** The argument holds for **parts** and fails for **wholes** --- and the whole is precisely the object whose parts span. Only half the objects were checked. What survives is now **clause 1a**; what the argument missed is now **clause 1b**, which is not free at all. The surviving half is still worth having: P1's claimed gains --- compile gate, end-state planner, singular `toHostId`, per-room compose --- only require that no object *is* multi-present, so they are satisfied by 1a plus whatever answers 1b.

**Two things still separate unused from removed** for clause 1a: the contract's "at most one room per `OBJECT#`" is only *true* if enforced rather than merely unexercised, and Features sit outside the decomposition story entirely (AB-30). **Hold even 1a loosely:** it follows from the decomposition *rule*, not as a theorem --- a lazy decomposition could leave a part spanning.

#### Three positions on reference, and what clause 1b actually costs

**Raised 2026-08-06 as the sharpest pressure clause 1b has taken, and the first arriving from a *requirement* rather than a scenario.** The requirement (now on **AB-32**): *reference and perception must reach a spanning whole and a local whole by the same mechanism.* If "the thread in room B" and "the flashlight on the table" resolve by different machinery, that difference propagates into every consumer.

**The argument as first put:** you can have **one scale-invariant processing rule** or **single-hosting**, not both --- because a single-hosted spanning whole cannot be a node in each room it passes through, so reaching it needs a second path, and a second path is an epicycle. **Substantially right, and it turns out there are three positions rather than two.**

| Position | Reference mechanism | Price |
| --- | --- | --- |
| **1. Multi-host the whole** | Local lookup everywhere --- the thread is a node in A, B and C; the flashlight is a node in A | **Not free, and this cost was missing from the original framing.** [C7](#c7-ariadnes-thread) showed one node cannot record *where along* the thread anything attaches, so segments are **still** required --- room B then holds both the thread node and its local segment, two nodes for overlapping things, and "pull the thread" in B has two plausible bindings. Every mutation writes N graphs |
| **2. Single-host, traversal as the *clean* rule** | Local nodes **plus their reachable ancestors**. Zero hops for an undecomposed flashlight (the node present *is* the whole); one hop for a thread | **Breaks the dichotomy.** A loop that sometimes iterates zero times is one mechanism, not two --- differing hop count is not an epicycle. Cost is that it **inverts clean and fast**: today's `graph.objectIds` lookups become the *optimisation*, needing a stated validity condition. Legitimate [dirt](#clean-fractal-dirty-implementation) by that section's own test --- a lookup returning what the traversal would return is "the clean answer, computed faster" |
| **3. Single-host, lookup as the clean rule** | Local lookup, plus a separate path for spanning wholes | **This is the position the argument kills**, and correctly: the second path is not an optimisation of the rule but a *different rule for a subclass* --- an epicycle in the precise sense |

**The narrowed dichotomy, which is the durable form:** ~~scale-invariance or single-hosting~~ --- **you can have single-hosting or lookup-as-the-clean-rule, not both.** Scale-invariance survives single-hosting only if the clean rule is traversal-shaped.

**And the plan is currently sitting in position 3 without having chosen it.** [Part-mediated reference](#part-mediated-reference-how-h1-answers-its-own-replacement-trigger) was introduced as the answer for spanning wholes while local lookup remained the unexamined norm for everything else --- **two paths, never reconciled.** So the epicycle this argument predicts is not a future risk; it is already in the design as written, and was introduced the moment the spanning case got its own answer.

**One reason position 2 is cheaper than it first sounds:** C7's derived-presence question already pushes *description* toward traversal independently --- something must work out that a thread passes through room B. If description traverses anyway, reference traversing too is one mechanism serving both rather than a new cost.

#### Candidate: drop clause 1b, and the whole chain with it

**Recorded 2026-08-06 as a candidate, not adopted --- and narrowed the same day to *extent only*.** Follows directly from the entailment above: if 1b generates 4 and 5, then declining 1b removes all three.

**Scope correction, [C8](#c8-the-flashlight-that-vanished-when-someone-looked-at-it).** As first written this candidate was derived **entirely from C7**, an extent case, and generalised without being checked against a containment case sitting in the same document. Applied to a flashlight it is destructive: decomposition would make the whole non-positional, so the flashlight leaves the room's graph, affordance compose describes three components, and --- because the referent catalog reads the same `objectIds` --- **"pick up the flashlight" stops grounding**. Looking closely at a thing would make it unreferrable, while fictionally nothing happened.

**So the candidate holds for extent and inverts for containment:** extent makes the *whole* non-positional, containment makes the *parts* non-positional. Positional status looks like a property of the **composition kind**, not a global rule --- which is AB-16's distinction surfacing positionally. Everything below is therefore scoped to **extent wholes**; the LCA machinery it would remove is correspondingly confined to **scattered containment** rather than eliminated. **Untested premise:** that containment and extent are cleanly separable --- a ship's rigging may be both.

**The observation that motivates it: nothing reads the whole's host.** P1 states Area-hosted objects presumably never surface in affordances. [Part-mediated reference](#part-mediated-reference-how-h1-answers-its-own-replacement-trigger) establishes that the whole is never catalog-visible and is reached by **traversal from a part**. Description reaches it the same way. So neither the reference path nor the description path consults clause 4's host --- **it is bookkeeping nothing reads.**

**The alternative.** The whole becomes a **non-positional record**: a `Meta` row carrying the member set and the part-connection graph, *not* a node in any position graph. Parts remain graph nodes; the whole is structure **over** them. It then has no host, and clauses 4 and 5 do not arise.

**This is already a listed option, not a new invention.** AB-4 asks whether the level-crossing relation is membership with an Object host, a new edge kind, **or something that is not an edge at all** --- and the third option is exactly what makes the whole non-positional. It has been in the row since it was written.

**What it would buy:**

| Gain | Detail |
| --- | --- |
| **P1's gains survive untouched** | All four --- gate, planner, `toHostId`, compose --- require only that no object *is* multi-present. A whole that is neither a graph node nor an adjacency row cannot be multi-present at all |
| **AB-25 deflates** | It exists almost entirely to serve clause 4, and is currently both H1's critical path **and** its only live rollback trigger. It would not vanish (AB-30's walls may still need containment answers) but would stop being what H1 lives or dies on |
| **Trigger 3 evaporates** | No migration means no cascade, no concurrent-migration race, and no thrash --- three queued test cases dissolve rather than needing to be run |
| **No `EphemeraAreaId` needed for this purpose** | P1's largest single unbuilt dependency was an ephemera-side Area identity, required so wholes had somewhere to host |

**What has to be true for it to work --- and both are real questions, not formalities:**

1. **Composition must live in the record, not in position-graph edges.** ~~If wholes can themselves be parts of bigger wholes, this collapses.~~ **Decider corrected 2026-08-06 --- the original made the requirement/representation conflation this plan has just named.** Wholes-nest is **answered yes** (AB-32) and the candidate **survives it**: a record-based whole holds a member set of ids, an id can be another whole's, so nesting is free and hosts recurse to leaf parts. **The real decider, as a requirement: must a relation ever attach to a *whole* rather than to one of its parts?** Relations need nodes, so a yes forces wholes to be nodes and this collapses. Every case so far says no; "the place setting is on the table" is the untested shape. **The candidate still hinges on AB-4** --- it is that row's third option carried to its consequence --- but by way of AB-32.
2. **Lifecycle needs an owner.** With no host, what cleans up a whole when its parts are all deleted? Orphan-sweep shaped, and the objects lane has precedent (delete-only repair, the S1 compensating delete), but it is currently the whole's host that would make it discoverable.

**Clause-targeted tests this framing suggests**, replacing the habit of building one elaborate scenario and hoping it stresses something:

- **Clause 1a** --- hunt the corpus for a spanning thing that **resists decomposition**. If none exists, 1a holds and is close to free. AB-30's wall is the nearest candidate and is already excluded from the decomposition story, which is suggestive.
- **Clause 1b** --- the decisive question is not a scenario but **AB-4**: does composition live in the record or in position-graph edges? Record means [the whole can be non-positional](#candidate-drop-clause-1b-and-the-whole-chain-with-it) and 1b, 4 and 5 all go; edges means wholes must be graph nodes and the chain stands. **Answer AB-4 before running any clause 4 or clause 5 test** --- three queued cases target machinery that may not need to exist.
- **Clause 2** --- dual-write drift: does the reverse index stay single-valued across a failed transaction, and does the parent index from [part-mediated reference](#part-mediated-reference-how-h1-answers-its-own-replacement-trigger) land on the same `Meta` row, making it a multi-writer hotspot?
- **Clause 4** --- AB-25's three sub-questions, of which the sharpest is that Area membership is **asset-stack dependent**: if the LCA is perspective-relative then an object's host is not world truth, which is disqualifying rather than expensive.
- **Clause 5** --- concurrent migration and thrash (both queued), plus AB-31's fission, where **one event migrates two wholes in opposite directions**.

**Why it was promoted rather than left open.** Two arguments, of unequal strength. The weaker one, stated first and repaired: *the route non-uniformity means any single consolidated representation beats the status quo.* True about the inconsistency, but **symmetric** --- it argues equally for consolidating on multi-host. What actually selects H1 is that the five sites are not symmetric in **cost**: under H1 the membership compile gate, the end-state planner, the relational host map, the singular `toHostId`, and per-room affordance compose all become **correct as written**, no changes at any of them; under framing (a) every one needs widening. Five sites that independently decided single-host is weak evidence that single-host is natural and strong evidence that widening is expensive.

**What it buys the design, beyond the code sites.** It predicts a specific answer to AB-1 rather than dissolving it. Extent stops being its own axis (the rope is parts plus a whole, so AB-8 collapses into AB-4/AB-26), and the functional-state aggregate plausibly shares the same *carrier* --- a generator-and-door system hosts at the Area by the same rule --- while the **relation kind** differs, since a generator is not *part of* a door. That is AB-1's "one substrate, distinct types above it" option, arrived at from a mechanism rather than chosen from a list.

**What it does not buy: net simplification.** H1 retires AB-8 and buys **AB-25**, which is not close to answered and is a bigger commitment than the proposal's wording suggests --- `EphemeraAreaId` does not exist ephemera-side at all, no room -> area index exists anywhere, and containment may not be a tree. It also makes **AB-9 acute** rather than deferrable (a rope and two rope ends in one graph; description must pick a level), and AB-9 is the scope-controlling row. Forcing AB-9 is arguably good. It is not a reduction, and the promotion should not be defended as one.

### What counts as a falsification case (learned the hard way, 2026-08-05)

Three cases in, two of the constructed ones had failed as tests --- one dissolved because it was not a test at all, one passed by acquiring dependencies --- while the corpus was meanwhile producing genuinely hard *scenarios* that H1 sailed through. **The diagnosis matters more than the individual cases: representational adequacy is nearly free, and it is not what H1 claims.**

**The tell is the tag count.** [C6](#c6-the-rope-the-candle-and-the-impromptu-timer) --- rope, candle, impromptu timer, about as baroque as play gets --- added **zero** `H1-dependent` rows. Everything it established is equally true under framing (a). **A case a competing model passes identically has no selective power over the choice between them.** It was measuring composition-in-general, which was worth doing, and was not a test of this hypothesis.

**Why "can it represent X" keeps passing.** Any sufficiently expressive graph model can express almost any situation --- there is always another node and another edge available. Fictional complexity is not structural difficulty. Meanwhile **H1's load-bearing claims are mechanism claims**: *the whole hosts at the node containing all its parts*, and *migration is automatic and bidirectional*. Nothing run so far has required the LCA to be **computed** rather than asserted to exist, or a migration to be **planned** against `computeStepSequenceFootprint`'s once-only footprint.

**So a case earns its place by being at least one of:**

| Kind | Test |
| --- | --- |
| **Discriminating** | H1 and framing (a) give **different** answers. If both handle it the same way, it is a composition case, not an H1 case --- file it as corpus, not as falsification. **Two clauses, and the second was added after the first proved insufficient. (1) Show the competing model's answer step by step; do not assert it.** "Framing (a) handles this trivially" turned out to be wrong on inspection --- (a) needed the same derivation H1 did. **(2) Then check whether the resulting state is *faithful*, not merely reachable.** Clause 1 alone still produced a wrong verdict on [C7](#c7-ariadnes-thread): the steps were shown, (a) came out cheaper, and the reason it was cheaper is that it **cannot represent the object** --- one node, no notion of where along the thread anything attaches. **An alternative that produces a wrong state cheaply is not winning.** Both failures were the same habit: evaluating the alternative on the dimension that is easy to count rather than the one that decides. |
| **Mechanism** | The LCA rule must actually run, and the resulting migration must be plannable in one footprint pass. |
| **Interaction** | H1 meets shipped machinery built assuming single-level objects --- carry closure's boundary-edge classification, the orphan sweep, Coyote's bulk clear, the eviction ladder. |

**Falsification cases --- run these before dependents accumulate.**

| Case | What would kill H1 |
| --- | --- |
| ~~**Born-spanning things** (river, cable run, wall)~~ **--- retired 2026-08-05, it was not a test** | Two faults. **(i)** [C5](#c5-the-chain-mounted-at-both-ends) already absorbed it: a river's parts are permanently separately-positioned, so the whole stays Area-hosted and migration-down simply never fires. A rule returning a stable answer for a stable thing is not vacuous --- `computeEndStateRoomDiff` returning `changed: false` on a no-op is not evidence against it either. **(ii)** The three examples were **not one category**: a river and a cable run are ordinary Objects with extent that H1 handles unstrained, while a **wall** is structurally different and was grouped in on the surface property "multi-room from creation." That misgrouping is now **AB-30**, which is a design question rather than a falsification case. |
| **"Pull the rope" while the rope is Area-hosted** *(replacement, 2026-08-05)* --- **run same day; passed conditionally** | The concern: P1 says Area-hosted objects presumably never surface in affordances, but a spanning rope is Area-hosted **exactly when a player most needs to refer to it**. **Resolved by [part-mediated reference](#part-mediated-reference-how-h1-answers-its-own-replacement-trigger)** --- the locally-present part takes the grounding and a parent index walks up to the whole, so the whole never needs to be catalog-visible and P1's affordance clause stays true as written. **Conditional on** AB-6 resolving to stored, AB-15's naming seam having a workable answer, and the parent index being built. |
| ~~**Rope state (iv), re-coiling**~~ **--- run 2026-08-05 via [C5](#c5-the-chain-mounted-at-both-ends), a sharper instrument** | Tested whether migration-down and reabsorption are the *same event* (AB-17). **They are not.** Re-coiling changes co-location and load-bearing simultaneously and so cannot distinguish them; C5's chain holds co-location fixed while the ends stay load-bearing. **Outcome: a sub-claim of P1 falsified, H1 itself untouched** --- nothing here bears on single-host objects or hosting at the containing node. Rope state (iv) still belongs in the corpus for its own sake; it is no longer load-bearing as a falsification case. |

| **One end of the rope held by a character who walks away** *(added 2026-08-05; run the same day as [C7, Ariadne's thread](#c7-ariadnes-thread))* | **Outcome, after one reversed verdict: evidence *for* decomposition, and not discriminating on hosting.** ~~First reading: H1 is more expensive than framing (a) --- (a) adds a room to a container set, H1 mints a span-part per crossing --- so H1 takes damage.~~ **Withdrawn same day.** That compared *operation cost* without checking whether the resulting state is **correct**. Under (a) the thread is **one node**, so a bystander grabs the entire thread rather than a middle, and three attachments on three hosts have **nothing recording where along the thread each sits** --- a container set is unordered, and the route cannot be recovered from exits because a labyrinth has multiple paths. **(a) cannot represent Ariadne's thread at all**; its cheapness is the cheapness of not modelling the object. The case therefore discriminates between **one node and parts**, not between multi-host and single-host --- and conceding parts lands in AB-8's framing (c), which H1 embodies. **What survives independent of the reversal:** lazy minting is circular (see the description-before-reference constraint), and both framings need the same presence derivation. Original framing below, kept because its AB-25 consequence stands. **Discriminating and mechanism at once.** Under framing (a) the rope is simply present in two hosts and nothing special happens. Under H1 the whole needs an LCA of a room and a **character** --- and `EphemeraMembershipHostId` is `Room \| Character`, so characters *are* hosts. The containment tree therefore has to admit characters as **interior** nodes (room contains character contains held object), which AB-25 does not contemplate, being framed purely as Room/Area. **The consequence to check before going further:** character navigation is the highest-frequency mutation in the system and routes through the same `commitStepSequence` as object take/drop (BD-36), so a walker carrying a rope end re-hosts the whole rope. Navigate must then pre-lock every whole whose part the walker carries --- feasible with the parent index, but it makes the commonest operation in the game structurally more expensive and exercises **rollback trigger 3 on ordinary movement** rather than on contrivances. |
| **Carry closure meets a composition edge** *(queued --- clause 3)* | **Interaction.** Pick up a chest with a rope tied to it, the far end in the next room. [`boundaryEdgeOutcomes`](../../../../../lambda/ephemera/dataSource/positions/positionGraph/expandValidate/interactionUnderTransfer.ts) classifies incident edges `carry` / `dissolve` / `defer` / `error` --- **is a composition edge a boundary edge, and which verdict?** This plan has three times found that discipline anticipating cases it was not written for (AB-28, AB-29, [C5](#c5-the-chain-mounted-at-both-ends)). Worth deliberately testing somewhere it might not, rather than continuing to expect it. |
| **Concurrent migration** *(queued --- clause 5)* | **Mechanism.** Two characters holding opposite ends, both moving in the same instant. Each transaction recomputes the whole's host from its own view and locks a **different** footprint. Whether the retry sees a coherent state is a `MultiKeyUpdate` question rather than a modelling one --- and it is the kind of thing that is cheap to check now and expensive to discover in production. |
| **Migration thrash and its narration** *(queued --- clause 5)* | **Mechanism, and partly a product question.** A character pacing across a room boundary re-hosts the rope on every crossing. Nothing happened fictionally --- but the presentation kernel narrates on host change. If each crossing narrates, the design is visibly wrong to a player; if it does not, something must suppress it, and that suppression rule does not exist. |

**Rollback triggers, named up front so "dead end" is not decided by mood:**

1. **AB-25 resolves as *containment is not a tree and cannot be made one*** --- H1 then has no node at which to compute a host, and no amount of downstream cleverness recovers it. This is H1's critical path and its most likely cause of death.
2. ~~**A born-spanning case reads wrong** under permanent Area-hosting.~~ **Retired 2026-08-05 --- it was not a test** (see the falsification table above). **Replaced by: the whole becomes unreferrable while Area-hosted.** If "pull the rope" cannot bind to the rope in the state H1 exists to model, the design has moved the difficulty rather than solved it. **Recorded as a weakening, not a win:** retiring a badly-built trigger leaves H1 *less* falsifiable than this section originally claimed, and the replacement is a genuine substitute only if it is actually run. **Run 2026-08-05 and passed conditionally** --- see [part-mediated reference](#part-mediated-reference-how-h1-answers-its-own-replacement-trigger). **This trigger is now spent**, so trigger 1 (AB-25) is the only live one remaining, and H1's falsifiability now rests almost entirely on it. Weigh that when Phase 2 reaches AB-25: it is both H1's critical path and its last remaining test.
3. **The migration cascade proves unplannable** --- every part move recomputes its whole's host and may cascade to wholes-of-wholes, while `computeStepSequenceFootprint` runs **once**, up front, and `MultiKeyUpdate` cannot be re-entered. If plan-time upward walking cannot pre-lock the affected hosts, H1 is unimplementable on the shipped kernel regardless of how well it models the fiction. **This trigger got *more* likely on 2026-08-05, not less.** [C5](#c5-the-chain-mounted-at-both-ends) shows parts persist through migration rather than dissolving, so dragging a chained chest out of a cell and back cycles the whole up to the Area and down again with the part structure intact --- **a high-frequency operation on a structure that is not changing.** Had reabsorption coincided with migration-down, cascades would at least have been rare events at decomposition boundaries. They are not; they are ordinary movement. Weight trigger 3 accordingly when Phase 2 reaches AB-25.

### Part-mediated reference: how H1 answers its own replacement trigger

**Proposed and assessed 2026-08-05.** Written here rather than inside a row because it touches AB-6, AB-11, AB-13 and AB-15 at once --- same reason [P1](#proposal-p1-single-host-objects--hosting-at-the-containing-node) is written up separately.

**Defect found 2026-08-06, and it is in this scheme's *framing* rather than its mechanics.** As introduced, part-mediated reference was the answer **for spanning wholes**, while local `graph.objectIds` lookup stayed the unexamined norm for everything else --- **two reference paths, never reconciled.** That is [position 3](#three-positions-on-reference-and-what-clause-1b-actually-costs), the one an epicycle argument correctly kills: a second path for a subclass rather than an optimisation of one rule. **The fix is not to discard the scheme but to promote it**: make traversal the *clean* rule for all reference, with zero hops in the local case (an undecomposed flashlight's node *is* the whole), and let today's lookup be a fast path with a stated validity condition. Everything below then describes the general rule rather than a special case.

**The scheme.** In any room where "pull the rope" is a sensible thing to do, **a part of the rope is present** --- an end, a span. That part takes the identification-grounding, because it is what is locally there. It carries a **parent index** naming the whole it belongs to. Reference walks *up* that index to the rope object, which then accumulates the state of its parts across every room in order to evaluate what pulling does.

**Why this is the better answer, not merely an answer.** The alternative was to make Area-hosted wholes surface in room affordances, which would require compose to reach up the containment tree and would drag non-present things into every room's catalog. Under part-mediated reference, **P1's "Area-hosted objects never surface in affordances" stays true as written.** The whole is never in a catalog; reference reaches it by traversal rather than by presence.

**What it costs, checked against shipped machinery:**

| Step | Status |
| --- | --- |
| Part is catalog-visible in its room | **Works today, unchanged** --- the part is an Object node, so `roomObjectCatalogForCharacter`'s `positionGraph.objectIds` scan already finds it |
| Grounding binds "rope" to the part | **The one real gap.** See the naming seam below |
| Part -> whole traversal | **New**, a `Meta::Object` parent field. Cheap, but a new dual write with the usual atomicity and drift story |
| Whole -> all parts, all hosts | **New**, and it forces AB-6 (below) |
| "Pull" applied across hosts | **Shape exists.** `MultiKeyUpdate` already folds N effects into one `transactWrite` |

**It answers AB-11 with the cheap option.** "Pull" is not a new step kind --- the whole **expands** into per-part steps, which is precisely Synthesize's Expansion sub-role, already growing one ungrounded change into an ordered sequence for carry closure and `sameHost` repair. **The kernel's step vocabulary does not grow.** That is the outcome AB-11 exists to determine, reached by a scheme that was not designed to reach it.

**Three consequences it forces, recorded so they are owned rather than rediscovered:**

1. **AB-6 resolves toward *stored*.** Evaluating the pull needs every part and its host known **before** the walk begins, because `computeStepSequenceFootprint` runs once, up front. A *derived* member set cannot supply that without already knowing where to look.
2. **The parent index is AB-25's missing upward direction, one level down.** AB-25 records that containment is authored downward with no room -> area index; this is the same gap at object scale. **Distinct from** the reverse *edge*-reference index the plan has flagged three times --- different index, same missing direction.
3. **The naming seam, and it is the step that does not fall out of existing machinery.** The catalog matches on `normalizedShortName`. The player says "pull the **rope**"; what is present is a part. If the part's short name is "rope end," the whole's name is not in the catalog and grounding has nothing to bind to. Candidates: parts alias the whole's short name, a two-phase match consults the parent index, or the catalog widens to include reachable wholes. **This is AB-15 arriving concretely** --- unqualified reference should bind to the authored level, and under H1 the authored level is exactly the thing not locally present.

**And one smaller consequence: make the parent field plural from the start.** Multiple parents (AB-13) means a part belonging to both a rope and a knotted pair, where "pull the rope" is ambiguous. The ambiguity can be deferred; the field shape should not be, since widening it later is the more expensive order.

**Trigger 2 therefore passes --- conditionally, and the condition is the point.** The trigger asked whether the whole stays referrable while Area-hosted. It does. But it passes *because* AB-6 resolves to stored, AB-15's binding question has a workable answer, and the parent index gets built. **H1 survives by acquiring three dependencies, not by needing nothing** --- a normal outcome for a hypothesis doing real work, recorded as conditional so a later reader does not read "passed" as "free."

**The failure mode this section is designed against is sunk cost, not error.** Conditional adoption goes de facto permanent once the dependent set is large enough that rollback means redoing fifteen answers --- at which point H1 survives on accumulated cost rather than merit, and this section documents a decision nobody is willing to act on. **The mitigation is ordering, not discipline:** run the two falsification cases *next*, while the rollback set is still near-empty. The tagging is insurance for what accumulates afterwards, which is when insurance is worth buying.

**Rollback mechanics.** IDs are stable and never reused, so withdrawing H1 does **not** renumber anything: every `H1-dependent` row returns to **Open** with a note naming H1 as what invalidated it, exactly as [Phase 3](#recommended-order) already does for rows a later answer invalidates. The branch point is this section.

### Candidate H2, not adopted: AB-12's privileged-level reframe

Named here so the next load-bearing candidate is explored through this mechanism rather than re-inventing one ad hoc. **AB-12** --- everything is an Object at *some* level --- has the same shape as H1: it would dissolve rows rather than answer them, and it is currently recorded as "do not treat as settled" with no way to explore it conditionally. **Deliberately not promoted yet:** two hypotheses in flight at once would make the dependent sets overlap, and a row tagged both `H1-dependent` and `H2-dependent` cannot be rolled back as a unit --- which is the whole mechanism. Promote H2 only after H1 resolves, or in place of it if H1 dies.

---

## Parked extension points this initiative would unpark

**Framing (2026-08-05, from the author of most of these):** these were laid down deliberately, in anticipation of a wider-reaching reasoning effort that would eventually need them. **This is that effort.** So unparking is the *expected shape* of implementation here, not a cost to be minimized or a sign the slice is too big --- and a candidate slice that unparks nothing is more likely mis-aimed than cheap.

Every row below was verified in this repo during the design conversation. Each is a **deliberate** deferral with a stated trigger, not an oversight.

| Parked capability | Where it is recorded | What unparks it |
| --- | --- | --- |
| **`OBJECT#` / Area membership hosts** | [`positions/AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md#fractal-position-graphs-container-scale-and-edges) --- "deferred until needed" | Composition, if part-of resolves as a host relationship (AB-4). Nesting is the case that makes them needed. |
| **A root concept on `EphemeraPositionGraph`** | [`positionGraph/AGENT.md`](../../../../../lambda/ephemera/dataSource/positions/positionGraph/AGENT.md#known-limitation-deferred) --- pinned in two places, with `CarryClosureFragment` set to collapse into it | AB-3. The reciprocal note exists precisely so whoever adds a root here finds it. |
| **Features as graph nodes** | Same --- node `tag` union is `'Character' \| 'Object'` only | AB-12's Room/Feature half. Note Features are designed as *narrower* participants, not Object peers. |
| **Character-relation widening (BD-36)** | Same --- `HostRelationalEdge` endpoints are object-only; `removeCharacter`'s assert is vacuously satisfied today | Relations over non-object nodes; the assert is the placeholder already installed for that day. |
| **Reverse edge-reference index / post-clear sweep** ***(most load-bearing --- three independent consumers)*** | Same --- adjacency indexes *node* placement, not edge-only references | Cross-host abstraction membership (AB-6, AB-8); part-connections referencing a licensing relation (AB-26); and the backwards discovery a licence-removing mutation needs (AB-29). Three separate lines of reasoning arrived here independently on 2026-08-05. |
| **Render-cache `n` / mark-state axis + `situationId`** | [`renderCache/AGENT.md`](../../../../../lambda/ephemera/dataSource/renderCache/AGENT.md); pinned to `[]` and `SITUATION#DEFAULT` by iteration 10 | AB-23. Situational *meaning* is this axis raised from rendering to interpretation. |
| **`thinking` `claimed` status + a claimer** | [`thinking/AGENT.md`](../../../../../lambda/ephemera/dataSource/thinking/AGENT.md); status shipped, no producer, retention already protects queued work | AB-18, AB-24 --- deferred recognition and the ambient context tier. |
| **`EMBEDDING#PERSPECTIVE#...` rows** | [`objects/AGENT.md`](../../../../../lambda/ephemera/dataSource/objects/AGENT.md) Follow-ups --- "when perspective-scoped similarity is needed" | AB-23. Perspective-scoped semantic similarity *is* situational meaning, expressed in the embedding layer. |
| **WML `Relational` tag / `EdgeList` consolidation** | [`positions/AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md#wml-convergence-future) --- deferred pending heterogeneous room/container edge lists | AB-10, AB-15 --- authored composition patterns and authored interpretive frames. |
| **`Object Placement Drift Finding` sweep** | [`positions/AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) --- deferred; `repairObjectPlacementDrift` has no caller | AB-8, and the contract-contradiction cleanup already queued in Phase 1. |
| **Backtrack channel (BD-18)** | [`AGENT.backtrackChannel.planning.md`](../actions/AGENT.backtrackChannel.planning.md) --- iteration 4, named not started | AB-20: an LLM that proposes a wrong recognition needs a structured critique path, not a silent guess. |

**Use this table in Phase 5.** A slice's cost is substantially "which of these does it unpark, and is that one ready to be unparked" --- several have their own prerequisites, and at least one (the backtrack channel) is a sibling initiative that would need scheduling rather than absorbing.

---

## Phase 0 corpus

The evidence base later phases argue from. Each case is written against **today's** model and names the **code site** where it stops --- a case that cites a remembered claim rather than a line of code is not yet corpus.

**Cases C1--C4 were mined from the ladder's data-blocked parks** (see [The dependency is asymmetric](#the-dependency-is-asymmetric-representation-unblocks-reasoning)) rather than invented, per Phase 0's first sub-step. Mining them produced three corrections to this plan, recorded in [Discussion record](#discussion-record) and applied in place.

**Annotation line, uniform across cases:** read-side | write-side --- part kind (AB-16) --- Improvisation | Recognition (AB-14) --- description | mechanism (AB-19) --- which of the [three improvisation gaps](#improvisation-is-the-objects-lanes-mechanism-generalized) it exercises. These tallies are the direct input to Phases 1 and 2, which is why the shape is fixed now rather than after the remaining cases land.

### C1. Look at the character standing here; look at the north wall

**Origin:** the CPG-5 remainder park.

**Today's model.** A room graph holds one tag-discriminated `_nodes` array, `'Character' | 'Object'`. Features are not nodes at any tag. [`roomObjectCatalogForCharacter.ts:76`](../../../../../lambda/ephemera/dataSource/actions/roomObjectCatalogForCharacter.ts) builds the referent catalog from `[...positionGraph.objectIds]` and never touches `characterIds`.

**Limit hit.** No non-Object candidate is producible at runtime, so neither utterance can ground. [`compileDescribeFromSkeleton.ts:92`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/compileDescribeFromSkeleton.ts)'s `candidates.find(isEphemeraObjectId)` **says so in its own comment** --- "filtering to `EphemeraObjectId` candidates is today a no-op guard." The filter cannot fire because nothing upstream can produce anything for it to reject. The north wall is worse than unresolvable: it is not merely absent from the catalog, it has no node kind it *could* occupy.

**Found while verifying.** The same function takes the *character's own* room as `containers[0]` (line 70) --- a third silent single-host assumption, in the read path rather than the write path. Noted for C2 rather than developed here.

**Annotations.** Read-side --- part kind n/a (this is the participation question, upstream of composition) --- Improvisation, and specifically AB-21's **latent** flavor (a room has walls whether or not anyone wrote them) --- description --- exercises none of the three gaps directly, because it is upstream of minting: the thing cannot be *referred to*, so the question of whether it would be *minted* never arises.

**Rows grounded.** AB-12 (the Room/Feature half, and this is the concrete case for it), AB-2, AB-9, AB-21.

### C2. The rope in two rooms, refused at the gate

**Origin:** the BD-16(3) `multiPresent` park.

**Today's model.** Adjacency stores `(OBJECT#, POSITION#ROOM#...)` rows with nothing unique-constraining them; `getMembershipContainers` returns an array. Multi-room presence is therefore **storable**.

**Limit hit --- and it is five layers disagreeing about one object, in three different ways.**

| Layer | Behavior on a two-room rope | Site |
| --- | --- | --- |
| Adjacency storage | Permits it | `getMembershipContainers` returns an array |
| **Membership compile** | **Terminal error to the player** | [`complexityPreGates.ts:29`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/complexityPreGates.ts) --- `containers.length > 1` -> `multiPresent` -> `preGateOutcomeToTerminalError` |
| **Relational compile** | **Silently omits it** | [`compileRelationalFromSkeleton.ts:168`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/compileRelationalFromSkeleton.ts) --- `if (containers.length === 1)` populates `hostByObjectId`; otherwise the object simply has no host for Expansion, and `getCurrentHostForExpansion` returns `undefined` |
| End-state planner | Scrubs every room but the target | `computeEndStateRoomDiff` --- `froms = priorContainers.filter(!== to)` |
| Presentation | Renders a whole rope in each room | Affordance compose reads `graph.objectIds` per room |

Refuse, ignore, scrub, duplicate --- four incompatible answers, none of them wrong locally. **This is the corpus's sharpest single argument that the level structure currently lives in each consumer's head rather than in the model**, which is the burden [Why this initiative exists](#why-this-initiative-exists) names.

**Note the gate is route-scoped, not global.** `evaluateComplexityPreGates` has exactly one caller, [`compileMembershipAtomic.ts:121`](../../../../../lambda/ephemera/dataSource/actions/enrich/objectManipulation/compileMembershipAtomic.ts). The relational route and `executeObjectMove` / `orchestrateObjectMove` never reach it. The plan already recorded that the cross-host move route bypasses the end-state planner; the non-uniformity is therefore **worse than recorded** --- it is not one constraint applied unevenly but three different constraints on three routes.

**Annotations.** Write-side --- **homogeneous extent** (AB-16) --- Improvisation, downward and operation-driven (lowering the rope is what makes the ends separately addressable) --- mechanism --- exercises gap 1, **no composition relation**: improvisation would mint two rope parts as free-standing objects with nothing expressing that they are one rope.

**Rows grounded.** AB-8 (primary), AB-16, AB-25, AB-26, AB-27.

### C3. The snare trap Coyote cannot name

**Origin:** the Coyote genre-bounded reasoning context park.

**Today's model.** [`loadCoyoteRoomObjectsByRoom`](../../../../../lambda/ephemera/dataSource/coyoteGame/utilities/coyoteRoomObjectSnapshot.ts) walks `getGameRooms()`, and per room takes `[...graph.objectIds]`, stitching each into a `CoyoteStagedObject` of `{ objectId, shortName, stableKey, tropeAffinities? }`.

**Limit hit --- and it is sharper than "a flat bag."** That type has **no edges in it**, and the snapshot never reads `graph.relationalEdges`. So the *arrangement is discarded at the boundary*: the rope tied to the branch, the loop laid on the road, and the same three objects lying unrelated in a heap produce **byte-identical** prompt context. Coyote's prediction pipeline is asked to reason about a contraption from an inventory of nouns with role tags and no structure at all. The room-scoping limit (everything staged, relevant or not) is real but secondary --- widening or narrowing the scope of a set that cannot express relation does not help.

This is the strongest available evidence for the conclusion already recorded under [Coyote as prior art](#coyote-as-prior-art-bounded-llm-reasoning-that-is-not-bounded-structurally): the missing piece is per-**assembly**, and it is missing at the point where structure is projected into prompt context.

**Annotations.** Write-side --- **heterogeneous separable** (AB-16) --- Recognition, and the **multi-step** variety Phase 0 requires: tie loop, attach to branch, lay on road are separated in time with no single transition to fire on --- mechanism (reasoning about a snare *as* a snare is the whole point) --- exercises gap 2, **Recognition fits less cleanly**: the snare's body is nearly empty and its **member set is the content**, which is exactly what the objects lane has no concept of.

**Rows grounded.** AB-18 (scope and trigger; the no-transition-to-fire-on problem is visible here), AB-19, AB-22, AB-14, AB-6.

### C4. The place setting at the Faerie Court

**Origin:** the situational-meaning / mark-state-axis park.

**Today's model, and this corrects the plan's own premise.** The situational axis is **not** "built and unpopulated." It is **populated for Rooms and structurally absent for everything else**: [`requestIntake.ts:11`](../../../../../lambda/ephemera/dataSource/renderOrchestration/requestIntake.ts) states it as a rule --- "**Feature/Knowledge/Object/Character hosts:** no `Meta::Room`; always `markState: { markValue: [] }` and `allowGeneration: false`" --- and [`findRender.ts:95`](../../../../../lambda/ephemera/dataSource/renderOrchestration/findRender.ts) gates generation on `isEphemeraRoomId`. Rooms meanwhile carry real state: [`handleApiStateChange.ts`](../../../../../lambda/ephemera/dataSource/state/handleApiStateChange.ts) merges `markState` into `Meta::Room.state.marks`, with [`computeDefaultMarksForRoom.ts`](../../../../../lambda/ephemera/dataSource/state/computeDefaultMarksForRoom.ts) supplying defaults.

**Limit hit.** The empty pin on non-Room hosts is **not a deferral waiting to be switched on** --- it is downstream of there being no state carrier. `Meta::Room` is the carrier, and no other host kind has one. So an interpretive frame can attach to *a room* and cannot attach to *a thing*: the Faerie Court can be a Faerie Court, but the place setting on its table cannot be situationally significant, because there is nowhere on the place setting for significance to live.

**This links AB-23 to AB-7 in a way the plan did not record.** Situational meaning for anything below room scale needs a diegetic-state carrier for non-Room hosts, which is AB-7's question --- flagged there as "possibly out of scope" and now with a second consumer.

**Annotations.** Read-side, becoming write-side exactly if Recognition mints --- heterogeneous separable (AB-16) --- Recognition --- **description in one frame and mechanism in the other, which is the case's whole point** --- exercises gap 3, `EMBEDDING#IMPROMPTU` being `shortName`-derived, since "the place setting" is the plan's own named example of a questionable embedding.

**A finding about the annotation scheme itself.** C4 does not have a description-vs-mechanism value. At an inn the place setting is inert and its recognition is pure projection under AB-9's guardrail; at the Faerie Court the same six objects carry consequence. **AB-19's tag is therefore a property of *(case, frame)*, not of the case** --- so a per-case column cannot hold it. This does not need a new row: AB-23 already says the member-affinity filter is context-dependent, and this is that statement arriving from the corpus side. It does mean Phase 2 should not read the AB-19 tally as a partition.

**Rows grounded.** AB-23 (primary), AB-7, AB-19, AB-22, AB-24.

### C5. The chain mounted at both ends

**Origin:** not a park --- raised in conversation 2026-08-05 as a sharper instrument than rope state (iv) for the same test. **This is the first [H1](#h1-single-host-objects-hosting-at-the-containing-node) falsification case run**, and it falsified a sub-claim; see below.

**The case.** A chain in a dungeon cell, mounted to the wall at one end and attached to a chest at the other. Later the chest is dragged into the next room and then dragged back.

**Why it is a better instrument than rope state (iv).** Re-coiling a rope changes co-location **and** load-bearing at the same moment, so it cannot distinguish them. The chain holds co-location fixed --- everything is in the cell --- while leaving the ends load-bearing. It isolates the variable.

**Today's model.** Not representable in full. Chain-end -> chest is an ordinary intra-host Relational edge and works today. Chain-end -> **wall** does not exist, because Features are not graph nodes at any tag ([C1](#c1-look-at-the-character-standing-here-look-at-the-north-wall)). So the case's more interesting anchor needs AB-12's Room/Feature half before it can be stated at all --- **noted rather than worked around**, since the half that can be stated is enough to carry the finding.

**What it establishes.** **Co-location and load-bearing are independent axes**, so migration-down and reabsorption are different events. All three nodes (chain, chain-end-1, chain-end-2) must persist in the cell's graph for the relations to have endpoints, *while* the whole is correctly hosted in the cell rather than the Area. Dragging the chest out and back cycles the chain up to the Area and down again **without ever dissolving a part**.

**The guard is already shipped, and it throws.** Reabsorbing a part means removing its node, and [`removeObject`](../../../../../lambda/ephemera/dataSource/positions/positionGraph/AGENT.md#known-limitation-deferred) **asserts and throws** `RelationalEdgeStillReferencedError` when a Relational edge still references it (BD-33/35, with deliberately no silent-strip variant). So dissolving chain-end-2 while it is attached to the chest is not a subtle data-loss bug --- it fails at the kernel. The load-bearing test for the relational half is therefore a **graph query** (`nodeHasRelationalEdge`, already on the class), not an LLM judgment, and the unsafe version is already impossible. Third instance of the same convergence AB-28 records: the boundary-edge discipline anticipated the case.

**A third decomposition trigger, previously unnamed.** The chain's ends exist *while everything is co-located*, so decomposition was not triggered by spanning. It was triggered by needing an endpoint **finer than the whole** to bear a relation --- "chain the chest to the chain" requires a chain-end. That is on-demand Improvisation under AB-14, and it restores the symmetry the granularity insight claimed while relocating the axis P1 guessed at: **parts exist while they are needed as distinct loci, by position *or* by relation; the whole hosts wherever it must to contain them.** Two independent questions that P1 collapsed into one.

**Annotations.** Write-side --- **homogeneous extent** (AB-16) --- Improvisation, on-demand and relation-driven (the newly named third trigger) --- mechanism --- exercises gap 1, **no composition relation**.

**Rows grounded.** AB-17 (primary --- splits its criterion), AB-9 (escalated from acute to routine), AB-14, AB-16, AB-12 (via the wall anchor), AB-27.

### C6. The rope, the candle, and the impromptu timer

**Origin:** not a park --- a scenario already used in conversation as a specimen of the hi-jinks creative players actually attempt. Raised 2026-08-05 as a hard test.

**The case.** Tie a rope to the bedpost. Throw the rope out of the window. Place a lit candle **under** the span of rope between bedpost and window. Climb down; wait for the candle to burn the rope through; retrieve most of the rope from below.

**What it establishes, and it is not what it was aimed at.** Offered as a test against AB-25, it is really **AB-13's** case: one part with two wholes, neither containing the other. **This is a DAG requirement, not a DAG preference** --- a tree cannot express it at all. It is stronger evidence than AB-13's existing fork example, because the fork's three wholes (place setting, silverware set, inheritance) are categories of one kind that a reader can talk into a nesting, whereas C6's two wholes are different **in kind**: the rope is a physical object and the impromptu timer is a mechanism.

**The constraint it puts on AB-25.** The two wholes host at **different heights** --- the rope spans room and garden so it hosts at the Area; the timer's members are both in the room so it hosts in the room. Walking up from the shared span therefore yields parents at inconsistent levels, so **hosting height does not correlate with composition depth** and no implementation may treat the parent index as "the next level up." AB-25's core question --- whether Room/Area containment is a tree --- is untouched.

**Granularity, again from the third trigger.** The candle is under the **span**, not an end, and the span must exist as a distinct locus *because the candle relation needs something to attach to*. That is [C5](#c5-the-chain-mounted-at-both-ends)'s newly-named third decomposition trigger firing in a scenario built for an unrelated purpose --- two independent cases producing it is reasonable evidence it is real.

**Two gaps it opens.** Dissolution is **per-whole**: the timer dies when the candle burns out while the rope persists, so the shared span is released by one parent and retained by another, which AB-17 could not previously express. And the scenario's whole payoff --- the rope **burning through** --- is **fission**, one whole becoming two, which no row owned. Now **AB-31**.

**One separation this case forces, so the expensive claim does not ride in on the cheap one's evidence.** *The representation must permit the timer* --- multi-parent composition, per-whole dissolution, fission --- is a hard requirement, and C6 is strong evidence for it. *The system spontaneously recognizes rope-plus-candle as a timer* is AB-18's unbounded search, and C6 is only an aspiration for it. Same discipline AB-12 applies to the Object versus Room/Feature bets. Worth noting the case is nonetheless a good advertisement for AB-22's affinity filter: a candle carrying `trigger` aptness beneath a rope under tension is exactly the arrangement cheap to notice **without** a pattern library.

**It does not threaten H1.** Every object in it is single-hosted and every whole hosts at the node containing its parts. Recorded explicitly because H1's other two falsification cases are spent, and a hard case that H1 passes incidentally must not be miscounted as a test that was aimed at it.

**Annotations.** Write-side --- **both part kinds in one case** (homogeneous extent for the rope, heterogeneous separable for the timer's candle-plus-span) --- Improvisation for the span, **Recognition** for the timer --- mechanism, emphatically --- exercises gap 2, since the timer's body is empty and its member set is the whole of its content.

**Rows grounded.** AB-13 (primary, and its best example), AB-31 (opens it), AB-17, AB-25 (constrains), AB-16, AB-18, AB-22.

### C7. Ariadne's thread

**Origin:** the character-held rope end, run 2026-08-05 as [H1](#h1-single-host-objects-hosting-at-the-containing-node)'s first *discriminating* falsification case, and then recognised for what it actually is. **Naming it correctly is not decoration** --- the myth is a specification, and players who know it will attempt it.

**The case.** Theseus ties one end of a thread at the labyrinth entrance and unspools it as he walks, room by room, so he can retrace the route. Later he follows it back. Along the way: a bystander could trip over it, it could be cut, and someone could follow it *inward*.

**Why it is stronger than the two-room rope.** **(i)** It is *many* rooms, so it tests the mechanism at scale rather than at its minimum. **(ii)** Its entire purpose **is** the path --- "which rooms does this pass through" is not an incidental question about the object, it is the object's reason for existing. **(iii)** It is unspooled **progressively**, one threshold at a time, so it is the per-crossing cost iterated twenty times rather than paid once. **(iv)** It is **retraced**, making the read path a first-class operation rather than an afterthought.

**A structural convergence worth noticing.** The thread **is** **AB-26**'s part-connection graph made literal: a record of which exits were traversed, with the licensing relation on each connection. The data structure the plan proposes and the fictional object turn out to be the same shape. **And it sharpens AB-26's own correction:** the part graph is not *assumed* to be a sequence, but a thread's happens to be one, because a thread is unspooled continuously. A net's is not. So "not a sequence" means the shape cannot be presumed --- not that paths never occur.

**What the case establishes --- the bystander test.** Someone standing in a labyrinth room who is *not* Theseus tries to grab the thread trailing past.

| Framing | What happens | Cost | **Is the resulting state faithful?** |
| --- | --- | --- | --- |
| **(a) multi-host** | Add the room to the thread's container set; it becomes a node in that room's graph and is grabbable | One pure-add per room --- **but `MutationKernelTransferStep`'s singular `toHostId` means the kernel cannot express a pure-add today** | **No.** The thread is **one node**, so the bystander grabs the *entire thread*, not a middle. Attachments (tied at the entrance, tied to a pillar, held by Theseus) sit on three hosts with **nothing recording where along the thread each is**. A container set is unordered, and the route is not recoverable from exits --- a labyrinth has multiple paths. **(a) cannot represent this object.** |
| **H1** | There is no thread node to add --- the whole is at the Area and only *parts* are room-present. Requires **minting a span-part** in the room, placing it, connecting it via AB-26, updating the member set, recomputing the LCA | A write **per threshold crossing**, times the length of the labyrinth, each unwound on the way back subject to [C5](#c5-the-chain-mounted-at-both-ends)'s load-bearing check | **Yes.** The bystander grabs the local segment; each attachment sits on a specific part; the part-connection graph records which exit each connection traverses, so the route is explicit. |

**Verdict, after two reversals recorded below: C7 is a *passed discriminating test of [H1 clause 3](#h1-is-a-conjunction-and-its-clauses-are-tested-separately)* --- spanning things are modelled as parts.** The strongest result a discriminating test can give: not that H1 survived, but that the alternative **broke**. It says nothing about clauses 1, 4 or 5, which were used in working the case and never stressed --- nothing here asks whether the LCA is computable or the cascade plannable. The axis it actually tests is **one node versus parts** --- and conceding parts lands in AB-8's framing (c), which H1 embodies. An (a)-with-segments variant would be framing (c) with multi-host still permitted, at which point single-host is a secondary question rather than a settled one.

**And "mint it lazily, on reference" does not rescue H1 --- it is circular.** A player cannot refer to what they cannot perceive, so the trigger cannot be reference. **The constraint that falls out is the durable finding: presence must be established at *description* time, not reference time**, because description is upstream of reference. That kills lazy minting rather than repairing it, since description runs on every look and mint-on-describe is eager minting with extra steps.

**Three live options, no choice made:** eager minting (a write per crossing); **derived presence** --- description walks the part-connection graph and collects the rooms the licensing relations traverse, which is AB-9's projection answer applied to *presence* rather than to level; or accept that only Theseus can interact with it, which is cheap and, for a thread lying across a floor, hard to defend.

**The finding that reframes the comparison: framing (a) needs the same derivation.** Someone must still infer that a character entering a room while holding one end means the thread now occupies that room --- a flat container set does not supply that either. **(a) stores the result at write time; H1 would compute it at read time.** So the real discriminator is **stored versus derived presence**, which is AB-6 one level over, and the trade is the familiar one: stored is cheap to read and needs drift repair, derived is always correct and pays per describe --- though the render cache already exists to absorb exactly that.

**Held and present are not mutually exclusive, and nothing can currently say both.** AB-4 records that today's membership is exclusive: an object is in a host. A thread is held by Theseus *and* lying across the floor of the room he is standing in. For a coin in a pocket exclusivity is right; for a spanning thing it is not. **This is the gap underneath the whole case**, and it is why both framings need an explicit step.

**Two more the case supplies cheaply.** **Cutting the thread** is AB-31's fission with the stakes made concrete --- the severed portion no longer connects to the entrance, so the *identity* question ("is this still Ariadne's thread?") has a real answer that matters to a player. And **the ball it unspools from** is a diminishing reservoir generating parts, which is an unusual composition shape the corpus has no other instance of; noted, not developed.

**Annotations.** Both sides --- read-side for retracing and description, write-side for unspooling --- **homogeneous extent** (AB-16) --- Improvisation, on-demand, per crossing --- mechanism --- exercises gap 1, and gap 3 for whether a thread gets a semantic embedding at all.

**Rows grounded.** AB-6 (stored vs derived, at presence level), AB-4 (held vs present), AB-8, AB-9, AB-15, AB-16, AB-26, AB-31.

### C8. The flashlight that vanished when someone looked at it

**Origin:** seed example 2, run 2026-08-06 against [the non-positional-whole candidate](#candidate-drop-clause-1b-and-the-whole-chain-with-it) --- which it broke. The case had been in this plan since day one and the candidate was never checked against it.

**The case.** Someone looks at Room A: a flashlight is there. They look *carefully* at the flashlight, and its parts are improvised --- battery, casing, bulb. **Is the flashlight now absent from the room, replaced by three components?**

**Under the candidate as written, yes, and worse than that.** Decomposition makes the flashlight a *whole*; a non-positional whole is not a graph node; so `graph.objectIds` for Room A holds battery, casing and bulb and no flashlight. Affordance compose describes three components. **And [`roomObjectCatalogForCharacter.ts:76`](../../../../../lambda/ephemera/dataSource/actions/roomObjectCatalogForCharacter.ts) builds the referent catalog from the same `objectIds`**, so "pick up the flashlight" stops grounding. **Looking closely at a thing makes it unreferrable.**

**The conceptual error: decomposition changed positional status while nothing happened fictionally.** Someone looked. The flashlight is still on the table.

**The finding --- containment and extent want *opposite* answers.**

| Composition kind | Positional | Non-positional |
| --- | --- | --- |
| **Containment** (flashlight, place setting) | the **whole** --- it is the thing in the room | the **parts** --- they are inside it |
| **Extent** ([Ariadne's thread](#c7-ariadnes-thread), rope, chain) | the **parts** --- each is somewhere | the **whole** --- it is not anywhere in particular |

This is **AB-16**'s heterogeneous-separable versus homogeneous-extent distinction surfacing as a *positional* difference rather than only a structural one --- evidence that the row's two part-kinds are genuinely distinct things, since they disagree about **which level is the one that is present**.

**What it does to the candidate: narrows it, does not kill it.** The non-positional whole was derived **entirely from C7**, an extent case, and generalised without being checked against a containment case sitting in the same document. Not wrong --- **overgeneralised from one part-kind**, which is the instance-taken-for-the-category error already twice in this plan's catalogue. Positional status looks like a property of the **composition kind** rather than a global rule. Pleasant consequence for clause 4: a containment whole needs no LCA (it sits where it sits) and an extent whole needs no host (it is non-positional), which would confine the LCA machinery to **scattered containment** --- a flashlight whose battery has been carried next door --- possibly rare enough to handle by dissolving the composition instead of hosting it.

**To test before trusting that split: is containment-versus-extent *clean*?** A ship's rigging is arguably containment at the ship's scale and extent within it. If compositions can be both, positional status cannot be read off the kind.

**A second, separable problem the case exposes --- AB-9 and AB-14 collide here.** "Look carefully" acting as a mint trigger puts **description on a write path**, which is exactly what AB-9's guardrail forbids (*description selects among levels that already exist and must never create one*). But AB-14 says operational necessity licenses minting and AB-20 says the player's own reference is the licence --- and a player asking for detail plausibly qualifies. **Both rows are defensible and they disagree about this case.** Note it is genuinely separable: the positional defect above stands even if a screwdriver triggered the decomposition instead of a look.

**Annotations.** Read-side trigger, write-side effect --- **heterogeneous separable** (AB-16) --- Improvisation, on-demand, downward --- description shading into mechanism, which is the collision --- exercises gap 1.

**Rows grounded.** AB-16 (primary --- positional asymmetry), AB-9 and AB-14 (the collision), AB-4, AB-32, AB-2.

### Parks deliberately not mined

The other four parks in [The dependency is asymmetric](#the-dependency-is-asymmetric-representation-unblocks-reasoning)'s right-hand column were considered and excluded, so a later reader does not re-check them: **BD-24** / iteration 6 and **C3** / `MultipleCommands` are *demand*-blocked (no concrete case currently demands either), **BD-18** / backtrack channel is *architecture*-blocked and is a sibling initiative to schedule against rather than absorb, and iteration 2's remaining fallback steps are prompt and calibration work. None would move on a representation change, so none yields a corpus case.

---

## Open decisions (design --- plan only)

Plan-only: decisions being made in order to design this initiative. When one ships, record it in the relevant durable doc ([`positions/AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) for normative rules, [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md) for graduated vocabulary) and remove the row here.

**IDs are stable and never reused**, following the ladder's BD-N convention.

**Requirement rows and representation rows are different, and most rows here are the second kind.** Noticed 2026-08-06 when AB-4 turned out to be conflating both. *What must the model be able to express?* is answerable from the fiction with confidence. *What is the representation?* is not, and depends on the first. A row asking both makes the answerable half unreachable --- so when a row resists, **check whether it is really two rows** before treating it as hard. AB-4 / AB-32 is the worked example; AB-1 is the main pre-existing requirements row. **Splitting a conflated row is not the same as adding scope:** the count rises, the surface does not.

**Rows whose answer is *derived from* a working hypothesis carry an `H1-dependent` marker in their Status cell**, per the tagging test in [Working hypotheses](#working-hypotheses). Merely being *compatible* with H1 earns no marker. The set of markers is the rollback unit if H1 is withdrawn, and its size is a deliberate measurement --- do not apply it defensively.

| ID | Question | Constrained by | Status |
| --- | --- | --- | --- |
| **AB-0** | **Is "Abstraction" the right name, and is it one name or several?** Provisional working term only. If AB-1 splits the concept, this splits with it, and a single name covering three unlike things would be the first place the design goes wrong. **Now has a concrete instance (2026-08-05):** "Improvisation" is being asked to name both the **mechanism** (mint an unauthored component --- the objects lane's, generalized) and one of AB-14's two **triggers**. Resolve as **(a)** Improvisation and Recognition stay the triggers and the mechanism takes its own name, or **(b)** Improvisation names the mechanism and the downward trigger is renamed. See [Improvisation is the objects lane's mechanism, generalized](#improvisation-is-the-objects-lanes-mechanism-generalized). **The original question --- is "Abstraction" the right name --- has a candidate answer as of 2026-08-06: *Abstraction Fractal*.** It arrived from the requirement that abstractions ladder up *and* down, and it earns its place by what it commits to rather than by sounding apt. **A fractal is self-similar across scale: the same relation at every level, no privileged one --- which is AB-12's reframe.** So if abstraction is fractal there is no `Abstraction` **type**, because there is nothing distinctive at any level to type; it is Objects at some scale related by part-of, all the way up and down. The name argues a position on the plan's largest open reframe, and argues it cleanly. Chimes with existing vocabulary too --- [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md#fractal-position-graphs-container-scale-and-edges) already has "Fractal position graphs" --- which is either useful resonance or a collision to check deliberately. **Caution, split into two kinds (see [Clean fractal, dirty implementation](#clean-fractal-dirty-implementation)):** deviations from scale-invariance that are *implementation dirt* are expected and fine; deviations that are *conceptual* are limits on the metaphor and must not be papered over by it. AB-13's DAG is the second kind. **This does not resolve the Improvisation instance**, which remains open. | AB-1, AB-12, AB-14 | **Open --- original question has a candidate; the Improvisation instance is still live** |
| **AB-1** | **One concept or several?** Do composition, functional-state aggregate, and multi-host extent share a structure (one type with facets), share only a membership mechanism (a common substrate, distinct types above it), or share nothing but a motivation? **Root question --- every other row reads differently by its answer.** **Note (2026-08-05):** AB-12's candidate reframe would dissolve the *composition* axis into "Object minting at another level," leaving AB-1 to arbitrate only between extent and state. Do not treat that as settled; it is one live candidate among several. | Phase 0 corpus, AB-12 | **Open --- must resolve first** |
| **AB-2** | **Node, host, or projection?** An abstraction plausibly wants to be all three: referrable as a thing (node), containing members (host), and summarizing for description (projection). Graph roles (hook 4) is the precedent for splitting rather than conflating. | AB-1 | **Open** |
| **AB-3** | **Does `EphemeraPositionGraph` gain a root concept?** If yes, `CarryClosureFragment` collapses into it per its own pinned trigger (hook 1), and `rootId` must remain an input, never derived. If no, composition needs a different carrier and the fragment's note should be updated to say so. | AB-1, AB-2 | **Open --- pre-registered trigger exists** |
| **AB-4** | **What is the level-crossing relation?** Is "part of" a membership relationship with an Object host (activating the deferred `OBJECT#` host, hook 2), a new edge kind, or something that is not an edge at all? Note that today's membership is exclusive, and parts-of-a-whole may or may not want that. **The exclusivity is now known to be a live problem, not a design question --- [C7](#c7-ariadnes-thread), 2026-08-05.** *Held* and *present in the room* are not mutually exclusive for a spanning thing: Ariadne's thread is held by Theseus **and** lying across the floor of the room he is standing in. For a coin in a pocket exclusivity is exactly right; for a thread it is wrong, and **nothing in the model can currently say both.** This is the gap underneath the whole bystander case, and it is why **both** framings need an explicit step --- so it is not an artefact of H1 and will not be resolved by rejecting H1. Decide whether the fix is a second, non-exclusive relation ("occupies", distinct from membership), or whether presence becomes derived (AB-6) and exclusivity survives untouched at the membership layer. **This row's third option --- "something that is not an edge at all" --- turned out on 2026-08-06 to be the highest-leverage choice in the initiative, and the row did not know it.** If composition lives in the **record** (a `Meta` row carrying the member set and the part-connection graph) rather than in position-graph edges, then a whole need not be a graph node, needs no host, and **[H1's clauses 1b, 4 and 5 all disappear together](#candidate-drop-clause-1b-and-the-whole-chain-with-it)** --- taking AB-25 off H1's critical path and dissolving three queued test cases. If composition lives in **edges**, wholes must be graph nodes and the whole chain stands. **The deciding sub-question:** can a whole itself be a part of a bigger whole? If composition nests and is expressed as edges, the record option collapses. **Sequencing consequence: answer this row before running any clause 4 or clause 5 test**, since those target machinery that may not need to exist. **Scope corrected 2026-08-06: this row asks *only* the representation question, and was conflating it with a requirements question now split out as AB-32.** "Is part-of a membership relationship, a new edge kind, or not an edge at all" is entirely about the planned implementation; nowhere did it ask what must be **expressible**. That mattered practically --- the requirements half is answerable with confidence while the representation half is not, and one row asking both made the answerable part unreachable. **Answer AB-32 first.** | AB-1, AB-2, AB-3, AB-6, AB-32 | **Open --- representation only; highest-leverage fork, blocked on AB-32** |
| **AB-5** | **Latent vs. materialized: who mints, and when?** The flashlight's battery does not exist until dismantling; the place setting does not exist until recognized. Is minting an event (improvisation precedent, hook 3), a continuous derivation, or a presentation-time inference that never persists? Is it player-visible? If an LLM can promote a subgraph to an abstraction, that is a **write**. | AB-1, AB-9 | **Open** |
| **AB-6** | **Membership derivation and decay.** Is an abstraction's member set **stored** (an explicit set) or **derived** (a predicate over graph edges)? Someone takes the fork --- does the place setting survive annotated, dissolve, or get recomputed? Trim-to-valid is the structural precedent (hook 5). Stored sets imply a drift-repair story; derived sets imply a cost and stability story. **Pushed toward *stored* 2026-08-05 by [part-mediated reference](#part-mediated-reference-how-h1-answers-its-own-replacement-trigger), on a mechanical rather than a preferential argument:** evaluating an operation over a spanning whole needs every part **and its host** known *before* the walk begins, because `computeStepSequenceFootprint` runs once, up front, and `MultiKeyUpdate` cannot be re-entered. A *derived* member set cannot supply that without already knowing where to look --- the derivation would need the answer it is computing. **What stays open:** the drift-repair story a stored set implies, and whether decay is trim-to-valid (hook 5) or something stronger. **This row is being asked its question twice, possibly with opposite answers --- noticed 2026-08-05 via [C7](#c7-ariadnes-thread).** Part-mediated reference forces **member sets** toward *stored* (the footprint must be known before the walk). Ariadne's thread pulls **presence** --- which rooms a spanning whole occupies --- toward *derived*, since a flat container set cannot supply "the thread now also occupies the room the walker entered" without the same inference either way. Those are different things and the answers may legitimately differ, but the row as written reads as though it has **one** answer. **Split it explicitly in Phase 2** rather than resolving it as a single question and discovering the other half later. | AB-1, AB-4, AB-5 | **Open --- member sets forced toward stored (`H1-dependent`); presence pulls toward derived; row needs splitting** |
| **AB-7** | **Is state the same object as summary?** Example 3's aggregate carries state and gates behavior; examples 1 and 2 carry no state at all. Decide whether state is a facet of one Abstraction type or a separate mechanism that merely *shares* a membership set. Confirm first that no home for diegetic state exists today (hook 8) --- if the slot is genuinely vacant, this row is larger than it looks and may deserve its own initiative. | AB-1 | **Open --- possibly out of scope** |
| **AB-8** | **Multi-host extent.** The rope. **[Proposal P1](#proposal-p1-single-host-objects--hosting-at-the-containing-node) (2026-08-05) proposes resolving this by *removing* multi-host objects entirely** and modelling spanning things as parts with the whole hosted at the containing node --- which would settle this row as framing (c), vindicate the contract clause rather than rewriting it, and move the difficulty into AB-25. Recorded as a proposal, not adopted; the framings below remain live. Three candidate framings: (a) one node with presence in many hosts --- **less cheap than an earlier draft of this row claimed.** Adjacency is natively multi-host, but "nothing enforces single-room today" was wrong: the membership compile gate **terminally errors** on `containers.length > 1` before planning is reached (corrected 2026-08-05 from [C2](#c2-the-rope-in-two-rooms-refused-at-the-gate); see [Multi-host presence](#multi-host-presence-what-is-actually-true-today)). Framing (a)'s real costs are therefore that gate, end-state planning collapsing it, a singular `toHostId` on the transfer step, intra-host-only relations, and per-room affordance compose rendering a whole rope in each room --- **five sites on four routes, each of which decided single-host independently.** That multiplicity is itself evidence about the framing: a design the codebase has re-derived against five times is being pushed uphill. **It strengthens P1 symmetrically** --- a single-host design makes the gate correct as written, exactly as it vindicates rather than rewrites the contract clause --- though P1 remains a proposal, not adopted; (b) the abstraction is itself a manipulation host above Room; (c) per-room segment nodes composed by a **composition** abstraction, which would **collapse AB-8 into AB-4**. Note (a) and (c) differ mainly in *where the seam is drawn*, not in capability --- weigh them on which one a player-facing description and a `pull` operation read more naturally from. **Strongest evidence for (c), 2026-08-05 --- [C7, Ariadne's thread](#c7-ariadnes-thread).** Framing (a) represents a spanning thing as **one node** with presence in many hosts, which means there is nowhere to record *where along it* an attachment sits: three attachments on three hosts, a container set that is unordered, and a route that cannot be recovered from exits because a labyrinth has multiple paths. For an object whose **purpose is the route**, (a) is not merely awkward --- it cannot express the object. **This is the clearest discriminator the corpus has produced against (a), and it is about node-versus-parts rather than about hosting**, so it argues for (c) without by itself settling single-host. **Earlier evidence for (c), same day:** a rope *tied to a rock* puts a relation on one end, not on the whole. Under (a) that edge is ambiguous about which host it lives on, and the kernel derives a relational step's host live and throws on mismatch; under (c) it is cleanly cliff-top-segment-to-rock, same host, no new machinery. ~~**Settled sub-point:** extents spanning hosts of *different kinds* (two adventurers holding opposite ends in different rooms) is **not** a concern --- confirmed 2026-08-05.~~ **Reopened the same day, and the reason is instructive: the words did not change but the question did.** That point was settled while framing (a) was live, where it is genuinely a non-issue --- a rope simply has presence in two hosts, of whatever kinds. **Under H1 it becomes a question about the shape of the containment tree**: the whole must host at an LCA of a room and a character, so characters have to be interior nodes of a structure AB-25 frames purely as Room/Area. Same sentence, different question. **Recorded as a caution beyond this row:** adopting a hypothesis silently re-poses settled questions that were settled against the *previous* model, and nothing flags them --- so the settled notes predating [H1](#h1-single-host-objects-hosting-at-the-containing-node) are worth a deliberate sweep rather than trusted by default. **Also carries a doc-correction obligation:** the contract's at-most-one-room-per-`OBJECT#` clause contradicts the intended design and must be reconciled in Phase 1. | AB-1, AB-2, AB-4, AB-16 | **Open --- provisionally framing (c) under [H1](#h1-single-host-objects-hosting-at-the-containing-node); `H1-dependent`, and the first member of the rollback set.** The row stays Open deliberately: H1 is a hypothesis being tested, not a decision, so (a) and (b) remain live and this row reverts to unframed if H1 is withdrawn. |
| **AB-9** | **Which level gets presented, and is that manipulation truth or a projection?** The stated motivation is LLM reasoning scalability, which is a *read-side* problem: the value is in being handed the right level without seeing all levels. Projections are already filters, not the graph (hook 6). **This row most controls total scope** --- if summary is a projection concern, much of axes 1 and 3 never becomes stored truth. Also: is the chosen level perspectival (a novice sees a place setting, an inspector sees five utensils)? **Guardrail, relocated here from AB-14 on 2026-08-05:** description **selects among levels that already exist and must never create one**. If merely looking at something can mint durable structure, then description-time LLM inference becomes a write path, and this row stops controlling scope at all. **Escalated 2026-08-05 by [C5](#c5-the-chain-mounted-at-both-ends), and the change is one of frequency, not kind.** [P1](#proposal-p1-single-host-objects--hosting-at-the-containing-node) already noted it makes multi-level graphs acute; C5 shows co-located parts persist **permanently** whenever they are load-bearing, so a chain mounted at one end and attached to a chest at the other is three nodes in the cell's graph indefinitely. Multi-level is therefore the **steady state of anything anchored**, not a transient during manipulation --- so level selection is a default read-path concern that every describe must answer, which raises this row's weight without changing its question. **The guardrail now has a case that contests it directly --- [C8](#c8-the-flashlight-that-vanished-when-someone-looked-at-it), 2026-08-06.** "Look *carefully* at the flashlight" mints its interior. That is **description acting as a write path**, which this row forbids outright; but AB-14 holds that operational necessity licenses minting and AB-20 that the player's own reference *is* the licence --- and a player asking for detail plausibly qualifies. **Both rows are defensible and they disagree about this case**, which is the first time the guardrail has been contested by anything other than an argument. Candidate resolutions, none chosen: the guardrail holds and close examination is not description but a *command* that happens to read; or the guardrail admits an exception for player-initiated detail requests, which weakens its scope control; or minting is deferred (AB-18's tier 2) so the look itself creates nothing. | AB-1, AB-14, AB-20 | **Open --- scope-controlling; guardrail contested by C8; `H1-dependent` for the C6 escalation only, not for the question** |
| **AB-10** | **Authored vs. play, and template vs. instance.** "A place setting" as a recognizable *pattern* is plausibly authored (WML); "this table's place setting" is play truth. Nothing in the model has a template/instance split today. Relates to the deferred WML `Relational` / `EdgeList` convergence in [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md#wml-convergence-future) --- authority stays split even after wire types align. | AB-1, AB-6 | **Open** |
| **AB-11** | **Manipulation semantics: operand or new step kind?** "Clear the place setting," "dismantle the flashlight," "pull the rope." Carry closure already computes and transfers *sets*, so an abstraction as a named set may make `transferMembership` work nearly unchanged --- but "pull" is neither a membership nor a relational change. Determines whether the kernel's step vocabulary grows at all. **Candidate answer 2026-08-05, and it is the cheap one: no new step kind.** Under [part-mediated reference](#part-mediated-reference-how-h1-answers-its-own-replacement-trigger), "pull the rope" grounds on a locally-present part, walks up to the whole, and the whole **expands** into per-part steps --- which is exactly Synthesize's **Expansion** sub-role, already growing one ungrounded change into an ordered sequence for carry closure (BD-13) and `sameHost` repair (BD-16). The resulting multi-host mutation also has a shipped shape: `MultiKeyUpdate` folds N effects into one `transactWrite`. **So the kernel does not change; a new expansion is written instead.** Notable that the scheme was not designed to answer this row and answers it anyway --- weak evidence the joint is real. **What stays open:** whether *every* abstraction-level operation decomposes this way, or only propagation-shaped ones like `pull`. | AB-1, AB-4, AB-8 | **Open --- candidate answer recorded (`H1-dependent`); generality untested** |
| **AB-12** | **Is there a privileged level at all --- and are `<Object>` / `<Room>` / `<Feature>` already abstractions?** Candidate reframe (2026-08-05): the battery / flashlight / casing regress has no natural floor or ceiling, so pinning `<Object>` as origin with `<Part>` below and `<Abstraction>` above would build two type hierarchies for one relation pointed in opposite directions. If instead everything is an Object at *some* level, the composition case needs **no new type**, and nothing converts when a part is separated --- only the composition edge changes. **The reframe is clean for Object and much larger for Room and Feature:** Room is a *host*, so treating it as another level unifies hosts and nodes (AB-2's most radical answer, rooms nesting in rooms, Area as a bigger Object). It also makes Feature a part-of-Room, which would *explain* rather than merely assert the documented "Features are never membership-moved" narrowing --- while contradicting that documented decision. **Take Object and Room/Feature as separable bets; do not let the second ride in on the first.** | --- | **Open --- reframes AB-1** |
| **AB-13** | **Partial order, not a ladder.** "Levels of abstraction" invites a scalar, but composition has overlap and multiple parents: the fork is part of the place setting, of the silverware set, and of grandmother's inheritance, none ranked against the table the setting sits on. Designing for "level N" would build an integer where a DAG is needed. The codebase already carries the artifact --- `computeCarryClosure` guards with `closureSet.has(...)` exactly because a node is reachable by more than one path, which is why `rootId` is **recorded, never derived** (hook 1). Decide whether multiple parents are permitted, and if so what that does to closure, transfer, and description. **Worked example added 2026-08-05 --- [C6](#c6-the-rope-the-candle-and-the-impromptu-timer), and it is stronger evidence than the fork.** The fork's three wholes (place setting, silverware set, grandmother's inheritance) are *categories* of the same kind, so a reader can talk themselves into a nesting. C6's two wholes are different **in kind**: the rope is a physical object, the impromptu timer is a mechanism, and neither contains the other. A DAG is not a modelling preference there --- a tree cannot express it at all. **One shape decision can be taken ahead of the question, 2026-08-05:** [part-mediated reference](#part-mediated-reference-how-h1-answers-its-own-replacement-trigger)'s parent index should be **plural from the start**. A part belonging to both a rope and a knotted pair makes "pull the rope" ambiguous --- but the *ambiguity* can be deferred while the *field shape* cannot, since widening a singular field later is the more expensive order and is exactly the mistake `MutationKernelTransferStep`'s singular `toHostId` now represents. | AB-3, AB-12 | **Open --- field shape decided ahead of the question** |
| **AB-14** | **One mechanism, two minting modes --- and the axis is criticality, not timing.** Refined 2026-08-05 (twice; see Discussion record for both supersessions). **The action is improvisation in both cases:** a sure-enough recognition that cups, plates and flatware form a table setting mints a `Table Setting` component of which the others become (at least temporarily) parts --- the same act as minting a north wall. What differs is **whether a pending operation is blocked on the mint.** **On-demand:** "lean the table against the north wall" cannot resolve until the referent grounds, so the mint is synchronous and on the command's critical path. **Recognition:** a loop of rope laid on a road may be understood as a snare immediately, thirty seconds later, or never, without materially harming responsiveness. Synchrony is a *consequence* of criticality, not the axis itself --- which matters because it predicts the three rows below. **Same persistence layer** (`ASSET#IMPROVISATION` is a merge layer, so provenance is invisible downstream), **potentially very different decision processes**, which sit entirely upstream of persistence. **The narration obligations also differ:** on-demand narration *is* the minting event (the parts come into existence for the player by being described); recognition's narration is a **contract with the player** --- how they learn the world accepted their construction as meaningful --- so it can never be a silent internal summarization pass. **Collides with AB-9's guardrail on a concrete case as of 2026-08-06 --- [C8](#c8-the-flashlight-that-vanished-when-someone-looked-at-it).** "Look carefully at the flashlight" is on this row's account an on-demand mint (the player asked for detail, so detail is licensed) and on AB-9's account forbidden (description must never create a level). **Resolve the two rows together rather than separately**, since each is individually defensible and they give opposite answers. See AB-9 for the candidate resolutions. | AB-5, AB-9, AB-16, AB-17, AB-20 | **Open --- shape proposed; collides with AB-9 on C8** |
| **AB-15** | **What does authoring privilege, if not ontology?** If no level is ontologically primary, an authored `<Object>` is plausibly the level the author asserts is worth having **by default** --- the level unqualified reference binds to, revisable by circumstance. ("Barring specific instructions, you pull out the whole coil.") That would give authoring a real role that survives AB-12's reframe, and answers part of AB-10's template/instance question without a template/instance split. Decide whether default-binding is the whole of what authorship contributes, or whether authored levels are also privileged for persistence, description, or legality. **A concrete instance arrived 2026-08-05 --- the naming seam, and it is the one step of [part-mediated reference](#part-mediated-reference-how-h1-answers-its-own-replacement-trigger) that does not fall out of existing machinery.** The referent catalog matches on `normalizedShortName`. A player says "pull the **rope**"; what is present in the room is a part. If the part's short name is "rope end," the whole's name is **not in the catalog at all** and grounding has nothing to bind to. So under H1 the authored level --- exactly the level this row proposes unqualified reference should bind to --- is precisely the thing that is *not locally present*. Three candidates, none chosen: **(a)** parts alias the whole's short name; **(b)** a two-phase match consults the parent index after a local miss; **(c)** the catalog widens to include reachable wholes. ~~Note (c) reintroduces the non-present things in the catalog that part-mediated reference exists to avoid.~~ **Corrected 2026-08-05 by [C7](#c7-ariadnes-thread): (c) is better than that dismissal allowed, and it needs splitting rather than rejecting.** A thread passing through a labyrinth room **is** present there --- physically, describably, trippable-over. What part-mediated reference exists to avoid is surfacing Area-hosted **bookkeeping** wholes in rooms they do not touch. Those are different sets. So (c) resolves into two candidates: widen the catalog to wholes that **pass through this room** (defensible, and required if presence is derived rather than stored), versus widen it to **all reachable** wholes (which is the version that reintroduces the problem). | AB-10, AB-12 | **Open --- concrete instance live (`H1-dependent`)** |
| **AB-16** | **Are all "parts" alike?** Two apparently distinct kinds surfaced 2026-08-05. **Heterogeneous separable parts** (flashlight -> battery, bulb): distinct nameable things, no required ordering or adjacency, able to go their separate ways permanently. **Homogeneous extent** (rope, chain, cable, possibly river or wall): "parts" are slices of a continuum, identified by *where* they are rather than *what* they are, and inseparable without an act that changes the whole (cutting a rope makes two ropes). **Correction 2026-08-05:** an earlier version of this row said such parts are "ordered along a path," and that is wrong --- see **AB-26**. The structure is an adjacency-mediated **graph**, which branches; the *distinction* between the two part kinds survives the correction, only the shape claim does not. Decide whether extent is a composition special case or a genuinely distinct third thing. **Strong evidence for *distinct*, 2026-08-06 from [C8](#c8-the-flashlight-that-vanished-when-someone-looked-at-it): the two kinds disagree about which level is *positional*.** Containment puts the **whole** in the room and its parts inside it (a flashlight is the thing present; its battery is not separately in the room). Extent puts the **parts** in rooms and leaves the whole nowhere in particular ([Ariadne's thread](#c7-ariadnes-thread) is not *in* any room). That is not a difference of degree or of convenience --- **it is a disagreement about what is present**, which is the most load-bearing property the position model has. **Consequence:** positional status may be a property of the composition *kind* rather than a global rule, which is what narrows [the non-positional-whole candidate](#candidate-drop-clause-1b-and-the-whole-chain-with-it) to extent. **The premise to test before relying on it:** whether containment and extent are cleanly separable at all. A ship's rigging is arguably containment at the ship's scale and extent within it; if a composition can be both, positional status cannot be read off the kind and this row's answer gets harder rather than easier. | AB-1, AB-8, AB-26 | **Open --- evidence for "distinct"; separability untested** |
| **AB-17** | **Must the transition run backwards, and how far?** Haul the rope up, coil it, stow it: it must re-collapse to one object. The flashlight hides this (nobody un-dismantles often enough to notice), so designing from that example alone would produce a one-way ratchet accumulating orphaned parts. **The rope is therefore the better design driver for AB-5/AB-6 than the flashlight.** Decide the reabsorption criterion (candidate: no part is separately referenced or separately positioned), whether reabsorption is lossy, and whether a part that acquired its own history or relations can be reabsorbed at all. **Split into two criteria 2026-08-05 by [C5](#c5-the-chain-mounted-at-both-ends), and note this row's disjunction was right all along --- it was [P1](#proposal-p1-single-host-objects--hosting-at-the-containing-node) that collapsed it.** The two conditions are independent, so **migration-down and reabsorption are different events** and neither implies the other. **(i) Hosting** keys on *separately positioned* only: the whole hosts at the node containing all its parts, and migrating down happens the moment they co-locate. **(ii) Reabsorption** requires *both* conditions false --- and the **relational half of "separately referenced" is already enforced by shipped code**: `removeObject` throws `RelationalEdgeStillReferencedError` on a node a Relational edge still references (BD-33/35, no silent-strip variant), so the check is a graph query via `nodeHasRelationalEdge`, not a judgment, and the unsafe reabsorption is already impossible. **What remains genuinely open** is the non-relational half --- what "separately referenced" means for a part carrying no edge but recently named by a player, or one that acquired its own history --- plus whether reabsorption is lossy. **And a third gap found 2026-08-05 by [C6](#c6-the-rope-the-candle-and-the-impromptu-timer): dissolution is *per-whole*, not per-part.** This row currently reads as though a part is reabsorbed or it is not. With multiple parents (AB-13) that is not expressible: the impromptu timer dies when the candle burns out while the rope persists, so the shared span is **released by one parent and retained by another**. The criterion therefore has to be evaluated per (part, whole) pair rather than per part, and "reabsorb the whole" has to mean "release my claim on these parts," not "delete them." | AB-5, AB-6, AB-16 | **Open --- hosting criterion settled under [H1](#h1-single-host-objects-hosting-at-the-containing-node) (`H1-dependent`); reabsorption criterion open, but its relational half is discharged by shipped machinery rather than by this initiative** |
| **AB-18** | **Recognition's scope, trigger, and convergence.** The two modes in AB-14 are **not** symmetric in difficulty. **On-demand minting is bounded by its operation** --- a screwdriver applied to a flashlight, or a referent that must ground, scopes exactly what to mint. **Recognition is an unbounded search**: noticing that several operations, possibly separated in time, have composed into a thing. Decide the scope (one host? a room? a carry closure?), the trigger (evaluated on each state *transition*, or periodically over *state* --- note an authored room whose table already bears six utensils has no transition to fire on), and the cost/latency policy when an LLM is in the loop. **Convergence requirement, found 2026-08-05:** the mode is a property of *the moment, not the thing*. "Check the snare" puts recognition on the critical path, so the same mint is reachable from a **blocking referent-resolution path and a deferred background path**, and the two **must produce the same component** --- otherwise a snare noticed one way differs from the snare noticed the other, or both fire and the world gets two. Ties directly to AB-21's identity obligation. Failure-cost asymmetry moved to AB-20. | AB-14, AB-19, AB-20, AB-21 | **Open** |
| **AB-19** | **Recognition for description, or for mechanism --- and what must an abstraction carry to bound an LLM step?** Reframed 2026-08-05; an earlier version of this row proposed that recognition-for-mechanism be tractable via an **authored pattern vocabulary with behavior attached**, and that is **rejected**: it is the compile-fictional-physics-into-deterministic-structure move, which is both the wrong tool and not what the fiction is about. **Settled direction:** most player-assembled contraptions will require an **LLM step** to reason about consequences. The abstraction's job is to make that step **bounded and manageable** --- to be the right-sized packet of context --- **not** to replace it with a lookup. **Description vs. mechanism therefore separates by stakes, not by machinery:** description-recognition is a nice-to-have first pass with no causally complicated consequences (a place setting, Jane Austen aside) and stays pure projection under AB-9's guardrail; **mechanism-recognition is essential** --- reasoning about a snare *as* a snare, in the abstract, is the whole point, and deriving its result from first principles every time is a fool's errand. **The real question this row now asks:** what must a recognized abstraction carry so that an LLM step over it is bounded? Prior art and its limits: [Coyote as prior art](#coyote-as-prior-art-bounded-llm-reasoning-that-is-not-bounded-structurally). | AB-7, AB-9, AB-10, AB-14, AB-15, AB-22 | **Open --- direction settled, shape open** |
| **AB-20** | **Failure semantics and authorization: the two consequences of AB-14's criticality axis.** **Failure:** an on-demand mint is a **correctness obligation** --- if it fails the command fails and the player is owed an error; recognition is **discretionary world enrichment** --- if it fails nothing happens and nobody is owed anything. Both disciplines already exist side by side in the objects lane (blocking-and-compensated existence+placement under **S1**, versus best-effort non-blocking embeddings and enrich prose), so this is a choice between shipped patterns, not new machinery. **Authorization:** on-demand minting needs **no confidence gate at all** --- the player's reference *licenses* the existence; they said "the north wall," so there is one. Recognition is **unlicensed**: the system volunteers an unprompted claim about what the player built. **This is why AB-18's false-positive cost lands only on recognition** --- the asymmetry follows from who authorized the mint, and should not be re-derived as an arbitrary policy choice. | AB-14, AB-18 | **Open** |
| **AB-21** | **Latent vs. invented, and the identity obligation.** The north wall may not be *invented* at all --- a room has walls whether or not anyone wrote them down, as the flashlight always had a battery. That is different in kind from conjuring something genuinely new, and it carries an obligation recognition largely does not: **identity**. Two commands referencing "the north wall" in one room must get the **same** wall, and so must a re-reference after a Lambda cold start; otherwise repeated reference silently multiplies the world. `stableKey` on `Meta::Object` exists for exactly this correlation problem (Coyote) and is the obvious hook. Decide whether latency-vs-invention is a real distinction worth modelling or only a narration nuance, and where the idempotency key comes from when the thing was never authored. | AB-14, AB-15, AB-20 | **Open** |
| **AB-22** | **When is affordance annotation computed, and in what vocabulary?** Coyote's answer, and the part that generalizes: **at mint time, once** --- `tropeAffinities` are written by the Acme enrich call and live durably on `Meta::Object`, so the prediction pipeline consumes structure rather than re-deriving it. That directly answers AB-18's cost worry. **(i) Timing:** annotate at mint, lazily on first reasoning need, or continuously --- **and see AB-23, which shows mint-time can only be a partial answer.** **(ii) Vocabulary:** Coyote's structural roles (`terminal`, `trigger`, `delivery`, `autonomous_agent`, `prep`, `creation`) and six tropes are genre-specific; `EnvironmentAffordanceObject` literally enumerates `boulder`, `cactus`. Keep the closed-structural-vocabulary / open-narrative-content split (`gimmick` is deliberately free text) and graded `aptness` over booleans. **Bonus mechanism:** member annotations are a cheap **filter** for whether an arrangement is worth recognizing at all --- fork + plate + cup carry no structural roles and are causally inert; a rope with `trigger` aptness beside a branch with `delivery` is worth a look. That attacks AB-18's unbounded search **without** an authored pattern vocabulary. **Caveat from AB-23: the filter is itself context-dependent** --- at the Faerie Court the place setting stops being inert. | AB-14, AB-18, AB-19, AB-23 | **Open** |
| **AB-23** | **Relevant meaning is situational, not a fixed world vocabulary.** Reframed 2026-08-05, superseding AB-22's implicit "is the vocabulary general or authored per world?" --- a **static** question with no good answer. The real question is **"what kind of meaning is relevant *right now*?"** Coyote may shortcut this (as a first iteration deserves) because its genre supplies a constant, unvarying interpretive context. A general world cannot: ambushed by goblins, meaning is relative to combat; dining at the Faerie Court, whether the place setting has causal complexity becomes very real --- **same world, same objects, different relevant meanings.** Consequences: **(a)** mint-time annotation can only capture *durable* properties, so situational significance must be layered on top --- annotation is plausibly **base + overlay**, not one pass; **(b)** an existing axis already models exactly this shape one layer down --- the render cache keys records on `n` (mark state, `Array<{mark, value}>`) plus perspective and carries `situationId`, documented as "one cache row per distinct render (component + n + perspective)." **Premise corrected 2026-08-05 from [C4](#c4-the-place-setting-at-the-faerie-court):** this row previously said the axis is "built and unpopulated," and that is wrong in a way that matters. It is **populated for Rooms** (`Meta::Room.state.marks`, merged by `handleApiStateChange`, defaulted by `computeDefaultMarksForRoom`) and **structurally absent for every other host kind** --- `requestIntake` pins Feature / Knowledge / Object / Character to `{ markValue: [] }` and `allowGeneration: false` **because they have no `Meta::Room` to carry state**, and `findRender` gates generation on `isEphemeraRoomId`. So the pin is not a switch awaiting a flip; it is downstream of a missing carrier. **Consequence: situational meaning below room scale needs a diegetic-state carrier for non-Room hosts, which is AB-7** --- flagged there as possibly out of scope, and now with a second consumer. This question remains that axis raised from *how a thing renders* to *what it means*. | AB-7, AB-15, AB-19, AB-22, AB-24 | **Open** |
| **AB-24** | **A third scheduling tier: ambient context-shift detection.** Proposed 2026-08-05. Beyond blocking on-demand minting and deferred-but-promotable recognition, a **low-immediacy scheduled thinking task** would periodically ask: *has the context or vocabulary for this room / these players shifted such that we should be watching for different kinds of meaning?* **It parameterizes recognition rather than performing it** --- tier 3 sets what tier 2 looks for. Substrate: [`thinking` as the scheduling substrate](#thinking-as-the-scheduling-substrate-shipped-ledger-unbuilt-queue). **Two design constraints worth stating up front. (i) Its failure mode differs in kind:** a missed snare is one missed snare, local and recoverable, while a stale interpretive frame makes tier 2 hunt for the wrong meanings *systematically* until it corrects --- silent and pervasive, which argues for cheap-and-frequent over expensive-and-rare. **(ii) The author already declares most of the frame:** a Faerie Court room is *authored* as a Faerie Court, so this task's job is detecting **deviation** (goblins ambush a room authored as a social space) rather than deriving a frame from nothing --- much cheaper and more verifiable. That second point is AB-15 again; see the note under [Constraint matrix](#constraint-matrix). | AB-15, AB-18, AB-23 | **Open** |
| **AB-25** | **Is the host containment tree actually a tree?** **Note (2026-08-05): AB-26 shows this row shares a substrate with part-connection licensing.** A **nesting** relation does double duty --- boat-in-bay both places the boat in the containment tree *and* licenses a connection across the boundary (over the rail). An **Exit** edge licenses a connection without implying containment either way. So the two rows overlap without being identical, and a nesting representation should be designed for both consumers at once rather than twice. Split out of [Proposal P1](#proposal-p1-single-host-objects--hosting-at-the-containing-node) 2026-08-05 as its load-bearing unknown, and **worth answering even if P1 is rejected** --- any scheme that hosts a whole "above" its parts needs it. Area membership is authored blueprint merged at participation order, so containment may be **asset-stack dependent**, while an object's host must be world truth rather than perspective-relative. Sub-questions: can one Room belong to two Areas (canon plus an overlay)? Do Areas nest, and how deep (`AREA#region` beside `AREA#downtown` suggests yes)? What is the root when two rooms share no Area? And since containment is authored **downward** (`StandardAreaData.positionGraph` lists members) with **no** room -> area index anywhere, where does the upward lookup live, given it must run on every part move? **A constraint arrived 2026-08-05 from [C6](#c6-the-rope-the-candle-and-the-impromptu-timer), and it forbids the shortcut this row is most likely to reach for: hosting height does not correlate with composition depth.** In C6 one part has two wholes hosted at **different heights** --- the rope spans room and garden so it hosts at the Area, while the impromptu timer's members are both in the room so it hosts in the room. So walking up from a part yields parents at inconsistent levels, and **no implementation may treat the parent index as "the next level up"** or index hosting by depth. This is AB-13's ladder-versus-DAG error one layer over, in the place it would look most tempting: *hosting* feels like it ought to be stratified, and it is not. Note the case does **not** bear on whether Room/Area containment is a tree --- that question is untouched and remains this row's core. **A second constraint the same day, and this one widens the row rather than restricting it: the tree is not Room/Area at all.** `EphemeraMembershipHostId` is `Room | Character`, so characters are hosts, and a rope with one end held by a walker needs an LCA of a room and a character. **Characters must therefore be interior nodes** --- room contains character contains held object --- which this row's framing (Areas, nesting, participation-order merge) does not contemplate. Two consequences: the "is it a tree" question now spans a structure with **two node kinds of different provenance** (authored Area membership versus play-time character presence), and character movement --- the highest-frequency mutation in the system --- becomes a **containment-tree mutation**, so every whole with a part held by the walker re-hosts. Reopens the pre-H1 settled note on AB-8. | AB-4, AB-8, AB-13 | **Open --- P1's critical path, and [H1](#h1-single-host-objects-hosting-at-the-containing-node)'s only live falsification trigger** |
| **AB-26** | **How do parts on different hosts connect? (Adjacency-mediated, not sequenced.)** Corrected 2026-08-05, superseding this plan's earlier claim that extent parts are "ordered along a path." **A sequence is the wrong shape** --- it looks right only because a rope is linear, and a net spanning four rooms, wiring from a generator to a powered door, or two ropes knotted together all branch. This is the same error as AB-13's ladder-vs-DAG, one layer down: a scalar imposed where a graph belongs. **The right shape is a graph of parts whose edges are labeled by the spatial relation licensing them** --- "the clifftop rope end connects to the shaft section *by way of the 'into the abyss' exit*." Four properties follow: branching is free; the edge *explains* the adjacency rather than asserting it; narration gets direction and wording from the exit (as the shipped move compiler already does with `exitName` as an ingredient) rather than from an index; and `pull` becomes propagation over a graph instead of an index walk. **Already scaffolded:** [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md#fractal-position-graphs-container-scale-and-edges) notes Area edge kinds beyond Exit "may express **non-traversable** spatial facts (e.g. 'north of' without a door)" --- exactly what a rope through a hole in the floor needs. **Two rules to pin: (i)** connections are **explicit but constrained** --- adjacency *licenses* an edge, it does not derive one, since two ropes in adjacent rooms are not connected; **(ii)** an edge **must** reference **world truth** and **never** the perspective-filtered output of `projectRoomExits`, or a rope's physical shape would vary by who is looking at it --- the authored-vs-projected line the graph-roles table already draws, in a new place. **Correction 2026-08-05:** rule (ii) first read "the *authored Exit* edge," which collapsed two independent axes into the one real constraint. **The licensing relation may be of any kind and any provenance:** *kind* --- an Exit edge (a hard-coded connection affordance), or an adjacency/nesting relation (someone goes over the rail of the boat and ends up in the bay); *provenance* --- authored **or improvised**, since an improvised relation is world truth the moment it is minted (`ASSET#IMPROVISATION` is a merge layer, not a flag). Only *projected* is excluded. | AB-8, AB-11, AB-16, AB-25, AB-28 | **Open --- shape settled, rules proposed** |
| **AB-27** | **Composition edges look host-relative, and only one half is new.** Observed 2026-08-05 while resolving AB-26. **Parts sharing a host** (a flashlight's battery, bulb, casing) can relate through the **existing** intra-host relational vocabulary --- shipped and working. **Parts on different hosts** (rope segments) cannot, because `applyStepSequenceCore` derives a relational step's shared host and **throws** on mismatch; those need AB-26's adjacency-mediated connection. So the genuinely new edge kind is required **exactly and only where the current model already refuses to go**, which suggests a real joint rather than a parallel mechanism. Decide whether these are two edge kinds or one kind with two validity rules, and what happens at the boundary --- when a co-located assembly is pulled apart across hosts, does a same-host relational edge **become** an adjacency-mediated connection, and is that a migration or a dissolve-and-recreate? | AB-4, AB-16, AB-26 | **Open** |
| **AB-28** | **Licensing relations are mutable --- and this is the *shipped* boundary-edge discipline, not a new problem.** Found 2026-08-05 as a consequence of AB-26 admitting improvised adjacency: repair a floor-hole and the relation licensing a rope threaded through it **disappears**. **Recognized the same day as an instance of an existing pattern** --- *a mutation that would sever a relationship is not legal until the severing is made explicit.* `removeObject`/`removeCharacter` assert-and-throw on residual relational edges (`RelationalEdgeStillReferencedError`, BD-33/35) with deliberately **no** silent-strip variant; [`boundaryEdgeOutcomes`](../../../../../lambda/ephemera/dataSource/positions/positionGraph/expandValidate/interactionUnderTransfer.ts) classifies; expansion classifies and the compiler sequences `dissolveRelation` steps **ahead of** the mutation. **The three candidate outcomes are the existing verdict channels:** "you cannot seal it" is `error`, "sealing severs the rope" is `dissolve`, "can you seal a hole with a rope through it?" is `defer`. **Carries over unchanged:** assert-and-throw over silent strip; classify-before-legal; commit-time re-validation on locked graphs; severance narrated in step order via `factsForStep`. **Three real differences (below) and one bifurcation (AB-29).** | AB-17, AB-26, AB-27, AB-29 | **Open --- pattern identified, extensions open** |
| **AB-29** | **Extending the boundary-edge discipline to licensing relations: three gaps and a bifurcation.** **(1) Discovery runs backwards.** Today: remove a *node*, find edges incident to it --- local, on a graph already held. Here: remove a *relation*, find the part-connections referencing it. That is a reverse lookup from a relation to its dependents, and **nothing indexes it**. It is the parked reverse-edge-reference index on its **third** appearance in this initiative, which makes it the most load-bearing parked item so far. **(2) The footprint reaches a third host.** Sealing a hole in Room A must lock Room A, Room B (the rope's far end), *and* the Area carrying the license --- all discoverable at plan time, since `computeStepSequenceFootprint` runs once and cannot be re-entered. **(3) The mutation may not route through the positions kernel at all** --- "repair the hole" is not a `transferMembership`. **The bifurcation, and it is the important part:** **play-driven** license removal has an operation to block or transform, so the shipped discipline applies directly and the conflict is **content**; **asset-withdrawn** license (an overlay deactivates and the adjacency it authored vanishes) has **no operation to make illegal** --- nobody did anything --- and needs the **eviction ladder's** trim-and-repair posture instead, one layer over. Only the first half is the existing pattern. | AB-26, AB-28 | **Open** |
| **AB-30** | **Addressable but not a member: what makes something a graph participant at all?** Opened 2026-08-05 out of a retired falsification case (see [Working hypotheses](#working-hypotheses)). A wall is not *in* a room, it **bounds** rooms --- so asking which host contains it is a category error, and H1's "host at the node containing all its parts" has no purchase because containment is not the relation. This is why a wall cannot migrate down; **not** because it is born spanning. **Two corpus cases need it addressable, from opposite directions, within one session:** [C1](#c1-look-at-the-character-standing-here-look-at-the-north-wall) needs the north wall *referrable* ("look at the north wall") and [C5](#c5-the-chain-mounted-at-both-ends) needs it *relation-targetable* (the chain's more interesting end mounts to it, and is currently unrepresentable). Neither needs it to be a member of anything. **That is a fourth answer AB-2's node / host / projection trichotomy does not offer**, and it is independent of AB-12: whichever way the privileged-level reframe goes, the criterion for what belongs on the graph still needs one. **Candidate answer already scaffolded:** [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md#fractal-position-graphs-container-scale-and-edges) notes Area edge kinds beyond Exit may carry **non-traversable spatial facts**, so a wall may be better modelled as an **edge** than a node --- which would *explain* the documented Feature narrowing (never membership-moved, never a relation subject, possibly a relation target) rather than asserting it. **The discriminator, and it is the sharpest test available: a wall separating two rooms.** Under the **edge** answer, spanning is free --- an edge between two rooms is inherently about both, and nothing needs a host. Under the **node** answer, the wall must exist in two graphs, which is exactly the multi-host problem H1 abolished for Objects, now needing per-graph duplication and a sync story --- and it would make [P1](#proposal-p1-single-host-objects--hosting-at-the-containing-node)'s "Features stay multi-host" sub-proposal an unremarked asymmetry rather than a decision. **No separate row for spanning Features:** the spanning case is this row's test, not its own question. | AB-2, AB-12, AB-26 | **Open --- candidate answer recorded, discriminator named** |
| **AB-31** | **Fission: one whole becomes two, and no row owns it.** Opened 2026-08-05 from [C6](#c6-the-rope-the-candle-and-the-impromptu-timer), whose entire payoff is the rope **burning through**. The lifecycle cluster has minting (AB-5), reabsorption (AB-17) and decay (AB-6); AB-16 remarks in passing that "cutting a rope makes two ropes" but treats it as a property of homogeneous extent rather than as an operation. **Fission is neither minting nor the inverse of reabsorption** --- it is sideways, and under [H1](#h1-single-host-objects-hosting-at-the-containing-node) with stored member sets and parent indices it is a concrete multi-step write: mint a second whole, **repartition** the member set, rewrite the parent pointers of the moved parts, and recompute **both** hosts (which may migrate in opposite directions --- in C6 the severed lower length falls into the garden and becomes single-room, while the upper remnant becomes single-room too, so *both* halves migrate *down* out of the Area as a consequence of one event). **Questions this row owns:** does fission mint one new whole or two and retire the original --- and does the answer matter for identity (AB-21) and for anything holding a reference? What happens to a **third** whole whose member set straddles the cut, as C6's impromptu timer does? Is fission always player-caused, or can decay produce it? Does the severed pair retain any relation recording that they were once one thing? **Note the footprint problem is acute here:** both resulting hosts, plus every host of every affected co-parent, must be known at plan time, and `computeStepSequenceFootprint` runs once. | AB-5, AB-6, AB-13, AB-16, AB-17, AB-21 | **Open --- newly named, unexplored** |
| **AB-32** | **What must the composition representation be able to *express*?** Split out of AB-4 on 2026-08-06, which was asking only what the representation *is*. **This is not a new question --- it is the answerable half of an existing one**, and separating them matters practically: requirements can be settled from the fiction with confidence, representation cannot, and one row asking both left the confident half unreachable. **First requirement, answered the same day: yes, a whole can be part of a bigger whole.** Abstractions must ladder up *and* down --- resoundingly, not marginally --- which is the requirement behind AB-0's *Abstraction Fractal* naming. **Note what that does and does not settle.** It does **not** force composition into graph edges: a record-based whole holds a member set of ids, an id can be another whole's, parent index points up and member set points down, so nesting is free and [the non-positional-whole candidate](#candidate-drop-clause-1b-and-the-whole-chain-with-it) survives it intact. An earlier framing of that candidate named wholes-nest as its decider; **that framing made this very conflation** --- nesting is the requirement, edges-versus-record is the representation. **The actual decider, restated as a requirement: must a relation ever attach to a *whole* rather than to one of its parts?** Relations need nodes, so a yes forces wholes to be nodes. Every case so far says no --- [C5](#c5-the-chain-mounted-at-both-ends)'s chain relates through its ends, [C6](#c6-the-rope-the-candle-and-the-impromptu-timer)'s candle sits under a span, and that is the third decomposition trigger doing exactly this work --- but **"the place setting is on the table" is the shape that might not decompose**, and it should be tested rather than assumed. **Second requirement, answered 2026-08-06 and consequential: reference and perception must reach a spanning whole and a local whole by the *same mechanism*.** "The thread in room B" and "the flashlight on the table" must not resolve by different machinery, or the difference propagates into every consumer. **This is the sharpest pressure clause 1b has taken**, and it produced [three positions with their prices](#three-positions-on-reference-and-what-clause-1b-actually-costs) plus a narrowed dichotomy --- *single-hosting or lookup-as-the-clean-rule, not both* --- and the finding that **the plan is currently in the losing position by accident.** Note the shape of the win: an argument from requirements settled more than four constructed scenarios did, which is the case for this row existing. **Other requirements to collect here:** must a whole be describable without enumerating its parts; must a part be manipulable without loading its whole; must the representation record *where along* an extent something attaches (yes --- C7); must a relation ever attach to a whole rather than a part (above). | AB-1, AB-4, AB-13 | **Open --- two requirements answered, collecting the rest** |

### Constraint matrix

The discipline this plan exists to enforce: **these do not close independently.** A row's answer is only provisionally valid until every row it constrains has been re-checked against it.

```text
   AB-12  (is there a privileged level at all?)  ---- may dissolve ---->  AB-4
      |                                                                  AB-2
      v
   AB-1   (root: one concept or several?)
      |
      +-----------------+------------------+
      |                 |                  |
 STRUCTURE          LIFECYCLE          CONSUMPTION
 AB-2  role         AB-5  minting      AB-9   level / projection
 AB-3  root         AB-6  decay        AB-11  operand vs step
 AB-4  level-edge   AB-10 authored     AB-7   state
 AB-8  extent       AB-14 minting      AB-15  default binding
 AB-13 DAG shape          modes        AB-19  describe vs mechanism
 AB-16 part kinds   AB-17 reabsorption AB-22  annotation
                    AB-18 recog. scope        vocab / timing
                    AB-20 failure/auth AB-23  situational
                    AB-21 identity            meaning
                    AB-24 context tier
```

**Three scheduling tiers (AB-14, AB-18, AB-24), each parameterizing the one above it:**

| Tier | Immediacy | Job |
| --- | --- | --- |
| **On-demand mint** | Blocking --- a pending operation cannot proceed | The referent must ground *now* |
| **Recognition** | Deferred, but promotable on demand | Did an arrangement compose into something? |
| **Context shift** | Low, ambient | Should we be watching for *different kinds* of meaning? |

**AB-14 is the lifecycle cluster's spine.** One mechanism --- improvisation --- invoked in two modes divided by **criticality**: on-demand (a pending operation is blocked) and recognition (nothing is blocked). AB-20 draws the two consequences of that axis (failure semantics, authorization), AB-21 the identity obligation it implies, AB-18 recognition's scope and the convergence of its two invocation paths, AB-17 the inverse operation, and AB-19 whether recognition is even a mint or merely AB-9 by another name.

Cross-cluster couplings that make a one-thread-at-a-time approach fail:

| Coupling | Why it bites |
| --- | --- |
| **AB-9 -> AB-5, AB-6** | If summary is a projection, minting and decay largely evaporate --- there is nothing stored to mint or to let rot. Answering AB-5 first would design machinery AB-9 may delete. |
| **AB-6 -> AB-3, AB-4** | A *derived* member set needs no root and no stored level-crossing edge; a *stored* one needs both, plus a drift-repair path. The structure cluster cannot close before this. |
| **AB-8 -> AB-4** | Framing (c) makes extent a composition case outright. Since (a) is also cheaper than first assumed, AB-8's cost is dominated by AB-4's answer rather than by any storage obstacle --- deciding AB-4 without testing (c) risks building host-spanning machinery for nothing. |
| **AB-2 -> AB-11** | If an abstraction is a node, it is already an operand and the executor may need no new vocabulary. If it is only a host or only a projection, "pull the rope" has nothing to bind to. |
| **AB-7 -> AB-1** | If state turns out to belong to a different lane entirely, example 3 leaves this initiative and AB-1's answer changes shape --- possibly from three axes to two. |
| **AB-10 -> AB-6** | An authored pattern definition is exactly what makes a *derived* member set cheap (match the pattern) instead of expensive (recognize ad hoc). |
| **AB-12 -> AB-1, AB-2, AB-4** | If no level is privileged, composition needs no new type and AB-4 becomes "what edge relates an Object to an Object at another level," not "what is a level-crossing relation." AB-1 then arbitrates only extent vs. state. Answering the structure cluster before AB-12 risks designing a type the reframe deletes. |
| **AB-14 -> AB-9** | Both minting operations are write-side, so AB-9 keeps its scope control **only if** its no-creation guardrail holds. AB-19 is where that boundary is actually contested: recognition-for-description may be indistinguishable from a projection. |
| **AB-19 -> AB-22 -> AB-18** | Superseded chain (2026-08-05). The authored-pattern-vocabulary shortcut is rejected, so AB-18's search is **not** bounded by a pattern library. It is bounded instead by AB-22's annotation vocabulary: if members carry structural-role affinities computed at mint time, an inert arrangement is cheap to skip and a causally loaded one is cheap to notice. **Design AB-22 before AB-18's cost policy** --- annotation timing determines whether the search is expensive at all. |
| **AB-14 -> AB-16, AB-17** | The minting modes cut *across* direction: on-demand minting usually runs downward and recognition upward, but neither is required to, and AB-16/AB-17 describe the same territory from the structural side. If direction and criticality turn out to be independent axes, these rows stay separate; if they co-vary, several collapse. |
| **AB-20 -> AB-18, AB-19** | Authorization is *why* only recognition needs a confidence gate. Designing AB-18's trust story without that grounding invites re-deriving it as arbitrary policy --- or, worse, imposing a symmetric confidence requirement on on-demand minting, which the player's own reference already licenses. |
| **AB-21 -> AB-18** | The convergence requirement (one mint, two invocation paths) and the identity obligation (one wall, many references) are the same problem asked from two directions. A `stableKey`-shaped answer to one is probably the answer to both --- **and `Meta::Result`'s `nonCollidingPutItem` first-write-wins already supplies the mechanism**, see the `thinking` section. |
| **AB-23 -> AB-22, AB-19** | Situational meaning makes mint-time annotation a partial answer at best, and makes the member-affinity *filter* context-dependent rather than fixed. Settling AB-22's timing and vocabulary before AB-23 would design a static annotation scheme the situational answer immediately invalidates. |
| **AB-24 -> AB-18** | Tier 3 parameterizes tier 2: what recognition looks for is set by the current interpretive frame. AB-18's scope and trigger cannot be specified against an unspecified vocabulary. |

**A pattern worth promoting rather than treating as three coincidences.** AB-15 has now surfaced three times in the same shape --- **authoring supplies a default, play revises it**: the default *binding level* for unqualified reference (AB-15), the default *pattern vocabulary* for what can be recognized (AB-19), and the default *interpretive frame* a context-shift task detects deviation from (AB-24). If that generalizes, it is a single principle rather than three separate authoring questions, and it makes each of the three cheaper: the expensive open-ended inference becomes deviation-detection against an authored baseline.
| **AB-16 -> AB-8, AB-11** | Ordered extent and unordered parts need different carriers and different operations. `pull` depends on the ordering; `clear the place setting` does not. One of these may be a set; the other cannot be. |
| **AB-17 -> AB-5** | A minting design with no reabsorption criterion is a ratchet. The two must be designed together or the second will be retrofitted onto a shape that cannot express it. |
| **AB-13 -> AB-3** | Multiple parents mean a node sits in several closures at once, which is the doubly-reachable case `rootId`-is-recorded-not-derived already exists to handle. Whether that guard generalizes or merely rhymes is a real question. |

---

## Recommended order

Use `[ ]` for pending and `[X]` for complete; mark nested lines `[X]` as each sub-step finishes. Nothing below is built, so all lines start `[ ]`. **Phases 1 through 3 are one design pass, not three sequential closures** --- see Phase 4.

- [ ] **Phase 0. Build the worked-example corpus.** The evidence base everything else argues from. For each case: write it out concretely against *today's* model (nodes, edges, hosts, adjacency), then state exactly which invariant or ergonomic limit it hits. Do not propose solutions in this phase. Cases land in [Phase 0 corpus](#phase-0-corpus).
  - [X] **Mine the ladder's data-blocked parks for corpus cases first** --- see [The dependency is asymmetric](#the-dependency-is-asymmetric-representation-unblocks-reasoning). Each is a requirement statement written from the reasoning side, grounded in a situation someone actually hit, and cheaper than inventing a case. Start there, then extend. **Done 2026-08-05: cases [C1](#c1-look-at-the-character-standing-here-look-at-the-north-wall)--[C4](#c4-the-place-setting-at-the-faerie-court), one per data-blocked park**, plus an explicit note on the four parks not mined. The mining produced three corrections to this plan (compile gate, route non-uniformity, mark-state premise) --- which is the sub-step's payoff and the argument for having ordered it first.
  - [ ] **Next: the two [H1](#h1-single-host-objects-hosting-at-the-containing-node) falsification cases, written *against* H1 rather than model-neutrally.** Ordering is the mitigation for H1's sunk-cost failure mode --- run these while the rollback set is still near-empty, not after fifteen `H1-dependent` answers have accumulated. They are the two bullets marked **(F)** below; do the rest of the corpus after.
  - [X] ~~**(F) Born-spanning things** --- a river, a cable run, a wall.~~ **Dissolved 2026-08-05 rather than written.** Challenged on the grounds that an object can be born spanning without that being complicated --- correct, and the case had two faults: [C5](#c5-the-chain-mounted-at-both-ends) already covers the river and the cable run (permanently Area-hosted is the rule returning a stable answer, not failing), and the wall was grouped in on a **surface** property. The wall's real question is now **AB-30**. Retired as a falsification case; **rollback trigger 2 replaced** rather than merely removed.
  - [X] **(F) "Pull the rope" while the rope is Area-hosted** --- trigger 2's replacement. **Run 2026-08-05: passed conditionally**, answered by [part-mediated reference](#part-mediated-reference-how-h1-answers-its-own-replacement-trigger). Conditional on AB-6 (stored), AB-15 (naming seam) and the parent index. Both of H1's falsification cases are now spent; **trigger 1 (AB-25) is the only live test remaining.**
  - [X] **(F) The co-location vs. load-bearing test** --- run 2026-08-05 as [C5, the chain mounted at both ends](#c5-the-chain-mounted-at-both-ends), substituted for rope state (iv) because it isolates the variable that re-coiling confounds. **Result: migration-down and reabsorption are separate events**; a sub-claim of P1 struck, H1 itself untouched, AB-17 split, AB-9 escalated, rollback trigger 3 made more likely.
  - [ ] **The rope, in all four of its states** --- still worth writing for its own sake, though no longer load-bearing as a falsification case: (i) coiled in a backpack, a plain human-scale object; (ii) lowered into an abyss, spanning rooms; (iii) **tied to a rock and then lowered**, which puts a relation on one *end*; (iv) hauled up and re-coiled, which must return to (i). State (iv) is what makes reabsorption mandatory (AB-17) and is the state the flashlight example cannot supply.
  - [X] **(F) One end of the rope held by a character who walks away** --- run 2026-08-05 as [C7, Ariadne's thread](#c7-ariadnes-thread). **Verdict reversed twice; standing outcome is a passed discriminating test of [H1 clause 3](#h1-is-a-conjunction-and-its-clauses-are-tested-separately)** (spanning things are parts), since framing (a) cannot represent the object at all --- one node, no record of where along the thread anything attaches. Clauses 1, 4 and 5 remain untested; see the clause table for the four targeted tests that replaced them.
  - [ ] **(F) Carry closure meets a composition edge**, then **concurrent migration**, then **migration thrash and its narration** --- all specified in [Working hypotheses](#working-hypotheses). The first is the most likely to hurt, since it tests the boundary-edge discipline somewhere it was not written for after three straight cases of it anticipating correctly.
  - [ ] The four seed examples above, each in full.
  - [ ] At least two cases that **should not** become abstractions, to bound the concept --- a plain object on a floor, and a room's ordinary contents. A concept that swallows these is too broad to be useful. **Also an H1 bound:** if "host the whole at the node containing all its parts" applies to these, the rule is vacuous rather than wrong.
  - [ ] At least one case per axis where the axes *interact* --- e.g. a dismantled flashlight whose parts are then arranged into a place setting-like pattern, or a rope whose state (taut/slack) gates a mechanism.
  - [ ] Note against each case whether the value is read-side (reasoning/display) or write-side (manipulation). This tally is the direct input to AB-9 and AB-14.
  - [ ] Note against each case whether its parts are **heterogeneous separable** or **homogeneous ordered extent** (AB-16). If the corpus splits cleanly on this, that is itself an answer.
  - [ ] Note against each case whether it arrives by **Improvisation** (downward, operation-driven) or **Recognition** (upward, arrangement-driven) --- and for Recognition cases, whether the payoff is description or mechanism (AB-19). Add at least one **multi-step Recognition** case where the composing operations are separated in time, since that is what makes AB-18's trigger question bite; the snare trap is the worked example.
  - [ ] ~~Confirm whether "Improvisation" here is the objects lane's existing mechanism or a homonym.~~ **Resolved 2026-08-05: it is the existing mechanism, generalized** --- see [Improvisation is the objects lane's mechanism, generalized](#improvisation-is-the-objects-lanes-mechanism-generalized). Remaining corpus work is to note, per case, which of the three gaps in that section it would exercise.
- [ ] **Phase 1. Resolve AB-1 provisionally** against the corpus --- one concept, one substrate with several types, or three separate things. Record the *reason*, not just the verdict: whichever way it goes, the next reader will want to know what evidence would overturn it.
  - [ ] **Reconcile the multi-host contract clause** (AB-8's doc obligation). Independent of AB-8's eventual framing: decide whether "at most one room per `OBJECT#`" was a character rule over-generalized to objects, and correct [`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) / [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md) accordingly. Decide at the same time what `repairObjectPlacementDrift` is *for* if multi-room presence becomes legitimate --- an unwired repairer for a now-legal state is dead code with a misleading name. **Fourth site, added 2026-08-05 from [C2](#c2-the-rope-in-two-rooms-refused-at-the-gate):** `evaluateComplexityPreGates`' terminal `multiPresent` error. It is the most **player-visible** of the four --- docs and an unwired repairer mislead readers, but this one refuses a command --- and reconciliation must also settle the *inconsistency* between it and the relational route's silent hostless treatment, which is a defect on its own terms whichever way multi-host resolves.
- [ ] **Phase 2. Answer every remaining row provisionally, in one pass**, in cluster order (structure, lifecycle, consumption). Provisional means: written down with its reasoning, explicitly not closed. Resist the pull to fully settle any single row here.
  - [ ] **Tag as you go, applying the [test](#the-tagging-test-and-why-it-is-the-point) per row:** is this answer *derived from* [H1](#h1-single-host-objects-hosting-at-the-containing-node), or merely *compatible* with it? Only the first earns `H1-dependent`. Tagging retroactively does not work --- the reasoning that shows which one it is exists while the answer is being written and is gone afterwards.
- [ ] **Phase 3. Consistency pass.** Walk the constraint matrix and re-check each coupling. Every row that a later answer invalidated goes back to Open with a note on what invalidated it. **Expect at least one round trip** --- if there is none, the provisional answers were probably not specific enough to conflict.
  - [ ] **Count the `H1-dependent` set and record the number**, per [Working hypotheses](#working-hypotheses). This is the point at which the set is finally known, and the count is the measurement the tagging exists to produce: **small** means H1 was cheap and most of the design stands without it; **large** means it is load-bearing and deserves more scrutiny than a hypothesis has had. Either result is informative; an untagged or over-tagged set produces neither.
- [ ] **Phase 4. Close the rows that the corpus can actually settle**, and explicitly park the rest with a named trigger ("revisit when X exists"), the way BD-36's character-relation widening is parked. A parked row with a trigger is a finished outcome; a parked row without one is debt.
- [ ] **Phase 5. Choose the thinnest vertical slice that exercises at least two clusters.** Not the easiest slice --- a slice touching only one cluster proves nothing about the couplings, which are the part most likely to be wrong. Candidate shape (**not a commitment**): read-side composition summary for the place setting, exercising structure (AB-2/3/4) and consumption (AB-9) while leaving state and extent alone.
  - [ ] Score each candidate against [Parked extension points](#parked-extension-points-this-initiative-would-unpark) --- **which does it unpark, and is that one ready?** Unparking is expected here rather than avoided; a slice that unparks nothing is more likely mis-aimed than cheap. Check each parked item's own prerequisites, and note that the backtrack channel is a **sibling initiative** to schedule against, not to absorb.
- [ ] **Phase 6. Build the slice, then graduate.** Falsifiable rules to [`positions/AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md); paths to [`AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.implementation.md); new vocabulary or a graduated target mental model to [`AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md) per its own Graduation rule. If AB-3 resolved yes, this is also when `CarryClosureFragment`'s pinned note gets discharged --- in both of its locations.
- [ ] **Phase 7. Decide whether this becomes a standing ladder** (like the object-manipulation index) or stays one plan. Do not decide up front; decide once the corpus has shown how many independent initiatives are actually in here.

---

## Relationship to the object-manipulation ladder

Separate initiatives, real interactions. Recorded so neither plan silently assumes the other's shape.

### The dependency is asymmetric: representation unblocks reasoning

**Framing, 2026-08-05.** The reasoning layer has for some time been parking work that could not be addressed *because the right data did not exist*. A representation initiative therefore unparks some of that as a matter of course --- not coincidence, but the shape of the dependency. The ladder's rungs consume the world model; when a rung was parked for want of model, this is the initiative that supplies it.

**The inversion is the more useful half: those parks are a *specification*, not just a beneficiary list.** A rung parked for want of Feature nodes is a requirement for AB-12, written from the reasoning side before this initiative existed. **Phase 0's corpus should mine them** rather than inventing every case from scratch --- they have the advantage of being grounded in situations someone actually hit.

**But it holds for several parks, not all**, and the split matters so nobody expects the whole backlog to fall out:

| Data-blocked --- this initiative plausibly unblocks | Blocked on something else --- will not move |
| --- | --- |
| Feature look-matcher dispatch; character-inclusive catalog population (CPG-5 remainder) --- **AB-12** | **BD-24** / iteration 6, location-disambiguating modifiers --- *demand*-blocked, "no concrete case currently demands it" |
| **BD-16 (3)**'s `multiPresent` all-Error treatment --- explicitly waiting on a `Custom`-relation defer resolving into genuine multi-host co-location | **C3** / `MultipleCommands` policy --- same posture; no confirmed real-world example remains |
| Coyote's genre-bounded reasoning context --- **AB-19**; bounded by genre because there is no assembly-level structure to bound it with | **BD-18** / backtrack channel --- *architecture*-blocked: addressable resume points, bounded retry, cost policy |
| Situational meaning and the unpopulated mark-state axis --- **AB-23** | Iteration 2's remaining fallback steps --- prompt and calibration work |

| Touchpoint | Direction | Note |
| --- | --- | --- |
| **Referent resolution** | Ladder -> here | If abstractions are referrable ("clear the place setting"), Identify/Grounding must produce them as candidates. The already-tracked [character-inclusive catalog population](../actions/AGENT.objectManipulationIterations.planning.md#deferred-not-rung-sized) gap is the same shape of problem, and the node-model guidance for that scan (additive union getter, **not** a collapsed typed surface) applies here too. |
| **Synthesize instruction language** | Both | AB-11. The executor's ordered step sequence is where an abstraction-aware operation would live. BD-27/29/30's phase-stratified worklist and shared expansion environment are the machinery a level-crossing expansion would extend. |
| **Carry closure** | Here -> ladder | AB-3. If the graph gains a root, `CarryClosureFragment` collapses into it, which changes types the ladder's Expansion work owns. |
| **Backtrack (iteration 4)** | Ladder -> here | If an LLM can propose an abstraction (AB-5), a wrong proposal needs the same structured-critique channel BD-18 designs, not a silent guess. |
| **Reposition narration** | Adjacent | [`AGENT.relationalNarration.planning.md`](../../AGENT.relationalNarration.planning.md)'s Phase 1 asks what a reposition op *is*. A composition change ("the place setting came apart") is plausibly a fourth narration family. Do not pre-empt it; note the overlap when Phase 5 picks a slice. |
| **Features** | Constraint | [`positionGraph/AGENT.md`](../../../../../lambda/ephemera/dataSource/positions/positionGraph/AGENT.md#known-limitation-deferred) records that Features are deliberately **narrower** participants than Objects --- never a relation subject, never membership-moved, more like a room's walls than its contents. An abstraction design must not assume a Feature node would inherit Object capabilities. |

---

## Discussion record

Reasoning trail behind the Open-decisions rows, kept for provenance. Not needed to know what to do next --- only *why* the questions are shaped the way they are, and what a future reader would otherwise have to re-derive. Following the pattern of [`AGENT.backtrackChannel.planning.md`](../actions/AGENT.backtrackChannel.planning.md)'s History section.

**Seeded 2026-08-05 from conversation.** The initiative began from a scalability observation: the current treatment of objects is a fine starting point, but complex manipulation on top of it would become unworkably burdensome to reason about --- for an LLM in particular. The proposed answer was aggregations reasoned about in summary, at levels both above and below the human-convenience scale of objects. Four seed examples were given (place setting, dismantled flashlight, powered door, rope through several rooms) and are recorded above.

**The examples were immediately suspected of not being one structure.** Three axes were separated on first pass --- composition, functional aggregate carrying state, multi-host extent --- and AB-1 exists because collapsing them prematurely, or keeping them separate unnecessarily, are equally live failure modes. Notably, the place setting and the flashlight are **mirror images of one relation**, differing only in which level pre-existed and which was improvised: the place setting's members are primary and the whole is minted, the flashlight's whole is primary and the parts are minted.

**A factual correction, 2026-08-05.** An earlier draft asserted that multi-host presence "breaks the at-most-one-host invariant" and that `repairObjectPlacementDrift` "exists to eliminate it." Both were overstated; the verified position is now in [Multi-host presence](#multi-host-presence-what-is-actually-true-today). The correction materially changed AB-8: framing (a) is far cheaper than it first appeared, so extent's cost is dominated by the modelling choice rather than by any storage obstacle.

**The granularity insight, 2026-08-05 --- the pivot the rest of the design turns on.** A rope coiled in a backpack is a plain object: no sub-part is separately addressable, and pulling it from the pack yields the whole coil by default. Lowered into an abyss, room-level atomicity breaks down and the ends become distinct loci. **The rope did not change; the useful granularity did.** That yields a candidate criterion --- decompose when parts become independently addressable or independently positioned --- which runs *symmetrically* in both directions: the place setting aggregates because its members stopped being separately interesting, the rope decomposes because its parts started being. Evidence that aggregation and decomposition are one axis rather than two mechanisms.

**Consequences drawn the same day.** (i) The transition must run backwards --- re-coiling the rope must restore one object --- which the flashlight example hides and which makes reabsorption mandatory rather than optional (AB-17). (ii) Rope parts are unlike flashlight parts: homogeneous slices of a continuum versus heterogeneous separable things (AB-16). This weakened an earlier claim in this plan that AB-8's segment framing cheaply collapses into AB-4. **[Corrected later the same day: the "ordered along a path, load-bearing for `pull`" half of this was wrong --- see the AB-26 entry below.]** (iii) Tying the rope to a rock puts a relation on an *end*, not the whole --- which under a single multi-room node leaves the relational step's host ambiguous, and under segments is cleanly same-host. Evidence for segments, arrived at from the relational side rather than the positional one.

**The reframe, 2026-08-05 --- AB-12.** Asking *which* things can be promoted led to the observation that `<Object>`, `<Room>`, and `<Feature>` are **already abstractions**: a battery is an object, so is the flashlight it is part of, so is the casing that is part of that. There is no natural floor or ceiling, so pinning `<Object>` as a privileged origin with `<Part>` below and `<Abstraction>` above is not philosophically sound --- it would build two hierarchies for one relation. **The question may therefore not be "what is this strange `Abstraction` type" but "how do we mint and reabsorb Objects at different levels of abstraction, as they are needed."** Recorded as a candidate, deliberately not adopted: deciding is premature and there is useful discussion left.

Two objections were raised against the reframe and are carried as their own rows rather than resolved. **AB-13:** "levels" invites a scalar, but composition has overlap and multiple parents, so the shape is a partial order, not a ladder. **AB-12's own second half:** Room is a *host*, not a node, so extending the reframe to Room and Feature unifies hosts with nodes --- AB-2's most radical answer --- and contradicts the documented decision that Features are deliberately narrower participants. It would, in exchange, *explain* that narrowing rather than assert it (a part cannot be membership-moved away from its whole without an act of decomposition). Object and Room/Feature are to be weighed as separable bets.

**Two further candidate answers recorded, not adopted.** *Minting triggers (AB-14):* operational necessity and attention are unlike needs, and keeping them separate --- operations mint durably, attention selects a projection level and mints nothing --- would prevent description-time LLM inference from becoming a write path. *Authoring (AB-15):* if no level is ontologically privileged, an authored `<Object>` is plausibly the **default binding level** for unqualified reference, revisable by circumstance, which preserves a real role for authoring without pinning the ontology.

**AB-14 reshaped, 2026-08-05 --- Improvisation and Recognition.** The row's first framing (operational necessity vs. attention) was superseded on the observation that **attention is not a minting trigger at all** --- it was a projection concern in a trigger's clothing, and the row therefore described one minting operation plus a note about description. The replacement pair, motivated by the necessity of narration and the structure of the knowledge-representation system, is two genuine operations, both write-side and both consequences of player action: **Improvisation** invents detail an in-flight operation requires but nobody has authored (the flashlight's interior under a screwdriver), and **Recognition** notices that a player's arrangement has composed into something above the level they were working at (the snare trap). The displaced guardrail --- description selects among existing levels and never creates one --- moved to AB-9, where it belongs and where it is more legible as scope control.

The narration obligations were what distinguished the two most sharply. Improvisation's narration **is** the minting event: the battery and the corroded bulb come into existence for the player by being described. Recognition's narration is a **contract with the player**: it is how they learn the world has accepted their construction as meaningful, and without it a player cannot tell whether a clever arrangement counts for anything and will re-attempt it looking for magic phrasing. This is the argument that Recognition can never be a silent internal summarization pass, and it is why AB-19 (description vs. mechanism) is a real fork rather than a shading.

**Two asymmetries recorded the same day, both loading onto Recognition** (AB-18): Improvisation is *bounded by its operation* while Recognition is an unbounded search over arrangements possibly separated in time; and a wrong Improvisation is cheap (plausible detail for a player who asked for detail) while a wrong Recognition changes what exists and what is subsequently legal. The candidate that would resolve much of both at once --- restricting recognition-for-mechanism to an **authored pattern vocabulary** --- is recorded on AB-19, along with its cost: players could then only construct what someone anticipated.

**AB-14 refined again, 2026-08-05 --- the pair was marking a real distinction but naming it wrongly.** The *action* is improvisation in **both** cases: a sufficiently confident recognition of cups, plates and flatware mints a `Table Setting` component of which the others become parts, which is the same act as minting a north wall. What actually differs is **when the mint must happen, and that is downstream of whether a pending operation is blocked on it.** "Lean the table against the north wall" cannot resolve until the referent grounds --- synchronous, on the command's critical path. A loop of rope laid on a road may be understood as a snare immediately, thirty seconds later, or never, with no material loss of responsiveness. **Same persistence layer, potentially very different decision processes** --- which the previous session's finding independently supports, since `ASSET#IMPROVISATION` is a merge layer and therefore renders provenance invisible downstream.

**Timing is the symptom; criticality is the axis.** Preferring the second framing is not pedantry --- it predicts three things the timing framing does not, each now carried as its own row. **(AB-20, failure semantics):** an on-demand mint is a correctness obligation whose failure fails the command, while recognition is discretionary enrichment whose failure owes nobody anything --- and both disciplines already ship side by side in the objects lane. **(AB-20, authorization):** on-demand minting needs no confidence gate because *the player's reference licenses the existence*, whereas recognition volunteers an unprompted claim --- so AB-18's false-positive cost follows from authorization rather than being an arbitrary policy asymmetry. **(AB-18, convergence):** the mode is a property of the moment, not the thing --- "check the snare" puts recognition on the critical path, so one mint is reachable from both a blocking and a deferred path and the two must converge.

**AB-19 reframed, 2026-08-05 --- the abstraction bounds the LLM step, it does not replace it.** The row's earlier proposal (recognition-for-mechanism made tractable by an **authored pattern vocabulary with behavior attached**) is rejected on the grounds that it compiles fictional physics into a deterministic structural context. That is the wrong tool --- we are not doing physics simulation, and it is not what the fiction is about anyway --- and deriving a snare's result from first principles on every interaction is a fool's errand regardless. **Most player-assembled contraptions will require an LLM step to reason about consequences; the design goal is to give that step the right information so it is bounded and manageable.**

Description and mechanism therefore separate by **stakes, not machinery**: recognizing a place setting is a pleasant first pass over cogitation with no causally complicated consequences, and stays pure projection under AB-9's guardrail; recognizing a snare *functionally*, and reasoning about its function in the abstract, is **essential**. This is a cleaner allocation than the previous framing, and it lets AB-9's no-creation rule stand rather than straining it --- description never mints, mechanism mints because something must reason over the result.

**Coyote named as the existing first-iteration attempt at exactly this** --- flawed and ad hoc by its own author's description, and worth reading before designing. The generalizable core (annotate at mint time; closed structural vocabulary with open narrative content; graded aptness; multi-hop structured parse; internal-only reasoning handoff) and the specific ad-hoc parts (no assembly-level object at all, room-scoped rather than contraption-scoped context, boundedness that comes from a tiny world and a closed genre rather than from structure) are written up in [Coyote as prior art](#coyote-as-prior-art-bounded-llm-reasoning-that-is-not-bounded-structurally). Two rows came out of it: **AB-19** re-aimed at "what must an abstraction carry to bound an LLM step," and **AB-22** on annotation timing and vocabulary --- including the observation that member affinities are a cheap filter for *whether an arrangement is worth recognizing at all*, which attacks AB-18's unbounded search without needing a pattern library.

**Scheduling substrate and situational meaning, 2026-08-05 (two findings from one exchange).** First, `mtw.ephemera.thinking` was named as the intended home for deferred recognition --- its future possibility of work "queued for consideration as time allows, but foregrounded by necessity if the results suddenly become needed" is exactly what tier 2 requires. Verification found the **persistence and vocabulary shipped and the execution model absent** (no `claimed` producer, no claimer, every job bootstrapped and completed in one synchronous invocation), plus one free win: `Meta::Result`'s `nonCollidingPutItem` first-write-wins already satisfies AB-18's convergence requirement. Detail: [`thinking` as the scheduling substrate](#thinking-as-the-scheduling-substrate-shipped-ledger-unbuilt-queue).

Second, and more consequential: **AB-22's vocabulary question was posed statically and should not have been.** "Is the vocabulary general, or authored per world?" has no good answer because relevant meaning is **situational and varies within a world** --- ambushed by goblins, meaning is combat-relative; dining at the Faerie Court, the place setting's causal complexity becomes very real. Coyote shortcuts this legitimately, as an early iteration should, because its genre supplies a constant interpretive context. Two consequences: mint-time annotation can only be a partial answer (durable base plus situational overlay), and the member-affinity *filter* is itself context-dependent. Recorded as **AB-23**, with **AB-24** for the proposed low-immediacy scheduled task that watches for frame shifts and parameterizes recognition. Note the second deliberately-deferred axis this conversation has landed on: the render cache's `n` / mark-state axis is **built and unpopulated** (`markState` pinned to `[]`, `SITUATION#DEFAULT` only, per iteration 10), and situational *meaning* is that same axis raised from rendering to interpretation.

**Part connections corrected, 2026-08-05 --- adjacency-mediated, not sequenced (AB-26, AB-27).** This plan had twice claimed extent parts are "ordered along a path." Wrong, and wrong in a way already diagnosed one layer up: it imposes a **scalar where a graph belongs**, the same error AB-13 records for levels. A sequence only looks right because a rope is linear; a net spanning four rooms, wiring from a generator to a powered door, and two ropes knotted together all branch. The correct shape is *"the clifftop rope end connects to the shaft section **by way of** the 'into the abyss' exit"* --- a graph whose edges are labeled by the spatial relation that licenses them. Branching is then free, the edge **explains** the adjacency instead of asserting it, narration takes direction and wording from the exit (as the shipped move compiler already does with `exitName`), and `pull` becomes graph propagation rather than an index walk. Area edge kinds beyond Exit were **already** documented as able to carry non-traversable spatial facts ("north of" without a door), which is exactly what a rope through a floor-hole needs --- a third anticipated axis this design lands on.

Two rules proposed with it: connections are **explicit but constrained** (adjacency *licenses* an edge, never derives one --- two ropes in adjacent rooms are not connected), and an edge must reference **world truth** rather than `projectRoomExits`' perspective-filtered output, or a rope's physical shape would vary by observer. **AB-27** then notes the encouraging corollary: parts sharing a host can use the *existing* intra-host relational vocabulary, so the new edge kind is needed **exactly and only where `applyStepSequenceCore` already throws** --- a sign of a real joint rather than a parallel mechanism.

**Rule (ii) corrected within the hour, and the correction matters more than the fix.** It was first written as "the **authored Exit** edge," which collapsed **three** independent things into one: *world truth* (the real constraint), *Exit* (one kind of spatial relation), and *authored* (one provenance). The licensing relation may be **any kind** --- an Exit edge hard-coding a connection affordance, **or** an adjacency/nesting relation, such as going over the rail of a boat and ending up in the bay --- and **any provenance**, authored **or improvised**, since an improvised relation is world truth the moment it is minted. Only the *projection* is excluded.

Three consequences followed. **(a)** The boat case is the deferred containment operator arriving: `In`/`inside` was explicitly excluded from relational v1 (BD-2) and parked for "a future nested container operator," and a rope over the rail needs exactly that relation to license it. **(b)** Nesting does **double duty** --- it structures the containment tree *and* licenses a boundary-crossing connection --- so AB-25 and AB-26 share a substrate, though not identically, since an Exit gives adjacency with no containment either way. **(c)** Improvised provenance has a concrete gap: an improvised area-level adjacency needs an **ephemera-side** Area graph, which does not exist because `EphemeraAreaId` does not exist --- the same missing piece P1 requires, so one gap with two consumers. Once it exists, mixing provenances is cheap, because participation-stack merge already does exactly that. And **AB-28** captures the failure mode neither framing had named: licensing relations are **mutable**, so a repaired floor-hole strands the rope threaded through it --- probably content rather than drift.

**AB-28 recognized as an instance of a shipped pattern, 2026-08-05 --- and it deflates from "new problem" to "existing discipline, extended."** The mutable-licence question is the same shape as the boundary-edge rule already in production: *a mutation that would sever a relationship is not legal until the severing is made explicit.* Assert-and-throw with no silent-strip fallback, `boundaryEdgeOutcomes` classification, expansion-classifies-compiler-sequences, dissolves ordered ahead of the mutation. The three outcomes guessed for the rope-through-a-sealed-hole case turned out to **be** the existing verdict channels --- `error`, `dissolve`, `defer` --- rather than needing new ones. A convergence worth noting: `defer` already exists as the verdict meaning *needs LLM judgment*, which is exactly the bounded LLM step AB-19 argues contraption consequences require. The classification vocabulary anticipated it.

**AB-29 carries what does not carry over**, and its bifurcation is the substantive finding: **play-driven** licence removal has an operation to block or transform (so the discipline applies and the conflict is content), while an **asset-withdrawn** licence has no operation to make illegal at all --- nobody did anything --- which is the **eviction ladder's** exact situation one layer over, and wants trim-and-repair rather than legality. An earlier guess in this plan that the whole case is "content, not drift" was therefore half right.

**The reverse edge-reference index is now the initiative's most load-bearing parked item.** Three independent lines of reasoning reached it on the same day: cross-host aggregate membership (AB-6/AB-8), part-connections referencing a licensing relation (AB-26), and the backwards discovery a licence-removing mutation requires (AB-29). Recorded in [Parked extension points](#parked-extension-points-this-initiative-would-unpark) with that annotation, because a slice that unparks it pays for itself three times.

**A pattern in this plan's own errors, worth watching.** Twice now a rule has privileged one *instance* of a category as though it were the category: `<Object>` over parts-and-aggregates (corrected by AB-12), and authored-Exit over spatial relations generally (corrected here). Adjacent to, but distinct from, the scalar-where-a-graph-belongs error recorded at AB-13 and AB-26. Both are worth checking for explicitly the next time a rule is proposed.

**And a complication surfaced by the wall itself (AB-21).** The north wall may not be *invented*: a room has walls whether or not anyone wrote them down, exactly as the flashlight always had a battery. Materializing something always implied is different from conjuring something new, and it carries an **identity obligation** --- two references to "the north wall" in one room must yield the same wall, across Lambda cold starts included, or repeated reference silently multiplies the world. `stableKey` on `Meta::Object` already solves this shape of problem for Coyote correlation and is the obvious hook. Recognition largely escapes this obligation; on-demand minting cannot.

**Naming check resolved, 2026-08-05: it is the objects lane's mechanism, raised from ad-hoc handling of one kind of thing into a general mechanism named for its purpose.** Verified against [`objects/AGENT.md`](../../../../../lambda/ephemera/dataSource/objects/AGENT.md), and the confirmation was stronger than the question assumed: `ASSET#IMPROVISATION` is an **asset layer in the participation stack**, not a flag on a special object kind, so improvised components already merge and render exactly as authored ones do. The general purpose was therefore already implemented and merely unnamed. Consequences --- what arrives pre-built, the sharpening of AB-12 into a cost question, three bounded gaps, and the naming fork now carried on AB-0 --- are in [Improvisation is the objects lane's mechanism, generalized](#improvisation-is-the-objects-lanes-mechanism-generalized) rather than here, because that content outlives this record.

~~**Settled, 2026-08-05:** extents spanning hosts of different kinds (two adventurers holding opposite ends of a rope in different rooms) is not a concern.~~ **Reopened later the same day by H1's adoption** --- see AB-8. Under framing (a) it really is a non-issue; under H1 it is a question about whether the containment tree admits characters as interior nodes. **The general lesson is the durable part: adopting a hypothesis silently re-poses questions that were settled against the model it replaced, and nothing marks them as stale.** Sweep the pre-H1 settled notes deliberately rather than trusting them.

**Phase 0's first sub-step run, 2026-08-05 --- mining the ladder's data-blocked parks, and it paid for itself in corrections.** Four cases landed ([C1](#c1-look-at-the-character-standing-here-look-at-the-north-wall)--[C4](#c4-the-place-setting-at-the-faerie-court)), one per park. Three of them falsified something this plan had asserted, which is the argument for having mined before inventing: an invented case cannot correct the plan, because it is built out of the plan's own assumptions.

**(1) The membership compile gate.** `evaluateComplexityPreGates` returns `multiPresent` for `containers.length > 1` and maps it to terminal player-facing error copy. So this plan's "the actual single-host pressure is the end-state planner" was understated --- the gate is harder, earlier, and the only one of the pressures a player can see. Found by reading BD-16(3)'s own code, which is exactly the park that named it.

**(2) The non-uniformity is worse than "not uniform across routes."** The plan recorded that the cross-host move route bypasses the end-state planner. Verification found **three different rules on three routes**: membership compile refuses, relational compile silently leaves the object hostless (`if (containers.length === 1)`), the move route does neither. That inconsistency is a defect on its own terms, independent of how multi-host resolves, and Phase 1's reconciliation sub-step now carries it.

**(3) AB-23's premise was wrong in a load-bearing way.** "The situational axis is built and unpopulated" suggested a switch awaiting a flip. It is populated for Rooms and structurally absent elsewhere: non-Room hosts are pinned to empty mark state **because they have no `Meta::Room` to carry it**. That converts AB-23 from "turn on an existing axis" into "there is no state carrier below room scale," which is AB-7 --- so AB-7 gains a second consumer and is less likely to be out of scope than the row currently guesses.

Two findings that corrected nothing but sharpened a case. **Coyote's limit is not scoping, it is projection:** `CoyoteStagedObject` carries no edges and the snapshot never reads `graph.relationalEdges`, so a rope tied to a branch and the same objects in a heap yield byte-identical prompt context. Widening or narrowing the scope of a set that cannot express relation does not help --- which strengthens the [Coyote as prior art](#coyote-as-prior-art-bounded-llm-reasoning-that-is-not-bounded-structurally) conclusion by locating the gap precisely. And **CPG-5's dead filter documents itself**: `compileDescribeFromSkeleton`'s `isEphemeraObjectId` guard carries a comment calling itself "a no-op guard," because nothing upstream can produce a candidate for it to reject.

**One finding against Phase 0's own method.** C4 has no description-vs-mechanism value: at an inn the place setting is inert, at the Faerie Court it is causally loaded. **AB-19's tag is a property of *(case, frame)*, not of the case**, so the per-case annotation column cannot hold it and Phase 2 must not read that tally as a partition. Deliberately **not** given a new AB row --- AB-23 already states that the member-affinity filter is context-dependent, and this is the same claim arriving from the corpus side. Noted here because the temptation to allocate AB-30 for it was real, and the discipline that a corpus case should *sharpen* rows rather than *breed* them is worth holding visibly.

**P1 promoted to a working hypothesis, 2026-08-05 --- and the posture change is the part worth recording.** This plan has run on *deciding is premature*, and that has been right: most of these rows still have useful discussion in them. What was noticed is that a question can be under-discussed for the opposite reason too --- with no grounding assumption, downstream rows have nothing to argue against and the discussion stays abstract rather than premature. **The middle move is to adopt a candidate explicitly as a test of how well it grounds further design**, tag everything derived from it, and keep the derived set rollback-able as a unit. If H1 dies, the trail says which answers die with it and where to branch. Recorded in [Working hypotheses](#working-hypotheses); a reader who finds the lock and reads it as drift should find this paragraph first.

**Why H1 and not framing (a), stated carefully because the obvious argument does not work.** The tempting one --- *the route non-uniformity means any consolidated representation beats this* --- is true about the inconsistency and **symmetric** between the two consolidations, so it selects nothing. What selects H1 is asymmetric **cost**: five sites become correct as written under H1 and all five need widening under (a). Five independent decisions for single-host is weak evidence single-host is natural and strong evidence widening is expensive. Recorded in that shape because the weaker version would equally license the expensive direction, and it is the version that comes to mind first.

**Two things the promotion does not buy, both recorded so the lock is not later defended as more than it was.** It is **not a net simplification**: it retires AB-8 and buys AB-25 --- which needs an ephemera-side `EphemeraAreaId` that does not exist, an upward room -> area index that exists nowhere, and an answer to whether containment is a tree at all --- while making AB-9, the scope-controlling row, acute rather than deferrable. And **surviving is not evidence of being right**, only that nothing has killed it yet. The named rollback triggers exist so that "dead end" is a test rather than a mood.

**The failure mode designed against is sunk cost rather than error**, and the mitigation is ordering: the two falsification cases (born-spanning things, rope state (iv)) move to the **front** of the remaining corpus, so H1 faces its cheapest tests while the rollback set is near-empty. Conditional adoption goes de facto permanent once rollback means redoing fifteen answers, at which point the hypothesis survives on accumulated cost and the section documents a decision nobody will act on.

**And the mechanism was made general rather than a P1 one-off.** AB-12's privileged-level reframe has the same shape --- a candidate that dissolves rows rather than answering them --- and is named as **candidate H2**, deliberately not promoted: two hypotheses in flight would overlap their dependent sets, and a row tagged both cannot be rolled back as a unit, which is the whole mechanism. This is the same promote-a-pattern-rather-than-treat-it-as-coincidence move already applied to AB-15.

**First falsification case run, 2026-08-05 --- and it worked, which is the meta-finding.** [C5, the chain mounted at both ends](#c5-the-chain-mounted-at-both-ends), was raised in conversation as a substitute for rope state (iv) on the grounds that re-coiling changes co-location and load-bearing **simultaneously** and therefore cannot distinguish them. The chain holds co-location fixed --- everything in one cell --- while the ends stay load-bearing. It falsified P1's speculative "migration-down and reabsorption may be the same event" immediately. **H1 itself is untouched:** nothing in the case bears on single-host objects or on hosting at the containing node, so this is a sub-claim dying, not a hypothesis. That distinction is exactly what the [Working hypotheses](#working-hypotheses) tagging exists to keep legible, and it held on its first use.

**The plan had already written the right answer and P1 walked it back.** AB-17's criterion has always been a disjunction --- "no part is separately referenced **or** separately positioned" --- and P1 clause 5 tests only the second half. So this is not a new discovery so much as the plan's earlier, more careful statement surviving a collision with its later, sloppier one. Worth recording in that shape: the failure was **collapsing a disjunction**, which is a fourth entry in this plan's own error catalogue alongside scalar-where-a-graph-belongs (AB-13, AB-26) and instance-mistaken-for-category (AB-12, AB-26's rule (ii)).

**The reabsorption guard turned out to be shipped already, and it throws.** Reabsorbing a part removes its node, and `removeObject` asserts and throws `RelationalEdgeStillReferencedError` when a Relational edge still references it (BD-33/35, deliberately no silent-strip variant). So the relational half of "separately referenced" is a **graph query** --- `nodeHasRelationalEdge`, already on the class --- and unsafe reabsorption is already impossible rather than being a hazard this initiative must guard. Third convergence of this kind, after AB-28's and AB-18's; the boundary-edge discipline keeps anticipating cases it was not written for.

**One honest gap, recorded rather than worked around.** C5's more interesting anchor --- chain-end to *wall* --- is not representable at all, because Features are not graph nodes ([C1](#c1-look-at-the-character-standing-here-look-at-the-north-wall)). Chain-end to chest works today; the wall end needs AB-12's Room/Feature half first. The finding rests on the half that can be stated.

**Two consequences drawn the same day.** **(i)** AB-9 escalates from acute to **routine**: load-bearing parts persist while co-located, so anything anchored sits in its host graph as a multi-level structure permanently, and level selection becomes a default read-path concern rather than an edge case. **(ii)** Rollback trigger 3 becomes **more** likely: parts surviving migration means dragging a chained chest out and back cycles the whole up to the Area and down again with structure intact --- a high-frequency cascade on an unchanging structure, where coincident reabsorption would have made cascades rare.

**And a third decomposition trigger, previously unnamed.** The chain's ends exist while everything is co-located, so decomposition was not triggered by spanning --- it was triggered by needing an endpoint **finer than the whole** to bear a relation. That is on-demand Improvisation (AB-14), and it restores the symmetry the granularity insight claimed while moving it off the axis P1 guessed: **parts exist while they are needed as distinct loci, by position or by relation; the whole hosts wherever it must to contain them.** Two independent questions, which P1 had as one.

**"Born-spanning" dissolved under challenge, 2026-08-05 --- and it was this plan's own case, not an inherited one.** The objection: an object can be born spanning without that being a complicated problem. Correct, on two counts. **(i)** [C5](#c5-the-chain-mounted-at-both-ends) had already absorbed it --- a river's parts are permanently separately-positioned, so the whole stays Area-hosted and migration-down never fires, which is a rule giving a stable answer for a stable thing rather than a rule doing no work. The original reasoning ("if it never migrates down, the hosting is bookkeeping for its own sake") does not survive contact with `computeEndStateRoomDiff`, which returns `changed: false` on a no-op without anyone calling it vacuous. **(ii)** River, cable run and wall were **not one category.** The first two are ordinary Objects with extent that H1 handles unstrained; the wall is structurally different and was grouped in because it shared the *surface* property "multi-room from creation." Grouping by symptom rather than by mechanism --- adjacent to the instance-mistaken-for-category entry already in this plan's error catalogue, and the second time in two days a category has been drawn around the wrong shared attribute.

**The residue is real and is now AB-30: *addressable but not a member*.** A wall is not in a room, it bounds rooms, so H1's containment rule has no purchase --- which explains why it cannot migrate down far better than "born spanning" did. Two corpus cases independently require walls to be addressable without being members: C1 wants the north wall referrable, C5 wants it relation-targetable. The candidate answer (a wall is an **edge**, not a node) was already scaffolded in `AGENT.concepts.md`'s note that Area edge kinds may carry non-traversable spatial facts, and it would *explain* the documented Feature narrowing rather than assert it.

**Spanning Features got no row of their own, deliberately.** Asked directly whether "how do we deal with Features that span rooms" had a home: it did not --- only P1's `Features stay multi-host` disposition and AB-12's different question. But it is **AB-30's discriminator rather than a separate question**: under the edge answer spanning is free, under the node answer the wall needs to exist in two graphs, which is the multi-host problem H1 just abolished for Objects. Making it a row would have split one decision across two.

**And it exposed an unremarked asymmetry in P1.** H1's whole thrust is removing multi-host for Objects while the sub-proposal *preserves* it for Features. Either a real distinction with a reason, or the clause describing the problem in the vocabulary H1 abandoned. Noted on the sub-proposal; AB-30 decides it.

**H1 is now less falsifiable than the section first claimed, and that is recorded as a weakening.** Trigger 2 was badly built, and retiring a bad test is not the same as passing it. The replacement --- **the whole becomes unreferrable while Area-hosted** --- is sharper: a spanning rope is Area-hosted exactly when a player most needs to refer to it, so if "pull the rope" cannot bind in that state, H1 has moved the difficulty rather than solved it. It counts as a substitute only if it is actually run.

**Trigger 2's replacement run the same day, 2026-08-05 --- part-mediated reference, and it passed conditionally.** The proposal: in any room where "pull the rope" makes sense a **part** of the rope is present, that part takes the identification-grounding because it is what is locally there, it carries a **parent index** to the whole, and the whole then accumulates its parts' state across rooms to evaluate the pull. **Better than the alternative rather than merely adequate:** the other way to keep the whole referrable was to surface Area-hosted objects in room affordances, which would make compose reach up the containment tree and pull non-present things into every catalog. Under part-mediated reference the whole is never catalog-visible at all, so P1's affordance clause stays true as written. Full assessment: [Part-mediated reference](#part-mediated-reference-how-h1-answers-its-own-replacement-trigger).

**It answered AB-11 as a side effect, which is the interesting part.** "Pull" needs no new step kind --- the whole expands into per-part steps, which is Synthesize's existing Expansion sub-role, and the multi-host mutation that results already has a shape in `MultiKeyUpdate`'s N-effects-one-`transactWrite`. The kernel does not change. **The scheme was not designed to answer that row**, which is weak evidence the joint is real rather than constructed --- the same kind of signal as AB-27's observation that the new edge kind is needed exactly where `applyStepSequenceCore` already throws.

**Three dependencies acquired, and they are recorded as the price of passing.** AB-6 is forced toward **stored** on a mechanical argument (footprint runs once, so every part's host must be known before the walk; a derived set would need the answer it is computing). The parent index is **AB-25's missing upward direction one level down** --- distinct from the thrice-flagged reverse *edge*-reference index, same missing direction. And the **naming seam** is the one step that does not fall out of existing machinery: the catalog matches short names, the player says "rope," and what is present is a part, so under H1 the authored binding level is exactly the thing not locally present. That is AB-15 arriving concretely, with three candidate fixes and none chosen.

**Recorded as *conditionally* passed, deliberately.** H1 survived by acquiring three dependencies rather than by needing nothing --- normal for a hypothesis doing real work, but "passed" without the qualifier would read as "free" to someone arriving later. **Both falsification cases are now spent**, one dissolved and one conditionally passed, which leaves trigger 1 (AB-25) as H1's only live test. Phase 2 should weigh that: AB-25 is simultaneously H1's critical path and the last thing that could falsify it.

**The rope-and-candle case, 2026-08-05 --- offered against AB-25, landed on AB-13, and opened a row nobody had.** Tie rope to bedpost, throw it out the window, set a lit candle under the span: an Agatha Christie contrivance already used in conversation as a specimen of what creative players actually attempt. Written up as [C6](#c6-the-rope-the-candle-and-the-impromptu-timer).

**The redirection is worth recording rather than quietly absorbing.** AB-25 asks whether *Room/Area containment* is a tree; C6 is about *composition* overlap --- one part, two wholes, neither containing the other. Different structures, different consequences, and conflating them would have let a strong AB-13 case be filed as evidence on a question it does not touch. **C6 is the best evidence AB-13 has**, better than the existing fork example, because the fork's three wholes are categories of one kind that a reader can talk into a nesting while C6's two are different **in kind** --- a physical object and a mechanism.

**What it does give AB-25 is a prohibition.** The rope hosts at the Area (spans room and garden); the timer hosts in the room (both members local). So one part's two parents sit at **different heights**, and hosting height does not track composition depth. No implementation may treat the parent index as "the next level up." That is the ladder-versus-DAG error one layer over from where AB-13 records it, in the place it looks most tempting --- hosting *feels* like it should be stratified.

**Two gaps followed.** Dissolution turns out to be **per-whole**: the timer dies with the candle while the rope persists, so a shared part is released by one parent and retained by another --- which AB-17's criterion, written as though a part is reabsorbed or not, could not express. And the scenario's payoff, the rope **burning through**, is **fission** --- one whole becoming two --- which AB-5 (minting), AB-17 (reabsorption) and AB-6 (decay) all gesture past and AB-16 mentions only as a property of extent. Now **AB-31**, with the observation that under H1 both halves migrate *down* out of the Area from one event, in opposite directions.

**Flagged at the time, and worth keeping: that was the second new row in two exchanges** (AB-30, then AB-31) after a stated sharpen-don't-breed discipline. Both are judged to earn it --- no row asked what makes something a graph participant, and three rows gesture at fission without owning it --- but the rate is the thing to watch, not the individual justifications, since each new row is individually defensible right up until the plan is unusable.

**And one separation made explicit so the expensive claim cannot ride in on the cheap one's evidence.** "So the system can understand the manipulation" is two claims. *The representation must permit it* --- multi-parent composition, per-whole dissolution, fission --- is a hard requirement that C6 strongly supports. *The system spontaneously recognizes rope-plus-candle as a timer* is AB-18's unbounded search, which C6 only motivates. Same discipline AB-12 already applies to its two bets. C6 is, though, a good advertisement for AB-22's affinity filter --- `trigger` aptness on a candle beneath a rope under tension is cheap to notice without a pattern library.

**The falsification cases were failing as tests, and the diagnosis is worth more than any of them, 2026-08-05.** Three in: one dissolved because it was not a test, one passed by acquiring three dependencies, and meanwhile [C6](#c6-the-rope-the-candle-and-the-impromptu-timer) --- rope, candle and an Agatha Christie timer, about as baroque as play gets --- sailed through. Reasonable to read that as H1 being robust. **The tag count says otherwise: C6 added zero `H1-dependent` rows**, because everything it established holds equally under framing (a). A case a competing model passes identically has no selective power over the choice between them. It was a good composition case and not a test of this hypothesis.

**Underneath that: representational adequacy is nearly free, and it is not what H1 claims.** Any sufficiently expressive graph model can express almost any situation --- there is always another node and another edge. Fictional complexity is not structural difficulty. H1's load-bearing claims are **mechanism** claims (the whole hosts at the containing node; migration is automatic and bidirectional), and nothing run so far has required the LCA to be *computed* rather than asserted, or a migration to be *planned* in one footprint pass. Criteria now written down at [What counts as a falsification case](#what-counts-as-a-falsification-case-learned-the-hard-way-2026-08-05): discriminating, mechanism, or interaction.

**The next case, and it is the first constructed one that discriminates: one end of the rope held by a character who walks away.** Framing (a) handles it with nothing special --- presence in two hosts, of whatever kinds. H1 needs an LCA of a **room and a character**, and since `EphemeraMembershipHostId` is `Room | Character`, characters have to be **interior nodes** of the containment tree. AB-25 does not contemplate that, being framed purely as Room/Area, and it widens the row in two ways: the tree now spans node kinds of different provenance (authored Area membership versus play-time character presence), and **character movement becomes a containment-tree mutation.** Since navigation is the highest-frequency mutation in the system and routes through the same `commitStepSequence` as take/drop (BD-36), a walker carrying a rope end re-hosts the whole rope --- so rollback trigger 3 gets exercised by ordinary movement rather than by contrivances.

**It also reopened a settled point, and the mechanism of that is the durable lesson.** "Extents spanning hosts of different kinds is not a concern" was confirmed earlier the same day --- correctly, *while framing (a) was live*. Under H1 the identical sentence is a question about the shape of the containment tree. **Adopting a hypothesis silently re-poses questions that were settled against the model it replaced, and nothing marks them stale.** The pre-H1 settled notes want a deliberate sweep; this one was caught by accident.

**Three more queued behind it**, all in [Working hypotheses](#working-hypotheses): carry closure meeting a **composition** edge (does `boundaryEdgeOutcomes` classify it, and as what --- worth testing precisely because that discipline has anticipated three cases already and the expectation is becoming unexamined); **concurrent migration**, where two characters moving simultaneously lock different footprints; and **migration thrash**, where pacing across a boundary re-hosts the rope repeatedly and the presentation kernel narrates on host change, so either the player sees nonsense or a suppression rule is needed that does not exist.

**Ariadne's thread, 2026-08-05 --- and the verdict on it was wrong twice before it was right.** Run as the character-held-rope-end case, then correctly renamed: the myth is a specification, and players who know it will attempt it. Written up as [C7](#c7-ariadnes-thread). Better than the two-room rope on four counts --- many rooms rather than two, the path is the object's *purpose* rather than an incidental property, it unspools progressively so the per-crossing cost is paid twenty times, and it is retraced, making the read path first-class.

**Verdict 1, wrong: "framing (a) handles it trivially."** (a) needs the same presence derivation H1 does --- a flat container set does not by itself know that a character entering a room while holding one end means the thread now occupies that room. Corrected by working the alternative rather than summarising it.

**Verdict 2, also wrong: "(a) is cheaper, so H1 takes damage."** The steps *were* shown this time, and (a) still came out cheaper --- one container add against a minted span-part per crossing. **What went unchecked was whether (a)'s resulting state is correct, and it is not.** Under (a) the thread is one node: the bystander grabs the whole thread rather than a middle, and three attachments across three hosts have nothing recording **where along the thread** each sits. A container set is unordered and the route cannot be recovered from exits, because a labyrinth has multiple paths --- which is what makes it a labyrinth. **(a) cannot represent Ariadne's thread at all.** Its cheapness is the cheapness of not modelling the object.

**Verdict 3, standing: C7 is a passed discriminating test of H1's clause 3.** The axis it tests is **one node versus parts**, and conceding parts lands in AB-8's framing (c) --- which H1 embodies, though an (a)-with-segments variant would reach the same place with multi-host still permitted, leaving single-host secondary rather than settled.

**A fourth reading, and it is a correction of my own over-correction rather than of the case.** Having twice been wrong in H1's favour, I then called H1 "untested against a serious attempt" --- which was **too coarse a statement to be true or false.** H1 is a **conjunction of five clauses**, and C7 is a genuine pass of clause 3 while leaving clauses 1, 4 and 5 untouched. Per-clause tracking was adopted in response ([the clause table](#h1-is-a-conjunction-and-its-clauses-are-tested-separately)), and it immediately earned itself twice: it revealed that **clause 2 has attracted no scrutiny whatsoever** across the entire design conversation, and it turned the next round of testing from "invent another elaborate scenario" into four targeted questions. **The lesson generalises past H1:** a hypothesis stated as a conjunction cannot be tested or reported as a unit, and "is it tested?" has no answer until you say *which part*.

**One argument that fell out of the per-clause split, and it reduces clause 1's cost.** Under decomposition, parts are local by construction --- a part spanning two rooms would be decomposed further, since the criterion is precisely "a distinct locus is needed here." So the whole spans by **having parts** rather than by having presence, and nothing needs multi-host. Clause 1 weakens from *remove the capability* to *nothing uses it*, and P1's claimed gains survive that weaker version intact. Not free --- the contract clause is only true if enforced, and Features sit outside decomposition entirely (AB-30) --- and to be held loosely, since it follows from the decomposition rule rather than as a theorem.

**The two wrong verdicts were the same mistake, and the first correction did not prevent the second.** Both evaluated the alternative on the dimension that is easy to count --- first operation existence, then operation cost --- rather than the one that decides. The criteria table's first clause (*show the competing model's answer step by step*) was added after verdict 1 and did not catch verdict 2, because the steps were shown and the state was not inspected. **A second clause now requires checking that the resulting state is faithful, not merely reachable.** Recorded at length because a rule that failed on its first outing after being written is worth more attention than one that has never been tested.

**"Mint it lazily, on reference" turned out to be circular**, and the constraint that replaced it is the durable finding: **presence must be established at description time, not reference time**, since a player cannot refer to what they cannot perceive and description is upstream of reference. That kills lazy minting rather than repairing it --- description runs on every look, so mint-on-describe is eager minting with extra steps.

**And the comparison then inverted again, which is why the case is not fatal.** Framing (a) needs **the same derivation**: a flat container set does not by itself know that a character entering a room while holding one end means the thread now occupies that room. (a) stores the result at write time; H1 would compute it at read time. **So the discriminator resolves into stored-versus-derived *presence*** --- AB-6 one level over --- rather than into either framing failing. Net: H1 is more expensive here, the margin scales with labyrinth length, and derived presence remains available as its answer.

**Three findings the case leaves behind.** **(i)** AB-6 is being asked its question **twice with possibly opposite answers** --- part-mediated reference forces member sets toward *stored*, presence pulls toward *derived* --- and the row reads as though it has one answer; it needs splitting in Phase 2. **(ii)** AB-15's candidate (c) was **dismissed too fast**: a thread passing through a room *is* present there, so "widen the catalog to wholes passing through this room" is defensible and distinct from "widen it to all reachable wholes," which is the version that reintroduces the problem. **(iii)** **Held and present are not mutually exclusive**, and AB-4's noted membership exclusivity is therefore a live defect rather than a design question --- one that **both** framings share, so it will not be resolved by rejecting H1.

**The thread is also AB-26's part-connection graph made literal** --- a record of which exits were traversed, licensing relation per connection --- which sharpens that row's own correction: a thread's part graph *is* a path, while a net's is not, so "not a sequence" means the shape cannot be **presumed**, not that paths never occur.

**The clauses are not independent, 2026-08-06 --- and following that through found a candidate that removes three of them.** Observed in conversation: enforcing clause 1 *implies* migration up and down, to cover what would otherwise be multi-hosted siblings. Correct, and it runs one step further. **Clause 1 applied to *wholes* generates clause 4** (decompose a spanning thing and the whole still needs a host, its parts being spread by construction), **and clause 4 generates clause 5** (the host is defined by where the parts are, so when parts move the host must follow --- migration is not a feature but the maintenance of clause 4's invariant). Three of five clauses are one commitment with two entailments, and the peer-listing in the first version of the clause table over-counted H1's independent content.

**It also halved a cost reduction I had written the day before.** "Parts are local by construction, so nothing needs multi-host" holds for **parts** and fails for **wholes** --- and the whole is precisely the object whose parts span. Only half the objects were checked. Split into **1a** (parts, nearly free, survives) and **1b** (wholes, expensive, generates the chain).

**And then the useful part: nothing reads the whole's host.** P1 says Area-hosted objects never surface in affordances; part-mediated reference establishes the whole is never catalog-visible and is reached by traversal from a part; description reaches it the same way. Clause 4's hosting is **bookkeeping nothing consults.** So the candidate: make the whole a **non-positional record** --- a `Meta` row with the member set and part-connection graph, not a node in any position graph --- and clauses 1b, 4 and 5 all disappear. P1's four claimed gains survive untouched (they require only that no object *is* multi-present, and a non-node cannot be), **AB-25 comes off H1's critical path**, rollback trigger 3 evaporates, three queued test cases dissolve, and the `EphemeraAreaId` that P1 needed *so wholes had somewhere to host* is no longer required for that purpose.

**It is AB-4's third option, which has been sitting in the row since it was written** --- "something that is not an edge at all." The row did not know it was the highest-leverage fork in the initiative; it does now. **The deciding sub-question is whether a whole can itself be a part of a bigger whole:** if composition nests and is expressed as graph edges, wholes must be graph nodes and the record option collapses. Second-order, and real: with no host, what makes a whole discoverable for cleanup when its parts are gone? Orphan-sweep shaped, with objects-lane precedent.

**Sequencing consequence, recorded on AB-4 and in the clause-targeted tests: answer AB-4 before running any clause 4 or clause 5 test.** Three queued cases currently target machinery that may not need to exist.

**AB-4 was conflating requirements with representation, 2026-08-06 --- and the observation is structural rather than local.** The row asks whether part-of is a membership relationship, a new edge kind, or not an edge at all: **entirely representation**, never *what must be expressible*. That mattered practically, because the two halves have different answerability --- requirements can be settled from the fiction with confidence, representation cannot --- so one row asking both left the confident half unreachable. Split as **AB-32** (requirements) blocking **AB-4** (representation), with a note in the Open-decisions preamble that most rows here are the second kind and a resistant row is worth checking for this before being treated as hard. **Splitting a conflated row is not breeding:** the count rises, the surface does not.

**First requirement answered: wholes nest, resoundingly.** And it **does not** kill the non-positional-whole candidate, contrary to the decider I had written for it --- which made this exact conflation in a single sentence, bolting "composition nests" (requirement) to "is expressed as edges" (representation). A record-based whole holds a member set of ids and an id can be another whole's, so nesting is free. **The decider restated as a requirement: must a relation ever attach to a whole rather than to one of its parts?** Every case so far says no --- C5 relates through the chain's ends, C6's candle sits under a span --- with "the place setting is on the table" as the untested shape.

**"Abstraction Fractal" proposed for AB-0, and it answers the original question rather than the live instance.** It came from the laddering requirement, and it earns its place by what it commits to: **a fractal is self-similar across scale, so if abstraction is fractal there is no `Abstraction` type at all** --- nothing distinctive at any level to type, just Objects at some scale related by part-of. That is AB-12's reframe, reached from the naming side. The Improvisation-names-two-things instance stays open.

**And the self-similarity caution was sharpened into a distinction worth keeping: [clean fractal, dirty implementation](#clean-fractal-dirty-implementation).** A philosophically clean fractal can be deliberately dirtied in implementation by human-scale anchoring, caches, fast paths and shorthands --- that is expected, and the clean model then serves as the **specification of correctness** with each deviation enumerated as an optimisation over it. **What the distinction protects against is the other kind of deviation:** a break in scale-invariance that is *conceptual*, which no implementation care removes. AB-13's DAG is that kind. AB-15's default binding level and the render cache are the first kind. AB-16 is unclassified and should be decided deliberately. The test is sharp: **a deviation that cannot be stated as "the clean answer, computed faster" is a model change wearing dirt's clothing.**

**The flashlight broke the non-positional-whole candidate, 2026-08-06 --- and the candidate was one day old.** Raised as a worry about naive perception: someone looks at Room A and sees a flashlight, looks *carefully*, its parts are improvised --- **is the flashlight now absent, replaced by a battery, a casing and a bulb?** Under the candidate as written, yes. And worse: the referent catalog reads the same `positionGraph.objectIds` as affordance compose, so **"pick up the flashlight" stops grounding**. Looking closely at a thing would make it unreferrable, while fictionally nothing happened at all. Written up as [C8](#c8-the-flashlight-that-vanished-when-someone-looked-at-it).

**The error was overgeneralisation from one part-kind, and the counter-example was already in the document.** The candidate was derived **entirely from C7** --- Ariadne's thread, an extent case --- and never checked against the dismantled flashlight, which has been seed example 2 since this plan was seeded. Third instance of the instance-taken-for-the-category error now in the catalogue, and the first where the disconfirming case was sitting in the same file the whole time. **Cheap corrective for next time: before generalising from a case, check the seed examples**, which exist precisely because they span the axes.

**What the break bought is worth more than the candidate lost: containment and extent disagree about which level is *positional*.** A flashlight is the thing in the room and its battery is inside it; Ariadne's thread is not in any room while each of its segments is somewhere. **Containment makes the parts non-positional; extent makes the whole non-positional.** That is AB-16's part-kind distinction surfacing as a disagreement about *what is present* --- not a difference of degree, and the most load-bearing property the position model has. Recorded as strong evidence for AB-16's "genuinely distinct third thing," with the separability premise flagged untested: a ship's rigging may be containment at the ship's scale and extent within it, and if a composition can be both, positional status cannot be read off the kind.

**The candidate survives, narrowed to extent.** Its consequence for clause 4 narrows with it: the LCA machinery is confined to **scattered containment** (a flashlight whose battery has been carried next door) rather than eliminated --- and that may be handled by dissolving the composition instead of hosting it.

**And a separable second finding: AB-9's guardrail has been contested by a case rather than by an argument.** "Look carefully" minting the flashlight's interior is **description acting as a write path**, which AB-9 forbids outright --- while AB-14 holds that operational necessity licenses minting and AB-20 that the player's own reference *is* the licence, and a player asking for detail plausibly qualifies. Both rows are defensible and they disagree. Flagged on both, to be resolved together. **Genuinely separable from the positional defect**, which stands even if a screwdriver had triggered the decomposition.

**The same-mechanism requirement, 2026-08-06 --- an argument from requirements that settled more than four constructed scenarios had.** Put as a dichotomy: *you can have a single scale-invariant processing rule or single-hosting, not both*, since a single-hosted spanning whole cannot be a node in each room it passes through, so reaching it needs a second path and a second path is an epicycle. **Substantially right**, and the first pressure on clause 1b to arrive from a requirement rather than a scenario --- which is the case for AB-32 existing, made two exchanges after the row was created.

**Three positions, not two, and the third is licensed by this plan's own [clean/dirty](#clean-fractal-dirty-implementation) vocabulary.** Multi-hosting the whole gives uniform lookup but is **not free** in a way the original framing omitted: C7 already showed one node cannot record where-along, so segments are still required, and room B then holds both a thread node and its local segment --- two nodes for overlapping things, with "pull the thread" ambiguous between them. **Single-hosting with *traversal* as the clean rule** breaks the dichotomy outright: reference resolves over local nodes plus reachable ancestors, running zero hops for an undecomposed flashlight and one for a thread, and **a loop that sometimes iterates zero times is one mechanism rather than two.** Its cost is inverting clean and fast --- today's `graph.objectIds` lookups become the optimisation and need a validity condition --- which is legitimate dirt by that section's own test. **Single-hosting with lookup as the clean rule** is the position the argument kills, correctly.

**So the durable form is narrower and stronger: you can have single-hosting or lookup-as-the-clean-rule, not both.** Scale-invariance survives single-hosting only if the clean rule is traversal-shaped.

**And the finding that matters most: the plan was already in the losing position, by accident.** Part-mediated reference was introduced as the answer *for spanning wholes* while local lookup remained the unexamined norm everywhere else --- two paths, never reconciled, created the moment the spanning case got its own answer. **The predicted epicycle was not a future risk but a present defect**, and it went unnoticed for two days because nothing forced the two reference paths to be compared. Fix recorded on the scheme itself: promote traversal to the general rule rather than discard it.

**Answered 2026-08-05, and it was the question that dissolved:** whether born-spanning things (a river, a cable run) mean changeable granularity is a property some things have rather than a lifecycle everything passes through. **It is a property some things have, and that turns out to cost nothing** --- under H1 a river is simply an extent whose parts never co-locate, so the hosting rule gives the same answer forever and no lifecycle is required. The question looked significant only while "born spanning" was being treated as a category; once the wall was separated out (AB-30), the remainder was ordinary. Kept here rather than deleted because the *shape* of the mistake is the reusable part.

## Verification

No code slice exists yet. Until Phase 6, verification is that the baseline still passes and that no durable doc has acquired normative text for unbuilt behavior:

```bash
cd lambda/ephemera && npm run test -- --watchAll=false \
  dataSource/positions/ \
  dataSource/actions/enrich/objectManipulation/
```

Phase 6 adds its own commands here.

---

## Progress

| Milestone | Status |
| --- | --- |
| Initiative named; seed examples and three axes recorded | Done (2026-08-05) |
| Open questions enumerated with constraint matrix | Done (2026-08-05) |
| Multi-host claim verified against code; contract contradiction found | Done (2026-08-05) |
| Granularity-change insight and its consequences recorded (AB-16, AB-17) | Done (2026-08-05) |
| Privileged-level reframe recorded as a candidate (AB-12, AB-13, AB-15) | Done (2026-08-05) |
| AB-14 reshaped to Improvisation / Recognition; AB-18, AB-19 split out | Done (2026-08-05) |
| Improvisation confirmed as the objects lane's mechanism generalized; three gaps named | Done (2026-08-05) |
| AB-14 re-axed onto criticality (not timing); AB-20, AB-21 split out | Done (2026-08-05) |
| AB-19 reframed (bound the LLM step, not replace it); Coyote prior art analyzed; AB-22 added | Done (2026-08-05) |
| `thinking` substrate verified (ledger shipped, queue absent); AB-23, AB-24 added; three tiers named | Done (2026-08-05) |
| Proposal P1 recorded and assessed; AB-25 (host tree) split out as its critical path | Done (2026-08-05) |
| Part connections corrected to adjacency-mediated graph (AB-26, AB-27); stale "ordering" claims fixed in 4 places | Done (2026-08-05) |
| Licensing rule widened to any kind + any provenance; AB-28 (mutable licenses) added | Done (2026-08-05) |
| AB-28 recognized as the shipped boundary-edge discipline; AB-29 (gaps + play-vs-asset bifurcation) split out | Done (2026-08-05) |
| Asymmetric dependency framed; data-blocked vs otherwise-blocked ladder parks split; Phase 0 points at them | Done (2026-08-05) |
| Ladder's data-blocked parks mined into corpus cases C1--C4; `multiPresent` entry gate found; AB-23's premise corrected | Done (2026-08-05) |
| P1 promoted to working hypothesis H1; tagging test, falsification cases and rollback triggers named; AB-12 named as candidate H2 | Done (2026-08-05) |
| Phase 0 corpus | **In progress** --- C1--C4 landed; the two H1 falsification cases are next, then seed examples, negative cases, cross-axis interactions |
| C5 (chain) run as H1's first falsification case; P1's same-event sub-claim struck; AB-17 split, AB-9 escalated, trigger 3 reweighted | Done (2026-08-05) |
| Born-spanning case dissolved under challenge; AB-30 opened (addressable but not a member); rollback trigger 2 replaced | Done (2026-08-05) |
| Part-mediated reference proposed and assessed; trigger 2 passed conditionally; AB-11 answered (no new step kind), AB-6 forced toward stored, AB-15 naming seam recorded | Done (2026-08-05) |
| C6 (rope + candle + timer) written; DAG established as a requirement for AB-13; AB-25 gains the no-level-indexing prohibition; AB-17 per-whole dissolution; AB-31 (fission) opened | Done (2026-08-05) |
| Falsification-case criteria written (discriminating / mechanism / interaction) after diagnosing why the constructed cases kept passing; four new cases queued; AB-8's hosts-of-different-kinds note reopened | Done (2026-08-05) |
| C7 (Ariadne's thread) run --- **passed discriminating test of H1 clause 3**, competitor failed outright; description-before-reference constraint found; AB-4 held-vs-present defect, AB-6 double question, AB-15 (c) corrected; falsification criteria gained a faithfulness clause | Done (2026-08-05) |
| H1 split into five separately-tested clauses; **clause 2 found wholly unexamined**; four clause-targeted tests specified | Done (2026-08-05) |
| Clause entailment found (1b -> 4 -> 5); clause 1 split into 1a/1b and the day-old cost reduction halved; **non-positional-whole candidate recorded**, which would remove 1b, 4 and 5 and take AB-25 off the critical path; AB-4 identified as the deciding fork | Done (2026-08-06) |
| AB-4 found to conflate requirements with representation; split as AB-32 (requirements) blocking AB-4; wholes-nest answered yes; candidate's decider restated as relation-attachment; **Abstraction Fractal** proposed for AB-0; clean-fractal/dirty-implementation distinction named | Done (2026-08-06) |
| C8 (flashlight) run --- **breaks the non-positional-whole candidate for containment**, narrowing it to extent; containment/extent found to disagree on which level is positional (AB-16); AB-9 guardrail contested by a case (AB-9/AB-14 collision) | Done (2026-08-06) |
| Same-mechanism requirement raised; three reference positions priced; dichotomy narrowed to *single-hosting or lookup-as-clean-rule*; **plan found already in the losing position** --- part-mediated reference left two unreconciled paths | Done (2026-08-06) |
| **AB-32 --- collect the composition requirements; blocks AB-4, which blocks clause 4/5 testing** | In progress --- two requirements answered (wholes nest; same reference mechanism); **next action** |
| Containment/extent separability --- can one composition be both (ship's rigging)? | Not started --- premise that AB-16's positional finding rests on |
| AB-25 --- H1's critical path and its live rollback trigger; now widened to admit characters as interior nodes | Not started; run after the character-held-end case, which feeds it directly |
| AB-1 resolved provisionally | Not started |
| Full provisional pass (Phase 2) | Not started |
| Consistency pass (Phase 3) | Not started |
| First vertical slice chosen | Not started |

## Lifecycle

Dispose once the design settles and its lasting content lives in [`positions/AGENT.concepts.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md) / [`AGENT.contract.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) / [`AGENT.implementation.md`](../../../../../lambda/ephemera/dataSource/positions/AGENT.implementation.md) --- **unless** Phase 7 concludes this is a multi-iteration ladder, in which case this file converts to a standing index (same posture as [`AGENT.objectManipulationIterations.planning.md`](../actions/AGENT.objectManipulationIterations.planning.md)) and its lifecycle note is rewritten accordingly.
