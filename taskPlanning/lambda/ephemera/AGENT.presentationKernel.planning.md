# Presentation kernel: positional state binding for narration

**Status:** In progress, 2026-07-30 --- Phase 0 (pure rename), Phase 1 (capture step), Phase 2 (abstract-op compile layer, navigate), and Phase 3 (connect/disconnect/ghost-purge migration; `MembershipPresentationFanInCluster` and `inferMembershipEmissionShape` retired) shipped; **Phase 3.5** (give the carry-closure sub-DAG a first-class result shape) next, then **Phase 3.6** (unify the two object-move execution modules, so Phase 4's object migration is written once), then Phase 4 (object take/drop onto the `Move` compiler). PB-8 and PB-9 --- the coupled pair asking how much of Expansion's output the abstract op should carry --- are **resolved 2026-07-30** ((d) rooted fragment; (i-b) Expansion keeps owning dissolve classification), so nothing gates Phase 3.5. Framed through conversation while closing out the `messageOrchestrationConsolidation` initiative (2026-07-25 to 2026-07-28; its task plan shipped all phases and was retired per [`taskPlanning/AGENT.md`](../../AGENT.md) --- the shipped mechanism is documented in [`dataSource/messageOrchestration/AGENT.md`](../../../lambda/ephemera/dataSource/messageOrchestration/AGENT.md)). This plan carries what that initiative scoped as its optional Phase 9 capstone ("generalize to a presentation `KernelStep[]` kernel"), **re-framed** around the organizing principle that actually motivates it: narration steps are *positionally* bound to world state, and the kernel walk is the only place that binding is available. Nothing below is built yet.

**Scope clarified 2026-07-29 (PB-L):** this plan does not build a *new* kernel. The presentation kernel's **describe branch already ships** as `perceiveStepSequence.ts` (misnamed "perception kernel"; see [`AGENT.perceptionKernel.planning.md`](dataSource/actions/AGENT.perceptionKernel.planning.md) Phase 3). This plan adds its **narration branch** plus the positional-capture mechanism that feeds it. The two documents are two halves of one kernel.

This document is task-scoped and follows [`taskPlanning/AGENT.md`](../../AGENT.md).

## Purpose

Four findings, confirmed against current code, together point at one change.

**1. The `[room, characterId]` targeting idiom is load-bearing in exactly one of its four occurrences.**

| Site | Is the trailing `characterId` doing work? |
| --- | --- |
| [`publishMembershipPresentation.ts:92`](../../../lambda/ephemera/dataSource/perception/publishMembershipPresentation.ts) (leave) | **Yes** --- the mover is already removed from `from` by commit time |
| [`publishMembershipPresentation.ts:111`](../../../lambda/ephemera/dataSource/perception/publishMembershipPresentation.ts) (arrive) | No --- the mover is in `to` |
| [`publishObjectManipulationPresentation.ts:56`](../../../lambda/ephemera/dataSource/perception/publishObjectManipulationPresentation.ts) | No --- take/drop move *objects*; the actor never left the room |
| [`publishObjectManipulationPresentation.ts:69`](../../../lambda/ephemera/dataSource/perception/publishObjectManipulationPresentation.ts) | No --- same |

`ROOM#` targets expand against the **live** roster at publish time ([`publishMessage/index.ts:83`](../../../lambda/ephemera/publishMessage/index.ts)), so post-commit the mover is missing from `from`'s roster and has to be re-added by hand. One occurrence out of four is a fix; three are a defensive tic copied across two narration families. Nothing at any call site distinguishes them.

**2. The world-state / transport seam already exists inside one function.** [`hydrateRoomRoster.ts:34-40`](../../../lambda/ephemera/internalCache/hydrateRoomRoster.ts) reads `Positions.getPositionGraph(roomId).characterIds` (world state), then hydrates each id with `CharacterSessions.get` (transport state). `ROOM#` expansion is *already* `positionGraph.characterIds` --- the same source the mutation kernel walks. Early binding does not introduce a split; it hoists the first half of an existing two-stage resolution to beat time and leaves the second half at flush.

**3. The kernel currently encodes a single, global *terminal* binding rule that narration cannot live under.** [`executeStepSequence.ts:40-46`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/executeStepSequence.ts) commits every mutation step, then perceives, and its header comment explicitly forbids callers relying on array order. That rule is right for `describe` steps (a description must reflect final committed state) and wrong for narration (a leave line must reflect the room the character was still standing in). This is the structural reason navigate's narration grew a separate fan-in path instead of joining the kernel.

**4. `inferMembershipEmissionShape` reconstructs information the plan already had.** [`membershipPresentationFanIn.ts:88-104`](../../../lambda/ephemera/dataSource/perception/membershipPresentationFanIn.ts) derives `leaveAndArrive` / `arriveOnly` / `leaveOnly` / `none` from `(froms, to)` endpoint data --- reasoning backwards to what kind of event occurred. Under a kernel step sequence, the shape *is* the plan (`[narrate-leave, move]` vs `[move, narrate-arrive]`), so the inference and the `MembershipEmissionShape` enum both dissolve. This is structurally the same discard-and-reconstruct pattern the messageOrchestration consolidation retired for *delivery ordering*, one layer down, applied to *event shape*.

**Not requirement-gated.** Navigate, connect, and disconnect all already route their mutations through [`commitStepSequence`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/commitStepSequence.ts) (see its header: `applyCharacterRoomMembership` (navigate/connect/disconnect)). The interleaving point exists for all three today; only the narration half is still outside.

## Design decisions (confirmed through conversation, 2026-07-28; PB-I/J/K added 2026-07-29)

- **PB-A. Narration steps are positionally bound; describe steps are terminally bound.** A narration step resolves its audience against graph state *at its own position in the walk*; a describe step resolves against final committed state. The unified kernel must carry both disciplines explicitly, per step kind --- collapsing them back into one rule in either direction reintroduces the bug this plan exists to remove.

- **PB-B. The binding-time rule, stated mechanically: early-bind a target iff resolving it requires reading *world state*; late-bind iff it requires reading *transport state*.** So `ROOM#` early-binds at beat time to a concrete `CHARACTER#[]`; `CHARACTER#` and `SESSION#` stay late-bound, because "which sessions does this character have connected" is a delivery concern, not a world-state one. `getRoomCharacterList` is the existence proof the two are separable --- it currently performs one of each, in that order.

- **PB-C. The only set-divergence introduced is concurrent third-party membership change, and early binding is the better answer there.** Beat-time and flush-time capture agree on everything the beat's own mutation does. They differ only when *another* invocation changes room membership between beat and flush --- and the audience for "Tess left" should be who was standing there at that beat, not who wandered in while an LLM was still generating a room header. This is a second bug fixed, not a tradeoff absorbed.

- **PB-D. Capture must be assignment, never append.** `applyStepSequenceCore` runs inside a `MultiKeyUpdate` reducer under `exponentialBackoffWrapper` ([`commitStepSequence.ts:115`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/commitStepSequence.ts)), so the reducer body can run several times. Every value the existing code retains across that boundary is an idempotent overwrite (`priorGraphs = graphs`, `committedGraphs = new Map(outcome.graphs)`). An accumulating `push` would duplicate narration across retry attempts.

- **PB-E. Anything retained past the reducer's return must be plain-copied.** The reason `committedGraphs` is `new Map(...)` and not the draft-derived value: `MultiKeyUpdate` reducers use Immer `produce()`, and retained draft-backed objects throw "revoked proxy" on later reads. Captured audiences must be plain arrays of ids.

- **PB-F. Capture ids only; hydration and validity stay late.** `hydrateRoomRosterFromCharacterIds` also drops characters whose `CharacterMeta` is missing (`{ check: true }`) --- a validity concern that is neither world nor transport. Beat-time capture snapshots a set of ids and validates nothing.

- **PB-G. Reordering execution is free, because delivery order is already decoupled.** The `messageOrchestration` bundle flushes in *declared* order and assigns `CreatedTime` itself ([`messageOrchestrationFanIn.ts`](../../../lambda/ephemera/dataSource/messageOrchestration/messageOrchestrationFanIn.ts)). Moving `narrate-leave` ahead of `move-mutation` in execution therefore does not perturb `[leave, header, arrive]` delivery at all. This is a direct payoff of the messageOrchestration consolidation and is what makes this change safe now --- before that work, reordering execution would also have reordered delivery.

- **PB-H. Presence and perspective are orthogonal; only presence is in scope.** Positional binding answers *who was where, when*. It does not answer *whether the actor receives their own event, and in what wording*. There is no actor/observer copy split anywhere today --- all narration is third person to one audience ([`publishObjectManipulationPresentation.ts:28-37`](../../../lambda/ephemera/dataSource/perception/publishObjectManipulationPresentation.ts)) --- so nothing needs one yet. Second-person copy is a natural `referent` build-in to the instruction set (an `ACTOR` / `!ACTOR` target kind, sibling to `!CHARACTER#`), **deliberately deferred**; see PB-4. Flagging it because the `[room, characterId]` idiom *looks* like a perspective mechanism and is not, and someone will otherwise try to solve perspective with the presence tool.

- **PB-I. Kernel plans are *compiled* from abstract operations, not hand-built per call site (added 2026-07-29).** Two levels, not one. An abstract op names *what happened in the world* (`Move(character, from, to)`); a **compiler** expands it into the kernel-ready sequence (`[narrate-leave, move, describe-header, narrate-arrive]`). Callers --- navigate, connect, disconnect --- emit abstract ops and never spell out narration themselves. Only the compiler knows that moving a character narrates, so "a move always brackets leave-then-arrive" becomes an invariant of one function rather than a convention re-derived at three sites. Purpose finding 1 is the precedent for why that matters: three call sites copied the same defensive `[room, characterId]` patch and only one of them needed it, precisely because nothing shared owned the decision. Note the compiler attacks `inferMembershipEmissionShape` from the *opposite* direction to Purpose finding 4: that function reasons **backwards** from `(froms, to)` endpoint data to an event shape, whereas the compiler holds the shape **forwards** from a named op and never has to infer it. Both routes delete it --- this one also prevents the next such inference from being written. The compile boundary is also where PB-2 (built copy vs. ingredients) is properly decided: intent data (`exitAware`, `home`, `connect`) is in hand at compile time, so it need not survive to flush.

- **PB-J. Positional capture is a first-class walk step, not a side-channel (resolves PB-5, 2026-07-29).** Neither of PB-5's two framings: not a sibling walker, and not a side-table of boundary indices threaded past a narrowly-typed `applyStepSequenceCore`. A capture enters the walk array itself.

  **The decisive reason is the footprint.** [`computeStepSequenceFootprint.ts:8-18`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/computeStepSequenceFootprint.ts) is the transaction lock-set declaration, and it is explicit that it must be computed once up front because `MultiKeyUpdate` "cannot be re-entered mid-reducer to lock a newly-discovered host." A capture reads a host's roster, so that host **must** be in the footprint or `graphs.get(hostId)` returns undefined inside the reducer. As an array step, the footprint picks it up automatically and it is structurally impossible to forget. As a side-table, every caller must remember to union capture hosts into the footprint --- an invariant that lives nowhere and is enforced by nothing. Worse, it would fail *late and selectively*: navigate happens to work, because the move already locks both rooms; the first break is a capture against an otherwise-unmutated host, in a feature written much later by someone who never read this plan.

  **The array's real membership test is "does this step need the walk's *position*?"** `describe` does not --- it is terminally bound (PB-A), owned by the perception kernel, and reads final state. Capture does, definitionally. The narrow-type boundary documented at [`kernelStep.ts:35-43`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/kernelStep.ts) was drawn *against `describe` specifically*, and describe's reasons do not transfer to capture. Treating that boundary as a founding principle rather than a scoped exclusion is a mistake this clause exists to prevent repeating.

  **Consequence: the walk's input type widens to admit capture.** Under PB-K's renaming this needs no new type at all --- `MutationKernelStep` means "what the mutation kernel accepts," so `MutationKernelCaptureStep` belongs inside it by definition. (An earlier draft of this clause introduced a separate `KernelWalkStep` purely to avoid `KernelMutationStep` becoming a lie; PB-K dissolves the need for it, which is the main reason that rename should land first.) All three consumers have exactly one production caller (`commitStepSequence`), so blast radius is one file, and each new case is meaningful rather than boilerplate: `applyStepSequenceCore` records the roster and continues; `computeStepSequenceFootprint` contributes the `hostId`, which is the *feature*; `factsForStep` yields nothing, because a capture is not a world event.

  **The half of the old boundary worth keeping, stated normatively:** a capture step carries `hostId` + `captureId` and **no write payload**, so it cannot contribute to the `transactWrite` write set. A read-only step is safe inside the mutation walk *only* under that shape constraint --- enforce it by shape, not by exclusion.

  **Narration itself never enters the walk** --- but it *is* a `KernelStep`. The compiler emits narration steps that reference a `captureId`; the **presentation kernel** (PB-L) filters them out of the shared list post-commit and builds copy and targets from the captured id sets. Because the capture sits at its own position in the walk array, `captureId` carries *identity* only, never position --- which removes index-correlation between the compiler's outputs entirely. (An earlier draft of this clause described that consumer as a bespoke "sibling assembler," which left it unclassified; PB-L identifies it as one branch of a kernel that already exists.)

- **PB-K. `Kernel` is not an unambiguous prefix; qualify per-kernel types with the kernel that owns them (2026-07-29).** There are exactly two kernels --- mutation and presentation (PB-L) --- so `Kernel` alone identifies neither.

  **`KernelStep` stays unprefixed --- it is the one name currently doing its job.** [`kernelStep.ts:46-54`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/kernelStep.ts) defines it as the *shared, cross-kernel* instruction vocabulary that "each kernel filters this shared list down to the steps it owns." It belongs to no single kernel, so it takes no kernel's name. Prefixing it would destroy the only distinction the current scheme gets right.

  **The actual defect is that the shared union's two halves are named on different schemes** --- `KernelMutationStep` on one side, `ExecutorDescribeStep` on the other. Nothing in either name says they are siblings, or that each is one kernel's filtered view of `KernelStep`.

  | Current | Becomes | Note |
  | --- | --- | --- |
  | `KernelStep` | *unchanged* | shared, cross-kernel; correctly unprefixed |
  | `KernelMutationStep` | `MutationKernelStep` | widened to include capture (PB-J) |
  | `ExecutorDescribeStep` | member of new `PresentationKernelStep` | `PresentationKernelStep = ExecutorDescribeStep \| <narration step>` (PB-L). **Do not rename `ExecutorDescribeStep`** --- it is owned by `executorTypes.ts` and reused verbatim; renaming it would steal it from the executor. Until the narration step exists this union has one member, which is fine --- it names the *filter*, not the arity |
  | `perceiveStepSequence` / `PerceiveStepSequenceDeps` | `presentStepSequence` / `PresentStepSequenceDeps` | the shipped describe-half of the presentation kernel (PB-L); 36 occurrences across 7 files |
  | `KernelTransferMembershipStep` | `MutationKernelTransferStep` | cannot simply drop the prefix --- `TransferMembershipStep` is already taken by the executor |
  | `KernelApplyOutcome`, `KernelCommitResult`, `KernelDropped` | `MutationKernel*` | all mutation-kernel-specific |
  | *(new, PB-J)* | `MutationKernelCaptureStep` | |

  **Scale:** 127 occurrences across 20 files, 88% of it `KernelMutationStep` (83) and `KernelStep` (29); all but five consumer files live under `positions/manipulation/kernel/`.

  **Honest weighting:** the `Kernel*` --> `MutationKernel*` transposition is the *least* valuable part --- `KernelMutationStep` vs. `MutationKernelStep` is a subtle flip that buys little alone. The value is in `PresentationKernelStep` (which makes the two halves visible as siblings, and gives the narration step a home to be added to) and in `MutationKernelStep` absorbing capture (which deletes a type PB-J would otherwise need). The transposition is what makes those two coherent, not a win on its own.

- **PB-L. There are exactly two kernels; the presentation kernel has a describe branch and a narration branch (2026-07-29).** Both publish into the player's transcript, and both filter the shared `KernelStep[]` post-commit. The describe branch **already ships** --- [`perceiveStepSequence.ts`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/perceiveStepSequence.ts), built by [`AGENT.perceptionKernel.planning.md`](dataSource/actions/AGENT.perceptionKernel.planning.md) Phase 3. This plan builds the narration branch and the capture mechanism that feeds it. **The two plans are two halves of one kernel, not two kernels.**

  | Kernel | Filters | Runs |
  | --- | --- | --- |
  | mutation | mutation + capture steps | in the walk, inside the transaction |
  | presentation | describe + narrate steps | after commit |

  **"Perception" was the wrong word and "presentation" is the right one, on the codebase's own usage.** Every `*Presentation*` identifier in production is narration publishing --- `publishMembershipPresentation` (leave/arrive), `publishObjectManipulationPresentation` (take/drop), the fan-ins, the label resolvers; 10 of 13 such files live in `dataSource/perception/`. So the repo already draws the line this clause needs: **perception** is the broad experience category *and a data source's name*; **presentation** is specifically publishing something into the transcript. Naming a kernel "perception" claimed a data source's territory and, worse, implied narration should route through it terminally --- the exact opposite of PB-A. Presentation is a step-kind category, parallel to mutation.

  **Consequence for PB-A, stated precisely:** positional vs. terminal binding is *not* about when publishing happens --- both branches publish post-commit. It is about **where the state came from**: a describe step reads final committed state; a narrate step reads a roster captured mid-walk (PB-J). Do not restate PB-A as an ordering rule.

  **Naming it "presentation" now rather than after the narration branch lands** is deliberate: the word is not false today (publishing a description *is* presentation, just a narrow slice), the widening is two phases out and concrete, and an interim "describe kernel" name would live about a week. The cost is that the name briefly overclaims; the alternative is renaming shipped code twice.

- **PB-M. Narration is a room's changelog, not a mover's itinerary (added 2026-07-30).** The requirement, stated plainly: *if an entity leaves a room, enters a room, or repositions within a room, the characters in that room are notified.* **Whether** to narrate reduces to "did this host's position graph change"; **how** reduces to "what was the operation, and what kind of intent produced it." Nothing else is doing real work in that decision.

  **The frame mismatch is what generates the epicycles.** `compilePositionKernelOp` compiles from the *mover's* perspective --- one `entityId`, its `froms`, its `to` --- while the requirement is stated from the *host's*. Every ad-hoc branch written against narration so far sits exactly where those two frames disagree, and each was reasoned out locally instead of being read off the requirement.

  **The code already half-agrees, which is the evidence this is the codebase's grain and not an idea imposed on it.** [`compilePositionKernelOp.ts:65-87`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/compile/compilePositionKernelOp.ts) is already host-shaped on the leave side: it maps over `op.froms` emitting one capture, one narrate, and one slot *per host*. That is `[...leave-messages-for-rooms-with-departures]` verbatim. The arrive side is singular only because `to` is singular --- not because arrival is a different kind of thing.

  **What this collapses:**
  - **Object take/drop is not a special single-narrate case** --- see Phase 4. An earlier draft of this plan reasoned that bracketing leave/arrive for an object "would produce one real audience and one structurally-guaranteed-empty one," and introduced a discriminated branch to avoid the empty side. That is backwards: the empty side is the *correct output* of a uniform rule. A character-hosted inventory graph has no roster, so its capture is empty and its narrate step reports nothing --- and the fan-in's deliberate tolerance of unresolved slots (PB-G, `messageOrchestration/AGENT.md` "Publish behavior") is precisely what makes that cost nothing. A structurally-guaranteed no-op became a union member because the reasoning started from "which entity moved" rather than "which host changed."
  - **`inferOperationFromFact` dies for a better reason than Phase 4 first gave.** Take-vs-drop stops being host-*kind* inference and becomes "which side of the delta was the room."
  - **The relational half stops being permanently out of scope.** Bullet three --- reposition within a host --- is the third member of this family, and the cost of not having it is visible: `ObjectRelationalPresentationFanInCluster` maintains its own leg types, cluster identity, endpoint-compatibility matcher and publish path ([`objectManipulationPresentationFanIn.ts`](../../../lambda/ephemera/dataSource/perception/objectManipulationPresentationFanIn.ts)), a parallel copy of machinery that exists only because "reposition" has no vocabulary in a leave/arrive op. Still out of scope for Phase 4 (PB-3 resolved take/drop only), but it is now a *deferral*, not a boundary.

    **Deferred to a follow-on initiative, deliberately and with a reason (decided 2026-07-30):** relations are much less developed than membership --- which cuts both ways. There is less existing behavior to reason *from*, and correspondingly less legacy accretion to sift *through*. Both halves argue for the same ordering: get membership's epicycles under control first, then approach relations from a simplified vantage where the question is "what *should* reposition narration be" rather than "what is it currently, and which parts of that were accidents." Folding relational into this initiative would invert that --- it would force the design of the less-understood half while the better-understood half is still mid-migration. **Do not read the deferral as low value**; PB-M is precisely the argument that relational narration belongs in this family, and PB-10 is a question that only that follow-on can answer.

  **Two things this framing deliberately does not reach --- recorded so it is not over-applied:**
  - **It explains PB-8 rather than removing it.** Graph delta answers *whether* exactly, but **delta cardinality is not event count**: a tray plus three items is four node removals and one sentence. The delta cannot locate the event boundary; the rooted fragment can. PB-8(d) survives, with a better justification than it had.
  - **The header is not a member of the list.** It is audience-of-one, addressed to the *mover*, not to witnesses --- perspective, not narration (PB-H). `[leave, header, arrive]` is a delivery-order preference (PB-G), and folding the header in structurally would re-confuse the two axes this plan has twice had to separate.

## Getting started

1. Skim [`taskPlanning/AGENT.md`](../../AGENT.md) (durability ladder, open-decisions litmus tests, Recommended order checkbox convention).
2. Read the mechanism this builds on: [`dataSource/messageOrchestration/AGENT.md`](../../../lambda/ephemera/dataSource/messageOrchestration/AGENT.md) --- in particular "Publish behavior" (the bundle assigns `CreatedTime` in declared order, which is what decouples execution order from delivery order, PB-G) and its closing note that a kernel step requesting a *header* slot is still undesigned.
3. Read the kernel this extends, in this order:
   - [`kernelStep.ts`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/kernelStep.ts) (step vocabulary and the mutation/non-mutation split)
   - [`applyStepSequenceCore.ts`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/applyStepSequenceCore.ts) (the ordered pure walk over an immutable graph map --- the mechanism this plan hangs capture on)
   - [`commitStepSequence.ts`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/commitStepSequence.ts) (the `MultiKeyUpdate` reducer and retry boundary --- PB-D/PB-E live here)
   - [`executeStepSequence.ts`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/executeStepSequence.ts) (today's global terminal-binding rule)
4. Read the narration being migrated: [`membershipPresentationFanIn.ts`](../../../lambda/ephemera/dataSource/perception/membershipPresentationFanIn.ts) + [`publishMembershipPresentation.ts`](../../../lambda/ephemera/dataSource/perception/publishMembershipPresentation.ts), and the second family [`publishObjectManipulationPresentation.ts`](../../../lambda/ephemera/dataSource/perception/publishObjectManipulationPresentation.ts).
5. Read the hand-rolled plan construction the compile layer replaces (PB-I): [`orchestrateNavigate.ts:44-70`](../../../lambda/ephemera/dataSource/positions/navigate/orchestrateNavigate.ts) --- one `inferMembershipEmissionShape` call feeding two hand-built lists (steps and `MessageOrchestrationSlotSpec[]`). This is the concrete shape of what PB-6/PB-7 are deciding.
6. Read the target-resolution seam: [`hydrateRoomRoster.ts`](../../../lambda/ephemera/internalCache/hydrateRoomRoster.ts) and [`publishMessage/index.ts`](../../../lambda/ephemera/publishMessage/index.ts)'s target expansion.
7. Durable doc destinations for shipped rules: [`dataSource/positions/AGENT.contract.md`](../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) (normative binding rule), [`AGENT.concepts.md`](../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md) (presence-vs-perspective vocabulary), [`AGENT.implementation.md`](../../../lambda/ephemera/dataSource/positions/AGENT.implementation.md) (paths and behavior).
8. Testing authority: [`lambda/ephemera/AGENT.testing.md`](../../../lambda/ephemera/AGENT.testing.md). If commands conflict, that document wins. There is no `taskPlanning/lambda/ephemera/AGENT.development.md`.
9. Baseline (run from `lambda/ephemera`, should pass before edits):

```bash
cd lambda/ephemera && npm run test -- --watchAll=false \
  dataSource/positions/manipulation/kernel/ \
  dataSource/positions/navigate/ \
  dataSource/perception/ \
  dataSource/messageOrchestration/
```

## Recommended order

Use `[ ]` for pending and `[X]` for complete. Mark nested lines as you finish each sub-step. Nothing below is built yet; all lines start `[ ]`.

- [X] **Phase 0. Rename per-kernel types and the presentation kernel itself (PB-K, PB-L). Pure rename --- no behavior change.**
  - [X] Apply PB-K's table. `KernelStep` is deliberately untouched; everything mutation-kernel-specific gains the `MutationKernel` prefix; `PresentationKernelStep` is added as a union *containing* `ExecutorDescribeStep`, which is not itself renamed. (`KernelDropped` in PB-K's table turned out not to exist in shipped code --- nothing to rename there.)
  - [X] Rename the shipped describe branch: `perceiveStepSequence` --> `presentStepSequence` (+ `PerceiveStepSequenceDeps` --> `PresentStepSequenceDeps`, and the file itself), and correct its doc comment, which previously called itself "iteration 9/Phase 3's perception kernel." Mutation-half and presentation-half renames landed together.
  - [X] **Landed as its own commit, before Phase 1.** Mechanical rename across 21 files (20 identified pre-change + the renamed test file's sibling); `npx tsc --noEmit` clean; full suite passes at the same 330 passed / 1 skipped baseline with zero test file behavior changes (renames only).
  - [X] Updated the doc comments that explain *why* the mutation types are narrow, and the "perception kernel" framing, in `kernelStep.ts` and `presentStepSequence.ts`. `applyStepSequenceCore.ts`, `computeStepSequenceFootprint.ts`, `factsForStep.ts`, `applyCharacterRoomMembership.ts` only referenced the renamed identifiers directly (no naming-rationale prose to update beyond the mechanical rename).
- [X] **Phase 1. Add a capture step kind to the kernel walk (PB-J).**
  - [X] Add `MutationKernelCaptureStep` (`hostId` + `captureId`, **no write payload**) as a member of `MutationKernelStep` (`kernelStep.ts`). Under PB-K no new union type is needed --- "what the mutation kernel accepts" already names it. Note narration is *not* a walk step and never enters the kernel (PB-J), so this phase's original "narration step kind" framing is superseded.
  - [X] Update the three consumers, each of which has exactly one production caller (`commitStepSequence`): `applyStepSequenceCore` (snapshot the host's `characterIds`, continue), `computeStepSequenceFootprint` (contribute `hostId` to the lock set), `factsForStep` (yield nothing --- a capture is not a world event).
  - [X] Return captured rosters as plain `EphemeraCharacterId[]` keyed by `captureId`, on the `legal` verdict. PB-F makes PB-E free here: ids are primitive strings, so no draft-backed object survives the reducer's return by construction rather than by discipline.
  - [X] Honor PB-D (assignment, not append) at the reducer boundary. A test that forces a reducer retry and asserts captures are not duplicated is the one that actually proves this.
  - [X] Tests: a capture before a mutation sees the entity still present; the same capture after it does not; **a capture naming a host no mutation touches still locks that host** (the footprint case --- this is the test that would have caught the side-table design); a failed/illegal commit discards captures entirely.
  - [X] **Landed as its own commit, after Phase 0.** `MutationKernelStep` widened (`kernelStep.ts`) with `isKernelMutationStep` guard updated; `MutationKernelApplyOutcome`'s `legal` branch and `MutationKernelCommitResult`'s `ok: true` branch widened with a new `MutationKernelCaptures` type (`types.ts`); `applyStepSequenceCore`, `computeStepSequenceFootprint`, `factsForStep`, `commitStepSequence` each updated with the capture case. `commitStepSequence` assigns `committedCaptures = new Map(outcome.captures)` fresh on every `MultiKeyUpdate` reducer invocation (PB-D). 12 files touched (6 implementation + 6 test); full suite passes at 330 passed suites / 2562 passed tests / 9 skipped; `npx tsc --noEmit` clean. No production caller emits a capture step yet --- that's Phase 2.
- [X] **Phase 2. Introduce the abstract-op compile layer, with navigate as its first consumer.**
  - [X] Added the abstract op vocabulary (`PositionKernelMoveOp`, `manipulation/kernel/compile/positionKernelOp.ts`) and the compiler (`compilePositionKernelOp`, `manipulation/kernel/compile/compilePositionKernelOp.ts`) that expands an op into `{ steps, slots }`. Resolved, revised from the original framing during implementation planning:
    - **PB-6:** the op is shaped at the component-referent level the instruction planner sees (`entityId: EphemeraObjectId | EphemeraCharacterId`, hosts as `EphemeraMembershipHostId`), mirroring `MutationKernelTransferStep`'s own already-shipped entity-kind generalization (BD-36) --- not a character-only, room-only op. `narration` is optional on the op so the same `Move` shape covers both narrating (navigate/home today) and non-narrating (object-lifecycle) moves; the compiler, not the op's shape, decides whether/how a move narrates.
    - **PB-7:** the compiler emits one `CompiledPositionKernelPlan { steps, slots }` per call, from one op, in one pass --- exactly one place `[leave, header, arrive]` ordering is decided. `slots` includes the header slot unconditionally (independent of `narration`), since header rendering is a separate, already-shipped mechanism (`registerIngressSlot`/`kickPassiveRenderRequestedForCharacterInRoom`), not the presentation kernel's `describe` branch --- the compiler therefore never emits a `describe` step for the header (an earlier planning draft assumed it would; doing so would have fired a second, conflicting render request).
  - [X] Expressed navigate as the op `Move(entityId, froms, to)` via `navigate/buildNavigateMoveOp.ts` (a faithful port of `leaveCopyKindForFrom`/`buildMembershipEmissionPlan`'s copy-kind selection, PB-2/PB-I). Compiles to capture-from steps, the transfer step, a capture-to step, narrate-leave/narrate-arrive steps --- replacing `orchestrateCharacterNavigate`'s hand-rolled shape inference and slot-list computation (former `orchestrateNavigate.ts:44-70`). **Compiled twice, deliberately:** once pre-commit in `executeCharacterNavigate.ts` (mutation-only steps, via `applyCharacterRoomMembership`'s new `compileMutationSteps` callback, invoked once the coordinator's own `planMembershipTransfer` resolves the diff), once post-commit in `orchestrateCharacterNavigate` (full plan, with the header slot resolved and captures threaded in, to declare `slots` and report narration). Both calls agree on capture ids because `compilePositionKernelOp`'s capture-id generation is a pure function of `froms`/`to` alone, never of narration content.
  - [X] Added the **narration branch** to the presentation kernel: `PresentationKernelNarrateStep` (`kind: 'narrate'`, single flat tag) joins `PresentationKernelStep`/`KernelStep` (`kernelStep.ts`), and `presentStepSequence` filters it (`isNarrateStep`) alongside `describe`, resolving targets from the commit's captured id sets (`captures` parameter) and building copy at flush time via `buildMembershipLeaveSuffix`/`buildMembershipArriveSuffix` (reused verbatim). **`kind` carries no leave/arrive split** (revised during implementation, per user 2026-07-30): no kernel-walk consumer (`isKernelMutationStep`, the footprint, the mutation walk) ever needs to distinguish a leave narration from an arrive one, so a `direction: 'leave' | 'arrive'` data field carries that instead --- mirroring `describe`'s own shape in the same file (`kind: 'describe'` at the walk-dispatch level, `referentKind` as the nested classifier only the describe handler reads). **That split was then made structural** (second revision, per user 2026-07-30): the copy-generator's ingredients live nested under a `narration: NarrationSpecification` field, leaving the step's flat fields as exactly the delivery half (`captureId`/`roomId` resolve the audience, `bundleId`/`slotId` route the report). `NarrationSpecification` is a discriminated union on narration *family* (`kind: 'membershipMove'` today, `direction` a member field within it), deliberately not on `direction` --- family is the axis a second member actually arrives along, since Phase 4's object take/drop narration (PB-3) would share none of the membership fields. Phase 4's diff is therefore purely additive: one union member, one `case` in `presentStepSequence`'s `buildNarrationCopy`. **The step's `roomId` was dropped in the same pass** (third revision, per user 2026-07-30, who asked what it was still doing there): `targets: [roomId, ...audience]` was unioning the capture with a bare `ROOM#` that re-expands against the *live* roster at flush (`publishMessage/index.ts`'s `getRoomCharacterList`), i.e. terminally --- PB-A's distinction collapsed, and the same defect class as the `[room, characterId]` tack-on this phase retired, entering from the other end (that patch existed because live expansion dropped the mover; we captured the roster to fix it, then kept the live expansion alongside). `captureId` is now the sole audience input, and an unresolvable one **throws** rather than degrading to a room target or an empty audience --- ids are minted only by the compiler, so a miss is an internal inconsistency, and either quiet recovery would hide it behind a silently-wrong or silently-absent delivery. Verified not a production hole first: `commitStepSequence`'s success result types `captures` as required, and the optional `captures?` further up the navigate call chain serves connect/disconnect, which compile no narrate steps and so never reach the throw. **Polymorphic classes considered and deferred, with the trigger recorded** in `kernelStep.ts`'s `NarrationSpecification` doc: escalate only when multiple distinct operations switch over the union *from separate files*, **and** family count churns faster than operation count, **and** per-family modules can't already provide the cohesion --- heavy per-family capture logic reads like class pressure but is cohesion pressure, answered by a family-named module plus a `buildCopy(narration, captures)` signature change. The case a closed union genuinely cannot serve is families contributed by code that doesn't own the union (asset-authored narration). Cost side: these specs ride inside `KernelStep[]` through structural test comparison and Immer-adjacent reducer code, which plain data survives and class instances do not. **PB-2 resolved as ingredients, not pre-built copy** (revised from the original planning draft, per user 2026-07-30): the narrate step carries `characterName`/`copyKind`/`exitName`, and `presentStepSequence` assembles the message string, leaving room for later slices to let copy react to what the mutation actually did rather than only what compile-time intent expected.
  - [X] Shipped the compiler with exactly one consumer (navigate); connect/disconnect/home are untouched (`compileMutationSteps`/`narration` both absent on their call sites, so `applyCharacterRoomMembership`/`orchestrateCharacterNavigate` fall back to their pre-Phase-2 behavior byte-for-byte). Phase 3 will prove the second/third consumer without a signature change to `compilePositionKernelOp` or `PositionKernelMoveOp`.
  - [X] Retired the load-bearing `[from, characterId]` patch **for navigate specifically** --- the captured audience now includes the mover by construction. `publishMembershipPresentation.ts:92`/`111` still carried the patch at the time, exercised only by connect/disconnect/home and never by navigate, since navigate's `Character Moved` fact now carries `narratedInline: true` and was dropped by `membershipPresentationLegAdapters.ts` before it ever reached that fan-in. (Phase 3 subsequently deleted the patch, the fan-in, and `membershipPresentationLegAdapters.ts` itself entirely --- see Phase 3's own notes below.)
  - [X] Confirmed delivery order is untouched (PB-G): the bundle still declares and flushes `[leave, header, arrive]` from `slots`, independent of `steps`' internal ordering.
  - [X] Tests: mover receives their own leave line; arrival-room occupants do not receive the leave line; departure-room occupants do not receive the arrive line (`presentStepSequence.test.ts`, narration branch describe block) --- plus new coverage in `compilePositionKernelOp.test.ts` (steps/slots ordering, arity-driven connect/disconnect shapes, ingredients-not-message), `executeCharacterNavigate.test.ts` (`compileMutationSteps` produces `[capture, transferMembership, capture]`), `applyCharacterRoomMembership.test.ts` (`compileMutationSteps`/`narrationHandledInline`/`captures` pass-through, and the unset-default path unchanged), and `membershipPresentationLegAdapters.test.ts` (`narratedInline: true` drops the fact leg).
  - [X] **Landed as its own commit, after Phase 1.** New files: `manipulation/kernel/compile/positionKernelOp.ts`, `manipulation/kernel/compile/compilePositionKernelOp.ts` (+ test), `navigate/buildNavigateMoveOp.ts`. Modified: `kernelStep.ts` (`PresentationKernelNarrateStep`, widened `KernelStep`/`PresentationKernelStep`, `isNarrateStep`), `presentStepSequence.ts` (narration branch, new `messageBus`/`captures` params), `executeStepSequence.ts` (threads `captures` through), `membership/types.ts` (`compileMutationSteps`, `narrationHandledInline`, `captures` on `MembershipApplyArgs`/`MembershipApplySuccessResult`), `applyCharacterRoomMembership.ts`, `executeCharacterNavigate.ts`, `afterCharacterMembershipNavigateChanged.ts`, `orchestrateNavigate.ts`, `positions/index.ts` (threads `intentKind`/`intentFromRoomId`/`exitName` from ingress), `positions/publishedEvents.ts`/`membership/buildCharacterMovedFact.ts`/`manipulation/kernel/factsForStep.ts`/`manipulation/kernel/commitStepSequence.ts` (`narratedInline` threading), `perception/membershipPresentationLegAdapters.ts` (fact-leg suppression), `dataSource/actions/index.ts` (added required `messageBus` dep to an existing `presentStepSequence` call site). Full suite passes at 331 passed suites / 2577 passed tests / 9 skipped (up from the Phase 1 baseline); `npx tsc --noEmit` clean.
- [X] **Phase 3. Migrate connect/disconnect through the same compiler; dissolve the shape inference.**
  - [X] Widened the shared narration-ingredient builder rather than adding a third hand-rolled bracketing: `positions/navigate/buildNavigateMoveOp.ts` renamed and relocated to `positions/membership/buildMembershipMoveOp.ts`, `intentKind` widened from `'navigate' | 'home'` to `'navigate' | 'home' | 'connect' | 'disconnect'`. Connect/disconnect's copy-kind branches are fixed values (`'connect'`/`'disconnect'`) rather than the exit-aware/matches-intent-from selection navigate/home need --- that machinery is simply never exercised on connect's empty `froms` or disconnect's null `to`.
  - [X] **Connect** reuses navigate's existing post-commit tail unchanged: `handleCharacterConnected` (`handleConnectionsCharactersPresence.ts`) mints a `bundleId`, compiles via `buildMembershipMoveOp({..., intentKind: 'connect'})` + `compilePositionKernelOp(...).steps.filter(isKernelMutationStep)` as `compileMutationSteps`, sets `narrationHandledInline: true`, and threads `intentKind: 'connect'` through `afterCharacterMembershipNavigateChanged` into `orchestrateCharacterNavigate` (which already declares the header slot connect legitimately wants, since it always has a destination room). `OrchestrateCharacterNavigateArgs.intentKind`/`AfterCharacterMembershipNavigateChangedArgs.intentKind` widened accordingly.
  - [X] **Disconnect** does not reuse `orchestrateCharacterNavigate` --- its header-render/`registerIngressSlot`/`Perception`-fallback logic only makes sense with a destination room, which disconnect never has. New `positions/membership/orchestrateCharacterDisconnect.ts`: declares the bundle when the compiled plan produced slots, presents the compiled narrate-leave step(s), nothing else. `handleCharacterDisconnected` fetches `characterMeta` (not needed before this phase), mints a `bundleId`, sets `compileMutationSteps`/`narrationHandledInline: true`, and calls `orchestrateCharacterDisconnect` on a changed, non-empty-`froms` result.
  - [X] **Ghost-purge migrated too, not left on the old fan-in** (scope correction found during implementation planning): `repairRoomOccupancyDrift.ts`'s ghost-purge sweep calls `applyCharacterRoomMembership({characterId, targetRoomId: null}, ...)` --- structurally identical to a real disconnect, and was the actual remaining fact-only caller PB-1 needed to account for (not `repairCharacterLegalPlacement.ts`, which the original PB-1 text named but which was already fine --- it routes through `executeCharacterNavigate`'s Phase 2 kernel-compiled plan). Ghost-purge now shares `buildMembershipMoveOp`/`orchestrateCharacterDisconnect` with real disconnect, using the identical `intentKind: 'disconnect'` and identical "has disconnected" copy --- a ghost session genuinely has disconnected, so no separate drift/ghost narration variant was needed.
  - [X] Proved PB-I: connect, disconnect, and ghost-purge all reach their step sequences through `compilePositionKernelOp` via `buildMembershipMoveOp`; no call site constructs a `{ kind: 'narrate', ... }`/`{ kind: 'capture', ... }` literal directly (verified by grep, see Verification below).
  - [X] Deleted `inferMembershipEmissionShape` and `MembershipEmissionShape` from `membershipPresentationFanIn.ts` (verified zero remaining references by grep).
  - [X] Deleted `MembershipPresentationFanInCluster` entirely, plus everything that existed only to feed it: `membershipPresentationClusterFromLeg`, `createMembershipPresentationFanInStore`, `createMembershipFanInHandlerContext`, `buildMembershipEmissionPlan`, `MembershipEmissionPlan`, `MembershipPresentationLeg`/`MembershipIntentLeg`/`MembershipFactLeg`, the `publishMembershipPresentation` publish function (its `buildMembershipLeaveSuffix`/`buildMembershipArriveSuffix` suffix builders survive, reused verbatim by `presentStepSequence.ts`), the fan-in wiring in `perception/index.ts`, and `perception/membershipPresentationLegAdapters.ts` in its entirety (including its subscription guards, removed from `isPerceptionSubscribedEnvelope`'s composition in `subscribedEvents.ts` so `Character Navigate`/`Character Home`/`Character Connected`/`Character Disconnected`/`Character Moved` events are no longer routed to the perception data source at all --- nothing there needs them anymore).
  - [X] Tests: connect narrates "has connected" with the captured post-connect roster as audience; disconnect and ghost-purge both narrate "has disconnected" identically, via the shared path; a `compileMutationSteps` callback for connect yields `[transferMembership, capture]` (no capture-from, since `froms` is empty); `orchestrateCharacterDisconnect` is a no-op when `froms` is empty and hard-throws on a missing capture (mirrors `presentStepSequence`'s own internal-consistency guard); `buildMembershipMoveOp`'s new connect/disconnect branches unit-tested directly, alongside a regression check that navigate/home's existing copy-kind selection is unchanged by the widened `intentKind` union. Retry/idempotency (PB-D) is covered generically at `commitStepSequence.test.ts`'s existing capture-retry test --- that mechanism is step-kind-agnostic, so it already proved connect/disconnect's capture steps too; not duplicated per-caller.
  - [X] **Landed as its own change set, after Phase 2.** New files: `positions/membership/buildMembershipMoveOp.ts` (+ test, renamed/relocated from `navigate/buildNavigateMoveOp.ts`), `positions/membership/orchestrateCharacterDisconnect.ts` (+ test). Modified: `positions/handleConnectionsCharactersPresence.ts` (+ test), `positions/navigate/afterCharacterMembershipNavigateChanged.ts`, `positions/navigate/orchestrateNavigate.ts`, `positions/navigate/executeCharacterNavigate.ts` (import path only), `positions/membership/repairRoomOccupancyDrift.ts` (+ test), `positions/manipulation/kernel/commitStepSequence.ts` and `positions/publishedEvents.ts` (stale doc-comment fixes for `narratedInline`), `perception/publishMembershipPresentation.ts` (+ test, trimmed to the two suffix builders), `perception/index.ts`, `perception/subscribedEvents.ts` (+ test fix). Deleted: `perception/membershipPresentationLegAdapters.ts` (+ its test), `perception/membershipPresentationFanIn.test.ts`. **Follow-up in the same session (user-prompted):** once trimmed to its one remaining export, `perception/membershipPresentationFanIn.ts` was itself judged too small to justify its own file --- `MembershipEmissionCopyKind` relocated into `positions/manipulation/kernel/kernelStep.ts` (next to `MembershipNarrationSpec`, its only structural consumer) and the file deleted; `positionKernelOp.ts`, `buildMembershipMoveOp.ts`, and `publishMembershipPresentation.ts` updated to import from there instead. Full suite passes at 331 passed suites / 2526 passed tests / 9 skipped; `npx tsc --noEmit` clean.
- [ ] **Phase 3.5. Give the carry-closure sub-DAG a first-class result shape (PB-8/PB-9 enabler). Behavior-preserving --- no narration, compiler, or presentation changes.**
  - [ ] Carved out as its own phase rather than a Phase 4 sub-item, on **Phase 0's precedent**: an enabling refactor whose gate is "full suite passes at the same baseline, zero behavior change" only carries that signal if it lands separately. Buried inside Phase 4 --- which already carries op widening, a compiler branch, a new narration family, bundle introduction, and three deletions --- a red suite would not say which half broke it.
  - [ ] **The function is already shared --- do not move it.** [`interactionUnderTransfer.ts`](../../../lambda/ephemera/dataSource/positions/positionGraph/expandValidate/interactionUnderTransfer.ts) lives in `positions/positionGraph/expandValidate/`, not inside Synthesize, and `lookupOrComputeClosure` is only a memoizing wrapper over it. What is missing is generality of *output*, not of *location*: `computeCarryClosure` runs a BFS that knows the edge by which each member was absorbed, then returns only the flat `Set` --- the same discard-and-reconstruct pattern this plan keeps finding (Purpose finding 4, PB-I), at the deepest level yet.
  - [ ] Widen `computeCarryClosure`'s return to `{ rootId, members, edges }` (PB-8(d), resolved): `members` the flat closure set as today, `edges` the closure's **internal** edges only (the induced subgraph). **`rootId` is `startId` --- recorded, not derived.** The BFS guards with `closureSet.has(otherId)`, so an object reachable by two carry edges is absorbed via whichever edge was traversed first: the traversal *tree* is order-dependent and must never be the source of primacy, while the induced edge set is not. No "unique node with no parent" derivation is needed --- the root is already an input.
  - [ ] **Leave `boundaryEdgeOutcomes` entirely alone --- signature and consumers.** It has five production consumers (`executor.ts`, `applyObjectRelationalChange.ts`, `applyTransferSet.ts`, `applyObjectClearMembership.ts`, `applyObjectRoomMembership.ts`), three of which pass a bare singleton `new Set([objectId])` rather than anything closure-shaped. Under PB-9's resolution to (i-b), severed boundary edges never enter the fragment at all --- Expansion keeps classifying them exactly where it does now, and the dissolve set travels to the compiler on the op. It already accepts a `ReadonlySet`, which the fragment's `members` satisfies, so its existing call sites need at most `.members` threading.
  - [ ] Blast radius (verified 2026-07-30): `computeCarryClosure` has exactly **two** production consumers --- `lookupOrComputeClosure` and `applyObjectRelationalChange.ts:67`. Files: `interactionUnderTransfer.ts` (+ its test, whose `toEqual(new Set([...]))` assertions shift shape), `expansionEnvironment.ts` (+ test), `executorTypes.ts` (`settledGroups: Map<GroupId, ReadonlySet<EphemeraObjectId>>`), `executor.ts` (`operandExpand`'s two `closure` uses become the fragment's member set), `applyObjectRelationalChange.ts`, `executor.test.ts`.
  - [ ] Executor behavior is unchanged by construction: both `operandExpand` cases keep receiving a flat member set, now derived from the fragment rather than returned directly.
  - [ ] **Record PB-8's revisit trigger at the definition site**, following the house pattern `NarrationSpecification`'s doc comment already sets (escalation/revisit conditions written where the type lives, not only in a plan): note on the fragment type that `{rootId, members, edges}` is a rooted graph --- what `EphemeraPositionGraph` would already be if it carried a root --- and that if `positionGraph` ever gains a root concept this shape should collapse into it rather than persist as parallel duplication. This is the load-bearing half of the trigger: the plan file that currently records it will be deleted when the initiative ships.
  - [ ] Verify: full suite at the Phase 3 baseline with no behavior changes beyond the type shift; `npx tsc --noEmit` clean. Land as its own commit, before Phase 3.6.
- [ ] **Phase 3.6. Unify `executeObjectTakeHold`/`executeObjectDrop` into one object-move execution path. Behavior-preserving --- lands before Phase 4 so that phase's object-side migration is written once.**
  - [ ] **The two modules differ by exactly one thing** (verified 2026-07-30 by diffing them with names normalized and comments stripped): the `(from, to)` referent pair on the `seedTransferMembership` seed. The apparent third difference --- `getCurrentHost: () => args.roomId` vs `() => args.characterId`, passed to both `createExpansionEnvironment` and `commitStepSequence` --- is **not** an independent axis: it means "where does this object currently live," i.e. `from`. Their dispatch sites in [`positions/index.ts`](../../../lambda/ephemera/dataSource/positions/index.ts) are identical apart from the function name, passing the same five fields.
  - [ ] **Unify execution; keep the intents distinct.** `Object Take Hold` and `Object Drop` stay two events with two Plan-stage legality branches --- `validateMembershipPlanDryRun` yields genuinely different player-facing errors per direction ("you're not carrying that" for a room-locus `drop`; "you're already holding that" for a held-object `takeHold`), and they are different utterances. The world-effect, though, is one operation: move an object between two membership hosts, exactly what `PositionKernelMoveOp`'s `froms`/`to` already expresses. **Rule of thumb this phase establishes: intents stay distinct where the player's meaning differs; execution unifies where the world-effect is the same.**
  - [ ] Two existing tells that this is the code's own grain rather than a new idea imposed on it: `resolveTakeHoldPresentationLabels` is named for take but **already serves both**; and Phase 4 deletes `inferOperationFromFact` (direction from host kind) in favor of `buildObjectMoveOp` (verb from host kind), so "which operation is this" is already becoming a function of `(from, to)`.
  - [ ] **Sequencing is the main argument for doing this first.** Phase 4 adds roughly eight identical lines to each function (resolve labels, mint `bundleId`, build op, compile, declare bundle, assemble, commit, present). Merging afterward means writing that migration twice and deleting one copy; merging first roughly halves Phase 4's object-side diff, and the merge is far easier to review against two files still near-identical than against two that have each grown a narration pipeline.
  - [ ] **Referent wrinkle to resolve at implementation:** the seed's `from`/`to` are *referent expressions* (`currentHostRef(actingCharacterRef)`, `actingCharacterRef`), not concrete host ids, while `getCurrentHost` and the graph fetches want concrete ids --- so the unified signature needs both, or a direction-to-referent-pair mapping. Note `actingCharacterRef` cannot express "some *other* character," so a future `give` needs a referent addition; out of scope here, but it is the reason to keep the direction axis explicit rather than boolean.
  - [ ] **Decide the discarded commit result here** --- the natural moment, since after the merge there is one place to handle it rather than two. Both functions today `await commitStepSequence(...)` and return `void`, so `ok: false` is invisible to the caller and yields no player feedback (server-side it is logged, and nothing persists, so this is a feedback gap rather than an integrity one). Whether to surface failure to the player is a behavior change and a product question, not a refactor --- resolve it explicitly rather than carrying it forward by default.
  - [ ] Payoff test: `give` (character -> character) becomes another `(from, to)` pair with a different verb rather than a third near-identical module --- PB-3's own text already says "take/drop/**give** direction," so this is not hypothetical.
  - [ ] Blast radius: the two modules and their two test files, plus two dispatch sites in `positions/index.ts`. `selectIdentityPlanTuple.ts`'s mention of these functions is a doc comment, not a call.
  - [ ] Verify: full suite green with no behavior change; `npx tsc --noEmit` clean. Land as its own commit, after Phase 3.5 and before Phase 4.
- [ ] **Phase 4. Retire the redundant `characterId` in the remaining three sites; migrate object take/drop onto the `Move` compiler (PB-3, resolved 2026-07-30).**
  - [ ] **The object-side steps below apply to Phase 3.6's single unified execution path, not to two mirrored modules.** If Phase 3.6 has not landed, do it first --- writing this migration twice and then merging is precisely the waste that phase exists to avoid.
  - [ ] Drop the no-op trailing `characterId` from navigate-arrive.
  - [ ] **Build this phase from PB-M's frame --- narration is the host's changelog, not the mover's itinerary.** Take = the room's graph lost a node (leave); drop = it gained one (arrive). Same bracket as navigate, same per-host mapping, no new structural shape.
  - [ ] Express take/drop through the same `PositionKernelMoveOp`/`compilePositionKernelOp` used by navigate --- not a new `Take`/`Drop` op. Add the compiler branching PB-3's resolution calls for: skip header-slot emission when a `Move`'s `from`/`to` host is a character (not a room), and add an object-narration `NarrationSpecification` union member (item vocabulary --- `objectShortName`, verb, and the primary/carried distinction read off PB-8's fragment) alongside the existing `membershipMove` member. **The new union member is a *copy* family, not a structural branch:** the leave/arrive bracket and the per-host capture/narrate/slot mapping stay exactly as they are for navigate.
  - [ ] **Do not special-case the character-hosted side of an object move** (revised 2026-07-30 per PB-M; an earlier draft of this phase specified a single-capture/single-narrate `objectManipulation` branch, and that was wrong). Let the bracket emit both sides uniformly: the character-inventory capture yields an empty roster, its narrate step reports nothing, and its declared slot settles harmlessly under the fan-in's documented tolerance. Suppressing the empty side is an optimization at best --- if it is ever taken, take it as a general "narrate steps with an empty captured audience emit no slot" rule in the compiler, never as an object-specific branch, or PB-M's frame is lost again at the first new caller.
  - [ ] Derive take/drop/give direction from **which side of the move is the room**, inside the compiler's `Move` case --- delete `inferOperationFromFact` once the compiler owns the decision (same pattern as PB-I/Purpose finding 4 applied to membership shape). Note this is a sharper statement than "mirror `inferOperationFromFact`'s host-kind logic": under PB-M the verb is a property of the *delta*, which is why the same rule extends to `give` (room on neither side) without a new discriminant.
  - [ ] Retire the redundant trailing `characterId` at both object-manipulation narration sites as part of this migration, not as a separate step --- the captured audience makes it structurally unnecessary.
  - [ ] **Never narrate a commit that did not happen.** `presentStepSequence` must run only on `commitStepSequence`'s `ok: true` branch. This is structurally enforced today --- `captures` exists only on that branch of `MutationKernelCommitResult`, and narration cannot resolve an audience without it (`presentStepSequence` hard-throws on an unresolvable `captureId`) --- but state it anyway, because the tempting way past that type error is to default `captures` to an empty map, which would narrate a take/drop that never persisted. This hazard is **new in Phase 4**: before it, a failed object commit simply meant nothing happened; after it, narration runs post-commit and can contradict the world. Note both execute functions currently **discard** the commit result outright (`await commitStepSequence(...)`, returning `void`), so `ok: false` is invisible to the caller and yields no player feedback --- pre-existing, but Phase 4 is what makes that silence consequential.
  - [ ] Declare the messageOrchestration bundle **after** a successful commit, matching `orchestrateCharacterNavigate`'s shape. **This is a consistency preference, not a correctness requirement** --- recorded explicitly so it is neither "corrected" later on a mistaken safety belief, nor treated as load-bearing. The fan-in is deliberately tolerant of unresolved slots: `messageOrchestrationFanIn`'s handler skips any declared slot with no report and does not increment `CreatedTime`'s offset for it, so a bundle declared before a failed commit settles harmlessly rather than hanging or blocking siblings (see [`messageOrchestration/AGENT.md`](../../../lambda/ephemera/dataSource/messageOrchestration/AGENT.md), "Publish behavior"). An earlier draft of this plan wrongly called declare-before-commit a bug.
- [ ] **Phase 5. Durable docs.**
  - [ ] `positions/AGENT.contract.md`: the binding-time rule (PB-B) as a normative clause --- which target kinds bind when, and why. Plus PB-J's write-free clause: a capture step carries no write payload and therefore cannot contribute to the `transactWrite` write set --- the constraint that makes a read-only step safe inside the mutation walk. State it as a shape requirement, since it is what replaces the old blanket "mutation steps only" exclusion.
  - [ ] `positions/manipulation/AGENT.*`: note that captured rosters are **load-bearing**, not diagnostics --- they *are* the narration audience. Someone reading "capture channel" as instrumentation will otherwise prune it as dead weight.
  - [ ] `positions/AGENT.concepts.md`: positional-vs-terminal binding, presence-vs-perspective (PB-A, PB-H), and the abstract-op / compiled-step two-level vocabulary (PB-I) --- including the normative rule that call sites emit ops and only the compiler names narration steps. Also PB-K's naming rule: `Kernel` alone names nothing, per-kernel types carry their kernel's name, and `KernelStep` stays unprefixed *because* it is the shared cross-kernel vocabulary. State the reason, or the next person will "fix" the inconsistency by prefixing it. And PB-L's perception-vs-presentation distinction --- perception is the experience category and a data source; presentation is publishing into the transcript. That one belongs in durable docs precisely because the repo has already made the mistake once.
  - [ ] `positions/AGENT.implementation.md` and `positions/manipulation/AGENT.implementation.md`: the capture channel's paths and behavior.
  - [ ] `dataSource/perception/AGENT.md`: correct the narration-delivery passages once the fan-in changes.
  - [ ] `positions/AGENT.contract.md`: **narration is presented only for a committed mutation** --- the presentation kernel's narrate branch runs on `commitStepSequence`'s `ok: true` branch and never otherwise. Falsifiable, normative, and not derivable from the code once the type-level guard (`captures` living only on the success branch) is read as an incidental shape rather than the enforcement it is. Scope it to this clause only: the *bundle-declaration ordering* discussed alongside it in Phase 4 is a consistency preference, not a contract clause, and must not be written up as normative --- the messageOrchestration fan-in is explicitly tolerant of unresolved slots by design.
  - [ ] [`positions/positionGraph/AGENT.md`](../../../lambda/ephemera/dataSource/positions/positionGraph/AGENT.md): the **reciprocal half** of PB-8's revisit trigger --- if `positionGraph` ever gains a root concept, the carry-closure fragment's `{rootId, members, edges}` collapses into it. Phase 3.5 writes the note at the fragment's own definition, but the trigger fires from *this* side: whoever adds a root to `positionGraph` is the person who needs to know, and they will not be reading `interactionUnderTransfer.ts`. Deliberately recorded in both places, because the shape is pinned duplication and this plan file --- the only other record of why --- is deleted when the initiative ships.

## Open decisions (implementation --- plan only)

Plan-only: decisions being made in order to implement the next slice(s). When a decision ships, record it in `positions/AGENT.contract.md` / `AGENT.implementation.md` and remove the row here.

| ID | Decision | Blocks | Status |
| --- | --- | --- | --- |
| PB-3 | Do the two object-manipulation narration sites migrate onto positional capture, or just drop their redundant `characterId`? They have no ordering problem today (the actor never moves), so migration may be uniformity for its own sake. **PB-I adds a second axis**: even if they never need positional capture, `Take`/`Drop` may be worth having as abstract ops so no narration is hand-rolled anywhere. Those two answers are independent. | Phase 4 | **Resolved 2026-07-30.** Both axes yes, and by the same mechanism rather than two separate ones: take/drop migrate onto positional capture, and they reuse the existing `Move` op (`PositionKernelMoveOp`) rather than gaining a sibling `Take`/`Drop` op. `entityId: EphemeraObjectId \| EphemeraCharacterId` and `EphemeraMembershipHostId` hosts already generalize this way by design (PB-6); take/drop is definitionally a move of an object between a room's and a character's position graph, the same shape as a character moving room-to-room. Direction (take vs. drop vs. give) falls out of `from`/`to` host kind inside the compiler's existing `Move` case, mirroring how `inferOperationFromFact` derives it today --- no new op discriminant needed. Two things the compiler's `Move` case must branch on that it doesn't today: (1) slot logic --- `navigateLeaveSlotId`/`NAVIGATE_ARRIVE_SLOT_ID` force-cast hosts to room ids for the header slot, which is meaningless when `to`/`from` is a character host, so object moves must skip header-slot emission entirely; (2) narration vocabulary --- the op's `narration` field only carries membership-shaped selectors (`characterName`, `MembershipEmissionCopyKind`, `exitName`), so object narration needs its own `NarrationSpecification` union member carrying item vocabulary (`objectShortName`, carried-object count, take/drop verb), already anticipated in `kernelStep.ts`'s doc comment as a second, independent family --- not a new op, a new case. |
| PB-4 | `ACTOR` / `!ACTOR` target kinds for second-person copy, as a `referent` build-in to the instruction set | A later, separate task --- explicitly not this plan | **Deliberately deferred 2026-07-28.** Named so the deferral is explicit rather than silently assumed permanent. Presence and perspective are orthogonal (PB-H); positional binding does not and should not answer perspective. Note `!CHARACTER#` and `GLOBAL` currently have **zero production producers** --- only `publishMessage/index.ts`'s own filter machinery references them --- so this would be building on a largely unexercised part of the target vocabulary. |
| PB-5 | Does `applyStepSequenceCore` grow the capture channel in place, or does a sibling walker own it? | Phase 1 | **Resolved 2026-07-29 --- see PB-J.** Neither. Capture becomes a first-class walk step, admitted by the mutation kernel's own step union (`MutationKernelStep` after PB-K). The footprint's lock-set requirement decided it: a side-table cannot guarantee capture hosts get locked. The premise that widening "touches routes that will never carry narration" was also wrong --- all three consumers have exactly one production caller (`commitStepSequence`). |
| PB-8 | How is a multi-entity `Move`'s moved set represented on `PositionKernelMoveOp`, and how is the *primary* entity identified? Object narration names the primary ("picks up **the tray** and everything on it"), so a bare set is insufficient. **(a)** `entityIds: ReadonlySet` with primacy implicit in whichever object's `objectShortName` got resolved --- cheapest, but the op cannot state which is primary. **(b)** `entityIds` + explicit `primaryEntityId` --- explicit, but introduces a membership invariant nothing enforces. **(c)** ordered list, "first is primary" --- conventional rather than structural, the same convention-re-derived-at-call-sites weakness PB-I objects to. **(d)** a rooted sub-graph fragment --- carry-closure *is* a rooted sub-DAG of the source graph, so primacy falls out structurally (the unique node with no parent inside the fragment) and cannot drift, and richer copy later ("the tray, with a cup on it") stays reachable because the edges survive instead of collapsing to a cardinality. (d) extends the same objection that motivates carrying the full set at all --- do not discard what Expansion computed and reconstruct a weaker summary --- one level deeper. Two constraints checked 2026-07-30: the executor does **not** return a fragment today (its transfer step carries a flat `objectIds` set; re-derivable by filtering the source graph's edges to those with both endpoints in the closure, but that derivation needs a home), and the Immer-revocation worry does **not** apply here --- `KernelStep[]` is built by the compiler *before* `commitStepSequence` runs, from `internalCache.Positions` reads, so a fragment's provenance is entirely upstream of any `produce()` draft. What remains is testing ergonomics only (`toStrictEqual` checks prototypes; getters/private fields are not own-enumerable), which favors a plain `{rootId, members, edges}` over an `EphemeraPositionGraph` instance as a preference, not a blocker. | Phase 3.5 (result shape) / Phase 4 (use) | **Resolved 2026-07-30: (d), the rooted sub-graph fragment.** Shape is `{ rootId, members, edges }`, where `edges` are the closure's **internal** edges (the induced subgraph) --- *not* its severed boundary edges, which stay with `boundaryEdgeOutcomes` and Expansion per PB-9's resolution. Carry-closure genuinely is a rooted sub-DAG, and collapsing it to a cardinality is the same discard-and-reconstruct objection that motivates carrying the moved set at all, one level deeper; retaining the internal edges is what keeps richer copy reachable later ("the tray, with a cup on it") rather than only "and everything on it". **`rootId` is `startId` --- recorded, not derived:** `computeCarryClosure` already takes it as an argument, and its BFS guards with `closureSet.has(otherId)`, so an object reachable by two carry edges is absorbed via whichever edge was traversed first, making the traversal *tree* order-dependent. Primacy must therefore never be read off tree shape --- which removes (d)'s only real fragility and makes it cheaper than this row originally implied. Plain data rather than an `EphemeraPositionGraph` instance, but **not** for the Immer reason first given here: a narrate step's fragment never goes near a `produce()` draft, since `KernelStep[]` is built by the compiler from cache reads before `commitStepSequence` runs. The surviving reason is test ergonomics (`toStrictEqual` compares prototypes; getters and private fields are not own-enumerable). The overstated Immer claim was also corrected in `kernelStep.ts`'s own doc comment. **Revisit trigger --- do not lose this:** the shape is *pinned duplication*. `{rootId, members, edges}` is a rooted graph, which is precisely what `EphemeraPositionGraph` would already be if it carried a root. If a root concept is ever added to `positionGraph` itself, this fragment should collapse into it rather than persist as a parallel shape re-expressing the same idea. Because task plans are **deleted** when the initiative ships, that trigger must be recorded in code and durable docs, not only in this row --- Phase 3.5 writes it at the definition site and Phase 5 adds the reciprocal pointer on the `positionGraph` side, which is where the triggering change would actually originate. |
| PB-9 | Should `compilePositionKernelOp` emit the `dissolveRelation` steps a carry's boundary sweep produces, rather than the call site splicing them into the compiled plan? Today `commandExpand`'s `isolatedFromRelations` mints them via `boundaryEdgeOutcomes`, and the minimal Phase 4 design has `executeObjectTakeHold`/`executeObjectDrop` hand-assemble `[captureFrom, ...dissolves, transfer, captureTo]` --- where order is load-bearing, since `factsForStep` streams in step order precisely so dissolve facts precede the moved fact. **(i)** the op carries the dissolves and the compiler passes them through --- thin, but puts the whole committed sequence back under one roof. **(ii)** the compiler reads live graph state and derives them --- **reject on sight**: it destroys the compiler's purity, and with it the snapshot-independence that is the entire reason compilation correctly skips the Plan-stage dry run (a state-reading compiler would have to run in both phases). **(iii)** the op carries a fragment including its severed boundary edges (PB-8d) and the compiler derives the dissolves from it, purely --- assembly then disappears entirely and the call site just commits what the compiler returns, the strongest available reading of PB-I. Weigh against (iii): a larger refactor of the two execute functions; it requires exactly one source of dissolves (the executor's steps become *data for the op* rather than committed output, or they double); and character moves never produce dissolves at all (character relational edges deferred, BD-36), so the `Move` case gains a branch only object routes exercise. | Phase 3.5 (result shape) / Phase 4 (use) | **Resolved 2026-07-30: (i), in an "(i-b)" shape.** The op carries the severed edges Expansion **already classified as dissolve**, and the compiler renders them into `dissolveRelation` steps while sequencing --- avoiding (i-a)'s layering wrinkle of an abstract op holding concrete kernel steps, and avoiding (iii) entirely. (iii)'s apparent advantage evaporated on inspection: "the whole committed sequence under one roof" is achieved by (i) too, since the compiler emits the complete ordered list either way --- the only difference is *where* dissolves are computed --- and the same holds for the `factsForStep` ordering guarantee and for any future narration reacting to what was severed. **The decisive argument is the verdict channel.** Expansion's `isolatedFromRelations` case does not merely *find* boundary edges; it returns one of three outcomes, two of which are verdicts rather than steps (`error` on a stray `carry`-classified boundary edge; `defer` on a `Custom` edge, BD-10). `compilePositionKernelOp` returns `{steps, slots}` and has no verdict channel --- and giving it one would destroy the purity and snapshot-independence that is the entire reason compilation correctly skips the Plan-stage dry run (a legality-deciding compiler would have to run in both phases). So the layer split stands as it is: **Expansion owns legality and makes dissolves explicit; the compiler owns sequencing and narration, and expands nothing.** Consequence for PB-8/Phase 3.5: the fragment does **not** carry boundary edges, and `boundaryEdgeOutcomes` keeps its current signature and all five of its production consumers untouched. |

| PB-10 | **Does a severed boundary edge narrate?** Surfaced by PB-M 2026-07-30 and askable only in its frame. When a tray is taken and a cup resting on it does *not* come along, the room's position graph changes twice: the tray's subtree leaves (an event with an actor and an intent) and the cup's `on-tray` relation is severed (a *consequence*). PB-M's rule --- host graph changed, therefore notify --- says both narrate. Product judgment may say the second is silent, or folds into the first's copy ("George picks up the tray, leaving the cup behind"). **(a)** silent --- consequences never narrate independently; **(b)** its own line, via the relational narration family; **(c)** folded into the primary event's copy, which PB-8(d)'s surviving edges make reachable since the fragment knows what was severed. Note the current code answers this *by accident*: the relational fan-in's dissolve leg exists, so whether a boundary sweep produces player-visible text today is a function of which events happen to be published, not of a decision anyone made. **This is a product call, not an architecture one** --- record the answer, do not derive it. | Not blocking Phase 4 (take/drop of a full closure severs nothing by construction); blocks any phase that migrates relational narration | **Resolved 2026-07-30: (b), its own line, as a serviceable first iteration.** "George takes the glass off the tray." / "George picks up the tray." is perfectly clear communication --- only a little stilted --- while (c)'s single merged line is a materially larger lift in copy generation. **(c) stays reachable at low cost, and the architecture already guarantees it**: PB-9(i-b) puts the severed-edge set and the primary move in `compilePositionKernelOp`'s hands *at the same moment*, so merging two lines into one is later a copy-and-sequencing change inside one pure function, not a data-plumbing change --- no re-plumbing of Expansion, no op-shape change. Slot count is compiler-owned too (call sites declare `plan.slots` verbatim), so collapsing two slots into one stays contained. This is explicitly *not* a corner being painted into. **One obligation (b) creates, which the existing ordering does not discharge:** dissolve narration must be *delivered* before the move's, or the pair reads backwards ("George picks up the tray. George takes the glass off the tray." implies the wrong sequence, or worse, leaves it ambiguous whether the glass travelled). Step order already gets this right --- a carry's steps are `[dissolveRelation*, transferMembership]` and `factsForStep` streams in step order deliberately (BD-28, documented in [`factsForStep.ts:28-33`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/factsForStep.ts)) --- but **delivery order comes from declared slot order, not step order** (PB-G), so the compiler must declare dissolve slots ahead of the move's. Unlike Phase 4's bundle-declaration ordering, this one *is* load-bearing, and for comprehensibility rather than correctness --- which makes it exactly the kind of thing a test should pin rather than a comment. **Revisit trigger for (c) --- a cardinality threshold, not "someday":** (b) degrades from stilted to genuinely bad when several relations sever at once (a tray with five items left behind yields five dissolve lines plus one move line). If that case becomes reachable in practice, that is the signal to do (c), not a general dissatisfaction with the prose. |

## Verification

Per slice, from `lambda/ephemera`:

```bash
# Baseline / per-slice
npm run test -- --watchAll=false \
  dataSource/positions/manipulation/kernel/ \
  dataSource/positions/navigate/ \
  dataSource/perception/ \
  dataSource/messageOrchestration/

# Full suite before marking a phase done
npm run test -- --watchAll=false

# Typecheck
npx tsc --noEmit
```

Grep checks once Phase 3 and Phase 4 land:

```bash
# The [room, characterId] idiom should have no remaining occurrences
grep -rn "targets: \[.*, *\(args\.\|plan\.\)characterId\]" --include="*.ts" lambda/ephemera | grep -v node_modules

# Shape inference should be gone
grep -rn "inferMembershipEmissionShape\|MembershipEmissionShape" --include="*.ts" lambda/ephemera | grep -v node_modules

# PB-I/PB-J: narration records and capture steps should be constructed only inside the
# compiler (kind: 'narrate' with the copy ingredients nested under a `narration`
# NarrationSpecification, not a narrate-leave/narrate-arrive kind split --- see Phase 2's closing
# note). Every hit outside the compiler's own directory constructing a `{ kind: 'narrate', ... }`,
# `{ kind: 'membershipMove', ... }`, or `{ kind: 'capture', ... }` literal is a call site
# hand-rolling its own plan.
grep -rn "kind: 'narrate'\|kind: 'membershipMove'\|kind: 'capture'" --include="*.ts" lambda/ephemera | grep -v node_modules | grep -v "\.test\.ts" | grep -v "manipulation/kernel/compile/" | grep -v "manipulation/kernel/kernelStep.ts"

# PB-K: no bare Kernel-prefixed identifier should survive except the shared `KernelStep`.
grep -rhoE "\bKernel[A-Za-z]+" --include="*.ts" lambda/ephemera | grep -v node_modules | sort -u
```
