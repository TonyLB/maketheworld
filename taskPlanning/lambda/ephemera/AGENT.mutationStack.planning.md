# Membership mutation stack: names, boundaries, and doc register

**Status:** Not started, seeded 2026-09-05. Nothing below is built. The next step is to field the [open decisions](#open-decisions-implementation--plan-only) --- several of them gate what the early slices actually do, and the slice list is deliberately written so that answering them narrows the work rather than expanding it.

**This plan *executes* [CD3](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order); it does not own it.** The design question --- what, if anything, replaces the carry-closure BFS --- stays in `AGENT.abstractionLayers.planning.md`'s Channel D, which has held it since 2026-08-22. What lands here is the code, and MS-8's downstream implementation fork. **Record CD3's outcome in that plan when this one finishes** (Phase 5); do not re-argue it here, and do not close CD3's checkbox from inside this plan without writing the result into its row.

This document is task-scoped and follows [`taskPlanning/AGENT.md`](../../AGENT.md). It is a **standard implementation plan**, not a design-stage one: the deliverable of every phase is a diff, and the open forks hang off the worklist rather than the worklist existing to close them.

**Scope note.** The subject spans two DataSources --- `positions/` (op, compile, commit, present) and `actions/enrich/objectManipulation/` (parse, plan, executor) --- which is why this plan sits at `taskPlanning/lambda/ephemera/` rather than under `positions/`. It is expected to broaden; the ID series and phase list have room reserved for that.

## Why this initiative exists

The membership mutation stack works. Its **naming and division of responsibility do not describe it.** Both grew by accretion across a run of shipped migrations (`applyObjectSet*` -> `executeObject*` -> the unified kernel; the async fan-ins -> the presentation kernel; the object-lifecycle and character-route Migrate rows), and each migration named its output after **where it was being called from** at the time rather than after what it fundamentally does.

The result is a system whose surface **self-documents its own history**. Four symptoms, each measured against the code as of this seeding --- the fourth added 2026-09-05, and it is the one that changes what the work is: the first three are comprehension cost, while the fourth is live machinery with no remaining reason to run.

1. **Names inherited from call sites.** `executeMembershipTransfer` --- the general, six-caller entry point, used by character membership --- lives inside a file named [`executeObjectMove.ts`](../../../lambda/ephemera/dataSource/positions/manipulation/membership/executeObjectMove.ts). There are two directories named `membership/` with no principle separating them. `applyCharacterRoomMembership` no longer applies anything (the kernel absorbed persistence); its body is now character-route side effects wrapped around a transfer, and its types still carry an *"Ingress-facing stable API (S1-7)"* label with three internal callers.

2. **Divisions drawn by guard clauses.** `orchestrateCharacterDisconnect` exists, per its own doc comment, because `orchestrateCharacterNavigate` returns early on `!to`. `intentKind` appears as three overlapping subsets of one enum, each sized to whoever happens to reach it.

3. **Comments written as diffs rather than as state.** [`kernelStep.ts`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/kernelStep.ts) is 368 lines, over half comment, in a consistent register of *change*: "widened again", "widened twice", "previously gated on", "an earlier revision of this comment overstated it", "see git history for that version's own doc comment". A reader asking *what is a transfer step* reconstructs it from four amendments.

4. **Machinery retained past its justification.** **Carry closure is not weakly motivated --- it is inert**, and has been since 2026-08-22. `computeCarryClosure` is a BFS whose only absorption condition is `classifyInteractionUnderTransfer(...) === 'carry'`; that classifier is a closed switch in which `Under`/`Against`/`Custom` return `defer`/`dissolve` and `On`/`In`/`PartOf`/`Present` throw. **Nothing returns `carry`**, so the closure is always exactly `{startId}` with no internal edges. The codebase already says so in four retired tests (*"`carry` is unreachable from any relation kind"*, `selectMembershipFromPool.test.ts:124` and siblings). Everything above it is currently a distinction with no difference --- see MS-8.

**The falsifiable half of (3):** `PB-J` (12 occurrences), `PB-M` (11) and `LP4h` (12) are plan-ID markers whose defining plans have been deleted --- they have **no referent anywhere in `taskPlanning/`**. That is ~35 comment references pointing at nothing, and it is the dangling-marker anti-pattern [`taskPlanning/AGENT.md`](../../AGENT.md) already names.

**On (4), keep two things apart.** The *requirement* did not die; it moved into the representation. "Take the tray and the cup comes too" is still true, but AB-54 made it structural rather than computational --- the cup is a node of the tray's own shard, and the tray's host is what changes, so the carry happens by not happening. What is dead is closure-the-computation, not carry-the-behavior.

**This is not a correctness initiative.** The suite is green (49 suites / 514 tests) and no defect is alleged. The cost being paid is comprehension cost, at the exact layer that has absorbed the most change and will absorb more.

**Stored data is not a concern for (4)** (confirmed 2026-09-05): peer-level `On` edges could only have been persisted before 2026-08-22, and `CoyoteGame` clears all objects frequently enough that the instance is several cleanup cycles past any that existed. No migration step is owed.

## What already exists to build on

Read these before the first slice --- they are the substrate, and two of them are what the durable half of this work merges *into*:

1. [`positions/AGENT.concepts.md` --- Manipulation layering](../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md) and **Abstract op and compiled step**, **Naming: `Kernel` alone names nothing**. The vocabulary is already largely right; the code has drifted from it. **Two of its claims are currently false in the code** --- see MS-2 and MS-7.
2. [`positions/manipulation/AGENT.implementation.md`](../../../lambda/ephemera/dataSource/positions/manipulation/AGENT.implementation.md) --- kernel spec, compile layer, per-route ingress map, and the code map that this plan's renames invalidate.
3. [`positions/AGENT.contract.md`](../../../lambda/ephemera/dataSource/positions/AGENT.contract.md) --- the normative clauses (capture shape, narration binding, single-hosted restriction) that constrain every slice here.
4. The stack itself, in execution order: `index.ts` dispatch -> `buildCharacterMoveOp` / `buildObjectMoveOp` -> [`compilePositionKernelOp`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/compile/compilePositionKernelOp.ts) -> [`commitStepSequence`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/commitStepSequence.ts) -> [`applyStepSequenceCore`](../../../lambda/ephemera/dataSource/positions/manipulation/kernel/applyStepSequenceCore.ts) -> `factsForStep` -> `presentStepSequence`.
5. **[`AGENT.abstractionLayers.planning.md`](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order), Channel D --- `CD2` / `CD2h` / `CD3` / `CD4`.** The design record for AB-54's `On` partition and everything that followed from it. **CD3 is the row this plan executes**, and its text was re-verified against the code on 2026-09-03: `InteractionUnderTransferOutcome` still reads `'dissolve' | 'carry' | 'defer'` and `computeCarryClosure` is still a BFS. Read `CD2`'s "Consequence, named rather than hidden: `carry` is now dead" paragraph before touching anything in Phase 1d --- it lists the test sweep already done, so the retired-with-a-comment tests are expected, not a gap.

**Testing authority:** [`lambda/ephemera/AGENT.testing.md`](../../../lambda/ephemera/AGENT.testing.md) --- `npm run test`, not `npm test`, run from `lambda/ephemera`. There is no `taskPlanning/lambda/ephemera/AGENT.development.md`.

**Two hazards this plan will hit repeatedly, both already documented and both earned the hard way:**

- `*.integration.test.ts` files sit **outside** the tsconfig include and mock modules **by path**. `npx tsc --noEmit` cannot catch a break in them. This plan is mostly renames and deletions, which is precisely the change class that breaks them --- run the full jest suite after every one, and grep module **paths**, not only symbols.
- `ts-jest` caches type-check results per unchanged file. Per `AGENT.testing.md`, after any change that makes a shared field required (or removes one), run `npm run test -- --clearCache` once before believing a green suite. **The files that break are the ones you did not touch.**

Baseline (passes as of seeding: 49 suites, 514 tests; run from `lambda/ephemera`):

```bash
npm run test -- --watchAll=false dataSource/positions/
```

## Recommended order

Use `[ ]` for pending and `[X]` for complete; mark nested lines `[X]` as each sub-step finishes. Nothing below is built yet, so all lines start `[ ]`.

**Phases 1--2 are subtraction and renaming with no behavior change, and are safe to run before the open decisions are fielded. Phase 3 is gated on them.** The ordering is deliberate: each phase removes noise that would otherwise have to be reasoned through in the next.

- [ ] **Phase 1. Subtract what is already known dead.** No design questions; each item is provable by grep or by exhaustion over a closed union.
  - [X] 1a. Delete `narratedInline` / `narrationHandledInline` end to end --- 11 files (the "8 files" estimate undercounted `commitStepSequence.ts`, `factsForStep.ts`, and `buildCharacterMovedFact.ts`), 2 exported types, one `PositionsPublishedPayload` field and its validator. Set by four call sites, written onto the `Character Moved` fact, and **read by nothing**; `handleConnectionsCharactersPresence.ts:40` already said so. Done 2026-09-06: full suite green (351 suites / 2849 tests), `tsc --noEmit` clean, grep for both names across `lambda/`/`packages/` returns nothing. No `AGENT.contract.md`/`AGENT.implementation.md` update was owed --- neither asserted the field as a rule, so there was nothing normative to correct.
  - [X] 1b. Collapse `syncMembershipAdjacency.ts` and `syncObjectMembershipAdjacency.ts` --- 66 lines each, identical apart from whether the parameter is called `characterId` or `objectId`. Gated on MS-1 for whether the survivor should exist at all. Done 2026-09-06: MS-1 resolved (a) --- kernel-routing didn't apply (both functions fix only the `positionAdjacency` reverse index for the case where the `ludicGraph` is already correct; the kernel's `transferMembership` step always asserts a graph mutation, so there's no step kind for "write the index only," and adding one would be new kernel design, not a subtraction). `syncObjectMembershipAdjacency.ts` deleted; `syncMembershipAdjacency.ts` widened to `componentId: EphemeraCharacterId | EphemeraObjectId` (the name the codebase already uses for this union). Both repair sweeps repointed; both test files merged; three durable-doc code-map/reference rows repointed (`AGENT.contract.md:276`, `manipulation/AGENT.implementation.md:286`, and one duplicate row deleted from `positions/AGENT.implementation.md`). Full suite green (350 suites / 2847 tests, 1 pre-existing skip), `tsc --noEmit` clean.
  - [X] 1c. Marker and expired-promise sweep. Done 2026-09-06: scope was bigger than the headline `PB-J`/`PB-M`/`LP4h` counts --- every `PB-<letter>` variant in the code (`2,3,6,7,8,9,A,D,E,F,G,I,J,L,M`) was dangling the same way, since 1c's own recommended-order line said "the dangling `PB-*` / `LP4h` references" (a wildcard); only `PB-K` still has a referent (`AGENT.abstractionLayers.discussion.planning.md`) and was left alone. 24 files, ~50 occurrences, all resolved via MS-4 option (c) (delete the marker, prose was already self-contained in every case). Two carve-outs: `computeCarryClosure`'s own body/doc-comment in `interactionUnderTransfer.ts` was left untouched (1d deletes the whole function next slice, so editing its markers now would be pure churn); `ludicGraph/AGENT.md`'s one `LP4h` mention was left as durable-doc historical narration. The expired-promise half was dropped: `executorTypes.ts:16-22`'s claim that `parsePlanStep.ts` "only loses [`hostRoomId`] at the Migrate slice" has shipped is false --- `EstablishRelationStep`/`DissolveRelationStep` still carry `hostRoomId`, still actively read/constructed by the live relational route (`filterLegalRelationalCandidates.ts`, `executor.ts`, `groundChange.ts`, `compileRelationalFromSkeleton.ts`); the comment is accurate as written and was left alone. Full suite green (350 suites / 2847 tests, 1 pre-existing skip), `tsc --noEmit` clean.
  - [X] 1d. **Execute [CD3](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order)'s two deletions.** `carry` leaves `InteractionUnderTransferOutcome` (shrinking it to `'dissolve' | 'defer'`), and `computeCarryClosure` stops being a BFS. Belongs in Phase 1 because both are provably-dead subtraction --- unreachability is a proof by exhaustion over a closed union, not a judgment call. **What replaces the BFS is CD3's question, not this plan's**; if CD3's answer is not yet settled when this slice comes up, take the narrowest step that preserves today's observable behavior (a singleton) and leave the replacement to CD3. The downstream consequences are MS-8's and belong in Phase 2a, not here. Done 2026-09-06: CD3's answer was not yet settled, so the narrowest step shipped --- `computeCarryClosure` is now a single pass over `startId`'s own edges (no queue, no absorption set), still calling `classifyInteractionUnderTransfer` per touching edge purely to preserve the AB-54 hosting-kind-throw invariant (`interactionUnderTransfer.test.ts:123-134` requires it), always returning a singleton graph. Two now-dead downstream `'carry'`-outcome defensive checks were removed (`applyTransferSet.ts:44-47`, `executor.ts:276-282`, both already stranded of live tests as of the 2026-08-22 retirement sweep). `AGENT.contract.md:114`'s BFS-mechanism sentence was corrected to match (Phase-1b precedent: fix contract/implementation text immediately when the code it describes changes, don't defer to Phase 5). `PositionKernelMovedSet`, `carriedCount`, and `executeObjectMove`/`executeMembershipTransfer` were left untouched --- MS-8's, gated on this slice, Phase 2a's job. CD3's own checkbox in `AGENT.abstractionLayers.planning.md` stays open; recording the outcome there is Phase 5a. Full suite green (350 suites / 2847 tests, 1 pre-existing skip), `tsc --noEmit` clean.
  - [X] 1e. **Convert the closed relation-kind vocabulary (`Under`, `Against`) from hardcoded literals to data, per MS-9.** Unlike 1a-1d, this item needed an actual design decision (MS-9, resolved 2026-09-06 in conversation) rather than being provable by grep/exhaustion --- it is grouped here for file/topic locality with 1d (same file, same closed-union machinery) rather than under Phase 1's "no design questions" framing; the plan's own scope note already reserves room for the phase list to broaden this way. Done 2026-09-06: **MS-9's own "at least four places" undercounted the persistence lane** --- a grep before touching code found a third hand-typed copy of `HOST_RELATIONAL_EDGE_KINDS: Set<HostRelationalEdgeKind>` (`dataSource/positions/publishedEvents.ts:47`) alongside the two MS-9 named (`ephemeraMeta.ts`, `positions/ludicGraph/baseClasses.ts`); see MS-9's row for the corrected count and the three other, deliberately-independent lanes left untouched. `ephemeraMeta.ts` gained `CLOSED_RELATION_KINDS` (`as const` array), the derived `ClosedRelationKind` type, and `isClosedRelationKind` typeguard, with `HostRelationalEdgeKind`'s own union now sourcing `'Under' | 'Against'` from the derived type instead of spelling them; all three `HOST_RELATIONAL_EDGE_KINDS` sets (`ephemeraMeta.ts`, `positions/ludicGraph/baseClasses.ts`, `positions/publishedEvents.ts`) now spread `CLOSED_RELATION_KINDS` instead of re-listing it. `interactionUnderTransfer.ts`'s `classifyInteractionUnderTransfer` now checks `isClosedRelationKind` first and looks up a local `CLOSED_RELATION_BEHAVIOR: Record<ClosedRelationKind, { onSubjectMove; onTargetMove }>` table, rather than case arms; the hosting-kind throw, `Present`'s throw, and `Custom`'s unconditional `defer` are unchanged. `AGENT.contract.md`'s relation-kind-enum section gained a one-line pointer to the new source of truth. `HostRelationalEdgeKindPublished` (`dataSource/actions/publishedEvents.ts`), `PeerRelationalEdgeKind` (`relationKind.ts`), `dataSource/actions/baseClasses.ts`'s own inline pair, and `expandSameHost.ts`'s inline check were left untouched --- deliberately independent lanes, not this slice's concern. No behavior change: `interactionUnderTransfer.test.ts`'s `it.each` table passed unmodified. Full suite green (350 suites / 2847 tests, 1 pre-existing skip), `tsc --noEmit` clean (both `lambda/ephemera` and `packages/mtw-interfaces`). Naming implications (should `ClosedRelationKind`/`isClosedRelationKind` reach into Phase 2's renames or Phase 4's comment register) remain noted for those phases, not resolved here.
  - [X] 1f. **Generalize narration's per-kind relation dispatch, per MS-10.** A post-1e survey of every remaining `'Under'`/`'Against'` literal in the codebase found one real generalization gap outside 1e's four sites: `publishObjectManipulationPresentation.ts`'s `buildEstablishRelationWorldMessage` picked a narration verb via an if/else chain hand-checking `'On'`/`'Under'`/`'Against'`/`'Custom'` directly. Resolved 2026-09-06 (MS-10): add a sibling `HOSTING_RELATION_KINDS`/`HostingRelationKind`/`isHostingRelationKind` triple to `ephemeraMeta.ts`, mirroring `CLOSED_RELATION_KINDS`'s shape, and merge it into `HostRelationalEdgeKind`'s own union arm the same way 1e merged `ClosedRelationKind` in. Hosting-kind *behavior* stays hardcoded everywhere it already was (`interactionUnderTransfer.ts`'s throw is untouched) --- only the vocabulary got a shared typeguard. Narration now checks `isHostingRelationKind` first against a `Partial<Record<HostingRelationKind, {verb; preposition}>>` table (`Partial` because only `On` has established copy; `In`/`PartOf` aren't producible on this route yet, so nothing is invented for them), then `isClosedRelationKind` second against an exhaustive `Record<ClosedRelationKind, {verb; preposition}>` mirroring `interactionUnderTransfer.ts`'s `CLOSED_RELATION_BEHAVIOR` --- two local tables gated by two typeguards, not one merged table, per the user's explicit design (containment kinds "can continue to be hard-coded"). No behavior change: identical narration strings for all four reachable kinds (`On`/`Under`/`Against`/`Custom`), verified against `publishObjectManipulationPresentation.test.ts`'s existing expectations. Full suite green (350 suites / 2847 tests, 1 pre-existing skip), `tsc --noEmit` clean (both packages).
- [X] **Phase 2. Name things for what they are.** Pure moves and renames; the payoff test is that a reader can find the character membership path without knowing the object routes' history.
  - [X] 2a. **Unify `executeObjectMove` and `executeMembershipTransfer` into one function, per MS-8 (resolved 2026-09-06).** *Reworked 2026-09-05: this was "split `executeObjectMove.ts` into two modules," on the assumption the two were distinct because of carry closure. Reworked again 2026-09-06, after MS-8 actually ran the merge question: the four surviving behavioral differences (defer-refusal, host-count/nullability, hosting-edge stripping, entity kind) looked like domain boundaries at first pass and were corrected on push-back to being unbuilt corners of one general operation, not separate concepts.* One function, one generalized data shape: entity (Object or Character), `fromHostIds` (0..N, generalizing `executeObjectMove`'s single `fromHostId` and `executeMembershipTransfer`'s possibly-many `priorContainers`), `toHostId` (0-or-1, generalizing `executeObjectMove`'s required `toHostId` and `executeMembershipTransfer`'s nullable `target`). **Preserve today's behavior exactly, do not extend it:** defer (`classifyInteractionUnderTransfer`/`boundaryEdgeOutcomes`) is honored only when there is a real destination; a departure (`toHostId` null) still unconditionally dissolves every touching chain --- MS-11's tracked gap, not something this slice re-derives or silently widens either direction. Drop the Synthesize-executor machinery `executeObjectMove` runs solely to re-derive what 1d already made a singleton (`createExpansionEnvironment`/`runExecutor`/`settledGroups`/`groupIdByObject`, and `seedGroundedTransferMembership`); call `classifyInteractionUnderTransfer`/`boundaryEdgeOutcomes` directly instead, the way `executeMembershipTransfer` already calls `findRelationalChainsTouching` directly. Fold `PositionKernelMovedSet`'s `closure`/`entity` union and `carriedCount` (always 1) into the same slice --- both are carry-closure-shaped machinery this unification also obsoletes. **Naming, locked in 2026-09-06:** the survivor is named `executeMembershipTransfer`, not `executeObjectMove` --- the "must not be named for one caller's verb" rule rules out `executeObjectMove` (`Object`-only, evokes the take/drop/rehost verb family specifically), but not `executeMembershipTransfer`, which was already the general-purpose name absorbing three prior near-identical bodies across place/spawn/remove/scrub/navigate/disconnect, already spans both entity kinds, and already stretches "transfer" to cover a null-target departure. This is a naming decision, not a scope decision: `executeObjectMove`'s callers (`orchestrateObjectMove`) move to calling the generalized `executeMembershipTransfer` instead; nothing about `executeObjectMove`'s current behavior survives under its own name. Should the broader `orchestrate*`/`execute*`/`commit*`/`present*`/`plan*`/`compile*` naming conversation (2026-09-06, not yet recorded as its own decision row) later conclude that this tier should be called `commit*`, that is a separate, later rename --- not blocked on by this row, and not resolved by it either. **Done 2026-09-07:** shipped as one function in a renamed file (`manipulation/membership/executeObjectMove.ts` -> `executeMembershipTransfer.ts`; both prior test files merged into `executeMembershipTransfer.test.ts`), with the "preserve exactly" instruction resolved concretely rather than literally generalized: `fromHostIds`/`toHostId` collapse into the function's existing `getMembershipContainers`-diffed `froms`/`target` (a caller that already knows a single concrete host, `orchestrateObjectMove`, supplies `getMembershipContainers: async () => [fromHostId]` instead of a new parameter), but the two dissolve *mechanisms* did not merge --- they are genuinely different operations (a player action that can be refused vs. an administrative reposition that never is), not two spellings of one, so a narrowly-scoped opt-in flag (`honorDefer?: boolean`, set only by `orchestrateObjectMove`) selects between them: `true` runs the single-hop, defer-aware `boundaryEdgeOutcomes` check (with the own-root-containment-edge strip and `containment` support) against only the departure host's own graph; false/omitted keeps the chain-aware, unconditional `findRelationalChainsTouching` sweep verbatim for every other caller. This is a corrected reading of "one generalized data shape," found while implementing: an earlier draft of this row's plan considered routing the chain-aware sweep's already-built `MutationKernelStep[]` through `compilePositionKernelOp`'s `dissolvedEdges: HostRelationalEdge[]` field (which hardcodes `hostId: op.froms[0]`) for uniformity --- rejected before landing, since a chain-aware dissolve can span hosts other than `froms[0]` and doing so would have silently mis-attributed steps. The Synthesize-executor detour is gone as specified: `classifyInteractionUnderTransfer`/`boundaryEdgeOutcomes` are called directly, and `createExpansionEnvironment`/`runExecutor`/`settledGroups`/`groupIdByObject` now appear only in the relational/tie routes that still need real Grounding (`compileRelationalFromSkeleton.ts`, `selectIdentityPlanTuple.ts`'s Plan-stage dry run). `seedGroundedTransferMembership` (found to have zero remaining production callers once `executeObjectMove` stopped calling it) was deleted from `executor.ts` along with its own unit test; the two `runExecutor` behavior tests that used it as a seed-builder now construct the equivalent `WorklistInstruction[]` inline. `PositionKernelMovedSet`/`carriedCount` collapsed as specified (`positionKernelOp.ts`, `compilePositionKernelOp.ts`, `kernelStep.ts`, `presentStepSequence.ts`, `buildObjectMoveOp.ts`); `moved` is now a bare `EphemeraObjectId | EphemeraCharacterId`. Result-type reconciliation: `ok: false` gained optional `errorCode`/`errorMessage` (populated on a real commit failure, omitted on an early defer-refusal), and `ok: true` gained an optional `plan: CompiledPositionKernelPlan` (populated only under `honorDefer`, since only `orchestrateObjectMove` needs `plan.slots` for its message bundle) --- `applyCharacterRoomMembership.ts`'s error-branch needed a fallback default to satisfy `MembershipApplyResult`'s still-non-optional error fields, since that route never sets `honorDefer` and so never actually hits the now-optional branch. Added the defer-refusal test `executeObjectMove.test.ts` never had (an `Under`, subject-move boundary edge, `honorDefer: true`) alongside every migrated case from both predecessor files. `AGENT.contract.md` (module paths, the `takeHold`/`drop` apply-mode paragraph, the moved-set shape paragraph, the object-move execution-path paragraph, the narration-ownership line) and `manipulation/AGENT.implementation.md` (the End-to-end flow ASCII diagram, the per-route ingress map, the code-map "Two entry points" row, `applyObjectRelationalChange`'s cross-reference) updated to match. Full suite green (349 of 350 suites, 1 pre-existing skip; 2842 tests), `tsc --noEmit` clean. **Flag renamed same day, 2026-09-07:** `carryClosureTransfer` -> `honorDefer`, caught in review as still another instance of this initiative's own named antipattern --- it named the flag after the retired carry-closure machinery it no longer touches, rather than what it does. `honorDefer` is explicitly documented (in the arg's own doc comment and in `AGENT.contract.md`) as a **temporary** flag: it exists only because administrative-reposition routes have no legality question to ask yet, not as a permanent two-tier design, and should be removed (not renamed again) if that ever stops being true in either direction.
  - [X] 2b. **Resolve the two `membership/` directories per MS-3.** Done 2026-09-07: `positions/membership/` (20 source files + 18 tests, ~3516 lines) merged flat into `positions/manipulation/membership/`, which already held `executeMembershipTransfer.ts`/`orchestrateObjectMove.ts`/`types.ts`; the standalone directory is gone. No barrel/`index.ts` existed in either directory and no consumer imported by directory path, so every fix was a mechanical path-string edit rather than a barrel-surface change --- confirmed by investigation before touching code. One real naming collision: both directories had a `types.ts` with zero overlapping exported symbols (`MembershipDiff`/`MembershipApplyArgs`/etc. vs. `ObjectMembershipDiff` alone); resolved by concatenating the two files into one `types.ts`, no symbol renames --- a directory-shape mechanical merge, not MS-5 (MS-5, still gated on Phase 3b, is the separate decision to eventually widen `MembershipDiff` itself to be host-general like `ObjectMembershipDiff`). Every cross-directory import (both directions --- `manipulation/membership/executeMembershipTransfer.ts`/`orchestrateObjectMove.test.ts` importing `buildObjectMoveOp` the other way, and eight files the other direction) collapsed to a same-directory `./` import; every other relative import in a moved file needed exactly one more `../` (moved one directory deeper) or one fewer (crossing into what's now a sibling rather than a cousin directory) --- fixed file by file, including `jest.mock()` path strings (which `tsc` does not check) and one file (`membershipContainersSharedMemo.test.ts`) whose test double had no matching same-named source, not orphaned, just independently named, and moved with `applyCharacterRoomMembership.ts`. External blast radius reached 20 files outside both directories --- `positions/index.ts`, `handleConnectionsCharactersPresence.ts`(+test), `receivePaths.integration.test.ts`, three `navigate/` files (+tests), `manipulation/kernel/factsForStep.ts`+`commitStepSequence.ts`, and six files entirely outside `positions/` (`narration/handleCharacterSpoke.ts`(+test), `objects/handleApiObjectsChange.ts`, `connectionsCharacterRegistered/handleCharacterRegisteredOrientation.ts`, `characterRegisteredOrientation.integration.test.ts`, `ephemeraUpdate/index.ts`(+test), `fetchEphemera/index.ts`(+test), `guestCharacter/index.ts`(+test), `internalCache/characterMeta.ts`) --- all fixed. Durable docs: `AGENT.contract.md`'s ~20 bare `membership/X.ts` links became `manipulation/membership/X.ts` (its own `manipulation/membership/...` links were already correct and untouched); `manipulation/AGENT.implementation.md`'s five `../membership/X.ts` links lost the `../` (the directory is now local to that doc) --- its code-map `### membership/` table was **not** expanded to the newly-arrived files, since Phase 5 explicitly owns correcting that code map and doing it piecemeal here would fight that later pass; `AGENT.concepts.md` got the same link fix plus a **pre-existing** staleness correction unrelated to this slice's own moves --- three `executeObjectMove.ts` references (2a's filename, retired 2026-09-07 in favor of `executeMembershipTransfer.ts`) that 2a's own Done note didn't cover, found only because this slice was already sweeping every `membership/` link in the file; one of the three also named the now-removed Synthesize-executor detour (MS-8's subtraction), corrected to match `AGENT.contract.md`'s wording. Full suite green (349 of 350 suites, 1 pre-existing skip; 2842 tests, matching the pre-slice baseline exactly), `tsc --noEmit` clean both before and after a `--clearCache` run.
  - [X] 2c. Rename `applyCharacterRoomMembership` for what it now does, and drop the stale "stable API" label from `membership/types.ts`. Done 2026-09-07: renamed to `orchestrateCharacterRoomMembership` (and `ApplyCharacterRoomMembershipDependencies` -> `OrchestrateCharacterRoomMembershipDependencies`) --- matches the sibling verb-tier already used for functions of this exact shape (pre-check/resolve -> call `executeMembershipTransfer` -> post-effects): `orchestrateObjectMove`, `orchestrateCharacterDisconnect`, `orchestrateCharacterNavigate`. Chosen over `coordinateCharacterRoomMembership` (the noun the docs already use to describe it, but with no verb precedent anywhere in the codebase) and over narrowing `apply*` to just the remaining side effects --- decided in conversation, not derived. Noted for the record, not resolved here: MS-6 (Phase 3c, not yet run) names exactly the three `orchestrate*` siblings above as a convergence candidate and does not mention this one; adding a fourth `orchestrate*` name isn't part of that decision and doesn't presume its outcome. `types.ts:13`'s `/** Ingress-facing stable API (S1-7). */` label (attached to `MembershipApplyArgs`) was deleted outright, no replacement text --- `S1-7` had no referent anywhere in `taskPlanning/` (confirmed by grep before touching code), the same dangling shape MS-4 already resolved for `PB-*`/`LP4h` (option (c): delete the marker, prose already self-contained); this is a direct application of that already-resolved policy, not a new decision, so no new Open-decisions row was added. Blast radius: the file + test file pair; the 3 production callers (`navigate/executeCharacterNavigate.ts`, `handleConnectionsCharactersPresence.ts`, `manipulation/membership/repairRoomOccupancyDrift.ts`) and their test files (mock paths, not just symbols, per this plan's own integration-test hazard note); `membershipContainersSharedMemo.test.ts`; the hazard file `receivePaths.integration.test.ts` (`jest.mock` path string + ~10 mock-variable usages, invisible to `tsc`); 5 comment-only mentions (`navigate/orchestrateNavigate.ts`, `manipulation/kernel/factsForStep.ts`, `manipulation/kernel/commitStepSequence.ts` x2, `manipulation/membership/executeMembershipTransfer.ts`, `manipulation/membership/orchestrateCharacterDisconnect.ts`); durable docs `AGENT.contract.md` (9 occurrences), `manipulation/AGENT.implementation.md` (5), `dataSource/positions/AGENT.implementation.md` (3 of 4 --- line 42's unrelated `executeObjectMove.ts` staleness is 2a's unfinished doc debt, left alone), `AGENT.event.md`, `AGENT.navigation.md`, `dataSource/perception/AGENT.md` (3), and two docs outside `lambda/ephemera` entirely (`lambda/connections/AGENT.md`, `documentation/dataSources/connections/index.md`) found only by a repo-wide (not `lambda/`-scoped) grep after the lambda-scoped sweep looked complete --- worth noting as a scope lesson: this initiative's own doc references aren't contained to the lambda that owns the code. Three docs (`dataSource/positions/AGENT.implementation.md`, `AGENT.event.md`, `AGENT.navigation.md`, `dataSource/perception/AGENT.md`) also carried a pre-existing stale path (`membership/...` missing the `manipulation/` segment from 2b's merge) fixed in the same edit since the line was already being touched --- not an independent sweep beyond that. Full suite green (349 of 350 suites, 1 pre-existing skip; 2842 tests, matching the pre-slice baseline exactly), `tsc --noEmit` clean.
  - [X] 2d. Reconcile the three `intentKind` subsets into one declared vocabulary. Done 2026-09-07: research found four inline declarations, not three --- `buildCharacterMoveOp.ts:22` (4-member, required), `orchestrateNavigate.ts:32` and `afterCharacterMembershipNavigateChanged.ts:16` (3-member each, textually identical), `executeCharacterNavigate.ts:20` (2-member) --- none derived from a shared type, reconciled only by structural-subtyping luck (narrower unions happen to be assignable into wider ones). No cast/switch/exhaustiveness check existed anywhere; `buildCharacterMoveOp.ts:50-59`'s plain `===` ternary (untouched by this slice) is the only place `intentKind` is consumed, mapping it into the separate, larger `MembershipEmissionCopyKind` vocabulary. Shipped as one exported `IntentKind` type (`'navigate' | 'home' | 'connect' | 'disconnect'`, matching the widest prior declaration verbatim) plus two derived types --- `NavigateIntentKind = Exclude<IntentKind, 'disconnect'>` (replacing both 3-member duplicates) and `ExecuteNavigateIntentKind = Extract<IntentKind, 'navigate' | 'home'>` --- all three declared once in `manipulation/membership/types.ts` (already the shared types file for this directory post-2b), imported by the four call sites instead of re-declared. No behavior change: every call site accepts exactly the same literal values as before; the ternary, defaults, and all runtime logic are untouched. `AGENT.contract.md:448` gained a pointer from the vocabulary's first behavioral reference to `IntentKind`'s declaration, mirroring 1e's `CLOSED_RELATION_KINDS` contract update. No new `MS-*` row --- the two design choices (where the type lives, how narrower subsets derive) were small enough to settle inline, per 1c/2b/2c's precedent for mechanical sub-decisions. Full suite green (349 of 350 suites, 1 pre-existing skip; 2842 tests, matching baseline exactly), `tsc --noEmit` clean.
- [ ] **Phase 3. Repair the divisions of responsibility.** Behavior-adjacent; each item is gated on its decision row.
  - [ ] 3a. Single owner for step shape (MS-2) --- retire `executeMembershipTransfer`'s hand-built default branch so every route reaches the kernel through the compiler.
  - [ ] 3b. One diff vocabulary (MS-5) --- remove the host/room narrow-and-rewiden that exists only to reconcile `MembershipDiff` with the host-general shape.
  - [ ] 3c. Converge the `orchestrate*` trio (MS-6), if MS-6 says they should converge.
- [ ] **Phase 4. Comment register.** Rewrite the stack's doc comments to describe present state rather than the path to it, per the standard MS-7 settles. Archaeology (retired predecessor filenames, superseded-design rationale) either graduates to a durable doc or goes; git retains it either way.
- [ ] **Phase 5. Durable docs, and retire this plan.** Correct `manipulation/AGENT.implementation.md`'s code map (every Phase 2 rename invalidates it), reconcile `AGENT.concepts.md`'s claims with what the code then actually does, add any earned rules to `AGENT.contract.md`, then delete this file.
  - [ ] 5a. **Record CD3's outcome in [`AGENT.abstractionLayers.planning.md`](dataSource/positions/AGENT.abstractionLayers.planning.md#recommended-order)** --- what shipped, what replaced the BFS (if anything), and what MS-8 decided --- then close its checkbox there. **This is the step that keeps CD3's design ownership real rather than nominal**, and it is the one most likely to be skipped, because by then the code will be merged and the row will look like paperwork. It is not: CD3 is cited by Channel D's own summary row and by the presence plan's PR-4, and a closed-in-code-but-open-in-plan row is exactly the drift this initiative exists to stop reproducing. **Budget real time**: the sibling `relationalNarration` plan records that a predecessor's durable docs were not merely missing new rules but actively stale, and the corrections were larger than the additions.

## Open decisions (implementation --- plan only)

Plan-only: decisions made in order to implement upcoming slices. When one ships, record it in `positions/AGENT.contract.md` / `AGENT.implementation.md` and remove the row here.

IDs are `MS-*` and are not reused. **MS-9 onward is deliberately unallocated** --- this plan expects to broaden, and new concerns should land as fresh rows here rather than being folded into existing ones. MS-8 was the first such arrival (2026-09-05) and is the worked example: it came in as a question about whether an existing body of functionality still had a reason to exist, and it reshaped a slice rather than adding one.

| ID | Decision | Blocks | Status |
| --- | --- | --- | --- |
| MS-2 | **Should the compiler be the only producer of step shape?** [`AGENT.concepts.md`](../../../lambda/ephemera/dataSource/positions/AGENT.concepts.md) states plans are "compiled from abstract operations, never hand-built per call site" --- but `executeObjectMove.ts:311-319` hand-builds a `transferMembership` literal whenever `compileMutationSteps` is absent, which is every object-lifecycle route. Either the code moves to the claim or the claim is wrong and should be narrowed. Resolving this is what makes MS-5 tractable. | 3a | Open |
| MS-3 | **What is the right directory shape?** Today `positions/membership/` and `positions/manipulation/membership/` split by nothing stateable, cross-import both ways, and put `buildObjectMoveOp` in one while `executeObjectMove` sits in the other. Options: merge into one; or keep two with a **stated** boundary that the file placement then has to obey. Resolved 2026-09-06: consolidate everything into `positions/manipulation/membership/` --- the standalone `positions/membership/` directory goes away, its contents move into the `manipulation/` tree, no stated-boundary option was taken. **Shipped 2026-09-07** --- see 2b's own Done note for the merge mechanics (the `types.ts` collision, cross-import fixes, and the 20-file external blast radius). | 2b | Resolved 2026-09-06, shipped 2026-09-07 |
| MS-4 | **Should live markers (`BD-*`, `RD-*`, `LP4a`) get the same delete-in-place treatment as dangling ones?** Resolved 2026-09-06 for the dangling half: every occurrence of every dangling `PB-*` letter and `LP4h` had its rule already stated in self-contained prose, so option (c) (delete the marker, keep the prose) applied uniformly --- no case needed (a) inline-the-rule or (b) link-to-contract. Resolved 2026-09-06 for the live half: no, live markers do **not** get swept now. They stay in place as references and are retired individually, one at a time, when the plan each one points to completes --- the same lifecycle every other live marker in the codebase already follows. No batch sweep is owed in Phase 4. | 4 | Resolved 2026-09-06 |
| MS-5 | **Does `MembershipDiff` stay room-typed?** It is `EphemeraRoomId`-shaped while the kernel below it is host-general, so `applyCharacterRoomMembership` narrows host->room and re-widens room->host in two places within thirty lines purely to reconcile them. Either the character route genuinely owes a room-only guarantee (in which case say where it is enforced) or the type should widen. Resolved 2026-09-06: the type widens --- `MembershipDiff` becomes host-general (matching the kernel it feeds), and the narrow-host->room / re-widen-room->host pair in `applyCharacterRoomMembership` is removed rather than justified. | 3b | Resolved 2026-09-06 |
| MS-6 | **Do `orchestrateCharacterNavigate` / `orchestrateCharacterDisconnect` / `orchestrateObjectMove` converge?** All three are build-op -> compile -> declare bundle -> present; the only real difference is navigate's header slot. The disconnect/navigate split is admittedly a guard clause. Counter-argument on the record: the sibling `buildObjectMoveOp` doc comment argues that merging two disjoint bodies under one name is worse than two siblings --- **that argument may apply here too**, and this row should test it rather than assume convergence is the goal. Resolved 2026-09-06: yes, converge. All three become one `orchestrateComponentMove`, which checks whether the component being moved is a Character and generates the header (navigate's slot) only in that case --- the `buildObjectMoveOp` counter-argument was weighed and doesn't reach here, since the header check is one conditional inside a shared body, not two disjoint bodies forced under a shared name. | 3c | Resolved 2026-09-06 |
| MS-7 | **What does a doc comment in this stack owe?** Needed as a *standard* before Phase 4, or the rewrite is taste. Candidate: a comment states present behavior and the constraint that forces it; supersession history, retired predecessor filenames, and "widened again" narration go to git or to a durable doc. Open sub-question: `kernelStep.ts`'s long comments contain real, load-bearing design reasoning (the expression-problem escalation trigger, the Immer provenance rule) that should **not** be lost --- so this row is partly "which durable doc receives it," not only "what gets cut." | 4 | Open |
| MS-8 | **Are `executeObjectMove` and `executeMembershipTransfer` one function?** The latter is documented as *"the no-carry-closure sibling of `executeObjectMove`"* --- **carry closure is the stated reason they are two**, and 1d retires it. **Counter-argument that was on the record, and confirmed real:** `boundaryEdgeOutcomes` stays live for the peer kinds and is genuinely object-only, so "no carry closure" was not the only apparent difference --- `executeObjectMove` refuses a move (`ok: false`) when a boundary edge classifies `'defer'`; `executeMembershipTransfer` unconditionally dissolves every touching chain with no defer concept; `executeObjectMove` requires two concrete hosts, `executeMembershipTransfer` takes a nullable target and diffs against possibly-many current hosts; `executeObjectMove` is Object-only, `executeMembershipTransfer` is Object-or-Character. **First pass (2026-09-06 conversation) read these as separate domains and recommended keeping two functions.** Corrected on push-back, same conversation: none of the four are structural domain boundaries --- they are unbuilt corners of one general "move a component" operation (entity × fromHosts 0..N × toHost 0-or-1 × per-touching-edge classify), each of today's two functions covering a different incomplete slice of that space (one-to-one-transfer-with-defer vs. zero/many-to-nullable-without-defer), not two different concepts. **Resolved 2026-09-06: yes, unify** --- one function, one generalized data shape (`fromHostIds: 0..N`, `toHostId: 0-or-1`, entity Object-or-Character), **preserving today's behavior exactly**: defer is honored only when there is a real destination (a transfer), a departure (`toHostId` null) still unconditionally dissolves every touching chain --- stated as a **named, tracked gap**, not re-derived as if newly decided, and not silently generalized to also block departures. The Synthesize-executor machinery `executeObjectMove` runs solely to re-derive what's now always a singleton (`createExpansionEnvironment`/`runExecutor`/`settledGroups`/`groupIdByObject`) has no remaining justification and is dropped in favor of calling `classifyInteractionUnderTransfer`/`boundaryEdgeOutcomes` directly, the way `executeMembershipTransfer` already calls `findRelationalChainsTouching` directly --- **not a re-run of CD3**, this is 1d's downstream consequence, not a rebuild of what CD3 already decided. `PositionKernelMovedSet`'s `closure`/`entity` union and `carriedCount` (always 1) are folded in as part of the same slice, since both were carry-closure-shaped machinery this unification also obsoletes. **Deferred, on the record, not this row's job:** whether/how defer should apply to a departure with no destination --- see MS-11. **Shipped 2026-09-07** as one function with a `honorDefer` mode flag selecting between the two dissolve mechanisms, rather than one merged mechanism --- see 2a's own Done note for the corrected reading of "one generalized data shape" found while implementing. | 2a | Resolved 2026-09-06, shipped 2026-09-07 |
| MS-11 | **Should defer apply to a departure with no destination (`toHostId: null`) --- can a component mid-relation refuse to leave with nowhere to go?** Named explicitly rather than left implicit when MS-8 unified `executeObjectMove`/`executeMembershipTransfer`, so the unification's preserved "departures unconditionally dissolve" behavior reads as a tracked gap, not a re-affirmed design decision. **Deliberately not answered here:** per 2026-09-06 conversation, this is moot today regardless of answer --- a character can never be a relational-edge endpoint (`executeMembershipTransfer`'s own standing comment), so no departure can hit a boundary edge in practice yet. It becomes live, and answerable, only when peer relationships targeting characters are built (e.g. "tied up" as a character-endpoint relation) --- that capability doesn't exist yet, and this row is deferred to whichever future row adds it, not to MS-8's unification or to this plan's own remaining phases. | (none yet --- blocked on an unbuilt capability, not on this plan's phases) | Deferred |
| MS-9 | **Should the closed relation-kind vocabulary (`Under`, `Against`) become data instead of hardcoded literals?** Resolved 2026-09-06: yes, but as a **halfway position**, not a wire-format change --- see 1e for the shipped shape. **Site-count correction (found while implementing 1e, before touching code):** this row's original text said "at least four places"; the accurate count is four *persistence-lane* sites orbiting `HostRelationalEdgeKind` itself --- its own union and `HOST_RELATIONAL_EDGE_KINDS` set (`ephemeraMeta.ts`), and two more duplicate copies of that same set (`positions/ludicGraph/baseClasses.ts`, `positions/publishedEvents.ts` --- this third copy is the one the original count missed) --- plus `classifyInteractionUnderTransfer`'s switch (`interactionUnderTransfer.ts`). Three **more** lanes hand-spell the same pair independently and are confirmed **out of scope**, each for the same reason (their own comments say they must not widen to match the persistence-lane type): `HostRelationalEdgeKindPublished` (`dataSource/actions/publishedEvents.ts`), `PeerRelationalEdgeKind` plus the inline pair in `dataSource/actions/baseClasses.ts`, and the inline `isPeerKind` check in `synthesize/expandSameHost.ts`. 1e ships only the four persistence-lane sites. `ephemeraMeta.ts` gains one `CLOSED_RELATION_KINDS` `as const` array plus a derived `ClosedRelationKind` type and `isClosedRelationKind` typeguard; the existing hand-typed sets spread from it instead of re-listing the literals, following the codebase's existing `Exclude<HostRelationalEdgeKind, ...>` narrowing pattern rather than replacing it. `interactionUnderTransfer.ts` keeps a local `CLOSED_RELATION_BEHAVIOR: Record<ClosedRelationKind, { onSubjectMove; onTargetMove }>` table, so `classifyInteractionUnderTransfer` becomes a lookup for closed kinds instead of switch arms, and gains a compile error if the upstream array grows without a matching local row here (today's switch-exhaustiveness, relocated rather than lost). **Considered and rejected:** folding `Under`/`Against` into one wrapper kind (`kind: 'ClosedLabel'`, `relationLabel` typed off the array), mirroring `Custom`'s shape --- `HostRelationalEdgeKind` is a wire-level discriminant consumed well outside this classifier (ingress parsing, `publishedEvents.ts`, `compileRelationalFromSkeleton.ts`, `findRelationalChain.ts`, `buildCrossingLegs.ts`), so reshaping it would be a cross-package migration, not a local refactor. **Naming implications deferred**, not resolved here: whether `ClosedRelationKind`/`isClosedRelationKind` naming should reach further into the stack is this plan's own Phase 2 (renames) and Phase 4 (comment register) territory. **Shipped in 1e, 2026-09-06.** | 1e | Resolved 2026-09-06, shipped 2026-09-06 |
| MS-10 | **Beyond `Under`/`Against` (MS-9), is any other hardcoded per-kind dispatch in the codebase a generalization gap?** Asked directly after 1e shipped, as a survey: grep every remaining `'Under'`/`'Against'` literal in non-test code. Resolved 2026-09-06: one real gap, `publishObjectManipulationPresentation.ts`'s narration verb selection (an if/else chain over `'On'`/`'Under'`/`'Against'`/`'Custom'`) --- shipped as 1f. Two look-alikes were checked and are **not** gaps: `synthesize/detectRelationalCycle.ts`/`filterLegalRelationalCandidates.ts` scope to `'On'`/`'Under'` for cycle detection, which is real physics unique to `Under` (no analogous chain semantics for `Against`), not a closed-vocabulary membership check; `normalizeRelationSpan.ts`'s phrase map already types off `PeerRelationalEdgeKind` (the ingress lane's own derived type) rather than a re-spelled literal. **Design, given directly by the user:** hosting/containment kinds (`On`/`In`/`PartOf`) do **not** get a behavior-data table the way `Under`/`Against` did --- their mechanism (AB-54's shard-hosting) stays hardcoded wherever it already was. What they get instead is a shared **vocabulary** typeguard (`HOSTING_RELATION_KINDS`/`HostingRelationKind`/`isHostingRelationKind`, mirroring `CLOSED_RELATION_KINDS`'s shape), so a consumer that needs to ask "is this a hosting kind?" (narration dispatch) has one answer instead of re-spelling the trio. A caller that needs both families checks hosting first (its own local, necessarily-partial table --- only `On` has established behavior/copy today) then closed second (its own local, exhaustive table) --- **two typeguard-gated local structures, not one combined table across both families**, since hosting and peer kinds don't share a behavior shape (one is "throw," or "no copy yet"; the other is a full lookup). | 1f | Resolved 2026-09-06, shipped 2026-09-06 |

## Verification

Per slice, from `lambda/ephemera`:

```bash
npm run test -- --watchAll=false dataSource/positions/

# Full suite before marking a phase done --- integration tests are NOT covered by tsc,
# and this plan is mostly renames and deletions, which is what breaks them.
npm run test -- --watchAll=false

npx tsc --noEmit
```

After any slice that removes or requires a shared field (1a especially), once:

```bash
npm run test -- --clearCache
```

Grep checks, from the repo root:

```bash
# Phase 1a: narratedInline should be gone in its entirety, not half-deleted.
grep -rn "narratedInline\|narrationHandledInline" --include="*.ts" lambda/ packages/ | grep -v node_modules

# Phase 1c / MS-4: no plan-ID marker in code should lack a referent in taskPlanning/.
# For each marker the first command reports, the second must return a file.
grep -rhoE "\b(PB-[A-Z0-9]|LP4[a-z]|RD-[0-9]+|BD-[0-9]+[a-z]?)\b" \
  --include="*.ts" lambda/ephemera/dataSource/positions | sort -u
grep -rl "<marker>" taskPlanning/

# Phase 1d / CD3: `carry` should be gone from the outcome union and from every consumer.
# `classifyInteractionUnderTransfer` and `boundaryEdgeOutcomes` both STAY --- they serve the
# peer kinds. It is the `carry` branch and the BFS that go, not the module.
grep -rn "'carry'\|computeCarryClosure\|CarryClosure\|carriedCount" --include="*.ts" \
  lambda/ephemera | grep -v node_modules

# Phase 2a / MS-8: the object-move path should no longer run the Synthesize executor to
# re-derive a set that can only be a singleton. Expect these to survive only in the
# relational/tie routes, which genuinely need Expansion.
grep -rn "runExecutor\|createExpansionEnvironment\|settledGroups\|groupIdByObject" \
  --include="*.ts" lambda/ephemera | grep -v node_modules | grep -v "\.test\.ts"

# Phase 2a: character membership must not import from a module named for object moves.
grep -rn "membership/executeObjectMove" --include="*.ts" lambda/ephemera | grep -v node_modules

# Phase 3a / MS-2: transferMembership steps are constructed only inside the compiler.
# Any hit outside it is a call site hand-rolling its own plan.
grep -rn "kind: 'transferMembership'" --include="*.ts" lambda/ephemera \
  | grep -v node_modules | grep -v "\.test\.ts" \
  | grep -v "manipulation/kernel/compile/"

# Standing invariant from manipulation/AGENT.implementation.md --- must stay empty.
grep -rn "getMembershipContainers" lambda/ephemera/dataSource/positions/manipulation/kernel/
```

**Anchor pass:** every rename in Phase 2 invalidates paths in `manipulation/AGENT.implementation.md`'s code map and in this file. Cross-file markdown anchors fail **silently** --- re-resolve them as an explicit step in Phase 5, and prefer copying an existing link to deriving a slug.

## Progress

| Phase | Status |
| --- | --- |
| 1. Subtract known-dead | 1a/1b/1c/1d/1e/1f done (2026-09-06). Phase 1 complete |
| 2. Name for what they are | 2a/2b/2c/2d done (2026-09-07). Phase 2 complete |
| 3. Repair responsibilities | Not started (gated on MS-1/2/3/5/6) |
| 4. Comment register | Not started (gated on MS-7) |
| 5. Durable docs and disposal | Not started |
