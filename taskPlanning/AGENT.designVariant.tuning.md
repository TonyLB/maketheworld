# Design-variant tuning

**Durability: Keep** --- an instrument, not a task plan. Disposes together with [`AGENT.designVariant.performance.md`](AGENT.designVariant.performance.md).

Candidate and recently-implemented changes to [`AGENT.designVariant.md`](AGENT.designVariant.md), motivated by the patterns accumulating in the [performance tally](AGENT.designVariant.performance.md).

**What this file is for, beyond holding proposals:** a live proposal biases attention while observations are being recorded. *Would this improvement have made a difference here? Would it have been the wrong instruction?* That question turns each observation into a test case run against the proposal set --- **which is the only way an implemented rule can lose.** Without it a rule in force accumulates nothing but confirming evidence, which is how [`AGENT.designVariant.md`](AGENT.designVariant.md)'s confidence table filled with clauses nobody has re-examined.

## Rules

**Cap: 10 rows, shared** between candidates and implemented-under-evaluation. Implementing a proposal does **not** free its slot. That is deliberate: shared pressure forces an evaluation to conclude rather than linger, and adding an eleventh row means evicting something. **The cost is real and is being watched** --- several long evaluations could squeeze the candidate pipeline, which is a signal to revisit the cap, not to quietly exceed it.

**Restate, never link --- at row grain.** A proposal names its motivating pattern **in its own text**, in a clause. Neither file may cite a **row** in the other: no `TP-n` in the tally, no tally line or pattern anchor here. File-level pointers between the two are fine and expected; it is **row-level** coupling that would make eviction expensive, and cheap eviction is the entire mechanism.

**Every proposal carries a counterfactual test, written before implementation:** what observation would show it working, and what observation would show it was the wrong instruction. **A proposal that cannot state its own falsifier is not ready to implement.**

**Three statuses, and eviction costs something different for each.** *Candidate* --- proposed, nothing built; evicting it is free. *[Prototype](AGENT.designVariant.md#graduation-tiers-when-a-decision-licenses-code)* --- built somewhere bounded to get evidence, with **no clause yet in `AGENT.designVariant.md`**; carries a dependency tag naming its rollback set, per the variant's own tier rules. *Implemented* --- a clause is live in `AGENT.designVariant.md`.

**Evicting an *implemented* row obliges one edit** to [that file's confidence table](AGENT.designVariant.md#where-each-clause-came-from), recording what the evaluation actually showed. Without it a rule ages out of evaluation while staying live in the instructions with no record of how it did --- the state that table is in today. **Evicting a *prototype* row obliges a disposition instead:** promote it to a clause, or unwind it and say so. A prototype that simply ages out leaves a mechanism running that nobody ever graded.

**Review both caps whenever a proposal is evicted as settled.** That is the moment the system has demonstrably improved, so it is the honest moment to act on the expected trajectory: **tally cap down** as noise falls, **tuning cap up** as quick wins deplete and proposals get more numerous and more lightly held.

**IDs are for conversation only.** Nothing outside these two files may cite a `TP-n`. Monotonic, not reused.

## Proposals

| # | Proposal | Motivating pattern | Status | Counterfactual test |
| --- | --- | --- | --- | --- |
| **TP-1** | Make **dissolution** a first-class row disposition beside answer / split / graduate, and have working a row begin by asking *what would make this question not need answering?* | Five of the largest artifacts in one plan --- a 10 KB decision row among them --- closed by the question dissolving rather than being answered, and **every instance was user-initiated**. The variant's vocabulary can express answer, split, graduate and defer; three of those grow the document and none can retire a question unanswered | **Candidate** | **Works if** a row is dissolved agent-first, with no user prompting. **Wrong instruction if** rows get closed as *dissolved* and later have to be reopened --- i.e. dissolution becomes an escape hatch for hard questions rather than a diagnosis of malformed ones |
| **TP-2** | A decision row, phase or step is not complete until it states **what would falsify it**, in one sentence, at the moment it is written | The same rule was independently derived three times at three grains inside one initiative (a brief should say what it would falsify before it is scheduled; restate a supplied mechanism mechanically; verify against the claim rather than remembered wording) and never generalised, while every recorded error class was **introduced at write time and caught only by the user or by reading code**. Periodic review cannot stabilise a system whose disturbance outruns it | **Candidate --- evaluated 2026-08-24 against the seven accumulated grain-mismatch observations, and the result is a split rather than a score.** **It would have caught two:** a gate that outlived its own removed precondition, and a row that inherited *blocked* from a whole provisional tier --- in both, the grain was **never stated anywhere**, and writing *what would falsify this* forces the writer to name the precondition or the clause. **It would have caught none of the other four**, and they fail identically: the disambiguating sentence **was already written, adjacent, in the same cell or paragraph**, and was read past. A gate whose own justification named two premises and nothing else was released twice by people reading the header instead; a finding that *named the conflation in its own paragraph* was still carried a field too wide; a clause quantifying **crossings** was read as bounding interior degree while sitting in a section costing ports per boundary. **Adding a sentence at write time cannot help a case whose sentence already exists.** The seventh is out of scope as worded --- it was a cited precedent, not a decision row, phase or step | **Works if** `stale-premise` and `framing-inherited` rates fall. **Wrong instruction if** falsifier sentences become ritual --- present, vacuous, never consulted --- which shows as rows carrying a falsifier that no later correction ever cites. **Sharpened 2026-08-24 by the evaluation above: it is also the wrong instruction if it is aimed at the grain-mismatch pattern**, which it only reaches where the grain was never written down. The read-side half is TP-4's, and the two should not both be credited for the same observation |
| **TP-3** | **A capped pool whose eviction is driven by the desire to add** as a cheap proxy for self-cleanup urgency, which the design system currently supplies none of | 46 open rows against 15 settled in one plan after 16 days, and a family that grew 84 KB to 2.07 MB in the same window. Every closure path in the variant can mint rows; none can retire a question unanswered, so opening costs one sentence and closing costs an argument | **[Prototype](AGENT.designVariant.md#graduation-tiers-when-a-decision-licenses-code), 2026-08-22.** Built because running it is the only affordable way to get evidence. **Dependency tag --- the rollback set is exactly these two files:** the cap mechanism governs this file and the tally, and **no clause in [`AGENT.designVariant.md`](AGENT.designVariant.md) implements a capped pool.** Promoting it to a rule there is a separate later step, not something this row has done | **Works if** evictions actually happen and both files sit at cap without special effort. **Wrong instruction if** a cap gets quietly raised rather than paid, or if pressure evicts real signal while noise survives --- i.e. if what leaves is not the least valuable thing |
| **TP-4** | **When a row cites another row, clause, gate or finding as its warrant, pull up the sentence that justifies it --- from the source, not from memory --- and read it against the claim before writing the citation.** Not *check the grain*; **fetch the sentence.** The actuator is mechanical and its whole effect is to put warrant and claim on screen together, where a mismatch is visible without anyone having to be looking for one. **The quote is transient by default and is *not* pasted into the document** --- ~~*paste the sentence inline*~~ **revised 2026-08-24, on the objection that pasting creates a second copy that the source can outrun.** It would: a quoted warrant is a denormalization, it goes stale silently when the cited row is amended, and a stale quote is worse than none because it reads as authoritative --- the same fact-stored-twice shape that already costs this codebase a drift-sweep lambda. **The persistence was never what caught anything; the fetch is.** **One exception, and it is the [PQ-1](lambda/ephemera/dataSource/positions/AGENT.abstractionLayers.proposals.planning.md#the-port-record-split-complementary-with-the-interior-authoritative-settled-2026-08-06) bar applied to this proposal itself --- *a denormalization earns a place by what it saves*: quote inline only when the fetch reveals a mismatch**, where the quote becomes evidence inside the correction rather than decoration on a citation. **That makes the surviving quotes exactly the caught ones**, which is self-limiting and, usefully, is also this proposal's hit counter | **The most frequent failure recorded in the tally, and the evaluation of the write-time proposal above is what isolated it.** Seven observations of a restriction, citation or summary carried at a coarser grain than its own justification supports; in **four** of them the justifying sentence was already written and adjacent, and was read past anyway --- a gate released twice against a justification naming two premises and nothing else, a finding carried a field wider than the caveat in its own paragraph, a clause about **crossings** read as bounding interior degree, and a two-row companion table compressed into a phrase that reversed the precedent it was cited for. **Those four are unreachable by any write-time discipline, because the writing was already correct.** Note the risk this proposal runs against the pattern it targets: the pattern sits at seven hits and **zero** misses, and zero is absence of counting rather than a clean record | **Candidate** | **Works if** a fetched sentence visibly fails to support the claim being written, and is corrected **before** anyone acts on it --- **the observable is a tally entry logged at the moment of citing, plus the quote surviving inline in the correction.** ~~*the observable is a correction that cites the quote*~~ **restated 2026-08-24 with the transience revision: a check that leaves no artifact when it passes needs its evidence to come from the tally rather than from the document**, and that is a real weakening --- silent successes are uncountable here, so this row's hit count will understate it and its miss count is the number to watch. **Wrong instruction if** the fetch degrades into recall --- reproducing the original failure at higher cost, and the likelier of the two failure modes; or if mismatches keep landing in rows whose author *did* fetch, which would say the sentence was on screen and still not read, and would move the fix off this axis entirely. **The pasting variant was evicted before implementation rather than tested**, so nothing here grades it. **Do not credit it for observations where the grain was never stated** --- those belong to the write-time proposal, and double-counting one observation across both is how a cap-limited file loses the ability to tell them apart |
| **TP-6** | **A row that answers what structure or schema carries a representation is not graded *answered* on the strength of naming one granularity level.** Where the same fiction could reasonably be represented at more than one level, the row must say whether the schema handles more than one, and if so how the levels interplay --- *"coarse as A, detailed as B, and here is how A and B interplay if both are represented"* --- not merely which single level was argued | **The edge-abstractions plan's PH-R was booked 2026-08-24 as a cross-plan audit, against an assumed stable target: one correct node/edge-granularity framework to check three sibling plans for blindness against.** Conversation on 2026-08-26 found that assumption false --- EA-10's displacement verdict and CD6's kind-agreement grading were each right for what was known when written, and CD6 needed a **re-grade**, not a compliance check, once PR-15 shipped. **There is no fixed target for a sweep to check; there are only prior verdicts, scoped to their own moment.** What the plan's rows show instead is uneven practice: EA-7's own "second pass" amendment already states multiple granularity levels and their interplay unprompted, while other rows in the same file settle for one level with nothing requiring otherwise | **Candidate** | **Works if**, applied to this plan's own remaining open rows (EA-1, EA-3, EA-5, EA-6, EA-9), it surfaces at least one row that silently assumed a single granularity level where the fiction supports more, and the fix is cheap once named. **Wrong instruction if** every remaining row already implicitly satisfies it once checked (ceremony, not a gap), or if the multi-level-interplay demand has no answerable form for a real case in this corpus (over-asking rather than a bar) |
| **TP-5** | **A grading that compares candidates records its costs in a table with a column per candidate, not in prose.** One row per demand or cost, one cell per candidate, **every cell filled** --- including the cells that say *this one pays it too* and *not applicable*. **The actuator is the shape of the document rather than the diligence of its author, which is the whole of the claim:** a cost levelled at one candidate and never priced against the other is, in a table, **a blank cell that anyone can see**; in prose it is a paragraph that was never written, and an unwritten paragraph looks exactly like a finished argument. **It is deliberately not *remember to ask whether the incumbent pays this too*.** That form is a rule, it depends on recall at the one moment recall is under load, and the habit migrates around it --- the same objection that made a vocabulary split preferable to a naming convention elsewhere in this project: **make the error ungrammatical rather than forbidden.** **Scope: comparative gradings only** --- two or more candidates against a shared set of demands. It says nothing about prose in single-candidate analysis, and **it is not a general instruction to prefer tables**, which this corpus already over-supplies | **One observation, and it is thin --- but it arrived with an internal control that no other row here has.** Grading a proposed representation against the incumbent, the question *does the incumbent pay this cost too?* was applied to three findings and skipped on a fourth; the skip produced a charge stated against one side only, which the user corrected within the hour. **All four were in one document, by one author, in one sitting**, so nothing about attention, fatigue or care separates the three that ran from the one that did not. **What does separate them is the format of the earlier run of the same case:** the 2026-08-05 original graded two framings in a **two-column table** against one demand and levelled no one-sided charge; the 2026-08-26 run dropped to prose and did. **Register the weakness plainly: n=1, and the control is suggestive rather than clean**, since the two runs also differ in age, author-knowledge and what was being asked. **A second instance is what this row is waiting for**, and a second instance landing in a *tabulated* grading would be the more informative one | **Candidate** | **Works if** a cell that would not have been written as prose gets filled and changes the weight of a verdict --- **the observable is a *same* or *pays this too* cell in a shipped grading, in a row whose cost was first noticed as one-sided.** **Wrong instruction if** cells get filled with restatement or *n/a* to complete the shape --- **the likelier failure and worse than prose, because a fully-populated table reads as a finished comparison** and buys the appearance of the check without the check; or if one-sided charges keep landing in gradings that **were** tabulated, which would say the shape never carried it. **A third failure mode is specific to this project and should be watched first:** a table presumes its row set is known before the run, and the grading that motivated this row had three demands that only became separable **because** a prior edit split them out of a verdict --- **a grading that discovers a demand mid-run is distorted by having to name its rows up front**, and that cost is not visible in the successful case. **And the actuator feeds a failure this project already logs:** tables here are single very long lines whose edits hide each other, so widening one is not free. **Do not credit it for gradings that were symmetric anyway** |
| **TP-7** | **In design-variant plans, a decision that has outgrown a table cell moves to its own `###` section, and its table row demotes to an index entry** --- ID, question, disposition in one clause, link. **The sub-headings are a closed set, and that closure *is* the size discipline:** content fitting no heading has nowhere to go. Provisional set, itself graded by the first conversion: *Question*, *Constrained by* (with its arrival kind), *Candidates*, *Grading* (per-candidate columns), *What graduating this requires*, *Status*. **A freeform set hands the readability win back as bloat** --- a 16 KB cell is visibly grotesque, whereas 16 KB under five tidy headings reads as thorough, and the table's accidental contribution was that **size was self-punishing**. **Two existing clauses become enforceable rather than merely required:** *disposition in the Status cell's first clause* holds automatically when the index row's status **is** one clause; and a cited row acquires a sub-anchor, so a citation can land on the sentence it means. **Scope: rows that have outgrown the cell.** Short rows stay rows --- this is a **third exit** beside graduate and split, not a replacement for the table, whose one irreplaceable job is the twenty-row status scan | **Three measurements, 2026-08-27.** **(1) Cross-references have no target.** 406 row-to-row citations across the three live design plans resolve to one of two anchors per file --- 98 and 56 in edge abstractions, 49 and 102 in presence, 68 and 33 in abstraction layers --- because **a markdown table row has no anchor**, so citing a row can only mean citing the table. The reader lands on the header and scans up to 9 KB for the sentence meant. **(2) The size clause's two exits both miss.** It holds that an over-long row is due for graduation or is two rows; five rows in edge abstractions are **open** (nothing may graduate) and ask **one** question each (splitting would fabricate a second), and sit at 8.3--9.3 KB. Live maxima elsewhere: 15.9 KB and 11.9 KB, both in presence. **A rule whose escape hatches both miss the actual case is an incomplete taxonomy, not a discipline failure.** **(3) Convergence.** The tabulated-grading proposal in this file argues that a blank cell is visible where an unwritten paragraph is not; **this is that actuator one grain up** --- a missing *What graduating this requires* heading is visible, a missing clause inside a 9 KB cell is not. That proposal also records, against itself, that widening a table here is not free because the lines are long enough to hide each other's edits: **it wants a column it cannot afford**, which is the same container failing from the opposite side | **Prototype, converted 2026-08-27 --- eight decisions, not the five estimated. The falsifier did not fire, and the measurement came out sharper than the test asked for.** The eight rows were **53,559 bytes** as table rows and **61,482** as sections, and **+7,919 of that +7,923 is content the rubric demanded and the rows did not have** --- **container overhead across all eight is +4 bytes.** Sections are byte-neutral; every byte of growth is *What graduating this requires*, *Candidates* and *Grading* that a table cell had no slot for. **The works-if clause was met on the first pass:** two rows state **no graduation requirement at all** and two more state it partially, and **no row in the plan records how any constraint arrived** --- derived, minimal primitive, placeholder --- a gap invisible in prose and immediate as an empty heading. **99 citations moved from the table anchor to `#ea-N`.** **Headings are the bare ID**, deliberately: this plan retitled two rows in one week and a title-bearing anchor would have broken silently both times. **The set closed at seven, not six.** *Superseded, retained* was added because four rows fenced their own history with the prose instruction *"read the amendment at the end of this cell; everything between here and it is derivation"* --- **an instruction that exists only because the container cannot express it, and that the conversion made false rather than merely redundant**; all three live ones are struck. **One risk this proposal did not name and the conversion produced:** disposition now lives in the index cell **and** the section, so the two can diverge silently --- booked in the converted plan's Verification, and it is the live thing to watch. **The counterfactual cell is left exactly as written**, 43.8 KB five-row baseline and all, because a test edited after its own run stops being a test. **The shape and rollback trigger below were named before the conversion rather than reconstructed after**, per the variant's own tier obligations: convert **edge abstractions only** --- smallest live design plan, five rows sitting in the gap, siblings left tabular as a control --- **dependency tag: that one file**. **Tag widened 2026-08-27 by the user** --- *"the results so far are encouraging enough to have earned that consistency sweep"* --- **to the inbound citations, and the sweep is the strongest evidence in this row.** 89 cross-file `[EA-N]` citations in six files moved onto `#ea-N`, all 142 inbound anchored citations resolve, and **the corpus shrank by 2,496 bytes**, the ID being shorter than the section slug it replaced: **once citations follow the conversion the container is not merely byte-neutral but byte-negative.** **It also caught two citations aimed at the wrong thing** --- an `EA-11` still pointing at the open table it had graduated out of, and an `AB-19` pointing at the edge plan, which has never held an AB row. **Both resolved cleanly beforehand**, because every row citation landed on the same valid heading; **anchor resolution was a vacuous check for row citations and is only now load-bearing.** That is a benefit this proposal did not claim --- it argued the fetch lands on the sentence, not that wrong destinations become *visible*. **Rollback now costs six files rather than one**, which is the price of the widening and is recorded rather than discounted. **Hold graduation constant across the conversion**, or the two mechanisms confound and a plan that shrank because rows graduated will read as evidence for sections. **It also disposes DV-4a**, which asks whether the organ-order split is a consequence of row size rather than of the variant --- a plan whose long rows have left the table is exactly the case that question was waiting for | **Works if** the conversion surfaces a structural gap that prose hid --- an empty *Grading* or *What graduating this requires* under a row that read as complete --- **and** at least one citation is rewritten to a sub-anchor whose fetch lands on the sentence rather than the table header. **Wrong instruction if total bytes rise without new answers:** the five rows are **43.8 KB today**, and if the same five questions occupy materially more after conversion with no verdict advanced, the container licensed prose rather than organising it --- **this is the load-bearing falsifier and the likelier failure**, since the change removes one of the few places where length is currently self-punishing in a corpus already at 2.2 MB across three plans. **Also wrong if** the sub-heading set must be extended per row --- an open set is not a cap, and the closure was the whole discipline; or if the index row's status clause starts re-accreting layers, which would say the row grew back where it was demoted from and the split bought a filing convention rather than a compaction. **Do not credit it for rows that were short anyway**, nor for citations that were already precise |
